---
phase: "04.3"
plan: "02"
subsystem: "localization"
tags: [i18n, navigation, auth, chat, PWA]
dependency_graph:
  requires: ["04.3-01"]
  provides: ["nav-i18n", "auth-i18n", "chat-i18n", "pwa-i18n"]
  affects: ["app/kid/login", "app/kid/chat", "components/chat", "components/kid", "app/manifest"]
tech_stack:
  added: []
  patterns: ["useT hook", "t() call replacement", "translation key namespacing"]
key_files:
  created: []
  modified:
    - app/kid/login/page.tsx
    - app/kid/chat/page.tsx
    - app/manifest.ts
    - components/chat/ChatThread.tsx
    - components/chat/SendBox.tsx
    - components/kid/CelebrationOverlay.tsx
    - components/kid/KidChatFAB.tsx
    - components/kid/ParentPreviewBanner.tsx
    - public/locales/en.json
    - public/locales/ru.json
decisions:
  - "CelebrationOverlay uses t('celebration.badgeEarned') for both the aria-label and the visible heading — single key serves both roles"
  - "KidChatPage senderName default changed from 'Ребёнок' (hardcoded Russian) to empty string — display_name from DB is used or empty string shown (not a hardcoded fallback)"
  - "manifest.ts updated to use FamilyCoins (English canonical) instead of KidsCoins — server-side file cannot use hooks"
  - "chat.* keys extended with camera/gallery/caption/sending/cancel/noMessages/familyChat — all new keys added to both en.json and ru.json"
metrics:
  duration_seconds: 433
  tasks_completed: 2
  files_modified: 10
  completed_date: "2026-05-16"
---

# Phase 04.3 Plan 02: Navigation, Auth, and Chat i18n Summary

**One-liner:** Localized all nav components (NavBar/KidNav/ParentNav already done), auth pages (kid/login 19 strings), chat components (ChatThread/SendBox), and small PWA components (CelebrationOverlay, ParentPreviewBanner, KidChatFAB, manifest) using useT() hook.

---

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Localize NavBar, KidNav, ParentNav + LanguageToggle | a9ab437 | components/NavBar.tsx, components/kid/KidNav.tsx, components/parent/ParentNav.tsx |
| 2 | Localize InstallPrompt, OfflineBanner, auth pages, small components | 4810abe | 10 files (see key_files above) |

---

## What Was Built

**Task 1 (pre-existing, committed in a9ab437):**
- NavBar: all labels use `t('nav.*')`, LanguageToggle added
- KidNav: TABS array replaced with TAB_DEFS + `t('kidNav.*')` dynamic labels, LanguageToggle added
- ParentNav: NAV_LINK_DEFS replaced with `t('nav.*')` calls

**Task 2 (committed in 4810abe):**
- `app/kid/login/page.tsx`: All 3 steps (family code, name picker, PIN) fully localized with `t('kidLogin.*')`. Error messages in async functions also localized.
- `app/kid/chat/page.tsx`: Title, online status, profile-not-found error all use `t('kidChat.*')`
- `components/chat/ChatThread.tsx`: noMessages, photo caption, cancel/send/sending buttons, sticker/camera/gallery titles all use `t('chat.*')`
- `components/chat/SendBox.tsx`: placeholder and send button use `t('chat.*')`
- `components/kid/ParentPreviewBanner.tsx`: banner text and exit button use `t('parentPreview.*')`
- `components/kid/CelebrationOverlay.tsx`: badge title and tap-to-continue use `t('celebration.*')`
- `components/kid/KidChatFAB.tsx`: aria-label uses `t('kidChat.title')`
- `app/manifest.ts`: name/short_name updated to 'FamilyCoins', description to English (server-side, no hooks)

**Translation keys added (both locales):**
- `kidChat.online`, `kidChat.profileNotFound`, `kidChat.loginAgain`
- `celebration.tapToContinue`
- `parentPreview.exitButton`
- `chat.camera`, `chat.gallery`, `chat.caption`, `chat.sending`, `chat.cancel`, `chat.noMessages`, `chat.familyChat`

---

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing functionality] Added missing translation keys to locale files**
- **Found during:** Task 2
- **Issue:** Components in the plan referenced translation keys (`chat.camera`, `celebration.tapToContinue`, `parentPreview.exitButton`, etc.) that did not exist in either locale file
- **Fix:** Added all missing keys to both `public/locales/en.json` and `public/locales/ru.json` at the end of their respective sections
- **Files modified:** `public/locales/en.json`, `public/locales/ru.json`
- **Commit:** 4810abe

**2. [Rule 1 - Bug] KidChatPage default senderName changed from hardcoded 'Ребёнок' to empty string**
- **Found during:** Task 2
- **Issue:** Default value 'Ребёнок' was a hardcoded Russian string, but senderName is set from `member.display_name` and if the member is found it always has a display_name. No i18n key was needed.
- **Fix:** Changed default to empty string `''` — display_name from DB is used when available
- **Commit:** 4810abe

---

## Verification

- `npx tsc --noEmit` passes with 0 errors
- NavBar renders `t('nav.*')` labels — switches with language toggle
- KidNav renders `t('kidNav.*')` labels — switches with language toggle
- InstallPrompt (done in wip commit): fully localized
- OfflineBanner (done in wip commit): fully localized
- app/kid/login: all 3 steps fully localized
- ChatThread, SendBox: all UI strings use t()
- CelebrationOverlay, ParentPreviewBanner, KidChatFAB: all strings use t()
- app/manifest.ts: English canonical strings (no hooks — server-side)

---

## Self-Check: PASSED

- SUMMARY.md: FOUND at .planning/phases/04.3-localization/04.3-02-SUMMARY.md
- Commit a9ab437 (Task 1): FOUND
- Commit 4810abe (Task 2): FOUND
- TypeScript: 0 errors
