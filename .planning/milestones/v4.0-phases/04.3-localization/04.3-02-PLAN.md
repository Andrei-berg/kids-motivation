---
phase: 04.3-localization
plan: "02"
type: execute
wave: 2
depends_on:
  - "04.3-01"
files_modified:
  - app/layout.tsx
  - components/NavBar.tsx
  - components/kid/KidNav.tsx
  - components/parent/ParentNav.tsx
  - components/InstallPrompt.tsx
  - components/OfflineBanner.tsx
  - app/login/page.tsx
  - app/kid/login/page.tsx
  - app/kid/chat/page.tsx
  - components/chat/ChatThread.tsx
  - components/chat/SendBox.tsx
  - components/kid/ParentPreviewBanner.tsx
  - components/kid/CelebrationOverlay.tsx
  - components/kid/KidChatFAB.tsx
  - app/manifest.ts
autonomous: true
requirements:
  - LOC-01

must_haves:
  truths:
    - "NavBar labels (Wallet, Expenses, etc.) display in the active language"
    - "KidNav labels (My Day, Trophies, Shop, Rating) display in the active language"
    - "InstallPrompt displays all text in the active language"
    - "OfflineBanner displays in the active language"
    - "Login page link and kid login page display in the active language"
    - "LanguageToggle appears in NavBar or KidNav so users can switch language"
  artifacts:
    - path: "components/NavBar.tsx"
      provides: "Localized parent navigation with LanguageToggle"
      contains: "useT"
    - path: "components/kid/KidNav.tsx"
      provides: "Localized kid navigation with LanguageToggle"
      contains: "useT"
    - path: "components/InstallPrompt.tsx"
      provides: "Localized install prompt"
      contains: "useT"
    - path: "components/OfflineBanner.tsx"
      provides: "Localized offline banner"
      contains: "useT"
  key_links:
    - from: "components/NavBar.tsx"
      to: "lib/i18n.tsx"
      via: "import { useT } from '@/lib/i18n'"
      pattern: "useT"
    - from: "components/LanguageToggle.tsx"
      to: "components/NavBar.tsx"
      via: "LanguageToggle rendered inside NavBar"
      pattern: "LanguageToggle"
---

<objective>
Apply i18n to navigation, entry-point components, and auth pages — the highest-visibility parts of the app.

Purpose: Navigation labels appear on every screen. InstallPrompt and OfflineBanner are critical PWA UX. These are the first things users see, so they need to be localized first.

Output: All nav components, install prompt, offline banner, login pages, and small chat/kid components use `t()` instead of hardcoded Russian strings.
</objective>

<execution_context>
@/Users/andrei/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrei/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.3-localization/04.3-01-PLAN.md

@components/NavBar.tsx
@components/kid/KidNav.tsx
@components/parent/ParentNav.tsx
@components/InstallPrompt.tsx
@components/OfflineBanner.tsx
@app/login/page.tsx
@app/kid/login/page.tsx

<interfaces>
From lib/i18n.tsx (created in Plan 01):
```typescript
export function useT(): (key: string, vars?: Record<string, string | number>) => string
export function useLanguage(): { language: 'ru' | 'en', setLanguage: (lang: 'ru' | 'en') => void }
export function LanguageProvider({ children }: { children: React.ReactNode }): JSX.Element
```

From components/LanguageToggle.tsx (created in Plan 01):
```typescript
export function LanguageToggle({ className }: { className?: string }): JSX.Element
// Renders a button showing current language (RU/EN), toggles on click
```

Translation key namespaces (from public/locales/en.json):
- nav.*  — NavBar and ParentNav labels
- kidNav.* — KidNav labels
- install.* — InstallPrompt strings
- offline.banner — OfflineBanner text
- login.kidLoginLabel — login page link text
- kidLogin.* — kid login page strings
- chat.placeholder — SendBox placeholder
- kidChat.title — kid chat page title
- celebration.* — CelebrationOverlay strings
- parentPreview.banner — ParentPreviewBanner text
- manifest.* — manifest.ts strings
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Localize NavBar, KidNav, ParentNav + add LanguageToggle</name>
  <files>components/NavBar.tsx, components/kid/KidNav.tsx, components/parent/ParentNav.tsx</files>
  <action>
Read each file fully before editing. Replace hardcoded Russian/English strings with `t()` calls. Add `useT` import from `@/lib/i18n`. Add `LanguageToggle` import and render it in each nav.

**NavBar.tsx pattern:**
- Add `import { useT } from '@/lib/i18n'` and `import { LanguageToggle } from './LanguageToggle'`
- Call `const t = useT()` inside the component
- Replace labels like `'💰 Кошелёк'` → `'💰 ' + t('nav.wallet')` (preserve emojis as literals, translate text only)
- Replace `'💸 Расходы'` → `'💸 ' + t('nav.expenses')`
- Replace title `'Родительский центр'` → `t('nav.parentCenter')`
- Replace aria-label strings with `t()` equivalents
- Add `<LanguageToggle />` near the settings icon (end of nav bar)

**KidNav.tsx pattern:**
- The nav items array has labels like `'Мой день'`, `'Кошелёк'`, `'Трофеи'`, `'Магазин'`, `'Рейтинг'`
- Change the nav items to derive labels from `t()`: call `useT()` hook, then reference `t('kidNav.myDay')` etc. in the render (not in the static array, since hooks cannot be used outside components)
- Move label generation inside the component: create a computed labels object from `t()`, then use in the nav items rendering
- Add `<LanguageToggle />` in the logout button area or settings area of KidNav
- Replace logout title and aria-labels with `t('kidNav.logout')` and `t('nav.navigation')`

**ParentNav.tsx pattern:**
- Same approach — replace the few Russian strings present with `t()` calls
- Replace `'Родительский центр'` title → `t('nav.parentCenter')`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>NavBar, KidNav, and ParentNav have no hardcoded Russian or English UI strings. LanguageToggle is visible in both parent and kid navigation. TypeScript 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Localize InstallPrompt, OfflineBanner, auth pages, and small components</name>
  <files>components/InstallPrompt.tsx, components/OfflineBanner.tsx, app/login/page.tsx, app/kid/login/page.tsx, app/kid/chat/page.tsx, components/chat/ChatThread.tsx, components/chat/SendBox.tsx, components/kid/ParentPreviewBanner.tsx, components/kid/CelebrationOverlay.tsx, components/kid/KidChatFAB.tsx, app/manifest.ts</files>
  <action>
For each file: read fully, add `import { useT } from '@/lib/i18n'`, call `const t = useT()` in the component, replace all hardcoded Russian strings with `t()` calls.

**InstallPrompt.tsx** (8 Russian strings):
- `'Установить приложение'` → `t('install.title')`
- `'Добавьте FamilyCoins на главный экран'` → `t('install.subtitle')`
- `'Добавить'` → `t('install.addButton')`
- aria-label close → `t('install.closeLabel')`
- `'Установить на iPhone'` → `t('install.iosTitle')`
- `'Нажмите'` → `t('install.iosTap')`
- `'→ «На экран "Домой"»...'` → `t('install.iosInstruct')`

**OfflineBanner.tsx** (1 Russian string):
- `'Нет соединения — данные могут быть устаревшими'` → `t('offline.banner')`

**app/login/page.tsx** (1 Russian string):
- `'Войти как ребёнок (по PIN-коду)'` → `t('login.kidLoginLabel')`

**app/kid/login/page.tsx** (19 Russian strings) — This is a complex page. Read the full file before editing. Replace all Russian strings throughout with `t('kidLogin.*)` keys. Common patterns: login button, PIN placeholder, error messages, child selection, back button.

**app/kid/chat/page.tsx** (8 Russian strings):
- Replace title string → `t('kidChat.title')`
- Other strings with `t('kidChat.*')` or `t('chat.*')` keys

**components/chat/ChatThread.tsx** (7 Russian strings):
- Replace with `t('chat.*')` keys

**components/chat/SendBox.tsx** (2 Russian strings):
- Placeholder → `t('chat.placeholder')`

**components/kid/ParentPreviewBanner.tsx** (2 Russian strings):
- Banner text → `t('parentPreview.banner')`

**components/kid/CelebrationOverlay.tsx** (3 Russian strings):
- Replace with `t('celebration.*')` keys

**components/kid/KidChatFAB.tsx** (1 Russian string):
- Replace with appropriate `t()` key

**app/manifest.ts** (1 Russian string):
- This is a server-side file (no hooks). It exports a static manifest object.
- Change `name` and `description` fields to English values directly (they do not go through i18n since manifest.ts is server-side): `name: 'FamilyCoins'`, `short_name: 'FamilyCoins'`, `description: 'Family motivation app for children'`
- The manifest is always served in one language — use English as the canonical PWA manifest language.

Important: InstallPrompt uses inline styles (not Tailwind) — preserve the existing styling, only change text content.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All files in this plan have 0 hardcoded Russian strings in visible UI text (comments are ok). TypeScript 0 errors. Manifest.ts uses English strings.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- NavBar renders English labels when language is 'en', Russian when 'ru'
- KidNav renders English labels when language is 'en', Russian when 'ru'
- LanguageToggle is visible in NavBar and KidNav
- InstallPrompt text switches with language toggle
- OfflineBanner text switches with language toggle
</verification>

<success_criteria>
Navigation, install prompt, offline banner, and auth pages have no hardcoded Russian or English strings in UI text. LanguageToggle is accessible from both parent and kid views.
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-localization/04.3-02-SUMMARY.md`
</output>
