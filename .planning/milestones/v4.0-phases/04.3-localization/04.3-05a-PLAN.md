---
phase: 04.3-localization
plan: "05a"
type: execute
wave: 2
depends_on:
  - "04.3-01"
files_modified:
  - app/wallboard/page.tsx
  - app/parent/shop/page.tsx
  - app/onboarding/v2/page.tsx
  - app/onboarding/join/page.tsx
  - app/page.tsx
  - components/PotentialWidget.tsx
  - components/ExpenseModal.tsx
  - components/ExpenseCharts.tsx
  - components/ScheduleEditor.tsx
autonomous: true
requirements:
  - LOC-01

must_haves:
  truths:
    - "Wallboard page displays all labels in the active language"
    - "Parent shop page displays all labels in the active language"
    - "Onboarding pages display all labels in the active language"
    - "PotentialWidget, ExpenseModal, ExpenseCharts, ScheduleEditor display labels in the active language"
    - "No hardcoded Russian strings remain in these 9 client files"
  artifacts:
    - path: "app/wallboard/page.tsx"
      provides: "Localized wallboard page"
      contains: "useT"
    - path: "app/onboarding/v2/page.tsx"
      provides: "Localized onboarding wizard"
      contains: "useT"
    - path: "components/PotentialWidget.tsx"
      provides: "Localized potential widget"
      contains: "useT"
    - path: "components/ExpenseModal.tsx"
      provides: "Localized expense modal"
      contains: "useT"
  key_links:
    - from: "app/wallboard/page.tsx"
      to: "lib/i18n.tsx"
      via: "import { useT } from '@/lib/i18n'"
      pattern: "useT"
    - from: "app/onboarding/v2/page.tsx"
      to: "lib/i18n.tsx"
      via: "import { useT } from '@/lib/i18n'"
      pattern: "useT"
---

<objective>
Apply i18n to wallboard, parent shop, onboarding pages, and remaining client-side components (PotentialWidget, ExpenseModal, ExpenseCharts, ScheduleEditor).

Purpose: These are the large remaining client pages not covered by plans 02–04. Wallboard (615 lines) and onboarding (722 + 899 lines) are major user flows that must display in the active language.

Output: 9 client files with 0 hardcoded Russian strings. New translation keys added to both en.json and ru.json where needed.
</objective>

<execution_context>
@/Users/andrei/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrei/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.3-localization/04.3-01-PLAN.md

@app/wallboard/page.tsx
@app/parent/shop/page.tsx
@app/onboarding/v2/page.tsx
@app/onboarding/join/page.tsx
@components/PotentialWidget.tsx
@components/ExpenseModal.tsx
@components/ExpenseCharts.tsx
@components/ScheduleEditor.tsx

<interfaces>
From lib/i18n.tsx (created in Plan 01):
```typescript
export function useT(): (key: string, vars?: Record<string, string | number>) => string
```

Translation key namespaces (from public/locales/en.json):
- wallboard.* — wallboard page strings
- parentShop.* — parent shop page strings
- onboarding.* — onboarding pages strings
- expenseModal.* — ExpenseModal strings
- expenseCharts.* — ExpenseCharts strings
- potentialWidget.* — PotentialWidget strings
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Localize wallboard, parent shop, onboarding, remaining client pages + components</name>
  <files>app/wallboard/page.tsx, app/parent/shop/page.tsx, app/onboarding/v2/page.tsx, app/onboarding/join/page.tsx, app/page.tsx, components/PotentialWidget.tsx, components/ExpenseModal.tsx, components/ExpenseCharts.tsx, components/ScheduleEditor.tsx</files>
  <action>
**CRITICAL:** Read each file fully before editing. Wallboard is 615 lines, onboarding/v2 is 722 lines, onboarding/join is 899 lines — read them completely. Only change string content.

For all client component files: add `import { useT } from '@/lib/i18n'` and `const t = useT()` inside each component function.

**app/wallboard/page.tsx** (53 Russian strings):

Key replacements:
- `'Загрузка...'` → `t('wallboard.loading')`
- `'⏱️ До воскресенья: {days} {dayWord}'` → `t('wallboard.untilSunday', { days: daysToSunday, dayWord: daysToSunday === 1 ? t('wallboard.day') : t('wallboard.days') })`
- `'👑 Лидерборд'` → `'👑 ' + t('nav.rating')`
- `'⭐ До {level} уровня'` → `t('wallboard.toLevel', { level: child.level + 1 })`
- `'Активная цель'` → `t('wallboard.activeGoal')`
- `'Прогресс недели'` → `t('wallboard.weekProgress')`
- Goal progress string `'{goalProgress}% • Осталось {formatMoney(goal.target - goal.current)}'` → `t('wallboard.goalProgress', { pct: goalProgress, remaining: formatMoney(goal.target - goal.current) })`
- All other Russian labels in the wallboard

**app/parent/shop/page.tsx** (41 Russian strings):

Key replacements:
- Title → `t('parentShop.title')`
- Add reward → `t('parentShop.addReward')`
- Loading → `t('parentShop.loading')`
- Form field labels → `t('parentShop.name')`, `t('parentShop.price')`, `t('parentShop.description')`
- Delete confirm → `t('parentShop.deleteConfirm')`
- Save/cancel → `t('parentShop.saveReward')`, `t('parentShop.cancel')`

**app/onboarding/v2/page.tsx** (48 Russian strings):

Read fully. This is the parent onboarding wizard (7+ steps). Key replacements:
- Step labels, button text, field labels → use `t('onboarding.*)` keys
- Navigation buttons: `t('onboarding.next')`, `t('onboarding.back')`, `t('onboarding.done')`, `t('onboarding.skip')`
- Field labels → `t('onboarding.familyName')`, `t('onboarding.childName')`, `t('onboarding.childAge')`
- Loading → `t('onboarding.loading')`
- Error states → `t('onboarding.error')`
- Any other Russian strings in the wizard steps

Note: This page may have many inline Russian strings for specific wizard content. For strings that don't fit existing keys, add new keys to both en.json and ru.json before referencing them. Key pattern: `onboarding.stepN.*` where N is the step number.

**app/onboarding/join/page.tsx** (27 Russian strings):

Read fully. This is the family join flow. Apply same pattern — `t('onboarding.*)` for generic strings, add specific keys as needed.

**app/page.tsx** (11 Russian strings):

This is the app root/redirect page. Read fully. Replace any visible Russian strings with `t()` calls. If this page only contains redirect logic, the strings may be in loading states or error messages.

**components/PotentialWidget.tsx** (50 Russian strings):

Read fully. Key replacements:
- Title → `t('potentialWidget.title')`
- Weekly/monthly labels → `t('potentialWidget.weekly')`, `t('potentialWidget.monthly')`
- Loading → `t('potentialWidget.loading')`
- All other Russian strings in this component

**components/ExpenseModal.tsx** (30 Russian strings):

Key replacements:
- Title → `t('expenseModal.title')`
- Field labels → `t('expenseModal.amount')`, `t('expenseModal.description')`, `t('expenseModal.section')`, `t('expenseModal.date')`
- Save/cancel → `t('expenseModal.save')`, `t('expenseModal.cancel')`
- Loading → `t('expenseModal.loading')`
- Error → `t('expenseModal.error')`

**components/ExpenseCharts.tsx** (24 Russian strings):

Key replacements:
- Title → `t('expenseCharts.title')`
- Chart labels → `t('expenseCharts.byCategory')`, `t('expenseCharts.byMonth')`, `t('expenseCharts.total')`
- Loading → `t('expenseCharts.loading')`

**components/ScheduleEditor.tsx** (25 Russian strings — root-level, NOT settings/):

This is the root-level `components/ScheduleEditor.tsx` (not `components/settings/ScheduleEditor.tsx`). Read fully.
- Apply `t('settings.scheduleEditor.*)` keys consistently
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All 9 client files have 0 hardcoded Russian strings in UI text. TypeScript 0 errors. New translation keys added to both en.json and ru.json where needed.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- All 9 files import useT and call t()
- Wallboard, parent shop, and onboarding pages render English labels in 'en' mode
- Language toggle in NavBar switches all labels immediately
</verification>

<success_criteria>
Plan 05a complete when:
1. All 9 client files have 0 hardcoded Russian strings in visible UI text
2. TypeScript compiles with 0 errors
3. New translation keys present in both en.json and ru.json
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-localization/04.3-05a-SUMMARY.md`
</output>
