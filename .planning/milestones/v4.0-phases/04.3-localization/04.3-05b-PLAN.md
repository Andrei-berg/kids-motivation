---
phase: 04.3-localization
plan: "05b"
type: execute
wave: 2
depends_on:
  - "04.3-01"
files_modified:
  - components/settings/CategoryManager.tsx
  - components/settings/TaskManager.tsx
  - components/settings/ScheduleEditor.tsx
  - components/settings/PeriodsManager.tsx
  - components/settings/SectionsManager.tsx
  - components/settings/SubjectsManager.tsx
  - components/settings/ActivitiesManager.tsx
  - components/settings/StreakSettings.tsx
  - components/settings/CoinRulesEditor.tsx
  - components/settings/NotificationSettings.tsx
  - components/settings/FamilyManager.tsx
  - app/actions/push-streaks.ts
  - app/actions/push.ts
  - app/actions/send-medal.ts
  - app/api/cron/reminders/route.ts
  - app/api/cron/missed-tasks/route.ts
  - app/kid/shop/actions.ts
autonomous: true
requirements:
  - LOC-01

must_haves:
  truths:
    - "All 10 settings manager components display labels in the active language"
    - "Language toggle switches all settings labels immediately without page reload"
    - "Push notification server files are unchanged except for added TODO comments"
    - "No hardcoded Russian strings remain in settings component UI text"
  artifacts:
    - path: "components/settings/CategoryManager.tsx"
      provides: "Localized category manager"
      contains: "useT"
    - path: "components/settings/TaskManager.tsx"
      provides: "Localized task manager"
      contains: "useT"
    - path: "components/settings/NotificationSettings.tsx"
      provides: "Localized notification settings"
      contains: "useT"
    - path: "app/actions/push-streaks.ts"
      provides: "Push action with TODO comment; existing Russian strings kept unchanged"
      contains: "TODO"
  key_links:
    - from: "components/settings/CategoryManager.tsx"
      to: "lib/i18n.tsx"
      via: "import { useT } from '@/lib/i18n'"
      pattern: "useT"
    - from: "app/actions/push-streaks.ts"
      to: "(no translation import)"
      via: "Russian push strings kept unchanged; TODO comment added for future localization"
      pattern: "TODO.*localize push"
---

<objective>
Localize all 10 settings manager components and add TODO comments to server-side push action files.

Purpose: Settings components contain the last major cluster of Russian strings in the codebase (11 files, ~270 strings total). Push notification server actions cannot use React hooks and the family language preference is not readily available in server context — they are marked TODO for a future enhancement rather than localized now. This completes the full UI-layer localization for phase 4.3.

Output: 10 settings components with 0 hardcoded Russian strings. Server-side push files have TODO comments. Production build succeeds.
</objective>

<execution_context>
@/Users/andrei/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrei/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.3-localization/04.3-01-PLAN.md

@components/settings/CategoryManager.tsx
@components/settings/TaskManager.tsx
@components/settings/ScheduleEditor.tsx
@components/settings/PeriodsManager.tsx
@components/settings/SectionsManager.tsx
@components/settings/SubjectsManager.tsx
@components/settings/ActivitiesManager.tsx
@components/settings/StreakSettings.tsx
@components/settings/CoinRulesEditor.tsx
@components/settings/NotificationSettings.tsx
@components/settings/FamilyManager.tsx

<interfaces>
From lib/i18n.tsx (created in Plan 01):
```typescript
export function useT(): (key: string, vars?: Record<string, string | number>) => string
```

Translation key namespaces (from public/locales/en.json):
- settings.categoryManager.* — CategoryManager strings
- settings.taskManager.* — TaskManager strings
- settings.scheduleEditor.* — ScheduleEditor strings
- settings.periodsManager.* — PeriodsManager strings
- settings.sectionsManager.* — SectionsManager strings
- settings.subjectsManager.* — SubjectsManager strings
- settings.activitiesManager.* — ActivitiesManager strings
- settings.streakSettings.* — StreakSettings strings
- settings.coinRules.* — CoinRulesEditor strings
- settings.notificationSettings.* — NotificationSettings strings
- settings.familyManager.* — FamilyManager strings

Note on server-side files: push actions and cron routes are NOT React components and cannot use useT(). The family language preference is not readily available in server actions. Per plan design: keep existing Russian push strings unchanged and add TODO comments. Do NOT attempt to import or call useT() in these files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Localize all 10 settings manager components</name>
  <files>components/settings/CategoryManager.tsx, components/settings/TaskManager.tsx, components/settings/ScheduleEditor.tsx, components/settings/PeriodsManager.tsx, components/settings/SectionsManager.tsx, components/settings/SubjectsManager.tsx, components/settings/ActivitiesManager.tsx, components/settings/StreakSettings.tsx, components/settings/CoinRulesEditor.tsx, components/settings/NotificationSettings.tsx, components/settings/FamilyManager.tsx</files>
  <action>
For each settings component: read fully, add `import { useT } from '@/lib/i18n'` and `const t = useT()` inside the component function, replace all Russian strings with `t()` calls using the appropriate `settings.*` namespace.

**components/settings/CategoryManager.tsx** (33 Russian strings):
- Title → `t('settings.categoryManager.title')`
- Add button → `t('settings.categoryManager.addCategory')`
- Field labels → `t('settings.categoryManager.name')`, `t('settings.categoryManager.emoji')`
- Save/cancel → `t('settings.categoryManager.save')`, `t('settings.categoryManager.cancel')`
- Delete confirm → `t('settings.categoryManager.deleteConfirm')`
- Active/inactive labels → `t('settings.categoryManager.active')`, `t('settings.categoryManager.inactive')`

**components/settings/TaskManager.tsx** (28 Russian strings):
- Use `settings.taskManager.*` keys
- All field labels, buttons, confirm dialogs, frequency labels

**components/settings/ScheduleEditor.tsx** (29 Russian strings):
- Use `settings.scheduleEditor.*` keys
- Item type labels: `t('settings.scheduleEditor.lesson')`, `t('settings.scheduleEditor.section')`, `t('settings.scheduleEditor.routine')`

**components/settings/PeriodsManager.tsx** (38 Russian strings):
- Use `settings.periodsManager.*` keys
- Period type labels: vacation/school

**components/settings/SectionsManager.tsx** (32 Russian strings):
- Use `settings.sectionsManager.*` keys

**components/settings/SubjectsManager.tsx** (15 Russian strings):
- Use `settings.subjectsManager.*` keys

**components/settings/ActivitiesManager.tsx** (17 Russian strings):
- Use `settings.activitiesManager.*` keys

**components/settings/StreakSettings.tsx** (15 Russian strings):
- Use `settings.streakSettings.*` keys

**components/settings/CoinRulesEditor.tsx** (11 Russian strings):
- Use `settings.coinRules.*` keys
- The grade/rule labels match the existing English labels in en.json

**components/settings/NotificationSettings.tsx** (26 Russian strings):
- Use `settings.notificationSettings.*` keys
- Test notification button → `t('settings.notificationSettings.testButton')`

**components/settings/FamilyManager.tsx** (25 Russian strings):
- Use `settings.familyManager.*` keys

Add any missing translation keys to both en.json and ru.json before referencing them.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All 10 settings components import useT and call t(). 0 hardcoded Russian strings in UI text across all 10 files. TypeScript 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add TODO comments to server-side push files + fix shop actions</name>
  <files>app/actions/push-streaks.ts, app/actions/push.ts, app/actions/send-medal.ts, app/api/cron/reminders/route.ts, app/api/cron/missed-tasks/route.ts, app/kid/shop/actions.ts</files>
  <action>
**Strategy:** Push notification server actions cannot use React hooks. The family language preference is not available in server context without an extra DB fetch. The success criteria requires UI localization only — push notification content is a future concern. Do NOT import useT() or attempt to create a server-side translation helper in these files.

For `app/actions/push-streaks.ts`:
- Read the file fully
- Keep all existing Russian push notification strings unchanged
- Add a comment at the top of the file (after imports): `// TODO: Phase 4.3+ — localize push notification strings based on family language preference stored in DB`
- Do not change any string values

For `app/actions/push.ts`:
- Same approach — add the TODO comment at top, keep Russian strings unchanged

For `app/actions/send-medal.ts`:
- Same approach — add TODO comment, keep Russian strings unchanged

For `app/api/cron/reminders/route.ts`:
- Same approach — add TODO comment, keep Russian strings unchanged

For `app/api/cron/missed-tasks/route.ts`:
- Same approach — add TODO comment, keep Russian strings unchanged

For `app/kid/shop/actions.ts` (2 Russian strings):
- These are server action error messages returned to the client
- Convert to English string literals directly (no hook needed): `'Ошибка'` → `'Error'`, `'Недостаточно монет'` → `'Insufficient coins'`

**Final verification pass — run Cyrillic detection:**
```bash
find app components -name "*.tsx" -o -name "*.ts" | xargs python3 -c "
import sys, os
files = sys.argv[1:]
for f in files:
    try:
        content = open(f).read()
        lines = [(i+1, l) for i, l in enumerate(content.split('\n')) if any('Ѐ' <= c <= 'ӿ' for c in l) and not l.strip().startswith('//') and not l.strip().startswith('*')]
        if lines:
            print(f'{f}: {len(lines)} lines with Cyrillic')
            for n, l in lines[:3]:
                print(f'  {n}: {l.strip()[:80]}')
    except: pass
" -- $(find app components -name "*.tsx" -o -name "*.ts" | grep -v node_modules | grep -v DailyModal-old)
```

Review output. Acceptable remaining Cyrillic: push action files (intentional TODO items) and strings that come from the database (user-entered data). Fix any unexpected UI-layer Cyrillic found in files not in the push actions list.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Push action files have TODO comments and unchanged Russian strings. app/kid/shop/actions.ts uses English error strings. TypeScript 0 errors. Production build succeeds.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- `npm run build` succeeds
- All 10 settings components import useT and call t()
- Language toggle in NavBar switches all settings labels immediately
- Push action files contain the TODO comment and unchanged Russian strings
- Cyrillic detection script shows 0 UI-layer files with unexpected Russian strings
</verification>

<success_criteria>
Plan 05b complete when:
1. All 10 settings components have 0 hardcoded Russian strings in UI text
2. Server-side push files have TODO comments; Russian push strings are unchanged
3. TypeScript compiles with 0 errors
4. Production build succeeds
5. The only remaining Russian strings are in: push notification server actions (marked TODO), code comments, and user-entered data from the database
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-localization/04.3-05b-SUMMARY.md`
</output>
