---
phase: 04.1-pwa
plan: "02"
subsystem: infra
tags: [pwa, service-worker, offline, caching, react]

# Dependency graph
requires:
  - phase: 04.1-pwa-01
    provides: InstallPrompt and updated app/layout.tsx with PushInit/InstallPrompt mounted

provides:
  - Offline caching via service worker (cache-first static, network-first pages, passthrough API)
  - OfflineBanner component rendering amber fixed banner when navigator.onLine is false
  - Pre-cached app shell on SW install for all key routes

affects:
  - 04.1-pwa-03  # will append pushsubscriptionchange to same sw.js

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Cache-first strategy for /_next/static/ chunks"
    - "Network-first with cache fallback for page routes"
    - "API/Supabase passthrough (never cached)"
    - "navigator.onLine + online/offline events for offline detection"

key-files:
  created:
    - components/OfflineBanner.tsx
  modified:
    - public/sw.js
    - app/layout.tsx

key-decisions:
  - "Three-strategy fetch handler: passthrough for API/Supabase, cache-first for static chunks, network-first for pages"
  - "addAll in install handler is best-effort (wrapped in catch) so SW installs even if pre-caching fails"
  - "OfflineBanner uses inline styles (not Tailwind) for reliability before CSS hydration — consistent with InstallPrompt approach"
  - "OfflineBanner placed before children in body so it is first in DOM; fixed position handles visual placement"

patterns-established:
  - "SW caching: never cache /api/ or supabase.co — always network-only"
  - "Offline UI: navigator.onLine init + addEventListener online/offline — no polling"

requirements-completed:
  - PWA-03

# Metrics
duration: 2min
completed: 2026-04-26
---

# Phase 4.1 Plan 02: Offline Caching Summary

**Service worker extended with three-strategy fetch caching (cache-first static, network-first pages, API passthrough) and OfflineBanner component showing fixed amber banner when offline**

## Performance

- **Duration:** 2 min
- **Started:** 2026-04-26T18:04:09Z
- **Completed:** 2026-04-26T18:05:29Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Extended public/sw.js with CACHE_NAME, STATIC_ASSETS, and three-strategy fetch handler while preserving push/notificationclick handlers
- App shell pre-cached on service worker install (best-effort, non-blocking)
- Old caches cleaned on activate to keep storage tidy
- OfflineBanner component created: listens to online/offline events, initializes from navigator.onLine, renders fixed amber banner with Russian text when offline
- OfflineBanner mounted in app/layout.tsx before children (alongside PushInit and InstallPrompt)

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend sw.js with offline caching strategy** - `4db3c67` (feat)
2. **Task 2: OfflineBanner component + mount in layout** - `89aa5ac` (feat)

**Plan metadata:** (docs commit below)

## Files Created/Modified
- `public/sw.js` - Extended with CACHE_NAME, STATIC_ASSETS, updated install/activate, new fetch handler
- `components/OfflineBanner.tsx` - New offline indicator component with event listeners
- `app/layout.tsx` - Added OfflineBanner import and render before PushInit

## Decisions Made
- Three-strategy fetch handler: passthrough for API/Supabase, cache-first for `/_next/static/`, network-first for everything else
- `addAll` wrapped in `.catch(() => {})` so pre-caching failures don't abort SW install
- Inline styles on OfflineBanner (consistent with InstallPrompt) — renders before Tailwind hydration
- OfflineBanner placed first in body DOM order; `position: fixed; top: 0; zIndex: 9999` handles visual placement

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Plan 03 (04.1-pwa-03) can now safely append `pushsubscriptionchange` handler to sw.js — caching handlers are in place and won't conflict
- Offline behavior can be tested immediately in Chrome DevTools → Network → Offline toggle

---
*Phase: 04.1-pwa*
*Completed: 2026-04-26*
