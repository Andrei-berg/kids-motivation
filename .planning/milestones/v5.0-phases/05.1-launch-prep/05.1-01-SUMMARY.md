# Plan 05.1-01 Summary — Rotate leaked service-role key + DB password

**Status:** Complete ✅
**Date:** 2026-07-06
**Type:** Operator-gated (checkpoint:human-action + checkpoint:human-verify)

## What was done

**Task 1 (operator, confirmed "rotated"):**
- Supabase `service_role` secret reset in Dashboard → Project Settings → API Keys
- Database password reset; `SUPABASE_DB_URL` (eu-north-1 session pooler) rebuilt
- `.env.local` updated locally (git-ignored; values never printed)
- Vercel Production env (`SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_DB_URL`) updated + redeploy
- Operator confirmed the old service_role secret no longer appears in the API Keys list

**Task 2 (verification, run 2026-07-06):**
- `node --env-file=.env.local scripts/verify-wallet-rls.mjs` → exit 0
  (child can READ own wallet; cannot UPDATE wallet; cannot INSERT wallet_transaction)
- `node --env-file=.env.local scripts/verify-award-idempotency.mjs` → exit 0
  (insert OK; duplicate `(child_id, source_type, source_id)` rejected with 23505; cleanup done)
- Prod smoke: `https://kids-motivation.vercel.app/` → HTTP 200

## Acceptance criteria

- [x] Old service_role secret gone from Supabase API Keys (operator-confirmed)
- [x] `.env.local` carries new credentials (presence confirmed; values never logged)
- [x] Vercel Production env updated, redeploy triggered
- [x] verify-wallet-rls.mjs exits 0 on new credentials
- [x] verify-award-idempotency.mjs exits 0 on new credentials
- [x] Prod URL loads (HTTP 200)
- [x] No secret values appear in transcript, repo files, or command output

## Notes

- No repository files changed by this plan (env-only rotation).
- SC1 of Phase 5.1 is satisfied: leaked credentials invalidated, prod runs on rotated ones.
