---
phase: 05.1-launch-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [LP-SC1]
user_setup:
  - service: supabase
    why: "Rotate the leaked service-role key and DB password (both appeared in an earlier chat session and must be treated as compromised)"
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Project Settings → API Keys → reset/roll the service_role secret"
      - name: SUPABASE_DB_URL
        source: "Supabase Dashboard → Project Settings → Database → Reset database password (update the password segment of the pooler connection string)"
    dashboard_config:
      - task: "Reset the service_role secret and the database password; then update the same vars in the Vercel project env and redeploy"
        location: "Supabase Dashboard + Vercel Project → Settings → Environment Variables"

must_haves:
  truths:
    - "A request to prod carrying the OLD service-role key is rejected (401/invalid)"
    - "The prod app functions with the NEW service-role key and NEW DB password"
    - "The live-DB verify scripts pass against prod using the rotated credentials"
  artifacts: []
  key_links:
    - from: ".env.local (local) + Vercel env"
      to: "Supabase prod project"
      via: "SUPABASE_SERVICE_ROLE_KEY + SUPABASE_DB_URL"
      pattern: "service-role auth + pooler connection succeed post-rotation"
---

<objective>
Rotate the leaked Supabase service-role key and database password so the old credentials
stop working, update the local and Vercel environments, redeploy, and prove prod still
functions on the new credentials.

Purpose: These secrets leaked in an earlier chat session (see STATE.md Blockers and
MEMORY security-key-rotation-pending). This is the P0 gate — no other v5.0 work should
ship on compromised credentials.
Output: Rotated prod credentials; old key invalid; verify scripts green on prod. No code
changes are committed (env only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-launch-prep/05.1-CONTEXT.md

# Canonical refs
@CLAUDE.md
@scripts/verify-wallet-rls.mjs
@scripts/verify-award-idempotency.mjs
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Rotate service-role key + DB password and update local/Vercel env (MANUAL)</name>
  <read_first>
    - CLAUDE.md — "Environment" section (exact env var names) and "Applying DB migrations" section (SUPABASE_DB_URL is the pooler connection string)
    - .env.local (inspect current keys; DO NOT print values into the transcript)
  </read_first>
  <what-built>
    This is an operator-only step. Claude cannot rotate dashboard secrets. Provide the
    operator these exact instructions and pause:

    1. Supabase Dashboard → Project Settings → API Keys → reset/roll the `service_role`
       secret. Copy the new value directly into your password manager — do NOT paste it
       into this chat, any file tracked by git, or any log.
    2. Supabase Dashboard → Project Settings → Database → "Reset database password".
       Rebuild the `SUPABASE_DB_URL` pooler connection string (host eu-north-1 session
       pooler) with the new password.
    3. Locally: edit `.env.local` (git-ignored) — replace `SUPABASE_SERVICE_ROLE_KEY`
       with the new secret and update the password segment of `SUPABASE_DB_URL`. Save.
    4. Vercel → the kids-motivation project → Settings → Environment Variables: update
       `SUPABASE_SERVICE_ROLE_KEY` (and `SUPABASE_DB_URL` if set there) to the new values
       for the Production environment. Trigger a redeploy of the latest commit.
    5. Confirm in the Supabase dashboard that the OLD service_role secret no longer
       appears in the API Keys list.

    CRITICAL: never echo the new (or old) secret value into the chat, into any file, or
    into a command that would land in shell history/logs. `.env.local` is the only place
    the new secret is written locally, and it is git-ignored.
  </what-built>
  <action>Operator rotates SUPABASE_SERVICE_ROLE_KEY and the DB password in the Supabase dashboard, updates .env.local and the Vercel Production env, and redeploys. No repository files change. Executor must NOT read back or print secret values; treat both old and new secrets as never-to-be-logged.</action>
  <verify>
    <automated>MISSING — operator-gated dashboard step; verification is Task 2</automated>
  </verify>
  <acceptance_criteria>
    - Operator confirms the old service_role secret is no longer listed in Supabase → API Keys
    - `.env.local` contains the new `SUPABASE_SERVICE_ROLE_KEY` and an updated `SUPABASE_DB_URL` (presence confirmed by the operator; value never printed)
    - Vercel Production env updated and a redeploy was triggered
    - No secret value appears anywhere in the chat transcript, repo files, or command output
  </acceptance_criteria>
  <resume-signal>Type "rotated" once the dashboard reset, .env.local update, Vercel env update, and redeploy are done</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify prod works on new credentials and the old key is rejected</name>
  <read_first>
    - scripts/verify-wallet-rls.mjs — RLS lock live-DB check (service-role)
    - scripts/verify-award-idempotency.mjs — idempotency contract live-DB check
    - CLAUDE.md — "Applying DB migrations" (how verify scripts are invoked with --env-file)
  </read_first>
  <action>Run the existing live-DB verify scripts against prod using the rotated .env.local credentials: `node --env-file=.env.local scripts/verify-wallet-rls.mjs` and `node --env-file=.env.local scripts/verify-award-idempotency.mjs`. Both must exit 0, proving the new service-role key and pooler password reach prod. Then load the prod app URL (https://kids-motivation.vercel.app) and confirm it loads and a parent can view a wallet (money tables are SELECT-readable). Old-key rejection is confirmed via Task 1 step 5 (old secret removed from Supabase → API Keys) — do NOT attempt to reproduce by pasting the old secret into a command.</action>
  <verify>
    <automated>node --env-file=.env.local scripts/verify-wallet-rls.mjs && node --env-file=.env.local scripts/verify-award-idempotency.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-wallet-rls.mjs` exits 0 (RLS lock intact, reachable with new key)
    - `scripts/verify-award-idempotency.mjs` exits 0 (idempotency index intact, reachable with new key)
    - Prod URL loads and a wallet balance renders (app works on new credentials)
    - Operator has confirmed the old service_role secret is gone from the Supabase API Keys list (old credential invalid)
  </acceptance_criteria>
  <resume-signal>Type "verified" or paste the script output / describe any failure</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → chat/git/logs | New/old secrets must never cross into the transcript, tracked files, or shell history |
| local/Vercel env → Supabase prod | Rotated credentials are the only path privileged writes authenticate through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-051-01 | Information Disclosure | New service-role key / DB password | mitigate | Written only to git-ignored .env.local + Vercel env UI; never echoed to chat/files/logs; executor forbidden to read back or print values |
| T-051-02 | Spoofing / Elevation | Old leaked service-role key | mitigate | Rotation invalidates the old secret; confirmed removed from Supabase API Keys list; old-key rejection NOT tested by re-pasting the secret |
| T-051-03 | Denial of Service | Prod after rotation | mitigate | Verify scripts + prod smoke check confirm the app functions before considering the phase safe |
</threat_model>

<verification>
- Both verify scripts exit 0 against prod with the rotated `.env.local`.
- Prod URL loads and a wallet balance renders.
- Old service_role secret absent from Supabase → API Keys.
- `git status` shows no tracked file changed (env-only rotation) and no secret in any diff.
</verification>

<success_criteria>
ROADMAP 5.1 SC1 met: service-role key and DB password rotated, old credentials invalid,
app works on prod with the new ones.
</success_criteria>

<output>
Create `.planning/phases/05.1-launch-prep/05.1-01-SUMMARY.md` when done. Record verify-script
outcomes and the prod smoke result. NEVER record any secret value.
</output>
