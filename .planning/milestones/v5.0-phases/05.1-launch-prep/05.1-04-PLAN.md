---
phase: 05.1-launch-prep
plan: 04
type: execute
wave: 2
depends_on: [05.1-02, 05.1-03]
files_modified: [package.json, package-lock.json, lib/analytics.ts, components/AnalyticsProvider.tsx, app/layout.tsx, components/kid/KidDayFillForm.tsx, components/DailyModal.tsx]
autonomous: false
requirements: [LP-SC2]
user_setup:
  - service: posthog
    why: "Product analytics (free tier, EU cloud) — pageviews + first custom event so v5.0 redesign decisions are data-informed"
    env_vars:
      - name: NEXT_PUBLIC_POSTHOG_KEY
        source: "PostHog (EU cloud, eu.posthog.com) → Project Settings → Project API Key"
      - name: NEXT_PUBLIC_POSTHOG_HOST
        source: "Set to https://eu.i.posthog.com (EU ingestion host)"
    dashboard_config:
      - task: "Create a PostHog project on EU Cloud (free tier) and copy the project API key"
        location: "eu.posthog.com → Create project"

must_haves:
  truths:
    - "With no PostHog env key set, the app builds and runs without crashing and sends nothing (no-op)"
    - "With the key set, page views are recorded on route changes"
    - "Saving a kid day (kid form or parent modal) records a `day_saved` custom event"
  artifacts:
    - path: "lib/analytics.ts"
      provides: "Vendor-swappable analytics wrapper (init + track), no-op without env key"
      exports: ["track"]
    - path: "components/AnalyticsProvider.tsx"
      provides: "Client provider that initialises PostHog and captures pageviews on navigation"
      contains: "posthog"
    - path: "app/layout.tsx"
      provides: "AnalyticsProvider mounted in root layout"
      contains: "AnalyticsProvider"
  key_links:
    - from: "components/kid/KidDayFillForm.tsx"
      to: "lib/analytics.ts"
      via: "track('day_saved', ...) after successful /api/wallet/award POST"
      pattern: "track\\('day_saved'"
    - from: "components/DailyModal.tsx"
      to: "lib/analytics.ts"
      via: "track('day_saved', ...) after successful /api/wallet/award POST"
      pattern: "track\\('day_saved'"
    - from: "lib/analytics.ts / AnalyticsProvider"
      to: "process.env.NEXT_PUBLIC_POSTHOG_KEY"
      via: "guard — everything no-ops when absent"
      pattern: "NEXT_PUBLIC_POSTHOG_KEY"
---

<objective>
Add PostHog product analytics (EU cloud) behind a small vendor-swappable wrapper: automatic
page views plus the first custom event `day_saved` fired from both day-fill flows.

Purpose: ROADMAP 5.1 SC2 (analytics half) — product analytics records a test event, so the
flexibility/redesign phases are not flown blind. CONTEXT decisions: PostHog free tier, EU
cloud preferred, lightweight wrapper so the vendor can be swapped, no-op safe without keys.
Output: posthog-js installed; wrapper + provider; `day_saved` instrumented in
KidDayFillForm and DailyModal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05.1-launch-prep/05.1-CONTEXT.md

@app/layout.tsx
@components/kid/KidDayFillForm.tsx
@components/DailyModal.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for posthog-js (before install)</name>
  <read_first>
    - package.json — confirm posthog-js is not already a dependency
    - .planning/phases/05.1-launch-prep/05.1-CONTEXT.md — Monitoring decision (PostHog chosen, EU cloud)
  </read_first>
  <what-built>
    No RESEARCH.md Package Legitimacy Audit exists for this phase, so `posthog-js` is
    treated as [ASSUMED] per fallback policy. Verify on
    https://www.npmjs.com/package/posthog-js that it is the official PostHog browser SDK
    (publisher: PostHog org, repo PostHog/posthog-js, high weekly downloads).
  </what-built>
  <action>Human verifies posthog-js legitimacy on npmjs.com (official PostHog publisher, source repo PostHog/posthog-js) and approves the install. This checkpoint is never auto-approvable regardless of auto_advance.</action>
  <verify>
    <automated>MISSING — human legitimacy verification; install happens in Task 2 only after approval</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms posthog-js is the official PostHog package
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with the install, or name an alternative</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Install posthog-js, create the analytics wrapper and provider, mount in layout</name>
  <files>package.json, package-lock.json, lib/analytics.ts, components/AnalyticsProvider.tsx, app/layout.tsx</files>
  <read_first>
    - app/layout.tsx — current provider nesting (LanguageProvider wraps OfflineBanner/PushInit/InstallPrompt/PageTransition); layout is a SERVER component with a metadata export, so PostHog init must live in a 'use client' child, mirroring the LanguageProvider pattern
    - components/InstallPrompt.tsx — example of an existing 'use client' root-layout child component
    - PostHog Next.js App Router docs (via Context7: resolve "posthog-js", query "Next.js app router pageview capture") — use the CURRENT recommended pageview pattern
  </read_first>
  <action>Install posthog-js with npm. Create lib/analytics.ts ('use client'-safe module) exporting: `initAnalytics()` — calls posthog.init(process.env.NEXT_PUBLIC_POSTHOG_KEY, { api_host: process.env.NEXT_PUBLIC_POSTHOG_HOST || 'https://eu.i.posthog.com', ... }) ONLY when NEXT_PUBLIC_POSTHOG_KEY is truthy and window is defined; and `track(event: string, properties?: Record<string, unknown>)` — silently returns when the key is absent or init never ran (never throws). Do not enable session recording; keep autocapture defaults minimal per current docs. Create components/AnalyticsProvider.tsx ('use client'): calls initAnalytics() once on mount and captures a `$pageview` on every route change using usePathname/useSearchParams (follow the current PostHog App Router pageview recipe; disable the SDK's own automatic pageview if the recipe requires manual capture). Mount `<AnalyticsProvider />` inside LanguageProvider in app/layout.tsx alongside OfflineBanner/PushInit (render as a null-UI sibling, not a wrapper, to avoid re-render surface). Preserve the FamilyCoins metadata from plan 05.1-02 untouched.</action>
  <verify>
    <automated>grep -q "posthog-js" package.json && grep -q "NEXT_PUBLIC_POSTHOG_KEY" lib/analytics.ts && grep -q "eu.i.posthog.com" lib/analytics.ts && grep -q "AnalyticsProvider" app/layout.tsx && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - posthog-js in package.json dependencies
    - lib/analytics.ts exports `track` and guards all posthog calls on NEXT_PUBLIC_POSTHOG_KEY presence (grep shows the env var checked before posthog.init)
    - Default api_host is the EU ingestion host `https://eu.i.posthog.com`
    - app/layout.tsx renders `<AnalyticsProvider />`; metadata export unchanged from plan 02 state
    - `npm run build` succeeds with NO PostHog env vars set (no-op-safe)
    - `track('x')` called without a key does not throw (unit-verifiable: function body returns early when key absent)
  </acceptance_criteria>
  <done>PostHog initialises and captures pageviews when the key is present; everything no-ops cleanly when absent.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fire `day_saved` from both day-fill flows</name>
  <files>components/kid/KidDayFillForm.tsx, components/DailyModal.tsx</files>
  <read_first>
    - components/kid/KidDayFillForm.tsx — the save handler around line 503 where `fetch('/api/wallet/award', ...)` fires after the day's rows are saved
    - components/DailyModal.tsx — the save handler around line 462 with the same award POST
    - lib/analytics.ts — the `track` signature from Task 2
  </read_first>
  <action>In both save flows, after the day save + award POST completes successfully (the existing success path, not on error), call `track('day_saved', { role: 'kid' })` in KidDayFillForm.tsx and `track('day_saved', { role: 'parent' })` in DailyModal.tsx. Import `track` from '@/lib/analytics'. Send NO PII: no child names, no emails, no coin amounts, no family identifiers — the `role` property only. The call must never affect the save flow: it is fire-and-forget after success and must not be awaited in a way that can reject the save (track never throws per Task 2). Do not alter any save/award logic.</action>
  <verify>
    <automated>grep -q "track('day_saved', { role: 'kid' })" components/kid/KidDayFillForm.tsx && grep -q "track('day_saved', { role: 'parent' })" components/DailyModal.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - KidDayFillForm.tsx contains exactly `track('day_saved', { role: 'kid' })` on the post-award success path
    - DailyModal.tsx contains exactly `track('day_saved', { role: 'parent' })` on the post-award success path
    - No property passed to track() in either file contains a name, email, childId, familyId, or coin amount
    - `npx tsc --noEmit` exits 0; save flows compile unchanged otherwise
  </acceptance_criteria>
  <done>Saving a day from either flow records a `day_saved` event (verifiable in PostHog Live Events once the key is configured on prod).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | posthog-js is third-party code shipped to every client (supply chain) |
| browser → PostHog EU ingest | Event payloads leave the user's browser to a third party (children's app — PII sensitivity is high) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-051-SC | Tampering | npm install posthog-js | mitigate | Legitimacy gate (Task 1, blocking-human) verifies official PostHog publisher before install |
| T-051-09 | Information Disclosure | Analytics event payloads | mitigate | EU ingestion host; `day_saved` carries only `{ role }`; no names/emails/ids/coin amounts; session recording disabled |
| T-051-10 | Information Disclosure | Autocapture on a kids' app | mitigate | Keep autocapture minimal per docs; no identify() calls with personal data in this phase |
| T-051-11 | Denial of Service | Missing key in dev/CI | mitigate | init + track guard on NEXT_PUBLIC_POSTHOG_KEY; build verified green with no env set |
</threat_model>

<verification>
- `npm run build` succeeds with no PostHog env vars (no-op-safe).
- Grep gates for the two `day_saved` call sites pass.
- After the operator sets NEXT_PUBLIC_POSTHOG_KEY/HOST in Vercel and redeploys: a pageview and a `day_saved` event appear in PostHog Live Events (manual confirm — record in SUMMARY).
</verification>

<success_criteria>
ROADMAP 5.1 SC2 (analytics half) met: product analytics records a test event.
</success_criteria>

<output>
Create `.planning/phases/05.1-launch-prep/05.1-04-SUMMARY.md` when done. Note the env vars
the operator must add in Vercel and the manual PostHog Live Events confirmation.
</output>
