---
phase: 05.1-launch-prep
verified: 2026-07-06T07:07:36Z
status: human_needed
score: 4/4 roadmap truths code-verified; 1 pending operator confirmation (SC2 live capture)
overrides_applied: 0
human_verification:
  - test: "Provision Sentry DSN (NEXT_PUBLIC_SENTRY_DSN + SENTRY_DSN) in Vercel Production env, redeploy, then GET https://kids-motivation.vercel.app/api/health?boom=1"
    expected: "HTTP 500 returned; a 'Sentry test error: /api/health?boom=1' issue appears in the Sentry project's Issues stream within ~1-2 minutes"
    why_human: "Requires creating a Sentry account/project and adding secrets to Vercel — no code path exists for the verifier to do this; SUMMARY 05.1-03 explicitly states this step was 'not yet performed'"
  - test: "Provision PostHog EU project key (NEXT_PUBLIC_POSTHOG_KEY) in Vercel Production env, redeploy, browse the site, then save a day from /kid/day or a parent daily modal"
    expected: "PostHog → Activity → Live Events shows a $pageview event and a day_saved event with properties.role = 'kid' or 'parent'"
    why_human: "Requires creating a PostHog account/project and adding a secret to Vercel — no code path exists for the verifier to do this; SUMMARY 05.1-04 explicitly states this step was 'not yet performed'"
---

# Phase 5.1: launch-prep Verification Report

**Phase Goal:** The project is safe to iterate on fast — leaked credentials rotated, errors observable, money logic covered by tests
**Verified:** 2026-07-06T07:07:36Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC1 | Supabase service-role key and DB password rotated; old credentials invalid; app works on prod with the new ones | VERIFIED | 05.1-01-SUMMARY documents operator-confirmed rotation in Supabase dashboard + Vercel env update + redeploy. `scripts/verify-wallet-rls.mjs` and `scripts/verify-award-idempotency.mjs` exist on disk and were run (exit 0) against the new credentials per SUMMARY; prod smoke `https://kids-motivation.vercel.app/` returned HTTP 200. No secret values (service-role key, DB password, JWTs) found anywhere in the phase directory, README, or the 20 most recent commits — grepped for `eyJ...` JWT patterns and `postgres://user:pass@` strings across `git log -p`, all clean (only placeholder tokens like `<service-role-key>` appear). This SC's actual rotation action happened outside the repo (Supabase/Vercel dashboards) and cannot be independently re-verified by the verifier without live credentials; accepted on the operator-confirmed checkpoint + passing verify-script evidence, consistent with this SC's inherently operator-gated nature. |
| SC2 | Sentry captures a deliberately thrown test error from prod; product analytics records a test event | CODE VERIFIED / LIVE CAPTURE PENDING | All code-side wiring confirmed on disk: `sentry.client.config.ts`, `sentry.server.config.ts`, `sentry.edge.config.ts` all guard `Sentry.init` behind `if (dsn)` reading `NEXT_PUBLIC_SENTRY_DSN` (client) / `SENTRY_DSN \|\| NEXT_PUBLIC_SENTRY_DSN` (server, edge) — true no-op with no env. `instrumentation.ts` `register()` branches on `NEXT_RUNTIME` (`nodejs`→server config, `edge`→edge config). `next.config.js` sets `experimental.instrumentationHook: true` and wraps `nextConfig` in `withSentryConfig`, source-map upload gated on `SENTRY_AUTH_TOKEN`. `app/api/health/route.ts` throws an uncaught `Error('Sentry test error: /api/health?boom=1')` when `?boom=1` is passed, otherwise unchanged 200 behavior. `lib/analytics.ts` only calls `posthog.init` when `NEXT_PUBLIC_POSTHOG_KEY` is set and `window` exists, EU host default (`https://eu.i.posthog.com`), `track()` no-ops silently and wraps `posthog.capture` in try/catch. `AnalyticsProvider` is mounted inside `LanguageProvider` in `app/layout.tsx`, calls `initAnalytics()` on mount and fires `trackPageview()` on route change. `track('day_saved', { role: 'kid' })` in `components/kid/KidDayFillForm.tsx:510` and `track('day_saved', { role: 'parent' })` in `components/DailyModal.tsx:469`, both gated on the `/api/wallet/award` `res.ok` branch, props limited to `{ role }` (no childId/familyId/names/coin amounts) — PII-free as required. **Residual (documented, not a code gap):** neither Sentry DSN nor PostHog key has been added to Vercel yet — 05.1-03-SUMMARY and 05.1-04-SUMMARY both explicitly list the live-capture confirmation as "not yet performed," to be done by the operator after provisioning the two external services. The literal SC2 truth ("Sentry captures... from prod; analytics records a test event") is therefore not yet observably true in production — it is code-ready and blocked only on operator-side account/env provisioning outside the codebase. |
| SC3 | Integration tests cover `/api/wallet/award` (idempotency per source, all source types), purchase, exchange — green locally | VERIFIED | `tests/integration/family-fixture.ts` (`hasIntegrationEnv`, `serviceClient()`, `createTestFamily()`/`destroyTestFamily()`), `tests/integration/award.test.ts` (6 `it()` blocks: all 7 source types, idempotency, 23505 backstop, child-role gating, teardown), `tests/integration/purchase.test.ts` (7 `it()` blocks: request/deduct, insufficient-coins 400, approve-no-double-deduct, reject-refund, non-pending guard, teardown), `tests/integration/exchange-withdraw.test.ts` (8 `it()` blocks: exchange rate/bonus math, overspend guard, input validation, withdraw-is-not-a-deduction, insufficient balance, **KNOWN GAP** literal marker for pending-withdrawal double-spend at line 209, teardown) all exist and match the plans' described scope. `package.json` has `test:integration` script (`node --env-file=.env.local ... vitest run tests/integration`); `vitest.config.ts` has explicit `include: ['tests/**/*.test.ts']` plus `hookTimeout`/`testTimeout: 60000`. Ran `npm test` myself: **3 passed \| 3 skipped test files, 31 passed \| 18 skipped tests**, exits clean with zero Supabase env — matches SUMMARY claim exactly. Did not re-run `npm run test:integration` (hits live prod DB per instruction); accepted the 05.1-06-SUMMARY's documented 18/18 green run against the live DB. |
| SC4 | App name is FamilyCoins in manifest, metadata, and README | VERIFIED | `app/layout.tsx` `metadata.title: 'FamilyCoins'`, `metadata.description` FamilyCoins-branded, `appleWebApp.title: 'FamilyCoins'`. `app/manifest.ts` `name: 'FamilyCoins'`, `short_name: 'FamilyCoins'`. `README.md` line 1: `# 🚀 FamilyCoins`. All three confirmed by direct file read. |

**Score:** 4/4 truths code-verified; SC2's literal "captures from prod" clause remains pending operator action (external service provisioning), correctly flagged as human verification rather than a code gap.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `sentry.client.config.ts` / `sentry.server.config.ts` / `sentry.edge.config.ts` | DSN-guarded init | VERIFIED | All three read env, `if (dsn) Sentry.init(...)`, no unconditional init |
| `instrumentation.ts` | register() per NEXT_RUNTIME | VERIFIED | Branches nodejs/edge exactly as claimed |
| `next.config.js` | withSentryConfig wrap + instrumentationHook | VERIFIED | Both present; source-map upload gated on `SENTRY_AUTH_TOKEN` |
| `app/api/health/route.ts` | `?boom=1` trigger | VERIFIED | Throws uncaught error only on `boom=1`, default behavior unchanged |
| `lib/analytics.ts` | no-op-safe PostHog wrapper, EU host default | VERIFIED | Guards on `NEXT_PUBLIC_POSTHOG_KEY`, `window`; EU host default `https://eu.i.posthog.com`; try/catch in `track()` |
| `components/AnalyticsProvider.tsx` | mounted in layout, pageview capture | VERIFIED | Mounted in `app/layout.tsx`; `Suspense`-wrapped `usePathname`/`useSearchParams` pageview capture |
| `components/kid/KidDayFillForm.tsx`, `components/DailyModal.tsx` | `track('day_saved')` PII-free | VERIFIED | Both call sites confirmed, `{ role }` only, gated on award `res.ok` |
| `tests/integration/family-fixture.ts` | test-family fixture | VERIFIED | Exists, exports match plan |
| `tests/integration/award.test.ts` | idempotency + 7 source types | VERIFIED | Exists, 6 test blocks |
| `tests/integration/purchase.test.ts` | request/approve/reject/refund | VERIFIED | Exists, 7 test blocks |
| `tests/integration/exchange-withdraw.test.ts` | exchange math + KNOWN GAP marker | VERIFIED | Exists, 8 test blocks, "KNOWN GAP" literal marker present at line 209 |
| `app/layout.tsx`, `app/manifest.ts`, `README.md` | FamilyCoins naming | VERIFIED | All three confirmed |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `KidDayFillForm.tsx` / `DailyModal.tsx` | `lib/analytics.ts` | `track('day_saved', ...)` import + call | WIRED | Both files import `track` and call it on the `res.ok` branch after `/api/wallet/award` POST |
| `app/layout.tsx` | `components/AnalyticsProvider.tsx` | JSX mount | WIRED | `<AnalyticsProvider />` rendered inside `LanguageProvider` |
| `instrumentation.ts` | `sentry.server.config.ts` / `sentry.edge.config.ts` | dynamic `import()` per `NEXT_RUNTIME` | WIRED | Confirmed branching logic |
| `next.config.js` | `@sentry/nextjs` | `withSentryConfig(nextConfig, {...})` | WIRED | Confirmed wrap, build-tested per SUMMARY (`npm run build` clean) |
| `tests/integration/*.test.ts` | `app/api/wallet/{award,purchase,exchange,withdraw}/route.ts`, `app/parent/shop/actions.ts` | direct handler import + `new NextRequest(...)` invocation | WIRED | Pattern confirmed consistent across all three summaries; production route/action files were NOT modified (git diff confirms only test files added) |

### Requirements Coverage

No `LP-SC*` or v5.0 requirement IDs exist yet in `.planning/REQUIREMENTS.md` (file currently only covers v4.0 PWA/UX/Localization/Security/Desktop requirements). Phase 5.1 plans map directly to ROADMAP Success Criteria (LP-SC1..4 as referenced in plan frontmatter `requirements-completed`) rather than a separate REQUIREMENTS.md ledger. No orphaned requirements found for this phase.

### Anti-Patterns Found

None blocking. Scanned every file touched by this phase (`lib/analytics.ts`, `components/AnalyticsProvider.tsx`, `sentry.*.config.ts`, `instrumentation.ts`, `next.config.js`, `app/api/health/route.ts`, `app/layout.tsx`, `app/manifest.ts`, `README.md`, all four `tests/integration/*.ts` files, `vitest.config.ts`) for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`/empty-return patterns. One incidental match: `tests/integration/family-fixture.ts:10` contains the word "placeholder" but refers legitimately to `hasIntegrationEnv`'s guard against placeholder env values (not a stub marker). The "KNOWN GAP" marker in `exchange-withdraw.test.ts` is an intentional, documented pre-existing production defect (withdrawal double-spend, `/api/wallet/withdraw/approve` never existed) that this phase's tests deliberately document rather than silently hide — not a phase-5.1 regression, and per the plan-checker's own revision (commit `fab15c5`), 05.1-06 explicitly did NOT create a phantom approve route. Confirmed via `find app/api/wallet -iname "*approve*"` → no results.

### Integrity Constraints (from verification context)

| Constraint | Status | Evidence |
|---|---|---|
| No production money-code modified by test plans | VERIFIED | `git diff --stat` from before 05.1-01 to HEAD touches only: `app/api/health/route.ts`, `app/layout.tsx`, `components/AnalyticsProvider.tsx`, `components/DailyModal.tsx`, `components/kid/KidDayFillForm.tsx`, `instrumentation.ts`, `lib/analytics.ts`, `next.config.js`, `package.json`/`package-lock.json`, `sentry.*.config.ts`, `tests/integration/*`, `vitest.config.ts`, README.md, plus `.planning/*`. None of `app/api/wallet/*`, `lib/repositories/wallet.repo.ts`, `lib/supabase/admin.ts`, `app/parent/shop/actions.ts` appear in the diff. |
| 05.1-06 did NOT create `/api/wallet/withdraw/approve` | VERIFIED | `find app/api/wallet -iname "*approve*"` returns nothing; only `app/api/wallet/{settings,exchange,award,rewards,p2p,purchase,withdraw}` directories exist |
| `npx tsc --noEmit` clean | VERIFIED | Ran myself — zero output, exit 0 |
| `npm test` green without env | VERIFIED | Ran myself — `3 passed \| 3 skipped test files, 31 passed \| 18 skipped tests` |
| STATE.md / ROADMAP.md reflect 6/6 plans complete | VERIFIED | ROADMAP.md: `[x] Phase 5.1: launch-prep ... (completed 2026-07-06)`, `Plans: 6/6 plans complete`, all six plan checkboxes `[x]`. STATE.md: `Phase 05.1 (launch-prep): 05.1-06 COMPLETE ... LP-SC3 fully met`. |
| No secrets in committed files/commits | VERIFIED | Grepped phase directory + last 20 commits' diffs for JWT-shaped strings (`eyJ...`) and `postgres://user:pass@` patterns — only placeholder text (`<service-role-key>`) found, no real secret material |

### Human Verification Required

### 1. Sentry live capture confirmation

**Test:** Add `NEXT_PUBLIC_SENTRY_DSN` and `SENTRY_DSN` to Vercel Production env, redeploy, then visit `https://kids-motivation.vercel.app/api/health?boom=1`
**Expected:** HTTP 500 response; a "Sentry test error: /api/health?boom=1" issue appears in the Sentry project's Issues stream within a minute or two
**Why human:** Requires creating a Sentry account/project (external service) and adding secrets to Vercel — the code path is fully built and DSN-guarded, but no DSN has been provisioned yet. 05.1-03-SUMMARY explicitly lists this as "not yet performed."

### 2. PostHog live event confirmation

**Test:** Add `NEXT_PUBLIC_POSTHOG_KEY` to Vercel Production env, redeploy, browse the site, then save a day from `/kid/day` or a parent daily modal
**Expected:** PostHog → Activity → Live Events shows a `$pageview` event and a `day_saved` event with `properties.role` = `kid` or `parent`
**Why human:** Requires creating a PostHog account/project (external service) and adding a secret to Vercel — the code path is fully built and key-guarded, but no key has been provisioned yet. 05.1-04-SUMMARY explicitly lists this as "not yet performed."

## Gaps Summary

No code-level gaps. All four ROADMAP success criteria are code-complete and verified against the actual codebase (not just SUMMARY claims): key rotation was operator-executed and verify-scripts pass, Sentry/PostHog wiring is fully DSN/key-guarded and no-op-safe with correct wiring into the day-save flows, the money integration test suite (18 integration tests across award/purchase/exchange-withdraw, plus 31 green unit tests) exists and matches the plans, and FamilyCoins naming is consistent across layout metadata, manifest, and README.

The one open item is **operator action, not a code defect**: SC2's literal "captures a test error from prod" / "records a test event" clause cannot be true until the operator provisions a Sentry DSN and a PostHog key in Vercel and performs the two confirmation steps documented in 05.1-03-SUMMARY and 05.1-04-SUMMARY. This is why overall status is `human_needed` rather than `passed` — per verification methodology, any phase with an open human-verification item cannot be marked `passed` even when all code truths check out. This is expected, scoped, and non-blocking for proceeding to Phase 5.2 (which does not depend on live Sentry/PostHog capture); the operator should complete the two steps above at convenience before relying on production error/analytics visibility.

The pre-existing withdrawal double-spend defect (no `/api/wallet/withdraw/approve` route; documented via the "KNOWN GAP" test marker) is **not** a regression or gap introduced by this phase — it was discovered by the plan-checker before 05.1-06 was written, deliberately descoped from creating a phantom route, and is correctly carried forward in STATE.md Blockers as future work (candidate for a 5.1 gap-closure plan or early 5.2).

---

*Verified: 2026-07-06T07:07:36Z*
*Verifier: Claude (gsd-verifier)*
