---
phase: 05.5-year-calendar
plan: 07
type: execute
wave: 4
depends_on: [05.5-01, 05.5-02, 05.5-03, 05.5-04, 05.5-05, 05.5-06]
files_modified:
  - scripts/verify-streaks-rls.mjs
autonomous: true
requirements: [05.5-SC1, 05.5-SC5]

must_haves:
  truths:
    - "All three phase migrations are applied to the live database, so family_calendar exists, vacation_periods has preset_id, and streaks is SELECT-only for clients (D-05, D-02, D-12.3)"
    - "A verification script proves a signed-in child CAN read but CANNOT write the streaks table (D-12.3)"
    - "The family_calendar table and vacation_periods.preset_id column are confirmed present in the live DB"
  artifacts:
    - path: "scripts/verify-streaks-rls.mjs"
      provides: "E2E streaks RLS lock verification"
      contains: "streaks"
  key_links:
    - from: "scripts/verify-streaks-rls.mjs"
      to: "streaks table (live DB)"
      via: "signed-in child update attempt"
      pattern: "from\\('streaks'\\)"
---

<objective>
Apply the three phase migrations to the live database and verify the schema landed correctly — most importantly that the `streaks` SELECT-only RLS lock (CR-01 D-12.3) is enforced against a real signed-in child. This is the mandatory [BLOCKING] schema-push step: build and type checks pass without it (types do not come from the live DB), so the phase cannot truly pass verification until the migrations are live.

Purpose: Closes the CR-01 trust boundary end-to-end (the server-side write path landed in 05.5-02; this locks the client out) and makes the calendar tables real.

Output: applied migrations + `scripts/verify-streaks-rls.mjs` proving the lock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05.5-year-calendar/05.5-CONTEXT.md
@.planning/phases/05.5-year-calendar/05.5-PATTERNS.md

<interfaces>
Migration files authored in 05.5-01 (in dependency-safe order):
  1. supabase/migrations/05.5-01-family-calendar.sql
  2. supabase/migrations/05.5-02-vacation-periods-preset-marker.sql
  3. supabase/migrations/05.5-03-streaks-rls-readonly.sql  (apply LAST — after 05.5-02 server-side updateStreaks is in the deployed code)

Apply mechanism (CLAUDE.md-documented, uses SUPABASE_DB_URL from .env.local):
  npm i pg --no-save && node --env-file=.env.local -e '<pg apply one-liner per file>'

Verify-script analog: scripts/verify-wallet-rls.mjs (creates throwaway family+child+auth user, signs in as child, asserts read-OK / write-denied, cleans up).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 [BLOCKING]: Apply the three migrations to the live DB</name>
  <files>(no source files — DB state change)</files>
  <read_first>
    - CLAUDE.md (§"Applying DB migrations" — the exact pg apply one-liner and the SUPABASE_DB_URL requirement)
    - supabase/migrations/05.5-01-family-calendar.sql
    - supabase/migrations/05.5-02-vacation-periods-preset-marker.sql
    - supabase/migrations/05.5-03-streaks-rls-readonly.sql
  </read_first>
  <action>
    First confirm `SUPABASE_DB_URL` is present in `.env.local` (`grep -q SUPABASE_DB_URL .env.local`). If it is ABSENT, STOP and surface manual instructions (apply the three files in order via the Supabase SQL Editor) — do not fabricate success. If present, run `npm i pg --no-save`, then apply the three migrations IN ORDER using the CLAUDE.md pg one-liner (substitute each filename): first `05.5-01-family-calendar.sql`, then `05.5-02-vacation-periods-preset-marker.sql`, then `05.5-03-streaks-rls-readonly.sql` LAST (the streaks lock must land after the server-side updateStreaks code from 05.5-02 is deployed, mirroring 04.4-03's deploy-order note). Each file is idempotent (`IF NOT EXISTS` / `DROP POLICY IF EXISTS`), so a re-run is safe. Capture and check each `applied` confirmation.

    `pg` is the project-documented migration tool (CLAUDE.md Commands), installed ephemerally with `--no-save`; it is not a new project dependency and is not persisted to package.json.
  </action>
  <verify>
    <automated>node --env-file=.env.local -e "import('pg').then(async({default:pg})=>{const c=new pg.Client({connectionString:process.env.SUPABASE_DB_URL});await c.connect();const t=await c.query(\"select to_regclass('public.family_calendar') as t\");const col=await c.query(\"select 1 from information_schema.columns where table_name='vacation_periods' and column_name='preset_id'\");const pol=await c.query(\"select cmd from pg_policies where tablename='streaks'\");if(!t.rows[0].t)throw new Error('family_calendar missing');if(col.rowCount===0)throw new Error('preset_id missing');const cmds=pol.rows.map(r=>r.cmd);if(cmds.some(x=>x!=='SELECT'))throw new Error('streaks has a non-SELECT policy: '+cmds.join(','));console.log('SCHEMA_APPLIED', JSON.stringify(cmds));await c.end()})"</automated>
  </verify>
  <acceptance_criteria>
    - `to_regclass('public.family_calendar')` is non-null (table exists).
    - `vacation_periods` has a `preset_id` column.
    - Every policy on `streaks` has `cmd = 'SELECT'` (no INSERT/UPDATE/DELETE policy for clients).
    - If `SUPABASE_DB_URL` is unset, the task did NOT claim success and instead surfaced manual apply instructions.
  </acceptance_criteria>
  <done>All three migrations are live; schema-presence checks pass.</done>
</task>

<task type="auto">
  <name>Task 2: Write and run the streaks RLS verification script</name>
  <files>scripts/verify-streaks-rls.mjs</files>
  <read_first>
    - scripts/verify-wallet-rls.mjs (full script — throwaway family+child+auth-user seed, child sign-in, read-OK / write-denied assertions, cleanup; mirror it for streaks)
    - supabase/migrations/05.5-03-streaks-rls-readonly.sql (the policy being verified)
  </read_first>
  <action>
    Create `scripts/verify-streaks-rls.mjs` mirroring `verify-wallet-rls.mjs`: seed an isolated throwaway family + child + auth user via the service-role client, insert a `streaks` row for that child (service-role, with `family_id`), sign in as the child (anon client), then assert: (1) the child CAN SELECT its own streaks row; (2) an attempted `update({ current_count: 99999 })` by the child changes nothing (re-read via service-role shows the original value — RLS denies the write); (3) an attempted client `insert` of a new streaks row is denied. Clean up all seeded rows/users in a `finally`, exactly like the analog. Exit non-zero on any failed assertion. Then run it: `node --env-file=.env.local scripts/verify-streaks-rls.mjs`.
  </action>
  <verify>
    <automated>node --env-file=.env.local scripts/verify-streaks-rls.mjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-streaks-rls.mjs` exists and mirrors the verify-wallet-rls structure (seed, sign-in, assert, cleanup).
    - Running it prints a passing result: child CAN read own streaks, CANNOT update, CANNOT insert.
    - The script exits 0 (all assertions pass) and leaves no residual test rows.
  </acceptance_criteria>
  <done>The streaks SELECT-only lock is proven against a real signed-in child (D-12.3 closed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local runner → live DB | Migrations applied via service-role Postgres connection (SUPABASE_DB_URL). |
| child (signed-in) → streaks | The exact boundary being verified as locked. |
| package install (pg) → build env | Ephemeral `--no-save` install of the documented migration tool. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-18 | Tampering | streaks client write | mitigate | Migration 05.5-03 applied; verified by `verify-streaks-rls.mjs` that a signed-in child cannot update/insert streaks (D-12.3). |
| T-05.5-19 | Repudiation | migration apply | mitigate | Each pg apply prints an `applied` confirmation and the post-apply schema query asserts the actual live state (not just file presence). |
| T-05.5-SC | Tampering | pg install | accept | `pg` is the CLAUDE.md-documented migration mechanism, installed with `--no-save` (not persisted, not a new tracked dependency); a well-established, widely-used Postgres client. No new supply-chain surface introduced by this phase; legitimacy pre-established by project convention. |
</threat_model>

<verification>
- Live DB: family_calendar exists, vacation_periods.preset_id exists, streaks policies are SELECT-only (Task 1 gate).
- `verify-streaks-rls.mjs` passes (Task 2 gate).
- If SUPABASE_DB_URL is unset, manual-apply path surfaced instead of a false pass.
</verification>

<success_criteria>
- All three migrations live (D-05, D-02, D-12.3).
- Streaks SELECT-only lock proven end-to-end (D-12.3, SC5).
- Calendar tables real so SC1 persistence works against prod.
</success_criteria>

<output>
Create `.planning/phases/05.5-year-calendar/05.5-07-SUMMARY.md` when done.
</output>
