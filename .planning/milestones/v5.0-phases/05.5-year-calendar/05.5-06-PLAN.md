---
phase: 05.5-year-calendar
plan: 06
type: execute
wave: 3
depends_on: [05.5-01, 05.5-02]
files_modified:
  - app/kid/day/page.tsx
  - components/DailyModal.tsx
  - components/kid/KidDayFillForm.tsx
autonomous: true
requirements: [05.5-SC4]

must_haves:
  truths:
    - "The kid day screen loads family_calendar alongside vacation periods and passes it to getDayType, so weekend/vacation display comes from configurable data (SC4, D-05, D-07)"
    - "The parent DailyModal and kid KidDayFillForm both recompute day type from family_calendar (SC4)"
    - "When family_calendar is absent, the existing hardcoded sat+sun fallback still applies (D-08)"
    - "Sick marking is unchanged — both parent and child can still set is_sick in the day form, with no new approval step (D-11)"
  artifacts:
    - path: "app/kid/day/page.tsx"
      provides: "family_calendar-aware day-type resolution"
      contains: "getFamilyCalendar"
    - path: "components/DailyModal.tsx"
      provides: "familyCalendar state + getDayType arg"
      contains: "familyCalendar"
    - path: "components/kid/KidDayFillForm.tsx"
      provides: "familyCalendar state + getDayType arg"
      contains: "familyCalendar"
  key_links:
    - from: "app/kid/day/page.tsx"
      to: "lib/day-type.ts"
      via: "getDayType(..., familyCalendar)"
      pattern: "getDayType\\("
    - from: "components/DailyModal.tsx"
      to: "lib/repositories/calendar.repo.ts"
      via: "getFamilyCalendar"
      pattern: "getFamilyCalendar"
---

<objective>
Make the kid day screen and both day-fill forms reflect the configurable calendar automatically. Each of the three day-type call sites loads `family_calendar` alongside vacation periods and passes it to the now data-driven `getDayType`, with the existing hardcoded sat+sun fallback preserved when no calendar row exists.

Purpose: Delivers SC4 (getDayType resolves entirely from this data; the kid day screen reflects vacation/weekend automatically). Depends on 05.5-02 because it edits the same two form files (KidDayFillForm, DailyModal) that plan 05.5-02 already modified — this plan runs after, in the next wave.

Output: family_calendar loading + getDayType wiring in `app/kid/day/page.tsx`, `components/DailyModal.tsx`, and `components/kid/KidDayFillForm.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05.5-year-calendar/05.5-CONTEXT.md
@.planning/phases/05.5-year-calendar/05.5-PATTERNS.md

<interfaces>
From 05.5-01 (lib/day-type.ts): getDayType(date, isSick, vacationPeriods, childId?, t?, familyCalendar?)
From 05.5-01 (lib/repositories/calendar.repo.ts): getFamilyCalendar(familyId): Promise<FamilyCalendar | null>
Note: these three files were also edited in 05.5-02 (streak-call removal) — start from that post-05.5-02 state.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire family_calendar into the kid day page</name>
  <files>app/kid/day/page.tsx</files>
  <read_first>
    - app/kid/day/page.tsx (the load-and-compute-day-type block at ~lines 96-106 and the Promise.all at ~lines 84-88; line 7 already imports localDateString)
    - lib/repositories/calendar.repo.ts (getFamilyCalendar)
    - lib/day-type.ts (new getDayType signature)
  </read_first>
  <action>
    Import `getFamilyCalendar` from `@/lib/repositories/calendar.repo`. In the existing day-type block, load the family calendar in parallel with `getVacationPeriods` (add it to the existing `Promise.all` or issue a parallel `getFamilyCalendar((childData as any).family_id)` call) and pass the result as the new 6th `familyCalendar` argument to `getDayType(today, false, vacations ?? [], resolvedId, undefined, familyCalendar)`. Leave the catch-fallback branch (hardcoded `dow === 0 || dow === 6`) unchanged — it is the D-08 legacy-default path when the calendar load fails or is absent.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "kid/day/page" || echo "TYPECHECK_CLEAN"; grep -q "getFamilyCalendar" app/kid/day/page.tsx && echo "PAGE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `app/kid/day/page.tsx` imports and calls `getFamilyCalendar` and passes its result to `getDayType`.
    - The catch-fallback hardcoded sat+sun branch is preserved (D-08).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The kid day page resolves day type from family_calendar with a legacy fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Wire family_calendar into both day forms</name>
  <files>components/DailyModal.tsx, components/kid/KidDayFillForm.tsx</files>
  <read_first>
    - components/DailyModal.tsx (post-05.5-02 state; the loadData() vacation-load block + the getDayType useEffect at ~lines 160-177; the is_sick handling — confirm it stays parent+child settable, D-11)
    - components/kid/KidDayFillForm.tsx (post-05.5-02 state; its mirror load block + getDayType call; the child-side is_sick control, D-11)
    - lib/repositories/calendar.repo.ts (getFamilyCalendar)
    - lib/day-type.ts (getDayType signature)
  </read_first>
  <action>
    In BOTH `DailyModal.tsx` and `KidDayFillForm.tsx`: add a `familyCalendar` state (`FamilyCalendar | null`, default null). In `loadData()`, load it alongside the vacation periods using the same try/catch-to-null fallback shape (`try { fc = await getFamilyCalendar(familyId) } catch { fc = null }`) and `setFamilyCalendar(fc)`. Pass `familyCalendar` as the 6th argument to every `getDayType(date, isSick, vacationPeriods, childId, t, familyCalendar)` call and add `familyCalendar` to the dependency array of the `getDayType`-recompute `useEffect`. Do NOT alter the `is_sick` control or its save path — both parent (DailyModal) and child (KidDayFillForm) keep setting `is_sick` exactly as before, with no new approval mechanism (D-11). Do NOT reintroduce any client-side `updateStreaks` call (removed in 05.5-02).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "DailyModal\.tsx|KidDayFillForm" || echo "TYPECHECK_CLEAN"; grep -q "familyCalendar" components/DailyModal.tsx && grep -q "familyCalendar" components/kid/KidDayFillForm.tsx && grep -q "getFamilyCalendar" components/DailyModal.tsx && grep -q "getFamilyCalendar" components/kid/KidDayFillForm.tsx && ! grep -q "updateStreaks" components/DailyModal.tsx && ! grep -q "updateStreaks" components/kid/KidDayFillForm.tsx && echo "FORMS_OK"</automated>
  </verify>
  <acceptance_criteria>
    - Both forms load `family_calendar` and pass it to `getDayType`; `familyCalendar` is in the recompute effect's deps.
    - The `is_sick` control and save path are unchanged in both forms (D-11) — still settable by parent and child, no new approval.
    - Neither form contains `updateStreaks` (confirming 05.5-02's removal held).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>Both day forms recompute day type from family_calendar; sick marking behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → family_calendar (read) | Forms read the family's calendar via the client repo under SELECT RLS. |
| is_sick input → day row | Parent or child sets the sick flag; already an existing end-to-end path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-16 | Information Disclosure | getFamilyCalendar read | mitigate | family_calendar SELECT RLS (05.5-01) confines reads to the caller's family; a form can only load its own family's calendar. |
| T-05.5-17 | Tampering | is_sick self-marking | accept | Per D-11, sick marking intentionally stays open to both parent and child with no approval; downstream streak effect is bounded (sick is transparent — never mints streak value, per 05.5-02 D-09). No coin is granted for a sick day. |
| T-05.5-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; N/A. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds.
- All three call sites load family_calendar and pass it to getDayType.
- No client-side updateStreaks remains.
</verification>

<success_criteria>
- Kid day screen + both forms reflect vacation/weekend from configurable data (SC4, D-05, D-07).
- Legacy sat+sun fallback preserved when no calendar row (D-08).
- Sick marking unchanged for both roles (D-11).
</success_criteria>

<output>
Create `.planning/phases/05.5-year-calendar/05.5-06-SUMMARY.md` when done.
</output>
