---
phase: 05.5-year-calendar
plan: 01
subsystem: database
tags: [supabase, postgres, rls, typescript, vitest, day-type]

# Dependency graph
requires:
  - phase: 05.4-streak-settings
    provides: streaks table with family_id (fix-family-id-and-streaks.sql), 04.4-03 SELECT-only RLS lock pattern to mirror
provides:
  - family_calendar table DDL + family-isolation RLS (not yet applied to live DB)
  - vacation_periods.preset_id marker column migration (not yet applied)
  - streaks SELECT-only RLS lock migration (not yet applied; deploy-order gated on plan 05.5-02)
  - lib/models/calendar.types.ts (TermMode, FamilyCalendar)
  - lib/repositories/calendar.repo.ts (getFamilyCalendar, upsertFamilyCalendar)
  - data-driven lib/day-type.ts with familyCalendar param and legacy fallback
affects: [05.5-02-cr01-streak-fix, 05.5-03, 05.5-04, 05.5-05, 05.5-06, 05.5-07-apply-migrations, phase-5.6-day-blocks]

# Tech tracking
tech-stack:
  added: []
  patterns: [family-isolation RLS mirrored from 05.2-01-room-tasks, SELECT-only RLS lock mirrored from 04.4-03, client-write repo mirrored from room.repo.ts]

key-files:
  created:
    - supabase/migrations/05.5-01-family-calendar.sql
    - supabase/migrations/05.5-02-vacation-periods-preset-marker.sql
    - supabase/migrations/05.5-03-streaks-rls-readonly.sql
    - lib/models/calendar.types.ts
    - lib/repositories/calendar.repo.ts
    - tests/day-type.test.ts
  modified:
    - lib/day-type.ts

key-decisions:
  - "family_calendar is client-writable family-isolation RLS (NOT a money table), per D-05 — mirrors room_tasks, not wallet tables"
  - "streaks RLS migration is authored now but its header states it MUST be applied AFTER the server-side updateStreaks path (plan 05.5-02) lands, mirroring 04.4-03's own deploy-order note"
  - "getDayType priority order: sick > explicit vacation period > out-of-year-bounds vacation (D-07) > weekend (familyCalendar.weekend_days or legacy sat+sun) > school"
  - "Migrations are authored only in this plan, not applied to the live DB — application happens in plan 05.5-07's blocking task"

patterns-established:
  - "Family-config (non-money) table RLS: SELECT/INSERT/UPDATE/DELETE all scoped by family_id IN (family_members WHERE user_id = auth.uid()), copied verbatim from 05.2-01-room-tasks.sql"
  - "Client-write repository convention: import the browser supabase singleton only, explicit comment forbidding the admin/service-role client import"

requirements-completed: [05.5-SC1, 05.5-SC2, 05.5-SC4, 05.5-SC5]

# Metrics
duration: 10min
completed: 2026-07-12
---

# Phase 05.5 Plan 01: Year Calendar Data Foundation Summary

**family_calendar table + RLS, vacation_periods preset_id marker, streaks SELECT-only RLS migration, and a data-driven getDayType(familyCalendar) rewrite with full legacy fallback**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-07-12T19:11:00Z (approx, per first migration commit at 22:11 local)
- **Completed:** 2026-07-12T19:20:46Z
- **Tasks:** 3 (Task 3 was TDD: RED + GREEN commits)
- **Files modified:** 7 (3 new migrations, 2 new lib files, 1 new test file, 1 modified lib file)

## Accomplishments
- Authored three idempotent migration SQL files (not yet applied to the live DB — that happens in plan 05.5-07): `family_calendar` table with family-isolation RLS, `vacation_periods.preset_id` marker column, and the `streaks` SELECT-only RLS lock (CR-01 D-12.3, mirroring 04.4-03).
- Created `lib/models/calendar.types.ts` (`TermMode`, `FamilyCalendar`) and `lib/repositories/calendar.repo.ts` (`getFamilyCalendar`/`upsertFamilyCalendar`), client-write only per D-05.
- Rewrote `lib/day-type.ts`'s `getDayType` to accept an optional `familyCalendar` 6th parameter, resolving weekend days and out-of-school-year dates from calendar data with an exact legacy fallback when no calendar exists (D-08).
- TDD-verified via `tests/day-type.test.ts` (5 cases, all green): legacy fallback, custom weekend_days, out-of-year-bounds vacation (D-07), sick-wins-all priority, and explicit-period priority preserved.

## Task Commits

Each task was committed atomically:

1. **Task 1: Write the three migration SQL files** - `cd69ac8` (feat)
2. **Task 2: Create calendar types and repository** - `b27237d` (feat)
3. **Task 3: Rewrite getDayType to be data-driven** - `452019d` (test, RED) → `221833b` (feat, GREEN)

**Plan metadata:** committed separately after this SUMMARY (see final commit in worktree log).

## Files Created/Modified
- `supabase/migrations/05.5-01-family-calendar.sql` - `family_calendar` table (one row/family, `UNIQUE(family_id)`) + 4 family-isolation RLS policies
- `supabase/migrations/05.5-02-vacation-periods-preset-marker.sql` - `vacation_periods.preset_id` nullable TEXT column
- `supabase/migrations/05.5-03-streaks-rls-readonly.sql` - `streaks` SELECT-only RLS lock (deploy-order gated on 05.5-02's server-side write path)
- `lib/models/calendar.types.ts` - `TermMode`, `FamilyCalendar` types
- `lib/repositories/calendar.repo.ts` - `getFamilyCalendar` (`.maybeSingle()`), `upsertFamilyCalendar` (`.upsert(..., { onConflict: 'family_id' })`)
- `lib/day-type.ts` - `getDayType` gains optional `familyCalendar?: FamilyCalendar | null` 6th param; new priority order (sick > explicit period > out-of-year vacation > weekend > school); `DayTypeInfo`/`isNonSchoolDay` unchanged
- `tests/day-type.test.ts` - 5 behavior cases covering the `<behavior>` block exactly

## Decisions Made
- Followed the plan's exact interface contracts (types, repo signatures, `getDayType` param order) verbatim — no deviation needed since the plan pre-specified the contracts precisely.
- Used single-space column formatting in the `family_calendar` DDL (rather than aligned columns) to keep the plan's literal grep-gate strings (`weekend_days INT[] NOT NULL DEFAULT`) matching exactly — aligned whitespace would have broken the automated verify gate.
- Reworded a repo-file comment that mentioned `lib/supabase/admin` in prose (which false-positived the "no admin import" grep gate) to describe the constraint without using the literal substring.

## Deviations from Plan

None — plan executed exactly as written. Two minor self-corrections were made during execution (both caught by the plan's own verify gates before committing, not after):
- Task 1: fixed DDL column whitespace so the required grep pattern matched exactly (no functional change).
- Task 2: reworded a comment to avoid a substring match against the "no admin import" negative-assertion grep gate (no functional change — the file never imported the admin client).
- Task 3: the initial `weekend_days=[5,6]` test used April 2026 dates, which fall before the test calendar's default `year_start` of `2026-09-01` — the out-of-year-bounds check (D-07) correctly took priority and returned `vacation` instead of `weekend`, which was a test-authoring bug, not an implementation bug. Fixed by choosing November 2026 dates that fall within the calendar's year bounds, isolating the weekend_days behavior being tested.

**Total deviations:** 0 (all adjustments were pre-commit self-corrections to satisfy the plan's own verify gates, not scope changes)
**Impact on plan:** None — behavior, contracts, and file set exactly match the plan.

## Issues Encountered
None beyond the self-corrections documented above.

## User Setup Required
None - no external service configuration required. Migrations are authored only; DB application is deferred to plan 05.5-07 per the plan's explicit instruction.

## Next Phase Readiness
- `lib/models/calendar.types.ts` and `lib/repositories/calendar.repo.ts` are ready for the CR-01 fix plan (05.5-02) and the settings UI plans (05.5-04/05/06) to consume.
- `getDayType`'s new signature is stable and backward-compatible (existing 5-arg call sites still work); downstream call-site updates (`app/kid/day/page.tsx`, `components/DailyModal.tsx`, `components/kid/KidDayFillForm.tsx`) are left for their respective plans per the pattern map.
- The three migration files exist on disk but are NOT applied to the live DB — plan 05.5-07 must apply them in order (05.5-01 family_calendar/preset marker before 05.5-03 streaks lock, and 05.5-03 specifically only after 05.5-02's server-side `updateStreaks` path ships).
- No blockers for downstream plans in this wave.

---
*Phase: 05.5-year-calendar*
*Completed: 2026-07-12*
