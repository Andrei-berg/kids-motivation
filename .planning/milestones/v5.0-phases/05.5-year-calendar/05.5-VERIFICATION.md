---
phase: 05.5-year-calendar
verified: 2026-07-13T21:05:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
human_verification_recommended: # non-blocking browser checks (operator ordered phase closure; code halves fully verified)
  - test: "Parent Center → Settings → Schedule → Calendar sub-tab: set year start/end, term mode, weekend days, region; save; reload"
    expected: "Card renders with correct RU/EN labels, values persist and re-load"
    why_human: "Visual rendering + form UX can't be exercised via grep; the wiring, RLS, i18n keys, and persistence path are all code-verified"
  - test: "Parent Center → Settings → Vacations: pick a preset, apply twice; confirm Replace vs Add-missing prompt; check preset badge on rows"
    expected: "Second apply prompts; replace keeps manual rows; preset rows show the badge; Settings→Calendar region select reflects the applied preset"
    why_human: "confirm()-dialog flow and badge appearance are visual; applyPreset logic itself is code-verified"
  - test: "On a configured weekend/vacation date, open /kid/day"
    expected: "Kid day screen shows weekend/vacation mode (no school grade section)"
    why_human: "End-to-end visual check; the data path (getFamilyCalendar → getDayType → dayType prop) is code-verified and unit-tested"
---

# Phase 5.5: year-calendar Verification Report

**Phase Goal:** Day types come from a configurable calendar: school year dates, regional vacation presets, custom periods, family weekend days.
**Verified:** 2026-07-13T21:05:00Z
**Status:** passed
**Re-verification:** No — initial verification (a prior run was interrupted before writing a report; no partial file existed)

Verified against the **post-review-fix state** (05.5-REVIEW.md: 3 Critical + 8 Warning, all 11 fixed in commits `0dd4e91..1679b84` — every fix commit confirmed present in git; fixed behavior re-verified below, not trusted from the fix report).

## Goal Achievement

### Observable Truths

| # | Truth (ROADMAP Success Criterion) | Status | Evidence |
|---|------|--------|----------|
| 1 | Parent sets school-year start/end and quarters/trimesters mode | VERIFIED (code) | `components/settings/CalendarSettingsManager.tsx` (248 lines, real editor, not a stub): date inputs for `year_start`/`year_end`, quarters/trimesters toggle, persisted via `upsertFamilyCalendar` (`lib/repositories/calendar.repo.ts:32-46` → `family_calendar` upsert keyed on `family_id`). Mounted in Parent Center at `components/parent-center/screens/Settings.tsx:407` (`sub === 'calendar'`). CR-01 fix confirmed: all `t()` calls use the `settings.calendarSettingsManager.*` namespace (grep for unprefixed calls: zero matches) and both locales carry 19 matching keys. Live DB (independent `pg` query, not SUMMARY): `family_calendar` table exists with all 9 expected columns; write policies (INSERT/UPDATE/DELETE) live-verified to require `role IN ('parent','extended')` (WR-01 / migration 05.5-04 applied). Browser rendering listed as a non-blocking human check. |
| 2 | Choosing a region fills vacations from a bundled JSON directory; every period is manually editable and the manual value wins | VERIFIED | `data/vacation-presets/` ships 4 static-imported JSON presets (ru-quarters, ru-moscow-trimesters, kz, by) covering the 2026-27 **and** 2027-28 school years (ru-quarters has 6 periods across both years; dates inspected). `lib/vacation-presets.ts` `applyPreset(familyId, presetId, mode)` materializes rows into `vacation_periods` with `preset_id` set; `replace` is insert-first-then-delete-old scoped to `family_id + preset_id + NOT IN(newIds)` (WR-04 fix confirmed in code) so manual rows (`preset_id` null) are never touched; `add-missing` diffs by `(start_date,end_date,name)`. "Manual wins" holds structurally: materialized rows are ordinary `vacation_periods` rows that `PeriodsManager` edits/deletes via `updateVacationPeriod`/`deleteVacationPeriod` (lines 120, 141), and `getDayType` reads only the rows. `PeriodsManager` has the real preset picker (`listPresets`/`applyPreset`, line 7/157), the Replace-vs-Add-missing `confirm()` prompt when preset rows already exist (lines 147-152), the preset-row badge (line 209), and syncs `family_calendar.region_preset` on apply (line 160, WARNING-1 resolution) — the same field CalendarSettingsManager's region select reads. Live DB: `vacation_periods.preset_id` column EXISTS; WR-03 backfill applied (0 remaining rows where `child_filter` holds a `family_members.id`); child picker now emits `children.id` (`PeriodsManager.tsx:73-81`). |
| 3 | Weekend days are configurable per family (default sat+sun); a sick day pauses school blocks without burning streaks | VERIFIED | DDL: `weekend_days INT[] NOT NULL DEFAULT '{0,6}'` (05.5-01, line 20) = Sun+Sat default. `getDayType` resolves weekend from `familyCalendar.weekend_days.includes(dow)` with hardcoded sat+sun fallback when no row exists (`lib/day-type.ts:69`); unit test proves a `[5,6]` calendar makes Friday a weekend and Sunday a school day. Streak pause: all four calculators in `lib/services/streaks.service.ts` use the anchored-run algorithm (CR-02/CR-03 fix verified in current code, lines 123-261) — sick days are transparent (skip, neither increment nor reset) for room/sport/behavior; weekend/vacation/sick transparent for study; a transparent TODAY carries the run (`anchored` flag) instead of zeroing `current`, and `updateStreak(..., todayTransparent)` suppresses the spurious `'broken'` event (line 285). 22 streak unit tests including the two transparent-TODAY CR-02 cases pass (run live in this verification, 73/73 total). Streaks are server-side only: `updateStreaks(admin, ...)` called exclusively from the guarded `/api/wallet/award` route and `requireParent()`-guarded `repair-achievements.ts` (repo-wide grep: no other call sites); `scripts/verify-streaks-rls.mjs` run live against prod — child CAN read, CANNOT update, CANNOT insert (all 3 assertions passed); live `pg_policies` shows `streaks` has only a SELECT policy. |
| 4 | `getDayType` resolves entirely from this data; the kid day screen reflects vacation/weekend automatically | VERIFIED (code) | `getDayType(date, isSick, vacationPeriods, childId?, t?, familyCalendar?)` resolves purely from data: sick > explicit vacation period (child_filter-aware) > out-of-year-bounds→vacation (D-07, skipped when bounds are NULL per WR-07 — nullable types confirmed in `calendar.types.ts`) > configurable weekend > school. No hardcoded coin calculators remain in the file (WR-05: `calcReadingCoins` etc. removed — confirmed). `app/kid/day/page.tsx:101-106` loads `getFamilyCalendar(familyId)` in parallel with vacation periods and passes it as the 6th `getDayType` arg; the resulting `dayType` drives the screen and is passed as a prop to `KidDayFillForm` (which by design does not compute day type itself — documented deviation in 05.5-06-SUMMARY, functionally equivalent, no duplicated logic). `components/DailyModal.tsx` holds `familyCalendar` state and passes it at both `getDayType` call sites (recompute effect line 171 and activities pre-filter line 315); D-08 catch-fallback preserved in both files. 5 day-type unit tests pass live. Visual kid-screen check listed as a non-blocking human item. |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/migrations/05.5-01-family-calendar.sql` | family_calendar table + family-isolation RLS | VERIFIED | `CREATE TABLE IF NOT EXISTS public.family_calendar` + 4 policies; **applied to prod** (table + columns confirmed via live information_schema query) |
| `supabase/migrations/05.5-02-vacation-periods-preset-marker.sql` | `preset_id` column | VERIFIED | `ADD COLUMN IF NOT EXISTS preset_id TEXT`; column confirmed live |
| `supabase/migrations/05.5-03-streaks-rls-readonly.sql` | streaks SELECT-only lock | VERIFIED | Applied; live pg_policies shows only `streaks_select_own_family`; E2E script passes |
| `supabase/migrations/05.5-04-calendar-writes-parent-only.sql` | parent/extended-only calendar writes (WR-01) | VERIFIED | Applied 2026-07-13; live policy quals for family_calendar + vacation_periods writes all check `role IN ('parent','extended')` |
| `supabase/migrations/05.5-05-vacation-periods-child-filter-backfill.sql` | child_filter identity backfill (WR-03) | VERIFIED | Applied; live query finds 0 remaining `family_members.id`-valued `child_filter` rows |
| `lib/models/calendar.types.ts` | `FamilyCalendar` + `TermMode`, nullable year/term (WR-07) | VERIFIED | `year_start/year_end: string \| null`, `term_mode: TermMode \| null` |
| `lib/repositories/calendar.repo.ts` | `getFamilyCalendar` / `upsertFamilyCalendar` | VERIFIED | Both exported; browser-client family-config path (not a money table); no admin import |
| `lib/day-type.ts` | data-driven `getDayType` with familyCalendar param | VERIFIED | 6-arg signature; D-07/D-08/WR-07 behavior; WR-05 calculators removed |
| `data/vacation-presets/*.json` + `index.ts` | RU×2/KZ/BY presets, 2026-27 + 2027-28 | VERIFIED | 4 files, static imports, 6-10 periods each, both school years present |
| `lib/vacation-presets.ts` | `listPresets` + `applyPreset(replace\|add-missing)` | VERIFIED | Both exported; insert-first replace (WR-04); preset_id marker on rows |
| `lib/vacation-api.ts` | `VacationPeriod.preset_id` field | VERIFIED | Line 17 + writable via update fields (line 67) |
| `lib/services/streaks.service.ts` | `updateStreaks(admin, childId, date)` + transparency | VERIFIED | Admin-client signature; anchored-run calculators; todayTransparent broken-event suppression |
| `app/api/wallet/award/route.ts` | server-validated streak-bonus date + folded updateStreaks + streakEvents response | VERIFIED | `serverToday ±1` gate (WR-06, line 247-248); `updateStreaks(admin,...)` line 65; `streakEvents` in JSON response line 263; settings-driven clamped bonus |
| `app/actions/repair-achievements.ts` | auth guard (WR-02) | VERIFIED | `await requireParent()` at line 27 |
| `components/settings/CalendarSettingsManager.tsx` | calendar settings editor | VERIFIED | 248 lines; correct i18n namespace (CR-01); all 5 fields wired to repo |
| `components/parent-center/screens/Settings.tsx` | 'calendar' sub-tab | VERIFIED | Line 407 renders `<CalendarSettingsManager/>` |
| `components/settings/PeriodsManager.tsx` | preset picker + dialog + badge + manual CRUD | VERIFIED | 340 lines; all present; child picker uses `children.id` (WR-03) |
| `app/kid/day/page.tsx` | family_calendar-aware day type | VERIFIED | `getFamilyCalendar` import + parallel load + 6th arg |
| `components/DailyModal.tsx` | familyCalendar state + getDayType args + WR-05/WR-08 | VERIFIED | State line 99, both call sites; noon-anchor weekday fix (WR-08); settings-driven coin preview |
| `components/kid/KidDayFillForm.tsx` | no browser streak write; streakEvents from award response | VERIFIED | `streakEvents` parsed from `/api/wallet/award` response (line 527); no `updateStreaks` call. Plan-06's literal "familyCalendar state" artifact was superseded by a documented deviation: dayType is a prop computed family_calendar-aware by the parent page (functionally equivalent, avoids duplicate logic) |
| `scripts/verify-streaks-rls.mjs` | E2E streaks RLS verification | VERIFIED | Exists; **run live in this verification** — all 3 assertions pass |
| `tests/day-type.test.ts` / `tests/streaks.test.ts` | unit coverage incl. CR-02 transparent-today | VERIFIED | 5 + 22 tests; transparent-TODAY cases present; all pass |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `lib/day-type.ts` | `lib/models/calendar.types.ts` | `import type FamilyCalendar` | WIRED | Line 6 |
| `lib/repositories/calendar.repo.ts` | `family_calendar` table | `supabase.from('family_calendar')` | WIRED | Read + upsert; table live |
| `app/api/wallet/award/route.ts` | `lib/services/streaks.service.ts` | `updateStreaks(admin, childId, date)` | WIRED | Line 65, inside `requireFamilyMember`/`assertChildInFamily` guard |
| `KidDayFillForm.tsx` / `DailyModal.tsx` | `/api/wallet/award` | `streakEvents` read from award response | WIRED | Both parse `streakEvents` and dispatch `notifyStreakEvents`; no client streak write remains |
| `lib/vacation-presets.ts` | `vacation_periods` table | insert with `preset_id` | WIRED | Lines 59-73; column live |
| `lib/vacation-presets.ts` | `data/vacation-presets/*.json` | static import of `PRESETS` | WIRED | Via `@/data/vacation-presets` index |
| `PeriodsManager.tsx` | `lib/vacation-presets.ts` | `listPresets` + `applyPreset(` | WIRED | Lines 7, 37, 157 |
| `PeriodsManager.tsx` | `calendar.repo.ts` | `upsertFamilyCalendar({ region_preset })` | WIRED | Line 160 — region pickers stay in sync (WARNING-1) |
| `Settings.tsx` | `CalendarSettingsManager.tsx` | sub-tab render | WIRED | Line 407 |
| `app/kid/day/page.tsx` | `lib/day-type.ts` | `getDayType(..., familyCalendar)` | WIRED | Lines 101-106; dayType prop flows to KidDayFillForm |
| `streaks.service.ts` | `lib/day-type.ts` | `dayTypeOf` resolver built from admin-loaded family_calendar + vacation_periods | WIRED | Lines 70-84 — streak transparency uses the same configurable data |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Type check | `npx tsc --noEmit` | Exit 0 | PASS |
| Full test suite incl. live-DB integration (re-run independently, not trusted from fix report) | `node --env-file=.env.local ./node_modules/.bin/vitest run` | **73/73 passed** (8 files; award/room-award/purchase/exchange-withdraw ran against prod) | PASS |
| Streaks RLS lock (live prod) | `node --env-file=.env.local scripts/verify-streaks-rls.mjs` | child READ ok, UPDATE denied, INSERT denied | PASS |
| `family_calendar` schema live | `pg` information_schema query | 9 columns present (id, family_id, year_start, year_end, term_mode, weekend_days, region_preset, created_at, updated_at) | PASS |
| `vacation_periods.preset_id` live | `pg` information_schema query | EXISTS | PASS |
| Parent-only calendar writes live (WR-01) | `pg_policies` qual inspection | All 3 write policies on family_calendar + vacation_periods write policies check `role IN ('parent','extended')`; streaks has SELECT-only | PASS |
| WR-03 backfill complete live | `pg` join query counting `child_filter = family_members.id` rows | 0 remaining | PASS |
| No client streak writes remain | repo-wide grep `updateStreaks(` | Only award route + guarded repair action | PASS |
| CR-01 i18n namespace | grep unprefixed `t('calendarSettingsManager` | Zero matches; 19 keys in each locale | PASS |
| All 10 review-fix commits exist | `git cat-file -t` on 0dd4e91..1679b84 | All 10 are commits | PASS |

### Requirements Coverage

Plans self-declare `requirements: [05.5-SC1..05.5-SC5]` mapping to the 4 ROADMAP Success Criteria plus the phase-internal security criterion (server-side streaks + RLS lock, D-12), all verified above. No phase-5.5 rows exist in `.planning/REQUIREMENTS.md` (same bookkeeping pattern as phase 5.4) — no orphaned requirement IDs.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `components/DailyModal.tsx.backup` | — | git-tracked stale backup with a pre-5.5 2-arg `updateStreaks` call | Info | Not compiled (`.backup` outside tsconfig), cannot ship or break the build; already logged in `deferred-items.md` for a future cleanup (fits Phase 5.11 legacy-cleanup) |

Scanned all 21 phase-modified files (`TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER|not yet implemented|coming soon`): **zero debt markers**.

Informational note (not a phase gap): `KidDayFillForm.tsx` has no child-side `is_sick` toggle — it never had one (pre-existing; only the parent `DailyModal` exposes it). The plan-06 truth "sick marking is unchanged" holds; a child-side control would be new scope, plausibly covered by Phase 5.6's per-block "who fills" config.

### Human Verification Required

None blocking. Code-level verification is sufficient for all four Success Criteria (data resolution, preset materialization, weekend/streak behavior, and streak security are all exercised by live unit + integration tests and live-DB policy checks). Three **non-blocking browser spot-checks** are recommended and listed in the frontmatter (`human_verification_recommended`): the Calendar settings card render/persist flow, the preset apply dialog + badge + region-sync flow, and the kid day screen on a weekend/vacation date.

### Gaps Summary

No gaps. All 4 ROADMAP Success Criteria are independently verified against the current post-review-fix code (not trusted from SUMMARYs): the configurable calendar exists live with parent-only writes; presets materialize editable rows with the preset marker and safe replace semantics; weekend days and sick-day streak transparency are data-driven and covered by 27 unit tests including the CR-02 transparent-today cases; `getDayType` is fully data-resolved and wired into the kid day screen and both day forms; streaks are server-side only with a live-verified SELECT-only RLS lock; and the full 73-test suite (including live-DB money integration) passes. All 11 review findings (3 Critical, 8 Warning) are confirmed fixed in the current code, and both post-review migrations (05.5-04, 05.5-05) are confirmed applied to prod by independent live queries.

---

_Verified: 2026-07-13T21:05:00Z_
_Verifier: Claude (gsd-verifier)_
