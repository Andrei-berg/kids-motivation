---
phase: 05.5-year-calendar
plan: 07
subsystem: database
tags: [supabase, postgres, rls, migrations, security]

# Dependency graph
requires:
  - phase: 05.5-year-calendar (plans 01-06)
    provides: family_calendar/vacation_periods.preset_id/streaks-RLS migration files (05.5-01, 05.5-02, 05.5-03) and the server-side updateStreaks path (05.5-02 code)
provides:
  - Live-DB schema: public.family_calendar table, vacation_periods.preset_id column, streaks table locked to SELECT-only RLS for clients
  - scripts/verify-streaks-rls.mjs — E2E proof that a signed-in child can read but not write streaks
affects: [05.5-year-calendar phase closeout, any future streaks/calendar work]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: [scripts/verify-streaks-rls.mjs]
  modified: []

key-decisions:
  - "Applied the three phase migrations in the plan-mandated order (05.5-01, 05.5-02, then 05.5-03 last) since SUPABASE_DB_URL was present and the server-side updateStreaks code (05.5-02) was already present in the worktree"
  - "verify-streaks-rls.mjs mirrors verify-wallet-rls.mjs exactly (seed/sign-in/assert/cleanup), asserting read-OK, update-denied, insert-denied against a real signed-in child"

patterns-established: []

requirements-completed: [05.5-SC1, 05.5-SC5]

# Metrics
duration: 12min
completed: 2026-07-13
---

# Phase 05.5 Plan 07: Apply migrations + verify streaks RLS lock Summary

**Live DB now has family_calendar + vacation_periods.preset_id, and the streaks table is proven SELECT-only for clients via a real signed-in-child E2E script (CR-01 D-12.3 closed).**

## Performance

- **Duration:** ~12 min
- **Completed:** 2026-07-13
- **Tasks:** 2 completed
- **Files modified:** 1 created (`scripts/verify-streaks-rls.mjs`)

## Accomplishments
- Applied `05.5-01-family-calendar.sql`, `05.5-02-vacation-periods-preset-marker.sql`, and `05.5-03-streaks-rls-readonly.sql` to the live production database, in the plan-mandated order
- Confirmed via live-DB query: `family_calendar` table exists, `vacation_periods.preset_id` column exists, and every RLS policy on `streaks` has `cmd = 'SELECT'` (no client write policy remains)
- Wrote and ran `scripts/verify-streaks-rls.mjs`, which seeds an isolated throwaway family/child/auth-user, signs in as the child, and proves: the child CAN read its own streaks row, CANNOT update it, and CANNOT insert a new one — all seeded rows cleaned up afterward

## Task Commits

Each task was committed atomically:

1. **Task 1: Apply the three migrations to the live DB** — no source-file commit (pure DB state change, per plan's `<files>` spec: "no source files — DB state change"); verified via the plan's automated schema-presence query (see Verification below)
2. **Task 2: Write and run the streaks RLS verification script** - `1cfdb58` (test)

**Plan metadata:** (this commit, pending)

_Note: Task 1 has no code diff to commit — its "commit" is the live-DB migration apply itself, confirmed by the automated verification query below._

## Files Created/Modified
- `scripts/verify-streaks-rls.mjs` - E2E RLS lock verification for the streaks table (mirrors `scripts/verify-wallet-rls.mjs`); seeds throwaway family/child/auth user, signs in as child, asserts read-OK/update-denied/insert-denied, cleans up

## Verification

Live-DB schema-presence query (run after all three migrations applied):
```
SCHEMA_APPLIED ["SELECT"]
```
- `to_regclass('public.family_calendar')` → non-null (table exists)
- `vacation_periods.preset_id` column → present
- `streaks` policies → exactly one, `cmd = 'SELECT'` (no INSERT/UPDATE/DELETE policy for clients)

`node --env-file=.env.local scripts/verify-streaks-rls.mjs` output:
```
✓ child can READ own streaks
✓ child CANNOT update own streaks
✓ child CANNOT insert streaks row

✅ Streaks RLS lock verified.
cleaned up
```

## Decisions Made
- `SUPABASE_DB_URL` was present in `.env.local` (copied from the main checkout into the worktree per orchestrator instructions, never committed), so the automated `pg`-based apply path was used rather than surfacing manual Supabase SQL Editor instructions
- Migrations applied in the exact order the plan specifies — 05.5-03 (streaks lock) last, after confirming the server-side `updateStreaks` code (from 05.5-02) was already present in the codebase, matching the deploy-order caveat in the migration's own header comment

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. Pre-apply check confirmed the schema was not already present (`family_calendar: null`, `preset_id rows: 0`, `streaks policies: [{"cmd":"ALL"}]`), so this was a clean first-time apply, not a re-run.

## User Setup Required

None - no external service configuration required. `.env.local` was copied into the worktree only as a local, git-ignored environment file for running the migration/verification scripts against the live DB; it was not committed.

## Next Phase Readiness

- All three phase 05.5 migrations are live: `family_calendar` and `vacation_periods.preset_id` are queryable, and the `streaks` table's CR-01 D-12.3 client-write hole is closed and proven end-to-end.
- This was the final [BLOCKING] schema-push plan for phase 05.5 (wave 4, depends on all of 05.5-01..06); with this applied, the phase's live-DB dependency is satisfied for closeout.
- No blockers.

---
*Phase: 05.5-year-calendar*
*Completed: 2026-07-13*

## Self-Check: PASSED

- FOUND: scripts/verify-streaks-rls.mjs
- FOUND: .planning/phases/05.5-year-calendar/05.5-07-SUMMARY.md
- FOUND: commit 1cfdb58 (test(05.5-07): add streaks RLS lock verification script)
- FOUND: commit 1c35a52 (docs(05.5-07): add plan 07 summary)
