---
phase: 05.4-streak-settings
plan: 03
subsystem: ui
tags: [react, i18n, parent-center, wallet-settings, streaks]

# Dependency graph
requires:
  - phase: 05.4-01
    provides: "wallet_settings.streak_room/study/sport_days+bonus columns (live), WalletSettings type, SETTINGS_DEFAULTS/getWalletSettings fallback"
  - phase: 05.4-02
    provides: "Award route reads streak_* fields from wallet_settings with authoritative clamps; PATCH /api/wallet/settings clamps streak_* on write"
provides:
  - "Streaks & Bonuses card in Parent Center → Settings → Coins Rules with 6 editable RuleRows (room/study/sport days+bonus)"
  - "i18n label keys (settings.coinRules.streak*) and section header (parentCenter.settings.coinsRules.streaks) in en.json + ru.json"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns: ["Reused existing CoinsRulesTab up()/RuleRow/save() machinery for new settings fields — no new save button, no new API call"]

key-files:
  created: []
  modified:
    - "components/parent-center/screens/Settings.tsx"
    - "public/locales/en.json"
    - "public/locales/ru.json"

key-decisions:
  - "Copied .env.local into the worktree (gitignored, untracked, not committed) to run `npm run dev` for the checkpoint verification environment — same precedent as 05.4-01/02"

patterns-established: []

requirements-completed: ["05.4-SC3"]

# Metrics
duration: 25min
completed: 2026-07-07
---

# Phase 05.4 Plan 03: Streaks & Bonuses Settings UI Summary

**Streaks & Bonuses card added to Parent Center → Settings → Coins Rules with 6 editable fields (room/study/sport day-thresholds + bonuses), persisting through the existing `wallet_settings` PATCH route — no new write path introduced; operator verified the full edit → persist → award loop and the server-side clamps**

## Performance

- **Duration:** 25 min (incl. operator verification round-trip)
- **Started:** 2026-07-07T10:10:00Z
- **Completed:** 2026-07-07T10:35:00Z
- **Tasks:** 2/2 (1 auto + 1 checkpoint:human-verify, approved by operator)
- **Files modified:** 3

## Accomplishments
- `CoinsRulesTab` renders a new "Streaks & Bonuses" card (after "Chores & Behavior") with 6 `RuleRow`s bound to `streak_room_days`, `streak_room_bonus`, `streak_study_days`, `streak_study_bonus`, `streak_sport_days`, `streak_sport_bonus` — using the existing `up()`/`save()` machinery, no second save button, no new API call
- Days rows use a 📅 icon and bonus rows a 🔥 icon; label text itself disambiguates units ("Room streak — days to bonus" vs "Room streak — bonus") so the day-threshold rows are never confused with coin amounts despite the shared 🪙 suffix in `RuleRow`
- Added `settings.coinRules.streakRoomDays/streakRoomBonus/streakStudyDays/streakStudyBonus/streakSportDays/streakSportBonus` and `parentCenter.settings.coinsRules.streaks` keys to both `public/locales/en.json` and `public/locales/ru.json`, kept key-for-key in sync
- `npx tsc --noEmit` exits 0; i18n verify script prints "i18n ok"; dev server started and confirmed responding (`status 200`) as the verification environment for the checkpoint below

## Task Commits

Each task was committed atomically:

1. **Task 1: Streaks card in CoinsRulesTab + i18n keys** - `a0e2558` (feat)
2. **Task 2: checkpoint:human-verify** - no file changes (verification only); operator responded **"approved"** — card renders with defaults, values persist via "Save rules", a re-triggered award uses the new thresholds/bonuses, clamps hold on bad input

**Plan metadata:** `1602fa6` (partial-progress SUMMARY at checkpoint) + final SUMMARY update commit

## Files Created/Modified
- `components/parent-center/screens/Settings.tsx` - New `<Card pad={16}>` in `CoinsRulesTab` between "Chores & Behavior" and the trainer/coach card, with 6 `RuleRow`s wired to the streak_* fields
- `public/locales/en.json` - 6 new `settings.coinRules.streak*` keys + `parentCenter.settings.coinsRules.streaks` header
- `public/locales/ru.json` - Same 6 keys + header, Russian translations

## Decisions Made
- Copied `.env.local` from the main repo checkout into the worktree (gitignored, untracked, never committed) to run `npm run dev` for the checkpoint's verification environment — consistent with the precedent set in 05.4-01 and 05.4-02 for accessing Supabase credentials from within an isolated worktree.
- Used the label text itself ("… days to bonus" / "… bonus") rather than a literal "(days)" suffix token to disambiguate the day-threshold rows from bonus rows, since the plan's intent (avoid confusing a day count with a coin amount) is satisfied by the wording and a distinct icon (📅 vs 🔥); this is a wording-level implementation detail within the plan's stated intent, not a deviation.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None. `npm run dev` resolved and started correctly via PATH climbing to the main checkout's `node_modules/.bin` (same behavior noted in 05.4-02's summary for `npm test`), despite the worktree's own `node_modules/` being empty.

## User Setup Required
None - no external service configuration required.

## Checkpoint Outcome

The blocking `checkpoint:human-verify` task was returned to the orchestrator with the dev server running at http://localhost:3000. The operator performed the verification checklist — card renders with defaults (Room 7 days/100, Study 14 days/100, Sport 7 days/100); edited values persist through "Save rules" and a tab reload; a re-triggered award uses the new threshold/bonus values; bad inputs (negative bonus, 0 days) are clamped server-side — and responded **"approved"**. SC3 is satisfied end-to-end.

## Next Phase Readiness
- SC3 fully satisfied end-to-end (operator-verified); phase 05.4-streak-settings is ready to close.
- No blockers.

---
*Phase: 05.4-streak-settings*
*Completed: 2026-07-07*

## Self-Check: PASSED

- FOUND: components/parent-center/screens/Settings.tsx (streak_room_days present)
- FOUND: public/locales/en.json (streakRoomDays present)
- FOUND: public/locales/ru.json (streakRoomDays present)
- FOUND commit: a0e2558
- FOUND commit: 1602fa6
- Checkpoint (Task 2): approved by operator
