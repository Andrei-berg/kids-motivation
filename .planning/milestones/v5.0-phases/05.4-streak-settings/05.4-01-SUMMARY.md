---
phase: 05.4-streak-settings
plan: 01
subsystem: database
tags: [supabase, postgres, wallet-settings, streaks, migration]

# Dependency graph
requires: []
provides:
  - "wallet_settings.streak_room_days/bonus, streak_study_days/bonus, streak_sport_days/bonus columns (live)"
  - "WalletSettings TS type carrying the 6 streak fields"
  - "SETTINGS_DEFAULTS (_lib.ts) and getWalletSettings() fallback (wallet.repo.ts) in sync with matching defaults"
affects: ["05.4-02 (award route reads these)", "05.4-03 (parent Settings UI edits these)"]

# Tech tracking
tech-stack:
  added: []
  patterns: ["Idempotent ALTER TABLE ... ADD COLUMN IF NOT EXISTS streak config pattern, mirroring 03-coin-rules-configurable.sql"]

key-files:
  created: ["supabase/migrations/05.4-01-streak-settings.sql"]
  modified: ["lib/models/wallet.types.ts", "app/api/wallet/_lib.ts", "lib/repositories/wallet.repo.ts"]

key-decisions:
  - "Copied .env.local from the main repo checkout into the worktree (gitignored, not committed) so the documented pg one-liner could reach SUPABASE_DB_URL — worktrees don't inherit untracked files"

patterns-established:
  - "Streak config columns follow the same per-family wallet_settings extension pattern used for coin rules (03-coin-rules-configurable.sql)"

requirements-completed: ["05.4-SC1"]

# Metrics
duration: 20min
completed: 2026-07-07
---

# Phase 05.4 Plan 01: Streak Settings Schema Foundation Summary

**Added streak_room/study/sport days+bonus columns to wallet_settings (live DB), WalletSettings type, and both defaults maps — defaults reproduce the current hardcoded award-route constants (7/14/7 days, 100 bonus each)**

## Performance

- **Duration:** 20 min
- **Started:** 2026-07-07T09:44:00Z
- **Completed:** 2026-07-07T10:04:39Z
- **Tasks:** 2
- **Files modified:** 4 (1 created, 3 modified)

## Accomplishments
- Idempotent migration adding 6 streak config columns to `wallet_settings`, applied to the live Supabase DB and verified via `information_schema`
- `WalletSettings` TS interface extended with the 6 new numeric fields
- `SETTINGS_DEFAULTS` (`app/api/wallet/_lib.ts`) and the `getWalletSettings()` fallback (`lib/repositories/wallet.repo.ts`) both carry identical default values (room 7/100, study 14/100, sport 7/100)

## Task Commits

Each task was committed atomically:

1. **Task 1: Write migration + extend type and both defaults maps** - `e74e33d` (feat)
2. **Task 2: [BLOCKING] Apply migration to the live database** - no file changes (DB-only mutation); verified live via information_schema query, no commit needed

**Plan metadata:** committed separately (SUMMARY.md)

## Files Created/Modified
- `supabase/migrations/05.4-01-streak-settings.sql` - Idempotent `ALTER TABLE wallet_settings ADD COLUMN IF NOT EXISTS` for the 6 streak columns + `NOTIFY pgrst, 'reload schema'`
- `lib/models/wallet.types.ts` - `WalletSettings` interface gains `streak_room_days`, `streak_room_bonus`, `streak_study_days`, `streak_study_bonus`, `streak_sport_days`, `streak_sport_bonus`
- `app/api/wallet/_lib.ts` - `SETTINGS_DEFAULTS` gains the 6 matching defaults
- `lib/repositories/wallet.repo.ts` - `getWalletSettings()` no-row fallback object gains the 6 matching defaults

## Decisions Made
- Copied `.env.local` from the main repo into the worktree (gitignored, never committed) to obtain `SUPABASE_DB_URL` for the mandatory live migration step — the file is untracked and worktrees don't inherit untracked files from the checkout they branch from. No secret was fabricated or generated; the existing operator-configured value was reused locally, matching the project's documented migration method in CLAUDE.md.

## Deviations from Plan

None - plan executed exactly as written. Task 2 required locating `.env.local` (see Decisions above), which is standard operational access, not a code deviation.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required. The migration was applied directly to the live DB as part of this plan (task 2 is the DB-apply step itself).

## Next Phase Readiness
- Plan 02 (award route) can now read `streak_room_days/bonus`, `streak_study_days/bonus`, `streak_sport_days/bonus` from `wallet_settings` via `loadSettings`/`getWalletSettings` — both defaults maps already carry byte-identical values to the current hardcoded constants, so behavior is unchanged until Plan 02 wires the reads.
- Plan 03 (parent Settings UI) can read/write these same 6 fields once the settings route accepts them.
- No blockers.

---
*Phase: 05.4-streak-settings*
*Completed: 2026-07-07*

## Self-Check: PASSED

- FOUND: supabase/migrations/05.4-01-streak-settings.sql
- FOUND: .planning/phases/05.4-streak-settings/05.4-01-SUMMARY.md
- FOUND: commit e74e33d
