---
phase: 05.4-streak-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/05.4-01-streak-settings.sql
  - lib/models/wallet.types.ts
  - app/api/wallet/_lib.ts
  - lib/repositories/wallet.repo.ts
autonomous: true
requirements: ["05.4-SC1"]

must_haves:
  truths:
    - "wallet_settings has streak_room_days/bonus, streak_study_days/bonus, streak_sport_days/bonus columns"
    - "Defaults equal the current code constants: room 7d/100, study 14d/100, sport 7d/100"
    - "The 6 fields exist on the WalletSettings TS type and in both defaults maps"
  artifacts:
    - path: "supabase/migrations/05.4-01-streak-settings.sql"
      provides: "ALTER TABLE wallet_settings adding 6 streak columns (idempotent)"
      contains: "streak_room_days"
    - path: "lib/models/wallet.types.ts"
      provides: "6 new numeric fields on WalletSettings interface"
      contains: "streak_room_days"
    - path: "app/api/wallet/_lib.ts"
      provides: "SETTINGS_DEFAULTS with 6 streak fields"
      contains: "streak_room_bonus"
    - path: "lib/repositories/wallet.repo.ts"
      provides: "getWalletSettings() fallback with 6 streak fields"
      contains: "streak_study_days"
  key_links:
    - from: "app/api/wallet/_lib.ts SETTINGS_DEFAULTS"
      to: "wallet_settings columns"
      via: "loadSettings spreads DEFAULTS then DB row"
      pattern: "streak_(room|study|sport)_(days|bonus)"
---

<objective>
Add per-family streak configuration to `wallet_settings`: for each of the three
bonus-eligible streak types (room, study, sport) a "days" threshold and a "bonus"
amount. Defaults exactly reproduce the constants currently hardcoded in
`app/api/wallet/award/route.ts` (`computeStreakBonus`): room ≥ 7 days → +100,
study ≥ 14 days → +100, sport ≥ 7 days → +100.

Purpose: This is the schema/type foundation. Plan 02 (award route reads these) and
Plan 03 (parent edits these in Settings) both depend on the columns, TS type, and
defaults created here.
Output: Migration file applied to the live DB, updated `WalletSettings` type, and the
two defaults maps (`SETTINGS_DEFAULTS` in `_lib.ts`, `getWalletSettings()` fallback in
`wallet.repo.ts`) carrying the 6 new fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current streak constants to reproduce as defaults (from award/route.ts computeStreakBonus):
     room:  current_count >= 7  → bonus 100
     study: current_count >= 14 → bonus 100
     sport: current_count >= 7  → bonus 100
     New columns (all INTEGER NOT NULL):
       streak_room_days DEFAULT 7,   streak_room_bonus DEFAULT 100
       streak_study_days DEFAULT 14,  streak_study_bonus DEFAULT 100
       streak_sport_days DEFAULT 7,   streak_sport_bonus DEFAULT 100
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration + extend type and both defaults maps</name>
  <files>supabase/migrations/05.4-01-streak-settings.sql, lib/models/wallet.types.ts, app/api/wallet/_lib.ts, lib/repositories/wallet.repo.ts</files>
  <read_first>
    - supabase/migrations/03-coin-rules-configurable.sql (established `ALTER TABLE wallet_settings ADD COLUMN IF NOT EXISTS ... NOT NULL DEFAULT ...` idempotent pattern + `NOTIFY pgrst, 'reload schema';` at the end)
    - supabase/migrations/05.2-01-room-tasks.sql (05.x migration file style/naming for this project)
    - lib/models/wallet.types.ts (WalletSettings interface, lines 96–120 — where to insert the 6 fields)
    - app/api/wallet/_lib.ts (SETTINGS_DEFAULTS object, lines 36–59)
    - lib/repositories/wallet.repo.ts (getWalletSettings fallback object, lines 185–214)
  </read_first>
  <action>
    Create `supabase/migrations/05.4-01-streak-settings.sql` mirroring the idempotent
    style of `03-coin-rules-configurable.sql`: a single `ALTER TABLE wallet_settings`
    with six `ADD COLUMN IF NOT EXISTS` clauses, each `INTEGER NOT NULL DEFAULT`:
    streak_room_days=7, streak_room_bonus=100, streak_study_days=14,
    streak_study_bonus=100, streak_sport_days=7, streak_sport_bonus=100. End the file
    with `NOTIFY pgrst, 'reload schema';` so PostgREST re-reads the schema. Do NOT
    migrate values from the legacy `settings` key/value table — defaults are the code
    constants (100), matching "behavior unchanged for families that never touched them".

    Add the same six fields (all `number`) to the `WalletSettings` interface in
    `lib/models/wallet.types.ts`, alongside the existing coin-rule fields.

    Add the six fields with identical default values (7/100/14/100/7/100) to
    `SETTINGS_DEFAULTS` in `app/api/wallet/_lib.ts` AND to the fallback object returned
    by `getWalletSettings()` in `lib/repositories/wallet.repo.ts`. Keep both maps in
    sync — they must produce the same streak values so award (server) and Settings UI
    (client fallback) agree.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "streak_room_days\|streak_study_days\|streak_sport_days" supabase/migrations/05.4-01-streak-settings.sql && grep -c "streak_room_days\|streak_study_days\|streak_sport_days" lib/models/wallet.types.ts && grep -c "streak_room_days\|streak_study_days\|streak_sport_days" app/api/wallet/_lib.ts && grep -c "streak_room_days\|streak_study_days\|streak_sport_days" lib/repositories/wallet.repo.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - Each of the four `grep -c` commands runs against a single file and matches (exit 0) — proving the streak column names are present in the migration, the type, `SETTINGS_DEFAULTS`, AND the `getWalletSettings()` fallback independently (no cross-file false positive).
    - `supabase/migrations/05.4-01-streak-settings.sql` contains all 6 column names and `NOTIFY pgrst, 'reload schema';`; every ADD uses `IF NOT EXISTS` and `INTEGER NOT NULL DEFAULT`.
    - Default values in the SQL, `SETTINGS_DEFAULTS`, and the `getWalletSettings()` fallback are identical: room 7/100, study 14/100, sport 7/100.
    - `WalletSettings` interface declares `streak_room_days`, `streak_room_bonus`, `streak_study_days`, `streak_study_bonus`, `streak_sport_days`, `streak_sport_bonus` as `number`.
  </acceptance_criteria>
  <done>Migration file written; type + both defaults maps carry the 6 fields with matching values; type check green.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Apply migration to the live database</name>
  <files>supabase/migrations/05.4-01-streak-settings.sql</files>
  <read_first>
    - CLAUDE.md ("Applying DB migrations" section — the `pg` one-liner using SUPABASE_DB_URL from .env.local; run `npm i pg --no-save` once)
    - supabase/migrations/05.4-01-streak-settings.sql (the file to apply)
  </read_first>
  <action>
    Apply the migration to the live Supabase DB using the project's documented method:
    `npm i pg --no-save` (once), then the `node --env-file=.env.local` `pg` one-liner
    from CLAUDE.md with FILE=`supabase/migrations/05.4-01-streak-settings.sql`. This is
    mandatory — the TS build/type check pass WITHOUT the columns existing in the DB
    (types are hand-written, not generated), so skipping this creates a false-positive
    verification state where the award route (Plan 02) and Settings write (Plan 03) would
    fail at runtime with "column does not exist".

    If `SUPABASE_DB_URL` is not present in `.env.local`, do NOT fabricate success: stop
    and surface that the migration must be applied manually via the Supabase SQL Editor,
    and mark this task blocked pending operator action.
  </action>
  <verify>
    <automated>node --env-file=.env.local -e 'import("pg").then(async({default:pg})=>{const c=new pg.Client({connectionString:process.env.SUPABASE_DB_URL});await c.connect();const r=await c.query("select column_name from information_schema.columns where table_name=$1 and column_name like $2 order by column_name",["wallet_settings","streak_%"]);console.log(r.rows.map(x=>x.column_name).join(","));await c.end();process.exit(r.rows.length===6?0:1)})'</automated>
  </verify>
  <acceptance_criteria>
    - The verify query prints exactly the 6 streak columns: `streak_room_bonus,streak_room_days,streak_sport_bonus,streak_sport_days,streak_study_bonus,streak_study_days` and exits 0.
    - If applied via SQL Editor instead (no SUPABASE_DB_URL), the same information_schema query run in the editor returns all 6 columns before this task is marked done.
  </acceptance_criteria>
  <done>All 6 streak columns exist on the live `wallet_settings` table; information_schema query returns 6 rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → wallet_settings | Money table; clients can SELECT but must never write directly |
| dev machine → live DB | Migration applied with a privileged Postgres connection string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-054-01 | Tampering | new streak columns on wallet_settings | mitigate | Columns live on `wallet_settings`, RLS SELECT-only for clients (migration 04.4-03); no new client write path is introduced — writes only via the service-role settings route |
| T-054-02 | Information disclosure | wallet_settings rows | accept | Streak thresholds/bonuses are non-sensitive family config; existing per-family RLS SELECT scoping is unchanged |
| T-054-SC | Supply chain | `npm i pg --no-save` for migration | accept | `pg` is the project-established migration tool documented in CLAUDE.md (`--no-save`, dev-only, not shipped); no new runtime dependency added |
</threat_model>

<verification>
- `npx tsc --noEmit` green.
- Migration file present and idempotent (safe to re-run).
- Live `wallet_settings` has the 6 streak columns with correct defaults.
</verification>

<success_criteria>
SC1 satisfied: `wallet_settings` gains streak fields (days + bonus per streak type) with
defaults equal to the current award-route constants (7/14/7 days, 100 bonus each).
</success_criteria>

<output>
Create `.planning/phases/05.4-streak-settings/05.4-01-SUMMARY.md` when done.
</output>
