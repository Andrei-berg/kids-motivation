---
phase: 05.2-room-tasks
plan: 04
type: execute
wave: 3
depends_on: [05.2-02]
files_modified:
  - components/kid/KidDayFillForm.tsx
# ends with a human-verify checkpoint (kid checklist renders + dual-write)
autonomous: false
requirements: [RT-SC2, RT-SC4]

must_haves:
  truths:
    - "The kid Room section renders items from room_tasks (active, ordered), not the hardcoded ROOM_ITEM_LABELS array"
    - "Saving writes room_checks rows for every rendered task (done true/false)"
    - "For tasks with a legacy_key, saving ALSO writes the matching days.room_* column (dual-write)"
    - "A task renamed in Settings shows its new name on the kid screen on next load"
  artifacts:
    - path: "components/kid/KidDayFillForm.tsx"
      provides: "checklist rendered from room_tasks + dual-write on save"
      contains: "getRoomTasks"
  key_links:
    - from: "KidDayFillForm room section"
      to: "getRoomTasks(familyId)"
      via: "load in useEffect, render list"
      pattern: "getRoomTasks"
    - from: "handleSubmit"
      to: "saveRoomChecks + saveDay legacy columns"
      via: "dual-write"
      pattern: "saveRoomChecks"
---

<objective>
Replace the hardcoded 5-item ROOM_ITEM_LABELS checklist in the kid day form with a list rendered from
`room_tasks` (active, ordered). On save, write `room_checks` rows for every rendered task AND — for tasks
that carry a `legacy_key` — keep writing the matching `days.room_*` boolean via the existing `saveDay` path
so the room_score trigger, `room_ok`, analytics and wallboard stay correct (dual-write). Custom tasks
(no legacy_key) live only in room_checks.

Purpose: The kid checklist becomes data-driven while legacy consumers keep working.
Output: KidDayFillForm renders from the table and dual-writes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-room-tasks/05.2-CONTEXT.md
@CLAUDE.md

<read_first>
- components/kid/KidDayFillForm.tsx — the CURRENT room implementation to replace: ROOM_ITEM_LABELS (lines 52-58), RoomItems interface + roomItems state (lines 40-46, 120-126), toggleRoomItem (318-321), the Room FillSection render (563-607), coinsPreview room count (269-274), and handleSubmit legacy write (390-428: dayParams.roomBed..roomTrash + saveDay).
- lib/repositories/room.repo.ts (Plan 02) — getRoomTasks, saveRoomChecks, getRoomChecks. RoomTask/RoomCheck types in lib/models/room.types.ts.
- lib/repositories/children.repo.ts saveDay — keeps taking roomBed/roomFloor/roomDesk/roomCloset/roomTrash; still called for legacy dual-write + all other day fields (mood, note, proof).
- CONTEXT decision 3 (dual-write, read-new-first) and decision 6 (kid form renders names/icons from the table; no i18n labelKey for custom names).
</read_first>

<interfaces>
RoomTask.legacy_key ∈ {'bed','floor','desk','closet','trash'} maps to saveDay params roomBed/roomFloor/roomDesk/roomCloset/roomTrash respectively.
getRoomTasks(familyId) → RoomTask[] active, sort_order asc.
saveRoomChecks(childId, date, [{taskId, done}]) → upsert room_checks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render Room checklist from room_tasks and dual-write on save</name>
  <files>components/kid/KidDayFillForm.tsx</files>
  <read_first>components/kid/KidDayFillForm.tsx (room state, render, submit); lib/repositories/room.repo.ts</read_first>
  <action>
    Load the family's active room_tasks in the mount useEffect (fetch the child's family_id via the existing
    supabase children query already used for proof upload, or getChild). Replace the fixed `RoomItems` object with
    dynamic state keyed by task id: `Record&lt;taskId, boolean&gt;`, pre-filled from getRoomChecks(childId,date) when
    the day was child-filled (mirror the existing isChildFilled pre-fill logic). Render the Room FillSection by
    mapping over the loaded tasks (task.icon + task.name), preserving the existing button styling/toggle/confetti;
    the section count badge becomes doneCount/taskCount. Update coinsPreview: room contributes coins_per_room_task
    when doneCount ≥ max(1, ceil(0.6*taskCount)) (match the server rule from Plan 03; keep it a preview only).
    In handleSubmit, DUAL-WRITE: (a) call saveRoomChecks(childId, date, tasks.map(t =&gt; ({taskId: t.id, done: checked[t.id] ?? false})));
    (b) for tasks with a legacy_key, set the corresponding saveDay param (roomBed/…/roomTrash) from checked-state so
    the existing saveDay call still writes the 5 legacy columns (default false for any legacy task not present).
    Leave the /api/wallet/award POST, streaks, badges, proof upload, and every non-room section unchanged.
    Do NOT remove ROOM_ITEM_LABELS' i18n keys usage elsewhere — but the seeded task names come from the table now.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; grep -q "getRoomTasks" components/kid/KidDayFillForm.tsx &amp;&amp; grep -q "saveRoomChecks" components/kid/KidDayFillForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Room section maps over getRoomTasks output (no reference to a hardcoded 5-element room array for rendering).
    - handleSubmit calls saveRoomChecks with one entry per rendered task.
    - Legacy-keyed tasks still populate saveDay's roomBed/roomFloor/roomDesk/roomCloset/roomTrash params.
    - `npx tsc --noEmit` clean; `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Kid Room checklist is data-driven; save dual-writes room_checks + legacy columns.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: kid Room checklist renders from room_tasks + dual-write verified</name>
  <action>Operator visually verifies the kid day form per how-to-verify steps and confirms room_checks + legacy columns both persisted.</action>
  <what-built>The kid day form now renders the Room checklist from room_tasks and dual-writes to room_checks + the legacy days.room_* columns.</what-built>
  <how-to-verify>
    1. `npm run dev`, open the kid Day screen for today (or /kid/day with a PIN child).
    2. Confirm the Room section shows the 5 seeded items (кровать/пол/стол/шкаф/мусор) with their emoji, in order.
    3. Check 3 items, save. Confirm the coin fly-up matches coins_per_room_task and the day saves without error.
    4. In Supabase (or a service-role query), confirm: room_checks has 5 rows for this child+date (3 done=true),
       AND the days row has 3 of room_bed/room_floor/room_desk/room_closet/room_trash = true (room_ok true).
    5. (SC4 preview) After Plan 06 ships, renaming a task in Settings and reloading shows the new name here.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what rendered/persisted incorrectly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kid browser → room_checks + days (RLS) | Child writes their own day data; award recomputes server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-052-13 | Tampering | Child dual-writing legacy columns to inflate room_ok | mitigate | Legacy columns already client-writable today; room_ok stays trigger-derived (≥3 booleans); award pays a single flat coin regardless — no new inflation path |
| T-052-14 | Information Disclosure | Rendering another family's tasks | mitigate | getRoomTasks filters by the child's own family_id; RLS SELECT (Plan 01) enforces isolation server-side |
| T-052-15 | Tampering | room_checks for a locked/other date | mitigate | Form's existing isLocked (date &lt; today) disables toggles; award only credits the posted date with a fixed idempotent amount |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run lint` exits 0.
- Human-verify checkpoint approved (checklist renders from table; room_checks + legacy columns both written).
</verification>

<success_criteria>
ROADMAP 5.2 SC2 (kid form renders from room_tasks; dual-write to room_checks + legacy columns) met for the
kid form. Contributes to SC4 (rename visible on kid screen).
</success_criteria>

<output>
Create `.planning/phases/05.2-room-tasks/05.2-04-SUMMARY.md` when done.
</output>
