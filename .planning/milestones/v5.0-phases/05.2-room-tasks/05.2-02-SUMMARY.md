---
phase: 05.2-room-tasks
plan: 02
subsystem: database
tags: [supabase, repository, room-tasks, onboarding]

# Dependency graph
requires:
  - phase: 05.2-01
    provides: "room_tasks/room_checks tables + RLS + seed_default_room_tasks() RPC + legacy-delete guard, live on prod DB"
provides:
  - "lib/repositories/room.repo.ts: getRoomTasks, getRoomChecks, saveRoomChecks (day-data upsert) + parent CRUD (addRoomTask, updateRoomTask, setRoomTaskActive, reorderRoomTasks, deleteRoomTask)"
  - "lib/models/room.types.ts: RoomTask/RoomCheck/RoomLegacyKey types"
  - "lib/room-api.ts: backward-compat re-export wrapper"
  - "createFamily seeds the 5 default room tasks for every new family (non-fatal on RPC failure)"
affects: [05.2-03-award-fallback, 05.2-04-forms-dual-write, 05.2-05-settings-editor, 05.2-06-new-family-seed-wiring]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "room.repo.ts mirrors children.repo.ts (browser supabase singleton from lib/supabase.ts, family_id resolved via a children lookup before writing day-data) rather than categories.repo.ts's createClient() pattern — kept consistent with the day-data repo it sits alongside (days/home_sports/section_attendance all resolve family_id the same way)"
    - "saveRoomChecks always writes the full passed checklist (not deltas) so unchecking a task persists via upsert onConflict child_id,date,task_id"

key-files:
  created:
    - lib/models/room.types.ts
    - lib/repositories/room.repo.ts
    - lib/room-api.ts
  modified:
    - lib/onboarding-api.ts

key-decisions:
  - "Used the lib/supabase.ts browser singleton (import { supabase } from '../supabase') in room.repo.ts, matching children.repo.ts, rather than categories.repo.ts's per-call createClient() — the plan's read_first pointed at children.repo.ts as the idiom to mirror since room_checks is day-data like days/home_sports, not family-config-only like categories."
  - "reorderRoomTasks issues one UPDATE per task id (sequential, throwing on first error) rather than a single batched upsert — matches the plan's stated behavior ('update sort_order per index') and keeps the RLS WITH CHECK path identical to a normal single-row update."

requirements-completed: [RT-SC1, RT-SC4]

# Metrics
duration: ~15min
completed: 2026-07-06
---

# Phase 05.2 Plan 02: room.repo.ts + new-family seed wiring Summary

**lib/repositories/room.repo.ts (8 exports: reads, day-data upsert, parent CRUD) plus createFamily now seeding the 5 default room tasks for every new family via a non-fatal seed_default_room_tasks RPC call.**

## Performance

- **Duration:** ~15 min
- **Completed:** 2026-07-06T13:51:32Z
- **Tasks:** 2/2
- **Files modified:** 4 (3 created, 1 modified)

## Accomplishments
- Authored `lib/models/room.types.ts` (`RoomTask`, `RoomCheck`, `RoomLegacyKey`) matching the `<interfaces>` contract and the live `room_tasks`/`room_checks` schema from Plan 01.
- Authored `lib/repositories/room.repo.ts`: `getRoomTasks` (family-scoped, `is_active` filter, `sort_order` ascending), `getRoomChecks`, `saveRoomChecks` (resolves `family_id` from `children`, upserts all passed tasks with `onConflict: 'child_id,date,task_id'` so unchecking persists), and 5 parent-CRUD functions (`addRoomTask`, `updateRoomTask`, `setRoomTaskActive`, `reorderRoomTasks`, `deleteRoomTask` — the last surfaces the DB delete-guard error rather than swallowing it).
- Added `lib/room-api.ts` as the backward-compat re-export wrapper, following the `expenses-api.ts`/`categories-api.ts` convention.
- Wired `createFamily` (the only production family-creation path — confirmed via `grep .from('families')` insert usage) to call `supabase.rpc('seed_default_room_tasks', { p_family_id })` in its own non-fatal try/catch immediately after `seedDefaultCategories`, so new families get the 5 default checklist tasks without risking family creation on seed failure.

## Task Commits

1. **Task 1: room.types.ts + room.repo.ts (reads, day-data writes, parent CRUD)** - `d4b1031` (feat)
2. **Task 2: Seed default room tasks on new-family creation** - `92a62d3` (feat)

**Plan metadata:** (this commit, docs)

## Files Created/Modified
- `lib/models/room.types.ts` - `RoomTask`/`RoomCheck`/`RoomLegacyKey` types
- `lib/repositories/room.repo.ts` - room checklist repo (reads, day-data upsert, parent CRUD)
- `lib/room-api.ts` - backward-compat re-export wrapper
- `lib/onboarding-api.ts` - `createFamily` now seeds default room tasks (non-fatal)

## Decisions Made
- Mirrored `children.repo.ts`'s browser-singleton + family_id-lookup idiom in `room.repo.ts` (not `categories.repo.ts`'s `createClient()` pattern), since `room_checks` is day-data resolved per-child like `days`/`home_sports`.
- `reorderRoomTasks` writes sort_order sequentially per id (throws on first failure) rather than a single batch call, matching the plan's literal instruction and keeping each write a normal single-row RLS-checked update.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- `lib/repositories/room.repo.ts` and `lib/room-api.ts` are ready for the kid/parent day-fill forms (dual-write), the award-fallback logic, and the Settings editor to import directly.
- New families created from this point forward get the 5 default room tasks seeded automatically; existing families were already backfilled in Plan 01.
- No blockers for the next plan.

---
*Phase: 05.2-room-tasks*
*Completed: 2026-07-06*

## Self-Check: PASSED

- FOUND: lib/models/room.types.ts
- FOUND: lib/repositories/room.repo.ts
- FOUND: lib/room-api.ts
- FOUND: lib/onboarding-api.ts
- FOUND: .planning/phases/05.2-room-tasks/05.2-02-SUMMARY.md
- FOUND commit: d4b1031
- FOUND commit: 92a62d3
