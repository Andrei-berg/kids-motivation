---
phase: 05.7-kid-redesign
plan: 10
type: execute
wave: 3
depends_on: ["05.7-01", "05.7-05"]
files_modified:
  - components/kid/CelebrationOverlay.tsx
  - components/kid/BadgeNudge.tsx
  - components/kid/KidChallenges.tsx
  - utils/confetti.ts
autonomous: true
requirements: [05.7-SC1, 05.7-SC3]

must_haves:
  truths:
    - "Badge-earned celebration uses the Stamp ceremony ('ПОЛУЧЕНО'), not confetti (D-20)"
    - "CelebrationOverlay and BadgeNudge and KidChallenges are on paper/ledger tones with no coral/Nunito remnants (D-12, SC1)"
    - "triggerGoalConfetti has no remaining LIVE callers in kid overlays/wallet; its export stays (dead-code components/GoalsModal.tsx still imports it) pending the Plan 12 dead-code disposition; triggerConfetti remains for streak/level-up (D-19)"
    - "prefers-reduced-motion is respected via the Stamp atom's built-in guard (no third guard)"
  artifacts:
    - path: "components/kid/CelebrationOverlay.tsx"
      provides: "Badge reveal via Stamp ceremony on paper"
      contains: "Stamp"
    - path: "utils/confetti.ts"
      provides: "confetti wrapper; triggerGoalConfetti marked @deprecated (no live callers); triggerConfetti preserved"
  key_links:
    - from: "components/kid/CelebrationOverlay.tsx"
      to: "Stamp ceremony"
      via: "replaces triggerGoalConfetti"
      pattern: "Stamp"
---

<objective>
Reskin the always-mounted kid overlays (CelebrationOverlay, BadgeNudge, KidChallenges) to the
family-bank language and convert badge-earned celebration from confetti to the Stamp ceremony,
retiring `triggerGoalConfetti` from all live callers (D-12, D-20, SC1/SC3).

Purpose: SC1 fails if an old-style overlay pops over the new screens; SC3 requires confetti
reserved for streak/level-up only. This plan owns the overlay files + the confetti util.
Output: reskinned overlays with Stamp ceremonies; confetti wrapper with triggerGoalConfetti deprecated (no live callers; literal export removal deferred to the dead-code disposition).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md
@.planning/phases/05.7-kid-redesign/05.7-PATTERNS.md
@CLAUDE.md

<interfaces>
Stamp from '@/components/design/atoms' (reduced-motion double-guarded — do not add a third guard).
CelebrationOverlay.tsx:33-40 fires triggerGoalConfetti() on a fresh badge → replace with a
Stamp('ПОЛУЧЕНО' = t('stamp.received')) reveal, -8° tilt, 450ms entrance (UI-SPEC Motion Contract).
BadgeNudge.tsx is already stamp-ceremony-shaped (gradient card, no confetti) — reskin to paper
tones, verify no confetti sneaks in.
KidChallenges.tsx — reskin its gradient card to paper/ledger tones.
utils/confetti.ts: keep triggerConfetti (still used by streak/level-up in Plan 11). After Plan 05
(GoalsPanel) + this plan's Task 1 (CelebrationOverlay), triggerGoalConfetti has no remaining LIVE
callers — BUT dead-code components/GoalsModal.tsx (root components/, OUT OF SCOPE) still imports and
calls it, so DELETING the export would break `npx tsc --noEmit`. Do NOT remove the export here:
annotate triggerGoalConfetti with an `@deprecated` JSDoc noting it is retired from live use and its
removal rides with the GoalsModal dead-code disposition (Plan 12 Wave-4 operator checkpoint / possibly
Phase 5.11). The live-caller check is scoped to `app/kid components/kid app/api/wallet` (excludes the
root-level dead-code modals).
Tokens: use paper.* / base fonts from lib/design/tokens; remove any legacy T.coral/T.sun/Nunito.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CelebrationOverlay + BadgeNudge + KidChallenges reskin with Stamp ceremony</name>
  <files>components/kid/CelebrationOverlay.tsx, components/kid/BadgeNudge.tsx, components/kid/KidChallenges.tsx</files>
  <read_first>
    - components/kid/CelebrationOverlay.tsx (full — triggerGoalConfetti at 33-40; badge reveal shape)
    - components/kid/BadgeNudge.tsx (full — verify no confetti; gradient card to reskin)
    - components/kid/KidChallenges.tsx (full — gradient card to reskin)
    - components/design/atoms.tsx (Stamp signature + guard)
    - .planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md (Motion Contract badge row; Color paper tones)
  </read_first>
  <action>
    In CelebrationOverlay: replace `triggerGoalConfetti()` with a `<Stamp>` badge-reveal showing
    `t('stamp.received')` ('ПОЛУЧЕНО') with the -8° tilt / 450ms entrance, reskinning the reveal
    card to paper.card/paper.line tones; remove the triggerGoalConfetti import. Reskin BadgeNudge
    and KidChallenges gradient cards to paper/ledger tones (LedgerRow/Amount where a coin figure
    appears, gold only on money). Remove any legacy T.coral/T.sun color-name usages and any Nunito
    reference in markup these reskins touch. Reuse the Stamp atom's built-in reduced-motion guard
    — do not add another guard layer.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "Stamp" components/kid/CelebrationOverlay.tsx && ! grep -q "triggerGoalConfetti" components/kid/CelebrationOverlay.tsx && ! grep -rq "Nunito" components/kid/CelebrationOverlay.tsx components/kid/BadgeNudge.tsx components/kid/KidChallenges.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CelebrationOverlay renders a `Stamp` with `t('stamp.received')`; no `triggerGoalConfetti` call/import remains
    - BadgeNudge and KidChallenges render no confetti and no `Nunito`/`T.coral`/`T.sun` in touched markup
    - Coin figures (if any) use `Amount`/`LedgerRow` (gold only on money)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Overlays on paper with the badge Stamp ceremony; no confetti, no remnants.</done>
</task>

<task type="auto">
  <name>Task 2: Deprecate triggerGoalConfetti (no live callers) — export preserved for dead-code cleanup</name>
  <files>utils/confetti.ts</files>
  <read_first>
    - utils/confetti.ts (full — triggerConfetti + triggerGoalConfetti)
    - .planning/phases/05.7-kid-redesign/05.7-CONTEXT.md (D-19 confetti = streak/level-up only; D-20 goal/badge/purchase → Stamp)
  </read_first>
  <action>
    Confirm no remaining LIVE importers/callers of `triggerGoalConfetti` within this phase's in-scope
    kid surface — grep `app/kid components/kid app/api/wallet` (after Plan 05's GoalsPanel fix + this
    plan's Task 1 there should be none). Do NOT widen the grep to root `components/` or `utils/`:
    dead-code `components/GoalsModal.tsx` still imports and calls it and is explicitly OUT OF SCOPE
    (its delete-now-vs-defer-to-5.11 disposition is decided at Plan 12's Wave-4 operator checkpoint).
    Do NOT remove the `triggerGoalConfetti` export — while GoalsModal.tsx still references it, deleting
    the export would break `npx tsc --noEmit`. Instead, add an `@deprecated` JSDoc to
    `triggerGoalConfetti` in utils/confetti.ts noting it has no live callers and its removal follows
    the GoalsModal dead-code disposition. KEEP `triggerConfetti` intact and unchanged — Plan 11 uses
    it for the streak-bonus + level-up bursts (D-19).
  </action>
  <verify>
    <automated>npx tsc --noEmit && ! grep -rq "triggerGoalConfetti" app/kid components/kid app/api/wallet && grep -q "export function triggerConfetti" utils/confetti.ts && grep -q "@deprecated" utils/confetti.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - No LIVE `triggerGoalConfetti` caller in `app/kid`, `components/kid`, or `app/api/wallet`
    - `triggerGoalConfetti` export still present with an `@deprecated` JSDoc (removal deferred to the GoalsModal dead-code disposition — Plan 12 / 5.11)
    - `triggerConfetti` still exported and unchanged
    - `npx tsc --noEmit` and `npm test` exit 0
  </acceptance_criteria>
  <done>triggerGoalConfetti has no live callers and is marked @deprecated; export preserved so tsc stays green while dead-code GoalsModal remains; streak/level-up confetti mechanism preserved for Plan 11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Client-only overlay reskin; no data reads/writes changed. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-057-18 | Spoofing | overlay celebration trigger | accept | Client-only visual state; no money moved (money is server-authoritative). UX-integrity only. |
| T-057-SC | Tampering | package installs | accept | N/A — no packages installed (canvas-confetti already present, unchanged). |
</threat_model>

<verification>
`npx tsc --noEmit` + `npm test` green; overlays on paper with Stamp; no goal/badge confetti; triggerConfetti preserved.
</verification>

<success_criteria>
All always-mounted overlays reskinned with the Stamp ceremony; goal/badge confetti retired; no remnants.
</success_criteria>

<output>
Create `.planning/phases/05.7-kid-redesign/05.7-10-SUMMARY.md` when done.
</output>
