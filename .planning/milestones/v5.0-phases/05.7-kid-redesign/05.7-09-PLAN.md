---
phase: 05.7-kid-redesign
plan: 09
type: execute
wave: 2
depends_on: ["05.7-01", "05.7-03"]
files_modified:
  - components/chat/ChatThread.tsx
  - app/kid/chat/page.tsx
autonomous: true
requirements: [05.7-SC1]

must_haves:
  truths:
    - "ChatThread is theme-aware (theme: 'paper' | 'ink'), defaulting to 'paper' — its hardcoded KT/Nunito token object is gone (D-11, SC1)"
    - "Both paper and ink token values are built; /kid/chat consumes theme='paper' (ChatPanel untouched)"
    - "The kid chat screen marks the thread read via markChatRead when opened (D-04)"
    - "The kid chat screen uses the shared ScreenHeader"
  artifacts:
    - path: "components/chat/ChatThread.tsx"
      provides: "theme-aware chat thread sourced from lib/design/tokens"
      contains: "theme"
    - path: "app/kid/chat/page.tsx"
      provides: "paper chat screen + markChatRead on open"
      contains: "markChatRead"
  key_links:
    - from: "components/chat/ChatThread.tsx"
      to: "resolve/theme tokens from lib/design/tokens"
      via: "theme prop replaces the KT object"
      pattern: "theme"
    - from: "app/kid/chat/page.tsx"
      to: "markChatRead"
      via: "mark read on view"
      pattern: "markChatRead"
---

<objective>
Make ChatThread theme-aware (paper/ink, default paper), deleting its hardcoded Nunito/DM-Sans
`KT` token object — the only literal font remnant in kid-reachable code — and mark the chat
read when the kid opens it (D-11, D-04, SC1). Parent ChatPanel is NOT wired here (5.8 scope).

Purpose: SC1's "no Nunito remnants" bar and D-11's "both variants built." Per RESEARCH.md
Pitfall 4, ChatThread is consumed only by /kid/chat today; building the ink values is
forward-compat, not a ChatPanel migration.
Output: theme-aware ChatThread, paper chat screen that marks read on open.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md
@.planning/phases/05.7-kid-redesign/05.7-PATTERNS.md
@.planning/phases/05.7-kid-redesign/05.7-RESEARCH.md
@CLAUDE.md

<interfaces>
ChatThread.tsx KT object to retire (lines 32-41): hardcoded coral/teal/plum + fDisp:'"Nunito"...'
/ fBody:'"DM Sans"...'. Replace with a theme prop + a resolver mirroring components/design/
atoms.tsx resolve(theme) (lines 20-51), pulling paper/ink from lib/design/tokens and fonts from
base.fontDisplay/fontBody/fontMono. Default theme='paper' (its only consumer, /kid/chat, is
behavior-unchanged).
DO NOT wire components/parent-center/screens/ChatPanel.tsx (740 lines, separate impl, never
imports ChatThread) — build the ink values for forward-compat only (RESEARCH.md Pitfall 4 / A1).
markChatRead(memberId) from '@/lib/repositories/chat.repo' (Plan 03) — call when the kid opens chat.
ScreenHeader from '@/components/kid/design/ScreenHeader'; the chat page's own header/tab markup
duplicates ChatThread's header — consolidate onto ScreenHeader where it fits.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make ChatThread theme-aware (retire KT/Nunito)</name>
  <files>components/chat/ChatThread.tsx</files>
  <read_first>
    - components/chat/ChatThread.tsx (full — KT object 32-41; every KT.* usage throughout the 659 lines)
    - components/design/atoms.tsx (resolve(theme) pattern 20-51 to mirror)
    - lib/design/tokens.ts (paper, ink, base fonts)
    - .planning/phases/05.7-kid-redesign/05.7-RESEARCH.md (Pitfall 4; Assumption A1 — do not wire ChatPanel)
  </read_first>
  <action>
    Add a `theme: 'paper' | 'ink'` prop (default 'paper') to ChatThread. Replace the module-level
    `KT` object with a resolver that maps to lib/design/tokens: paper → paper.* grounds +
    base.fontDisplay/fontBody/fontMono; ink → ink.* grounds + the same base fonts. Delete the
    literal `"Nunito"`/`"DM Sans"` strings entirely. Replace every `KT.<key>` reference with the
    resolved value. Build real ink values (forward-compat for 5.8) but keep the default 'paper' so
    /kid/chat renders unchanged. Do NOT import or modify ChatPanel.tsx.
  </action>
  <verify>
    <automated>npx tsc --noEmit && ! grep -q "Nunito" components/chat/ChatThread.tsx && ! grep -q "DM Sans" components/chat/ChatThread.tsx && grep -q "theme" components/chat/ChatThread.tsx</automated>
  </verify>
  <acceptance_criteria>
    - No `Nunito` or `DM Sans` literal remains in ChatThread.tsx
    - ChatThread accepts a `theme` prop defaulting to `'paper'`; colors/fonts come from lib/design/tokens
    - No import of `ChatPanel` added
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ChatThread is theme-aware and remnant-free; ink values built without a ChatPanel consumer.</done>
</task>

<task type="auto">
  <name>Task 2: Kid chat screen — ScreenHeader + theme='paper' + mark read on open</name>
  <files>app/kid/chat/page.tsx</files>
  <read_first>
    - app/kid/chat/page.tsx (full — header/tab/view-toggle markup; where ChatThread mounts)
    - components/kid/design/ScreenHeader.tsx (Plan 03)
    - lib/repositories/chat.repo.ts (markChatRead from Plan 03)
    - .planning/phases/05.7-kid-redesign/05.7-CONTEXT.md (D-04 mark read; D-11 kid on paper; D-13 header)
  </read_first>
  <action>
    Mount `<ScreenHeader title={t('kidHeader.chat')} .../>` and render `<ChatThread theme="paper"/>`.
    Consolidate the page's duplicated header/tab markup onto ScreenHeader/the shared Tabs where it
    fits. On mount (and when new messages arrive while the screen is focused), call
    `markChatRead(memberId)` so the nav unread badge (Plan 08) clears — resolve memberId from the
    store/family_members as the page already does. Keep the realtime subscription and send flow
    unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "markChatRead" app/kid/chat/page.tsx && grep -q "theme=\"paper\"" app/kid/chat/page.tsx && grep -q "ScreenHeader" app/kid/chat/page.tsx && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `app/kid/chat/page.tsx` renders `<ChatThread theme="paper"/>` and mounts `ScreenHeader`
    - `markChatRead` is called on chat open (clears the nav unread badge)
    - Realtime/send flow unchanged
    - `npx tsc --noEmit` and `npm test` exit 0
  </acceptance_criteria>
  <done>Kid chat on paper with a unified header; opening the thread marks it read.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client(child) → markChatRead write | Own-row read-marker write under Plan 03's RLS. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-057-16 | Tampering | markChatRead | mitigate | Writes only the caller's own family_members row (RLS WITH CHECK user_id=auth.uid(), Plan 03); memberId resolves to the authenticated member. |
| T-057-17 | Information Disclosure | ChatThread theme values | accept | Ink token values are static design constants; no data exposure. |
| T-057-SC | Tampering | package installs | accept | N/A — no packages installed. |
</threat_model>

<verification>
`npx tsc --noEmit` + `npm test` green; no Nunito/DM Sans remnant; /kid/chat on paper; mark-read wired; ChatPanel untouched.
</verification>

<success_criteria>
ChatThread theme-aware and remnant-free; kid chat on paper with mark-read clearing the nav badge.
</success_criteria>

<output>
Create `.planning/phases/05.7-kid-redesign/05.7-09-SUMMARY.md` when done.
</output>
