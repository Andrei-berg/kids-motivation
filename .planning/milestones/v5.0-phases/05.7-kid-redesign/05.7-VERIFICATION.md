---
phase: 05.7-kid-redesign
verified: 2026-07-19T04:25:00Z
status: passed
score: 3/3 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 2/3
  gaps_closed:
    - "All kid screens use unified tokens/atoms; no Nunito/coral remnants (ROADMAP SC1) — fixed in commit 44c88dd"
  gaps_remaining: []
  regressions: []
---

# Phase 05.7: Kid Redesign Verification Report

**Phase Goal:** Kid UI fully on the unified system — warm paper theme, ledger rows, consolidated navigation.
**Verified:** 2026-07-19T04:25:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (initial verification 2026-07-19 found SC1 gap; closed by commit `44c88dd`)

## Goal Achievement

### Observable Truths

| # | Truth (ROADMAP Success Criterion) | Status | Evidence |
|---|---|---|---|
| 1 | All kid screens use unified tokens/atoms; no Nunito/coral remnants (SC1) | ✓ VERIFIED | Re-verified after fix `44c88dd`: `grep -rn "FF6B35\|E8551F\|FFE4D6" app/kid components/kid components/chat components/design` → 0 hits; `grep -rn "Nunito"` in the same scope → 0 hits. `ChatThread.resolveChatTokens` now sources coral/coralDeep/coralSoft from `base.indigo/indigoDeep/indigoSoft`, teal from `base.success`, sun/sunDeep from `base.gold/goldDeep` (matching the 05.3 `components/kid/design/tokens.ts` mapping); `Avatar`'s default `shirt` is `T.coral` (= `base.indigo`); the fire-mood swatch uses `T.warn`; the legend-rarity bg uses `T.coralSoft`. All 5 kid screens mount the shared `ScreenHeader` (token-driven); `app/kid/layout.tsx` uses `paper.bg`/`paper.ink`; kid login, shop, wallet, achievements, day all consume `lib/design/tokens` directly or via the 05.3 `T` re-export. |
| 2 | Navigation has 5 items; leaderboard lives as a tab inside Awards (SC2) | ✓ VERIFIED | `components/kid/KidNav.tsx` `TAB_DEFS` has exactly 5 entries (День/Кошелёк/Награды/Магазин/Чат) — re-confirmed post-fix; `app/kid/leaderboard/` does not exist (re-confirmed); only reference to `kid/leaderboard` repo-wide is a code comment in `lib/kid/rating-rank.ts` documenting the retired anti-pattern; `app/kid/achievements/page.tsx` has a `tabRating` tab wired to `rankChildren()` (N-child podium/list) with a 1-child "vs last week" solo view (D-08). Build output confirms no `app/kid/leaderboard` route. |
| 3 | Coin credit uses the signature stamp+count-up gesture; confetti fires only for streak bonuses and level-ups (SC3) | ✓ VERIFIED | `app/kid/day/page.tsx`: `Stamp` fires only when `savedResult.creditedCoins > 0` (server-confirmed, via `detectAward` from `lib/kid/awardResult.ts` — never `coinsPreview`); `triggerConfetti()` fires only `if (result.hasStreak || result.leveledUp)` (line 159, re-confirmed post-fix). Ordinary day-form toggles (`RoomBlock`/`ActivitiesBlock`/`BookBlock`/`CustomBlock`) use the `Tick` micro-tick atom (200ms, no particles, `prefers-reduced-motion` guarded via `matchMedia`), not confetti. `triggerGoalConfetti` is `@deprecated` with zero live callers (only the deferred dead-code `components/GoalsModal.tsx`). |

**Score:** 3/3 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `lib/hooks/useDesktop.ts` | Shared breakpoint hook | ✓ VERIFIED | Exports `useDesktop`; imported by 4 kid pages + `KidNav.tsx` |
| `components/design/atoms.tsx` | Tabs / Tick / Stamp / useCountUp / Amount / LedgerRow atoms | ✓ VERIFIED | All present, theme-aware via `resolve(theme)`, reduced-motion guarded |
| `public/locales/en.json` / `ru.json` | Full key parity for phase-5.7 keys | ✓ VERIFIED | Flattened-key diff: 0 gaps in phase scope; only pre-existing, out-of-scope `dailyModal.*` (32 keys, legacy parent UI) is en-only |
| `lib/kid/level.ts`, `lib/kid/rating-rank.ts`, `lib/kid/awardResult.ts` | Pure fns for level-up/ranking/award parsing | ✓ VERIFIED | `computeLevelUp`/`levelForXp` (formula matches `lib/services/badges.service.ts:582` exactly), `rankChildren` (N-child podium/list), `detectAward` (server-confirmed only) all present with passing tests |
| `app/api/wallet/award/route.ts` | Additive `appliedSources`/`appliedItems` fields | ✓ VERIFIED | Both fields present, additive-only, response still driven by `creditAwards` |
| `supabase/migrations/05.7-01-chat-read-marker.sql` + `05.7-02-chat-read-marker-fix.sql` | `chat_last_read_at` column + safe own-row write path | ✓ VERIFIED (live DB) | Confirmed against production Postgres: `chat_last_read_at` column exists, `mark_chat_read()` is `SECURITY DEFINER`, and the vulnerable `family_members_self_update` policy from `05.7-01` is **absent** (dropped by `05.7-02`, commit `b5b74a2`) |
| `lib/repositories/chat.repo.ts` | `getChatUnreadCount` + `markChatRead` | ✓ VERIFIED | `markChatRead` calls `supabase.rpc('mark_chat_read')` (not a raw `.update`), matching the hardened migration |
| `components/kid/design/ScreenHeader.tsx` | Unified header on all 5 kid screens | ✓ VERIFIED | Mounted on all 5 kid screens; Amount/gold balance + D-03 logout (Day variant, danger-colored confirm dialog); Avatar fallback now token-driven via `T.coral` (= `base.indigo`) after `44c88dd` |
| `components/kid/KidNav.tsx` | 5-tab ledger nav, unread badge, no FAB | ✓ VERIFIED | 5 `TAB_DEFS`, `getChatUnreadCount` wired (30s poll + on route change), `components/kid/KidChatFAB.tsx` does not exist, zero remaining references |
| `app/kid/leaderboard/page.tsx` | DELETED | ✓ VERIFIED | Directory absent; build output has no `app/kid/leaderboard` route |
| `components/chat/ChatThread.tsx` | Theme-aware (paper/ink), Nunito/coral literals gone | ✓ VERIFIED | `resolveChatTokens(theme)` sources grounds from `paper.*`/`ink.*`, fonts from `base.font*`, and (after `44c88dd`) all accents from `base.indigo/success/gold` families — zero raw legacy hexes; `theme` prop defaults to `'paper'`, ink values built for 5.8; parent `ChatPanel.tsx` untouched |
| `utils/confetti.ts` | `triggerGoalConfetti` deprecated, `triggerConfetti` preserved | ✓ VERIFIED | `@deprecated` JSDoc present; zero live callers outside the deferred dead-code `GoalsModal.tsx` |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `KidDayFillForm.creditAwardAndFinish` | `/api/wallet/award` | `fetch(...)` → `detectAward` | ✓ WIRED | Response parsed by `detectAward`, never `coinsPreview`; `onSaved({creditedCoins, hasStreak, leveledUp, appliedItems})` |
| `app/kid/day/page.tsx` | `Stamp` / `triggerConfetti` | `savedResult.creditedCoins > 0` / `hasStreak \|\| leveledUp` gating | ✓ WIRED | Confirmed by direct read of the gating conditionals |
| `KidNav.tsx` chat tab | `getChatUnreadCount` | `family_members.chat_last_read_at` poll | ✓ WIRED | Counts real unread rows via RLS-readable SELECT |
| `app/kid/chat/page.tsx` | `markChatRead` | on-open + on new realtime message while visible | ✓ WIRED | RPC call confirmed in `lib/repositories/chat.repo.ts` |
| `app/kid/achievements/page.tsx` Rating tab | `rankChildren` | `{podium, list} = rankChildren(entries)` | ✓ WIRED | Confirmed; solo-compare branch present for N=1 |
| `components/kid/design/ScreenHeader.tsx` | `Avatar` (design atom) | default fallback avatar | ✓ WIRED (token-driven) | `Avatar` default `shirt = T.coral` → `base.indigo` after `44c88dd` |
| `lib/kid/level.ts levelForXp` | `lib/services/badges.service.ts:582` | `Math.floor(xp/1000)+1` parity | ✓ WIRED | Formulas identical |
| `en.json` ↔ `ru.json` | key parity | flattened-key diff | ✓ WIRED | 0 phase-scope gaps; `achievements.tabRating` present in both |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| 05.7-SC1 | 01,03,04,05,06,07,08,09,10,12 | Unified tokens/atoms, no Nunito/coral remnants | ✓ SATISFIED | Truth 1 (post-fix `44c88dd`) |
| 05.7-SC2 | 01,02,03,06,08,12 | Nav 6→5, leaderboard merged into Awards | ✓ SATISFIED | Truth 2 |
| 05.7-SC3 | 01,02,04,05,10,11,12 | Signature stamp+count-up gesture; confetti scoped to streak/level-up | ✓ SATISFIED | Truth 3 |

**Note (documentation hygiene, non-blocking):** `.planning/REQUIREMENTS.md`'s "v5.0 Requirements (success-criteria traceability)" table registers SC-IDs only through phase 5.5; `05.7-SC1/SC2/SC3` rows are missing despite all 12 phase plans declaring them in `requirements:` frontmatter. Verification proceeded directly against ROADMAP.md's authoritative Success Criteria text. The three rows should be registered per the file's own stated convention.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| (all phase-touched files) | — | No `TBD`/`FIXME`/`XXX` debt markers | — | Clean |
| `components/kid/KidDayFillForm.tsx` | 82-85 | Pre-existing mood swatches for neutral/meh/sad/tired remain literal hexes (`#4ECDC4`, `#F5A623`, `#6C5CE7`, `#FF8FB1`) — teal literal is a legacy value but SC1's wording targets Nunito/coral; `#6C5CE7` equals `base.indigo` | ℹ️ Info | Pre-existing, tiny mood-picker swatches; candidates for 5.11 legacy-cleanup |
| `app/kid/day/page.tsx` | 179 | Desktop sidebar tint `rgba(61,190,168,0.03)` — literal RGB, not token-sourced | ℹ️ Info | 3% opacity, pre-existing |
| `app/kid/wallet/page.tsx` | 95-99 | 3 of 4 wallet quick-action links dead (`/kid/analytics` 404s, `#goals`/`#gift` anchors missing) — REVIEW.md WR-01 | ⚠️ Warning (advisory, not SC-blocking) | Broken affordance; tracked in 05.7-REVIEW.md |
| `app/kid/wallet/page.tsx`, `app/kid/achievements/page.tsx` | various | `normalizeDate(new Date())` (UTC date) instead of `localDateString()` — REVIEW.md WR-02 | ⚠️ Warning (advisory) | Week-range off-by-one near local midnight–3am |
| `components/kid/KidDayFillForm.tsx` | 513, 529-537 | Post-award `checkAndAwardBadges` exception path can strand save / duplicate grade rows — REVIEW.md WR-03 | ⚠️ Warning (advisory) | Edge-case error path |
| `lib/repositories/chat.repo.ts` | 4-18 | `getMessages` loads oldest 50, not newest — REVIEW.md WR-04 (pre-existing ordering, made user-visible by unread badge) | ⚠️ Warning (advisory) | Chat history window |
| `components/chat/ChatThread.tsx` | multiple | Hardcoded Russian UI strings — REVIEW.md WR-05 | ⚠️ Warning (advisory) | Mixed-language chat for en-locale families |
| `app/kid/shop/page.tsx` | 289-322 | Approval sheet stamps «ПОЛУЧЕНО» on a timer, not real approval state — REVIEW.md WR-06 | ⚠️ Warning (advisory) | Stamp semantics premature |

The six ⚠️ items are the known advisory findings already logged in `05.7-REVIEW.md`; per operator decision they are not phase-goal blockers and none falsifies a must-have.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full test suite (post-fix) | `npm test` | 71 passed / 26 skipped, 0 failed | ✓ PASS |
| Type check (post-fix) | `npx tsc --noEmit` | exit 0 | ✓ PASS |
| Coral/Nunito remnant sweep (post-fix) | `grep -rn "FF6B35\|E8551F\|FFE4D6\|Nunito" app/kid components/kid components/chat components/design` | 0 hits | ✓ PASS |
| CR-01 fix live on prod DB | direct `pg` query against `SUPABASE_DB_URL` | `family_members_self_update` policy absent; `mark_chat_read()` present, `prosecdef=true`; `chat_last_read_at` column present | ✓ PASS |
| `/kid/leaderboard` route removed | filesystem + build output check | absent | ✓ PASS |

### Probe Execution

No probes declared for this phase (UI/design-system phase, not migration/tooling) and none found at `scripts/*/tests/probe-*.sh`. Skipped per Step 7c.

### Human Verification Required

None. SC1/SC2/SC3 visual/interactive sign-off was completed and approved by the operator at the plan-12 checkpoint in the running app. The one code-level contradiction found in the initial pass (coral literals below the operator's visual audit threshold) has been fixed in `44c88dd` and re-verified programmatically.

### Gaps Summary

None remaining. The initial verification (same day) found one SC1 gap — literal `#FF6B35`/`#E8551F`/`#FFE4D6` coral remnants in `ChatThread.resolveChatTokens`, the `Avatar` default shirt (inherited by the shared `ScreenHeader` on all 5 kid screens), the fire-mood swatch, and the legend-rarity background. Commit `44c88dd` retired all four: ChatThread accents now source `base.indigo/success/gold` families (matching the 05.3 token mapping), `Avatar` defaults to `T.coral` (= `base.indigo`), the mood swatch uses `T.warn`, and the rarity bg uses `T.coralSoft`. A post-fix repo grep across `app/kid`, `components/kid`, `components/chat`, and `components/design` returns zero raw coral hexes and zero `Nunito` references; tsc and the 71-test suite are green; SC2/SC3 evidence re-confirmed unregressed (fix commit touched no nav/gating code paths).

Phase goal achieved: kid UI is on the unified system (paper theme, ledger rows, token-driven atoms), navigation is consolidated to 5 tabs with the leaderboard living as a ranked tab inside Awards, and motion is disciplined to the single signature stamp+count-up gesture with confetti scoped to streak bonuses and level-ups under a `prefers-reduced-motion` guard.

---

_Verified: 2026-07-19T04:25:00Z_
_Verifier: Claude (gsd-verifier)_
