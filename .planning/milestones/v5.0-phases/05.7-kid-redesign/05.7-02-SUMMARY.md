---
phase: 05.7-kid-redesign
plan: 02
subsystem: testing
tags: [vitest, pure-functions, wallet-award, leaderboard, xp-levels]

# Dependency graph
requires:
  - phase: 05.6-day-blocks
    provides: lib/day-blocks.ts precedent (pure function + colocated vitest, node environment)
provides:
  - "lib/kid/level.ts — levelForXp/computeLevelUp pure XP-level-crossing helpers"
  - "lib/kid/rating-rank.ts — rankChildren N-children podium/list splitter"
  - "lib/kid/awardResult.ts — detectAward defensive parser of the award response"
  - "appliedSources field on /api/wallet/award success response (additive-only)"
affects: [05.7-day-motion, 05.7-achievements, kid-leaderboard]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pure lib/kid/*.ts modules (no I/O, no React) with colocated tests/*.test.ts — same shape as lib/day-blocks.ts"
    - "Additive-only API response fields for money routes: append new keys, never touch existing crediting logic"

key-files:
  created:
    - lib/kid/level.ts
    - lib/kid/rating-rank.ts
    - lib/kid/awardResult.ts
    - tests/level-up.test.ts
    - tests/rating-rank.test.ts
    - tests/award-result.test.ts
  modified:
    - app/api/wallet/award/route.ts

key-decisions:
  - "rankChildren ties broken by ascending id string comparison for deterministic output"
  - "detectAward reads only creditedCoins/appliedSources from the award response, never coinsPreview, per server-authoritative UX-integrity invariant"

patterns-established:
  - "Pattern: N-children pure ranking replaces hardcoded 2-child .find() lookups — podium is always slice(0,3), list is the remainder"

requirements-completed: [05.7-SC3, 05.7-SC2]

# Metrics
duration: 5min
completed: 2026-07-17
---

# Phase 05.7 Plan 02: Pure Functions for Level, Rating, Award-Result Summary

**Three pure lib/kid modules (level-crossing, N-children podium ranking, award-response parsing) with Vitest coverage, plus one additive `appliedSources` field on `/api/wallet/award` unlocking server-confirmed stamp/confetti triggers.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-07-17T00:26:01+03:00 (first commit)
- **Completed:** 2026-07-17T00:26:56+03:00 (last commit)
- **Tasks:** 2 completed
- **Files modified:** 7 (4 created, 1 modified, 2 test files created for Task 1 + 1 for Task 2)

## Accomplishments
- `lib/kid/level.ts` mirrors `lib/services/badges.service.ts:582`'s `Math.floor(xp/1000)+1` formula exactly, with a pure `computeLevelUp` crossing check
- `lib/kid/rating-rank.ts` generalizes the leaderboard to N children, replacing the hardcoded 2-child `.find()` anti-pattern seen in `app/kid/leaderboard/page.tsx` with a deterministic `slice(0,3)` podium/list split
- `/api/wallet/award` now exposes `appliedSources` (derived from the existing `applied` array) so clients can detect a credited `'streak'` without touching `creditAwards`/`AwardIntent`/idempotency logic
- `lib/kid/awardResult.ts` provides a defensive `detectAward()` that never throws, even on `{}`/`undefined`/`null`

## Task Commits

Each task was committed atomically (TDD RED -> GREEN per task):

1. **Task 1: Pure functions level.ts + rating-rank.ts with tests**
   - `c09e780` (test) — failing tests for level-up and rating-rank (RED)
   - `8b84b4d` (feat) — level.ts + rating-rank.ts implementation (GREEN)
2. **Task 2: Award response additive field + detectAward helper + test**
   - `ff816d1` (test) — failing test for detectAward (RED)
   - `52478a4` (feat) — appliedSources field + awardResult.ts (GREEN)

_TDD tasks produced separate RED (test) and GREEN (feat) commits per the plan's `tdd="true"` tasks._

## Files Created/Modified
- `lib/kid/level.ts` - `levelForXp(xp)` and `computeLevelUp(prevXp, newXp)`, pure, no imports
- `lib/kid/rating-rank.ts` - `RankEntry` type + `rankChildren(entries)` returning `{ podium, list }`
- `lib/kid/awardResult.ts` - `detectAward(res)` returning `{ creditedCoins, hasStreak }`
- `tests/level-up.test.ts` - level-cross behavior cases (900->1100 true, 1100->1500 false, 1500->1000 false, boundary values)
- `tests/rating-rank.test.ts` - 3-entry ordering, 4-entry podium/list split, 1-entry solo, tie-break determinism
- `tests/award-result.test.ts` - non-streak sources, streak detection, defensive defaults on `{}`/`undefined`/`null`
- `app/api/wallet/award/route.ts` - single-line additive change: `appliedSources: applied.map(a => a.sourceType)` appended to the existing success response object; no other lines touched

## Decisions Made
- Followed the plan's exact tie-break spec (ascending `id`) for `rankChildren` — matches "deterministic" requirement in `<behavior>` without inventing a secondary sort key not specified by the plan
- Verified via `git diff` that the award-route change is a true one-line addition (confirmed in Task 2 verification) — no risk to the money-mutation invariant in CLAUDE.md

## Deviations from Plan

None - plan executed exactly as written. Both tasks matched their `<action>` specs verbatim; no Rule 1-4 triggers encountered.

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- `lib/kid/level.ts`, `lib/kid/rating-rank.ts`, and `lib/kid/awardResult.ts` are ready for consumption by the Wave 2 Achievements plan and Wave 3 Day-motion plan (per this plan's stated purpose)
- `appliedSources` is live on the award response in this worktree branch; any consumer (e.g. Day-motion stamp/confetti trigger) can call `detectAward(awardResponseJson)` directly
- `app/kid/leaderboard/page.tsx` itself was NOT modified in this plan (out of scope — plan only extracted the pure ranking function); a follow-up plan should wire `rankChildren` into that page to retire the 2-child `.find()` pattern
- Full `npm test` (71 passed, 26 skipped) and `npx tsc --noEmit` both green after this plan's changes

---
*Phase: 05.7-kid-redesign*
*Completed: 2026-07-17*

## Self-Check: PASSED

All created/modified files verified present on disk; all task and metadata commit hashes (`c09e780`, `8b84b4d`, `ff816d1`, `52478a4`, `088ce9a`) confirmed in `git log`.
