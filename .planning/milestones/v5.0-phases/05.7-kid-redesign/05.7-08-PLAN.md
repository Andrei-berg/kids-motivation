---
phase: 05.7-kid-redesign
plan: 08
type: execute
wave: 2
depends_on: ["05.7-01", "05.7-03"]
files_modified:
  - components/kid/KidNav.tsx
  - app/kid/layout.tsx
  - components/kid/KidChatFAB.tsx
  - app/kid/login/page.tsx
autonomous: true
requirements: [05.7-SC1, 05.7-SC2]

must_haves:
  truths:
    - "The kid nav has exactly 5 tabs: День · Кошелёк · Награды · Магазин · Чат (D-01)"
    - "The bar is a full-width ledger panel with a paper.line hairline top border; active tab is indigo for all tabs, inactive ink3 (D-02)"
    - "The mobile chat FAB is deleted and no longer mounted (D-01)"
    - "The floating top-right logout button is removed; logout lives in the Day header (D-03)"
    - "The chat tab shows a real unread counter from getChatUnreadCount (D-04)"
    - "app/kid/layout.tsx uses paper tokens, not hardcoded #FFFBF5/#1A1423 (SC1)"
    - "The kid login screen is reskinned to the paper theme; PIN/lockout logic untouched (D-10)"
    - "D-15: the desktop keeps the 4.5 layout structure (sticky columns, sidebar); the sidebar nav becomes the same 5-item set in the new style — only the visual language changes"
  artifacts:
    - path: "components/kid/KidNav.tsx"
      provides: "5-tab ledger-panel nav (mobile bar + desktop sidebar) with unread badge"
      contains: "getChatUnreadCount"
    - path: "components/kid/KidChatFAB.tsx"
      provides: "DELETED"
    - path: "app/kid/login/page.tsx"
      provides: "Paper-theme login reskin"
  key_links:
    - from: "components/kid/KidNav.tsx chat tab"
      to: "getChatUnreadCount"
      via: "unread badge count"
      pattern: "getChatUnreadCount"
    - from: "app/kid/layout.tsx"
      to: "paper.bg / paper.ink tokens"
      via: "token-driven background"
      pattern: "paper\\."
---

<objective>
Rebuild the kid navigation as a 5-tab full-width ledger panel (chat promoted to a tab, unread
badge, indigo active state), delete the FAB and floating logout, fix the layout's hardcoded
colors to paper tokens, and reskin the login screen — D-01, D-02, D-03, D-04(display), D-10, SC1/SC2.

Purpose: SC2 requires exactly 5 nav items with leaderboard gone; this is the chrome + entry plan.
Output: rebuilt KidNav, token-fixed layout, deleted FAB, reskinned login.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md
@.planning/phases/05.7-kid-redesign/05.7-PATTERNS.md
@CLAUDE.md

<interfaces>
New tab set (D-01): /kid/day, /kid/wallet, /kid/achievements, /kid/shop, /kid/chat
  labels t('kidNav.myDay'|'wallet'|'awards'|'shop'|'chat'). Reuse the existing TabIcon SVG
  dispatcher (KidNav.tsx:18-66) — it already has a 'chat' icon case (line 58). Drop the 'vs'
  leaderboard tab entirely.
D-02 style: full-width paper.card panel with a paper.line hairline top border (mobile bottom bar)
  — remove the floating glass pill (rgba(255,255,255,0.92) + backdropFilter + borderRadius:26 at
  KidNav.tsx:117-155). Active tab = indigo (paper.accent) icon+bold label for ALL tabs; inactive
  = paper.ink3. Nav height >=64px with >=44px tap targets. Desktop sidebar (KidNav.tsx:85-115)
  becomes the same 5-item set in the new style (D-15).
D-03: delete the mobile floating logout (KidNav.tsx:157-173); logout now lives in the Day
  ScreenHeader (Plan 03/04). Desktop sidebar logout may stay or move — keep behavior, use kidNav.logout*.
D-04 display: chat tab shows unread count via getChatUnreadCount(familyId, memberId, lastReadAt)
  from '@/lib/repositories/chat.repo' (Plan 03). Read the member's chat_last_read_at + familyId
  (family_members). Render a small count badge (repurpose StatusChip/dot) with aria t('kidNav.unreadAria').
useDesktop from '@/lib/hooks/useDesktop' (replace KidNav's own breakpoint usage).
layout.tsx: replace background:'#FFFBF5', color:'#1A1423' (lines 28 & 47) with paper.bg / paper.ink
  from '@/lib/design/tokens'; remove the <KidChatFAB/> mount and its import (both branches).
Login (app/kid/login/page.tsx): PIN/lockout logic (lookupFamily/signInWithPin//api/kid/login
  fetch, lines 29-90) is UNTOUCHED — reskin JSX/styles below line 94 only; replace ad-hoc amber
  (#f59e0b/#d97706) and fontFamily 'Inter, system-ui' with paper.accent / base.fontDisplay / base.fontBody.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild KidNav (5-tab ledger panel + unread badge) + layout token fix + delete FAB</name>
  <files>components/kid/KidNav.tsx, app/kid/layout.tsx, components/kid/KidChatFAB.tsx</files>
  <read_first>
    - components/kid/KidNav.tsx (full — TAB_DEFS 10-16, TabIcon 18-66, desktop sidebar 85-115, mobile bar 117-155, floating logout 157-173)
    - app/kid/layout.tsx (full — hardcoded colors lines 28 & 47; KidChatFAB mount lines 9,34,52)
    - lib/repositories/chat.repo.ts (getChatUnreadCount signature from Plan 03)
    - lib/design/tokens.ts (paper.bg/ink/accent/ink3/line/card)
    - .planning/phases/05.7-kid-redesign/05.7-CONTEXT.md (D-01/D-02/D-03/D-04/D-15)
  </read_first>
  <action>
    Rewrite `TAB_DEFS` to the 5 D-01 tabs (day/wallet/achievements/shop/chat), dropping the color
    field (single indigo active state, not per-tab rainbow). Rebuild the mobile bar as a full-width
    paper.card panel with a paper.line hairline top border (no backdropFilter/floating pill/border
    radius), each tab >=44px tap target inside a >=64px bar; active tab icon+label = paper.accent
    bold, inactive = paper.ink3. Rebuild the desktop sidebar to the same 5-item set in the new
    style. Delete the mobile floating logout button (D-03). Add an unread badge on the chat tab:
    on mount, resolve the member's familyId + chat_last_read_at (family_members) and call
    `getChatUnreadCount(...)`, render a count badge with `aria-label={t('kidNav.unreadAria',{count})}`
    when >0; refresh on route change / an interval or the existing realtime channel. Import
    `useDesktop` from the shared hook. In `app/kid/layout.tsx`, replace the two hardcoded
    background/color literals with `paper.bg`/`paper.ink` from tokens, remove the `<KidChatFAB/>`
    mounts (both branches) and its import. Then delete `components/kid/KidChatFAB.tsx`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "getChatUnreadCount" components/kid/KidNav.tsx && ! grep -q "leaderboard" components/kid/KidNav.tsx && ! grep -q "KidChatFAB" app/kid/layout.tsx && test ! -f components/kid/KidChatFAB.tsx && ! grep -q "#FFFBF5" app/kid/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `TAB_DEFS` (or equivalent) has exactly 5 entries ending in `/kid/chat`; no `/kid/leaderboard`, no `vs` tab
    - Mobile bar has no `backdropFilter`/`borderRadius: 26` floating pill; uses a paper.line top border
    - Mobile floating logout button removed; `KidChatFAB.tsx` deleted and unmounted
    - Chat tab calls `getChatUnreadCount` and renders an aria-labelled unread badge
    - `app/kid/layout.tsx` uses `paper.bg`/`paper.ink`; no `#FFFBF5`/`#1A1423` literal
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>5-tab ledger-panel nav with unread badge; FAB + floating logout gone; layout on paper tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin kid login to the paper theme (logic untouched)</name>
  <files>app/kid/login/page.tsx</files>
  <read_first>
    - app/kid/login/page.tsx (full — PIN/lockout logic 29-90 is OFF-LIMITS; JSX/styles from ~94; 3-step progress 118-137, 230-245, 380-395)
    - lib/design/tokens.ts (paper.*, base.fontDisplay/fontBody)
    - .planning/phases/05.7-kid-redesign/05.7-CONTEXT.md (D-10 login reskin; first brand impression)
    - .planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md (Color, Typography, Copywriting)
  </read_first>
  <action>
    Reskin only the JSX/inline styles (below line ~94): replace ad-hoc amber `#f59e0b`/`#d97706`
    with `paper.accent`, `fontFamily: 'Inter, system-ui, sans-serif'` with `base.fontDisplay`/
    `base.fontBody`, and page background/surfaces with paper.bg/paper.card tones. Preserve the
    3-step flow structure and the progress-bar/back-button layout. Do NOT touch lookupFamily,
    signInWithPin, the `/api/kid/login` fetch, lockout handling, or any auth logic (lines 29-90).
    Use `paper.dangerText` for lockout/error messaging.
  </action>
  <verify>
    <automated>npx tsc --noEmit && ! grep -q "Inter, system-ui" app/kid/login/page.tsx && ! grep -q "#f59e0b" app/kid/login/page.tsx && grep -q "api/kid/login" app/kid/login/page.tsx && npm test</automated>
  </verify>
  <acceptance_criteria>
    - No `Inter, system-ui` fontFamily and no `#f59e0b`/`#d97706` amber literal remain
    - The `/api/kid/login` fetch and PIN/lockout code paths are unchanged (still present)
    - Errors/lockout use `paper.dangerText`
    - `npx tsc --noEmit` and `npm test` exit 0
  </acceptance_criteria>
  <done>Login screen on the paper theme; auth logic byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| pre-auth client → /api/kid/login | Login page posts PIN; reskin does not touch this boundary. |
| client(child) → getChatUnreadCount read | Unread count read under existing family-RLS. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-057-14 | Spoofing | login PIN flow | mitigate | Auth logic (verify_child_pin, lockout, synthetic-user model) untouched — reskin is JSX/style only; no change to the pre-auth boundary. |
| T-057-15 | Information Disclosure | unread badge count | mitigate | getChatUnreadCount is RLS-scoped to the child's own family_id; count reveals only own-family message volume. |
| T-057-SC | Tampering | package installs | accept | N/A — no packages installed. |
</threat_model>

<verification>
`npx tsc --noEmit` + `npm test` green; 5-tab nav, ledger panel, unread badge; FAB + floating logout removed; layout + login on paper tokens; auth logic unchanged.
</verification>

<success_criteria>
Nav is the 5-item ledger panel with a real unread counter; FAB/floating-logout deleted; layout + login reskinned to paper.
</success_criteria>

<output>
Create `.planning/phases/05.7-kid-redesign/05.7-08-SUMMARY.md` when done.
</output>
