---
phase: 05.7-kid-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/hooks/useDesktop.ts
  - components/design/atoms.tsx
  - public/locales/en.json
  - public/locales/ru.json
autonomous: true
requirements: [05.7-SC1, 05.7-SC2]

must_haves:
  truths:
    - "A single useDesktop() hook exists; kid screens can import it instead of re-declaring it"
    - "A theme-aware Tabs atom exists that renders active tab in indigo (paper) per D-02/D-05"
    - "A Tick micro-tick primitive exists that draws a checkmark in <=200ms with no particles (D-18)"
    - "All new/renamed kid-screen copy keys exist in BOTH en.json and ru.json (D-01,D-05,D-08,D-13,D-17,D-20 labels)"
  artifacts:
    - path: "lib/hooks/useDesktop.ts"
      provides: "Shared window.innerWidth>=1024 breakpoint hook"
      exports: ["useDesktop"]
    - path: "components/design/atoms.tsx"
      provides: "Tabs (theme-aware) + Tick micro-tick, added to existing atom kit"
      contains: "export function Tabs"
    - path: "public/locales/en.json"
      provides: "English copy for every new/renamed 5.7 key"
    - path: "public/locales/ru.json"
      provides: "Russian copy for every new/renamed 5.7 key"
  key_links:
    - from: "components/design/atoms.tsx Tabs"
      to: "resolve(theme)"
      via: "theme-resolved colors (accent for active)"
      pattern: "resolve\\(theme\\)"
    - from: "public/locales/en.json"
      to: "public/locales/ru.json"
      via: "key parity"
      pattern: "achievements\\.tabRating"
---

<objective>
Build the shared primitives every Wave-2 kid screen consumes, so the screen reskins
never hand-roll a fourth tab bar / fifth useDesktop copy, and never touch the shared
locale files in parallel.

Purpose: This is the interface-first foundation. Registering all i18n keys HERE (not
per-screen) keeps `public/locales/*.json` out of every Wave-2 plan's `files_modified`,
letting the seven screen reskins run in parallel without locale-file collisions.
Output: `useDesktop` hook, theme-aware `Tabs` atom, `Tick` micro-tick atom, and the
complete phase i18n key set in both locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md
@.planning/phases/05.7-kid-redesign/05.7-PATTERNS.md
@CLAUDE.md

<interfaces>
Existing atom kit (components/design/atoms.tsx, do NOT fork — extend in place):
- `resolve(theme: 'paper'|'ink')` (module-internal, atoms.tsx:20-51) returns
  { text, muted, mutedText, goldText, leader, success, warning, danger, ...Text }.
  It does NOT currently return `accent` — add `accent` to both branches
  (paper.accent / ink.accent) so Tabs can color the active tab indigo.
- `Stamp` (atoms.tsx:250-288) is the reference for the reduced-motion guard pattern:
  keyframes injected once via a guarded `<style>` inside `@media (prefers-reduced-motion:
  no-preference)`, plus a JS post-mount check dropping the animation. Tick MUST reuse this
  exact double-guard shape — do NOT add a new guard style.

Tabs port source (components/parent-center/ui.tsx:330-358, ink-hardcoded today):
  function Tabs({ tabs, value, onChange, scroll }: {
    tabs: { id: string; label: string; icon?: string }[]
    value: string; onChange: (id: string) => void; scroll?: boolean })
  Replace T.bg1/T.cardHi/T.cardBorder/T.text/T.muted with resolve(theme) output.

useDesktop source (verbatim in app/kid/{wallet,achievements,shop,day}/page.tsx):
  const [is,setIs]=useState(false); useEffect(()=>{const c=()=>setIs(window.innerWidth>=1024);
  c(); window.addEventListener('resize',c); return()=>window.removeEventListener('resize',c)},[])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract useDesktop hook + add Tabs and Tick atoms</name>
  <files>lib/hooks/useDesktop.ts, components/design/atoms.tsx</files>
  <read_first>
    - components/design/atoms.tsx (see resolve() at lines 20-51 and Stamp at 250-288 — the guard pattern to reuse)
    - components/parent-center/ui.tsx (lines 330-358 — the Tabs shape to port)
    - app/kid/wallet/page.tsx (lines 17-26 — the useDesktop copy to extract verbatim)
    - .planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md (Motion Contract row "checkbox toggle", Typography Atom-Owned Type)
    - lib/design/tokens.ts (paper.accent = #5B4BD4, ink.accent = #8B7BF5)
  </read_first>
  <action>
    Create `lib/hooks/useDesktop.ts` exporting a named `useDesktop(): boolean` hook —
    the verbatim `window.innerWidth >= 1024` + resize-listener implementation currently
    duplicated in the kid pages. Mark `'use client'`.

    In `components/design/atoms.tsx`: (a) add `accent: paper.accent` to the paper branch
    and `accent: inkTheme.accent` to the ink branch of `resolve()`. (b) Add
    `export function Tabs({ tabs, value, onChange, theme = 'paper', scroll = false })`
    ported from parent-center/ui.tsx:330-358, using `resolve(theme)` colors: active tab
    background = `c.card`-equivalent surface (use the theme card via a small local: paper→#FFFFFF, ink→resolve card is not in resolve, so read `paper.card`/`ink.card` from tokens import already present), active label + optional icon color = `c.accent` with fontWeight 700, inactive label = `c.muted`; container hairline border = `c.leader`; each tab button min-height 44 (touch target per UI-SPEC). (c) Add
    `export function Tick({ on, theme = 'paper' }: { on: boolean; theme?: AtomTheme })` —
    an inline SVG checkmark whose stroke draws in via a stroke-dashoffset keyframe (<=200ms)
    plus a short scale pulse, gated by the SAME guarded-`<style>` + `@media
    (prefers-reduced-motion: no-preference)` + JS post-mount pattern used by `Stamp`
    (reuse a second STYLE_ID constant e.g. `fc-tick-kf`); under reduced motion it renders
    the final checked/unchecked state with no animation. No `canvas-confetti`, no particles.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "export function Tabs" components/design/atoms.tsx && grep -q "export function Tick" components/design/atoms.tsx && grep -q "export function useDesktop" lib/hooks/useDesktop.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/hooks/useDesktop.ts` contains `export function useDesktop` and `'use client'`
    - `components/design/atoms.tsx` contains `export function Tabs` and `export function Tick`
    - `resolve()` paper branch and ink branch each return an `accent` field
    - `Tick` reuses the Stamp-style guarded `@media (prefers-reduced-motion: no-preference)` keyframe injection (grep `no-preference` appears >=2 times in atoms.tsx)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>useDesktop hook extracted; Tabs and Tick atoms exported from the shared kit; tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Register all phase i18n keys in en.json and ru.json</name>
  <files>public/locales/en.json, public/locales/ru.json</files>
  <read_first>
    - public/locales/en.json (existing kidNav.*, achievements.*, wallet.* dotted-key structure)
    - public/locales/ru.json (mirror structure)
    - .planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md (Copywriting Contract table — exact ru/en strings)
    - .planning/phases/05.7-kid-redesign/05.7-CONTEXT.md (D-01 tab set, D-05 tab names, D-08 solo copy, D-20 stamp copy)
  </read_first>
  <action>
    Add (or update the value of) every new/renamed key this phase introduces, in BOTH
    files, same nesting, using the exact strings from UI-SPEC Copywriting Contract. Required keys:
    nav — `kidNav.myDay` (День/My day), `kidNav.wallet` (Кошелёк/Wallet), `kidNav.awards`
    (Награды/Awards — the renamed trophies tab), `kidNav.shop` (Магазин/Shop), `kidNav.chat`
    (Чат/Chat), `kidNav.unreadAria` (Непрочитанные сообщения: {{count}} / {{count}} unread messages),
    `kidNav.logout`, and `kidNav.logoutConfirm.title` (Выйти из аккаунта?/Log out?),
    `.body` (Тебе нужно будет снова войти по коду и PIN./You'll need to sign back in with your code and PIN.),
    `.cancel` (Отмена/Cancel), `.confirm` (Выйти/Log out);
    awards — `achievements.tabBadges` (Бейджи/Badges), `.tabStreaks` (Стрики/Streaks),
    `.tabRating` (Рейтинг/Rating), `.soloCompareHeading` (Ты против себя/You vs. last week),
    `.soloCompareEmpty` (per UI-SPEC);
    day — `kidDay.saveDay` (Сохранить день/Save day), `kidDay.saveError` (per UI-SPEC);
    wallet — `wallet.emptyHeading` (Пока пусто/Nothing here yet), `wallet.emptyBody` (per UI-SPEC);
    header titles — `kidHeader.day`, `.wallet`, `.awards`, `.shop`, `.chat` (contextual screen titles);
    stamp — `stamp.received` (ПОЛУЧЕНО/Received), `stamp.goalReached` (ЦЕЛЬ ДОСТИГНУТА/Goal reached).
    Follow the frontend-design writing rules (plain verbs, sentence case, no leading emoji on new copy).
    Do NOT delete or rename any existing key still referenced elsewhere; only add/retune values.
  </action>
  <verify>
    <automated>node -e "const e=require('./public/locales/en.json'),r=require('./public/locales/ru.json');const flat=(o,p='')=>Object.entries(o).flatMap(([k,v])=>typeof v==='object'&&v?flat(v,p+k+'.'):[p+k]);const ek=new Set(flat(e)),rk=new Set(flat(r));const req=['achievements.tabRating','achievements.soloCompareHeading','kidDay.saveDay','wallet.emptyHeading','stamp.received','stamp.goalReached','kidNav.chat','kidNav.logoutConfirm.title'];const missing=req.filter(k=>!ek.has(k)||!rk.has(k));if(missing.length){console.error('MISSING',missing);process.exit(1)}console.log('i18n keys present in both locales')"</automated>
  </verify>
  <acceptance_criteria>
    - Both en.json and ru.json parse as valid JSON
    - Every required key listed in the action exists in BOTH files (verify command exits 0)
    - `achievements.tabRating`, `stamp.received`, `stamp.goalReached`, `kidNav.logoutConfirm.title` present in both locales
  </acceptance_criteria>
  <done>All phase copy keys registered in both locales; parity check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | This plan adds only client-render primitives + static locale strings. No endpoints, no inputs, no data reads/writes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-057-01 | Information Disclosure | locale strings | accept | Static UI copy, no secrets; low value. |
| T-057-SC | Tampering | package installs | accept | N/A — this phase installs zero new packages (RESEARCH.md Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
`npx tsc --noEmit` green; both locale files valid JSON with parity on required keys; `npm test` still green (no test subject touched).
</verification>

<success_criteria>
useDesktop/Tabs/Tick available for import; all phase i18n keys live in both locales; tsc + test suite green.
</success_criteria>

<output>
Create `.planning/phases/05.7-kid-redesign/05.7-01-SUMMARY.md` when done.
</output>
