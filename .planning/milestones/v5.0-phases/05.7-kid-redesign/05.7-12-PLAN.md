---
phase: 05.7-kid-redesign
plan: 12
type: execute
wave: 4
depends_on: ["05.7-04", "05.7-05", "05.7-06", "05.7-07", "05.7-08", "05.7-09", "05.7-10", "05.7-11"]
files_modified:
  - public/locales/en.json
  - public/locales/ru.json
autonomous: false
requirements: [05.7-SC1, 05.7-SC2, 05.7-SC3]

must_haves:
  truths:
    - "en.json and ru.json have full key parity for every kid-screen key used this phase (no locale falls back)"
    - "No coral/Nunito remnant remains in any kid-reachable file (SC1)"
    - "The kid nav has exactly 5 items and /kid/leaderboard 404s (SC2)"
    - "The operator has verified the stamp+count-up gesture and confetti scoping in the running app (SC3)"
    - "The dead-code modals (ShopModal/ExchangeModal/GoalsModal) disposition is decided by the operator"
  artifacts:
    - path: "public/locales/en.json"
      provides: "Verified key parity with ru.json"
  key_links:
    - from: "public/locales/en.json"
      to: "public/locales/ru.json"
      via: "full key parity"
      pattern: "achievements\\.tabRating"
---

<objective>
Close the phase: prove full-suite + build green, verify en/ru key parity, sweep for any residual
coral/Nunito remnant, then run the consolidated human-verify of the three success criteria and
the dead-code-modals decision (D-12 flagged items).

Purpose: This repo has no jsdom/component-render tests (RESEARCH.md), so SC1/SC2/SC3's visual
correctness is verified by the operator here — matching the 05.3/05.6 consolidated-checkpoint precedent.
Output: green gate + parity fix (if needed) + operator sign-off + dead-code disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md
@.planning/phases/05.7-kid-redesign/05.7-VALIDATION.md
@.planning/phases/05.7-kid-redesign/05.7-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Green gate + i18n parity + remnant sweep</name>
  <files>public/locales/en.json, public/locales/ru.json</files>
  <read_first>
    - public/locales/en.json, public/locales/ru.json (full key trees)
    - .planning/phases/05.7-kid-redesign/05.7-VALIDATION.md (full-suite command)
    - .planning/phases/05.7-kid-redesign/05.7-RESEARCH.md (SC1 remnant sources: ChatThread KT, layout hardcodes, legacy T key names)
  </read_first>
  <action>
    Run the full gate: `npm test && npx tsc --noEmit && npm run build`. Compute flattened-key
    parity between en.json and ru.json; for any key present in one file but missing in the other,
    add the missing translation (both locales, following the UI-SPEC copy voice) — this is the only
    file edit this plan makes. Then grep kid-reachable code for residual remnants: `Nunito`,
    `DM Sans`, `#FFFBF5`, `#f59e0b`, and legacy brand-color key names (`T.coral`, `T.teal`,
    `T.sun`, `T.pink`, `T.plum`) across `app/kid/`, `components/kid/`, `components/chat/`. Record
    any hit as a residual-remnant finding for the operator checkpoint (Task 2) — do NOT silently
    fix code here beyond locale parity; code remnants indicate a screen plan missed something and
    should be surfaced.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run build && node -e "const e=require('./public/locales/en.json'),r=require('./public/locales/ru.json');const flat=(o,p='')=>Object.entries(o).flatMap(([k,v])=>typeof v==='object'&&v?flat(v,p+k+'.'):[p+k]);const ek=new Set(flat(e)),rk=new Set(flat(r));const only=[...ek].filter(k=>!rk.has(k)).concat([...rk].filter(k=>!ek.has(k)));if(only.length){console.error('KEY PARITY GAP',only);process.exit(1)}console.log('en/ru parity OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test`, `npx tsc --noEmit`, `npm run build` all exit 0
    - en.json/ru.json flattened-key parity check exits 0 (no key in one locale only)
    - A residual-remnant grep report is produced (Nunito/DM Sans/#FFFBF5/#f59e0b/legacy T.* names) with the hit list (empty or itemized) for Task 2
  </acceptance_criteria>
  <done>Full suite + build green; locales at parity; remnant sweep report ready for operator.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator verification of SC1/SC2/SC3 + dead-code disposition</name>
  <what-built>
    The kid UI is fully on the family-bank paper system: 5-tab ledger-panel nav (chat as a tab,
    leaderboard folded into Awards' Rating tab), unified ScreenHeader, hero wallet, receipt-style
    day form with micro-tick toggles, shop on Amount/StatusChip, theme-aware chat, and the
    signature day-save stamp+count-up with confetti reserved for streak bonus + level-up.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and sign in as a kid.
    1. SC1 (no remnants): open /kid/day, /kid/wallet, /kid/achievements, /kid/shop, /kid/chat,
       and /kid/login — confirm warm paper theme everywhere, no coral/orange or rounded-bubble
       Nunito styling; gold appears ONLY on money figures. Cross-check the Task 1 remnant report:
       any grep hit means a screen is not fully migrated.
    2. SC2 (nav): confirm exactly 5 tabs (День · Кошелёк · Награды · Магазин · Чат), the chat tab
       shows an unread count that clears when you open chat, and visiting /kid/leaderboard 404s.
    3. SC3 (motion): fill and save a day — confirm the "+N 🪙" stamp fires AFTER the save (matching
       the header balance count-up), ordinary checkbox toggles show a quick tick (no confetti), and
       confetti fires only on a streak bonus or a level-up. Toggle OS reduce-motion and re-verify
       animations degrade to instant.
    4. Dead-code modals: decide whether ShopModal.tsx / ExchangeModal.tsx / GoalsModal.tsx (zero
       import sites, confirmed dead) should be deleted now or deferred to Phase 5.11.
    5. Chat: confirm the parent chat (ChatPanel) still works unchanged.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms SC1, SC2, SC3 pass in the running app (or files specific gaps)
    - Operator records the dead-code-modals decision (delete now vs defer to 5.11)
    - Any residual remnant from Task 1's report is triaged (fix-now vs follow-up)
  </acceptance_criteria>
  <resume-signal>Type "approved" (optionally with the dead-code decision), or list issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Verification + locale parity only; no runtime code paths added. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-057-21 | Tampering | phase gate | mitigate | Full suite + build + tsc must pass before sign-off; blocks a broken merge. |
| T-057-SC | Tampering | package installs | accept | N/A — no packages installed this phase. |
</threat_model>

<verification>
Full suite + build + tsc green; en/ru parity clean; operator sign-off on SC1/SC2/SC3 and dead-code disposition.
</verification>

<success_criteria>
Phase gate green; locales at parity; the three success criteria verified in the running app; dead-code modals decided.
</success_criteria>

<output>
Create `.planning/phases/05.7-kid-redesign/05.7-12-SUMMARY.md` when done.
</output>
