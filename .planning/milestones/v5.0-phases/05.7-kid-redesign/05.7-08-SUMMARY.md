---
phase: 05.7-kid-redesign
plan: 08
subsystem: kid-ui
tags: [nav, ledger-panel, unread-badge, paper-theme, login-reskin]
requires:
  - 05.7-01 (useDesktop shared hook)
  - 05.7-03 (getChatUnreadCount + chat_last_read_at marker)
provides:
  - 5-tab ledger-panel kid nav (mobile bar + desktop sidebar) with real unread badge
  - paper-token kid layout (no hardcoded colors)
  - paper-theme login screen
affects:
  - 05.7-09 (chat screen marks read → badge clears)
  - 05.7-12 (leaderboard route deletion — nav no longer links to it)
tech-stack:
  added: []
  patterns:
    - single indigo active state for nav (D-02, indigo = interactive)
    - direct paper/base token imports in nav/layout (no legacy T re-export)
key-files:
  created: []
  modified:
    - components/kid/KidNav.tsx
    - app/kid/layout.tsx
    - app/kid/login/page.tsx
  deleted:
    - components/kid/KidChatFAB.tsx
decisions:
  - "Chat tab label uses existing kidNav.chat key; awards tab switched from kidNav.trophies to the existing kidNav.awards key (D-01 label set День · Кошелёк · Награды · Магазин · Чат)"
  - "Unread badge refresh: 30s interval + refetch on route change (pathname in effect deps) — no new realtime channel to avoid channel-leak surface"
  - "Badge caps display at 9+ and uses paper.danger fill with white 10px text; aria-label from kidNav.unreadAria"
  - "Desktop sidebar logout kept in place (PATTERNS.md allowed either); icon recolored to paper.accent (interactive per UI-SPEC)"
  - "Login code/PIN inputs use base.fontMono (JetBrains Mono) for digit legibility; buttons flat paper.accent (gradients removed)"
metrics:
  duration: ~35 min (across a session-limit boundary)
  tasks: 2
  files: 4
  completed: 2026-07-18
---

# Phase 05.7 Plan 08: Kid Nav Ledger Panel + Login Reskin Summary

5-tab ledger-panel kid nav (chat promoted to a tab with a real getChatUnreadCount badge, FAB and floating logout deleted), kid layout on paper tokens, and the login screen reskinned to the paper theme with auth logic byte-identical.

## Tasks

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Rebuild KidNav (5-tab ledger panel + unread badge) + layout token fix + delete FAB | 2910357 | components/kid/KidNav.tsx, app/kid/layout.tsx, components/kid/KidChatFAB.tsx (deleted) |
| 2 | Reskin kid login to the paper theme (logic untouched) | a4cbbe5 | app/kid/login/page.tsx |

## What Was Built

### KidNav (D-01/D-02/D-03/D-04/D-15)
- `TAB_DEFS` is exactly 5 entries: `/kid/day, /kid/wallet, /kid/achievements, /kid/shop, /kid/chat` — leaderboard tab and the per-tab rainbow `color` field removed; the `vs` TabIcon case dropped.
- Mobile bar: full-width `paper.card` panel with a `paper.line` hairline top border, `minHeight: 64`, each tab a full-height ≥44px tap target. No `backdropFilter`, no `borderRadius: 26` floating pill. Active tab = `paper.accent` icon + 700-weight label; inactive = `paper.ink3`, 500 weight (UI-SPEC two-weight rule).
- Desktop sidebar (D-15): same 5-item set, structure preserved (fixed left 64px column), restyled to `paper.card` + `paper.line` border and indigo active tint. Sidebar logout button kept (PATTERNS.md planner's-call), icon recolored to `paper.accent`.
- Mobile floating top-right logout button deleted (D-03) — logout lives in the Day ScreenHeader (Plan 03/04).
- Unread badge (D-04 display): on mount and every 30s (plus on route change), resolves the member's `family_id` + `chat_last_read_at` from `family_members` by `child_id = activeMemberId`, calls `getChatUnreadCount(familyId, memberId, lastReadAt)`, renders a `paper.danger` count badge (9+ cap) with `aria-label={t('kidNav.unreadAria', { count })}` on the chat tab in both bar and sidebar. Cancellation guard + interval cleanup in the effect.
- Breakpoint via the shared `useDesktop()` hook (05.7-01) instead of Tailwind `lg:` classes for the two nav variants.

### Layout (SC1)
- `app/kid/layout.tsx`: both branches now use `paper.bg`/`paper.ink` from `@/lib/design/tokens`; `#FFFBF5`/`#1A1423` literals gone. `<KidChatFAB/>` mounts and import removed; `components/kid/KidChatFAB.tsx` deleted (no other references existed).

### Login (D-10)
- All ad-hoc amber (`#f59e0b`/`#d97706`/amber gradient bg) replaced with `paper.accent` / `base.indigoSoft` / `paper.bg`; `'Inter, system-ui, sans-serif'` replaced with `base.fontBody` (root) + `base.fontDisplay` (step headings) + `base.fontMono` (code/PIN inputs).
- Errors and lockout messaging (`kidLogin.lockedRetry`, `wrongPinRetry`, etc.) render in `paper.dangerText`.
- 3-step flow, progress bar, and back-button layout preserved. `lookupFamily`, `signInWithPin`, the `/api/kid/login` fetch, and 429-lockout handling are byte-identical (verified via git diff — zero hunks touch lines 29–90 logic).

## Deviations from Plan

None - plan executed exactly as written.

## Verification

- `npx tsc --noEmit` — exit 0
- `npm test` — 71 passed, 26 skipped (8 files passed, 4 skipped)
- Task 1 grep gates: `getChatUnreadCount` present in KidNav; no `leaderboard`, no `KidChatFAB` in layout, no `#FFFBF5`; `KidChatFAB.tsx` deleted
- Task 2 grep gates: no `Inter, system-ui`, no `#f59e0b`; `api/kid/login` fetch still present
- Key links: KidNav chat tab → `getChatUnreadCount` ✓; layout → `paper.bg`/`paper.ink` ✓

## Known Stubs

None — the unread badge is wired to the real `getChatUnreadCount` query.

**Deployment dependency (carried from Plan 03):** migration `supabase/migrations/05.7-01-chat-read-marker.sql` (adds `family_members.chat_last_read_at`) was not yet applied to prod as of Plan 03's summary. Until applied, the nav's `family_members` select including `chat_last_read_at` will error on prod → badge silently stays 0. Apply the migration (idempotent) before verifying the badge live.

## Threat Flags

None — no new security surface beyond the plan's threat model (T-057-14 login boundary untouched; T-057-15 unread count read is family-RLS-scoped).

## Self-Check: PASSED

- components/kid/KidNav.tsx — FOUND
- app/kid/layout.tsx — FOUND
- app/kid/login/page.tsx — FOUND
- components/kid/KidChatFAB.tsx — CONFIRMED DELETED
- Commit 2910357 — FOUND
- Commit a4cbbe5 — FOUND
