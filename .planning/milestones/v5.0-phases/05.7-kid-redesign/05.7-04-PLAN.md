---
phase: 05.7-kid-redesign
plan: 04
type: execute
wave: 2
depends_on: ["05.7-01", "05.7-02", "05.7-03"]
files_modified:
  - components/kid/KidDayFillForm.tsx
  - components/kid/day-blocks/RoomBlock.tsx
  - components/kid/day-blocks/ActivitiesBlock.tsx
  - components/kid/day-blocks/BookBlock.tsx
  - components/kid/day-blocks/CustomBlock.tsx
  - components/kid/day-blocks/BlockCard.tsx
  - app/kid/day/page.tsx
autonomous: true
requirements: [05.7-SC1, 05.7-SC3]

must_haves:
  truths:
    - "The day form renders each block through a decomposed per-block renderer in the family-bank language (receipt cards, ledger price rows) (D-09)"
    - "Ordinary checkbox toggles show a <=200ms micro-tick, not confetti (D-18)"
    - "No triggerConfetti call remains in KidDayFillForm's toggle handlers"
    - "Save/award behavior is byte-identical: handleSubmit still POSTs /api/wallet/award with {childId,date}"
  artifacts:
    - path: "components/kid/day-blocks/BlockCard.tsx"
      provides: "Shared receipt-style ledger card wrapper (replaces FillSection visually)"
      exports: ["default"]
    - path: "components/kid/day-blocks/RoomBlock.tsx"
      provides: "Room checklist renderer with micro-tick toggles"
    - path: "components/kid/KidDayFillForm.tsx"
      provides: "Reskinned form delegating to block renderers; save logic unchanged"
      contains: "/api/wallet/award"
  key_links:
    - from: "components/kid/day-blocks/RoomBlock.tsx"
      to: "Tick atom"
      via: "micro-tick on toggle"
      pattern: "Tick"
    - from: "components/kid/KidDayFillForm.tsx"
      to: "assembleDayBlocks"
      via: "unchanged 5.6 block-type→renderer seam"
      pattern: "assembleDayBlocks|block_type"
---

<objective>
Decompose the 1417-line KidDayFillForm into per-block renderer components restyled in the
family-bank language, and replace every per-toggle confetti burst with the Tick micro-tick
(D-09, D-18). Save/award logic is untouched — render-only.

Purpose: SC1 requires no old-style surface on the busiest kid screen; SC3 requires killing
the "confetti on every checkbox" policy. This plan does the visual + structural work; the
day-save stamp/count-up orchestration (D-17/D-19) is Plan 11 (Wave 3), which builds on this.
Output: BlockCard + 4 block renderers, reskinned KidDayFillForm, reskinned day/page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md
@.planning/phases/05.7-kid-redesign/05.7-PATTERNS.md
@.planning/phases/05.6-day-blocks/05.6-CONTEXT.md
@CLAUDE.md

<interfaces>
Shared atoms (from Plan 01/03): Tick, Tabs from '@/components/design/atoms';
LedgerRow, Amount from '@/components/design/atoms'; ScreenHeader default from
'@/components/kid/design/ScreenHeader'; useDesktop from '@/lib/hooks/useDesktop'.

The 5.6 seam to PRESERVE (do not fork): lib/day-blocks.ts assembleDayBlocks(blocks,
dayType, date); each row's block_type/legacy_key selects a renderer. Reskin the renderers'
inner JSX only — keep the assembly/selection switch intact.

FillSection wrapper being replaced visually (KidDayFillForm.tsx:64-76) → becomes BlockCard
(receipt-style ledger card).

Confetti call sites to REMOVE (grep -n triggerConfetti components/kid/KidDayFillForm.tsx):
room ~635, activity ×2 ~681/1116 (mobile+desktop), book ×2 ~923/1367, custom ~998, plus
the <Confetti trigger={confettiTrig}/> mount ~1028. Each toggle shape:
  onClick={() => { toggleRoomTask(task.id); if (!on) triggerConfetti() }}
→ replace triggerConfetti() with the Tick micro-tick render on that item (no particles).

DO NOT MODIFY (Plan 11 owns these lines): handleSubmit's fetch('/api/wallet/award') block
(KidDayFillForm.tsx:586-616) and onSaved(coinsPreview) — leave behavior byte-identical here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BlockCard shell + extract room/activities/book/custom renderers</name>
  <files>components/kid/day-blocks/BlockCard.tsx, components/kid/day-blocks/RoomBlock.tsx, components/kid/day-blocks/ActivitiesBlock.tsx, components/kid/day-blocks/BookBlock.tsx, components/kid/day-blocks/CustomBlock.tsx</files>
  <read_first>
    - components/kid/KidDayFillForm.tsx (FillSection 64-76; room block ~600-660; activity blocks ~670-700 & ~1100-1130; book blocks ~910-960 & ~1350-1380; custom block ~980-1010)
    - components/design/atoms.tsx (LedgerRow, Amount, Tick signatures)
    - .planning/phases/05.7-kid-redesign/05.7-PATTERNS.md (KidDayFillForm section, lines 197-258)
    - .planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md (Spacing, Typography, Color rules; Motion Contract toggle row)
  </read_first>
  <action>
    Create `components/kid/day-blocks/BlockCard.tsx` — a `'use client'` receipt-style card
    wrapper `{ title: string; icon?: string; sub?: string; children }` on paper.card with
    paper.line hairline, 16px padding (md), Heading-role title (18px/700 Bitter), replacing
    FillSection's visual role. Extract the four built-in block bodies into
    RoomBlock/ActivitiesBlock/BookBlock/CustomBlock, each a `'use client'` component taking the
    props the current inline JSX already uses (the toggle state + toggle callbacks passed down
    from KidDayFillForm — keep the SAME callback identities/signatures, this is extraction not
    rewrite). Inside each, render item toggles with the `Tick` micro-tick (checked state) and
    ledger price rows via `LedgerRow`/`Amount` (paper, gold only on the coin figure). Do NOT
    call triggerConfetti anywhere. Coin values come from props/existing preview data — never
    hardcode coin amounts (CLAUDE.md).
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f components/kid/day-blocks/BlockCard.tsx && grep -rq "Tick" components/kid/day-blocks/ && ! grep -rq "triggerConfetti" components/kid/day-blocks/</automated>
  </verify>
  <acceptance_criteria>
    - BlockCard.tsx + RoomBlock/ActivitiesBlock/BookBlock/CustomBlock.tsx all exist
    - Each renderer imports `Tick` and renders no `triggerConfetti` call (grep on the day-blocks dir returns 0 confetti hits)
    - Coin figures render through `LedgerRow`/`Amount`; no hardcoded coin literal added
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Four block renderers + shared receipt card extracted in family-bank language, micro-tick toggles, zero confetti.</done>
</task>

<task type="auto">
  <name>Task 2: Wire KidDayFillForm to renderers + reskin day/page.tsx, remove confetti sites</name>
  <files>components/kid/KidDayFillForm.tsx, app/kid/day/page.tsx</files>
  <read_first>
    - components/kid/KidDayFillForm.tsx (full — especially the block-selection switch, both mobile+desktop render paths, and the confetti sites listed in interfaces)
    - app/kid/day/page.tsx (lines 26-35 useDesktop copy; 154-158 onSaved/confetti gating; sticky-sidebar desktop layout from 4.5)
    - .planning/phases/05.7-kid-redesign/05.7-CONTEXT.md (D-09, D-15 keep 4.5 desktop layout, D-18)
  </read_first>
  <action>
    In `components/kid/KidDayFillForm.tsx`: replace the inline built-in block JSX (both mobile
    and desktop render paths) with `<RoomBlock/> <ActivitiesBlock/> <BookBlock/> <CustomBlock/>`
    wrapped in `<BlockCard>`, keeping the existing assembleDayBlocks-driven selection/switch and
    all toggle state/callbacks. Remove ALL six `triggerConfetti()` toggle calls AND the
    `<Confetti trigger={confettiTrig}/>` mount (~1028) and its now-unused `confettiTrig` state and
    `triggerConfetti`/`Confetti` imports. DO NOT touch handleSubmit's `/api/wallet/award` fetch or
    `onSaved(coinsPreview)` — leave byte-identical (Plan 11 owns the stamp wiring). Replace the
    local `useDesktop` copy with the shared `import { useDesktop } from '@/lib/hooks/useDesktop'`.
    In `app/kid/day/page.tsx`: mount `<ScreenHeader title={t('kidHeader.day')} showLogout .../>`
    (D-03/D-13), replace its local useDesktop copy with the shared hook, keep the 4.5 sticky
    sidebar layout (D-15). Leave the onSaved coins gating as-is for now (Plan 11 replaces it).
  </action>
  <verify>
    <automated>npx tsc --noEmit && ! grep -q "triggerConfetti" components/kid/KidDayFillForm.tsx && grep -q "/api/wallet/award" components/kid/KidDayFillForm.tsx && grep -q "useDesktop" app/kid/day/page.tsx && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep triggerConfetti components/kid/KidDayFillForm.tsx` returns 0 hits; `<Confetti` mount removed
    - `KidDayFillForm.tsx` still contains the `/api/wallet/award` POST with `{ childId, date }` body unchanged
    - Both KidDayFillForm and day/page import `useDesktop` from `@/lib/hooks/useDesktop` (no local copy)
    - `app/kid/day/page.tsx` mounts `ScreenHeader` with `showLogout`
    - `npx tsc --noEmit` and `npm test` exit 0
  </acceptance_criteria>
  <done>Day form delegates to reskinned block renderers, all per-toggle confetti gone, save logic intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Render-only reskin. The client→/api/wallet/award boundary is unchanged (same {childId,date} POST). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-057-09 | Tampering | day save/award flow | mitigate | handleSubmit fetch + onSaved left byte-identical; coin values sourced from props, never hardcoded — no client-side money write (CLAUDE.md). |
| T-057-SC | Tampering | package installs | accept | N/A — no packages installed. |
</threat_model>

<verification>
`npx tsc --noEmit` + `npm test` green; zero confetti in day form/blocks; award POST unchanged; day screen carries ScreenHeader.
</verification>

<success_criteria>
Day form fully in family-bank language with micro-tick toggles; block renderers decomposed; save/award behavior preserved.
</success_criteria>

<output>
Create `.planning/phases/05.7-kid-redesign/05.7-04-SUMMARY.md` when done.
</output>
