---
phase: 05.7-kid-redesign
plan: 07
type: execute
wave: 2
depends_on: ["05.7-01", "05.7-03"]
files_modified:
  - app/kid/shop/page.tsx
autonomous: true
requirements: [05.7-SC1]

must_haves:
  truths:
    - "Shop keeps the product card grid but prices render via the gold mono Amount atom (D-14)"
    - "Purchase request statuses render via StatusChip (D-14)"
    - "The real/virtual segmented control uses the shared Tabs atom (retires the hand-rolled one)"
    - "Approved purchases/exchanges show a Stamp ceremony ('ПОЛУЧЕНО'), not confetti (D-20)"
    - "Cards restyled to paper tones; no coral/teal/sun legacy color names remain"
  artifacts:
    - path: "app/kid/shop/page.tsx"
      provides: "Card grid + Amount prices + StatusChip statuses + Tabs + paper tones"
      contains: "StatusChip"
  key_links:
    - from: "app/kid/shop/page.tsx price"
      to: "Amount(money)"
      via: "gold mono price"
      pattern: "Amount"
    - from: "app/kid/shop/page.tsx status"
      to: "StatusChip"
      via: "pending/approved/rejected pill"
      pattern: "StatusChip"
---

<objective>
Restyle the kid shop to the family-bank language: keep the product card grid, swap prices to
the gold `Amount` atom, statuses to `StatusChip`, the real/virtual toggle to the shared `Tabs`
atom, and give approved purchases/exchanges the Stamp ceremony (D-14, D-20).

Purpose: SC1 requires the shop off the legacy CoinPill / hand-rolled status pills and coral tones.
Output: reskinned app/kid/shop/page.tsx on paper atoms.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md
@.planning/phases/05.7-kid-redesign/05.7-PATTERNS.md
@CLAUDE.md

<interfaces>
Atoms: Amount, StatusChip, Tabs, Stamp from '@/components/design/atoms';
ScreenHeader from '@/components/kid/design/ScreenHeader'; useDesktop from '@/lib/hooks/useDesktop'.
Replacements (app/kid/shop/page.tsx as-built):
- CoinPill value={item.price_coins} (lines 163/204/287) → <Amount value={price} theme="paper" money size="md"/>
- hand-rolled status pill (264-267) → <StatusChip tone={approved?'success':rejected?'danger':'warning'} theme="paper">{label}</StatusChip>
- hand-rolled real/virtual segmented control (124-139) → <Tabs theme="paper" .../>
- local useDesktop (5-14) → shared hook
Purchase flow (requestPurchase server action) and price/coin math are UNCHANGED — reskin only.
D-20: when a purchase/exchange flips to approved, show a Stamp('ПОЛУЧЕНО' = t('stamp.received')) instead of confetti.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shop card grid reskin — Amount prices, StatusChip, Tabs, ScreenHeader</name>
  <files>app/kid/shop/page.tsx</files>
  <read_first>
    - app/kid/shop/page.tsx (full — CoinPill sites 163/204/287; status pill 264-267; tab bar 124-139; approval sheet state; local useDesktop 5-14)
    - components/design/atoms.tsx (Amount, StatusChip, Tabs, Stamp)
    - components/kid/design/ScreenHeader.tsx (from Plan 03)
    - .planning/phases/05.7-kid-redesign/05.7-CONTEXT.md (D-14 shop; D-15 desktop layout)
    - .planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md (Color gold-money invariant; StatusChip tones)
  </read_first>
  <action>
    Mount `<ScreenHeader title={t('kidHeader.shop')} .../>`. Keep the product card grid (kids need
    visual goods) but restyle cards to paper.card / paper.line tones. Replace every `CoinPill`
    price with `<Amount value={price} theme="paper" money size="md"/>` (gold, mono). Replace the
    hand-rolled purchase-status pill with `<StatusChip tone=... theme="paper">`. Replace the
    real/virtual segmented control with the shared `<Tabs theme="paper">`. Swap the local useDesktop
    for the shared hook and keep the 4.5 sticky/desktop grid layout (D-15). Leave the purchase
    request flow (requestPurchase) and all price/coin math untouched. Remove any legacy
    T.coral/T.teal/T.sun usages introduced in markup this reskin touches.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "StatusChip" app/kid/shop/page.tsx && grep -q "Amount" app/kid/shop/page.tsx && grep -q "Tabs" app/kid/shop/page.tsx && ! grep -q "CoinPill" app/kid/shop/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Prices render via `Amount ... money`; no `CoinPill` remains in the file
    - Statuses render via `StatusChip`; the real/virtual toggle uses the shared `Tabs`
    - `ScreenHeader` mounted; `useDesktop` from the shared hook
    - No `T.coral`/`T.teal`/`T.sun` in markup this reskin touched
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Shop grid on paper atoms with gold Amount prices, StatusChip statuses, shared Tabs.</done>
</task>

<task type="auto">
  <name>Task 2: Stamp ceremony on approved purchase/exchange</name>
  <files>app/kid/shop/page.tsx</files>
  <read_first>
    - app/kid/shop/page.tsx (the purchase/exchange status-change handling; any current confetti call)
    - components/design/atoms.tsx (Stamp)
    - .planning/phases/05.7-kid-redesign/05.7-CONTEXT.md (D-20 approved purchases/exchanges → stamp, lose confetti)
    - .planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md (Motion Contract: purchase approved → Stamp -8° 450ms)
  </read_first>
  <action>
    When a purchase or exchange transitions to approved (the point that currently triggers any
    celebratory confetti, if present), render a `<Stamp>` ceremony with `t('stamp.received')`
    ('ПОЛУЧЕНО') on the relevant card/sheet instead of confetti. Reuse the Stamp atom's built-in
    reduced-motion guard; do not import canvas-confetti. If the shop page never fired confetti on
    approval, still add the Stamp ceremony on the approved transition per D-20. Do not change the
    approval/refund server logic.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "Stamp" app/kid/shop/page.tsx && grep -q "stamp.received" app/kid/shop/page.tsx && ! grep -q "canvas-confetti" app/kid/shop/page.tsx && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Approved purchase/exchange renders a `Stamp` with `t('stamp.received')`
    - No `canvas-confetti` import in the shop page
    - `npx tsc --noEmit` and `npm test` exit 0
  </acceptance_criteria>
  <done>Approved purchases/exchanges celebrate with the Stamp ceremony, not confetti.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Reskin only; purchase request/approve server actions (service-role) unchanged. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-057-13 | Tampering | purchase price display | mitigate | Prices rendered from server-provided reward data via Amount; no client-side price/coin mutation; purchase remains a server action (CLAUDE.md). |
| T-057-SC | Tampering | package installs | accept | N/A — no packages installed. |
</threat_model>

<verification>
`npx tsc --noEmit` + `npm test` green; shop on paper atoms; gold only on prices; approved → Stamp.
</verification>

<success_criteria>
Shop fully on the shared atom kit (Amount/StatusChip/Tabs) with the Stamp ceremony on approval.
</success_criteria>

<output>
Create `.planning/phases/05.7-kid-redesign/05.7-07-SUMMARY.md` when done.
</output>
