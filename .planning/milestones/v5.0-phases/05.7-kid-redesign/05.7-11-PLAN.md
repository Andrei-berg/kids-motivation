---
phase: 05.7-kid-redesign
plan: 11
type: execute
wave: 3
depends_on: ["05.7-04", "05.7-02"]
files_modified:
  - components/kid/KidDayFillForm.tsx
  - app/kid/day/page.tsx
autonomous: true
requirements: [05.7-SC3]

must_haves:
  truths:
    - "After a day save, the signature stamp '+N 🪙' fires ONLY after /api/wallet/award resolves, keyed to creditedCoins (D-17)"
    - "The header balance count-ups to the new server-confirmed total (D-17)"
    - "coinsPreview is never stamped/animated — only server-confirmed numbers are (D-17, Pitfall 1)"
    - "Confetti fires only when the award response credited a 'streak' source OR the XP level crossed upward (D-19)"
    - "Awarded items render as ledger rows in the day summary (D-17)"
  artifacts:
    - path: "components/kid/KidDayFillForm.tsx"
      provides: "Stamp+count-up orchestration wired to the award response"
      contains: "detectAward"
    - path: "app/kid/day/page.tsx"
      provides: "Stamp/count-up + streak/level-up confetti gating"
  key_links:
    - from: "components/kid/KidDayFillForm.tsx"
      to: "detectAward(awardResponse)"
      via: "server-confirmed creditedCoins/hasStreak"
      pattern: "detectAward"
    - from: "app/kid/day/page.tsx"
      to: "computeLevelUp"
      via: "level-crossing confetti"
      pattern: "computeLevelUp"
---

<objective>
Wire the one orchestrated motion moment: after a day save, read the /api/wallet/award response,
stamp the server-confirmed "+N 🪙", count-up the header balance, render awarded items as ledger
rows, and fire confetti only for a credited streak source or an XP level-up (D-17, D-19, SC3).

Purpose: This is the phase's signature interaction and the SC3 payoff. It depends on Plan 04's
decomposed day form and Plan 02's appliedSources field + detectAward/computeLevelUp helpers.
Output: stamp+count-up orchestration and streak/level-up confetti gating on the Day flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md
@.planning/phases/05.7-kid-redesign/05.7-PATTERNS.md
@CLAUDE.md

<interfaces>
Helpers (Plan 02): detectAward(res) → { creditedCoins, hasStreak } from '@/lib/kid/awardResult';
computeLevelUp(prevXp, newXp) from '@/lib/kid/level'. Atoms: Stamp, useCountUp, LedgerRow from
'@/components/design/atoms'. triggerConfetti from '@/utils/confetti' (unchanged mechanism).
Award response now includes appliedSources (Plan 02): { ok, creditedCoins, awards, appliedSources, streakEvents }.
Current save flow (KidDayFillForm.tsx:586-616): triggerCoinFlyup(coinsPreview) stays as a pre-save
estimate; the fetch('/api/wallet/award',{childId,date}) block is where the response must now be
parsed via detectAward and passed to the caller (replace the current `onSaved(coinsPreview)` with
onSaved carrying { creditedCoins, hasStreak, appliedItems } — server numbers, NOT coinsPreview).
Level-up: capture child.xp BEFORE checkAndAwardBadges(childId,date), re-fetch after, computeLevelUp.
day/page.tsx onSaved gating (~154-158) currently uses coinsPreview to setConfetti — rework to use
the server-confirmed { creditedCoins, hasStreak } + computeLevelUp result.
UI-SPEC Motion Contract: stamp 450ms scale+translateY; count-up header; NEVER animate coinsPreview.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parse award response in KidDayFillForm; pass server numbers to onSaved</name>
  <files>components/kid/KidDayFillForm.tsx</files>
  <read_first>
    - components/kid/KidDayFillForm.tsx (handleSubmit 586-616; onSaved signature; triggerCoinFlyup usage)
    - lib/kid/awardResult.ts (detectAward from Plan 02)
    - lib/kid/level.ts (computeLevelUp from Plan 02)
    - .planning/phases/05.7-kid-redesign/05.7-RESEARCH.md (Pitfall 1: never stamp coinsPreview; Pattern 4 level-crossing)
    - .planning/phases/05.7-kid-redesign/05.7-CONTEXT.md (D-17, D-19)
  </read_first>
  <action>
    In handleSubmit, after the `/api/wallet/award` fetch resolves, parse the JSON with
    `detectAward(res)` to get server-confirmed `{ creditedCoins, hasStreak }`. Capture the child's
    XP before `checkAndAwardBadges(childId, date)` and re-fetch after, computing
    `computeLevelUp(prevXp, newXp)`. Replace `onSaved(coinsPreview)` with an onSaved call carrying
    the server-confirmed payload `{ creditedCoins, hasStreak, leveledUp, appliedSources }` (update
    the onSaved prop type accordingly). Keep `triggerCoinFlyup(coinsPreview)` as the pre-save
    estimate flyup (unchanged) — but NEVER pass coinsPreview to the stamp/confetti path. If the
    award fetch fails, surface `t('kidDay.saveError')` with a retry (do not stamp).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "detectAward" components/kid/KidDayFillForm.tsx && grep -q "computeLevelUp" components/kid/KidDayFillForm.tsx && grep -q "/api/wallet/award" components/kid/KidDayFillForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - handleSubmit calls `detectAward` on the award response and `computeLevelUp` around checkAndAwardBadges
    - `onSaved` receives server-confirmed `{ creditedCoins, hasStreak, leveledUp }` — not `coinsPreview`
    - `triggerCoinFlyup(coinsPreview)` retained as the pre-save estimate only
    - The `/api/wallet/award` POST body is still `{ childId, date }` (unchanged)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Day save parses server-confirmed award data and hands it (not coinsPreview) to the caller.</done>
</task>

<task type="auto">
  <name>Task 2: Stamp + count-up + streak/level-up confetti on day/page</name>
  <files>app/kid/day/page.tsx</files>
  <read_first>
    - app/kid/day/page.tsx (onSaved handler ~154-158; ScreenHeader mount from Plan 04)
    - components/design/atoms.tsx (Stamp, useCountUp, LedgerRow)
    - utils/confetti.ts (triggerConfetti)
    - .planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md (Motion Contract table; stamp/count-up/confetti guards)
  </read_first>
  <action>
    Rework the onSaved handler to consume the server-confirmed `{ creditedCoins, hasStreak,
    leveledUp }`: render a `<Stamp trigger={creditedCoins}>+{creditedCoins} 🪙</Stamp>` (fires only
    after the server responds), drive the ScreenHeader/header balance via `useCountUp` to the new
    total, and render the awarded items as `LedgerRow` rows in the day summary. Fire
    `triggerConfetti()` ONLY when `hasStreak || leveledUp` (D-19) — never on an ordinary award.
    Remove any remaining coinsPreview-based `setConfetti` gating. Rely on the Stamp/useCountUp
    built-in reduced-motion guards (no extra guard). If creditedCoins is 0 (idempotent re-save),
    do not stamp or count-up spuriously.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "Stamp" app/kid/day/page.tsx && grep -q "useCountUp" app/kid/day/page.tsx && grep -q "triggerConfetti" app/kid/day/page.tsx && ! grep -q "coinsPreview" app/kid/day/page.tsx && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Stamp is keyed to server `creditedCoins`; header balance uses `useCountUp` to the new total
    - `triggerConfetti()` is called only under `hasStreak || leveledUp` (grep shows the guard)
    - No `coinsPreview` reference remains in day/page.tsx confetti/stamp path
    - Awarded items render as `LedgerRow`
    - `npx tsc --noEmit` and `npm test` exit 0
  </acceptance_criteria>
  <done>Signature stamp+count-up fires on server-confirmed coins; confetti reserved for streak/level-up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → /api/wallet/award (response consumption) | Client animates server-confirmed numbers only; boundary unchanged. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-057-19 | Spoofing | stamp/confetti trigger | accept | A modified client could fire the stamp arbitrarily, but no money moves (server-authoritative, money tables RLS SELECT-only). D-17 scopes "animate only server-confirmed numbers" as UX-integrity, not a security boundary (RESEARCH.md Security Domain). |
| T-057-20 | Tampering | onSaved payload | mitigate | Stamp/count-up/confetti keyed to detectAward(response) from the server — coinsPreview is never on the animation path (Pitfall 1). |
| T-057-SC | Tampering | package installs | accept | N/A — no packages installed. |
</threat_model>

<verification>
`npx tsc --noEmit` + `npm test` green; stamp keyed to creditedCoins; count-up header; confetti only for streak/level-up; coinsPreview off the animation path.
</verification>

<success_criteria>
The signature day-save gesture (stamp + count-up + ledger summary) fires on server-confirmed coins; confetti scoped to streak bonus + level-up.
</success_criteria>

<output>
Create `.planning/phases/05.7-kid-redesign/05.7-11-SUMMARY.md` when done.
</output>
