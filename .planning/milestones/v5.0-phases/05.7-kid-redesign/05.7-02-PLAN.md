---
phase: 05.7-kid-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kid/level.ts
  - lib/kid/rating-rank.ts
  - lib/kid/awardResult.ts
  - tests/level-up.test.ts
  - tests/rating-rank.test.ts
  - tests/award-result.test.ts
  - app/api/wallet/award/route.ts
autonomous: true
requirements: [05.7-SC3, 05.7-SC2]

must_haves:
  truths:
    - "computeLevelUp(prevXp,newXp) returns true only when the integer level crossed upward (D-19)"
    - "rankChildren(entries) sorts N children by weekly coins and assigns top-3 to podium, rest to list (D-07, N>=1)"
    - "The /api/wallet/award response exposes appliedSources so the client can detect a credited 'streak' (D-17/D-19)"
    - "detectAward(response) returns { creditedCoins, hasStreak } from the award response (never from coinsPreview)"
  artifacts:
    - path: "lib/kid/level.ts"
      provides: "Pure computeLevelUp + level formula (Math.floor(xp/1000)+1)"
      exports: ["computeLevelUp", "levelForXp"]
    - path: "lib/kid/rating-rank.ts"
      provides: "Pure N-children podium/list ranking"
      exports: ["rankChildren"]
    - path: "lib/kid/awardResult.ts"
      provides: "Pure parse of the award response for stamp/confetti triggers"
      exports: ["detectAward"]
    - path: "app/api/wallet/award/route.ts"
      provides: "Additive appliedSources field on the success response"
      contains: "appliedSources"
  key_links:
    - from: "app/api/wallet/award/route.ts"
      to: "applied.map(a => a.sourceType)"
      via: "additive-only response field"
      pattern: "appliedSources"
    - from: "lib/kid/level.ts levelForXp"
      to: "lib/services/badges.service.ts:582 formula"
      via: "Math.floor(xp/1000)+1 parity"
      pattern: "Math\\.floor"
---

<objective>
Extract the three pure functions the motion + rating features need (level-crossing,
N-children ranking, award-response parsing) with Vitest coverage, and make the ONE
additive change to the award route that unlocks server-confirmed stamp/confetti.

Purpose: These are the testable contracts consumed by the Day-motion plan (Wave 3) and
the Achievements plan (Wave 2). Isolating them as pure functions is the only automatable
slice of an otherwise visual phase (RESEARCH.md Validation Architecture Wave-0 gaps).
Output: level.ts, rating-rank.ts, awardResult.ts (+ tests), and appliedSources on the award response.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.7-kid-redesign/05.7-RESEARCH.md
@.planning/phases/05.7-kid-redesign/05.7-PATTERNS.md
@CLAUDE.md

<interfaces>
Award route final response today (app/api/wallet/award/route.ts:611-613):
  const { creditedCoins, applied } = await creditAwards(admin, childId, intents)
  return NextResponse.json({ ok: true, creditedCoins, awards: applied.length, streakEvents })
`applied` is AwardIntent[] (app/api/wallet/_lib.ts:268-274), each with a `sourceType`
string ('room' | 'grade' | 'sport' | 'activity' | 'book' | 'behavior' | 'streak' | ...).
Additive change ONLY: add `appliedSources: applied.map(a => a.sourceType)`. Do NOT touch
creditAwards / AwardIntent / any crediting or idempotency logic.

Level formula (lib/services/badges.service.ts:582, unchanged): Math.floor(newXP/1000)+1.
getWeekScore(childId, weekStart) returns a numeric weekly coin score (reused by rating).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure functions level.ts + rating-rank.ts with tests</name>
  <files>lib/kid/level.ts, lib/kid/rating-rank.ts, tests/level-up.test.ts, tests/rating-rank.test.ts</files>
  <behavior>
    - levelForXp(1500) === 2; levelForXp(0) === 1; levelForXp(999) === 1; levelForXp(1000) === 2
    - computeLevelUp(900, 1100) === true (crossed 1→2); computeLevelUp(1100, 1500) === false (still 2); computeLevelUp(1500, 1000) === false (no downward)
    - rankChildren of 3 entries with weekly coins [30,10,20] → podium ids ordered [c1,c3,c2], list empty
    - rankChildren of 4 entries → podium length 3, list length 1
    - rankChildren of 1 entry → podium length 1 (single-child; caller renders solo view), list empty
    - rankChildren ties broken deterministically (e.g. by childId) so output is stable
  </behavior>
  <read_first>
    - lib/services/badges.service.ts (line ~582, the level formula to mirror exactly)
    - app/kid/leaderboard/page.tsx (lines 42-70 — the N-children query + the 2-child `find` anti-pattern to REPLACE, not port)
    - lib/day-blocks.ts (precedent: pure function + colocated vitest file, node environment)
    - .planning/phases/05.7-kid-redesign/05.7-RESEARCH.md (Pitfall 5: N-children generalization)
  </read_first>
  <action>
    Create `lib/kid/level.ts` exporting `levelForXp(xp: number): number` = `Math.floor(xp/1000)+1`
    and `computeLevelUp(prevXp: number, newXp: number): boolean` = `levelForXp(newXp) > levelForXp(prevXp)`.
    Create `lib/kid/rating-rank.ts` exporting a `RankEntry` type ({ id: string; name: string;
    coinsWeek: number; avatarUrl?: string | null } plus any secondary stat fields the caller passes
    through) and `rankChildren(entries: RankEntry[]): { podium: RankEntry[]; list: RankEntry[] }`
    that sorts DESC by `coinsWeek` with a stable `id` tiebreak, slices the first 3 into `podium`
    and the remainder into `list`. Handles length 1/2/3+ uniformly (no hardcoded 2-child branch —
    single-child solo view is the caller's concern via podium.length===1). Pure, no I/O, no React.
    Add `tests/level-up.test.ts` and `tests/rating-rank.test.ts` covering the behaviors above.
  </action>
  <verify>
    <automated>npm test -- level-up rating-rank</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- level-up rating-rank` exits 0 with the behavior cases above passing
    - `lib/kid/rating-rank.ts` contains no literal `.find(` 2-child comparison; podium is a slice(0,3)
    - `lib/kid/level.ts` contains `Math.floor` and no dependency import (pure)
  </acceptance_criteria>
  <done>Two pure modules with green Vitest coverage for 1/2/3+/level-cross cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Award response additive field + detectAward helper + test</name>
  <files>app/api/wallet/award/route.ts, lib/kid/awardResult.ts, tests/award-result.test.ts</files>
  <behavior>
    - detectAward({ ok:true, creditedCoins:12, appliedSources:['room','grade'] }) === { creditedCoins:12, hasStreak:false }
    - detectAward({ ok:true, creditedCoins:20, appliedSources:['room','streak'] }).hasStreak === true
    - detectAward({}) === { creditedCoins:0, hasStreak:false } (defensive default — never throws)
  </behavior>
  <read_first>
    - app/api/wallet/award/route.ts (lines 600-616 — the final NextResponse.json to extend additively)
    - app/api/wallet/_lib.ts (lines 268-360 — AwardIntent.sourceType and creditAwards return; DO NOT modify)
    - .planning/phases/05.7-kid-redesign/05.7-RESEARCH.md (Code Examples: additive award response; Anti-Patterns: never fork creditAwards)
    - CLAUDE.md (Money / security model — additive read-only field only)
  </read_first>
  <action>
    In `app/api/wallet/award/route.ts`, change ONLY the final success response to add
    `appliedSources: applied.map(a => a.sourceType)` alongside the existing
    `{ ok, creditedCoins, awards: applied.length, streakEvents }`. No other line changes;
    `creditAwards`, `AwardIntent`, idempotency, and all crediting logic stay byte-identical.
    Create `lib/kid/awardResult.ts` exporting `detectAward(res: any): { creditedCoins: number;
    hasStreak: boolean }` that reads `res?.creditedCoins ?? 0` and
    `Array.isArray(res?.appliedSources) && res.appliedSources.includes('streak')`, never throwing.
    Add `tests/award-result.test.ts` covering the behaviors above.
  </action>
  <verify>
    <automated>npm test -- award-result && grep -q "appliedSources" app/api/wallet/award/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/wallet/award/route.ts` success response contains `appliedSources: applied.map(a => a.sourceType)`
    - No diff to `creditAwards`/`AwardIntent` (grep confirms _lib.ts unchanged in this plan's files_modified — it is NOT listed)
    - `npm test -- award-result` exits 0
    - `detectAward({})` returns the defensive default without throwing
  </acceptance_criteria>
  <done>Award response exposes appliedSources; detectAward parses it with tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → /api/wallet/award | Authenticated child requests a day reconcile; server credits idempotently (unchanged). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-057-03 | Information Disclosure | appliedSources field | accept | Exposes only the child's own credited source types (room/grade/streak/...) to the authenticated owner; no cross-family data, no secrets. Response already returned creditedCoins. |
| T-057-04 | Tampering | award crediting logic | mitigate | Change is additive-only (one `.map`); creditAwards / AwardIntent / RLS-locked money tables untouched — no client-side money write introduced (CLAUDE.md invariant preserved). |
| T-057-05 | Spoofing | client stamp/confetti trigger | accept | A modified client firing a stamp animates no money (money is server-authoritative); UX-integrity only, not a security boundary (RESEARCH.md Security Domain). |
| T-057-SC | Tampering | package installs | accept | N/A — no packages installed this phase (RESEARCH.md Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
`npm test` full suite green (including new level-up / rating-rank / award-result tests); `npx tsc --noEmit` green; award route diff is additive-only.
</verification>

<success_criteria>
Three pure modules + tests green; award response carries appliedSources with no money-logic change.
</success_criteria>

<output>
Create `.planning/phases/05.7-kid-redesign/05.7-02-SUMMARY.md` when done.
</output>
