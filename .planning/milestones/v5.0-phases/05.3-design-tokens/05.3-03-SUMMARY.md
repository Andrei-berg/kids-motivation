---
phase: 05.3-design-tokens
plan: 03
subsystem: ui
tags: [design-atoms, ledger-row, pilot-adoption, kid-wallet, parent-dashboard]

# Dependency graph
requires:
  - phase: 05.3-design-tokens plan 02
    provides: components/design/atoms.tsx (LedgerRow, Amount — theme-aware, gold-for-money)
provides:
  - app/kid/wallet/page.tsx TxnRow rendering via shared LedgerRow (theme=paper)
  - components/parent-center/screens/Dashboard.tsx ActivityRow rendering via shared LedgerRow (theme=ink)
  - proof-of-adoption for the shared atoms on both grounds (paper card + ink dark), satisfying DT-SC3's pilot bar
affects: [5.7/5.8 rollout phases — remaining screens adopt the same atoms following this pattern]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "LedgerRow as the drop-in replacement for bespoke label+dotted-leader+amount row markup — name/sub/amount/tone/theme props only, no local color literals in the row"
    - "Tone mapping convention: credits/earn_coins/bonus -> tone='earn' (gold); debits/penalty -> tone='spend'|'penalty' (neutral/danger)"

key-files:
  created: []
  modified:
    - app/kid/wallet/page.tsx
    - components/parent-center/screens/Dashboard.tsx

key-decisions:
  - "LedgerRow does not expose a `signed` prop (locked by 05.3-02's built interface), so credit amounts no longer show an explicit '+' prefix (the prior TxnRow did). This is the shared atom's as-built behavior, not a per-pilot deviation — coins_change already carries its own sign (positive for credits, negative for debits via wallet.repo.ts), so the number itself communicates direction; gold color plus the number's absence of a leading '-' still makes credits visually distinct."
  - "Parent dashboard ActivityRow: dropped the redundant tone Pill (earn/penalty/bonus label chip) and the separate <Coin> amount in favor of a single LedgerRow — the plan explicitly permitted this ('if it reads cleaner, the inline text+Pill block'). Tone conveys the same earn/penalty distinction via LedgerRow's color, so no information is lost; removed the now-unused Coin import and toneMap/labelMap locals."
  - "Kid wallet TxnRow: kept the leading emoji tile (💰/🛍️) as the plan allowed either keeping or dropping it; only the label+time+amount block became LedgerRow."

patterns-established:
  - "Row components that previously duplicated label/dotted-leader/mono-amount markup with local T.teal/T.coral or Coin/Pill combos now delegate entirely to LedgerRow, keeping only the surrounding avatar/emoji + motion wrapper + border markup in the pilot component."

requirements-completed: [DT-SC3, DT-SC4]

# Metrics
duration: ~7min
completed: 2026-07-07
---

# Phase 05.3 Plan 03: Pilot LedgerRow/Amount Adoption Summary

**Kid wallet transaction history and parent dashboard activity list both now render through the shared LedgerRow/Amount atoms (paper + ink themes respectively), with a green full build (tsc/lint/test/next build) proving the token recolor + pilots didn't break anything.**

## Performance

- **Duration:** ~7 min
- **Completed:** 2026-07-07T04:00:00Z
- **Tasks:** 2/2 auto tasks completed; 1 checkpoint pending operator sign-off (see below)
- **Files modified:** 2

## Accomplishments

- `app/kid/wallet/page.tsx` — `TxnRow` now renders its label/time/amount through `<LedgerRow theme="paper" name={label} sub={time} amount={x.coins_change} tone={isIn ? 'earn' : 'spend'}/>`, removing the local `col`/teal/coral amount coloring. Credits render gold (earn tone), debits neutral (spend tone). The emoji tile prefix was kept.
- `components/parent-center/screens/Dashboard.tsx` — `ActivityRow` now renders through `<LedgerRow theme="ink" name={a.text} sub={\`${child.name} · ${a.time}\`} amount={a.amt} tone={a.type === 'penalty' ? 'penalty' : 'earn'}/>`, replacing the trailing `<Coin v={a.amt}/>` and the inline text+Pill block. earn_coins/bonus map to gold (earn); penalty maps to danger (penalty). Removed the now-unused `Coin` import and the `toneMap`/`labelMap` locals.
- Both edits are surgically scoped — `git diff` per file confirms only the row-rendering function (+ its single new atoms import) changed; balance hero, quick actions, goals column, child cards, pending approvals, and reading checks in both files are untouched.
- Full verification suite green after Task 2: `npx tsc --noEmit` (clean), `npm run lint` (exit 0, only pre-existing unrelated warnings in other files), `npm test` (31 passed / 22 skipped — same baseline as 05.3-02), `npm run build` (55/55 pages generated successfully).

## Task Commits

1. **Task 1: Adopt LedgerRow/Amount in the kid wallet history list** — `94e1e87` (feat)
2. **Task 2: Adopt LedgerRow in the parent dashboard activity list + full build** — `d7c6328` (feat)

**Plan metadata:** committed with this SUMMARY (see final commit)

## Files Created/Modified

- `app/kid/wallet/page.tsx` — `TxnRow` delegates label/time/amount rendering to `LedgerRow` (paper theme); import added, local amount-coloring removed.
- `components/parent-center/screens/Dashboard.tsx` — `ActivityRow` delegates to `LedgerRow` (ink theme); import added, `Coin` import and unused tone/label maps removed.

## Decisions Made

- See `key-decisions` in frontmatter: no `+` prefix on credit amounts (atom's as-built interface has no `signed` prop on `LedgerRow`); dropped the redundant Pill+Coin markup in the parent pilot per the plan's explicit "if it reads cleaner" allowance; kept the kid wallet's emoji tile.

## Deviations from Plan

None beyond the documented, plan-anticipated choices above (both are covered by the plan's own "your call" / "if it reads cleaner" language, not unplanned scope). No Rule 1-4 auto-fixes were needed — both pilot surfaces mapped cleanly onto the LedgerRow interface as built in 05.3-02.

## Issues Encountered

None. `npx tsc --noEmit`, `npm run lint`, `npm test`, and `npm run build` all passed on the first attempt after Task 2's edit.

## Checkpoint: pending operator verification

The plan's final task is a consolidated `checkpoint:human-verify` (visual check of the recolor + fonts + both pilots). Per the execution instructions, this executor completed all auto tasks and the full build gate, then stopped here rather than blocking — the checkpoint requires a human to actually look at the running app.

**What was built:** Phase 5.3 is code-complete: (1) unified `lib/design/tokens.ts` recolors all kid + parent screens via key-preserving token re-exports (05.3-01); (2) Bitter / Golos Text / JetBrains Mono load via `next/font` with Cyrillic support (05.3-01); (3) shared atoms (`LedgerRow` / `Amount` / `StatusChip` / `Stamp` / `useCountUp`, from 05.3-02) now pilot the kid wallet history and the parent dashboard activity list (this plan).

**How to verify** (run `npm run dev`, open `http://localhost:3000`):

1. **KID WALLET** (`/kid/day` → wallet, or `/kid/wallet`): the transaction history shows ledger rows with a dotted leader and mono amounts; earned (positive) amounts are GOLD, spends are neutral. Confirm no coral/teal remnants and the warm paper background.
2. **PARENT DASHBOARD** (`/parent-center`): the activity list shows ledger rows on the dark ink theme; coin amounts are gold, a penalty (if present) is red; cyan/neon-green are gone.
3. **FONTS:** headings render in Bitter (serif), body in Golos Text, numbers in JetBrains Mono — including Cyrillic (switch language to RU if needed). No FOUT flash to a broken glyph.
4. **UNMIGRATED SCREENS** render without breakage — spot-check kid shop, kid achievements, parent Analytics, parent Settings, and one LEGACY page (e.g. `/dashboard` or `/wallet`): legacy pages should still look Inter-styled and intact (they are intentionally untouched).
5. **GOLD RULE:** confirm gold appears ONLY on money/coins anywhere you look — not on buttons, chips, or nav.

**Resume signal:** Type "approved" or describe any recolor/font/pilot issue to fix.

## Known Stubs

None — both edits wire real data (`x.coins_change`, `a.text`/`a.amt`/`a.type`) through the atoms; no placeholder or hardcoded-empty values introduced.

## Threat Flags

None — both pilots are pure UI rendering changes reading pre-existing props; no new network endpoints, auth paths, file access, or schema changes.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Both pilot surfaces (kid wallet paper theme, parent dashboard ink theme) prove the shared atoms work on both grounds. 5.7/5.8 rollout phases can extend the same `LedgerRow`/`Amount` pattern to the remaining un-piloted screens.
- Blocked only on the operator's visual sign-off above (checkpoint, not code) before phase 05.3 can be marked fully complete.

---
*Phase: 05.3-design-tokens*
*Completed: 2026-07-07*

## Self-Check: PASSED

Both modified files verified on disk and both contain `LedgerRow`; both task commit hashes (94e1e87, d7c6328) verified in git log.
