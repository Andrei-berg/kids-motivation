---
phase: 05.3-design-tokens
plan: 03
type: execute
wave: 3
depends_on: ["05.3-01", "05.3-02"]
files_modified:
  - app/kid/wallet/page.tsx
  - components/parent-center/screens/Dashboard.tsx
autonomous: false
requirements: [DT-SC3, DT-SC4]
must_haves:
  truths:
    - "The kid wallet history list renders each transaction with the shared LedgerRow + Amount atoms"
    - "The parent dashboard activity list renders each entry with the shared LedgerRow atom"
    - "Money amounts in both pilots display in gold; non-money/spend amounts are neutral"
    - "npm run build succeeds; unmigrated kid + parent screens render without visual breakage"
  artifacts:
    - path: "app/kid/wallet/page.tsx"
      provides: "Kid wallet history list adopting LedgerRow/Amount (paper theme)"
      contains: "LedgerRow"
    - path: "components/parent-center/screens/Dashboard.tsx"
      provides: "Parent dashboard activity list adopting LedgerRow (ink theme)"
      contains: "LedgerRow"
  key_links:
    - from: "app/kid/wallet/page.tsx"
      to: "components/design/atoms.tsx"
      via: "import of LedgerRow/Amount"
      pattern: "from ['\"]@/components/design/atoms['\"]"
    - from: "components/parent-center/screens/Dashboard.tsx"
      to: "components/design/atoms.tsx"
      via: "import of LedgerRow"
      pattern: "from ['\"]@/components/design/atoms['\"]"
---

<objective>
Pilot the shared atoms on exactly two surfaces — the kid wallet transaction history and the
parent dashboard activity list — swapping their bespoke row markup for the shared `LedgerRow`
(+ `Amount` on the kid side). Then run the full build and a single consolidated visual check
confirming the recolor + fonts + pilots landed and nothing unmigrated broke.

Purpose: prove the atoms work on both grounds (paper + ink) and satisfy the "adopted on at
least kid wallet + parent dashboard" bar, while `npm run build` + the visual pass guard the
un-piloted screens (which recolor automatically via the token substitution from Plan 01).
Output: two surgically edited pilot files, a green build, and operator sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.3-design-tokens/05.3-CONTEXT.md
@.planning/phases/05.3-design-tokens/05.3-01-SUMMARY.md
@.planning/phases/05.3-design-tokens/05.3-02-SUMMARY.md

<constraints>
- SURGICAL diffs only: touch ONLY the transaction/activity row rendering in each file.
  Do not restructure the balance hero, quick actions, goals column, child cards, pending
  approvals, or reading checks. Other screens keep their markup (they recolor via tokens).
- Gold appears ONLY on money amounts. In the kid wallet, coins_change IS money → gold for
  credits; debits/spends render neutral (money=false). Do not tint the whole row gold.
- No new npm packages; do not touch app/globals.css.
</constraints>

<interfaces>
<!-- components/design/atoms.tsx (Plan 02):
  LedgerRow({ name, amount, tone: 'earn'|'spend'|'penalty'|'pending', theme: 'paper'|'ink', sub? })
  Amount({ value, theme, money, size, signed })
-->

<!-- Kid pilot — current row (app/kid/wallet/page.tsx, TxnRow ~L223): a flex row with an emoji
     tile, label + relative time, and a colored +/- coins_change. `isIn = x.coins_change > 0`.
     Map: name = label, sub = time, amount = x.coins_change, theme='paper',
     tone = isIn ? 'earn' : 'spend'. Keep the emoji tile if desired; the label→dotted→amount
     span is what becomes LedgerRow. Preserve the existing framer-motion stagger wrapper. -->

<!-- Parent pilot — current row (components/parent-center/screens/Dashboard.tsx, ActivityRow ~L132):
     Avatar + text + tone Pill + <Coin v={a.amt}/>. toneMap: earn_coins/penalty/bonus.
     Map: name = a.text, sub = `${child.name} · ${a.time}`, amount = a.amt, theme='ink',
     tone = a.type==='penalty' ? 'penalty' : 'earn' (earn_coins & bonus are money → earn/gold).
     Keep the Avatar; replace the trailing <Coin> (and optionally the inline text/Pill block)
     with LedgerRow. Preserve the framer-motion stagger wrapper. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adopt LedgerRow/Amount in the kid wallet history list</name>
  <files>app/kid/wallet/page.tsx</files>
  <read_first>
    - app/kid/wallet/page.tsx (TxnRow function ~L223-247 and its call site ~L200-205)
    - components/design/atoms.tsx (LedgerRow/Amount signatures)
  </read_first>
  <action>
    In app/kid/wallet/page.tsx, import `{ LedgerRow }` (and `Amount` if used directly) from
    '@/components/design/atoms'. Rewrite ONLY the `TxnRow` component so the label + amount
    render through `<LedgerRow theme="paper" name={label} sub={time} amount={x.coins_change}
    tone={x.coins_change > 0 ? 'earn' : 'spend'} />`. Keep the surrounding card container, the
    framer-motion `motion.div variants={itemV}` wrapper, and the `isLast` bottom-border logic.
    The leading emoji tile may stay as a prefix inside the row or be dropped — your call — but
    do not otherwise change the page. Credits (positive) MUST show gold (earn tone); debits
    render neutral (spend tone). Remove the now-unused local `col`/`T.teal`/`T.coral` amount
    coloring for that row.
  </action>
  <acceptance_criteria>
    - `grep -q "@/components/design/atoms" app/kid/wallet/page.tsx && grep -q "LedgerRow" app/kid/wallet/page.tsx`
    - Earn/spend tone wired: `grep -q "tone=" app/kid/wallet/page.tsx`
    - Diff is scoped to the history row: `git diff app/kid/wallet/page.tsx` shows changes only within/around TxnRow (balance hero, quick actions, goals untouched).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Kid wallet transactions render via LedgerRow with gold credits / neutral debits; rest of the page unchanged; tsc clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Adopt LedgerRow in the parent dashboard activity list + full build</name>
  <files>components/parent-center/screens/Dashboard.tsx</files>
  <read_first>
    - components/parent-center/screens/Dashboard.tsx (ActivityRow ~L132-159 + activity map ~L269-275)
    - components/design/atoms.tsx (LedgerRow signature)
  </read_first>
  <action>
    In components/parent-center/screens/Dashboard.tsx, import `{ LedgerRow }` from
    '@/components/design/atoms'. Rewrite ONLY the `ActivityRow` component so the entry renders
    through `<LedgerRow theme="ink" name={a.text} sub={`${child.name} · ${a.time}`}
    amount={a.amt} tone={a.type === 'penalty' ? 'penalty' : 'earn'} />`. Keep the leading
    `<Avatar>` and the framer-motion stagger wrapper at the call site. Replace the trailing
    `<Coin v={a.amt}/>` (and, if it reads cleaner, the inline text+Pill block) with LedgerRow.
    earn_coins and bonus are money → `earn` (gold); penalty → `penalty` (danger). Do NOT touch
    the greeting, fill-day card, reading checks, child cards, or pending approvals sections.
    After the edit, run the full build.
  </action>
  <acceptance_criteria>
    - `grep -q "@/components/design/atoms" components/parent-center/screens/Dashboard.tsx && grep -q "LedgerRow" components/parent-center/screens/Dashboard.tsx`
    - Penalty vs earn tone wired: `grep -q "penalty" components/parent-center/screens/Dashboard.tsx`
    - Diff scoped to ActivityRow: `git diff components/parent-center/screens/Dashboard.tsx` shows no changes to ChildCard / pending / reading sections.
    - `npx tsc --noEmit`, `npm run lint`, `npm test`, and `npm run build` all succeed.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm test && npm run build</automated>
  </verify>
  <done>Parent activity list renders via LedgerRow (gold money / danger penalty); unmigrated sections untouched; full build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Consolidated visual verification of recolor + fonts + pilots</name>
  <action>Operator-run visual verification gate — no code changes. Confirm the recolor, fonts, and both pilots per the steps below.</action>
  <what-built>
    Phase 5.3 is code-complete: (1) unified lib/design/tokens.ts recolors all kid + parent
    screens via key-preserving token re-exports; (2) Bitter / Golos Text / JetBrains Mono load
    via next/font (cyrillic); (3) shared atoms (LedgerRow / Amount / StatusChip / stamp+count-up)
    now pilot the kid wallet history and the parent dashboard activity list.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open http://localhost:3000, then:
    1. KID WALLET (/kid/day → wallet, or /kid/wallet): the transaction history shows ledger
       rows with a dotted leader and mono amounts; earned (positive) amounts are GOLD, spends
       are neutral. Confirm no coral/teal remnants and the warm paper background.
    2. PARENT DASHBOARD (/parent-center): the activity list shows ledger rows on the dark ink
       theme; coin amounts are gold, a penalty (if present) is red; cyan/neon-green are gone.
    3. FONTS: headings render in Bitter (serif), body in Golos Text, numbers in JetBrains Mono
       — including Cyrillic (switch language to RU if needed). No FOUT flash to a broken glyph.
    4. UNMIGRATED SCREENS render without breakage — spot-check kid shop, kid achievements,
       parent Analytics, parent Settings, and one LEGACY page (e.g. /dashboard or /wallet):
       legacy pages should still look Inter-styled and intact (they are intentionally untouched).
    5. GOLD RULE: confirm gold appears ONLY on money/coins anywhere you look — not on buttons,
       chips, or nav.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any recolor/font/pilot issue to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pilot edits → rest of each page | a non-surgical edit could regress balance hero / child cards |
| token recolor → unmigrated + legacy screens | must render without visual breakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-053-07 | Tampering | pilot surgical scope | mitigate | edits confined to TxnRow / ActivityRow; `git diff` review gate per task |
| T-053-08 | Denial | full build regression | mitigate | `npm run build` gate in Task 2 before the checkpoint |
| T-053-09 | Information | gold-for-money on real data | mitigate | credits/earn=gold, spends/penalty=neutral/danger; human-verify step 5 confirms rule |
| T-053-10 | Repudiation | unmigrated/legacy breakage unseen | mitigate | consolidated human-verify explicitly spot-checks unmigrated + one legacy page |
| T-053-SC | Tampering | package installs | accept | none — no npm install this phase |
</threat_model>

<verification>
- `npx tsc --noEmit && npm run lint && npm test && npm run build` — all green (Task 2).
- `git diff` per pilot file confirms surgical scope.
- Consolidated human-verify checkpoint confirms recolor, fonts (incl. Cyrillic), both pilots,
  the gold-only-for-money rule, and that unmigrated + legacy screens are intact.
</verification>

<success_criteria>
- Kid wallet history + parent dashboard activity both render via the shared atoms (DT-SC3).
- npm run build succeeds; unmigrated + legacy screens render without breakage (DT-SC4).
- Gold appears only on money; operator signs off.
</success_criteria>

<output>
Create `.planning/phases/05.3-design-tokens/05.3-03-SUMMARY.md` when done.
</output>
