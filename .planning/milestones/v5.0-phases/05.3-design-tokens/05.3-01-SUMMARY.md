---
phase: 05.3-design-tokens
plan: 01
subsystem: ui
tags: [design-tokens, next-font, google-fonts, tailwind-free-tokens, kid-ui, parent-center]

# Dependency graph
requires:
  - phase: 05.3-design-tokens (CONTEXT)
    provides: locked palette hex values (base/paper/ink) and font family choices from the approved design contract
provides:
  - lib/design/tokens.ts as the single source of design truth (base palette + paper/ink themes + font-variable strings)
  - components/kid/design/tokens.ts and components/parent-center/tokens.ts recolored as key-preserving re-exports (zero consumer edits)
  - Bitter/Golos Text/JetBrains Mono wired via next/font/google with cyrillic+latin subsets on <html>
affects: [05.3-02, 05.3-03, future kid/parent-center UI phases (5.7/5.8 nav & ledger rows)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Single design-token source (lib/design/tokens.ts) with per-surface theme re-exports that preserve legacy key names — recolor without touching any consumer"
    - "next/font/google CSS variables (--font-display/--font-body/--font-mono) referenced from token font strings with system-font fallbacks"

key-files:
  created:
    - lib/design/tokens.ts
  modified:
    - components/kid/design/tokens.ts
    - components/parent-center/tokens.ts
    - app/layout.tsx

key-decisions:
  - "Radius keys (r/rM/rL/rXL/rPill) split onto individual lines in components/parent-center/tokens.ts (still numeric literals) — required for the line-anchored grep key-count gate (30 keys) to pass; values and types unchanged"
  - "CHILD_ACCENTS re-derived to 5 non-neon, non-gold hexes: #8B7BF5, #3FBF92, #E88AA6, #5FB3E0, #C58BE0"
  - "Soft-tint discretion (tealSoft #D7EFE5, pink #C48CA8/pinkSoft #F0E2EA, mint #9CC7B3, indigoSoft/goldSoft rgba variants) chosen non-neon and AA-legible on their respective grounds, per CONTEXT's discretion note"

patterns-established:
  - "Token re-export pattern: import { base, paper } / { base, ink } from '@/lib/design/tokens' and map every legacy key to a base/theme value — future token changes happen in lib/design/tokens.ts only"

requirements-completed: [DT-SC1, DT-SC2]

# Metrics
duration: 25min
completed: 2026-07-06
---

# Phase 05.3 Plan 01: Design Tokens Foundation Summary

**Unified `lib/design/tokens.ts` (base palette + paper/ink themes) with both legacy `T` token files recolored as key-preserving re-exports, and Bitter/Golos Text/JetBrains Mono wired via next/font — zero consumer component edits, verified by a clean `tsc` and a successful production build.**

## Performance

- **Duration:** ~25 min
- **Completed:** 2026-07-06T19:06:12Z
- **Tasks:** 3/3 completed
- **Files modified:** 4 (1 created, 3 modified)

## Accomplishments
- Created `lib/design/tokens.ts` exporting `base` (indigo/gold/success/warning/danger + font-variable strings), `paper` (kid light theme), and `ink` (parent dark theme) with all CONTEXT-locked hex values.
- Recolored `components/kid/design/tokens.ts` (26 keys) and `components/parent-center/tokens.ts` (30 keys + `CHILD_ACCENTS`) as thin re-exports — every original key name preserved, no consumer file touched, confirmed via `git diff --name-only` and `tsc`.
- Wired Bitter (600/700/800), Golos Text (400/500/600/700), and JetBrains Mono (500/600/700) via `next/font/google` with `latin`+`cyrillic` subsets and `display: swap`; CSS variables applied to `<html>` in `app/layout.tsx`. `app/globals.css` untouched.
- Full production build (`npm run build`) succeeds — fonts resolve and download correctly at build time, all 55 routes generate.

## Task Commits

Each task was committed atomically:

1. **Task 1: Create lib/design/tokens.ts** - `ed59464` (feat)
2. **Task 2: Recolor both legacy T objects as key-preserving re-exports** - `9752239` (feat)
3. **Task 3: Wire Bitter / Golos Text / JetBrains Mono via next/font** - `823aba9` (feat)

**Plan metadata:** committed with this SUMMARY (see final commit below)

## Files Created/Modified
- `lib/design/tokens.ts` - New single source of design truth: `base` palette, `paper` (kid) and `ink` (parent) themes, font-variable strings
- `components/kid/design/tokens.ts` - `T` re-export (26 keys unchanged) sourced from `base`/`paper`
- `components/parent-center/tokens.ts` - `T` re-export (30 keys unchanged) + `CHILD_ACCENTS` sourced from `base`/`ink`
- `app/layout.tsx` - `next/font/google` imports for Bitter/Golos Text/JetBrains Mono, `.variable` classNames applied to `<html>`

## Decisions Made
- Split the parent-center radius keys (`r, rM, rL, rXL, rPill`) onto individual lines (still numeric literals, same values) so the plan's line-anchored grep key-count acceptance gate (expects 30 matching lines) passes — this is a formatting-only change with no behavioral impact; caught during Task 2 verification when the initial single-line format only counted as 1 key match instead of 5.
- CHILD_ACCENTS re-derived per CONTEXT guidance: `['#8B7BF5', '#3FBF92', '#E88AA6', '#5FB3E0', '#C58BE0']` — 5 distinguishable, non-neon, no-gold entries.
- Soft-tint/derived colors not explicitly locked in CONTEXT (kid `tealSoft`, `pink`, `pinkSoft`, `mint`; parent `indigoSoft`, `cyanSoft`, `successSoft`, `warningSoft`, `dangerSoft` rgba tints) chosen to stay non-neon and AA-legible against their respective paper/ink grounds, per the plan's "Claude's Discretion" note.

## Deviations from Plan

None - plan executed exactly as written. The radius-key line-splitting (see Decisions Made) is a formatting adjustment to satisfy the plan's own acceptance-criteria grep, not a deviation from the plan's intent (values/types identical to what the plan specified).

## Issues Encountered
- Initial single-line format for parent-center radius keys (`r: 12, rM: 14, ...` all on one line, matching the original file's style) caused the acceptance-criteria grep (`grep -cE "^\s+(...)：" ... equals 30`) to count only 26 matching lines, since `grep -c` counts matching lines not per-line occurrences. Fixed by splitting each radius key onto its own line — values, types (numeric), and behavior unchanged. Re-verified: 30/30 key count, `tsc --noEmit` clean, no quoted radius values.

## User Setup Required

None - no external service configuration required. `next/font/google` self-hosts font files at build time; no runtime third-party requests, no new npm packages.

## Next Phase Readiness
- `lib/design/tokens.ts` is now the single source of truth for 05.3-02 (shared atoms: LedgerRow, Amount, StatusChip) and 05.3-03 (pilot adoption on kid wallet + parent dashboard) to build on.
- All kid + parent-center screens are now recolored via token substitution (indigo/gold/success/warning/danger) with zero markup changes — visual verification of the full app recommended before 05.3-02/03 proceed, since this plan recolors every kid+parent-center screen at once.
- No blockers. `npx tsc --noEmit`, `npm run lint` (exit 0, only pre-existing unrelated warnings), `npm test` (31 passed / 22 skipped), and `npm run build` all green.

---
*Phase: 05.3-design-tokens*
*Completed: 2026-07-06*

## Self-Check: PASSED

All created/modified files verified present; all 3 task commit hashes (ed59464, 9752239, 823aba9) verified in git log.
