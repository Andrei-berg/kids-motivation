---
phase: 05.3-design-tokens
plan: 02
type: execute
wave: 2
depends_on: ["05.3-01"]
files_modified:
  - components/design/atoms.tsx
autonomous: true
requirements: [DT-SC3]
must_haves:
  truths:
    - "A shared LedgerRow atom renders name + dotted leader + mono amount with earn/spend/penalty/pending tones"
    - "A shared Amount atom renders JetBrains-Mono tabular-nums, gold for money, with size variants"
    - "A shared StatusChip atom renders success/warning/danger soft chips"
    - "A stamp entrance + count-up animation exists and respects prefers-reduced-motion"
    - "Every atom is theme-aware via a `theme: 'paper' | 'ink'` prop and pulls colors from lib/design/tokens"
  artifacts:
    - path: "components/design/atoms.tsx"
      provides: "Shared brand atoms: LedgerRow, Amount, StatusChip, useCountUp, Stamp"
      exports: ["LedgerRow", "Amount", "StatusChip", "useCountUp", "Stamp"]
      min_lines: 90
  key_links:
    - from: "components/design/atoms.tsx"
      to: "lib/design/tokens.ts"
      via: "import of base + paper + ink for theme-aware colors"
      pattern: "from ['\"]@/lib/design/tokens['\"]"
    - from: "Amount (money) / LedgerRow earn tone"
      to: "theme goldText"
      via: "gold reserved for money only"
      pattern: "goldText"
---

<objective>
Create the shared brand atoms that give FamilyCoins its ledger signature: `LedgerRow`
(name · dotted-leader · mono amount), `Amount` (JetBrains-Mono, tabular-nums, gold for
money), `StatusChip` (soft success/warning/danger), and a `Stamp` entrance + `useCountUp`
hook. All atoms are theme-aware (`theme: 'paper' | 'ink'`) and read their colors from
lib/design/tokens.ts — no hardcoded new-system hexes here.

Purpose: these are the reusable primitives Plan 03 pilots on the kid wallet history and
parent dashboard activity list, and that phases 5.7/5.8 will roll out everywhere.
Output: components/design/atoms.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.3-design-tokens/05.3-CONTEXT.md
@.planning/phases/05.3-design-tokens/05.3-01-SUMMARY.md

<constraints>
- UI-only. No new npm packages. Do NOT touch app/globals.css.
- Gold appears ONLY on money amounts/coins. Warning is orange. Indigo/accent is interactive.
- Simplest theme mechanism wins: an explicit `theme` prop, NOT a context/provider, NOT any
  CSS-in-JS library. Inline styles only (matches existing atoms/ui idioms).
- The stamp keyframes must be self-contained (globals.css is off-limits): inject a single
  guarded <style> tag from within the module, or drive the animation with rAF/state. Either
  way it MUST no-op under prefers-reduced-motion: reduce.
</constraints>

<interfaces>
<!-- From lib/design/tokens.ts (Plan 01): -->
  base: { indigo, indigoDeep, indigoSoft, gold, goldDeep, goldSoft, success, successDeep,
          warning, danger, fontDisplay, fontBody, fontMono }
  paper: { bg, card, ink, ink2, ink3, line, lineSoft, accent, goldText, success, warning, danger }
  ink:   { bg, bg2, card, cardHi, text, textDim, muted, accent, goldText, success, warning, danger }

<!-- Reuse pattern (do not copy the file, mirror the approach): rAF cubic ease-out from
     components/kid/design/atoms.tsx AnimatedNum and components/parent-center/screens/Dashboard.tsx useCountUp:
       e = 1 - Math.pow(1 - t, 3); setN(Math.round(from + (to - from) * e)) -->

<!-- Color resolution per theme:
     const c = theme === 'ink' ? inkTheme : paper
     text color  = c.ink ?? c.text        (paper uses `ink`, ink-theme uses `text`)
     muted color = c.ink3 ?? c.muted
     money/gold  = c.goldText
     leader dots = c.line ?? 'rgba(255,255,255,0.10)'
     penalty     = c.danger ; success = c.success ; warning = c.warning -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Amount + StatusChip atoms (theme-aware, gold-for-money)</name>
  <files>components/design/atoms.tsx</files>
  <read_first>
    - lib/design/tokens.ts (base/paper/ink)
    - components/kid/design/atoms.tsx (CoinPill/AnimatedNum idioms — inline-style, fNum usage)
    - components/parent-center/ui.tsx (Pill/Coin idioms — soft chip tone tables)
  </read_first>
  <action>
    Create `components/design/atoms.tsx` starting with `'use client'`. Import `{ base, paper,
    ink as inkTheme }` from '@/lib/design/tokens'. Add a small internal `resolve(theme)`
    helper returning the active ground colors per the <interfaces> resolution notes.

    `Amount({ value, theme='paper', money=true, size='md', signed=false })`: render the number
    in `base.fontMono` with `fontVariantNumeric: 'tabular-nums'`, `letterSpacing: -0.3`. Color
    = `resolve(theme).goldText` when `money` is true, else the theme text color. `size`
    variants sm/md/lg → fontSize 13/15/22, fontWeight 700/800/800. When `signed`, prefix '+'
    for value > 0 (never '+' for 0 or negatives; '−' comes from the raw number). Format with
    `toLocaleString('ru-RU')`.

    `StatusChip({ tone, children, theme='paper' })`: tone ∈ 'success'|'warning'|'danger'.
    Soft pill (height 22, radius 999, padding '0 9px', fBody) — background = the tone color at
    ~14% alpha, foreground = the theme's TEXT-SAFE tone variant, NOT the raw base color.
    AA fix (plan-checker 2026-07-06): on the paper theme the raw base colors fail WCAG AA for
    small text (#2E9E77→3.35:1, #E07A2E→3.00:1, #D95563→3.87:1 on white). Add text-safe
    derivatives to the paper theme in lib/design/tokens.ts (mirroring goldText):
    successText #1F7A5A, warningText #A85512, dangerText #B93A48 (each ≥4.5:1 on #FFFFFF/#FAF3E9 —
    verify with a contrast check before committing; darken further if any pair is below 4.5).
    The ink theme keeps its existing on-dark-legible variants as the text-safe set. StatusChip and
    LedgerRow penalty/pending tones must use these *Text variants for foreground text. Mirror the
    Pill tone-table pattern from ui.tsx but source colors from the resolved theme, not the old
    parent T.
  </action>
  <acceptance_criteria>
    - `test -f components/design/atoms.tsx` and first line is `'use client'`.
    - `grep -q "export function Amount" components/design/atoms.tsx && grep -q "export function StatusChip" components/design/atoms.tsx`
    - Amount uses mono + tabular-nums: `grep -q "fontMono" components/design/atoms.tsx && grep -q "tabular-nums" components/design/atoms.tsx`
    - Gold reserved for money: `grep -q "goldText" components/design/atoms.tsx`
    - Imports the shared source: `grep -q "@/lib/design/tokens" components/design/atoms.tsx`
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Amount and StatusChip exist, theme-aware, mono+tabular-nums, gold only when money=true; tsc clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: LedgerRow signature + useCountUp/Stamp (reduced-motion safe)</name>
  <files>components/design/atoms.tsx</files>
  <read_first>
    - components/design/atoms.tsx (Amount/StatusChip + resolve() from Task 1)
    - components/kid/design/atoms.tsx (AnimatedNum rAF cubic ease-out to mirror)
    - components/parent-center/screens/Dashboard.tsx (useCountUp reference implementation)
  </read_first>
  <action>
    In the same file add:

    `LedgerRow({ name, amount, tone='earn', theme='paper', sub })` — the brand signature row:
    a flex row with the `name` (theme text, fBody) on the left, a dotted leader filling the
    middle (a flex:1 element with `borderBottom: 1px dotted <leaderColor>`, alignment near the
    baseline), and an `<Amount>` on the right. Tone map: `earn`→ money Amount in gold (money
    true), `spend`→ neutral Amount (money false, theme text), `penalty`→ Amount in
    theme.danger (money false), `pending`→ muted Amount with a "≈" prefix (money false, muted
    color). Optional `sub` renders a small muted second line under `name`. Use the resolved
    theme colors only.

    `useCountUp(target, duration=700)` — rAF cubic ease-out (mirror the existing AnimatedNum
    math: `e = 1 - Math.pow(1 - t, 3)`), returning the current integer. If
    `window.matchMedia('(prefers-reduced-motion: reduce)').matches`, skip the animation and
    return `target` immediately.

    `Stamp({ children, trigger })` — wraps children and plays a ~450ms entrance
    (translateY(8px)+scale(0.96) → settle) on mount / when `trigger` changes. Implement the
    keyframes self-contained: inject ONE guarded `<style id="fc-stamp-kf">` on first use (check
    document for the id before appending), or drive via rAF/state. Under prefers-reduced-motion
    the wrapper renders children with NO transform/animation.
  </action>
  <acceptance_criteria>
    - Exports present: `grep -q "export function LedgerRow" components/design/atoms.tsx && grep -q "export function useCountUp" components/design/atoms.tsx && grep -q "export function Stamp" components/design/atoms.tsx`
    - LedgerRow renders a dotted leader: `grep -q "dotted" components/design/atoms.tsx`
    - Pending tone supports the "≈" prefix: `grep -q "≈" components/design/atoms.tsx`
    - Reduced-motion honored in both animated atoms: `grep -c "prefers-reduced-motion" components/design/atoms.tsx` ≥ 2
    - Count-up reuses cubic ease-out: `grep -q "Math.pow(1 - t, 3)" components/design/atoms.tsx`
    - File is substantive: `wc -l < components/design/atoms.tsx` ≥ 90
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm test</automated>
  </verify>
  <done>LedgerRow (all 4 tones + dotted leader), useCountUp and Stamp exist and no-op under reduced motion; tsc + lint + test clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| atoms → consuming screens | atoms carry the gold-for-money rule; misuse would leak gold onto non-money UI |
| animation → user with vestibular sensitivity | motion must be suppressible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-053-04 | Tampering | gold-for-money invariant | mitigate | Amount uses goldText ONLY when `money` true; LedgerRow earn tone is the sole gold path; grep gate on goldText |
| T-053-05 | Denial (a11y) | Stamp / useCountUp motion | mitigate | both check `prefers-reduced-motion: reduce` and render/return static; grep gate ≥2 |
| T-053-06 | Information (contrast) | tone colors on paper vs ink | mitigate | colors resolved from theme (paper.goldText #9C6B10 / ink.goldText #F0BE6A already text-safe); no raw hexes in atoms |
| T-053-SC | Tampering | package installs | accept | none — no npm install this phase |
</threat_model>

<verification>
- `npx tsc --noEmit` — atoms compile against Plan 01 tokens.
- `npm run lint` and `npm test` — no regressions introduced by the new module.
- Grep gates confirm exports, dotted leader, pending "≈", reduced-motion, and count-up reuse.
</verification>

<success_criteria>
- LedgerRow, Amount, StatusChip, useCountUp, Stamp all exported from components/design/atoms.tsx (DT-SC3, atoms-exist half).
- Theme-aware via a `theme` prop; colors sourced from lib/design/tokens; no hardcoded new hexes.
- Gold only on money; animations respect prefers-reduced-motion.
</success_criteria>

<output>
Create `.planning/phases/05.3-design-tokens/05.3-02-SUMMARY.md` when done.
</output>
