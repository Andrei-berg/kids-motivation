---
phase: 05.10-automation
plan: 03
type: execute
wave: 2
depends_on: [05.10-01, 05.10-02]
files_modified:
  - app/api/cron/daily/route.ts
  - utils/helpers.ts
  - vercel.json
  - tests/integration/allowance.test.ts
autonomous: true
requirements: [SC1]
must_haves:
  truths:
    - "On a child's scheduled allowance day (weekly or monthly, per its anchor), the daily cron credits their allowance_amount coins exactly once (D-06)"
    - "Re-running the daily cron on the same period never double-credits the allowance, reusing the existing wallet_transactions idempotency mechanism (D-09)"
    - "A child with no allowance_period configured is never credited an allowance — allowance is on/off only, no pause control (D-08)"
    - "A child added mid-period is not credited until their next scheduled period — no partial-period proration (D-07)"
    - "The daily cron is scheduled in vercel.json and runs once per day, staying on the Vercel Hobby tier (D-10)"
  artifacts:
    - path: "app/api/cron/daily/route.ts"
      provides: "Allowance crediting folded into the daily pass via creditAwards"
      contains: "source_type"
    - path: "vercel.json"
      provides: "Daily cron schedule entry for /api/cron/daily"
      contains: "/api/cron/daily"
    - path: "utils/helpers.ts"
      provides: "isoWeekKey() period-key helper for weekly allowance"
      contains: "isoWeekKey"
  key_links:
    - from: "app/api/cron/daily/route.ts"
      to: "app/api/wallet/_lib.ts creditAwards"
      via: "sourceType='allowance', sourceId=periodKey (idempotency)"
      pattern: "allowance"
    - from: "vercel.json crons"
      to: "/api/cron/daily"
      via: "daily schedule"
      pattern: "/api/cron/daily"
---

<objective>
Deliver scheduled allowance (SC1): fold per-child allowance crediting into the
daily cron route from Plan 02, and schedule that route in vercel.json. Allowance
is idempotent per period via the existing `(child_id, source_type, source_id)`
mechanism (D-09), weekly or monthly (D-06), no proration (D-07).

Purpose: SC1 — "Scheduled allowance credits coins per family schedule via cron
(idempotent per period)."
Output: allowance logic added to the daily route + `isoWeekKey` helper +
vercel.json cron entry + integration coverage (credit-once + no-double-credit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.10-automation/05.10-CONTEXT.md
@.planning/phases/05.10-automation/05.10-PATTERNS.md
@CLAUDE.md

<interfaces>
From app/api/wallet/_lib.ts (reuse — do NOT reimplement idempotency):
- `creditAwards(admin, childId, intents: AwardIntent[])` → { creditedCoins, applied }.
  Skips any intent whose `(child_id, source_type, source_id)` already exists in
  wallet_transactions; applies delta via `wallet_apply`; reverses on 23505 race.
- `AwardIntent = { coins: number; description: string; icon: string; sourceType: string; sourceId: string }`.

From children (schema applied in Plan 01):
- `trust_limit_coins INTEGER NOT NULL DEFAULT 0`
- `allowance_amount INTEGER | null`, `allowance_period 'weekly'|'monthly'|null`,
  `allowance_anchor INTEGER | null` (1-7 dow weekly, 1-31 dom monthly).

From vercel.json (current — only one cron):
  crons: [ { path: "/api/cron/missed-tasks", schedule: "0 20 * * *" } ]

Period keys (D-09): weekly → `YYYY-Www` (ISO week), monthly → `YYYY-MM`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add isoWeekKey helper</name>
  <files>utils/helpers.ts, tests/helpers.test.ts</files>
  <read_first>
    - utils/helpers.ts (localDateString/normalizeDate/addDays style + exports)
    - tests/helpers.test.ts (existing date-helper unit tests)
  </read_first>
  <behavior>
    - isoWeekKey(new Date('2026-07-13')) → '2026-W29' (ISO-8601 week, Monday-first).
    - isoWeekKey for a date in the first ISO week straddling Jan 1 returns the correct ISO week-year (may differ from calendar year).
  </behavior>
  <action>
    Export `isoWeekKey(date: Date): string` computing the ISO-8601 week number
    (weeks start Monday; week 1 contains the year's first Thursday) and returning
    `${isoWeekYear}-W${twoDigitWeek}`. Use the standard Thursday-shift algorithm.
    Add unit tests in tests/helpers.test.ts covering a mid-year date and a
    year-boundary date.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- helpers</automated>
  </verify>
  <acceptance_criteria>
    - `isoWeekKey` exported from utils/helpers.ts.
    - Tests assert the mid-year `YYYY-Www` value and a correct week-year at a Jan boundary.
  </acceptance_criteria>
  <done>isoWeekKey exists and is unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Fold allowance crediting into the daily cron route</name>
  <files>app/api/cron/daily/route.ts, tests/integration/allowance.test.ts</files>
  <read_first>
    - app/api/cron/daily/route.ts (Plan 02's route — extend it, keep the reminder checks intact)
    - app/api/wallet/_lib.ts (creditAwards + AwardIntent — call, do not reimplement)
    - app/parent/reading/actions.ts (creditAwards usage example: sourceType/sourceId shape)
    - tests/integration/purchase.test.ts + tests/integration/family-fixture.ts (live-DB integration pattern + fixture)
  </read_first>
  <action>
    In the daily route, before or after the reminder checks, add an allowance
    pass. Query `children` where `allowance_period IS NOT NULL` AND
    `allowance_amount > 0` (a child with no period is never credited — must_have).
    For each such child compute eligibility for today (localDateString):
    - weekly: eligible when `allowance_anchor === todayDow` (ISO 1-7); periodKey
      = `isoWeekKey(now)` (e.g. `2026-W29`).
    - monthly: eligible when today's day-of-month === `allowance_anchor`, with a
      clamp — if `allowance_anchor` exceeds the number of days in the current
      month, treat the month's LAST day as eligible (so anchor 31 still fires in
      short months). periodKey = `YYYY-MM` from today. NO proration for a
      mid-period-added child (D-07): eligibility is purely the anchor match.
    For each eligible child call
    `creditAwards(admin, child.id, [{ coins: child.allowance_amount, description: 'Пособие', icon: '💰', sourceType: 'allowance', sourceId: periodKey }])`.
    Idempotency (re-run safety, retries, overlapping invocations) is fully handled
    by creditAwards' `(child_id,'allowance',periodKey)` pre-check + unique-index
    backstop (D-09, threat T-0510-09) — add NO new idempotency code. Optionally
    push the child "Пособие зачислено: +{amount} 🪙" via the same
    sendPushToSubscription path already in the route. Extend the route's JSON
    summary with `allowanceCredited` (count). Add tests/integration/allowance.test.ts:
    configure a test child with `allowance_period='weekly'`, `allowance_anchor`
    = today's ISO dow, `allowance_amount` (e.g. 50); invoke GET twice with a
    valid Bearer; assert the wallet is credited exactly once (one 'allowance'
    wallet_transactions row for the period key, balance +50 total across both
    runs) and a child with `allowance_period=null` is never credited.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- allowance</automated>
  </verify>
  <acceptance_criteria>
    - Route selects children by `allowance_period IS NOT NULL` and `allowance_amount > 0`.
    - `creditAwards` is called with `sourceType: 'allowance'` and `sourceId` = the ISO-week or YYYY-MM period key.
    - allowance.test.ts: two GET invocations produce exactly ONE 'allowance' transaction for the period and a net credit equal to a single allowance_amount; a null-period child gets zero.
    - Grep: no bespoke idempotency query inside the allowance pass (relies on creditAwards).
  </acceptance_criteria>
  <done>Allowance credits once per period per eligible child; re-runs never double-credit.</done>
</task>

<task type="auto">
  <name>Task 3: Schedule the daily cron in vercel.json</name>
  <files>vercel.json</files>
  <read_first>
    - vercel.json (current single-cron file)
    - .planning/phases/05.10-automation/05.10-CONTEXT.md D-10 (Hobby tier, daily cadence)
  </read_first>
  <action>
    Add a second `crons` entry for `path: "/api/cron/daily"` with a daily
    schedule `0 13 * * *` (13:00 UTC = 16:00 MSK — the fixed "evening" cutoff for
    reminders; allowance is period-keyed so time-of-day is irrelevant to it).
    Keep the existing `/api/cron/missed-tasks` `0 20 * * *` entry unchanged. Total
    crons after this = two (missed-tasks + daily), within the Vercel Hobby daily-
    cron budget per D-10 — do NOT add any third cron. The dead
    `/api/cron/reminders` route stays unscheduled.
  </action>
  <verify>
    <automated>node -e 'const c=require("./vercel.json").crons;const p=c.map(x=>x.path);process.exit(p.includes("/api/cron/daily")&&p.includes("/api/cron/missed-tasks")&&c.length===2?0:1)'</automated>
  </verify>
  <acceptance_criteria>
    - vercel.json `crons` has exactly two entries: `/api/cron/missed-tasks` and `/api/cron/daily`.
    - `/api/cron/daily` schedule is `0 13 * * *`.
    - `/api/cron/reminders` is NOT scheduled.
  </acceptance_criteria>
  <done>The daily cron is scheduled once per day; cron count stays at two (Hobby-safe).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → /api/cron/daily | already cron-authed in Plan 02; allowance crediting now runs behind that same guard |
| cron route → wallet money tables | credits via service-role creditAwards only (RLS SELECT-only for clients) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0510-09 | Tampering (double-credit) | allowance crediting on cron re-run/overlap | mitigate | Rely on creditAwards' `(child_id,'allowance',periodKey)` idempotency pre-check + unique-index backstop (D-09) — retries and overlapping invocations credit at most once per period |
| T-0510-10 | Elevation | allowance eligibility | mitigate | Amount/period/anchor read server-side from `children` via admin client; no client input path to the cron; a null-period child is filtered out |
| T-0510-11 | Spoofing | /api/cron/daily entry | mitigate | Guarded by `assertCronAuth` (established in Plan 02); scheduling adds no new unauthenticated surface |
| T-0510-12 | DoS (cost) | new recurring infra | accept | Cron count held at two on Hobby (D-10); no Pro upgrade, no per-minute polling |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm test -- helpers`, `npm test -- allowance` green.
- vercel.json has exactly two cron entries incl. `/api/cron/daily`.
</verification>

<success_criteria>
- Eligible child credited exactly once per period; re-runs idempotent; null-period child never credited; daily cron scheduled (SC1).
</success_criteria>

<output>
Create `.planning/phases/05.10-automation/05.10-03-SUMMARY.md` when done.
</output>
