# Phase 05.10: automation - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-22
**Phase:** 05.10-automation
**Areas discussed:** Trust-limit auto-approve, Allowance rules

---

## Trust-limit auto-approve

| Option | Description | Selected |
|--------|-------------|----------|
| Per-child | Matches roadmap wording; different kids can get different auto-approve ceilings | ✓ |
| Family-wide | One limit for every child, simpler but less granular | |

**User's choice:** Per-child

| Option | Description | Selected |
|--------|-------------|----------|
| OR-combine | Auto-approve if reward.auto_approve OR price <= trust limit | ✓ |
| Reward flag can force manual review | Adds a tri-state override on rewards | |

**User's choice:** OR-combine

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, distinct reason shown | New processed_by value distinguishes trust-limit from reward-flag auto-approvals | ✓ |
| No, single generic badge | All auto-approvals look the same | |

**User's choice:** Yes, distinct reason shown

| Option | Description | Selected |
|--------|-------------|----------|
| 0 / disabled by default | No auto-approve via trust limit until parent opts in | ✓ |
| Small nonzero default | Feature demonstrates value out of the box | |

**User's choice:** 0 / disabled by default

**Notes:** All four questions resolved on the recommended option with no pushback or additional detail volunteered.

---

## Allowance rules

| Option | Description | Selected |
|--------|-------------|----------|
| Per-child | Consistent with per-child trust limit; different amounts per child | ✓ |
| Family-wide | One schedule for everyone | |

**User's choice:** Per-child

| Option | Description | Selected |
|--------|-------------|----------|
| Weekly + monthly | Covers both common cadences with an anchor field each | ✓ |
| Weekly only | Simpler idempotency surface | |

**User's choice:** Weekly + monthly

| Option | Description | Selected |
|--------|-------------|----------|
| Full amount on next scheduled date | No proration logic; child isn't eligible until next period boundary | ✓ |
| Prorate for the partial first period | More "fair" but adds rounding/edge-case complexity | |

**User's choice:** Full amount on next scheduled date

| Option | Description | Selected |
|--------|-------------|----------|
| No — on/off only for now | Simplest version; pausing one cycle is a manual coin adjustment | ✓ |
| Yes — add a pause/skip-next-cycle control | Real UI affordance for a consequence lever | |

**User's choice:** No — on/off only for now

**Notes:** All four questions resolved on the recommended option with no pushback or additional detail volunteered.

---

## Claude's Discretion

- **Cron cadence / Vercel Pro tradeoff** (Cron cadence / Vercel Pro area — user chose not to discuss it). The existing `/api/cron/reminders` route needs 5-minute cadence (Vercel Pro); Hobby only allows 1 run/day per cron. Not decided — flagged in CONTEXT.md as a decision research/planning must surface back to the user before committing to an SC3 implementation shape.
- **Reminder scope & timing** (Reminder scope & timing area — user chose not to discuss it). Which of the 3 reminder types ship, "evening" cutoff definition, and recipient (child/parent/both) were left open, partly dependent on the cadence decision above.
- Exact `processed_by` value/label for trust-limit auto-approvals.
- Exact UI/copy placement for trust-limit and allowance settings (likely Parent Center → Settings, following existing per-child settings card patterns).

## Deferred Ideas

None — discussion stayed within phase scope.
