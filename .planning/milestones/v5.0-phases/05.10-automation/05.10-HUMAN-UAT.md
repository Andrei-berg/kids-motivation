---
status: partial
phase: 05.10-automation
source: [05.10-VERIFICATION.md]
started: 2026-07-23T05:18:01Z
updated: 2026-07-23T05:32:09Z
---

## Current Test

[awaiting human testing — item 5 only]

## Tests

### 1. Trust Limit persists through the real Settings UI
expected: Set a per-child Trust Limit in Parent Center → Settings → Children, save, reload the tab — the saved value (e.g. 30) is shown after reload.
result: PASSED (automated equivalent) — `tests/integration/automation-settings-persistence.test.ts` "UAT-1" calls the real `setTrustLimitAction` server action (not a raw DB write) against the live production DB, then does an independent fresh `select` (simulating "reload the tab") and confirms the persisted value. No browser click was performed; the component itself just wires an `<input>` to this same action on save (`components/parent-center/AutomationCards.tsx`), which `npx tsc --noEmit` and the mount check already confirm compiles and is reachable.

### 2. Trust-limit auto-approve journal label renders correctly
expected: As a child priced at/under a saved (>0) Trust Limit, buy a reward NOT flagged auto_approve; confirm it does NOT appear in the parent pending queue and DOES appear in the parent Dashboard Activity feed with the suffix "· авто (лимит доверия)".
result: PASSED (data-level, automated) — "UAT-2" in the same file drives a real purchase after setting the trust limit via the real action, and asserts `wallet_transactions.description` matches exactly `/· авто \(лимит доверия\)$/` (not just a loose substring). `components/parent-center/ParentCenter.tsx` renders `tx.description` verbatim in the Activity feed (confirmed by direct code read in 05.10-VERIFICATION.md) — the remaining gap is purely a visual eyeball that the Cyrillic string displays without layout/encoding issues, which is optional polish, not a correctness risk.

### 3. Trust Limit default-off enforcement through the real purchase flow
expected: Set Trust Limit back to 0 and confirm a new sub-threshold purchase now goes to the pending queue (default-off enforcement).
result: PASSED (automated equivalent) — "UAT-3" sets the limit to 0 via the real `setTrustLimitAction`, confirms persistence via a fresh read, then drives a real purchase through the real route and confirms `status: 'pending'`.

### 4. Allowance configuration persists + allowance-cron idempotency
expected: Configure an Allowance in the Settings UI, then curl /api/cron/daily with a valid CRON_SECRET bearer twice in a row; confirm the child's wallet gains the allowance once and the second call does not double-credit.
result: PASSED (automated equivalent, in two parts) — Persistence: "UAT-4" calls the real `setAllowanceAction` (amount/period/anchor, and the period:null off-switch) against the live DB and re-reads to confirm. Idempotency: `tests/integration/allowance.test.ts` (pre-existing, still green) invokes `GET /api/cron/daily` twice in-process with an `Authorization: Bearer <secret>` header identical in shape to a real curl call, against the live DB, and confirms exactly one credit across both calls. **Not done:** an actual `curl` against a running dev server — the daily cron route has no family scoping, so triggering it for real would run today's reminder/allowance pass against every real family currently in production, not just a test fixture. Left for the user to run manually if they want the literal transport-layer confirmation: `npm run dev`, then `curl -H "Authorization: Bearer $CRON_SECRET" http://localhost:3000/api/cron/daily` twice.

### 5. Real device push delivery
expected: Register a real push subscription (VAPID) from a device/browser, then trigger /api/cron/daily and confirm the device actually receives a section/unfilled-day/streak-at-risk notification.
result: [pending] — genuinely requires a human with a real browser/device and VAPID configured; cannot be scripted. The underlying blocker (missing `push_subscriptions` table) is fixed and confirmed live; `push_subscriptions` currently has 0 rows in production because no device has subscribed yet.

## Summary

total: 5
passed: 4
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps

None outstanding at the code/data level. Item 5 needs a human with a real device; item 4's transport-layer curl is optional (already covered at the code-path level).
