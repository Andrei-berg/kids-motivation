---
phase: 05.8-parent-redesign
plan: 05
type: execute
wave: 2
depends_on: [05.8-01]
files_modified:
  - components/settings/DayBlocksManager.tsx
autonomous: true
requirements: [D-02, D-04, D-09]

must_haves:
  truths:
    - "DayBlocksManager renders entirely on ink T tokens with no rgba/hex remnants (D-04, part of the D-09 sweep)"
    - "A per-child block can be linked to one of the child's schedule_items via an in-row picker"
    - "Picking a schedule item writes schedule_link + days_of_week (converted 1..7 to 0..6) in one updateDayBlock call"
    - "The linked state shows a success StatusChip with the synced item title; unlinked state shows an indigo trigger"
  artifacts:
    - path: "components/settings/DayBlocksManager.tsx"
      provides: "ink-recolored Day Constructor + D-02 schedule-link picker"
      contains: "scheduleDowToBlockDow"
  key_links:
    - from: "components/settings/DayBlocksManager.tsx"
      to: "lib/day-blocks.ts"
      via: "import scheduleDowToBlockDow"
      pattern: "scheduleDowToBlockDow"
    - from: "components/settings/DayBlocksManager.tsx"
      to: "lib/repositories/schedule.repo.ts"
      via: "getScheduleItems(familyId, childMemberId)"
      pattern: "getScheduleItems"
    - from: "components/settings/DayBlocksManager.tsx"
      to: "lib/repositories/day-blocks.repo.ts"
      via: "updateDayBlock({ schedule_link, days_of_week })"
      pattern: "updateDayBlock"
---

<objective>
Recolor DayBlocksManager onto the ink theme (D-04) and add the D-02 schedule-link picker: an in-row expander (matching the existing who-fills/day-types expansion pattern) that lists a child's `schedule_items` and, on pick, denormalizes the item's weekdays into the block's `days_of_week` so `assembleDayBlocks` and `/api/wallet/award` need zero changes.

Purpose: Delivers the "block auto-appears only on its linked schedule item's weekdays" capability that Phase 5.6 explicitly reserved for this phase, using the denormalize-on-write approach locked by RESEARCH/UI-SPEC (not a live join).
Output: An ink-themed Day Constructor with a working, tested-helper-backed schedule-link picker. (D-03 per-child override lands in Plan 08, layered on this file.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05.8-parent-redesign/05.8-PATTERNS.md
@.planning/phases/05.8-parent-redesign/05.8-UI-SPEC.md
@.planning/phases/05.8-parent-redesign/05.8-01-SUMMARY.md

<interfaces>
lib/day-blocks.ts — export function scheduleDowToBlockDow(days: number[]): number[]  // 1..7 to 0..6 (from Plan 01)
lib/repositories/schedule.repo.ts — export async function getScheduleItems(familyId: string, childMemberId: string): Promise<ScheduleItem[]>
  ScheduleItem: { id, title, type, day_of_week: number[] /*1=Mon..7=Sun*/, child_member_id, ... }
lib/repositories/day-blocks.repo.ts — export async function updateDayBlock(id: string, fields: { name?; icon?; price?; day_types?; who_fills?; multipliers?; schedule_link?: string|null; days_of_week?: number[] }): Promise<void>
lib/models/day-block.types.ts — DayBlock.schedule_link: string | null  (reserved, unread before this phase); DayBlock.days_of_week: number[]
components/parent-center/tokens.ts — import { T } from '@/components/parent-center/tokens'  (NO gold key)
components/parent-center/ui.tsx — Btn (variant="ghost", size="sm"), Icon (name="chevD")
components/design/atoms.tsx — StatusChip({ tone, children, theme, style })  (use theme="ink", tone="success")
Child-id resolution pattern (reuse verbatim, PeriodsManager.tsx:71-83): supabase.from('family_members').select('id, child_id, display_name').eq('family_id', fid).eq('role','child').order('created_at') — never getFamilyChildren().
i18n keys (created in Plan 01): settings.dayBlocksManager.scheduleLink.none, .scheduleLink.synced ({{title}}), .resync, .ariaMoveUp/.ariaMoveDown/.ariaEnable/.ariaDisable/.ariaDelete, addBlock ("+ Add block")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recolor DayBlocksManager.tsx (D-04) + add icon-button aria-labels</name>
  <files>components/settings/DayBlocksManager.tsx</files>
  <read_first>
    - components/settings/DayBlocksManager.tsx (current inline rgba/hex palette, row layout, iconBtn const ~lines 414-418)
    - .planning/phases/05.8-parent-redesign/05.8-PATTERNS.md DayBlocksManager recolor substitution table (line-referenced) and UI-SPEC D-04 table
    - .planning/phases/05.8-parent-redesign/05.8-UI-SPEC.md Accessibility (aria-label copy for row move/enable/disable/delete)
  </read_first>
  <action>
    Add `import { T } from '@/components/parent-center/tokens'` (absolute path — this file is under components/settings/, not parent-center/). Apply the PATTERNS/UI-SPEC D-04 substitution table verbatim: row bg `rgba(255,255,255,0.05)` to `T.card`, row border `rgba(255,255,255,0.1)` to `T.cardBorder`, muted text `rgba(238,238,255,0.5/0.4/0.35/0.3)` to `T.muted`, `#fff` to `T.text`, add-form panel + icon-picker active `rgba(99,102,241,0.06/0.2/0.15/0.5/0.8)` to `T.indigoSoft`/`T.cardBorderHi`/`T.indigo`, error banner `rgba(244,63,94,*)`/`#F43F5E` to `T.dangerSoft`/`T.danger`, active-toggle `rgba(16,185,129,*)`/`#34d399` to `T.successSoft`/`T.success`, `iconBtn` const borders/colors to `T.cardBorder`/`T.card`/`T.muted`. Bump row internal padding `'10px 12px'` to `'12px 12px'` (UI-SPEC Spacing exception). Add `aria-label` (from the Plan 01 i18n keys) to each icon-only row control: move-up (ariaMoveUp), move-down (ariaMoveDown), toggle-active (ariaEnable/ariaDisable, state-dependent), delete (ariaDelete). Preserve structure exactly: row layout, button order (up, down, toggle, delete), "system block" lock badge, vacation multiplier field on built-ins, who-fills select, day-types checkboxes. No gold, no amber.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -viE '^\s*//' components/settings/DayBlocksManager.tsx | grep -iE 'rgba\(255,255,255|rgba\(238,238,255|#fff|#F43F5E|#34d399|amber|#F0BE6A' ; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - The final grep finds nothing (prints `exit:1`)
    - `grep -c "aria-label" components/settings/DayBlocksManager.tsx` returns at least 4 (move up/down, toggle, delete)
    - `grep -c "'12px 12px'\|\"12px 12px\"" components/settings/DayBlocksManager.tsx` returns at least 1 (row padding updated)
    - `grep -q "@/components/parent-center/tokens" components/settings/DayBlocksManager.tsx`
  </acceptance_criteria>
  <done>DayBlocksManager is fully on ink tokens, row controls carry aria-labels, structure preserved; no forbidden remnants.</done>
</task>

<task type="auto">
  <name>Task 2: Add D-02 schedule-link picker (in-row expander, denormalize-on-write)</name>
  <files>components/settings/DayBlocksManager.tsx</files>
  <read_first>
    - components/settings/DayBlocksManager.tsx (existing who-fills/day-types in-row expansion pattern ~lines 373-403, paddingLeft:28)
    - .planning/phases/05.8-parent-redesign/05.8-UI-SPEC.md Screen Contract Day Constructor D-02 (concrete layout, trigger states, one-updateDayBlock-call write)
    - .planning/phases/05.8-parent-redesign/05.8-PATTERNS.md D-02 insertion point + child-id resolution excerpt
    - lib/repositories/schedule.repo.ts getScheduleItems, lib/day-blocks.ts scheduleDowToBlockDow
  </read_first>
  <action>
    Resolve children with the verbatim PeriodsManager child-id query (family_members: id + child_id + display_name; role='child') so both `children.id` and `family_members.id` (child_member_id) are available — never getFamilyChildren(). For each block rendered in a per-child context (a block with a non-null child_id, or a family-default block being edited for a specific child — the D-03 plan wires the child context; here, gate the picker to render only when a childMemberId is resolvable for the block's child_id), add a new in-row expander line AFTER the existing who-fills/day-types line, using the same paddingLeft:28 visual language. The trigger is a `Btn variant="ghost" size="sm"` showing `scheduleLink.none` copy when unlinked (rendered in T.indigo to invite the tap) or `scheduleLink.synced` copy ({{title}} = linked item title) with a success StatusChip (theme="ink") when linked (trigger reverts to T.text — do not double-signal indigo AND chip). Tapping opens an inline list (not a modal) of that child's `getScheduleItems(familyId, childMemberId)` rendered as "{title} — {abbreviated days}", plus a "Not linked" clear option at top. On pick, call `updateDayBlock(block.id, { schedule_link: item.id, days_of_week: scheduleDowToBlockDow(item.day_of_week) })` then reload; on "Not linked", call `updateDayBlock(block.id, { schedule_link: null, days_of_week: [] })`. Add a small "re-sync" ghost icon-button (resync i18n copy) that re-runs the same write to re-copy days if the schedule item later changed (manual re-copy, not a live join). Wrap the async handler in the existing try/catch → setError pattern.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "scheduleDowToBlockDow" components/settings/DayBlocksManager.tsx && grep -q "getScheduleItems" components/settings/DayBlocksManager.tsx && grep -q "schedule_link" components/settings/DayBlocksManager.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and the verify prints `OK`
    - `grep -c "getFamilyChildren" components/settings/DayBlocksManager.tsx` returns 0 (correct child-id resolution used)
    - The picker write uses a single `updateDayBlock` call passing both `schedule_link` and `days_of_week: scheduleDowToBlockDow(...)` (grep shows scheduleDowToBlockDow adjacent to updateDayBlock)
    - `grep -c "StatusChip" components/settings/DayBlocksManager.tsx` returns at least 1 (synced-state chip)
    - No changes to lib/day-blocks.ts assembleDayBlocks or any /api/wallet/award file (git status shows only DayBlocksManager.tsx modified in this plan)
  </acceptance_criteria>
  <done>The schedule-link picker lists the child's schedule items, writes schedule_link + converted days_of_week in one call, shows synced/unlinked states, and touches no award/assembly code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (browser client, RLS) | Schedule-link writes go to `day_blocks` (NOT a money table) via existing browser client under family-isolation RLS scoped to parent/extended roles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-058-08 | Tampering | schedule-link write (updateDayBlock) | mitigate | Writes go through the existing browser client under day_blocks RLS `WITH CHECK` (family_id scoped to auth.uid()'s family); item.id comes from getScheduleItems which is itself RLS-scoped to the family — no cross-family id injection possible |
| T-058-09 | Elevation of Privilege | day_blocks config write | mitigate | RLS restricts day_blocks INSERT/UPDATE to parent/extended roles; UI does not import lib/supabase/admin.ts (day-blocks.repo header forbids it) |
| T-058-10 | Tampering | days_of_week off-by-one | mitigate | Conversion goes through the Plan-01 tested `scheduleDowToBlockDow` helper, not ad-hoc math — prevents silent day-shift |

No new endpoint, no money-table write, no free-text field (schedule items are picked from a scoped list, not typed). Existing RLS is the boundary.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- Recolor grep gate finds zero forbidden remnants
- Picker uses scheduleDowToBlockDow + getScheduleItems + single updateDayBlock write; no getFamilyChildren
- `npm test` still green (no award/assembly logic touched)
</verification>

<success_criteria>
- DayBlocksManager fully ink-themed with aria-labelled row controls
- Working schedule-link picker with synced/unlinked/re-sync states, denormalize-on-write
- assembleDayBlocks and /api/wallet/award unchanged
</success_criteria>

<output>
Create `.planning/phases/05.8-parent-redesign/05.8-05-SUMMARY.md` when done
</output>
