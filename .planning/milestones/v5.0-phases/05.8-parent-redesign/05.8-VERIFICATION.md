---
phase: 05.8-parent-redesign
verified: 2026-07-21T08:33:07Z
status: human_needed
score: 9/9 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found (code review — 05.8-REVIEW.md, not a formal VERIFICATION.md gate)
  previous_score: "2 Critical + 7 Warning findings open"
  gaps_closed:
    - "CR-01: D-02 schedule-link picker was unreachable dead code (gated on childMemberId that was always undefined in the only rendering list)"
    - "CR-02: Weekly Summary 'Tasks done' was a same-day, hardcoded-total(5) metric, structurally capped at 40%, mislabeled as weekly"
    - "WR-01: weeklyError flag was unreachable dead code (getTransactions never rejects)"
    - "WR-02: ChatPanel day-delta indicators carried banned neon colors as decimal RGB, missed by the hex-literal sweep grep"
    - "WR-03: New Tabs usages (Year Calendar child filter) didn't get the indigo active-tab accent"
    - "WR-04: UI-SPEC's D-05 tap-cell-to-add/edit-vacation-period interaction was never implemented"
    - "WR-05: No regression test coverage for addDayBlock legacyKey passthrough / getDayBlocks dedup merge"
    - "WR-06: Per-child override price field silently accepted non-numeric input and nulled the price"
    - "WR-07: writeChildOverride's create-then-carry-over path had a partial-write window that could permanently drop days_of_week/multipliers/schedule_link"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Open Parent Center → Settings → Schedule → Day Constructor (byChild layout, default). Select a child's tab (not 'Family default'). On a built-in block row (e.g. Room), tap the '🔗 Schedule link' trigger, pick a schedule item from the dropdown, and confirm a green 'Custom' tag + synced StatusChip appear on that child's row, and that the family-default tab / other children are unaffected."
    expected: "The picker opens, the item list shows that child's schedule_items, picking one closes the picker and shows a success StatusChip with the item's title; re-opening the picker and picking 'None' clears it back to unlinked (indigo trigger)."
    why_human: "The 05.8-09 checkpoint's human sign-off for D-02 was recorded against the OLD (broken) rendering location (renderFamilyDefaultList), which the operator could never actually reach — CR-01 relocated the picker into a different view (renderChildResolvedList, reached via a per-child tab) after that checkpoint ran. Code-level wiring, data flow, and the write path are verified by reading the code (writeChildOverride → addDayBlock/updateDayBlock, same pattern as the already-approved price/who-fills handlers) and by the WR-05 regression tests on the underlying dedup logic, but no human has visually clicked through the picker in its new location."
  - test: "Open Parent Center → Analytics. Confirm the 'This week → Tasks done' StatusChip on the Weekly Summary card renders success (green, ≥80%) or warning (yellow, 50-79%) for a family with mostly-filled, mostly-good days this week, rather than being permanently red."
    expected: "The badge color reflects actual week performance and is not structurally stuck at danger/red."
    why_human: "CR-02's fix is verified at the code/data level (getWeekScore is genuinely week-scoped; roomOkDays+behaviorDays over filledDays*2 removes the old hardcoded /5 cap) but the actual rendered color/copy in the browser against a real family's data was not part of the original 09 checkpoint (which ran before this fix existed) and has not been re-confirmed visually."
---

# Phase 5.8: parent-redesign — Re-Verification Report

**Phase Goal:** Parent Center on unified tokens + Day Constructor UI + Year Calendar screen + Weekly Summary card
**Verified:** 2026-07-21T08:33:07Z
**Status:** human_needed
**Re-verification:** Yes — after 05.8-REVIEW.md (2 Critical + 7 Warning findings) and 05.8-REVIEW-FIX.md (9/9 fixed via 9 atomic commits)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC1 — Parent Center screens use the unified ink theme; no Sora/cyan/neon-green remnants | ✓ VERIFIED (see note) | `tsc --noEmit` clean, `npm run build` 55/55 pages, hex-literal sweep (`#00E676`, `#4DDCFF`, `#F59E0B`, `bg/text-amber-`, `#F0BE6A`) returns zero matches across all 9 plans' files; D-09 human-approved 2026-07-21 (05.8-09-SUMMARY.md). **Caveat:** a pre-existing (not introduced by 5.8), out-of-declared-scope decimal-RGB remnant was found in `components/parent-center/ui.tsx`'s `Btn`/`Pill` success/danger palettes — see Anti-Patterns below. Does not block this truth (out of D-09's CONTEXT.md-declared file list, which explicitly scopes the sweep to screens, not the shared `ui.tsx` atom file beyond the one already-fixed cyan-hover line). |
| 2 | SC2 — Day Constructor screen manages `day_blocks` per child (toggle, who fills, price, day types) | ✓ VERIFIED | `components/settings/DayBlocksManager.tsx`: toggle (`toggleActive`), who-fills (`saveWhoFills`/`handleChildWhoFillsEdit`), price (`savePrice`/`handleChildPriceEdit`, now `Number.isFinite`-guarded per WR-06), day types (`toggleDayType`) all present and wired to `updateDayBlock`/`writeChildOverride`. |
| 3 | SC3 — Year Calendar screen manages 5.5 data; Analytics shows the Weekly Summary card | ✓ VERIFIED | `components/settings/CalendarGrid.tsx` renders a real month/year grid (day-type coloring, sick-dot overlay, WR-04 tap-to-add/edit); `components/parent-center/screens/Analytics.tsx`'s `WeeklySummaryCard` now shows real (not static) coins/tasks/streak facts, backed by `ParentCenter.tsx`'s week-scoped aggregation. |
| 4 | D-02 (re-check) — schedule-link picker is reachable through a real user path and writes `schedule_link`+`days_of_week` via the copy-on-write path | ✓ VERIFIED | `DayBlocksManager.tsx:773-887` (`renderChildResolvedList`), rendered at line 993 whenever `layout === 'byChild'` (default) and a child tab is selected (`selectedChildTab !== 'family'`) — a fully reachable, no-prerequisite UI path. Picker gated on `isBuiltIn && childMemberId` (both always true for a resolved built-in row under a real child). `pickChildScheduleLink` (line 443) calls `writeChildOverride(childId, row.legacy_key, {schedule_link, days_of_week})` (line 446), which goes through the same `addDayBlock`/`updateDayBlock` copy-on-write path as the already-shipped price/who-fills handlers (lines 376-409). Traced end-to-end; no dead code remains in this path. |
| 5 | D-08 (re-check) — Weekly Summary "Tasks done" is week-scoped, not structurally capped, 80/50 thresholds reachable | ✓ VERIFIED | `lib/services/coins.service.ts`'s `getWeekScore` queries `days` with `.gte('date', week.start).lte('date', week.end)` (line 25, genuinely week-scoped) and now returns `behaviorDays` (line 34, previously computed but discarded) alongside `roomOkDays`/`filledDays`. `ParentCenter.tsx:156-157` computes `weekTasksDone = roomOkDays + behaviorDays`, `weekTasksTotal = filledDays * 2` (no hardcoded 5), aggregated across children at lines 211-213 into `taskRate`. `completionTone` (`lib/weekly-summary.ts:21-25`) at 80/50 is unchanged and now reachable — a family with well-filled days can genuinely hit 80%+ (2/2 booleans true on every filled day), unlike the old `2/5` hard cap. |
| 6 | WR-01..WR-07 fixes present, no regressions, consistent with PLAN.md must_haves | ✓ VERIFIED | All 7 fixes traced in code (see Requirements Coverage + commit list below); `npx tsc --noEmit` clean; `npm test` → 88 passed / 26 skipped (up from the pre-fix 83 passed, consistent with the 5 new WR-05 test cases in `tests/day-blocks-repo.test.ts`); `npm run build` → 55/55 pages, no errors. No file outside the 9 documented fix commits' scope was touched. |

**Score:** 6/6 truths verified (9/9 counting the individual WR sub-items rolled into truth 6 as a single gated check)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `components/settings/DayBlocksManager.tsx` | D-02 picker reachable + D-03 per-child override UI, WR-06/WR-07 fixes | ✓ VERIFIED | Picker wired into `renderChildResolvedList` (CR-01); `handleChildPriceEdit` rejects non-finite input (WR-06); `writeChildOverride`'s first-edit path is a single `addDayBlock` call carrying all fields (WR-07 — two-write window eliminated) |
| `components/parent-center/ParentCenter.tsx` | Week-scoped Weekly Summary aggregation, reachable error state | ✓ VERIFIED | `weekTasksDone`/`weekTasksTotal` (CR-02); `getTransactionsStrict(...).catch(() => weeklyError = true)` (WR-01) |
| `lib/services/coins.service.ts` | `getWeekScore` exposes `behaviorDays` | ✓ VERIFIED | Line 34 computes and returns `behaviorDays` |
| `lib/repositories/wallet.repo.ts` | `getTransactionsStrict` rethrows on Supabase error | ✓ VERIFIED | Lines 919-936, `if (error) throw error` |
| `components/parent-center/screens/ChatPanel.tsx` | Token-derived day-delta colors (no decimal-RGB neon) | ✓ VERIFIED | Local `hexAlpha` helper + `hexAlpha(T.success/T.danger, alpha)` at line 193; old `rgba(0,230,118,*)`/`rgba(255,107,107,*)` literals gone |
| `components/settings/CalendarGrid.tsx` | Indigo active-tab Tabs (WR-03) + tap-to-add/edit-period (WR-04) | ✓ VERIFIED | Imports `Tabs` from `@/components/design/atoms` (indigo-active) with `theme="ink"`; `onCellClick` prop wired to `Settings.tsx`'s `calendarCellRequest` state |
| `components/settings/PeriodsManager.tsx` | Consumes `openRequest`/`onOpenRequestHandled` to open add/edit form from a cell tap | ✓ VERIFIED | Lines 28-138: `useEffect` on `openRequest` calls `openEdit` (matched period) or pre-fills `startDate`/`endDate` (no period) |
| `tests/day-blocks-repo.test.ts` | New regression coverage for `addDayBlock` legacyKey + `getDayBlocks` dedup (WR-05) | ✓ VERIFIED | 5 test cases, `vi.mock('@/lib/supabase')`-backed, matching the established pattern; all passing |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `DayBlocksManager.tsx` schedule-link picker (per-child view) | `lib/repositories/day-blocks.repo.ts` `addDayBlock`/`updateDayBlock` | `pickChildScheduleLink` → `writeChildOverride` | ✓ WIRED | Reachable render path confirmed (byChild layout is the default; any child tab exposes it); write path identical to already-shipped price/who-fills handlers |
| `ParentCenter.tsx` weekly aggregation | `lib/services/coins.service.ts` `getWeekScore` | `Promise.all(...).then(weekScore => weekTasksDone/weekTasksTotal)` | ✓ WIRED | `roomOkDays`/`behaviorDays`/`filledDays` all consumed; result flows into `setWeeklySummary` → `AnalyticsScreen` → `WeeklySummaryCard` → `completionTone(taskRate)` |
| `ParentCenter.tsx` `weeklyError` | `Analytics.tsx` `WeeklySummaryCard`'s error banner | `weeklyError` prop → `if (weeklyError) return <...loadError banner>` | ✓ WIRED | `getTransactionsStrict` now actually throws on failure, making the `.catch()` reachable |
| `CalendarGrid.tsx` cell tap | `PeriodsManager.tsx` add/edit form | `onCellClick` → `Settings.tsx` `calendarCellRequest` state → `openRequest` prop | ✓ WIRED | `AccordionSection` made optionally-controlled so the vacations accordion auto-expands to reveal the opened form |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| Weekly Summary "Tasks done" StatusChip | `taskRate` | `getWeekScore(childId, weekStart)` → real `days` table query, `.gte`/`.lte` on the week window | Yes — genuine per-child, per-week DB aggregation, not a static/hardcoded value | ✓ FLOWING |
| D-02 schedule-link picker item list | `scheduleItemsByMember[childMemberId]` | `getScheduleItems(fid, memberId)` in `loadChildrenAndSchedule` | Yes — real `schedule_items` DB query per child | ✓ FLOWING |
| D-03 per-child resolved list (`renderChildResolvedList` rows) | `overridesByChild[childId]` | `getDayBlocks(familyId, {childId})` in `loadOverrides` | Yes — real dedup-merged DB query, covered by WR-05 regression tests | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Type check passes | `npx tsc --noEmit` | Clean, no output | ✓ PASS |
| Test suite passes (incl. new WR-05 coverage) | `npm test` | 88 passed / 26 skipped (12 files, 4 skipped) | ✓ PASS |
| Production build succeeds | `npm run build` | 55/55 pages generated, no errors | ✓ PASS |
| No exact-hex neon/amber remnants in the 9 in-scope files | grep sweep across all 13 recolored files for `#00E676`/`#4DDCFF`/`#F59E0B`/`bg-amber-`/`text-amber-`/`#F0BE6A`/decimal-RGB equivalents | Zero matches except a pre-existing, out-of-plan-scope hit in `ui.tsx` (see Anti-Patterns) | ✓ PASS (with disclosed caveat) |
| WR-05's own target (dedup mechanism) is exercised by the kid-facing day-fill form, not just settings | `grep getDayBlocks app/kid/(app)/day/page.tsx components/DailyModal.tsx` | Both call `getDayBlocks(familyId, {childId, activeOnly:true})` — same function, same dedup path the new tests cover | ✓ PASS |

### Requirements Coverage

D-01 through D-09 are phase-local Decision IDs defined in `05.8-CONTEXT.md` (not REQ-IDs/SC-IDs in `.planning/REQUIREMENTS.md` — that file's "v5.0 Requirements" traceability table currently only registers SC-IDs through Phase 5.5; Phases 5.6–5.11's Decision-ID-style requirements are tracked via ROADMAP.md/CONTEXT.md/PLAN frontmatter instead, consistent with how Phases 5.6 and 5.7 are also absent from that table — **pre-existing documentation-process gap, not introduced by this phase, not blocking**).

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| D-01 | 05.8-06 | Day Constructor/Year Calendar stay nested, no new top-level nav | ✓ SATISFIED | `ParentCenter.tsx` `navItems` unchanged (dashboard/children/tasks/shop/analytics/expenses/audit); human-confirmed in 05.8-09-SUMMARY.md |
| D-02 | 05.8-05, fixed by CR-01 | Per-child schedule-link picker | ✓ SATISFIED | Re-verified above — now reachable and wired |
| D-03 | 05.8-08 | Per-child override UI, both candidates behind toggle | ✓ SATISFIED | Operator explicitly kept both layouts (05.8-09-SUMMARY.md); code present for both `renderChildResolvedList` (a) and the "Override for…" expander (b) |
| D-04 | 05.8-05 | DayBlocksManager ink-theme recolor | ✓ SATISFIED | Uses `T`/tokens throughout; part of the clean hex sweep |
| D-05 | 05.8-06, extended by WR-04 | Year Calendar visual grid + tap-to-add/edit vacation period | ✓ SATISFIED | Grid renders; WR-04 fix implements the previously-missing tap interaction |
| D-06 | 05.8-06 | Per-child sick-day overlay | ✓ SATISFIED | `sickChildrenFor`/dot rendering/popover present in `CalendarGrid.tsx` |
| D-07 | 05.8-02 | CalendarSettingsManager/PeriodsManager recolor | ✓ SATISFIED | Both files use `T` tokens; part of clean hex sweep |
| D-08 | 05.8-07, fixed by CR-02 + WR-01 | Weekly Summary card, real facts, error state | ✓ SATISFIED | Re-verified above — genuinely week-scoped, error banner reachable |
| D-09 | 05.8-02..05.8-09 | Full ink-theme recolor sweep | ✓ SATISFIED (see caveat) | Human-approved 2026-07-21; automated hex sweep clean for all plan-declared files; one pre-existing, out-of-declared-scope decimal-RGB remnant found in shared `ui.tsx` (see Anti-Patterns) — does not invalidate the sweep as scoped in `05.8-CONTEXT.md` |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `components/parent-center/ui.tsx` | 98, 100, 144, 146 | `Btn`/`Pill` `success`/`danger` variant borders/hovers hardcode `rgba(0,230,118,*)` / `rgba(255,107,107,*)` — the exact decimal-RGB equivalents of the banned `#00E676`/`#FF6B6B` neon hex (same class of bug WR-02 just fixed in `ChatPanel.tsx`), disconnected from the actual `T.success`(`#3FBF92`)/`T.danger`(`#FF8A96`) ink tokens used for the fill | ⚠️ Warning | Pre-existing (predates Phase 5.8 — `ui.tsx` is explicitly "unchanged by this phase" per WR-03's own finding text), and outside `05.8-CONTEXT.md`'s D-09 declared scope (which lists specific screens, not the shared `ui.tsx` atom file beyond the already-fixed cyan-hover line). Rendered today via `Btn variant="success/danger"` in `ChildrenTasksShop.tsx` (approve/decline purchase buttons) and `Pill tone="success/danger"` in `Analytics.tsx` — low visual impact (25%/18% alpha borders/hovers only, fills are already token-correct) but is a literal contradiction of SC1's "no neon-green remnants" if read outside the phase's declared file scope. Recommend a follow-up ticket to extend the decimal-RGB sweep (as WR-02's own fix note already recommended) to `ui.tsx`, matching the precedent just set in `ChatPanel.tsx`. Not treated as a phase-5.8 blocker since it is untouched debt outside this phase's own commit set. |

### Human Verification Required

See `human_verification` in frontmatter. Summary:

1. **D-02 schedule-link picker, new location** — the 05.8-09 human checkpoint approved D-02 against a location that turned out to be unreachable (the bug CR-01 fixed). The picker's actual working location (per-child tab in the byChild view) has never been clicked through by a human. Code-level wiring, dedup safety (WR-05 tests), and the write path are all verified — this is a "confirm it feels right in the browser" check, not a correctness gate.
2. **D-08 Weekly Summary card color/copy** — CR-02's fix is verified at the data/logic level (genuinely week-scoped, cap removed) but the actual rendered StatusChip color against a real family's week has not been visually re-confirmed since the fix (the 09 checkpoint predates it).

### Gaps Summary

No blocking gaps. All 9 code-review findings (CR-01, CR-02, WR-01 through WR-07) were traced end-to-end in the current codebase and confirmed fixed as described in `05.8-REVIEW-FIX.md` — no partial fixes, no regressions, `tsc`/tests/build all green. One new, low-severity, pre-existing, out-of-declared-scope anti-pattern was found during this re-verification (`ui.tsx` decimal-RGB neon remnants in the `Btn`/`Pill` success/danger palette) — disclosed above as a Warning/follow-up candidate, not a blocker. Status is `human_needed` solely because CR-01's fix relocated the D-02 picker to a spot the operator's prior browser checkpoint never actually exercised (it approved the old, broken location), and because CR-02's fix has not had a post-fix visual confirmation — both are recommended, not required, per this task's explicit escalation guidance.

---

_Verified: 2026-07-21T08:33:07Z_
_Verifier: Claude (gsd-verifier)_
