---
phase: 05.8-parent-redesign
plan: 03
subsystem: ui
tags: [react, nextjs, design-tokens, ink-theme, tailwind]

# Dependency graph
requires:
  - phase: 05.3-design-tokens
    provides: components/parent-center/tokens.ts (T ink-theme token object), lib/design/tokens.ts single source
provides:
  - Four Schedule-tab settings CRUD managers (Activities, Sections, Subjects, RoomTasks) fully recolored onto the ink `T` token system
  - Established category/day-type/coin-amount → T-token substitution mapping (no gold/amber token exists; warning-orange used instead) for future mechanical recolor plans in this phase
affects: [05.8-parent-redesign remaining recolor plans, any future audit of components/settings/*]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Category/semantic accent colors (blue/green/pink/orange/purple) collapse onto the 4-hue T token family: T.indigo (blue/purple), T.success (green), T.danger (pink/red), T.warning (orange/amber) — T has no discrete 5th hue, so 'other'-style categories reuse T.indigoHi as a lighter variant of indigo"
    - "Money/coin-amount display in config screens uses T.warning, never gold — T has no gold/goldText key by design (Parent Center is money-adjacent but not the money ledger itself)"
    - "Alpha-tier convention for translucent white text: 0.6ish alpha -> T.textDim, 0.35-0.45 alpha -> T.muted, 0.25-0.3 alpha -> T.faint"
    - "Alpha-tier convention for translucent white surfaces/borders: 0.02-0.05 alpha backgrounds -> T.card / T.cardHi (elevated), 0.06-0.08 alpha borders -> T.cardBorder, 0.09-0.10 alpha borders -> T.cardBorderHi"
    - "rgba(0,0,0,*) backdrop/shadow literals are NOT part of the recolor scope (established codebase-wide convention in ui.tsx/ActionModal.tsx/ExpensesPanel.tsx) — left untouched"
    - "Bottom-sheet modal panel background switched from ad-hoc gradient hex to T.bg1 (matches ActionModal.tsx / ExpensesPanel.tsx sheet convention)"
    - "Tailwind stock color classes (bg-gray-*, text-gray-*, text-red-*, text-yellow-*, text-green-*, bg-indigo-*, text-white) replaced with inline T-token styles; non-color layout/transition/opacity Tailwind classes (px-*, py-*, rounded-*, opacity-0, group-hover:opacity-100, transition-*) kept as-is"

key-files:
  created: []
  modified:
    - components/settings/ActivitiesManager.tsx
    - components/settings/SectionsManager.tsx
    - components/settings/SubjectsManager.tsx
    - components/settings/RoomTasksManager.tsx

key-decisions:
  - "Category chip colors (5 distinct hues in ActivitiesManager: academic/physical/creative/chore/other) mapped onto the 4-token T family with 'other' reusing T.indigoHi as a lighter-purple variant of T.indigo (academic) — some visual distinctiveness is reduced by design since T intentionally has no 5th hue"
  - "Coin/reward-amount badges (+N💰) recolored to T.warning instead of any gold-like hex, per plan must_have 'Gold never appears (no money figures in these managers)' — T has no gold token at all"
  - "SubjectsManager (Tailwind-based) dropped hover-shade color variants (e.g. hover:text-yellow-300) and input focus-ring/placeholder-color styling since inline style cannot express CSS pseudo-classes/pseudo-elements — pure visual simplification, no behavior change, consistent with PATTERNS.md precedent for PeriodsManager ('or drop hover-color change, keep muted→text')"

requirements-completed: [D-09]

# Metrics
duration: 12min
completed: 2026-07-21
---

# Phase 05.8 Plan 03: Recolor Schedule-tab CRUD Managers Summary

**Mechanically recolored the four highest-volume Schedule-tab settings managers (Activities, Sections, Subjects, RoomTasks) from raw rgba/hex/Tailwind-gray literals onto the shared ink `T` token object, with zero CRUD/structural changes.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-21T05:41:57+03:00
- **Completed:** 2026-07-21T05:53:20+03:00
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- All four managers import `T` from `@/components/parent-center/tokens` and render entirely on ink tokens
- Zero `rgba(255,255,255,*)` / `rgba(238,238,255,*)` / `#fff` / `#F43F5E` / `#34d399` / amber / `bg-gray-*` / `text-gray-*` remnants remain in any of the four files (verified via the plan's exact grep gate)
- Zero stray hex/rgba color literals remain at all outside the accepted `rgba(0,0,0,*)` shadow/backdrop convention (verified via a broader supplementary grep beyond the plan's gate)
- No gold/amber color renders on coin/reward figures — coin badges use `T.warning` (a distinct semantic orange token, not a gold hex)
- `npx tsc --noEmit` exits 0; full `npm test` suite green (71 passed, 26 skipped — unrelated pre-existing skips)

## Task Commits

Each task was committed atomically:

1. **Task 1: Recolor ActivitiesManager.tsx and SectionsManager.tsx** - `7305dd6` (style)
2. **Task 2: Recolor SubjectsManager.tsx and RoomTasksManager.tsx** - `e428625` (style)

**Plan metadata:** (this commit, docs: complete plan)

## Files Created/Modified
- `components/settings/ActivitiesManager.tsx` - Activities CRUD manager, recolored (highest literal count, ~44 instances: category chips, day-type badges, coin amounts, templates overlay, add/edit modal)
- `components/settings/SectionsManager.tsx` - Sports sections CRUD manager, recolored (child selector, tabs, add form, archive/delete actions)
- `components/settings/SubjectsManager.tsx` - Subjects CRUD manager, recolored (Tailwind-stock-class file, fewest literals: child selector, add input, active/inactive lists)
- `components/settings/RoomTasksManager.tsx` - Room tasks CRUD manager, recolored (emoji picker, reorder/toggle/delete row actions); `useAppStore()` familyId resolution and legacy-task delete guard confirmed untouched

## Decisions Made
See `key-decisions` in frontmatter above — category-color hue collapse onto the 4-token T family, coin-amount → T.warning (never gold), and Tailwind hover/focus-pseudo-class simplification in SubjectsManager.

## Deviations from Plan

None - plan executed exactly as written. The plan's substitution recipe explicitly covered the shared rgba(255,255,255,*)/rgba(238,238,255,*)/#fff/indigo/danger/success literal set; this plan additionally applied the same T-token discipline to each file's own category-specific accent colors (blue/green/pink/orange/purple chip colors, amber/gold coin and weekend-day accents) to satisfy the plan's broader must_have truths ("No cyan/neon-green/amber/hardcoded-rgba/hex remnants remain" and "Gold never appears") — this is the intended scope of "recolor only," not new functionality, and required no rule-4 architectural decision.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All four mounted Schedule-tab CRUD managers are now ink-token-compliant; remaining recolor targets in phase 05.8 (`CalendarSettingsManager.tsx`, `PeriodsManager.tsx`, `DayBlocksManager.tsx`, `ChatPanel.tsx`, `ui.tsx`) are unaffected by this plan and tracked in other 05.8 plans per `05.8-PATTERNS.md`
- No blockers for downstream 05.8 plans

---
*Phase: 05.8-parent-redesign*
*Completed: 2026-07-21*
