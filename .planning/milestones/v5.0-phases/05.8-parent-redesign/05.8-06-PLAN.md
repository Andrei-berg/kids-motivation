---
phase: 05.8-parent-redesign
plan: 06
type: execute
wave: 2
depends_on: [05.8-01, 05.8-02]
files_modified:
  - components/settings/CalendarGrid.tsx
  - components/parent-center/screens/Settings.tsx
autonomous: true
requirements: [D-01, D-05, D-06]

must_haves:
  truths:
    - "The Year Calendar sub-tab shows a visual month grid with day cells color-coded by day type"
    - "Month navigation (prev/next/today) works; today's cell has an indigo ring"
    - "A child filter switches between family-wide day types and a single-child view"
    - "In family-wide view, sick children appear as colored dots (from CHILD_ACCENTS) with a tap popover (D-06)"
    - "Day Constructor and Year Calendar stay nested at their current depth (Settings → Schedule → Blocks/Calendar/Vacations); no new top-level Parent Center nav item was added (D-01)"
    - "The recolored settings form and vacation list appear as collapsed accordions below the grid"
  artifacts:
    - path: "components/settings/CalendarGrid.tsx"
      provides: "visual month/year grid with day-type coloring + sick overlay"
      contains: "eachDayOfInterval"
    - path: "components/parent-center/screens/Settings.tsx"
      provides: "calendar sub-tab composition: grid + accordions"
      contains: "CalendarGrid"
  key_links:
    - from: "components/settings/CalendarGrid.tsx"
      to: "lib/repositories/calendar.repo.ts"
      via: "getDaysInRange for sick dots"
      pattern: "getDaysInRange"
    - from: "components/settings/CalendarGrid.tsx"
      to: "lib/day-type.ts"
      via: "getDayType for cell coloring"
      pattern: "getDayType"
---

<objective>
Build the D-05 visual month/year grid (net-new `CalendarGrid` component) with day cells color-coded by day type (school/weekend/vacation), Monday-first columns, today ring, month nav, and a child filter; add the D-06 per-child sick-day overlay (dots from CHILD_ACCENTS + tap popover). Compose the calendar sub-tab so the grid is the hero with the (already-recolored) settings form and vacation list as collapsed accordions below it.

Purpose: Turns the two functional-but-unstyled calendar editors into a real visual calendar surface, the phase's second screen upgrade.
Output: `CalendarGrid.tsx` + a restructured `calendar` sub-tab in Settings.tsx. Day-type logic reuses `getDayType()` unchanged; sick data comes from the Plan-01 `getDaysInRange`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05.8-parent-redesign/05.8-PATTERNS.md
@.planning/phases/05.8-parent-redesign/05.8-UI-SPEC.md
@.planning/phases/05.8-parent-redesign/05.8-01-SUMMARY.md
@.planning/phases/05.8-parent-redesign/05.8-02-SUMMARY.md

<interfaces>
lib/repositories/calendar.repo.ts — getFamilyCalendar(familyId), getDaysInRange(childId, startDate, endDate)  // getDaysInRange from Plan 01; returns days rows incl. is_sick, date
lib/vacation-api.ts — getVacationPeriods(familyId): Promise<VacationPeriod[]>  // period rows carry child_filter ('all' | childId)
lib/day-type.ts — getDayType(date, isSick, vacationPeriods, childId, t, familyCalendar) → day-type string; already handles child_filter matching and weekend_days fallback
date-fns (^3.0.6, existing dep): startOfMonth, endOfMonth, eachDayOfInterval, getDay, format, addMonths, subMonths
components/parent-center/ui.tsx — Icon (name="chevL"/"chevR"), Tabs (child filter row), Card, Btn (variant="ghost", size="sm")
components/parent-center/tokens.ts — T tokens + export const CHILD_ACCENTS = ['#8B7BF5','#3FBF92','#E88AA6','#5FB3E0','#C58BE0']
components/design/atoms.tsx — hexAlpha(hex, alpha) is PRIVATE (not exported); copy its 6-line impl into CalendarGrid if needed for cell tints
Child-id resolution: PeriodsManager.tsx:71-83 verbatim query (family_members id+child_id, role='child'); never getFamilyChildren()
Settings.tsx mount point (~lines 396-432): the `calendar` sub-tab currently renders `<CalendarSettingsManager/>`; the `vacations` sub-tab renders `<PeriodsManager/>`
i18n keys (Plan 01): settings.calendarSettingsManager.ariaPrevMonth/.ariaNextMonth/.today/.filterFamily/.sickOne ({{name}})/.sickMany
Noon-anchor date convention (avoid DST/tz shift): new Date(dateStr + 'T12:00:00') — reuse per lib/day-type.ts / PeriodsManager formatDateRange
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CalendarGrid.tsx — month grid + day-type coloring + nav + child filter</name>
  <files>components/settings/CalendarGrid.tsx</files>
  <read_first>
    - components/settings/PeriodsManager.tsx (date-math noon-anchor convention + child-id resolution query ~lines 71-83)
    - lib/day-type.ts getDayType signature + day-type return values
    - lib/repositories/calendar.repo.ts (getFamilyCalendar, getDaysInRange), lib/vacation-api.ts getVacationPeriods
    - components/parent-center/ui.tsx Icon/Tabs/Card/Btn, tokens.ts T + CHILD_ACCENTS
    - .planning/phases/05.8-parent-redesign/05.8-UI-SPEC.md Screen Contract Year Calendar D-05 (concrete layout) + color-mapping table + Typography (day-number 11px/600 JetBrains Mono)
  </read_first>
  <action>
    Create a new `'use client'` component `CalendarGrid` in components/settings/. State: current visible month (Date, default today's month), selected child filter ('family' | childId). Resolve children via the verbatim PeriodsManager family_members query. Load getFamilyCalendar(familyId) and getVacationPeriods(familyId) once; load getDaysInRange(childId, monthStart, monthEnd) per visible month for whichever children are needed for sick data (all children in family view, one child in single-child view). Build the grid with date-fns: startOfMonth/endOfMonth/eachDayOfInterval for the visible month, pad the leading/trailing cells to full Mon-first weeks. Map JS getDay() (Sun=0..Sat=6) to Mon-first column index via `(getDay(d) + 6) % 7` at RENDER time only — never change weekend_days/getDayType conventions. For each cell compute the day type via getDayType(dateStr, isSick, vacationPeriods, childId, t, familyCalendar) and color per the UI-SPEC table: school → T.card bg / T.text; weekend → T.bg1 bg / T.muted; vacation → hexAlpha(T.warning,0.14) bg / T.warning; single-child sick → hexAlpha(T.danger,0.14) bg / T.danger; today → add a 2px T.indigo inset ring on top of the fill. Day-number uses Label role 11px/600 with fontFamily T.fMono (JetBrains Mono) for tabular legibility. Weekday header row (Mon…Sun) uses Label role. Month nav: Icon chevL/chevR ghost icon-buttons (aria-labels ariaPrevMonth/ariaNextMonth) flanking a Bitter 17px/700 month-year label; a "Today" ghost Btn (i18n `today`) resets to the current month. Child-filter Tabs at top ("Family" = filterFamily copy, then one tab per child). Cell gap is 4px (xs) to keep cells ≥44px at 375px width. Apply the prefers-reduced-motion double-guard to any month-switch animation. Do NOT write to any table — this is read-only rendering (period editing is delegated to the accordions in Task 2).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "eachDayOfInterval" components/settings/CalendarGrid.tsx && grep -q "getDayType" components/settings/CalendarGrid.tsx && grep -q "getDaysInRange" components/settings/CalendarGrid.tsx && grep -q "(getDay" components/settings/CalendarGrid.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and verify prints `OK`
    - `grep -c "getFamilyChildren" components/settings/CalendarGrid.tsx` returns 0
    - `grep -c "fMono" components/settings/CalendarGrid.tsx` returns at least 1 (day-number JetBrains Mono per UI-SPEC)
    - `grep -viE '^\s*//' components/settings/CalendarGrid.tsx | grep -iE '#F0BE6A|goldText'` finds nothing (no gold on the calendar — day types are not money)
    - Column mapping uses `(getDay` + `+ 6) % 7` (Mon-first at render only)
  </acceptance_criteria>
  <done>CalendarGrid renders a Mon-first month grid, colors cells by day type, has working month nav + today + child filter, day-numbers in mono; read-only, no gold, no writes.</done>
</task>

<task type="auto">
  <name>Task 2: D-06 sick-day dot overlay + popover, and calendar sub-tab composition</name>
  <files>components/settings/CalendarGrid.tsx, components/parent-center/screens/Settings.tsx</files>
  <read_first>
    - components/settings/CalendarGrid.tsx (from Task 1)
    - .planning/phases/05.8-parent-redesign/05.8-UI-SPEC.md D-06 sick-day overlay concrete spec (dots 5px, 2px gap, bottom-right 4px inset, up to 3 + "+N", tap popover) + D-05 accordion composition
    - components/parent-center/screens/Settings.tsx (calendar/vacations sub-tab switch ~lines 396-432; already on T tokens)
    - components/parent-center/ui.tsx Card (for the popover chrome)
  </read_first>
  <action>
    In CalendarGrid, when the filter is family-wide, render up to 3 sick dots (5px diameter, 2px gap, bottom-right corner, 4px inset) per cell — one per child marked sick that date (is_sick true in that child's getDaysInRange rows), colored from CHILD_ACCENTS in the same child order used elsewhere; if >3 children sick that day, render 2 dots + a `+N` 8px mono label. Tapping a cell with ≥1 sick dot opens a small Card-styled popover (T.cardHi bg) anchored to the cell listing sick child name(s) via `sickOne` ({{name}}) for one child or the `sickMany` heading for 2+. In single-child filtered view, sick is the full-cell danger tint from Task 1 (no dots). This overlay is READ-ONLY — no write path; the is_sick flag is still set from the day-fill form. In Settings.tsx, restructure the `calendar` sub-tab to render `<CalendarGrid/>` as the hero on top, then the existing `<CalendarSettingsManager/>` form and `<PeriodsManager/>` list as two collapsed accordion sections below it (default collapsed; a simple T-token disclosure toggle). Keep the `vacations` sub-tab as-is (PeriodsManager standalone) OR fold it — either is valid under D-01; do NOT add any new top-level nav item or sub-tab id. Do not modify CalendarSettingsManager.tsx or PeriodsManager.tsx themselves (they are recolored in Plan 02 and only composed here).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "CalendarGrid" components/parent-center/screens/Settings.tsx && grep -q "CHILD_ACCENTS" components/settings/CalendarGrid.tsx && grep -q "is_sick" components/settings/CalendarGrid.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and verify prints `OK`
    - `grep -c "CHILD_ACCENTS" components/settings/CalendarGrid.tsx` returns at least 1 (dots use accents, not a new color)
    - Settings.tsx `calendar` sub-tab mounts CalendarGrid + both existing managers as accordions (git diff shows no new sub-tab id added, no new nav item)
    - `git status` shows CalendarSettingsManager.tsx and PeriodsManager.tsx are NOT modified by this plan
    - No write call (insert/update/delete) is added in CalendarGrid.tsx for is_sick (read-only overlay)
  </acceptance_criteria>
  <done>Sick dots + tap popover render in family view; calendar sub-tab shows grid hero + collapsed accordions; no new nav depth; overlay read-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (browser client, RLS) | CalendarGrid reads days/family_calendar/vacation_periods under existing family-isolation RLS; renders only, no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-058-11 | Information Disclosure | getDaysInRange sick data | mitigate | Reads `days` (incl. is_sick) via the browser client under existing RLS; childId comes from the family-scoped family_members query — sick data cannot cross families |
| T-058-12 | Tampering | Settings.tsx composition | accept | Rearranges mount order only; no data-layer change; CalendarSettingsManager/PeriodsManager write paths untouched |
| T-058-13 | Spoofing/EoP | sick overlay | accept | Read-only overlay; no write path, no is_sick mutation from the grid |

No new endpoint, no money-write, no free-text field (child filter is a scoped picker; popover renders display_name from the RLS-scoped query). Existing RLS is the boundary.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- CalendarGrid uses getDayType + getDaysInRange + Mon-first render mapping; no getFamilyChildren; no gold
- Settings.tsx calendar sub-tab renders grid + accordions with no new nav depth
- `npm test` still green
</verification>

<success_criteria>
- Working visual month grid with day-type coloring, month nav, today ring, child filter
- Per-child sick dots + tap popover in family view; single-child sick tint
- Recolored form + vacation list as collapsed accordions below the grid; no new nav item
</success_criteria>

<output>
Create `.planning/phases/05.8-parent-redesign/05.8-06-SUMMARY.md` when done
</output>
