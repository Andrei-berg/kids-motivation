---
phase: 05.8-parent-redesign
plan: 07
type: execute
wave: 2
depends_on: [05.8-01]
files_modified:
  - components/parent-center/ParentCenter.tsx
  - components/parent-center/screens/Analytics.tsx
autonomous: true
requirements: [D-08]

must_haves:
  truths:
    - "Analytics shows a 'This week' card above the KPI grid with three real facts"
    - "Coins earned sums only positive wallet_transactions in the week window (not getWeekScore)"
    - "Completion rate shows as a success/warning/danger StatusChip at 80/50 thresholds"
    - "A streak highlight shows the highest child on a >=5-day streak, or a 'no streak yet' fallback"
    - "The card has empty and load-error states, never a blank gap"
  artifacts:
    - path: "components/parent-center/screens/Analytics.tsx"
      provides: "WeeklySummaryCard rendered above the KPI grid"
      contains: "weeklySummary"
    - path: "components/parent-center/ParentCenter.tsx"
      provides: "week-scoped tx fetch + weekly-summary props computed in loadAll"
      contains: "sumWeeklyCoins"
  key_links:
    - from: "components/parent-center/ParentCenter.tsx"
      to: "lib/weekly-summary.ts"
      via: "sumWeeklyCoins / completionTone / topStreak"
      pattern: "sumWeeklyCoins"
    - from: "components/parent-center/ParentCenter.tsx"
      to: "lib/repositories/wallet.repo.ts"
      via: "getTransactions(undefined, 500, weekStartISO)"
      pattern: "getTransactions\\(undefined, 500"
---

<objective>
Add the D-08 Weekly Summary card to Analytics: a single real "This week" card (coins earned / tasks done / streak highlight) computed from already-available aggregates, using the correct money source (positive `wallet_transactions` summed over the week window via the Plan-01 `sumWeeklyCoins` helper — NOT the stale `getWeekScore.total`). Computed in ParentCenter's existing loadAll batch and passed down as props, matching the established data-flow.

Purpose: The one truly-real content block on an Analytics screen whose charts below stay mocked; closes the "Weekly Summary card (static text for now)" success criterion.
Output: A `WeeklySummaryCard` in Analytics.tsx fed by three new props, with empty/error states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05.8-parent-redesign/05.8-PATTERNS.md
@.planning/phases/05.8-parent-redesign/05.8-UI-SPEC.md
@.planning/phases/05.8-parent-redesign/05.8-01-SUMMARY.md

<interfaces>
lib/weekly-summary.ts (Plan 01) — sumWeeklyCoins(txs: {coins_change:number}[]): number; completionTone(pct): 'success'|'warning'|'danger'; topStreak(entries: {name;current_count}[], threshold=5): {name;days}|null
lib/repositories/wallet.repo.ts — getTransactions(childId?, limit=50, since?): Promise<WalletTransaction[]>  (coins_change field)
utils/helpers.ts — getWeekRange(new Date()).start (YYYY-MM-DD), localDateString()
components/parent-center/ParentCenter.tsx — existing loadAll() Promise.all batch (~lines 106-123): const [pendingData, txData, readingData] = await Promise.all([...]); per-child batch (~117-125) already calls getStreaks(c.id) and reduces maxStreak; AnalyticsScreen mount ~line 300
components/parent-center/screens/Analytics.tsx — existing KPI-card grid (~lines 139-150); children-overview Pill thresholds (>=80 success / >=50 warning) ~line 210; wrapper div ~line 122
components/design/atoms.tsx — Amount({ value, theme, money, size }) theme="ink" money size="lg" for the gold coin figure; StatusChip({ tone, theme, children }) for the rate
components/parent-center/tokens.ts — T (T.warning for streak text, T.muted for fallback, T.dangerSoft/T.danger for error banner). NO gold key — gold only via Amount.
i18n keys (Plan 01): analytics.weeklySummary.title/.coinsEarned/.tasksDone/.streakHighlight ({{name}}/{{days}})/.noStreak/.empty/.loadError
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute weekly-summary facts in ParentCenter loadAll and pass as props</name>
  <files>components/parent-center/ParentCenter.tsx</files>
  <read_first>
    - components/parent-center/ParentCenter.tsx (loadAll Promise.all batch ~106-123, per-child getStreaks/maxStreak ~117-125, AnalyticsScreen mount ~300, existing getWeekRange/localDateString imports)
    - lib/weekly-summary.ts (Plan 01 helpers) and lib/repositories/wallet.repo.ts getTransactions
    - .planning/phases/05.8-parent-redesign/05.8-PATTERNS.md WeeklySummaryCard data-source section (correct query, compute-in-ParentCenter locked)
  </read_first>
  <action>
    In loadAll(), compute `const weekStartISO = new Date(getWeekRange(new Date()).start + 'T00:00:00').toISOString()`. Add a fourth entry to the existing top-level Promise.all: `getTransactions(undefined, 500, weekStartISO).catch(() => [])` (limit 500, NOT the 30-row activity-feed cap — separate call). Compute `coinsThisWeek = sumWeeklyCoins(weekTxData)`. Compute a family-wide completion rate from the already-loaded per-child day-fill / today-% data (reuse whatever today/completion metric the per-child batch already produces; average across children into a 0-100 `taskRate`), and derive its tone with `completionTone(taskRate)`. Compute `streakHighlight = topStreak(children.map(c => ({ name: c.name, current_count: c.maxStreak })), 5)` using the maxStreak already reduced per child. Pass `coinsThisWeek`, `taskRate`, and `streakHighlight` as three new props into `<AnalyticsScreen .../>`. Wrap the week-tx fetch in the existing catch pattern so a failure yields an error flag prop (`weeklyError: true`) rather than throwing loadAll. Import sumWeeklyCoins/completionTone/topStreak from '@/lib/weekly-summary'.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "getTransactions(undefined, 500" components/parent-center/ParentCenter.tsx && grep -q "sumWeeklyCoins" components/parent-center/ParentCenter.tsx && grep -q "topStreak" components/parent-center/ParentCenter.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and verify prints `OK`
    - `grep -c "getWeekScore" components/parent-center/ParentCenter.tsx` shows getWeekScore is NOT newly used for the coin figure (the week-tx path uses getTransactions + sumWeeklyCoins)
    - The new getTransactions call passes limit 500 and the week-start `since` (grep confirms `getTransactions(undefined, 500`)
    - Three new props (coinsThisWeek, taskRate, streakHighlight) are passed to AnalyticsScreen
  </acceptance_criteria>
  <done>ParentCenter computes the three facts from correct sources in the existing batch and passes them to Analytics; failure sets an error flag, not a throw.</done>
</task>

<task type="auto">
  <name>Task 2: Render WeeklySummaryCard above the KPI grid in Analytics</name>
  <files>components/parent-center/screens/Analytics.tsx</files>
  <read_first>
    - components/parent-center/screens/Analytics.tsx (KPI-card grid ~139-150, wrapper div ~122, existing Pill threshold usage ~210, prop type / AnalyticsScreen props interface)
    - components/design/atoms.tsx Amount + StatusChip signatures
    - .planning/phases/05.8-parent-redesign/05.8-UI-SPEC.md Screen Contract Weekly Summary card (layout, Amount size lg gold, StatusChip rate, T.warning streak, empty/error states)
  </read_first>
  <action>
    Extend AnalyticsScreen's props to accept `coinsThisWeek: number`, `taskRate: number`, `streakHighlight: {name:string;days:number}|null`, `weeklyError?: boolean`. Add a new `Card pad={16}` placed ABOVE the existing KPI grid inside the same wrapper div. Heading `t('analytics.weeklySummary.title')` (Heading role 17px/700). Below it a 3-column flex row (wraps to 1 column under ~360px), each fact a stat block with a Label-role caption + value: Fact 1 coins earned — caption `coinsEarned`, value via `Amount value={coinsThisWeek} theme="ink" money={true} size="lg"` (gold, the card hero); Fact 2 tasks done — caption `tasksDone`, value a `StatusChip theme="ink" tone={completionTone-derived tone}` reading e.g. "82%"; Fact 3 streak — caption from the streakHighlight: if non-null show `streakHighlight` copy ({{name}}/{{days}}) in T.warning color, else show `noStreak` copy in T.muted. Card-level empty state: if coinsThisWeek===0 AND taskRate===0 (first-week case), render `weeklySummary.empty` copy centered instead of the 3-column layout. Card-level error state: if `weeklyError`, render `weeklySummary.loadError` in T.dangerSoft/T.danger (same error-banner language as the managers) in place of the 3 facts. Do NOT touch the existing mocked BarChart/LineChart below — out of scope.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "weeklySummary.title" components/parent-center/screens/Analytics.tsx && grep -q "coinsThisWeek" components/parent-center/screens/Analytics.tsx && grep -q "money" components/parent-center/screens/Analytics.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and verify prints `OK`
    - The coin figure renders via `Amount` with `money` + `theme="ink"` + `size="lg"` (grep shows Amount with money and size lg near coinsThisWeek)
    - `grep -viE '^\s*//' components/parent-center/screens/Analytics.tsx | grep -iE '#F0BE6A|goldText'` finds nothing (no hand-rolled gold; gold only through Amount)
    - `grep -c "T.warning" components/parent-center/screens/Analytics.tsx` returns at least 1 (streak in warning, not gold/success)
    - The existing BarChart/LineChart mock code is unchanged (git diff shows only the new card added above the KPI grid)
  </acceptance_criteria>
  <done>WeeklySummaryCard renders three facts (gold coins, chip rate, warning streak) with empty + error states above the KPI grid; mocked charts untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (browser client, RLS) | Weekly card reads wallet_transactions/streaks/days directly — money tables are SELECT-only for clients (exactly what's needed), scoped by existing family RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-058-14 | Information Disclosure | week-scoped getTransactions read | mitigate | Reads wallet_transactions via the browser client under existing money-table SELECT RLS (family-scoped); no cross-family leak, no write |
| T-058-15 | Tampering | coin figure source | mitigate | Sums positive coins_change from wallet_transactions (the money source of truth), NOT the hardcoded-math getWeekScore — matches CLAUDE.md "do not hardcode coin values" |
| T-058-16 | Denial of Service | week-tx fetch failure | accept | Wrapped in .catch → weeklyError flag → loadError copy; never blocks loadAll or shows a blank gap |

No new write path, no money mutation (read-only summary), no new free-text field. Existing SELECT-only money-table RLS is the boundary.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- Coin figure uses sumWeeklyCoins over getTransactions(undefined,500,weekStartISO); no getWeekScore for the money figure
- Card renders three facts + empty + error states; no hand-rolled gold; mocked charts untouched
- `npm test` still green (Plan 01 helper tests cover the coin-sum math)
</verification>

<success_criteria>
- "This week" card above the KPI grid with coins (gold Amount), rate (chip), streak (warning)
- Correct money source; empty + load-error states present
- Mocked bar/line charts unchanged
</success_criteria>

<output>
Create `.planning/phases/05.8-parent-redesign/05.8-07-SUMMARY.md` when done
</output>
