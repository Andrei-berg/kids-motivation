# Phase 5.8: parent-redesign - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-20
**Phase:** 05.8-parent-redesign
**Areas discussed:** IA placement, Day Constructor depth, Year Calendar visual, Weekly Summary content, Schedule link, Override UI, Calendar scope

---

## IA placement

| Option | Description | Selected |
|--------|-------------|----------|
| Keep nested in Settings → Schedule | Redesign the existing sub-tabs onto the ink token system in place — same navigation depth, just properly styled. | ✓ |
| Promote to dedicated top-level screens | Give Day Constructor and Year Calendar their own Parent Center nav entries. | |

**User's choice:** Keep nested in Settings → Schedule.
**Notes:** Parent Center bottom nav already has 7 items + a separate settings button; avoided further nav clutter.

---

## Day Constructor depth

| Option | Description | Selected |
|--------|-------------|----------|
| Restyle only — family-wide blocks | Same editing scope as today, just moved onto the ink design system. | |
| Add per-child overrides | Add a child selector so a parent can override price/day-types/multipliers for one specific kid. | |
| Add per-child overrides + wire schedule_link | Also let a block attach to a schedule entry so it auto-appears only on scheduled days. | ✓ |

**User's choice:** Add per-child overrides + wire schedule_link (the deepest option).
**Notes:** schedule_link was explicitly reserved by 5.6 for this phase.

---

## Schedule link

| Option | Description | Selected |
|--------|-------------|----------|
| Link to schedule_items | Block picker shows the child's existing lessons/sections/routines; picking one makes the block auto-appear only on those weekdays. | ✓ |
| Freeform days_of_week only | Skip wiring schedule_link this phase; keep using day_types + days_of_week set directly by the parent. | |

**User's choice:** Link to schedule_items.

---

## Override UI

| Option | Description | Selected |
|--------|-------------|----------|
| Child tabs/selector at the top | A child picker above the block list; picking a kid shows effective blocks (family default + overrides highlighted). | Build both |
| Inline per-block "override for…" expander | Each block row gets an expandable section listing all children with per-child override fields inline. | Build both |

**User's choice:** Free-text — "как говорил архитектор из антропика делаем все варианты и лучшее выбираем" (build all variants, pick the best).
**Notes:** Mirrors the 5.7 D-11 chat-theme precedent (build both, decide at verification). Captured in CONTEXT.md D-03.

---

## Year Calendar visual

| Option | Description | Selected |
|--------|-------------|----------|
| Restyle the two existing editors | Keep the settings-form + period-list structure, just reskin both. | |
| Build an actual calendar/month-grid view | A real visual year/month grid showing school/weekend/vacation/sick days color-coded. | ✓ |

**User's choice:** Build an actual calendar/month-grid view.

---

## Calendar scope

| Option | Description | Selected |
|--------|-------------|----------|
| Family-wide grid only | Grid shows day type only, same for every child; sick days stay in the per-child day-fill flow. | |
| Family grid + per-child sick overlay | Add a way to see which child(ren) were marked sick on a given day directly on the grid. | ✓ |

**User's choice:** Family grid + per-child sick overlay.

---

## Weekly Summary content

| Option | Description | Selected |
|--------|-------------|----------|
| Coins earned this week (family total) | Sum of positive wallet_transactions this week across all children. | ✓ |
| Task/day completion rate | % of expected day-fills actually completed this week. | ✓ |
| Streak highlights | Call out any child on a notable streak (≥5 days). | ✓ |

**User's choice:** All three (multiSelect) — coins earned, completion rate, and streak highlights all appear on the card.

---

## Claude's Discretion

- Exact child-selector vs inline-expander visual polish; whether both ship behind a toggle or one is dropped after comparison.
- Exact multi-child sick-day cell design (dots, stacked avatars, tooltip-on-tap).
- Weekly Summary card layout/copy order within family-bank writing rules.
- Whether Analytics' existing mocked bar/line charts get touched (default: no).
- Order of recolor work across the full screen list; atom adoption beyond pure recolor is a bonus, not required for SC1.

## Deferred Ideas

- Rebuilding Analytics' mocked charts with real data — future phase, not 5.8.
- Legacy pages recolor/purge, globals.css cleanup, FamilyCoins app icon — Phase 5.11.
- Rule presets, grade_scale per family, configurable behavior tags — Phase 5.9.
- Scheduled allowance, auto-approve under trust limit — Phase 5.10.
