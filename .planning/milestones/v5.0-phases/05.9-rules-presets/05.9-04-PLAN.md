---
phase: 05.9-rules-presets
plan: 04
type: execute
wave: 3
depends_on: [05.9-02, 05.9-03]
files_modified:
  - app/api/wallet/_lib.ts
  - app/api/wallet/settings/route.ts
  - lib/repositories/wallet.repo.ts
autonomous: true
requirements: [05.9-SC2]
must_haves:
  truths:
    - "loadSettings (server) returns a grade_coin_map, deriving it from coins_per_grade_1..5 when the column is null (read-time fallback, no backfill)"
    - "getWalletSettings (client) returns the same fallback-derived grade_coin_map"
    - "A PATCH with a malformed grade_coin_map (non-numeric / out-of-bounds / non-object) is rejected/dropped before upsert"
  artifacts:
    - path: "app/api/wallet/_lib.ts"
      provides: "SETTINGS_DEFAULTS.grade_scale + loadSettings grade_coin_map fallback"
      contains: "grade_coin_map"
    - path: "app/api/wallet/settings/route.ts"
      provides: "clampGradeCoinMap validator"
      contains: "clampGradeCoinMap"
  key_links:
    - from: "loadSettings"
      to: "grade_coin_map"
      via: "fallback from coins_per_grade_1..5"
      pattern: "coins_per_grade_5"
---

<objective>
Make `grade_coin_map` readable everywhere (server + client, with a read-time fallback from the legacy five-point columns) and writable safely (a defense-in-depth validator on the settings PATCH). RESOLVED Open Question 4: read-time fallback in BOTH code paths, NO migration backfill — simpler, always-correct for existing and brand-new families.

Purpose: The award route (plan 06) and every settings UI (plans 08/09) rely on grade_coin_map being present and trustworthy.
Output: Extended _lib defaults+loadSettings, a settings-route validator, a client-repo fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.9-rules-presets/05.9-RESEARCH.md
@.planning/phases/05.9-rules-presets/05.9-PATTERNS.md
@.planning/phases/05.9-rules-presets/05.9-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SETTINGS_DEFAULTS + loadSettings grade_coin_map fallback (server) and getWalletSettings fallback (client)</name>
  <files>app/api/wallet/_lib.ts, lib/repositories/wallet.repo.ts</files>
  <read_first>
    - app/api/wallet/_lib.ts lines 36-76 (SETTINGS_DEFAULTS + loadSettings — merge shape)
    - lib/repositories/wallet.repo.ts lines 177-240 (getWalletSettings — the client read path that must gain the SAME fallback)
    - .planning/phases/05.9-rules-presets/05.9-RESEARCH.md §"Code Examples" (loadSettings fallback draft) and §"Open Questions" Q4 (why both paths need it)
  </read_first>
  <action>
    _lib.ts: add `grade_scale: 'five_point'` to SETTINGS_DEFAULTS (typed as string in the existing `Record<string, number | string>`). In `loadSettings`, after building `merged`, if `merged.grade_coin_map` is null/undefined, set it to `{ '5': merged.coins_per_grade_5, '4': merged.coins_per_grade_4, '3': merged.coins_per_grade_3, '2': merged.coins_per_grade_2, '1': merged.coins_per_grade_1 }`. This makes grade_coin_map always present for the award route regardless of whether a family ever populated it. Note grade_coin_map is JSONB, not a scalar — do NOT give it a numeric default in SETTINGS_DEFAULTS.
    wallet.repo.ts: apply the SAME fallback in `getWalletSettings()` so the client-side previews/editors read an identical map — after fetching the row, if grade_coin_map is absent, derive it from coins_per_grade_1..5 the same way. If getWalletSettings uses a defaults-merge helper, add the fallback there; otherwise inline it before returning. Import `defaultGradeCoinMap`/`GradeScale` from lib/presets.ts only if convenient — the five-point fallback here is specifically the family's OWN customized coins_per_grade values, not the generic scale default, so derive from the row's columns (matching the server).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "grade_coin_map" app/api/wallet/_lib.ts && grep -q "grade_coin_map" lib/repositories/wallet.repo.ts</automated>
  </verify>
  <done>Both server loadSettings and client getWalletSettings return a grade_coin_map, falling back to the family's coins_per_grade_1..5 when the column is null; grade_scale default present; tsc clean.</done>
  <acceptance_criteria>
    - loadSettings sets grade_coin_map from coins_per_grade_5..1 when absent (grep for the derivation)
    - getWalletSettings has the equivalent fallback (grep for grade_coin_map in wallet.repo.ts)
    - No migration/backfill UPDATE was added anywhere (resolved OQ4 — fallback only)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: clampGradeCoinMap validator on the settings PATCH route</name>
  <files>app/api/wallet/settings/route.ts</files>
  <read_first>
    - app/api/wallet/settings/route.ts (full — clampStreakFields at lines 16-35, its call site at line 44-46, the `{ id, family_id, updated_at, ...safe }` discipline)
    - .planning/phases/05.9-rules-presets/05.9-RESEARCH.md §"Pitfall 2" (the ...safe spread has no per-field validation; JSONB introduces shape risk) and §"Security Domain" T-2
  </read_first>
  <action>
    Add `clampGradeCoinMap(safe: Record<string, unknown>): void` mirroring clampStreakFields' defense-in-depth idiom: if `'grade_coin_map'` is not in safe, return. If `safe.grade_coin_map` is not a plain non-array object, `delete safe.grade_coin_map` and return (fall back to server default rather than storing garbage). Otherwise iterate its entries: coerce each value with `Number()`; if any value is NaN or non-finite, delete the whole `grade_coin_map` field (reject the entire malformed map, don't store a partial). Clamp each finite value to `[-100000, 100000]` (matching the streak-bonus bound) and floor it. Optionally also validate keys against the request's `grade_scale` value set (from lib/presets.ts GRADE_SCALE_VALUES) IF `safe.grade_scale` is present — drop keys not in the scale's value list; this is best-effort, the award route's `?? 0` lookup is the authoritative backstop. Call `clampGradeCoinMap(safe)` immediately after the existing `clampStreakFields(safe)` call. Also allow `grade_scale` through the ...safe spread unchanged (it is already permitted since only id/family_id/updated_at are stripped) — no change needed there beyond confirming it isn't accidentally clamped.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "clampGradeCoinMap" app/api/wallet/settings/route.ts</automated>
  </verify>
  <done>clampGradeCoinMap validates/bounds/rejects a malformed grade_coin_map before the upsert; called after clampStreakFields; tsc clean.</done>
  <acceptance_criteria>
    - clampGradeCoinMap is defined and invoked in the PATCH handler after clampStreakFields
    - A non-object grade_coin_map results in the field being deleted (source-readable branch)
    - Value bound is `[-100000, 100000]` (matches streak-bonus clamp)
    - `id`/`family_id` are still never trusted from the client body (existing discipline preserved)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent client → PATCH /api/wallet/settings | untrusted body; grade_coin_map JSONB is a new shape-injection surface |
| DB → award route | grade_coin_map read authoritatively server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-059-02 | Tampering | grade_coin_map PATCH body | mitigate | clampGradeCoinMap: plain-object check, finite-number coercion, `[-100000,100000]` clamp, whole-map rejection on any invalid value — mirrors clampStreakFields defense-in-depth. |
| T-059-08 | Information Disclosure | loadSettings fallback | accept | Fallback derives from the family's own coins_per_grade columns only; no cross-family data. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- grade_coin_map present from both read paths; validator present on the write path.
</verification>

<success_criteria>
grade_coin_map is always readable (fallback-derived) and always safe to write (validated) — the award route and settings UIs can depend on it.
</success_criteria>

<output>
Create `.planning/phases/05.9-rules-presets/05.9-04-SUMMARY.md` when done.
</output>
