---
phase: 05.9-rules-presets
plan: 07
type: execute
wave: 5
depends_on: [05.9-06]
files_modified:
  - app/parent/behavior/actions.ts
  - components/parent-center/screens/BehaviorApprovalQueue.tsx
  - components/DailyModal.tsx
  - tests/integration/behavior-approval.test.ts
autonomous: true
requirements: [05.9-SC3]
must_haves:
  truths:
    - "Only a parent can approve/reject a pending behavior mark (child self-approval is refused server-side)"
    - "Approval flips status to 'approved' but moves no coins itself — the next /api/wallet/award POST credits it"
    - "A dedicated parent approval queue lists pending child-proposed marks; approve/reject act on one row each — behavior tags support a child-propose/parent-approve flow via a status column (pending/approved/rejected), mirroring the shop purchase-request pattern (D-12)"
    - "A parent applying a tag directly in DailyModal takes effect immediately (not gated by approval)"
    - "Saving DailyModal DUAL-WRITES days.good_behavior in sync with the seeded default 'good_behavior' tag (D-09) — the legacy column stays live so Plan 06's zero-marks fallback and the coins.service/streaks.service legacy readers stay correct for the default tag"
  artifacts:
    - path: "app/parent/behavior/actions.ts"
      provides: "approveBehaviorMark + rejectBehaviorMark + applyTagDirect (requireParent, service-role, status-only)"
      exports: ["approveBehaviorMark", "rejectBehaviorMark", "applyTagDirect"]
    - path: "components/parent-center/screens/BehaviorApprovalQueue.tsx"
      provides: "dedicated pending-mark review surface"
  key_links:
    - from: "approveBehaviorMark"
      to: "behavior_marks.status"
      via: "service-role update pending→approved, no coin mutation"
      pattern: "status.*approved"
    - from: "DailyModal handleSave"
      to: "days.good_behavior (legacy dual-write) + default-tag approved mark"
      via: "api.saveDay goodBehavior param kept in sync with the seeded default tag"
      pattern: "good_behavior|goodBehavior"
---

<objective>
Build the money-adjacent approval layer for behavior tags. RESOLVED Open Question 3 (parent review placement): a DEDICATED approval queue surface (`BehaviorApprovalQueue.tsx`) mirroring the shop purchase-approval mental model — the UI-SPEC's "approval queue" copy contract leans this way over folding into DailyModal. Approval flips status only (service-role, requireParent); crediting happens on the next award POST (plan 06's summation). Parent-direct tag application in DailyModal is immediate and ungated (D-12 last bullet). DailyModal also DUAL-WRITES `days.good_behavior` in sync with the seeded default tag (D-09, mirroring 05.2-05's legacy_key dual-write) so the legacy fallback and legacy readers stay correct.

Purpose: The propose→approve→credit boundary, guarded so a child cannot self-approve, plus the D-09 legacy dual-write on the parent day-fill form.
Output: Server actions + queue component + DailyModal behavior section (tags + legacy dual-write) + approval integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.9-rules-presets/05.9-PATTERNS.md
@.planning/phases/05.9-rules-presets/05.9-UI-SPEC.md
@.planning/phases/05.9-rules-presets/05.9-05-SUMMARY.md
@.planning/phases/05.9-rules-presets/05.9-06-SUMMARY.md
@.planning/phases/05.2-room-tasks/05.2-05-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: app/parent/behavior/actions.ts — approveBehaviorMark / rejectBehaviorMark / applyTagDirect</name>
  <files>app/parent/behavior/actions.ts</files>
  <read_first>
    - app/parent/shop/actions.ts (full — the exact analog: `'use server'`, requireParent(), createAdminClient(), fetch-by-id scoped to member.familyId, status==='pending' guard, status transition, best-effort insertAuditEvent + notifyChild)
    - lib/supabase/admin.ts (requireParent, createAdminClient, Membership, AuthError)
    - .planning/phases/05.9-rules-presets/05.9-PATTERNS.md §"app/parent/behavior/actions.ts" (the deliberate divergence: approval moves NO coins — only flips status; do NOT port adjustCoins/applyWalletDelta)
  </read_first>
  <action>
    Create `'use server'` file. `approveBehaviorMark(markId: string): Promise<BehaviorMark>` — `const member = await requireParent()` (child role throws AuthError 403 — a child CANNOT self-approve, threat T-059-01), `createAdminClient()`, fetch the mark by id scoped to `.eq('family_id', member.familyId)`, throw if not found, throw if `status !== 'pending'`, then UPDATE `status='approved'`, `decided_by = member.userId`, `decided_at = now()`. Do NOT move coins here — deliberately divergent from shop's adjustCoins; the next /api/wallet/award POST credits the mark via plan 06's approved-sum (idempotent per mark id). Best-effort side effects (try/catch, fire-and-forget): `void insertAuditEvent({...action_type appropriate...})` and `notifyChild(...)` announcing the approval.
    `rejectBehaviorMark(markId: string, note?: string): Promise<BehaviorMark>` — same guard/fetch/pending-check, UPDATE `status='rejected'`, decided_by/decided_at. No coins to refund (nothing was credited while pending) — simpler than rejectPurchaseAction. Best-effort notify child.
    `applyTagDirect(childId: string, date: string, tagId: string): Promise<BehaviorMark>` — the parent-direct (ungated) path used by DailyModal: `requireParent()`, `createAdminClient()`, assert the child + tag belong to member.familyId, then INSERT a behavior_marks row with `status='approved'`, `proposed_by = decided_by = member.userId`, `decided_at = now()`. Runs service-role so it legitimately bypasses the client 'pending'-only RLS WITH CHECK (threat T-059-03). This action is how DailyModal creates an approved mark for the seeded default tag (the D-09 dual-write, Task 2) and for any parent-direct custom tag.
    Never trust a client-supplied approver/status field — derive everything from requireParent() + the DB row.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "requireParent" app/parent/behavior/actions.ts && grep -q "'approved'" app/parent/behavior/actions.ts && grep -q "applyTagDirect" app/parent/behavior/actions.ts && ! grep -q "adjustCoins\|applyWalletDelta\|wallet_apply" app/parent/behavior/actions.ts</automated>
  </verify>
  <done>approve/reject are parent-guarded, service-role, status-only transitions with a pending precondition and best-effort notify/audit; applyTagDirect inserts an approved mark service-role; no coin mutation inside any action; tsc clean.</done>
  <acceptance_criteria>
    - approve/reject/applyTagDirect all call requireParent() (child role → AuthError 403)
    - approve/reject fetch scoped to member.familyId and assert status==='pending' before transition
    - applyTagDirect inserts status='approved' service-role, scoped to member.familyId
    - No adjustCoins/applyWalletDelta/wallet_apply anywhere (crediting is the award route's job)
    - decided_by/decided_at set from server-derived values, never client body
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: BehaviorApprovalQueue.tsx (dedicated queue) + DailyModal behavior section (tags + D-09 good_behavior dual-write)</name>
  <files>components/parent-center/screens/BehaviorApprovalQueue.tsx, components/DailyModal.tsx</files>
  <read_first>
    - app/kid/(app)/shop/page.tsx (the pending-status list mental model + StatusChip tone="warning" pattern the UI-SPEC references at line 215)
    - components/DailyModal.tsx (goodBehavior state default true ~line 154; pre-fill from dayData ~line 296; api.saveDay goodBehavior param ~line 539; behaviorCoins preview ~lines 666-667; behavior checkbox render ~lines 1060-1067; gradeCoinsFor ~line 672; day-block behavior switch ~line 1157)
    - .planning/phases/05.2-room-tasks/05.2-05-PLAN.md Task 1 (THE legacy_key dual-write precedent: derive the legacy days.* column from the new-model state on save; custom entries skip the legacy column)
    - .planning/phases/05.9-rules-presets/05.9-UI-SPEC.md §"Copywriting Contract" (approval queue empty state + "Approve tag"/"Reject tag" + Btn variants; ink theme) and §"Color" (warning=pending only)
    - .planning/phases/05.9-rules-presets/05.9-05-SUMMARY.md (behavior.repo exports: getBehaviorTags, getBehaviorMarks; and behavior.types)
  </read_first>
  <action>
    BehaviorApprovalQueue.tsx (NEW, ink theme, `'use client'`): list PENDING child-proposed marks across the family (fetch pending marks + join tag name/icon/price; resolve child name). One row per mark showing child name, tag icon+name, and the tag's coin value via `<Amount money={true} theme="ink" size="md"/>`, plus **"Approve tag"** (`Btn variant="success"`) and **"Reject tag"** (`Btn variant="danger"`) calling approveBehaviorMark/rejectBehaviorMark, then refresh the list. Empty state per UI-SPEC copy ("No tags waiting for review"). Row padding 8px 16px. Reject is a single tap (no confirm dialog, mirrors shop reject). All strings via useT().
    DailyModal.tsx behavior section — replace the single `good_behavior` checkbox intent UI with a behavior-tags view for that child+date, AND keep the D-09 legacy dual-write:
    - Load the family's active behavior_tags (getBehaviorTags) + this child+date's existing behavior_marks (getBehaviorMarks, all statuses).
    - Identify the seeded DEFAULT tag (legacy_key='good_behavior'). Retain a `goodBehavior` boolean that maps to whether that default tag is applied for this child+date — DEFAULT it to `true` (preserving today's default-true semantics), and for a sick day force it `true` (unchanged from today, line 539/1060). The default tag renders as a checked-by-default chip; toggling it flips `goodBehavior`.
    - Additional (non-legacy) tags render as chips a parent can apply directly.
    - DUAL-WRITE on save (mirror 05.2-05): (a) KEEP the existing `api.saveDay({ ..., goodBehavior })` write of `days.good_behavior` — do NOT drop it; its value is the default-tag applied state (sick → true). (b) Reconcile behavior_marks via the service-role actions from Task 1: when `goodBehavior` is true ensure an approved default-tag mark exists for this child+date (`applyTagDirect(childId, date, defaultTagId)`), when false ensure it is removed/rejected; for each parent-directly-applied custom tag call `applyTagDirect`. Custom tags live ONLY in behavior_marks and never touch the legacy column — exactly as room custom tasks skip the legacy room_* columns.
    - Because Plan 06's fallback only fires on ZERO marks, and DailyModal now creates an approved default-tag mark whenever goodBehavior is true, the mark path credits (price 5 for the seeded default) and the legacy fallback stays dormant for these days — NO double credit. The dual-written `days.good_behavior` exists purely to keep the legacy readers (coins.service analytics, streaks.service) correct for the default tag.
    - Pending child-proposed marks show a StatusChip tone="warning" ("Waiting for parent"); the canonical review action lives in the dedicated queue (inline approve here is acceptable but the queue is canonical).
    - Keep gradeCoinsFor updated to the grade_coin_map lookup (mirror the award route; grade is now string) so the modal's coin preview does not diverge.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "Approve tag\|approveBehaviorMark\|behaviorApproval" components/parent-center/screens/BehaviorApprovalQueue.tsx && grep -q "grade_coin_map\|gradeCoinsFor" components/DailyModal.tsx && grep -q "goodBehavior\|good_behavior" components/DailyModal.tsx && grep -q "applyTagDirect" components/DailyModal.tsx</automated>
  </verify>
  <done>A dedicated ink-theme approval queue lists pending marks with Approve/Reject actions; DailyModal shows behavior tags (default tag checked-by-default, pending=warning chip), DUAL-WRITES days.good_behavior in sync with the default tag (D-09), and reconciles approved marks via service-role applyTagDirect; gradeCoinsFor uses the map lookup; tsc clean.</done>
  <acceptance_criteria>
    - Queue renders one row per pending mark with child name, tag, Amount-rendered value, Approve/Reject buttons
    - Reject has no separate confirm dialog (single tap, mirrors shop)
    - DailyModal STILL writes `days.good_behavior` via api.saveDay (grep matches) — the D-09 legacy dual-write is NOT dropped; its value tracks the seeded default tag; sick day forces true
    - Parent-direct tag apply in DailyModal writes an approved mark via the `applyTagDirect` server action (never a client 'approved' insert)
    - DailyModal gradeCoinsFor no longer uses a numeric 1-5 switch (uses grade_coin_map)
    - Pending status uses StatusChip tone="warning" (no invented pending color)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: tests/integration/behavior-approval.test.ts — propose→approve→credit boundary</name>
  <files>tests/integration/behavior-approval.test.ts</files>
  <read_first>
    - tests/integration/purchase.test.ts (the approve/reject-then-recheck-balance template; mocks requireParent/requireFamilyMember)
    - tests/integration/family-fixture.ts (fixture setup/teardown)
    - app/parent/behavior/actions.ts (the actions under test)
    - .planning/phases/05.9-rules-presets/05.9-VALIDATION.md §SC3c
  </read_first>
  <action>
    Model on purchase.test.ts. Cases: (1) a child-proposed mark is created status='pending' and a subsequent /api/wallet/award POST credits 0 for it; (2) approveBehaviorMark flips it to 'approved' and moves NO coins itself (balance unchanged immediately after approve); (3) the NEXT award POST credits the tag price exactly once; re-POST is idempotent (no double-credit); (4) a child-role membership calling approveBehaviorMark throws (self-approval refused — mock a child membership, expect AuthError/403); (5) rejectBehaviorMark flips to 'rejected' and the mark never credits; (6) applyTagDirect (parent) inserts an approved mark that the next award POST credits immediately (no separate approval step). Mock requireParent/requireFamilyMember per the purchase.test.ts partial-vi.mock pattern; let createAdminClient run for real against the live DB.
  </action>
  <verify>
    <automated>npm run test:integration -- tests/integration/behavior-approval.test.ts</automated>
    <note>Accepted latency tradeoff: live-DB integration test (~2-3 min, exceeds the 30s Nyquist WARNING / 180s phase target). Unavoidable for the approve→credit money boundary; runs per-wave, not per-task-commit — fast sampling stays on `npm test`.</note>
  </verify>
  <done>All six approval-boundary cases pass against the live DB, including the child-self-approval refusal and parent-direct apply.</done>
  <acceptance_criteria>
    - `npm run test:integration -- tests/integration/behavior-approval.test.ts` passes
    - A child-role approve attempt is asserted to throw (T-059-01 covered)
    - Approve moves no coins; credit occurs only on the subsequent award POST; re-POST idempotent
    - Rejected marks never credit; applyTagDirect marks credit on the next award POST
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → approveBehaviorMark/rejectBehaviorMark/applyTagDirect | untrusted caller; must be a parent of the mark's family |
| parent-direct tag apply → behavior_marks(status='approved') | must go through service-role (RLS forbids a client 'approved' insert) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-059-01 | Elevation of Privilege | behavior-mark approval | mitigate | requireParent() in all actions — a child role throws AuthError 403; approver/decided_by derived server-side, never from the client body. Test case 4 asserts refusal. |
| T-059-03 | Tampering | parent-direct 'approved' insert | mitigate | applyTagDirect runs service-role (requireParent) so it legitimately bypasses the client 'pending'-only RLS WITH CHECK; the client path can never produce an 'approved' mark. |
| T-059-13 | Repudiation | approve/reject not audited | accept | Best-effort insertAuditEvent (fire-and-forget, non-blocking) mirrors shop-approval; audit failure never blocks the parent action. |
| T-059-17 | Repudiation | days.good_behavior legacy column silently frozen | mitigate | DailyModal keeps dual-writing days.good_behavior in sync with the default tag (D-09); Plan 06 Test 5 asserts no double-credit; documented gap for custom-only families recorded in the 05.9-06 SUMMARY. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm run test:integration -- tests/integration/behavior-approval.test.ts` green (live-DB, ~2-3 min — accepted latency tradeoff per the Task 3 note).
</verification>

<success_criteria>
The propose→approve→credit flow is complete and guarded: children cannot self-approve, approval only flips status, credit is idempotent on the next award, a dedicated parent queue drives the interaction, and DailyModal preserves the D-09 legacy days.good_behavior dual-write for the default tag.
</success_criteria>

<output>
Create `.planning/phases/05.9-rules-presets/05.9-07-SUMMARY.md` when done. Note the BehaviorApprovalQueue export + that applyTagDirect was added (for plan 09's mount), and confirm the DailyModal days.good_behavior dual-write is in place (D-09).
</output>
