# Phase 05.9: rules-presets - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-21
**Phase:** 05.9-rules-presets
**Areas discussed:** Preset definitions & apply behavior, grade_scale storage & scale-switch semantics, Behavior tag set model

---

## Preset definitions & apply behavior

| Option | Description | Selected |
|--------|-------------|----------|
| No-penalties = zero grade-penalty columns only | coins_per_grade_3/2/1 → 0 | |
| No-penalties/Bonuses-only distinct, Bonuses-only also lowers positive rewards | Two genuinely different value tables | |
| You decide | Claude designs concrete numbers | ✓ (user gave philosophy context instead of picking) |

**User's choice:** Free text — explained that "Classic" should keep real
penalties (grade 3/2/1 → -3/-5/-10) because a child should learn actions have
negative consequences, not just positive reinforcement.

**Claude's follow-up proposal:** Classic = current defaults; No-penalties =
floor negatives to 0; Bonuses-only = no penalties + flat reward tier (grade 5
and grade 4 pay the same amount).

| Option | Description | Selected |
|--------|-------------|----------|
| Keep as designed | 3 distinct value sets as proposed | |
| Simpler: only 2 real presets | No-penalties and Bonuses-only numerically identical, just distinct UI cards | ✓ |
| Let me describe different numbers | User types custom values | |

**User's choice:** Simpler: only 2 real presets (No-penalties and
Bonuses-only share the same floored-to-0 values; Classic stays as-is).

---

| Option | Description | Selected |
|--------|-------------|----------|
| Grade coins only | Just coins_per_grade_1..5 | |
| Grade coins + room/behavior/exercise | Broader philosophy scope | |
| Everything preset-relevant except streaks and exchange rate | Grade + room/behavior/exercise + coach ratings | ✓ |

**User's choice:** Everything preset-relevant except streaks and exchange rate.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Straight overwrite with confirm dialog | Simple confirm, no diff shown | |
| Silent overwrite, no confirmation | No confirmation step | |
| Preview diff before applying | Old → new values shown side by side | ✓ |

**User's choice:** Preview diff before applying.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Preset picker replaces StepCoinRules entirely | No number fields in onboarding | |
| Preset picker sits above StepCoinRules as a shortcut | Numbers stay editable in onboarding | |

**User's choice:** Initial question was unclear ("я не понимаю твоих
объяснений"). Claude re-asked with concrete ASCII mockups of both options.

| Option | Description | Selected |
|--------|-------------|----------|
| Onboarding A: preset picker REPLACES manual entry entirely | 3 cards, no number fields, fine-tune later in Settings | ✓ |
| Option B: picker + numbers still visible | Cards pre-fill editable fields in the same step | |

**User's choice:** Option A (replace entirely).

**Notes:** User's Russian free-text response reflects their intent that
"consequences exist" (negative outcomes tied to the child's own actions) is a
deliberate parenting philosophy behind keeping Classic's penalties — this
shaped keeping Classic unchanged rather than softening all three presets.

---

## grade_scale storage & scale-switch semantics

| Option | Description | Selected |
|--------|-------------|----------|
| One JSON map in wallet_settings, keyed to active scale | grade_scale + grade_coin_map (JSONB) | ✓ |
| Keep fixed columns, add coins_per_grade_6..12 etc. | More hardcoded columns per scale | |

**User's choice:** One JSON map in wallet_settings.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Widen grade to allow any scale's raw value + drop numeric CHECK | Store literal value ("5", "11", "B+") | ✓ |
| Keep grade numeric always; map A-F letters to numbers internally | A=5,B=4,C=3,D=2,F=1 relabeling | |

**User's choice:** Widen grade to allow any scale's raw value.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Documented known-gap, fixed in a later phase | Badges/streaks stay 5-point-only, flagged as a known limitation | ✓ |
| In scope — generalize badges/streaks to be scale-aware too | Bigger lift touching 2 more service files | |

**User's choice:** Documented known-gap, fixed in a later phase.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Switch anytime; old grades keep their original literal value untouched | No reinterpretation needed, award idempotency already protects past coins | ✓ |
| Lock the scale once any grade exists this school year | Prevent mixed-scale gradebook | |

**User's choice:** Switch anytime.

---

## Behavior tag set model

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — behavior_tags + behavior_marks, same shape as room_tasks/room_checks | Family-defined list + per-day marks + legacy dual-write fallback | ✓ |
| Simpler — JSON list of tag definitions in wallet_settings, no marks table | Loses per-day auditability | |

**User's choice:** Room Tasks pattern (behavior_tags + behavior_marks).

---

| Option | Description | Selected |
|--------|-------------|----------|
| Both — a tag's price can be positive or negative | e.g. +5 for good, -10 for bad | ✓ |
| Bonus-only — tags can only add coins | No penalty mechanism | |

**User's choice:** Both positive and negative.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Multiple tags per day, sum of all applied tags' prices | Same shape as extra_activities/activity_logs | ✓ |
| One tag per day (replaces old single flag 1:1) | Closest to today's boolean | |

**User's choice:** Multiple tags per day, sum of prices.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Parent-only, unchanged | Matches today's architecture note | |
| Child can propose tags, parent approves | New two-step flow, closer to shop purchase-approval pattern | ✓ |

**User's choice:** Child can propose tags, parent approves.

**Follow-up — propose/approve flow shape:**

| Option | Description | Selected |
|--------|-------------|----------|
| Child proposes from KidDayFillForm; parent approves in DailyModal/Parent Center, same shape as shop purchase approval (pending → approved/rejected) | behavior_marks gets a status column | ✓ |
| Simpler: child can only propose POSITIVE tags; negative/penalty tags remain parent-only, applied directly with no approval step | Avoids approval step for parent-applied penalties | |

**User's choice:** Full pending/approved/rejected status flow, mirroring
reward_purchases.

**Follow-up — pending visibility:**

| Option | Description | Selected |
|--------|-------------|----------|
| Shown as 'pending' on the child's day (no coins yet), parent sees it in approval queue | Mirrors shop purchase requests | ✓ |
| Invisible until approved | No feedback to the child | |

**User's choice:** Shown as pending, mirrors shop purchase requests.

---

## Claude's Discretion

- Exact screen/component for parent review of pending child-proposed behavior
  tags (new queue vs. folded into existing DailyModal behavior section).
- Exact i18n copy/labels for preset cards, scale picker, and tag manager UI.
- Whether grade_coin_map migration backfills existing custom
  coins_per_grade_1..5 values, or those columns stay as fallback only.

## Deferred Ideas

- Badges/streaks becoming grade-scale-aware (currently hardcode 5-point
  semantics: grade===5 excellent, grade<=2 penalty) — explicitly deferred
  past 5.9, documented as a known gap in CONTEXT.md.
- A genuinely distinct third preset value set (flat-tier-collapsing
  "Bonuses-only") — considered, then rejected in favor of two value sets
  under three UI labels.
