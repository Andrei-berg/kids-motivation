---
phase: 05.9-rules-presets
plan: 05
subsystem: ui
tags: [typescript, react, supabase, rls, settings]

# Dependency graph
requires:
  - phase: 05.9-rules-presets (plan 02, wave 1 — behavior.types.ts, i18n keys)
    provides: "BehaviorTag/BehaviorMark types and settings.behaviorTags.*/behaviorApproval.* i18n keys this plan's repo/component consume"
  - phase: 05.9-rules-presets (plan 03, wave 1 — DB migrations)
    provides: "behavior_tags/behavior_marks tables + RLS (pending-only INSERT WITH CHECK, legacy-delete guard trigger) this plan's repo writes against"
provides:
  - "lib/repositories/behavior.repo.ts — getBehaviorTags, addBehaviorTag, updateBehaviorTag, setBehaviorTagActive, reorderBehaviorTags, deleteBehaviorTag, proposeMark, getBehaviorMarks"
  - "components/settings/BehaviorTagsManager.tsx — parent-side tag editor (signed price, legacy lock, Amount-rendered coin values)"
affects: [05.9-rules-presets remaining plans (07 — server-side approval/reject route consuming behavior_marks + proposeMark's pending contract; 08/09 — KidDayFillForm propose-tag UI, Settings screen wiring BehaviorTagsManager in)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Client-writable family-config repo (not money): behavior.repo.ts mirrors room.repo.ts's family_id-from-children resolution + CRUD shape, with proposeMark hard-coding status='pending' as defense-in-depth alongside the DB RLS WITH CHECK boundary"
    - "Click-to-edit Amount: BehaviorTagsManager renders each tag's coin value via <Amount money theme=\"ink\"/> in display mode, swapping to a number <input> on click (same interaction shape as the existing name click-to-edit), rather than an always-visible input like DayBlocksManager's price field"

key-files:
  created:
    - lib/repositories/behavior.repo.ts
    - components/settings/BehaviorTagsManager.tsx
  modified:
    - public/locales/en.json
    - public/locales/ru.json

key-decisions:
  - "Header comment in behavior.repo.ts rewords 'lib/supabase/admin' to 'the service-role admin client' to avoid tripping the plan's own verify grep for the literal substring 'supabase/admin' (same class of self-referential grep conflict documented in 05.9-02-SUMMARY) while preserving the load-bearing intent (never import the service-role client here)"
  - "Added settings.behaviorTags.namePlaceholder/pricePlaceholder/invalidPrice i18n keys (en+ru) — the plan 02 i18n pass covered title/subtitle/addBtn/empty*/deleteConfirm/systemTag but not the add-form's two input placeholders or the price-validation error string, both of which are structurally required by the add-form and price-edit flow this plan builds"
  - "Behavior tag price is editable via click-to-edit (matching the existing name-edit interaction) rather than an always-visible input (DayBlocksManager's pattern) — keeps the row's resting state as a clean Amount display per UI-SPEC's anchor-emphasis guidance for tag rows"

patterns-established:
  - "Signed-price add-form: name + price inputs side-by-side, price defaults to 0 if left blank, non-numeric input rejected inline via settings.*.invalidPrice before the repo call — same shape as DayBlocksManager's price handling, applied here to a byte-for-byte RoomTasksManager clone"

requirements-completed: [05.9-SC3]

# Metrics
duration: ~15min
completed: 2026-07-21
---

# Phase 05.9 Plan 05: Behavior Tag Config Layer Summary

**`behavior.repo.ts` (room.repo.ts clone + signed price + pending-only proposeMark + status-aware getBehaviorMarks) and `BehaviorTagsManager.tsx` (RoomTasksManager.tsx clone + Amount-rendered signed price, 8px 16px rows, aria-labeled icon buttons, locked default tag) — the parent's tag-set editor and the child's propose primitive, both client-writable and RLS-guarded, with zero money-adjacent logic (approval stays server-side for plan 07).**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-07-21T22:00:00+03:00 (approx.)
- **Completed:** 2026-07-21T22:05:00+03:00
- **Tasks:** 2/2 completed
- **Files modified:** 4 (2 created, 2 modified)

## Accomplishments
- `lib/repositories/behavior.repo.ts` ships all 8 required exports: the six room-parity CRUD functions (`getBehaviorTags`, `addBehaviorTag`, `updateBehaviorTag`, `setBehaviorTagActive`, `reorderBehaviorTags`, `deleteBehaviorTag`) plus `proposeMark` (hard-codes `status: 'pending'`) and `getBehaviorMarks` (reads `status`/`proposed_by`/`decided_by`) — never imports the service-role admin client
- `components/settings/BehaviorTagsManager.tsx` renders the full tag list with a signed-price add-form, click-to-edit `<Amount/>` coin values (never a raw `<span>`), 26×26 icon buttons carrying both `title` and `aria-label`, `8px 16px` row padding (this phase's spacing token, not the legacy `10px 12px`), and the "🔒 Default tag" lock hiding delete on the seeded `good_behavior` tag while leaving rename/toggle/reorder/price-edit available
- Filled two i18n gaps (`namePlaceholder`, `pricePlaceholder`, `invalidPrice` under `settings.behaviorTags`) that plan 02's i18n pass didn't cover but this plan's add-form/price-edit structurally required, in both `en.json` and `ru.json`

## Task Commits

Each task was committed atomically:

1. **Task 1: lib/repositories/behavior.repo.ts** - `4dc500a` (feat)
2. **Task 2: components/settings/BehaviorTagsManager.tsx** - `da0f34d` (feat)

**Plan metadata:** (this commit, following SUMMARY.md write)

## Files Created/Modified
- `lib/repositories/behavior.repo.ts` - Room-parity CRUD on `behavior_tags` (with `price`) + `proposeMark` (pending-only insert) + `getBehaviorMarks` (status-aware read)
- `components/settings/BehaviorTagsManager.tsx` - Parent settings editor: add-form (name+icon+price), row list with click-to-edit name/price, reorder/toggle/delete icon buttons, legacy lock
- `public/locales/en.json` - Added `settings.behaviorTags.namePlaceholder`/`pricePlaceholder`/`invalidPrice`
- `public/locales/ru.json` - Same key additions, Russian translations

## Decisions Made
- Reworded the repo's header comment to avoid the literal substring `supabase/admin`, since the plan's own verify script `! grep -q "supabase/admin" ...` would otherwise be tripped by an explanatory comment rather than an actual import (mirrors the 05.9-02 precedent of comment text tripping a grep-based acceptance check)
- Chose click-to-edit for the price field (reusing the existing name click-to-edit pattern) over an always-visible input, keeping the row's resting state a clean `<Amount/>` display
- Added the three missing i18n keys under the existing `settings.behaviorTags` namespace rather than introducing a new namespace, matching plan 02's established convention of namespacing new strings under an existing manager's key tree

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Header comment tripped the plan's own "no admin import" verify grep**
- **Found during:** Task 1, acceptance-criteria verification
- **Issue:** The plan's action text asked to keep the load-bearing header comment "Do NOT import lib/supabase/admin here" verbatim, but the plan's own automated verify command (`! grep -q "supabase/admin" lib/repositories/behavior.repo.ts`) checks for the literal absence of that substring anywhere in the file — including comments — so following the action text literally would have made the verify command fail even though no actual import exists
- **Fix:** Reworded the comment to "Do NOT import the service-role admin client here", preserving the load-bearing intent without the literal substring
- **Files modified:** lib/repositories/behavior.repo.ts
- **Verification:** `grep -q "supabase/admin" lib/repositories/behavior.repo.ts` → no match; `npx tsc --noEmit` clean; `grep -q "proposeMark"` and `grep -q "status: 'pending'"` both match
- **Committed in:** 4dc500a (Task 1 commit)

**2. [Rule 2 - Missing Critical] Added missing i18n keys required by the add-form and price validation**
- **Found during:** Task 2, component implementation
- **Issue:** `settings.behaviorTags` (added in plan 02) had `title`/`subtitle`/`addBtn`/`emptyTitle`/`emptyBody`/`deleteConfirm`/`systemTag` but no placeholder strings for the add-form's name/price inputs, nor an invalid-price error string — both structurally required to avoid hardcoded literals (plan's "All strings via useT()" requirement) and to implement the price-validation guard mirrored from `DayBlocksManager`'s `invalidPrice` pattern
- **Fix:** Added `namePlaceholder`, `pricePlaceholder`, `invalidPrice` under `settings.behaviorTags` in both `en.json` and `ru.json`, matching the existing `dayBlocksManager` key shapes for the same concepts
- **Files modified:** public/locales/en.json, public/locales/ru.json
- **Verification:** `node -e "JSON.parse(...)"` on both files succeeds; `npx tsc --noEmit` clean; component has zero hardcoded UI strings
- **Committed in:** da0f34d (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (1 Rule 1 — bug/self-contradicting verify check, 1 Rule 2 — missing critical i18n coverage)
**Impact on plan:** Both fixes were necessary to satisfy the plan's own stated acceptance criteria and the "no hardcoded UI strings" requirement against a correct implementation. No scope creep — no new features, no architectural changes.

## Issues Encountered
None beyond the two auto-fixed items above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `behavior.repo.ts` exports (`getBehaviorTags`, `addBehaviorTag`, `updateBehaviorTag`, `setBehaviorTagActive`, `reorderBehaviorTags`, `deleteBehaviorTag`, `proposeMark`, `getBehaviorMarks`) are stable, importable — plan 07 (server-side approve/reject route) can import `getBehaviorMarks` for reads without needing this repo to know about the approval transition; plans 08/09 (KidDayFillForm propose UI, Settings screen wiring) can import `proposeMark` and mount `<BehaviorTagsManager/>` directly
- `BehaviorTagsManager.tsx` is not yet wired into any Settings screen — that wiring is out of this plan's scope and belongs to a later Settings-integration plan (per the plan's file list, only the two new files were in scope)
- No DB dependency issues — plan 01/03's migrations (behavior_tags/behavior_marks tables + RLS) were already applied in a prior wave

---
*Phase: 05.9-rules-presets*
*Completed: 2026-07-21*
