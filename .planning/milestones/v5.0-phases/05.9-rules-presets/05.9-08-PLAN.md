---
phase: 05.9-rules-presets
plan: 08
type: execute
wave: 4
depends_on: [05.9-04, 05.9-05]
files_modified:
  - components/kid/KidDayFillForm.tsx
  - components/BulkModal.tsx
autonomous: true
requirements: [05.9-SC2, 05.9-SC3]
must_haves:
  truths:
    - "The kid grade input is data-driven from the active grade_scale (renders that scale's N options), stores the literal value as a string like '11' or 'A' (D-06), and previews coins via grade_coin_map"
    - "A child can propose behavior tags from KidDayFillForm; proposed tags show a 'Waiting for parent' pending state and no coins yet"
    - "The kid form proposes pending marks only and — by design, documented (D-09) — does NOT write days.good_behavior; behavior credit is parent-gated, so the legacy dual-write lives on the parent DailyModal (Plan 07), not here"
    - "BulkModal compiles against string grade (kept 5-point-only, legacy) — badges/streaks 5-point-only grade semantics are an explicit out-of-scope known gap for 5.9 (D-07)"
  artifacts:
    - path: "components/kid/KidDayFillForm.tsx"
      provides: "data-driven grade input + behavior tag propose picker"
      contains: "grade_coin_map"
  key_links:
    - from: "KidDayFillForm behavior picker"
      to: "behavior.repo.ts proposeMark"
      via: "client insert status='pending'"
      pattern: "proposeMark"
---

<objective>
Make the kid day-fill grade input scale-aware (data-driven from grade_scale, storing string literals, previewing via grade_coin_map) and add the child-side "Propose a tag" behavior picker (paper theme). Fix BulkModal's compile break type-only (legacy, 5-point-only per A1/D-07).

Purpose: The child's grade entry and behavior-proposal surface.
Output: KidDayFillForm grade+behavior changes; BulkModal type-only fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.9-rules-presets/05.9-PATTERNS.md
@.planning/phases/05.9-rules-presets/05.9-UI-SPEC.md
@.planning/phases/05.9-rules-presets/05.9-04-SUMMARY.md
@.planning/phases/05.9-rules-presets/05.9-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: KidDayFillForm — data-driven grade input (string) + grade_coin_map preview</name>
  <files>components/kid/KidDayFillForm.tsx</files>
  <read_first>
    - components/kid/KidDayFillForm.tsx (GradeEntry type ~line 142; coinsPreview switch ~lines 298-325; grade-button grid + color-by-grade ~lines 727-790, e.g. `entry.grade >= 4 ? T.tealDeep : ...` ~line 776)
    - lib/presets.ts (GRADE_SCALE_VALUES, defaultGradeCoinMap — to render the active scale's options)
    - lib/repositories/wallet.repo.ts getWalletSettings (now returns grade_coin_map — plan 04)
    - .planning/phases/05.9-rules-presets/05.9-RESEARCH.md §"Pitfall 1" (every grade read site) + §"Pattern 2" (map lookup)
  </read_first>
  <action>
    Change `GradeEntry.grade` from `number | null` to `string | null` (D-06). Load the family's `grade_scale` + `grade_coin_map` via getWalletSettings. Render the grade-button grid data-driven: map over `GRADE_SCALE_VALUES[grade_scale]` (fallback five_point) to produce one button per possible value instead of a hardcoded 5-button block; the button's stored value is the literal string. Replace the coinsPreview numeric switch with `grade_coin_map[entry.grade] ?? 0` summation (mirror the award route's lookup exactly — client preview must not diverge). Replace numeric color-by-grade (`entry.grade >= 4 ? ... : entry.grade === 3 ? ...`) with a sign-based rule driven by the previewed coin value (coins > 0 → positive tone, coins < 0 → danger tone, 0 → neutral) so it works for every scale rather than only 1-5. The saved value written to subject_grades is the literal string (no Number cast). Keep the save/dual-write flow otherwise intact.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "grade_coin_map" components/kid/KidDayFillForm.tsx && grep -q "GRADE_SCALE_VALUES" components/kid/KidDayFillForm.tsx && ! grep -q "entry.grade >= 4" components/kid/KidDayFillForm.tsx</automated>
  </verify>
  <done>Grade input renders the active scale's options, stores string literals, previews via grade_coin_map, and colors by coin sign; tsc clean.</done>
  <acceptance_criteria>
    - GradeEntry.grade is `string | null`
    - Grade buttons are mapped from GRADE_SCALE_VALUES (no hardcoded 5-button block)
    - coinsPreview uses grade_coin_map lookup (no numeric 1-5 switch)
    - Color-by-grade is sign-based, not numeric threshold (`entry.grade >= 4` grep returns 0)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: KidDayFillForm — behavior tag propose picker (paper theme) + BulkModal type-only fix</name>
  <files>components/kid/KidDayFillForm.tsx, components/BulkModal.tsx</files>
  <read_first>
    - components/kid/KidDayFillForm.tsx (the grade-button toggle idiom ~lines 750-752 `const on = entry.grade === g` — reuse this click-to-set-state pattern for chip selection; the room-checklist section for placement reference)
    - components/design/atoms.tsx (Tick / StatusChip / Amount — the paper-theme primitives to build the picker from)
    - lib/repositories/behavior.repo.ts (getBehaviorTags, getBehaviorMarks, proposeMark — plan 05)
    - components/BulkModal.tsx (updateGrade(...grade: number), hardcoded `grade >= 2 && grade <= 5` validation, color-by-grade UI)
    - .planning/phases/05.9-rules-presets/05.9-UI-SPEC.md §"Copywriting Contract" (kid-side "Propose a tag" + "Waiting for parent") + §"Color" (paper accent for selected chip; warning=pending)
  </read_first>
  <action>
    KidDayFillForm behavior section: load active behavior_tags (getBehaviorTags) + this child+date's existing behavior_marks (getBehaviorMarks). Render a tag-chip picker built from existing paper primitives — a Tick-style selected-chip using `selectedTagIds` state and the same click-to-toggle idiom as the grade buttons (no new selection primitive). A **"Propose a tag"** button calls `proposeMark({ childId, date, tagId, proposedBy })` for the selected tag(s), inserting status='pending'. Already-proposed pending marks render a `StatusChip tone="warning" theme="paper"` "Waiting for parent" and show NO coins yet (a negative-price tag's value, if shown, uses `<Amount money={true} theme="paper"/>` in dangerText — never raw hex, never indigo on money). Selected-chip accent uses paper.accent (Tick `on` styling). All strings via useT().
    D-09 SCOPE NOTE (deliberate, documented divergence from the Room Tasks two-form dual-write): the kid form does NOT dual-write `days.good_behavior`. Add a one-line code comment in this section stating so, and record it in the SUMMARY. Rationale — behavior coins are parent-only (the award route never credits behavior on a child's own award POST; see award/route.ts lines ~177-180 "Behavior coins are a PARENT assessment"), the kid form historically never wrote `good_behavior` (it is not present in KidDayFillForm today), and child-proposed marks are `status='pending'` (zero coins until a parent approves). The D-09 legacy dual-write/fallback for behavior therefore lives entirely on the parent DailyModal (Plan 07) + the award-route zero-marks fallback (Plan 06). This divergence is recorded here rather than silently dropped — do NOT add a client-side days.good_behavior write to the kid form (it would be an unapproved-credit / RLS-surface change).
    BulkModal.tsx: TYPE-ONLY fix (A1/D-07 — legacy single-family surface, kept 5-point-only, NOT re-architected). Change `updateGrade(..., grade: number)` and related signatures/state to string where the compiler requires it after subject_grades.grade became TEXT; keep the hardcoded `grade >= 2 && grade <= 5` behavior by comparing string literals (`['2','3','4','5'].includes(grade)`), and keep the color-by-grade UI as-is (5-point-only). Add a one-line comment: "Legacy 5-point-only surface (A1/D-07, Phase 5.9): TEXT grade compile-fix only, not scale-generalized." Do not add behavior tags or grade_coin_map here.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "proposeMark" components/kid/KidDayFillForm.tsx && grep -q "Legacy 5-point-only" components/BulkModal.tsx</automated>
  </verify>
  <done>Kid can propose behavior tags (pending, no coins) via a paper-theme chip picker built from existing atoms; the kid form deliberately (documented) does not write days.good_behavior; BulkModal compiles as a documented 5-point-only legacy surface; tsc clean.</done>
  <acceptance_criteria>
    - Behavior picker uses `proposeMark` (status='pending') and shows StatusChip tone="warning" for pending marks
    - No new selection primitive introduced (reuses Tick/chip toggle idiom)
    - A code comment documents that the kid form intentionally does NOT dual-write days.good_behavior (D-09 — parent-gated behavior; dual-write lives on DailyModal, Plan 07)
    - Any coin value shown to the child renders via `<Amount/>` (no raw hex, no indigo on money)
    - BulkModal compiles with string grade, keeps 5-point behavior, carries the legacy comment
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child client → proposeMark INSERT | must be status='pending' only (RLS WITH CHECK, plan 01) |
| child client → subject_grades write | family-isolation RLS (not money); literal string stored, award recomputes coins server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-059-03 | Tampering/EoP | child proposes an approved mark | mitigate | proposeMark inserts 'pending' only; RLS INSERT WITH CHECK (plan 01) is the boundary; no coins reflected until a parent approves (plan 07). |
| T-059-14 | Tampering | child enters an out-of-scale grade literal | accept | award route `grade_coin_map[grade] ?? 0` yields 0 for unknown keys; UI only offers the active scale's values. No unbounded mint. |
| T-059-18 | Elevation | child self-crediting behavior via a legacy days.good_behavior write | mitigate | The kid form deliberately does NOT write days.good_behavior (documented D-09 scope note); behavior credit is parent-gated (award route parent-only + approved-marks only). No child-writable behavior-credit path exists. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- Kid grade input and behavior picker are data-driven and paper-themed.
</verification>

<success_criteria>
The child enters grades in the family's scale (any of the three) and proposes behavior tags that stay pending until a parent approves — all via existing paper-theme atoms and RLS-guarded writes; the kid form intentionally leaves the D-09 legacy dual-write to the parent DailyModal.
</success_criteria>

<output>
Create `.planning/phases/05.9-rules-presets/05.9-08-SUMMARY.md` when done. Record the deliberate D-09 decision that the kid form does not dual-write days.good_behavior (dual-write lives on DailyModal / Plan 07; fallback on the award route / Plan 06).
</output>
