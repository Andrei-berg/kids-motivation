---
phase: 05.9-rules-presets
plan: 05
type: execute
wave: 3
depends_on: [05.9-02, 05.9-03]
files_modified:
  - lib/repositories/behavior.repo.ts
  - components/settings/BehaviorTagsManager.tsx
autonomous: true
requirements: [05.9-SC3]
must_haves:
  truths:
    - "A parent can add/rename/toggle/reorder/delete behavior tags (with a per-tag signed price), except the seeded legacy tag which cannot be deleted"
    - "A child-side propose inserts a behavior_marks row with status='pending' via the client (RLS-guarded)"
    - "getBehaviorMarks reads marks including their status"
  artifacts:
    - path: "lib/repositories/behavior.repo.ts"
      provides: "reads + parent CRUD + proposeMark + getBehaviorMarks"
      exports: ["getBehaviorTags", "addBehaviorTag", "updateBehaviorTag", "setBehaviorTagActive", "reorderBehaviorTags", "deleteBehaviorTag", "proposeMark", "getBehaviorMarks"]
    - path: "components/settings/BehaviorTagsManager.tsx"
      provides: "settings editor with price field"
      min_lines: 120
  key_links:
    - from: "BehaviorTagsManager"
      to: "behavior.repo.ts"
      via: "direct client CRUD imports"
      pattern: "from '@/lib/repositories/behavior.repo'"
---

<objective>
Build the behavior-tag config layer: `behavior.repo.ts` (a near-verbatim clone of `room.repo.ts` plus a price field, `proposeMark`, and a status-aware `getBehaviorMarks`) and `BehaviorTagsManager.tsx` (a clone of `RoomTasksManager.tsx` plus a signed-price input). These are family-config, client-writable, NOT money — the approval/credit transition lives server-side in plan 07.

Purpose: The parent's tag-set editor and the child's propose primitive.
Output: One new repo, one new settings manager component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.9-rules-presets/05.9-PATTERNS.md
@.planning/phases/05.9-rules-presets/05.9-UI-SPEC.md
@.planning/phases/05.9-rules-presets/05.9-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/repositories/behavior.repo.ts</name>
  <files>lib/repositories/behavior.repo.ts</files>
  <read_first>
    - lib/repositories/room.repo.ts (full — the exact analog: imports, getRoomTasks read pattern, family_id-from-children resolution, addRoomTask/updateRoomTask/setRoomTaskActive/reorderRoomTasks/deleteRoomTask CRUD)
    - lib/models/behavior.types.ts (BehaviorTag/BehaviorMark shapes from plan 02)
    - .planning/phases/05.9-rules-presets/05.9-PATTERNS.md §"lib/repositories/behavior.repo.ts" (read/write/CRUD excerpts + the behavior-specific additions)
  </read_first>
  <action>
    Clone room.repo.ts. Keep the load-bearing header comment adapted: "Family-config / day-data (NOT money tables) — writes go through the browser client under family RLS. Do NOT import lib/supabase/admin here." Import `supabase` from '../supabase', `normalizeDate` from '@/utils/helpers', and BehaviorTag/BehaviorMark from '../models/behavior.types'.
    Parity functions on behavior_tags (copy room_tasks CRUD, add the `price` field to insert/update): `getBehaviorTags(familyId, { activeOnly = true })` ordered by sort_order; `addBehaviorTag({ familyId, name, icon?, price, sortOrder? })` inserting `{ family_id, name, icon: icon ?? null, price, legacy_key: null, sort_order: sortOrder ?? 0, is_active: true }`; `updateBehaviorTag(id, { name?, icon?, price? })`; `setBehaviorTagActive(id, active)`; `reorderBehaviorTags(orderedIds)` (loop sort_order = i); `deleteBehaviorTag(id)` (surface the DB legacy-delete-guard error, do not swallow).
    Behavior-specific additions (beyond room parity): `getBehaviorMarks(childId, date)` selecting `*` (must include `status`, `proposed_by`, `decided_by`) for that child+date; `proposeMark({ childId, date, tagId, proposedBy })` resolving family_id from the children table (the room.repo idiom) then INSERTing `{ child_id, family_id, date: normalizeDate(date), tag_id, proposed_by, status: 'pending' }` — do NOT set status to anything but 'pending' from the client (the RLS INSERT WITH CHECK from plan 01 also enforces this; the repo must not attempt otherwise). Do NOT put any status→approved/rejected transition in this file — that is server-side only (plan 07).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "proposeMark" lib/repositories/behavior.repo.ts && grep -q "status: 'pending'" lib/repositories/behavior.repo.ts && ! grep -q "supabase/admin" lib/repositories/behavior.repo.ts</automated>
  </verify>
  <done>behavior.repo.ts exports the room-parity CRUD plus proposeMark (pending only) and status-aware getBehaviorMarks; never imports admin; tsc clean.</done>
  <acceptance_criteria>
    - All eight exports present (six CRUD + proposeMark + getBehaviorMarks)
    - proposeMark inserts `status: 'pending'` and never 'approved'/'rejected'
    - No import of `lib/supabase/admin` (client-only repo)
    - deleteBehaviorTag does not catch/swallow the legacy-delete-guard error
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: components/settings/BehaviorTagsManager.tsx</name>
  <files>components/settings/BehaviorTagsManager.tsx</files>
  <read_first>
    - components/settings/RoomTasksManager.tsx (full — the exact analog: imports, state, add-form, row rendering with legacy lock + reorder + toggle + delete, iconBtn sizing at line ~287, error banner at line ~139)
    - .planning/phases/05.9-rules-presets/05.9-UI-SPEC.md §"Copywriting Contract" + §"Spacing Scale" (row padding 8px 16px for NEW components — do NOT copy RoomTasksManager's 10px 12px; iconBtn 26×26 copied verbatim; aria-label + title on the four icon buttons; "🔒 Default tag" lock copy; "+ Add tag"; empty state; delete confirmation)
    - .planning/phases/05.9-rules-presets/05.9-PATTERNS.md §"BehaviorTagsManager.tsx"
  </read_first>
  <action>
    Clone RoomTasksManager.tsx (`'use client'`, resolve familyId via useAppStore(), useT(), T from '@/components/parent-center/tokens'). Wire to behavior.repo.ts CRUD (getBehaviorTags/addBehaviorTag/updateBehaviorTag/setBehaviorTagActive/reorderBehaviorTags/deleteBehaviorTag). Structural additions/differences from the analog:
    - Add a signed-number `price` input to the add-form (RoomTasksManager has no price) alongside name + emoji picker. Render each tag row's coin value via `<Amount money={true} theme="ink" size="md"/>` from components/design/atoms.tsx (NEVER a raw span — UI-SPEC atom law; gold is the sole money color). Negative-price rows use the danger emphasis per UI-SPEC Color table.
    - Row vertical/horizontal padding: `8px 16px` (this phase's spacing token) — NOT the legacy `10px 12px`.
    - iconBtn (move up/down/toggle/delete) 26×26 copied verbatim from RoomTasksManager; set BOTH `title` and `aria-label` (same i18n string) on each of the four buttons.
    - Legacy lock: the seeded 'good_behavior' tag (legacy_key non-null) shows "🔒 Default tag" (settings.behaviorTags.* key) and hides the delete button (only the delete is blocked; rename/toggle/reorder/price-edit remain allowed, matching room-tasks).
    - Empty state, delete confirmation (inline confirm()), and error banner (T.dangerSoft bg / T.danger border) copy all from the UI-SPEC contract via i18n keys added in plan 02.
    All strings via useT() — no hardcoded literals.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "Amount" components/settings/BehaviorTagsManager.tsx && grep -q "aria-label" components/settings/BehaviorTagsManager.tsx && grep -Eq "padding: '8px 16px'|padding:'8px 16px'|8px 16px" components/settings/BehaviorTagsManager.tsx</automated>
  </verify>
  <done>BehaviorTagsManager renders the tag list with a signed price input, Amount-rendered values, 26×26 icon buttons with title+aria-label, legacy lock on the default tag, and 8px 16px row padding; tsc clean.</done>
  <acceptance_criteria>
    - Coin values render via `<Amount .../>`, never a raw `<span>🪙</span>` in a tag row
    - All four icon buttons carry both `title` and `aria-label`
    - The seeded default tag hides its delete button and shows the "🔒 Default tag" lock
    - Row padding uses `8px 16px` (not `10px 12px`)
    - No hardcoded UI strings (all via useT())
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent client → behavior_tags CRUD | family-isolation RLS; client-writable config (not money) |
| child client → proposeMark INSERT | must produce status='pending' only — enforced by RLS (plan 01), repo never attempts otherwise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-059-03 | Tampering/EoP | proposeMark client insert | mitigate | Repo inserts status='pending' only; the DB RLS INSERT WITH CHECK (plan 01) is the actual boundary — a crafted client cannot set 'approved'. |
| T-059-05 | Elevation | deleteBehaviorTag on legacy tag | mitigate | DB legacy-delete guard trigger raises; repo surfaces the error; UI hides delete for the locked tag. |
| T-059-09 | Tampering | behavior_tags price value | accept | Prices are parent-only config (family RLS); the award route reads price authoritatively; no unbounded-mint path since award clamps block coins already. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- Manager wires to the repo; repo never imports admin.
</verification>

<success_criteria>
The behavior tag set is fully editable by a parent (with signed prices and a locked default) and proposable by a child (pending only), all through client-writable, RLS-guarded paths.
</success_criteria>

<output>
Create `.planning/phases/05.9-rules-presets/05.9-05-SUMMARY.md` when done. List the repo exports so plans 07/08/09 can import them.
</output>
