# Phase 05.11: legacy-cleanup - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-23
**Phase:** 05.11-legacy-cleanup
**Areas discussed:** Wallboard removal, Extended-role landing target, App icon direction, Mark concept

---

## Wallboard removal

| Option | Description | Selected |
|--------|-------------|----------|
| Cut it, no replacement | Redirect it like the other 9 legacy routes and let the feature go — if a TV-mode view matters later, it's a new phase. | ✓ |
| Note it as a deferred idea | Cut it now, but record "family TV/kiosk display" as a backlog idea for a future phase — possibly related to the "family feed" concept already in memory. | |
| Keep it alive, don't delete | Leave /wallboard as-is (out of the delete list) — descope it from this phase entirely. NavBar.tsx also stays. | |

**User's choice:** Cut it, no replacement.
**Notes:** No backlog/deferred-idea note added per user's explicit choice over the "note it as a deferred idea" alternative.

---

## Extended-role / fallback landing target

| Option | Description | Selected |
|--------|-------------|----------|
| /parent-center | Same hub a parent sees. Simplest, matches how middleware already treats parents — extended members get a read-mostly view of the same screens. | ✓ |
| /family | Route already exists as a bare placeholder — the "shared, any authenticated member" destination per CLAUDE.md — would need real content, likely more than this phase intends. | |

**User's choice:** /parent-center.
**Notes:** Applies to `middleware.ts:74` (the `extended`/unknown-role fallback) and `app/page.tsx:37,44` (post-login default before role is known) — both currently hard-code `/dashboard`.

---

## App icon direction

| Option | Description | Selected |
|--------|-------------|----------|
| Design a simple brand mark now | Create a monogram/coin-style mark consistent with the locked tokens (indigo + gold, Bitter display font) and generate the icon set from it. | ✓ |
| Ship the flat color square as-is | Treat solid-brand-color as "good enough" branding for now; icon design becomes a separate future task. | |
| I have/want to provide an existing logo | User supplies the actual logo asset instead of Claude designing one. | |

**User's choice:** Design a simple brand mark now.

---

## Mark concept

| Option | Description | Selected |
|--------|-------------|----------|
| Coin/medallion mark | A simple stylized coin (circle + inner detail, e.g. a star or check) in gold on the indigo square — ties to the core "earn coins" mechanic and the gold-is-money-only rule. | ✓ |
| Monogram — stylized "F" | A geometric "F" in gold or white on the indigo square, in the Bitter display weight. | |
| Ledger-row mark | A miniature version of the app's signature ledger-row element. | |

**User's choice:** Coin/medallion mark.

---

## Claude's Discretion

- `globals.css` purge verification methodology (grep-per-class + build + visual pass) — no user preference expressed, implementation detail.
- Redirect map implementation shape (single `redirects()` array vs. grouped/commented) — implementation detail.
- Exact redirect targets for the 9 non-wallboard legacy routes were verified against current code (each already resolves to `/parent-center`, via `/parent/*` stubs except `/settings` which resolves to the real `/parent/settings` page) — collapsing to direct `/parent-center` targets in `next.config.js` is Claude's call, not a user preference.

## Deferred Ideas

None — the wallboard-as-deferred-idea alternative was explicitly declined (see above).
