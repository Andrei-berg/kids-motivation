---
phase: 05.11-legacy-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/icon-192x192.png
  - public/icon-512x512.png
  - public/apple-touch-icon.png
  - app/manifest.ts
  - app/layout.tsx
autonomous: false
requirements: ["05.11-SC3"]
user_setup: []

must_haves:
  truths:
    - "D-05/D-06: The three app-icon PNGs render the FamilyCoins mark: a gold coin (with a gold-deep rim and an indigo 5-point-star negative-space cutout) centered on an indigo square"
    - "D-07: Icon files are at their correct sizes (192x192, 512x512, apple-touch 180x180) and are fully opaque (no alpha channel)"
    - "D-08: manifest.ts start_url points at a surviving route (/), and theme_color/background_color match the locked design tokens"
    - "D-08: layout.tsx viewport.themeColor matches manifest.ts theme_color"
  artifacts:
    - path: "public/apple-touch-icon.png"
      provides: "180x180 opaque iOS home-screen icon (dimension corrected from 192x192)"
    - path: "app/manifest.ts"
      provides: "manifest with start_url '/', theme_color #6C5CE7, background_color #141021"
      contains: "start_url: '/'"
    - path: "app/layout.tsx"
      provides: "viewport.themeColor #6C5CE7"
      contains: "#6C5CE7"
  key_links:
    - from: "app/manifest.ts theme_color"
      to: "app/layout.tsx viewport.themeColor"
      via: "identical locked hex #6C5CE7"
      pattern: "#6C5CE7"
    - from: "app/manifest.ts start_url"
      to: "/ (middleware role-based landing)"
      via: "surviving route"
      pattern: "start_url: '/'"
---

<objective>
Ship real FamilyCoins branding: (1) regenerate the three app-icon PNGs from a single 512px
master — a flat two-tone coin-medallion mark (gold coin + gold-deep rim + indigo 5-point-star
cutout, on the indigo brand square) per UI-SPEC; (2) fix the two stale manifest fields
(`start_url`, colors) plus the layout viewport color to the locked design tokens.

Purpose: satisfy ROADMAP 5.11 Success Criterion 3 — app icon, splash, and manifest carry
FamilyCoins branding. The current icons are flat indigo squares with no mark (D-05); the
manifest `start_url` points at the now-deleted `/dashboard` and its colors predate the locked
palette (D-08).
Output: three branded, correctly-sized, opaque PNGs + a manifest/layout aligned to
`lib/design/tokens.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.11-legacy-cleanup/05.11-CONTEXT.md
@.planning/phases/05.11-legacy-cleanup/05.11-UI-SPEC.md
@.planning/phases/05.11-legacy-cleanup/05.11-PATTERNS.md

<interfaces>
<!-- Locked visual spec (UI-SPEC "App Icon / Brand Mark") + locked metadata (D-08). -->

Icon geometry (single 512x512 master, per UI-SPEC export matrix + safe-zone rules):
  - Background: full-bleed 512x512 square, no pre-rounded corners, fill #6C5CE7 (base.indigo).
  - Coin disc: circle centered at (256,256), diameter 66% of canvas = 337.92px
    (radius ≈ 169px), fill #E9A83C (base.gold). Stays inside the maskable 80% safe zone; do
    NOT enlarge past ~72% diameter.
  - Coin rim: a ring drawn just inside the disc edge, stroke width 3% of canvas ≈ 15px, color
    #C6871E (base.goldDeep), flat stroke only (no gradient/bevel). Implement as a circle of
    radius ≈ 161.5px (169 − 15/2), fill none, stroke #C6871E, stroke-width 15.
  - Star cutout: a 5-point star centered at (256,256), overall diameter 45% of the coin disc
    (≈152px → outer radius ≈76px), fill #6C5CE7 (SAME as background — negative space, not a
    third color). First point up (pointing to 12 o'clock). Use inner/outer radius ratio 0.5
    (inner radius ≈38px) for a full star that holds its silhouette at favicon scale.
  Result: exactly 2 brand hues (indigo + gold) — gold reserved to the coin only (design-system
  "gold = money" rule; the coin IS a money element, so this is compliant).

Export matrix (regenerate ALL from the one master; opaque, sRGB, flattened — NO alpha):
  - public/icon-512x512.png  → 512x512 (the master itself)
  - public/icon-192x192.png  → 192x192 (downscaled)
  - public/apple-touch-icon.png → 180x180 (downscaled — CORRECT the current wrong 192x192 size)
  Do NOT add a favicon.ico or any 4th file. Do NOT change the manifest `icons` array shape
  (same 3 filenames/sizes/purposes) — only the PNG bytes change.

Tooling: no image lib in package.json. Use a one-off `npm i sharp --no-save` scratch script
(mirrors CLAUDE.md's `npm i pg --no-save` migration pattern) that renders an SVG string of the
geometry above and writes the three PNGs. sharp: render SVG buffer → `.flatten({background:
'#6C5CE7'})` (guarantees opaque, no alpha) → `.resize(size, size)` → `.png()`. Keep the script
in scratch (not committed); only the 3 PNGs are committed.

Locked metadata edits (D-08 / UI-SPEC metadata table):
  app/manifest.ts:  start_url '/dashboard' → '/'    (was a deleted route)
                    background_color '#0f0f23' → '#141021'   (ink.bg)
                    theme_color '#6366f1' → '#6C5CE7'        (base.indigo)
                    name/short_name/description/display/orientation/icons array UNCHANGED
  app/layout.tsx:   viewport.themeColor '#6366f1' → '#6C5CE7'   (match manifest)
                    metadata/appleWebApp/font calls UNCHANGED
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify `sharp` package legitimacy before the one-off install</name>
  <files>(none — verification gate, no file change)</files>
  <read_first>
    - CLAUDE.md ("Applying DB migrations" section — the `npm i pg --no-save` one-off precedent)
    - .planning/phases/05.11-legacy-cleanup/05.11-PATTERNS.md ("No Analog Found" — why a one-off image tool is needed)
  </read_first>
  <action>
    Do NOT run `npm i sharp` yet. Present the legitimacy evidence to the operator and block on
    approval. No RESEARCH.md Package Legitimacy Audit table exists for this phase, so `sharp`
    is [ASSUMED] and requires operator sign-off before install (Package Legitimacy Gate). This
    checkpoint is not auto-advanceable regardless of workflow.auto_advance.
  </action>
  <what-built>
    Nothing yet — this gate precedes the `npm i sharp --no-save` used to rasterize the icons.
    No RESEARCH.md Package Legitimacy Audit table exists for this phase, so `sharp` is treated
    as [ASSUMED] and must be operator-verified before install (Package Legitimacy Gate).
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/sharp
    2. Confirm it is the official high-throughput image library (`lovell/sharp`), millions of
       weekly downloads, actively maintained — not a typosquat.
    3. Confirm the install will use `--no-save` (never enters package.json or the lockfile;
       removed after use), matching the repo's blessed `npm i pg --no-save` one-off pattern.
    Type "approved" to allow the install, or name an alternative (e.g. a pure-Node PNG encoder)
    if you object to sharp.
  </how-to-verify>
  <verify>
    <automated>MISSING — human legitimacy gate; no automated proof. Operator approval recorded in SUMMARY.</automated>
  </verify>
  <acceptance_criteria>
    - Operator has typed "approved" (or supplied an alternative) — this checkpoint is NOT auto-advanceable
    - The chosen tool is recorded in the SUMMARY with its npm/source URL
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with `npm i sharp --no-save`, or name an alternative tool.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author the coin-mark SVG and rasterize the three icon PNGs</name>
  <files>public/icon-512x512.png, public/icon-192x192.png, public/apple-touch-icon.png</files>
  <read_first>
    - .planning/phases/05.11-legacy-cleanup/05.11-UI-SPEC.md ("App Icon / Brand Mark — Full Visual Spec" + "Export matrix" — the authoritative geometry, colors, sizes, opacity rules)
    - lib/design/tokens.ts (confirm base.indigo=#6C5CE7, base.gold=#E9A83C, base.goldDeep=#C6871E, ink.bg=#141021 — the exact hex values to hardcode in the SVG)
    - CLAUDE.md ("Applying DB migrations" — the `npm i pg --no-save` scratch-script pattern to mirror with sharp)
  </read_first>
  <action>
    Write a one-off scratch script (in the session scratchpad, NOT committed) that:
    (1) Builds an SVG string, viewBox 0 0 512 512, with, in draw order:
        - a `<rect>` 0,0,512,512 fill #6C5CE7;
        - a `<circle>` cx=256 cy=256 r=169 fill #E9A83C (coin disc);
        - a `<circle>` cx=256 cy=256 r=161.5 fill none stroke #C6871E stroke-width 15 (rim);
        - a `<polygon>` 5-point star centered (256,256), outer radius 76, inner radius 38,
          first vertex pointing up (start angle −90°, 10 alternating outer/inner vertices at
          36° steps), fill #6C5CE7 (indigo negative-space cutout — same as background).
        Compute the 10 star points from the standard alternating-radius formula
        (x = 256 + r*cos(theta), y = 256 + r*sin(theta)); do not eyeball coordinates.
    (2) Runs `npm i sharp --no-save` (only after Task 1 approval), then for each target size
        renders the SVG buffer through sharp with `.flatten({ background: '#6C5CE7' })`
        (removes alpha → fully opaque), `.resize(size, size)`, `.png()`, and writes:
        512→public/icon-512x512.png, 192→public/icon-192x192.png, 180→public/apple-touch-icon.png.
    Overwrite the three existing PNGs in place. Do NOT commit the scratch script or the
    node_modules sharp install; only the three PNG files are part of this change.
  </action>
  <verify>
    <automated>file public/icon-192x192.png | grep -q "192 x 192" && file public/icon-512x512.png | grep -q "512 x 512" && file public/apple-touch-icon.png | grep -q "180 x 180" && ! file public/apple-touch-icon.png | grep -qi "RGBA" && ! file public/icon-512x512.png | grep -qi "RGBA"</automated>
  </verify>
  <acceptance_criteria>
    - `file public/icon-512x512.png` reports `512 x 512` and `RGB` (not `RGBA` — opaque, no alpha)
    - `file public/icon-192x192.png` reports `192 x 192` and `RGB`
    - `file public/apple-touch-icon.png` reports `180 x 180` and `RGB` (dimension corrected from the prior wrong 192x192)
    - All three differ from the pre-change bytes (`git diff --stat` shows all three changed)
    - The `icons` array in app/manifest.ts is unchanged (same 3 filenames/sizes/purposes)
    - No sharp entry appears in package.json or package-lock.json (`grep -c '"sharp"' package.json` returns 0)
    - Human visual pass (at phase verification): the rendered icon shows a gold coin with a rim and an indigo star cut-out, centered on indigo, legible at home-screen size
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update manifest.ts and layout.tsx to the locked start_url and brand colors</name>
  <files>app/manifest.ts, app/layout.tsx</files>
  <read_first>
    - app/manifest.ts (current stale fields: start_url '/dashboard', background_color '#0f0f23', theme_color '#6366f1')
    - app/layout.tsx (current viewport.themeColor '#6366f1' at ~line 26)
    - .planning/phases/05.11-legacy-cleanup/05.11-UI-SPEC.md ("Manifest / viewport metadata" locked-values table)
    - lib/design/tokens.ts (source of the new literals: base.indigo #6C5CE7, ink.bg #141021)
  </read_first>
  <action>
    In app/manifest.ts change exactly three scalar values: `start_url: '/dashboard'` → `'/'`;
    `background_color: '#0f0f23'` → `'#141021'`; `theme_color: '#6366f1'` → `'#6C5CE7'`. Leave
    name, short_name, description, display, orientation, and the entire `icons` array unchanged.
    Do NOT add an import of lib/design/tokens.ts — keep the existing hardcoded-literal
    convention, just update the literal values (per PATTERNS.md "Design-token sourcing" note).

    In app/layout.tsx change the single `viewport.themeColor` value `'#6366f1'` → `'#6C5CE7'`
    (matching manifest.ts theme_color). Leave the `metadata`/`appleWebApp` object and the three
    next/font/google calls untouched.
  </action>
  <verify>
    <automated>grep -q "start_url: '/'" app/manifest.ts && grep -q "theme_color: '#6C5CE7'" app/manifest.ts && grep -q "background_color: '#141021'" app/manifest.ts && grep -q "themeColor: '#6C5CE7'" app/layout.tsx && ! grep -q "#6366f1" app/manifest.ts app/layout.tsx && ! grep -q "'/dashboard'" app/manifest.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "#6366f1" app/manifest.ts app/layout.tsx` returns 0 (old brand color fully removed)
    - app/manifest.ts contains `start_url: '/'`, `theme_color: '#6C5CE7'`, `background_color: '#141021'`
    - app/layout.tsx contains `themeColor: '#6C5CE7'`
    - manifest.ts `name`/`short_name`/`icons` array are byte-unchanged
    - No `import` of lib/design/tokens.ts was added to either file
    - `npm run build` exits 0 and emits /manifest.webmanifest
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → npm registry | one-off `npm i sharp --no-save` pulls a build tool used only at authoring time |
| browser → static assets/config | PNG icons + manifest/viewport are static, served as-is; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.11-SC | Tampering (supply chain) | one-off `npm i sharp --no-save` | mitigate | No RESEARCH.md Package Legitimacy Audit table exists → sharp treated as [ASSUMED]; Task 1 is a blocking-human legitimacy checkpoint verifying sharp on npmjs.com before install (not auto-advanceable). Installed with `--no-save` so it never enters package.json/lockfile (acceptance criterion asserts `"sharp"` absent from package.json) — mirrors CLAUDE.md's blessed `pg --no-save` precedent. Only the 3 PNG outputs are committed. |
| T-05.11-05 | Information disclosure / Tampering | app/manifest.ts + app/layout.tsx edits | accept | Static config value changes only (three hex literals + start_url '/'). No user input, no new endpoint or data path. `start_url: '/'` resolves through the existing middleware role-based landing, which is already authentication-guarded — it does not expose any route that was previously gated. |
| T-05.11-06 | Tampering | regenerated PNG assets | accept | Static images with no executable content; served from /public. Flattened to opaque sRGB; no metadata/script payload. No security surface. |
</threat_model>

<verification>
- `file public/*.png` confirms 192x192, 512x512, and 180x180, all RGB (opaque, no alpha).
- `npm run build` emits /manifest.webmanifest with start_url '/', theme_color #6C5CE7, background_color #141021.
- `grep` confirms no residual `#6366f1` or `/dashboard` in manifest.ts / layout.tsx.
- Human visual pass (phase verification): installed PWA/home-screen icon shows the gold-coin-on-indigo mark; splash background is the dark ink color.
</verification>

<success_criteria>
- Three branded, correctly-sized, opaque icon PNGs (apple-touch corrected to 180x180) generated from one 512px master.
- manifest.ts start_url '/', theme_color #6C5CE7, background_color #141021; icons array unchanged.
- layout.tsx viewport.themeColor #6C5CE7.
- sharp not persisted to package.json; `npm run build` exits 0.
</success_criteria>

<output>
Create `.planning/phases/05.11-legacy-cleanup/05.11-03-SUMMARY.md` when done (record the
legitimacy-check outcome + tool URL, and note WalletDashboard.tsx as an out-of-scope future
cleanup candidate if relevant).
</output>
