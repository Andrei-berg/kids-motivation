---
phase: 05.11-legacy-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["05.11-01"]
files_modified:
  - app/globals.css
autonomous: true
requirements: ["05.11-SC2"]

must_haves:
  truths:
    - "globals.css contains no NAVIGATION-section classes (.nav/.navL/.navR and the .nav .brand rule) — they were used only by the deleted NavBar"
    - "Every class deleted from globals.css was proven (via repo grep) to have zero references in surviving app/ and components/ files"
    - "EXPENSES and WALLET-SYSTEM sections remain intact (still referenced by surviving screens / out-of-scope orphan component)"
    - "Every surviving page still builds and renders byte-identically (no visual regression)"
  artifacts:
    - path: "app/globals.css"
      provides: "reset + genuinely shared utilities with the dead NAVIGATION block removed"
      contains: "TYPOGRAPHY"
  key_links:
    - from: "app/globals.css"
      to: "surviving screens"
      via: "no class deleted that a surviving file references"
      pattern: "expense-page"
---

<objective>
Shrink `app/globals.css` (currently 4516 lines) by removing CSS classes that were used
ONLY by the now-deleted legacy pages — concretely the NAVIGATION section (`.nav`, `.navL`,
`.navR`, and the `.nav .brand` rule) that only `components/NavBar.tsx` consumed — plus any
other class a repo-wide grep proves is now dead. This is grep-verified, class-by-class
deletion, NOT section-by-section deletion.

Purpose: satisfy ROADMAP 5.11 Success Criterion 2 — globals.css shrinks to reset +
genuinely shared utilities, with no classes left that only the deleted legacy pages used.
Output: a smaller globals.css whose removals cause zero visual change to any surviving page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.11-legacy-cleanup/05.11-CONTEXT.md
@.planning/phases/05.11-legacy-cleanup/05.11-PATTERNS.md

<interfaces>
<!-- Verified during planning (grep + direct reads). Executor: trust and re-confirm. -->

Confirmed-dead (delete): the NAVIGATION section (globals.css ~line 571). Its classes
`.nav`, `.navL`, `.navR`, and the `.nav .brand` rule were used only by components/NavBar.tsx,
which Plan 01 deletes. NavBar's className set is exactly: nav, navL, navR, brand, muted
(`.muted` is shared — do NOT delete it; verify).

Confirmed NOT dead (do NOT touch as a side effect):
  - EXPENSES PAGE / EXPENSE MODAL / EXPENSE CHARTS sections (~lines 1795/2182/2456) — still
    referenced by components/parent-center/screens/ExpensesPanel.tsx, components/ExpenseModal.tsx,
    components/ExpenseCharts.tsx, components/settings/ActivitiesManager.tsx,
    components/DailyModal.tsx, components/settings/SectionsManager.tsx. Class-by-class only.
  - WALLET SYSTEM section (~line 2777, `.wallet-dashboard`/`.wallet-card*`) — only consumer
    is components/WalletDashboard.tsx, an orphan NOT on this phase's deletion list
    (CONTEXT.md scope boundary). Leave the CSS AND the component alone; may be noted as a
    future cleanup candidate in the SUMMARY, but must not become a change in this plan.
  - `.spinner` (LOADING & SPINNERS section) — wallboard used it but it is shared; keep.

deleted-legacy pages other than wallboard were 2-line redirect stubs that consumed no CSS,
so the practical dead-class surface is the NAVIGATION section plus anything grep proves dead.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep-verify and remove dead legacy-only classes from globals.css</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (read the NAVIGATION section at ~line 571 and the section-header comment convention; note the EXPENSES ~1795/2182/2456 and WALLET SYSTEM ~2777 sections you must NOT touch)
    - .planning/phases/05.11-legacy-cleanup/05.11-PATTERNS.md (the globals.css section table and the per-class grep methodology — this is the authoritative verification recipe)
    - .planning/phases/05.11-legacy-cleanup/05.11-CONTEXT.md (Claude's Discretion note: verify via full-repo grep per class, then npm run build + visual pass)
  </read_first>
  <action>
    Delete the NAVIGATION section (`.nav`, `.navL`, `.navR`, `.nav .brand`) from globals.css.
    Before deleting each selector, run a proof grep to confirm zero remaining references in
    surviving code: for each class name run
    `grep -rn "className=\"[^\"]*\b<class>\b" app components --include="*.tsx"` (and a broader
    `grep -rn "\b<class>\b" app components --include="*.tsx"` sanity pass). Only delete a rule
    whose class has ZERO hits outside files this phase already deleted. Specifically:
    - `.nav`, `.navL`, `.navR` → expect 0 hits after Plan 01 (NavBar deleted) → delete.
    - `.nav .brand` (the brand rule scoped under .nav) → delete only the .nav-scoped rule;
      if a bare `.brand` or `.muted` is referenced by any surviving file, KEEP those.
    - `.muted` → grep will show surviving hits → KEEP (shared utility).

    Do NOT delete anything in the EXPENSES sections, the WALLET SYSTEM section, or `.spinner`.
    Do NOT modify components/WalletDashboard.tsx (out of scope per CONTEXT.md).

    Optionally, for any OTHER class you grep-prove dead (zero references repo-wide in surviving
    tsx), you may remove it — but each removal requires the same zero-hit grep proof recorded
    in the SUMMARY. When uncertain, keep the rule (removals must never risk a surviving page).
  </action>
  <verify>
    <automated>test $(grep -cE '^\s*\.nav(L|R)?\b' app/globals.css) -eq 0 && grep -q "wallet-card" app/globals.css && grep -q "expense-page" app/globals.css && grep -q "\.spinner" app/globals.css && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "className=\"[^\"]*\bnav\b" app components --include="*.tsx"` returns zero className hits for `.nav`/`.navL`/`.navR` (proof they are dead), AND those selectors no longer appear as rule definitions in app/globals.css
    - EXPENSES sections still present: `grep -c "expense-page\|expense-modal\|expense-chart" app/globals.css` > 0
    - WALLET SYSTEM section still present: `grep -c "wallet-card\|wallet-dashboard" app/globals.css` > 0
    - `.spinner` and `.muted` rules still present in app/globals.css (shared utilities, not deleted)
    - components/WalletDashboard.tsx is unchanged (`git diff --quiet components/WalletDashboard.tsx`)
    - `app/globals.css` line count is strictly less than 4516 (the deletion actually happened)
    - `npm run build` exits 0
    - SUMMARY records, per deleted class, the zero-hit grep output that justified its removal
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits a static stylesheet only; no request, input, or data path crosses any boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.11-04 | Tampering (visual regression via over-deletion) | app/globals.css purge | mitigate | Every selector is deleted only after a repo-wide grep proves zero references in surviving app/ and components/ tsx files; EXPENSES and WALLET-SYSTEM sections are explicitly excluded and asserted-present by acceptance criteria; `npm run build` + visual pass gate the change. CSS-only, no logic/security surface. |
| T-05.11-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No package-manager install in this plan. Not applicable. |
</threat_model>

<verification>
- `npm run build` succeeds after the purge.
- Visual pass (human, at phase verification): load /parent-center, /parent/settings, /kid/day,
  /kid/wallet, /kid/shop — confirm no unstyled/broken elements introduced by the CSS removal.
- Assert the excluded sections (EXPENSES, WALLET SYSTEM, .spinner, .muted) still exist in globals.css.
</verification>

<success_criteria>
- NAVIGATION-section classes (.nav/.navL/.navR/.nav .brand) removed from globals.css.
- No class was deleted that any surviving app/ or components/ file references.
- EXPENSES + WALLET SYSTEM sections and shared utilities (.spinner, .muted) untouched.
- `npm run build` exits 0; globals.css line count decreased.
</success_criteria>

<output>
Create `.planning/phases/05.11-legacy-cleanup/05.11-02-SUMMARY.md` when done (include the
per-class grep proofs for every deleted selector).
</output>
