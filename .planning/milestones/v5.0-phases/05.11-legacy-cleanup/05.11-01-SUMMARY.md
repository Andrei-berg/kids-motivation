---
phase: 05.11-legacy-cleanup
plan: 01
subsystem: infra
tags: [nextjs, routing, redirects, middleware]

# Dependency graph
requires: []
provides:
  - "10 legacy single-family routes retired via next.config.js server-level redirects (permanent: true)"
  - "10 legacy page.tsx files + components/NavBar.tsx deleted from the repo"
  - "Post-login landing (middleware.ts else branch, app/page.tsx postAuthPath + auto-redirect) repointed from deleted /dashboard to /parent-center"
affects: [05.11-02, 05.11-03]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "next.config.js async redirects() as the canonical place for retired-route destinations (sibling to existing async headers())"

key-files:
  created: []
  modified:
    - next.config.js
    - middleware.ts
    - app/page.tsx
  # deleted (tracked separately from "modified" since they no longer exist):
  # app/dashboard/page.tsx, app/wallet/page.tsx, app/analytics/page.tsx, app/wallboard/page.tsx,
  # app/expenses/page.tsx, app/settings/page.tsx, app/streaks/page.tsx, app/records/page.tsx,
  # app/audit/page.tsx, app/coach-rating/page.tsx, components/NavBar.tsx

key-decisions:
  - "All 9 non-/settings legacy routes redirect to /parent-center; /settings uniquely redirects to /parent/settings (a real page, not another stub) per the verified interfaces table in the plan"
  - "middleware.ts else-branch fallback and both app/page.tsx /dashboard literals repointed to /parent-center rather than /parent/dashboard, per plan D-03"

patterns-established:
  - "Retired route destinations live in next.config.js redirects(), not in-app stub pages"

requirements-completed: ["05.11-SC1"]

# Metrics
duration: 25min
completed: 2026-07-23
---

# Phase 05.11 Plan 01: Legacy Route Retirement Summary

**Replaced 10 legacy in-app redirect-stub pages with server-level next.config.js redirects, deleted the stub files plus the now-orphaned NavBar component, and repointed the two remaining hardcoded /dashboard landing references to /parent-center.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-07-23T15:30:00Z (approx.)
- **Completed:** 2026-07-23T15:55:54Z
- **Tasks:** 2 completed
- **Files modified:** 3 (next.config.js, middleware.ts, app/page.tsx); 11 files deleted

## Accomplishments
- `next.config.js` gained a 10-entry `async redirects()` (all `permanent: true`): `/dashboard`, `/wallet`, `/analytics`, `/expenses`, `/streaks`, `/records`, `/audit`, `/coach-rating`, `/wallboard` → `/parent-center`; `/settings` → `/parent/settings`
- Deleted the 10 legacy `page.tsx` redirect-stub route files and their now-empty directories, plus the orphaned `components/NavBar.tsx` (sole consumer was the deleted `app/wallboard/page.tsx`)
- `middleware.ts` extended/unknown-role fallback and both `/dashboard` literals in `app/page.tsx` (`postAuthPath`, auto-redirect effect) now target `/parent-center` — the route the deleted stubs ultimately resolved to
- `npm run build` (full production build, all 40+ routes) and `npx tsc --noEmit` both pass cleanly

## Task Commits

Each task was committed atomically:

1. **Task 1: Add next.config.js redirects() and delete the 10 legacy route files + NavBar** - `fb55030` (feat)
2. **Task 2: Repoint the two deleted-/dashboard landing references to /parent-center** - `24f8287` (fix)

_No plan-metadata commit for STATE.md/ROADMAP.md — this is a worktree-isolated execution; the orchestrator updates those after merge._

## Files Created/Modified
- `next.config.js` - added `async redirects()` sibling to existing `async headers()`, 10 permanent redirect entries
- `middleware.ts` - else-branch fallback `url.pathname` changed from `/dashboard` to `/parent-center`
- `app/page.tsx` - `postAuthPath`'s `hasFamily` return arm and the auto-redirect effect's fallback arm changed from `/dashboard` to `/parent-center`
- `app/dashboard/page.tsx`, `app/wallet/page.tsx`, `app/analytics/page.tsx`, `app/wallboard/page.tsx`, `app/expenses/page.tsx`, `app/settings/page.tsx`, `app/streaks/page.tsx`, `app/records/page.tsx`, `app/audit/page.tsx`, `app/coach-rating/page.tsx` - deleted (2-line redirect stubs, except wallboard which was a 617-line real page cut with no replacement per plan D-01)
- `components/NavBar.tsx` - deleted (zero remaining importers after wallboard deletion)

## Decisions Made
- Followed the plan's verified interfaces table exactly for redirect destinations — no re-derivation needed, all stub targets and their downstream resolution had already been confirmed during planning.
- No deviations from the plan's file scope; `app/parent/settings/page.tsx` (the real page `/settings` now redirects to) was left untouched, confirmed present post-execution.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- `npm run build` initially failed in the fresh worktree with `Error: @supabase/ssr: Your project's URL and API key are required` because `.env.local` is git-ignored and not present in a freshly created worktree. This is a worktree-environment gap, not a defect introduced by this plan's changes (the same failure would occur on any plan run in a clean worktree without env vars). Resolved by copying `.env.local` from the main repo checkout into the worktree for the duration of the build verification, then deleting the copy before the final commit (confirmed via `git status --short` that `.env.local` never appeared as trackable/untracked — it matches the `.env*.local` gitignore pattern). No code change was needed; the build passed once env vars were present.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- All 10 legacy routes now resolve via static server-level redirects with no page code compiled for them; `05.11-SC1` ("One UI remains") route-retirement requirement is satisfied for this plan's scope.
- `components/NavBar.tsx`-specific CSS in `app/globals.css` (NAVIGATION section, `.nav`/`.navL`/`.navR` classes) was flagged as dead by `05.11-PATTERNS.md` but is explicitly out of this plan's `<files>` list (CSS purge is a separate task in a later plan of this phase) — not touched here.
- `components/WalletDashboard.tsx` was flagged by `05.11-PATTERNS.md` as an orphaned component (zero importers even before this plan) but was not named in this plan's file list — left untouched, flagged for a later plan/operator decision.
- Ready for the next wave/plan in phase 05.11 (icon/manifest/layout token refresh and CSS purge, per `05.11-PATTERNS.md`'s remaining sections).

---
*Phase: 05.11-legacy-cleanup*
*Completed: 2026-07-23*

## Self-Check: PASSED

- FOUND: `.planning/phases/05.11-legacy-cleanup/05.11-01-SUMMARY.md`
- FOUND: commit `fb55030` (Task 1)
- FOUND: commit `24f8287` (Task 2)
