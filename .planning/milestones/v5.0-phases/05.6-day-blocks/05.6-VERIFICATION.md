---
phase: 05.6-day-blocks
verified: 2026-07-15T10:00:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 2/4
  gaps_closed:
    - "Kid day form renders the assembled block list; flag-off families keep today's hardcoded forms byte-for-byte (D-07 parity) — CR-01"
    - "/api/wallet/award computes coins from the assembled blocks, keeping idempotency per (child_id, source_type, source_id) — CR-02"
  gaps_remaining: []
  regressions: []
deferred: []
human_verification: []
---

# Phase 5.6: day-blocks Verification Report

**Phase Goal:** The child's day is assembled from data — day type × schedule × block rules — not from hardcoded form sections.
**Verified:** 2026-07-15T10:00:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (plans 05.6-06, 05.6-07, 05.6-08)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `day_blocks` config per child carries type, enabled, day types, who fills, coin price, multipliers, schedule link, order (SC1) | ✓ VERIFIED | Unchanged since initial verification. `supabase/migrations/05.6-01-day-blocks.sql:18-55` defines `day_blocks` with `legacy_key`/custom name, `is_active`, `day_types TEXT[]`, `who_fills` (CHECK-constrained), `price`, `multipliers JSONB`, `schedule_link`, `sort_order`. Regression check: table/columns still present, `seed_default_day_blocks` (now guarded, see truth 3) still backfills all 10 default blocks per family — confirmed by direct read of `05.6-02-day-block-entries-hardening.sql:86-124` (body byte-identical to 05.6-01 plus the new guard). |
| 2 | Kid day form renders the assembled block list; on a vacation day school blocks disappear and schedule-driven blocks appear only on scheduled days; flag-off families keep today's forms unchanged (D-07 parity) | ✓ VERIFIED (previously FAILED, gap closed) | Flag-on path unchanged from initial verification (still works). **CR-01 closed:** `components/kid/KidDayFillForm.tsx:1078-1084` now renders a `FillSection` wrapping the shared `roomBody` constant, unconditionally, as the first item in the flag-off `<>` branch — confirmed by direct read: `{dayBlocksEnabled && visibleBlocks.length > 0 ? (...) : (<><FillSection title={t('kidFillForm.roomSection')} icon="🏠" sub={...}>{roomBody}</FillSection>{/* Extra activities */}...)}`. `roomBody` is the same constant used by `renderBuiltinBlock('room')` on the flag-on path — no duplicated logic, `handleSubmit`'s room dual-write is untouched and now receives correct kid-toggled state again. `npm run build` and `npx tsc --noEmit` both clean with this change applied. |
| 3 | `/api/wallet/award` computes coins from the assembled blocks, keeping idempotency per `(child_id, source_type, source_id)`; verify scripts pass (SC3) | ✓ VERIFIED (previously FAILED, gap closed) | **CR-02 closed at both layers.** Application layer: `app/api/wallet/award/route.ts:569-575` now keys `custom_block` intents on `` sourceId: `${date}:${entry.block_id}` `` (natural key, immune to `entry.id` UUID rotation) — confirmed by direct read, plus the entries query is scoped `.eq('family_id', member.familyId)` (route.ts:552) closing the cross-family read gap (WR-01 half). DB layer: `supabase/migrations/05.6-02-day-block-entries-hardening.sql` drops the `day_block_entries` DELETE policy (confirmed live: `pg_policies` query returned only INSERT/SELECT/UPDATE for `day_block_entries`, no DELETE row) and ties INSERT/UPDATE `WITH CHECK` to `child_id ∈ children WHERE family_id = day_block_entries.family_id`. A collision-guarded backfill rewrote previously-awarded `custom_block` `wallet_transactions.source_id` values to the new natural-key format so the format switch itself doesn't create a re-award window. **Live re-run of `scripts/verify-day-blocks-award.mjs` in this verification session** exercises the actual entry-rotation exploit (insert entry → award → delete+reinsert entry with new UUID → attempt second award) and confirms the second award is rejected with `23505` ("Entry-rotation double-credit rejected by unique index (23505) — CR-02 stays closed"). `verify-award-idempotency.mjs` and `verify-award-reads.mjs` also re-run clean. `npx tsc --noEmit` clean; `npm test` 62/62 passed (incl. `tests/day-blocks.test.ts` 15/15); `npm run build` clean. |
| 4 | Rollout behind a per-family feature flag: own family → beta families → all (SC4) | ✓ VERIFIED (with caveat, unchanged) | `families.day_blocks_enabled BOOLEAN NOT NULL DEFAULT false` remains the sole mechanism; unaffected by the gap-closure plans. `loadFeatureFlag`/`getFamilyDayBlocksEnabled` still the only readers; live check confirms the column still defaults `false` (verify script output: "families.day_blocks_enabled column present (sample value: false)"). No in-app UI writes this column — staged rollout is operator SQL only, matching the phase's explicit D-07 v1 scope decision. Not a gap. |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/migrations/05.6-01-day-blocks.sql` | day_blocks + day_block_entries tables, flag column, RLS, seed RPC, backfill | ✓ VERIFIED | Unchanged, applied live (re-confirmed this session). |
| `supabase/migrations/05.6-02-day-block-entries-hardening.sql` | Drop DELETE policy, tie INSERT/UPDATE WITH CHECK to child_id↔family_id, guard seed RPC, backfill source_id | ✓ VERIFIED | Applied live — confirmed via direct `pg_policies` query in this session: only INSERT/SELECT/UPDATE policies exist on `day_block_entries`, no DELETE. |
| `lib/models/day-block.types.ts` | DayBlock/DayBlockEntry/DayBlockLegacyKey types | ✓ VERIFIED | Unchanged since initial verification. |
| `lib/repositories/day-blocks.repo.ts` | reads, entries upsert, parent CRUD, flag read | ✓ VERIFIED | Unchanged since initial verification. |
| `lib/day-blocks.ts` | assembleDayBlocks + resolveBlockPrice + resolveMultiplier | ✓ VERIFIED | Unchanged; 15/15 unit tests still pass. |
| `app/api/wallet/_lib.ts` loadFeatureFlag | server-side flag read | ✓ VERIFIED | Unchanged. |
| `app/api/wallet/award/route.ts` | feature-flag branch + block-aware pricing + custom_block intents, safe idempotency key | ✓ VERIFIED | CR-02 fix confirmed at route.ts:569-575 (natural-key `sourceId`) and route.ts:552 (`family_id` scope on entries read). No longer "VERIFIED-BUT-VULNERABLE". |
| `components/kid/KidDayFillForm.tsx` | flag-on assembled renderer + flag-off parity | ✓ VERIFIED | CR-01 fix confirmed at lines 1078-1357: Room `FillSection` now renders unconditionally in the flag-off branch using the shared `roomBody` constant. No longer "PARTIALLY WIRED". |
| `components/DailyModal.tsx` | parallel assembled renderer, same assembly call | ✓ VERIFIED | Unchanged — never had the CR-01 regression (only the kid form did). |
| `components/settings/DayBlocksManager.tsx` | minimal per-family editor | ✓ VERIFIED | Unchanged, still wired into Settings.tsx `'blocks'` sub-tab. |
| `scripts/verify-day-blocks-award.mjs` | Idempotency verify script covering the actual exploit path | ✓ VERIFIED | Extended in Plan 08 with a self-cleaning entry-rotation probe; re-run live in this session, passes (see Probe Execution). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `lib/onboarding-api.ts createFamily` | `seed_default_day_blocks` RPC | non-fatal `supabase.rpc` | ✓ WIRED | Unchanged; Plan 08 confirmed (by direct read) that `createFamily()` upserts the creator's `family_members` row with `role='parent'` before calling the RPC, so the new WR-07 membership guard does not break onboarding. |
| `KidDayFillForm.tsx` (flag-off branch) | `roomBody` shared constant | direct render, no duplication | ✓ WIRED (newly fixed) | `roomBody` is referenced in exactly 3 places (definition, flag-on `renderBuiltinBlock('room')`, new flag-off `FillSection`) — confirmed by grep, no duplicated JSX/logic. |
| `KidDayFillForm.tsx` + `DailyModal.tsx` | `lib/day-blocks.ts assembleDayBlocks` | shared import | ✓ WIRED | Unchanged. |
| `app/api/wallet/award/route.ts` custom_block intents | `wallet_transactions.source_id` uniqueness | natural key `${date}:${entry.block_id}` | ✓ WIRED (newly fixed) | Confirmed at route.ts:569-575; live exploit probe proves the key survives entry-row UUID rotation. |
| `app/api/wallet/award/route.ts` entries read | `family_id` scope | `.eq('family_id', member.familyId)` | ✓ WIRED (newly fixed) | Confirmed at route.ts:552. |
| `day_block_entries` RLS | INSERT/UPDATE | `child_id ∈ children WHERE family_id = ...` WITH CHECK | ✓ WIRED (newly fixed) | Confirmed in migration file and via live `pg_policies` read. |
| `Settings.tsx` sub-tabs | `DayBlocksManager` | `sub === 'blocks'` render | ✓ WIRED | Unchanged. |

### Probe Execution

| Probe | Command | Result | Status |
|-------|---------|--------|--------|
| Day-blocks award idempotency incl. entry-rotation exploit path | `node --env-file=.env.local scripts/verify-day-blocks-award.mjs` | Re-run live in this verification session: literal-duplicate rejected (23505); rotation probe — entry deleted and re-inserted with new UUID, second natural-key award correctly rejected (23505) — "CR-02 stays closed", exit 0 | ✓ PASS |
| Legacy award idempotency | `node --env-file=.env.local scripts/verify-award-idempotency.mjs` | Re-run live, exit 0 | ✓ PASS |
| Award source reads | `node --env-file=.env.local scripts/verify-award-reads.mjs` | Re-run live, resolved room/behavior/grade/sport correctly, exit 0 | ✓ PASS |
| Live RLS check | `pg_policies` query on `day_block_entries` | Only INSERT/SELECT/UPDATE policies present, no DELETE row returned | ✓ PASS |
| Type check | `npx tsc --noEmit` | Clean, exit 0 | ✓ PASS |
| Day-blocks unit tests | `npx vitest run tests/day-blocks.test.ts` | 15/15 passed | ✓ PASS |
| Full test suite | `npm test` | 62 passed, 26 skipped (pre-existing skips, unrelated to this phase) | ✓ PASS |
| Production build | `npm run build` | Clean, all routes compiled including `/kid/day` | ✓ PASS |

### Anti-Patterns Found

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers in any file touched by the gap-closure plans (`components/kid/KidDayFillForm.tsx`, `app/api/wallet/award/route.ts`, `supabase/migrations/05.6-02-day-block-entries-hardening.sql`, `scripts/verify-day-blocks-award.mjs`) — confirmed by direct grep in this session.

The two prior Blocker-level findings (CR-01, CR-02) are closed — reconfirmed by direct source reading, not just SUMMARY claims. No new Blocker-level issues found.

The fresh code review (`05.6-REVIEW.md`, 2026-07-15) found 0 critical issues, 6 warnings, 7 info items on re-review. These are all pre-existing/newly-surfaced non-blocking issues, not phase-goal blockers:

| File | Line | Issue | Severity | Impact |
|------|------|-------|----------|--------|
| `supabase/migrations/05.6-01-day-blocks.sql:220-267` | interaction with 05.6-02 | Re-running migration 05.6-01 after 05.6-02 would silently revert the DELETE-policy drop and the seed-guard (WR-01 of the review, not a phase-goal blocker but an operational footgun) | ⚠️ Warning | Requires an explicit re-run of a superseded migration file; not triggered by normal operation. Recommend fixing before it bites. |
| `components/kid/KidDayFillForm.tsx:1074-1078` | vs `DailyModal.tsx:1282-1288` | Flag-on family with zero visible blocks falls back to the full legacy form in the kid form, but DailyModal shows an empty state — preview drift | ⚠️ Warning | Only affects flag-on families (currently just the operator's own family per Plan 08's SUMMARY); does not affect the flag-off default population. |
| `app/api/wallet/_lib.ts:87-94` | `loadFeatureFlag` | Swallows query errors, treating a transient DB error the same as "flag off" | ⚠️ Warning | Rare failure mode; does not affect default operation. |
| `components/settings/DayBlocksManager.tsx:107-120` | `saveVacationMultiplier` | Clearing the multiplier input does not clear the stored value | ⚠️ Warning | Parent-facing settings UX bug, not money-critical to the core flow. |
| `app/api/wallet/award/route.ts:72-86,319-325` | vs `day-blocks.repo.ts:22-61` | Inactive per-child override dedupe logic diverges between award route and client repo | ⚠️ Warning | Latent — per-child overrides are reserved for Phase 5.8; no UI creates them yet. |
| `scripts/verify-day-blocks-award.mjs:125-214` | rotation probe cleanup | Some `process.exit(1)` failure paths bypass cleanup of the synthetic probe block | ⚠️ Warning | Only triggers on probe failure itself (the probe currently passes); does not affect production data under normal (passing) operation. |

These warnings are pre-existing design tradeoffs or newly-surfaced edge cases scoped for future follow-up — none of them contradict a phase Success Criterion, and none affect the default flag-off population that represents every current family. They are noted here for traceability but do not block phase-goal achievement.

### Requirements Coverage

Not applicable — `phase_req_ids` is null for this phase, no requirement IDs are declared in any of the eight plans' `requirements:` frontmatter (all empty `[]`), and no entries in `.planning/REQUIREMENTS.md` reference phase 5.6. Confirmed by direct grep in this session (no matches for "Phase 5.6"/"Phase 05.6" in REQUIREMENTS.md).

### Human Verification Required

None. Both previously-blocking issues (CR-01, CR-02) were code- and DB-verifiable, and were independently reconfirmed in this session via direct source reading, a live RLS policy query, and live re-execution of the entry-rotation exploit probe — not just SUMMARY.md narration.

### Gaps Summary

No gaps remain. Both Critical findings from the initial verification (2026-07-14) are closed:

1. **CR-01 (Room checklist regression)** — closed by Plan 05.6-06. Directly confirmed: `KidDayFillForm.tsx`'s flag-off branch (the default for every existing family, since `day_blocks_enabled` defaults `false`) now renders the Room `FillSection` using the same `roomBody` constant as the flag-on path.
2. **CR-02 (money-minting vulnerability)** — closed at both the application layer (Plan 05.6-07: natural `${date}:${entry.block_id}` idempotency key, immune to row-UUID rotation) and the database layer (Plan 05.6-08: DELETE policy dropped from `day_block_entries`, so the rotation primitive itself is now unavailable to any client role; INSERT/UPDATE WITH CHECK ties `child_id` to `family_id`). The live re-run of `verify-day-blocks-award.mjs` in this verification session exercises the actual delete+reinsert exploit path (not just a literal-duplicate check) and confirms the second award attempt is rejected.

All four phase Success Criteria are now verified:
- SC1 (day_blocks config schema) — verified, unchanged since initial pass.
- SC2 (assembled block list, vacation-day filtering, flag-off parity) — verified; flag-off D-07 parity restored.
- SC3 (award computation + idempotency, verify scripts pass) — verified; idempotency now provably holds against the entry-rotation exploit, confirmed live.
- SC4 (per-family feature flag rollout) — verified, unchanged since initial pass (operator-SQL rollout per D-07 v1 scope).

Six Warning-level and seven Info-level findings remain from the fresh code review — none are phase-goal blockers, all are candidates for a future hardening/cleanup pass (most urgently WR-01: pin the migration file so a re-run of 05.6-01 cannot silently revert 05.6-02's hardening).

---

_Verified: 2026-07-15T10:00:00Z_
_Verifier: Claude (gsd-verifier)_
