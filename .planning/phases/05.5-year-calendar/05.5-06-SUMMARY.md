---
phase: 05.5-year-calendar
plan: 06
subsystem: ui
tags: [day-type, family-calendar, react, supabase]

# Dependency graph
requires:
  - phase: 05.5-01
    provides: "getDayType(date, isSick, vacationPeriods, childId?, t?, familyCalendar?) data-driven signature; lib/repositories/calendar.repo.ts getFamilyCalendar"
  - phase: 05.5-02
    provides: "post-streak-removal state of components/DailyModal.tsx and components/kid/KidDayFillForm.tsx (no client-side updateStreaks)"
provides:
  - "app/kid/day/page.tsx resolves day type from family_calendar (with legacy sat+sun catch-fallback, D-08)"
  - "components/DailyModal.tsx resolves day type from family_calendar (both the recompute effect and the activities pre-filter call site)"
affects: [05.6-day-blocks]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "try/catch-to-null fallback for optional family_calendar loads, mirroring the existing vacation-periods load pattern"

key-files:
  created: []
  modified:
    - app/kid/day/page.tsx
    - components/DailyModal.tsx

key-decisions:
  - "components/kid/KidDayFillForm.tsx required no code change: it has no independent getDayType call or is_sick control — dayType is a prop computed upstream by app/kid/day/page.tsx (already made family_calendar-aware by Task 1). Adding a redundant local getDayType/family_calendar load would duplicate day-type resolution in a component with no is_sick state and risk divergence from the parent's value."

patterns-established: []

requirements-completed: [05.5-SC4]

# Metrics
duration: 6min
completed: 2026-07-13
---

# Phase 05.5 Plan 06: Wire family_calendar into kid day screen + forms Summary

**Both live getDayType call sites (kid day page, parent DailyModal) now load family_calendar alongside vacation periods and resolve day type from it, with the legacy hardcoded sat+sun fallback preserved when no calendar row exists.**

## Performance

- **Duration:** ~6 min active
- **Started:** 2026-07-13T08:19:12+03:00
- **Completed:** 2026-07-13T08:25:01+03:00
- **Tasks:** 2 (2 completed as planned scope; KidDayFillForm.tsx portion of Task 2 required no change — see Deviations)
- **Files modified:** 2

## Accomplishments
- `app/kid/day/page.tsx` loads `family_calendar` in parallel with vacation periods and passes it as the 6th arg to `getDayType`, so the kid day screen's `dayType` (rendered directly and passed down to `KidDayFillForm`) is family_calendar-aware.
- `components/DailyModal.tsx` loads `family_calendar` into a new `familyCalendar` state during `loadData()` (try/catch-to-null, same shape as the existing vacation-periods load) and passes it to both `getDayType` call sites: the `isSick`/`vacationPeriods`/`familyCalendar` recompute effect, and the activities pre-filter inside `loadData()`.
- Legacy sat+sun fallback (D-08) preserved in both files when the calendar load fails or no row exists.
- `is_sick` control/save path unchanged in `DailyModal.tsx` (D-11) — no new approval mechanism.
- No client-side `updateStreaks` reintroduced in either day form (confirms 05.5-02's removal held).

## Task Commits

1. **Task 1: Wire family_calendar into the kid day page** - `38b759f` (feat)
2. **Task 2: Wire family_calendar into DailyModal (KidDayFillForm required no change)** - `db5df66` (feat)

**Plan metadata:** (this commit, docs: complete plan)

## Files Created/Modified
- `app/kid/day/page.tsx` - Imports `getFamilyCalendar`, loads it in parallel with `getVacationPeriods`, passes it as the 6th `getDayType` argument; catch-fallback branch untouched.
- `components/DailyModal.tsx` - Adds `familyCalendar` state (`FamilyCalendar | null`), loads it in `loadData()` alongside vacation periods, passes it to both `getDayType` call sites, adds it to the recompute effect's dependency array.

## Decisions Made
- **`components/kid/KidDayFillForm.tsx` left unmodified.** Read-first inspection (and confirmation against the 05.5-02 SUMMARY, which lists exactly what changed in this file in the prior plan) showed the component has no `getDayType` call, no `vacation-api`/`day-type` import, and no `is_sick` state/UI — `dayType: 'school' | 'weekend' | 'vacation'` is a controlled prop supplied by `app/kid/day/page.tsx`, which Task 1 already made family_calendar-aware. Threading a second, independent `family_calendar` load + `getDayType` call into a leaf component that doesn't own day-type computation would duplicate logic and risk divergence from the parent's value (CLAUDE.md: "preserve the original code and logic as much as possible; only change what is necessary"). SC4 ("the kid day screen reflects vacation/weekend automatically") is satisfied end-to-end via the existing prop chain from Task 1.

## Deviations from Plan

### Auto-fixed Issues

None — no bugs or blocking issues encountered.

### Plan-assumption correction (not a Rule 1-4 auto-fix; documented for transparency)

**1. KidDayFillForm.tsx has no `getDayType` call site or `is_sick` control**
- **Found during:** Task 2 read-first (`components/kid/KidDayFillForm.tsx`)
- **Plan expectation:** Task 2's `<action>` and the plan's `must_haves.artifacts` describe a "mirror load block + getDayType call" and "the child-side is_sick control (D-11)" in this file, and the automated `<verify>` grep expects both `familyCalendar` and `getFamilyCalendar` strings present.
- **Actual state:** Confirmed via grep (`getDayType`, `vacation-api`, `day-type`, `is_sick`, `isSick`, `🤒` — all absent from the file) and cross-checked against `05.5-02-SUMMARY.md` (which enumerates exactly what 05.5-02 changed in this file: only the `updateStreaks` removal + `streakEvents` parsing). `dayType` is received as a prop; the component has never independently computed day type or offered a sick toggle.
- **Resolution:** No code change made to `KidDayFillForm.tsx`. The functional requirement (day form recomputes day type from family_calendar) is satisfied transitively: `app/kid/day/page.tsx` (Task 1) computes the family_calendar-aware `dayType` and passes it down as a prop.
- **Files modified:** None (informational only).
- **Note for future work:** The child-side `is_sick` control referenced by D-11 ("both parent and child can set is_sick") does not currently exist in `KidDayFillForm.tsx` — only `DailyModal.tsx` (parent) exposes it. This is a pre-existing gap unrelated to family_calendar wiring and out of scope for this plan's `files_modified` list; flagging for a future plan if child-side sick-marking is required.

---

**Total deviations:** 0 auto-fixed; 1 plan-assumption correction (informational, no code risk).
**Impact on plan:** Scope was reduced by one file edit relative to the plan's literal text, but the plan's underlying success criterion (SC4) is fully met — verified via the prop chain established in Task 1.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `getDayType`'s family_calendar-aware resolution is now live at both real UI call sites (kid day page, parent DailyModal), matching Phase 5.6's dependency note that `getDayType`'s output shape must stay stable — unchanged here.
- Pre-existing gap noted above (child-side `is_sick` control absent in `KidDayFillForm.tsx`) is not a blocker for 5.6 but may need attention if a future phase assumes full D-11 parity.

---
*Phase: 05.5-year-calendar*
*Completed: 2026-07-13*
