---
phase: 05.5-year-calendar
plan: 05
type: execute
wave: 3
depends_on: [05.5-01, 05.5-03, 05.5-04]
files_modified:
  - components/settings/PeriodsManager.tsx
  - public/locales/en.json
  - public/locales/ru.json
autonomous: true
requirements: [05.5-SC2]

must_haves:
  truths:
    - "A parent can pick a region/term-mode preset and apply it, materializing vacation rows into vacation_periods (D-01, D-02)"
    - "When preset rows already exist, applying again prompts Replace-preset-periods vs Add-missing-only (D-04)"
    - "Preset-sourced rows (non-null preset_id) are visually flagged distinct from manual rows (D-02)"
    - "The old inline PRESETS quick-add block is replaced; manual add/edit/delete stays fully functional (D-01)"
    - "Applying a preset also records the chosen region on family_calendar.region_preset so the Settings→Calendar region select (Plan 04) reflects the same choice — the two region pickers stay in sync, not disconnected (WARNING-1 resolution)"
  artifacts:
    - path: "components/settings/PeriodsManager.tsx"
      provides: "real preset picker + replace/add-missing dialog + preset badge"
      contains: "applyPreset"
  key_links:
    - from: "components/settings/PeriodsManager.tsx"
      to: "lib/vacation-presets.ts"
      via: "listPresets + applyPreset"
      pattern: "applyPreset\\("
    - from: "components/settings/PeriodsManager.tsx"
      to: "lib/repositories/calendar.repo.ts"
      via: "upsertFamilyCalendar(familyId, { region_preset })"
      pattern: "upsertFamilyCalendar"
---

<objective>
Replace the ad hoc inline `PRESETS` quick-add block in PeriodsManager with a real region/term-mode preset picker backed by `lib/vacation-presets.ts`. Applying a preset materializes marked rows; re-applying prompts Replace vs Add-missing (D-04). Preset-sourced rows get a visual badge so they read differently from manual rows. Manual add/edit/delete stays first-class. Applying a preset ALSO upserts the chosen region onto `family_calendar.region_preset` via `upsertFamilyCalendar`, so the Settings→Calendar region select (Plan 04, which reads/writes the same field) and this preset picker stay in sync rather than being two disconnected region controls (WARNING-1 resolution).

Purpose: Delivers the UI half of SC2 (choosing a region fills vacations; manual value wins).

Output: modified `PeriodsManager.tsx` + preset-picker i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05.5-year-calendar/05.5-CONTEXT.md
@.planning/phases/05.5-year-calendar/05.5-PATTERNS.md

<interfaces>
From 05.5-03 (lib/vacation-presets.ts):
  listPresets(): VacationPreset[]   // { id, region, label, termMode, periods }
  applyPreset(familyId, presetId, mode: 'replace' | 'add-missing'): Promise<number>
From 05.5-03 (lib/vacation-api.ts): VacationPeriod now has preset_id?: string | null
From 05.5-01 (lib/repositories/calendar.repo.ts): upsertFamilyCalendar(familyId, fields) — used here to sync region_preset on preset apply
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline templates with the preset picker + dialog + badge</name>
  <files>components/settings/PeriodsManager.tsx, public/locales/en.json, public/locales/ru.json</files>
  <read_first>
    - components/settings/PeriodsManager.tsx (full file: the inline PRESETS const ~lines 13-19, the Templates quick-add block ~lines 268-316 to replace, the list render ~lines 179-198 for the badge, and handleDelete ~line 135 using confirm() — reuse that confirm() convention for the D-04 dialog)
    - lib/vacation-presets.ts (listPresets/applyPreset — created in 05.5-03)
    - lib/vacation-api.ts (VacationPeriod.preset_id — created in 05.5-03)
    - lib/repositories/calendar.repo.ts (upsertFamilyCalendar — created in 05.5-01; used to sync region_preset)
    - lib/store.ts (useAppStore familyId)
    - components/settings/RoomTasksManager.tsx (the 🔒 lock-badge render at ~lines 232-236 — mirror this badge style for preset-sourced rows)
    - public/locales/en.json, public/locales/ru.json (existing settings.periodsManager.* keys to extend)
  </read_first>
  <action>
    Remove the inline `PRESETS` const and its Templates quick-add block. In its place render a preset picker: a `<select>` (or button row) sourced from `listPresets()` showing region + term-mode label (make clear it is a типовой/template schedule per the CONTEXT specifics note — actual dates vary, editing expected). On selecting a preset and confirming, determine the mode: if any existing row already has this preset's `preset_id`, use the D-04 confirm-dialog convention (reuse the existing `confirm(...)` pattern from `handleDelete`, not a new modal) to ask Replace-preset-periods vs Add-missing-only — map the two choices to `applyPreset(familyId, presetId, 'replace' | 'add-missing')`; if no rows for this preset exist yet, apply directly as `'add-missing'` (equivalent to a fresh add). After apply, also call `upsertFamilyCalendar(familyId, { region_preset: presetId })` (import from `@/lib/repositories/calendar.repo`) so the chosen region is recorded on `family_calendar` and the Settings→Calendar region select (Plan 04) reflects the same selection — the two region pickers must not diverge (WARNING-1). Then reload the period list. Show the returned inserted-count in a transient status line.

    In the list render, flag preset-sourced rows (`period.preset_id != null`) with a small badge mirroring RoomTasksManager's lock-badge style (e.g. a 📅/preset chip) so preset rows read differently from manual rows (D-02). Manual add/edit/delete controls stay exactly as-is (D-01) — do not gate editing/deleting of preset rows (manual edits win by value; deleting a preset row is allowed).

    Add the new keys under `settings.periodsManager.*` (or a `presetPicker` sub-object) in both en.json and ru.json: picker label, the "template schedule" hint, the Replace-vs-Add-missing confirm text (two prompts or one confirm mapping), the applied-count status, and the preset-badge label. Keep both JSON files valid.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "PeriodsManager" || echo "TYPECHECK_CLEAN"; node -e "JSON.parse(require('fs').readFileSync('public/locales/en.json','utf8'));JSON.parse(require('fs').readFileSync('public/locales/ru.json','utf8'));console.log('JSON_VALID')"; grep -q "applyPreset(" components/settings/PeriodsManager.tsx && grep -q "listPresets" components/settings/PeriodsManager.tsx && grep -q "preset_id" components/settings/PeriodsManager.tsx && grep -q "upsertFamilyCalendar" components/settings/PeriodsManager.tsx && ! grep -q "const PRESETS = \[" components/settings/PeriodsManager.tsx && echo "PICKER_OK"</automated>
  </verify>
  <acceptance_criteria>
    - The inline `const PRESETS = [` block is gone (`grep` returns nothing for it).
    - PeriodsManager calls `applyPreset` with `'replace'` and `'add-missing'` reachable via the confirm-dialog branch when preset rows already exist (D-04).
    - Preset-sourced rows (`preset_id != null`) render a distinct badge; manual rows do not.
    - Manual add/edit/delete controls remain present and functional (D-01).
    - On preset apply, `upsertFamilyCalendar(familyId, { region_preset: presetId })` is called so `family_calendar.region_preset` matches the applied preset (WARNING-1: the Settings→Calendar region select reads the same field).
    - Both locale files parse; `npm run build` succeeds.
  </acceptance_criteria>
  <done>PeriodsManager applies real presets with replace/add-missing, flags preset rows, and preserves manual entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent (browser) → vacation_periods | Preset apply performs client-side bulk insert/delete under family-isolation RLS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-14 | Tampering | replace-mode delete via UI | mitigate | The delete is executed by `applyPreset` (05.5-03), scoped to `family_id` + `preset_id`; the UI cannot broaden it. Manual rows (preset_id NULL) are never deleted by replace (D-04). |
| T-05.5-15 | Denial of Service | repeated preset apply | accept | Bulk insert bounded by the finite bundled preset period count; re-apply in replace mode is idempotent-ish (delete-then-insert), add-missing dedups. No unbounded growth. |
| T-05.5-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; N/A. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds.
- Locale files valid; inline PRESETS removed; applyPreset wired.
</verification>

<success_criteria>
- Region preset picker applies vacations with replace/add-missing (D-01, D-02, D-04).
- Preset rows visually flagged; manual entry first-class (D-01, D-02).
</success_criteria>

<output>
Create `.planning/phases/05.5-year-calendar/05.5-05-SUMMARY.md` when done.
</output>
