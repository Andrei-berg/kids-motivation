---
status: partial
phase: 05.10-automation
source: [05.10-VERIFICATION.md]
started: 2026-07-23T05:18:01Z
updated: 2026-07-23T05:18:01Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Trust Limit persists through the real Settings UI
expected: Set a per-child Trust Limit in Parent Center → Settings → Children, save, reload the tab — the saved value (e.g. 30) is shown after reload.
result: [pending]

### 2. Trust-limit auto-approve journal label renders correctly
expected: As a child priced at/under a saved (>0) Trust Limit, buy a reward NOT flagged auto_approve. It does NOT appear in the parent pending queue and DOES appear in the parent Dashboard Activity feed with the suffix "· авто (лимит доверия)".
result: [pending]

### 3. Trust Limit default-off enforcement through the real purchase flow
expected: Set Trust Limit back to 0 — a new sub-threshold purchase now goes to the pending queue (not auto-approved).
result: [pending]

### 4. Allowance-cron idempotency via real curl invocations
expected: Configure an Allowance in the Settings UI, then curl /api/cron/daily with a valid CRON_SECRET bearer twice in a row — the child's wallet gains the allowance once; the second call does not double-credit. (Needs a dev CRON_SECRET provisioned first.)
result: [pending]

### 5. Real device push delivery
expected: Register a real push subscription (VAPID) from a device/browser, then trigger /api/cron/daily — the device actually receives a section/unfilled-day/streak-at-risk notification. Note: push_subscriptions currently has 0 rows in prod since no device has subscribed yet (delivery path was broken until this gap-closure pass); this check now validates true end-to-end delivery, not a known-broken dependency.
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps
