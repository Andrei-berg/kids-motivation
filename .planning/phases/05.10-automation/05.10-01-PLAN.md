---
phase: 05.10-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/05.10-01-trust-limit-allowance.sql
  - app/api/wallet/_lib.ts
  - tests/integration/purchase.test.ts
autonomous: true
requirements: [SC2]
must_haves:
  truths:
    - "A coin purchase priced at or below the child's trust_limit_coins auto-approves without entering the pending queue"
    - "A family that never sets a trust limit (default 0) gets zero trust-limit auto-approvals"
    - "The parent journal shows WHY a purchase auto-approved: trust limit vs instant reward"
  artifacts:
    - path: "supabase/migrations/05.10-01-trust-limit-allowance.sql"
      provides: "Per-child trust_limit_coins + allowance_amount/period/anchor columns"
      contains: "trust_limit_coins"
    - path: "app/api/wallet/_lib.ts"
      provides: "processPurchase() trust-limit OR-combine + distinct processed_by marker"
      contains: "trust_limit_coins"
  key_links:
    - from: "app/api/wallet/_lib.ts processPurchase()"
      to: "children.trust_limit_coins"
      via: "server-side admin select at purchase time"
      pattern: "trust_limit_coins"
    - from: "app/api/wallet/_lib.ts processPurchase()"
      to: "reward_purchases.processed_by"
      via: "distinct 'auto_trust' marker on trust-limit auto-approval"
      pattern: "auto_trust"
---

<objective>
Deliver the trust-limit auto-approve engine (SC2) and the shared schema both
SC1 (allowance) and SC2 (trust limit) build on. A purchase priced at or below a
child's per-child trust limit skips the parent approval queue; the parent still
sees it in the journal, labelled with WHY it was auto-approved.

Purpose: SC2 — "Purchases under a per-child trust limit auto-approve; the parent
sees them in the journal." Also lands the migration all downstream plans need.
Output: migration file + applied schema (4 columns on `children`) + extended
`processPurchase()` + integration coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.10-automation/05.10-CONTEXT.md
@.planning/phases/05.10-automation/05.10-PATTERNS.md
@CLAUDE.md

<interfaces>
From app/api/wallet/_lib.ts — the existing function to EXTEND (do not restructure):
- `processPurchase(admin, familyId, childId, rewardId)` — loads reward, loads
  wallet, computes `priceCoins = isCoins ? reward.price_coins||0 : 0`, currently
  sets `const autoApprove = reward.auto_approve === true`, debits atomically with
  a 0 floor, then inserts a `reward_purchases` row with
  `status: autoApprove ? 'approved' : 'pending'` and, on auto-approve,
  `processed_by: 'auto', processed_at: <iso>`. When `priceCoins > 0` it also
  inserts a `wallet_transactions` row with `description: ` + backtick
  `Куплено: ${reward.title}` + backtick.

Migration analog — supabase/migrations/2026-06-22-reading-verification.sql:
  uses `ALTER TABLE children ADD COLUMN IF NOT EXISTS ... DEFAULT ...` for a
  per-child additive column. No idempotency index is needed: the existing
  `uniq_wallet_tx_source` on `(child_id, source_type, source_id)` from
  04.4-02-wallet-tx-idempotency.sql already covers any `source_type`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the trust-limit + allowance migration</name>
  <files>supabase/migrations/05.10-01-trust-limit-allowance.sql</files>
  <read_first>
    - supabase/migrations/2026-06-22-reading-verification.sql (per-child additive-column DEFAULT idiom)
    - supabase/migrations/04.4-02-wallet-tx-idempotency.sql (confirm the existing unique index already covers source_type='allowance' — no new index here)
  </read_first>
  <action>
    Create the migration adding four additive columns to `children`, all
    `IF NOT EXISTS`:
    - `trust_limit_coins INTEGER NOT NULL DEFAULT 0` — per-child auto-approve
      ceiling; 0 = disabled (D-01/D-04, opt-in only).
    - `allowance_amount INTEGER` — nullable; coins credited per period (D-05).
    - `allowance_period TEXT` — nullable; expected values `'weekly'` | `'monthly'`
      | NULL (NULL = allowance off) (D-06/D-08). Postgres has no
      `ADD CONSTRAINT IF NOT EXISTS`, so the CHECK constraint MUST be inlined
      directly on the `ADD COLUMN IF NOT EXISTS allowance_period TEXT CHECK
      (allowance_period IN ('weekly','monthly'))` clause itself, not added as
      a separate `ALTER TABLE ... ADD CONSTRAINT` statement — that way a
      re-run of the migration skips the whole clause (column already exists)
      instead of erroring on a duplicate constraint.
    - `allowance_anchor INTEGER` — nullable; day-of-week 1..7 for weekly or
      day-of-month 1..31 for monthly (D-06).
    Include a top-of-file comment block stating: default 0 = trust-limit
    disabled; NULL allowance_period = allowance off; no backfill needed. Do NOT
    add any idempotency index (the existing uniq_wallet_tx_source already
    covers `source_type='allowance'`).
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS" supabase/migrations/05.10-01-trust-limit-allowance.sql | grep -qx 4 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly 4 `ADD COLUMN IF NOT EXISTS` statements naming
      `trust_limit_coins`, `allowance_amount`, `allowance_period`, `allowance_anchor`.
    - `trust_limit_coins` line contains `NOT NULL DEFAULT 0`.
    - A CHECK constraint restricts `allowance_period` to `'weekly'`, `'monthly'`, or NULL.
    - The CHECK constraint is inlined on the same `ADD COLUMN IF NOT EXISTS allowance_period` clause (no standalone `ALTER TABLE ... ADD CONSTRAINT` statement) so re-applying the file is a no-op once the column exists.
    - No `CREATE ... INDEX` statement is present.
  </acceptance_criteria>
  <done>Migration file exists with the four columns and the period CHECK constraint.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Apply the migration to the live DB</name>
  <files>supabase/migrations/05.10-01-trust-limit-allowance.sql</files>
  <read_first>
    - CLAUDE.md §Commands → "Applying DB migrations" (the exact `pg` invocation)
  </read_first>
  <action>
    Apply the migration to the live database. `SUPABASE_DB_URL` IS present in
    `.env.local` (confirmed at plan time), so run the project's standard `pg`
    apply command, substituting this migration's filename:
    `node --env-file=.env.local -e 'import("pg").then(async({default:pg})=>{const fs=await import("fs");const c=new pg.Client({connectionString:process.env.SUPABASE_DB_URL});await c.connect();await c.query(fs.readFileSync("supabase/migrations/05.10-01-trust-limit-allowance.sql","utf8"));console.log("applied");await c.end()})'`
    This task is BLOCKING and MANDATORY: TS types in this project do NOT come
    from the live schema, so build/tsc pass even if the columns are absent —
    Task 3's trust-limit read and Plans 03/04 would silently misbehave against a
    schema that lacks the columns. If `SUPABASE_DB_URL` is unexpectedly missing
    at execution time, STOP and apply the SQL manually via the Supabase SQL
    Editor before continuing (and record that the plan ran non-autonomously).
  </action>
  <verify>
    <automated>node --env-file=.env.local -e 'import("pg").then(async({default:pg})=>{const c=new pg.Client({connectionString:process.env.SUPABASE_DB_URL});await c.connect();const r=await c.query("select column_name from information_schema.columns where table_name=$1 and column_name in ($2,$3,$4,$5)",["children","trust_limit_coins","allowance_amount","allowance_period","allowance_anchor"]);console.log(r.rows.length);process.exit(r.rows.length===4?0:1)})'</automated>
  </verify>
  <acceptance_criteria>
    - The information_schema query returns all 4 column names (exit 0).
    - Re-running the apply command is a no-op (IF NOT EXISTS) — no error.
  </acceptance_criteria>
  <done>All four columns exist on `children` in the live DB.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend processPurchase with trust-limit OR-combine + distinct marker</name>
  <files>app/api/wallet/_lib.ts, tests/integration/purchase.test.ts</files>
  <read_first>
    - app/api/wallet/_lib.ts (processPurchase, lines ~218-294 — the exact insert + tx shapes to extend)
    - tests/integration/purchase.test.ts (existing mock/fixture pattern for the purchase flow)
    - tests/integration/family-fixture.ts (createTestFamily / serviceClient helpers)
  </read_first>
  <behavior>
    - Purchase priced ≤ child.trust_limit_coins (limit > 0) → status 'approved', processed_by 'auto_trust'.
    - Purchase priced > trust_limit_coins, reward.auto_approve false → status 'pending', no processed_by.
    - reward.auto_approve true (regardless of price/limit) → status 'approved', processed_by 'auto' (existing behavior unchanged).
    - Family with trust_limit_coins = 0 (default) and reward.auto_approve false → status 'pending' (no trust auto-approval).
  </behavior>
  <action>
    In `processPurchase()`, after loading `wallet`, fetch the child's trust limit
    server-side: `admin.from('children').select('trust_limit_coins').eq('id', childId).maybeSingle()`,
    `const trustLimit = childRow?.trust_limit_coins ?? 0`. NEVER accept the limit
    or priceCoins from the request body — read both server-side (D-02, threat
    T-0510-01). Replace the single `autoApprove` line with:
    `const rewardAutoApprove = reward.auto_approve === true`;
    `const trustAutoApprove = trustLimit > 0 && priceCoins > 0 && priceCoins <= trustLimit`;
    `const autoApprove = rewardAutoApprove || trustAutoApprove` (D-02 additive OR —
    nothing existing changes behavior). On the `reward_purchases` insert, set
    `processed_by` to `'auto_trust'` when `trustAutoApprove && !rewardAutoApprove`,
    otherwise `'auto'` (keep `processed_at` on any auto-approve) (D-03).
    For the journal distinction (D-03), build the `wallet_transactions`
    description as the base `Куплено: ${reward.title}` plus a suffix:
    ` · авто (лимит доверия)` when trust-only auto-approve, ` · авто (мгновенная
    награда)` when reward-flag auto-approve, and no suffix when pending. This
    surfaces WHY in the existing parent Dashboard Activity feed (built from
    wallet_transactions) with no UI change. Add integration cases to
    purchase.test.ts covering all four rows in <behavior> — seed
    `trust_limit_coins` on the test child via the service client.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- purchase</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - New purchase.test.ts cases assert: sub-limit purchase → status 'approved' AND processed_by 'auto_trust'; over-limit non-flagged → 'pending'; trust_limit 0 default → 'pending'; reward.auto_approve → 'approved' with processed_by 'auto'.
    - Grep confirms `processPurchase` reads `trust_limit_coins` via the admin client, not from a request parameter.
  </acceptance_criteria>
  <done>All purchase.test.ts cases green; the OR-combine and distinct marker land additively.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kid/parent client → /api/wallet/purchase (+ requestPurchase action) | untrusted body: childId, rewardId. priceCoins and trust limit must be resolved server-side only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0510-01 | Tampering/Elevation | processPurchase trust-limit check | mitigate | Read `children.trust_limit_coins` and `reward.price_coins` server-side via admin client; never accept limit/price from request body — a client cannot inflate its own ceiling |
| T-0510-02 | Elevation | Default trust limit | mitigate | Column DEFAULT 0 + guard `trustLimit > 0`; a family that never opts in gets zero trust auto-approvals (D-04 enforced by schema + code, not just docs) |
| T-0510-03 | Repudiation | Auto-approval provenance | mitigate | Distinct `processed_by='auto_trust'` + description suffix records WHY each purchase skipped review, visible in the journal (D-03) |
| T-0510-04 | Tampering | Money mutation path | accept | Debit stays server-side via existing `applyWalletDelta`/`wallet_apply` with 0-floor; RLS keeps money tables SELECT-only for clients (no new surface) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm test -- purchase` green (existing + new trust-limit cases).
- Live-DB column check returns 4.
</verification>

<success_criteria>
- Sub-limit coin purchase auto-approves with `processed_by='auto_trust'`; default-0 family never trust-auto-approves; journal shows the reason. (SC2 engine + journal.)
- Schema for all four columns applied to prod, unblocking Plans 03/04.
</success_criteria>

<output>
Create `.planning/phases/05.10-automation/05.10-01-SUMMARY.md` when done.
</output>
