---
phase: 05.1-launch-prep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [app/layout.tsx, app/manifest.ts, README.md]
autonomous: true
requirements: [LP-SC4]

must_haves:
  truths:
    - "The installed PWA name shows as FamilyCoins (manifest name + short_name)"
    - "The browser tab / document title reads FamilyCoins"
    - "The README title reads FamilyCoins"
  artifacts:
    - path: "app/manifest.ts"
      provides: "PWA name FamilyCoins"
      contains: "name: 'FamilyCoins'"
    - path: "app/layout.tsx"
      provides: "Document metadata title FamilyCoins"
      contains: "FamilyCoins"
    - path: "README.md"
      provides: "Project title FamilyCoins"
      contains: "FamilyCoins"
  key_links:
    - from: "app/layout.tsx metadata.title"
      to: "browser tab / installed app title"
      via: "Next.js Metadata API"
      pattern: "title:\\s*'FamilyCoins'"
---

<objective>
Make the product name FamilyCoins consistent everywhere user-visible: the PWA manifest,
the document metadata, and the README title.

Purpose: The app has been rebranded to FamilyCoins; user-visible strings must not read the
legacy "Kids Motivation". package.json `name` (`kids-motivation-v2`) stays as-is — it is
private and not user-visible (per CONTEXT: Claude's discretion).
Output: Manifest, layout metadata, and README all say FamilyCoins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.1-launch-prep/05.1-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Set FamilyCoins in document metadata and confirm the manifest</name>
  <files>app/layout.tsx, app/manifest.ts</files>
  <read_first>
    - app/layout.tsx — current `metadata` export (title is `'FamilyCoins — Kids Motivation'`, appleWebApp.title already `'FamilyCoins'`)
    - app/manifest.ts — current manifest (name and short_name are already `'FamilyCoins'`; description is `'Family motivation app for children'`)
  </read_first>
  <action>In app/layout.tsx set `metadata.title` to exactly `'FamilyCoins'` (drop the "— Kids Motivation" suffix) and set `metadata.description` to a FamilyCoins-branded sentence (e.g. mentions FamilyCoins as a family motivation app for children). Leave `appleWebApp.title` (already 'FamilyCoins'), `manifest`, `viewport`, and the component body untouched. In app/manifest.ts confirm `name` and `short_name` are `'FamilyCoins'` (already true) — no change needed unless they drift; leave `start_url`, icons, and colors untouched. Do not restyle or refactor anything else.</action>
  <verify>
    <automated>grep -q "title: 'FamilyCoins'" app/layout.tsx && grep -q "name: 'FamilyCoins'" app/manifest.ts && grep -q "short_name: 'FamilyCoins'" app/manifest.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `app/layout.tsx` contains `title: 'FamilyCoins'` (exact, no legacy suffix)
    - `app/layout.tsx` description mentions FamilyCoins and contains no "Kids Motivation" string
    - `app/manifest.ts` has `name: 'FamilyCoins'` and `short_name: 'FamilyCoins'`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Metadata title is FamilyCoins; manifest confirmed FamilyCoins; type check passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rebrand the README title to FamilyCoins</name>
  <files>README.md</files>
  <read_first>
    - README.md — current H1 is `# 🚀 Kids Motivation` followed by a Russian description
  </read_first>
  <action>Change the README H1 to read FamilyCoins (e.g. `# FamilyCoins`, emoji optional). Update only the title/first-line branding so the product name is FamilyCoins; preserve the existing description body and structure otherwise. Do not add unrelated sections.</action>
  <verify>
    <automated>head -1 README.md | grep -q "FamilyCoins" && ! grep -qi "^# .*Kids Motivation" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md first line contains "FamilyCoins"
    - No H1 heading in README.md reads "Kids Motivation"
  </acceptance_criteria>
  <done>README title reads FamilyCoins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → PWA install / SEO | Manifest + metadata strings are user-visible branding surface (no untrusted input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-051-04 | Tampering | manifest.ts / layout metadata | accept | Static string edits only; no user input; tsc + grep gates confirm intended values |
| T-051-05 | Spoofing (brand) | Document title / manifest name | mitigate | Exact-string assertions pin the name to FamilyCoins so a stray legacy string cannot ship |
</threat_model>

<verification>
- `grep "title: 'FamilyCoins'" app/layout.tsx` matches.
- `grep "name: 'FamilyCoins'" app/manifest.ts` matches.
- README H1 contains FamilyCoins.
- `npx tsc --noEmit` and `npm run build` succeed.
</verification>

<success_criteria>
ROADMAP 5.1 SC4 met: app name is FamilyCoins in manifest, metadata, and README.
</success_criteria>

<output>
Create `.planning/phases/05.1-launch-prep/05.1-02-SUMMARY.md` when done.
</output>
