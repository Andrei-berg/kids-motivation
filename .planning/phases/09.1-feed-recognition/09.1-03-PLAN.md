---
phase: 09.1-feed-recognition
plan: 03
type: execute
wave: 3
depends_on: ["09.1-01", "09.1-02"]
files_modified:
  - components/feed/FamilyFeed.tsx
autonomous: true
requirements: [FEED-01]
must_haves:
  truths:
    - "Opening the Feed shows one story bubble per child who has at least one event today"
    - "A child with zero events today gets no bubble and no placeholder"
    - "An unseen highlight shows a colorful gradient ring; a seen one shows a flat neutral border"
    - "Tapping a bubble opens a full-bleed celebratory overlay showing that highlight's icon, title and body enlarged"
    - "Opening a bubble marks it seen for that child for today and the ring flattens immediately"
    - "The seen state survives a page reload"
    - "The story reel issues no additional Supabase query"
  artifacts:
    - path: "components/feed/FamilyFeed.tsx"
      provides: "todaysHighlights memo, StoryReel, StoryBubble, StoryMomentModal"
      contains: "todaysHighlights"
      min_lines: 520
  key_links:
    - from: "components/feed/FamilyFeed.tsx"
      to: "lib/kid/feed-highlights.ts"
      via: "pickHighlight over today's events grouped by child_id"
      pattern: "pickHighlight\\("
    - from: "components/feed/FamilyFeed.tsx"
      to: "lib/repositories/feed.repo.ts"
      via: "getStorySeen / markStorySeen"
      pattern: "markStorySeen\\("
    - from: "components/feed/FamilyFeed.tsx"
      to: "utils/helpers.ts"
      via: "localDateString() supplies the seen-key date"
      pattern: "localDateString\\(\\)"
---

<objective>
Add the story reel — one bubble per child showing today's single biggest highlight — above the
day-grouped card list, and the full-bleed celebratory modal it taps through to (FEED-01,
D-04 through D-07).

Purpose: this is the piece that reframes the Feed from "what happened" to "what's worth
celebrating right now". It is also the phase's only new interaction surface.

Output: `components/feed/FamilyFeed.tsx` gains a `todaysHighlights` memo and three new local
components (`StoryReel`, `StoryBubble`, `StoryMomentModal`), all colocated in the same file
alongside the existing `EventRow` and `CommentThread`, matching this file's established
convention.

**Preserve-original-code rule (CLAUDE.md):** the `groups` memo, `dayLabel`, `relTime`,
`palette()`, `getFeed`/`loadMore`/`subscribeFeed`, `toggleReaction`, `submitNote`, the composer,
the empty state, `EventRow` (as restyled in plan 09.1-02) and `CommentThread` are all
additive-only from here. Do not refactor them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/09.1-feed-recognition/09.1-CONTEXT.md
@.planning/phases/09.1-feed-recognition/09.1-PATTERNS.md
@.planning/phases/09.1-feed-recognition/09.1-UI-SPEC.md
@.planning/phases/09.1-feed-recognition/09.1-RESEARCH.md
@.claude/skills/sketch-findings-kids-motivation/references/feed-and-recognition.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase; no exploration required. -->

From `lib/kid/feed-highlights.ts` (plan 09.1-01):
  export function pickHighlight(events: FeedEvent[]): FeedEvent | null
  export const RAIL_COLOR_MAP: Partial<Record<FeedEventKind, string>>

From `lib/repositories/feed.repo.ts` (plan 09.1-01):
  export function getStorySeen(familyId: string, childId: string, dateStr: string): boolean
  export function markStorySeen(familyId: string, childId: string, dateStr: string): void

From `utils/helpers.ts`:
  export function localDateString(date?: Date): string   // family-local YYYY-MM-DD (UTC+3)

From `components/feed/FamilyFeed.tsx` (existing, in-scope call sites):
  const groups: { label: string; items: FeedEvent[] }[]      // line ~179, label 'Сегодня' for today
  const childOf  = (id: string | null) => Child | null       // line ~140
  const accentFor = (id: string | null) => string            // line ~141, CHILD_ACCENTS by index
  const C = palette(variant)                                 // ground/card/line/ink/ink2/ink3/accent/fHead/fBody/fNum
  // `K` is already imported at line 22.

From `lib/models/child.types.ts`:
  interface Child { id: string; name: string; emoji?: string | null; ... }

From `components/kid/ProfileSheet.tsx` lines 71-90 (the overlay shape to reuse):
  <div role="dialog" aria-modal="true" onClick={onClose}
       style={{ position: 'fixed', inset: 0, background: 'rgba(33,49,74,0.5)', zIndex: 240,
                display: 'flex', alignItems: 'flex-end', justifyContent: 'center' }}>
    <div onClick={(e) => e.stopPropagation()} style={{ /* panel */ }}>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: todaysHighlights memo + StoryReel / StoryBubble</name>

  <files>components/feed/FamilyFeed.tsx</files>

  <read_first>
    - `components/feed/FamilyFeed.tsx` — read the whole file in its post-09.1-02 state; the
      insertion points are just after the `groups` memo (~line 188) and between the composer
      block and the `loading ? … : …` list (~line 229)
    - `lib/kid/feed-highlights.ts` and `lib/repositories/feed.repo.ts` — the plan 09.1-01 exports
    - `utils/helpers.ts` — `localDateString()` and why the UTC date is forbidden
    - `.claude/skills/sketch-findings-kids-motivation/sources/001-family-feed/index.html`
      lines 151-160 (`.story-reel` / `.story-ring` / `.story-bubble` CSS) and lines 514-527
      (variant D story-reel markup) — layout reference only, not copy text
    - `.planning/phases/09.1-feed-recognition/09.1-UI-SPEC.md` — "Component Inventory",
      "Story bubble seen/unseen storage", "Spacing Scale" (62px bubble, 3px ring padding),
      "Copywriting Contract" (empty state = structural omission)
    - `.planning/phases/09.1-feed-recognition/09.1-PATTERNS.md` — "Story reel data source" snippet
  </read_first>

  <action>
**1. Imports.** Add `pickHighlight` to the existing `@/lib/kid/feed-highlights` import (which
already brings in `RAIL_COLOR_MAP` from plan 09.1-02), add `getStorySeen, markStorySeen` to the
existing `@/lib/repositories/feed.repo` import list, and add
`import { localDateString } from '@/utils/helpers'`.

**2. `todaysHighlights` memo.** Immediately after the existing `groups` memo, add a new
`useMemo` keyed on `[groups, children]` producing `{ child: Child; event: FeedEvent }[]`:
  - Return `[]` unless `groups[0]?.label === 'Сегодня'`. `dayLabel()` already produces that
    exact literal for today, so `groups[0]` is today's bucket when it exists.
  - Group `groups[0].items` by `child_id` into a `Map<string, FeedEvent[]>`, skipping events
    with a null `child_id` (parent notes are not a child's story).
  - For each entry call `pickHighlight(events)`; resolve the child via `childOf(childId)`.
  - Drop entries whose highlight is `null` or whose child is `null`. A child with zero events
    today simply has no map entry and therefore no bubble — D-05: absence is the empty state,
    never a placeholder bubble.
  - Do NOT add a Supabase query, do NOT change `getFeed`'s limit, and do NOT modify the
    `groups` memo. The reel reads the already-fetched `events` state so it can never drift from
    the card list (anti-pattern called out in `09.1-RESEARCH.md`). The known limitation — a very
    busy family could push a child's today-events past `getFeed`'s 30-row page — is accepted
    for this phase; record it in the SUMMARY, do not pre-optimize it.

**3. Seen state.** Add `const [storySeen, setStorySeen] = useState<Record<string, boolean>>({})`.
Populate it in a `useEffect` keyed on `[familyId, todaysHighlights]` that, for each highlight,
calls `getStorySeen(familyId, child.id, localDateString())` and builds the record. Read
localStorage **only inside the effect**, never during render — `FamilyFeed` is a client
component that still server-renders its first pass, so a render-time `window.localStorage`
read would throw during SSR / desync hydration. Never call `window.localStorage` directly in
this component; always go through the `feed.repo` helpers.

**4. `StoryReel` + `StoryBubble` (new local components below `EventRow`).** Render `StoryReel`
between the `canPost` composer block and the `loading ? …` list. When `todaysHighlights.length
=== 0`, `StoryReel` returns `null` — no heading, no empty-state row, no "нет событий" copy
(UI-SPEC "Empty state — story reel").
  - `StoryReel`: `display: 'flex'`, `gap: 14`, `padding: '4px 0 18px'`, `overflowX: 'auto'`,
    and a horizontal-scroll-friendly `WebkitOverflowScrolling: 'touch'`. Not sticky — it
    scrolls away normally, which is why plan 09.1-02 left the day-label sticky offset
    (`top: hideHeader ? 65 : 0`) unchanged (Pitfall 4).
  - `StoryBubble`: a `<button type="button">` (not a div — it is interactive and must be
    keyboard-reachable) with `background: 'transparent'`, `border: 'none'`, `padding: 0`,
    `cursor: 'pointer'`, `display: 'flex'`, `flexDirection: 'column'`, `alignItems: 'center'`,
    `gap: 6`, `flexShrink: 0`, and `aria-label` of the form `${child.name}: ${event.title}`.
    Inside:
      - Ring: 62×62, `borderRadius: '50%'`, `padding: 3`, `display: 'flex'`.
        Unseen `background`: `linear-gradient(135deg, ${K.mango}, ${K.berry}, ${K.grape})`
        (the linear equivalent of the sketch's 4-stop conic ring, per UI-SPEC "Story-ring
        unseen state"). Seen `background`: flat `K.line` — never a color from the gradient.
      - Avatar circle inside the ring: `width: '100%'`, `height: '100%'`, `borderRadius: '50%'`,
        `border: 3px solid ${C.ground}`, centered flex, `fontSize: 26`, background
        `${accentFor(child.id)}1F`, content `child.emoji || event.icon || '⭐'`. Match this
        file's existing hand-rolled avatar-circle idiom (`EventRow` lines ~309-316) rather than
        importing the `Avatar` atom — consistency within the file wins, and refactoring
        `EventRow`'s circle is out of scope per CLAUDE.md's preserve-original-code rule.
      - Label: `child.name`, `fontFamily: C.fHead`, `fontSize: 11`, `fontWeight: 700`,
        `color: C.ink3`, `maxWidth: 68`, single-line ellipsis.
  - The bubble's `onClick` is wired in Task 2; in this task give it a no-op prop so the
    component compiles and renders.

Use no `🪙` character and no `+N`/`−N` amount string anywhere in this task's new markup — the
sketch's `STORIES` object contains `'+420 🪙'` placeholder copy that violates D-01 and must not
be ported (Pitfall 1).
  </action>

  <verify>
    <automated>npx tsc --noEmit && npm run lint && npx vitest run tests/feed-highlights.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
    - `grep -c "todaysHighlights" components/feed/FamilyFeed.tsx` returns at least `3` (memo declaration, effect dep, reel prop)
    - `grep -c "pickHighlight(" components/feed/FamilyFeed.tsx` returns `1`
    - `grep -c "getStorySeen(" components/feed/FamilyFeed.tsx` returns `1`
    - `grep -c "localDateString()" components/feed/FamilyFeed.tsx` returns at least `1`
    - `grep -c "getFeed(" components/feed/FamilyFeed.tsx` returns `2` — unchanged (initial load + `loadMore`); proves no new query was added
    - `grep -c "window.localStorage" components/feed/FamilyFeed.tsx` returns `0`
    - `grep -c "Сегодня" components/feed/FamilyFeed.tsx` returns `2` — the `dayLabel` literal plus the memo's guard
    - `grep -c "function StoryReel\|function StoryBubble" components/feed/FamilyFeed.tsx` returns `2`
    - `grep -v "^\s*//" components/feed/FamilyFeed.tsx | grep -c "🪙"` returns `0`
    - `grep -c "top: hideHeader ? 65 : 0" components/feed/FamilyFeed.tsx` returns `1` (sticky offset still untouched)
    - `git diff --numstat components/feed/FamilyFeed.tsx` shows fewer than 10 deleted lines (additive change)
  </acceptance_criteria>

  <done>`todaysHighlights` derives one highlight per child from today's already-fetched events, `StoryReel` renders a bubble per qualifying child with the correct seen/unseen ring, and renders nothing when no child qualifies.</done>
</task>

<task type="auto">
  <name>Task 2: StoryMomentModal + tap-through and mark-seen wiring</name>

  <files>components/feed/FamilyFeed.tsx</files>

  <read_first>
    - `components/feed/FamilyFeed.tsx` — post-Task-1 state, specifically `StoryBubble`'s
      `onClick` prop and the root `<div>` where the modal mounts
    - `components/kid/ProfileSheet.tsx` lines 71-90 — the inline-style
      `position: fixed; inset: 0` dialog, backdrop `onClick={onClose}`, panel
      `onClick={(e) => e.stopPropagation()}` shape to reuse (z-index 240 there)
    - `components/kid/CelebrationOverlay.tsx` — the alternative CSS-class overlay pattern that
      is deliberately NOT used here (its backdrop is kid-theme-specific)
    - `.claude/skills/sketch-findings-kids-motivation/sources/001-family-feed/index.html`
      lines 178-194 (`.story-modal` / `.story-modal-card` / `.story-modal-close` CSS) —
      geometry reference only; its `STORIES` JS object at lines 715-719 is placeholder copy
      containing `'+420 🪙'` and must NOT be ported
    - `.planning/phases/09.1-feed-recognition/09.1-UI-SPEC.md` — Typography (Display row
      22px/700, the single 800 hero exception), Copywriting Contract (icon-only dismiss,
      44×44 tap target)
    - `lib/kid/feed-highlights.ts` — `RAIL_COLOR_MAP`, the source of the modal's gradient
  </read_first>

  <action>
**1. `StoryMomentModal` (new local component, below `StoryBubble`).** Props:
`{ event: FeedEvent; child: Child; C: ReturnType<typeof palette>; onClose: () => void }`.
Reuse `ProfileSheet`'s inline-style dialog shape, changed from a bottom sheet to a centered
celebratory card:
  - Backdrop `<div role="dialog" aria-modal="true" onClick={onClose}>` with
    `position: 'fixed'`, `inset: 0`, `background: 'rgba(20,20,30,0.55)'`, `zIndex: 260`
    (above `ProfileSheet`'s 240), `display: 'flex'`, `alignItems: 'center'`,
    `justifyContent: 'center'`, `padding: 20`.
  - Panel `<div onClick={(e) => e.stopPropagation()}>` with `width: '100%'`, `maxWidth: 340`,
    `borderRadius: 22`, `padding: '26px 22px'`, `position: 'relative'`, `color: '#fff'`,
    `textAlign: 'center'`, and `background: linear-gradient(135deg, ${rail}, ${K.berry})`
    where `const rail = RAIL_COLOR_MAP[event.kind] ?? K.grape`. Derive the gradient from the
    rail color so the modal reads as the same category signal as its card — do not hardcode a
    per-child gradient the way the sketch's `STORIES` object does.
  - Content, top to bottom: the glyph `event.icon || child.emoji || '⭐'` at `fontSize: 44`;
    the headline `event.title` at `fontFamily: C.fHead`, `fontSize: 22`, `fontWeight: 700`,
    `lineHeight: 1.15`, `margin: '10px 0 4px'` (the UI-SPEC Display row); and, when
    `event.body` is non-empty, the body at `fontFamily: C.fBody`, `fontSize: 14`,
    `fontWeight: 400`, `opacity: 0.92`, `whiteSpace: 'pre-wrap'`. Render **no** amount, no
    `🪙`, and no `+N`/`−N` string — `event.amount` must not be referenced anywhere in this
    component (D-01).
  - Dismiss `<button type="button" aria-label="Закрыть" onClick={onClose}>`: absolutely
    positioned `top: 6, right: 6`, `width: 44, height: 44` tap target, transparent background
    and `border: 'none'`, containing a 30×30 centered circle with
    `background: 'rgba(255,255,255,0.2)'`, `borderRadius: '50%'`, `color: '#fff'`,
    `fontSize: 16` and the `×` glyph. Icon-only — no text label (UI-SPEC Copywriting Contract).
  - Add a `useEffect` registering a `keydown` listener on `window` that calls `onClose` on
    `Escape`, returning the removal function as cleanup.

**2. Wiring in `FamilyFeed`.** Add
`const [openStory, setOpenStory] = useState<{ child: Child; event: FeedEvent } | null>(null)`.
Pass an `onOpen` handler into `StoryReel` → `StoryBubble` that, for a highlight `h`:
  - calls `setOpenStory(h)`,
  - calls `markStorySeen(familyId, h.child.id, localDateString())`,
  - and optimistically calls `setStorySeen(prev => ({ ...prev, [h.child.id]: true }))` so the
    ring flattens to `K.line` immediately rather than on the next mount (D-07: seen is marked
    on open, not on close).
Render `{openStory && <StoryMomentModal event={openStory.event} child={openStory.child} C={C} onClose={() => setOpenStory(null)} />}` as the last child of the component's root `<div>`,
outside the `maxWidth: 600` content column so the overlay is truly full-bleed.

Guard the `markStorySeen` call on a non-null `familyId` (it is `string | null` in this
component's state).
  </action>

  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm test</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
    - `npm test` exits 0
    - `grep -c "function StoryMomentModal" components/feed/FamilyFeed.tsx` returns `1`
    - `grep -c "aria-modal" components/feed/FamilyFeed.tsx` returns `1`
    - `grep -c "markStorySeen(" components/feed/FamilyFeed.tsx` returns `1`
    - `grep -c "zIndex: 260" components/feed/FamilyFeed.tsx` returns `1`
    - `grep -c "aria-label=\"Закрыть\"" components/feed/FamilyFeed.tsx` returns `1`
    - `grep -c "RAIL_COLOR_MAP" components/feed/FamilyFeed.tsx` returns `3` (import + `EventRow` rail + modal gradient)
    - `grep -c "e\.amount\|event\.amount" components/feed/FamilyFeed.tsx` returns `0`
    - `grep -v "^\s*//" components/feed/FamilyFeed.tsx | grep -c "🪙"` returns `0`
    - `grep -c "420" components/feed/FamilyFeed.tsx` returns `0` (no sketch placeholder copy ported)
    - `npm run build` exits 0
  </acceptance_criteria>

  <done>Tapping a story bubble opens a full-bleed gradient overlay showing that highlight's icon, title and body enlarged with no money content; the bubble's ring flattens immediately and stays flat after a reload; Escape, the × button and a backdrop tap all dismiss it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres (`family_events`, RLS SELECT-only) → browser render | Already-authorized, family-scoped rows already present in component state are re-rendered in a new surface. No new query, no new write, no new server call. |
| `window.localStorage` (same-origin, per-device) → component state | Story-seen booleans, written and read only through `feed.repo` helpers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-091-07 | Information Disclosure | `StoryMomentModal` | mitigate | The modal renders only `event.icon` / `event.title` / `event.body`; `event.amount` is never referenced, asserted by the `grep -c "event\.amount"` → `0` gate. The sketch's `'+420 🪙'` placeholder copy is explicitly forbidden from being ported. |
| T-091-08 | Information Disclosure | Story reel visibility scope | accept | The reel derives strictly from `events`, which `getFeed` already scopes to the viewer's `family_id` under the existing RLS SELECT policy. No child sees another family's data; a sibling seeing a sibling's highlight is the intended product behavior. |
| T-091-09 | Tampering | `story_seen_*` localStorage keys | accept | Cosmetic only — a forged value changes a ring's border style. Not an authorization input, grants no data access. |
| T-091-10 | Denial of Service | SSR crash from a render-time `window` read | mitigate | localStorage is read only inside a `useEffect`; a `grep -c "window.localStorage"` → `0` gate on the component enforces that access stays behind the `feed.repo` helpers |
| T-091-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages installed (`09.1-RESEARCH.md`: "Package Legitimacy Audit: Not applicable"). No install task, no legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build` all exit 0
- `grep -c "getFeed(" components/feed/FamilyFeed.tsx` returns 2 — the reel added no query
- `grep -v "^\s*//" components/feed/FamilyFeed.tsx | grep -c "🪙"` returns 0
- Visual/interaction correctness is deliberately NOT asserted here — it is covered by the
  `checkpoint:human-verify` in plan 09.1-04, because `vitest.config.ts` runs in the `node`
  environment with no jsdom and no `@testing-library/react`
</verification>

<success_criteria>
- FEED-01: a story reel of per-child today-highlights renders above the day-grouped list, and
  tapping a bubble opens a full celebratory moment view
- D-05 honored: no bubble, no placeholder, for a child with zero events today
- D-06/D-07 honored: seen state is per-viewer/per-child/per-day in localStorage, written on open
- No new Supabase query, no new DB write, no money content
</success_criteria>

<output>
Create `.planning/phases/09.1-feed-recognition/09.1-03-SUMMARY.md` when done
</output>
