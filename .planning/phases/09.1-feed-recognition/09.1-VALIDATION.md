---
phase: 09.1
slug: feed-recognition
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-09-17
---

# Phase 09.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.1.2 |
| **Config file** | `vitest.config.ts` (environment: `node` — no jsdom/happy-dom, no `@testing-library/react`) |
| **Quick run command** | `npx vitest run tests/feed-highlights.test.ts` |
| **Full suite command** | `npm test` (== `vitest run`) |
| **Estimated runtime** | ~2 seconds (quick), ~10 seconds (full suite) |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run tests/feed-highlights.test.ts`
- **After every plan wave:** Run `npm test`
- **Before `/gsd:verify-work`:** Full suite must be green, plus one `checkpoint:human-verify` covering story-reel tap-through, rail colors across all 3 variants (kid/parent/family), and confirming zero `🪙` amounts anywhere in the redesigned feed
- **Max feedback latency:** ~10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD (Wave 0) | TBD | 0 | FEED-01/FEED-02 | — | N/A | unit | `npx vitest run tests/feed-highlights.test.ts -t "pickHighlight"` | ❌ Wave 0 | ⬜ pending |
| TBD (Wave 0) | TBD | 0 | FEED-02 | — | N/A | unit | `npx vitest run tests/feed-highlights.test.ts -t "RAIL_COLOR_MAP"` | ❌ Wave 0 | ⬜ pending |
| TBD | TBD | 1 | FEED-03 | — | N/A | structural (grep) | `grep -n "e.amount" components/feed/FamilyFeed.tsx` returns zero render-related matches | ✅ | ⬜ pending |
| TBD | TBD | 1 | FEED-04 | — | N/A | static/manual | `grep -rn "emitFeedEvent(" app lib --include="*.ts"` — confirm kind list unchanged (no penalty/correction emitter added) | ✅ | ⬜ pending |
| TBD | TBD | final | FEED-01/FEED-02 (visual) | — | N/A | manual | `checkpoint:human-verify` in browser at `/kid/feed`, `/family/feed`, Parent Center Feed tab | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*Task ID / Plan / Wave columns are TBD — the planner assigns these; requirement→test mapping above is fixed by research and MUST be preserved when plans are written.*

---

## Wave 0 Requirements

- [ ] `lib/kid/feed-highlights.ts` — new pure module: `pickHighlight()`, `RANK_ORDER`, `RAIL_COLOR_MAP` — does not exist yet, must be created before tests can be written against it
- [ ] `tests/feed-highlights.test.ts` — new unit test file covering FEED-01 (`pickHighlight` ranking + zero-events → `null`) and FEED-02 (`RAIL_COLOR_MAP` covers every real `FeedEventKind`)
- [ ] No framework install needed — Vitest is already configured and green (`npm test` today)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Story reel renders one bubble per child with today's highest-ranked event; tapping opens full-bleed modal; day-grouped cards show correct rail color per kind; day grouping intact; zero coin amounts rendered anywhere | FEED-01, FEED-02, FEED-03 | `vitest.config.ts` runs in `node` environment — no jsdom/`@testing-library/react` — React component rendering cannot be unit-tested in this repo | `checkpoint:human-verify`: open `/kid/feed`, `/family/feed`, and Parent Center's Feed tab; confirm story bubbles appear only for children with events today, tap-through opens the modal and marks seen, every card's left rail matches its kind's color, and no `🪙`/coin amount appears on any card including reward purchases |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
