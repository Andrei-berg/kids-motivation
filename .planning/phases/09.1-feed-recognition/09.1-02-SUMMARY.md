---
phase: 09.1-feed-recognition
plan: 02
subsystem: ui
tags: [family-feed, rail-card, vitest, content-safety]

# Dependency graph
requires:
  - "lib/kid/feed-highlights.ts — RAIL_COLOR_MAP, RANK_ORDER (from plan 09.1-01)"
provides:
  - "components/feed/FamilyFeed.tsx — EventRow restyled as an 18px-radius rail card, zero coin-amount rendering"
  - "tests/feed-highlights.test.ts — 'FEED-04 content rule' describe block locking RAIL_COLOR_MAP's exact key set"
affects: [09.1-03, 09.1-04, 09.2-feed-social]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Card-as-rail shape: outer flex div (overflow hidden, borderRadius 18) holding a 5px aria-hidden rail div + a flex:1 content div carrying the original padding — mirrors CollapsibleRow (components/kid/design/atoms.tsx)"
    - "Rail color is read unconditionally from RAIL_COLOR_MAP[e.kind] ?? K.line, never routed through palette()/variant — keeps it identical across kid/parent/family renders"

key-files:
  created: []
  modified:
    - components/feed/FamilyFeed.tsx
    - tests/feed-highlights.test.ts

key-decisions:
  - "FEED-04 audit found 12 emitFeedEvent references (1 definition + 11 call sites) exactly as the plan predicted — no penalty/correction/behavior kind is ever emitted; enforcement is the absence of an emitter plus the family_events.kind CHECK constraint, not a client-side filter"
  - "Documented FEED-04/D-03 as a 6-line comment in FamilyFeed.tsx's existing top-of-file header block rather than a new comment block elsewhere, keeping the single source of file-level doc comments"

patterns-established:
  - "Any future FamilyFeed rail-card work reuses the outer-flex + aria-hidden-rail + flex:1-content-div shape rather than re-deriving it from CollapsibleRow each time"

requirements-completed: [FEED-02, FEED-03, FEED-04]

# Metrics
duration: 22min
completed: 2026-09-17
---

# Phase 09.1 Plan 02: Feed Rail Cards + Content Rule Summary

**Restyled `FamilyFeed.tsx`'s `EventRow` into an 18px-radius, 5px kind-colored left-rail card and deleted the `±N🪙` coin-amount render for every event kind, then locked the "no punitive content" property behind a regression-guarded vitest suite.**

## Performance

- **Duration:** 22 min
- **Started:** 2026-09-17T22:58:00Z
- **Completed:** 2026-09-17T23:20:00Z
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments
- `components/feed/FamilyFeed.tsx` — `EventRow`'s card root restructured to a flex row: `overflow: hidden`/`borderRadius: 18` outer div, a first-child 5px `aria-hidden` rail div colored via `RAIL_COLOR_MAP[e.kind] ?? K.line`, and the original content row (avatar/title/body/reactions/comments) now carrying `flex: 1, minWidth: 0, padding: 14`. The `amountColor` variable and the `{e.amount != null && <span>±N🪙</span>}` block are gone entirely — no kind (including `purchase`/`reward_approved`) renders a coin figure. `FeedEvent.amount` and its DB write path are untouched.
- `components/feed/FamilyFeed.tsx` header comment — added a 6-line FEED-04/D-03 doc block stating the feed renders only positive recognition events, no emitter for punitive content exists anywhere in the codebase, and enforcement lives at the `family_events.kind` CHECK constraint, not this component.
- `tests/feed-highlights.test.ts` — new `describe('FEED-04 content rule')` block: asserts `RAIL_COLOR_MAP`'s key set is exactly the 10 locked non-punitive kinds (sorted deep-equal) and that all 8 `RANK_ORDER` kinds resolve to a truthy `RAIL_COLOR_MAP` entry, so a future accidental punitive-kind addition fails CI.
- Audited `emitFeedEvent(` across `app/` and `lib/` — exactly 12 matches (1 definition + 11 call sites: `app/api/wallet/award/route.ts` x5, `app/parent/reading/actions.ts`, `app/actions/post-feed-note.ts`, `app/actions/emit-badge-feed.ts`, `app/parent/shop/actions.ts`, `app/actions/send-medal.ts`, `app/kid/(app)/shop/actions.ts`) — matches the plan's predicted set exactly, none has grown. Zero matches for a `kind: 'penalty'|'correction'|'behavior'` literal near any call site.

## Task Commits

Each task was committed atomically:

1. **Task 1: EventRow → rail card, amount span removed** - `b77d467` (feat)
2. **Task 2: FEED-04 emitter audit + content-rule regression guard** - `0459d16` (test)

## Files Created/Modified
- `components/feed/FamilyFeed.tsx` - `EventRow` restyled as a rail card (RAIL_COLOR_MAP import + use, amount render deleted, FEED-04/D-03 header comment added)
- `tests/feed-highlights.test.ts` - `FEED-04 content rule` describe block added (RAIL_COLOR_MAP key-set lock + RANK_ORDER coverage assertion)

## Decisions Made
- No deviations from the plan's literal structural instructions — the three surgical edits (import, amount-render deletion, rail wrap) and the audit-then-document-then-guard sequence for Task 2 were followed exactly as specified.

## Deviations from Plan

None - plan executed exactly as written. All acceptance-criteria greps (🪙 count 0, `e.amount` count 0, `amountColor` count 0, `RAIL_COLOR_MAP` count 2, exact rail pattern count 1, `width: 5, flexShrink: 0` count 1, `borderRadius: 18` count 1, sticky offset unchanged, reaction/comment code count unchanged at 6, `emitFeedEvent(` count 12, zero punitive-kind matches, `FEED-04` present in both files, Task 2 diff on `FamilyFeed.tsx` insertions-only) matched the plan's required values on first pass.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Every feed card (kid, parent, family variants) now shows an identical kind-colored 5px rail and renders zero coin amounts — the visual foundation plan 09.1-03 (story reel) builds on top of.
- The FEED-04 regression guard is live: any future `RAIL_COLOR_MAP` addition for a punitive kind fails `tests/feed-highlights.test.ts` immediately.
- Day grouping, sticky day-label offset, reactions, comment toggle/thread, composer, pagination, and realtime subscription are all structurally unchanged — confirmed via untouched line counts and a clean `git diff --numstat` on `lib/models/feed.types.ts` / `lib/services/feed.service.ts`.
- No blockers. `npx tsc --noEmit` clean, `npm run lint` exits 0 (pre-existing warnings only, none in touched files), `npm test` green (148 passed, 57 pre-existing skips, 0 failures).

---
*Phase: 09.1-feed-recognition*
*Completed: 2026-09-17*

## Self-Check: PASSED

- FOUND: components/feed/FamilyFeed.tsx
- FOUND: tests/feed-highlights.test.ts
- FOUND: .planning/phases/09.1-feed-recognition/09.1-02-SUMMARY.md
- FOUND: b77d467 (Task 1 commit)
- FOUND: 0459d16 (Task 2 commit)
