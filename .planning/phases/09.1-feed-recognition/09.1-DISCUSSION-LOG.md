# Phase 9.1: feed-recognition - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-17
**Phase:** 9.1-feed-recognition
**Areas discussed:** Money scope, Highlight pick, Seen state + detail, Rail colors

---

## Money scope

| Option | Description | Selected |
|--------|-------------|----------|
| Strip amounts everywhere | Match sketch findings literally: no card ever shows a coin number, for any event kind. Amount column stays in `family_events` for internal use. | ✓ |
| Only hide it for purchases | Follow REQUIREMENTS.md literally: keep amounts on day/streak/boost/reading/medal cards, hide only reward-purchase price per FEED-03. | |

**User's choice:** Strip amounts everywhere (recommended option).
**Notes:** Found during codebase scout that `EventRow` in `FamilyFeed.tsx` currently renders
`family_events.amount` as `±N🪙` for almost every event kind, not just purchases — this
decision resolves that gap against the sketch's broader "no money, ever" rule.

---

## Highlight pick

| Option | Description | Selected |
|--------|-------------|----------|
| Fixed priority ranking | Rank event kinds by significance (badge > medal > boost > streak-record > reading > day_filled); no events today → no bubble. | ✓ |
| Highest coin amount wins | Pick whichever event carries the largest amount value as the ranking signal (amount itself never displayed); no events today → no bubble. | |
| Always show a bubble, even empty | Every child always gets a bubble; zero events today shows a neutral "day not filled yet" prompt instead of omitting the child. | |

**User's choice:** Fixed priority ranking (recommended option).
**Notes:** Recommended ranking captured in CONTEXT.md D-04 is a starting point for
planning/implementation to refine, not an exactly locked order.

---

## Seen state + detail

| Option | Description | Selected |
|--------|-------------|----------|
| localStorage seen tracking + modal detail | Track per-viewer "seen today" per child in localStorage (same pattern as existing `feed_seen_` key); tapping opens a full-bleed modal overlay, then marks seen. | ✓ |
| Skip seen/unseen for v1 | Every bubble always renders with the colorful ring; tapping still opens the detail modal; seen-state tracking deferred to a later phase. | |

**User's choice:** localStorage seen tracking + modal detail (recommended option).
**Notes:** Confirmed `lib/repositories/feed.repo.ts` already has a `feed_seen_${familyId}`
localStorage pattern for whole-feed seen state; this decision extends that pattern per-child.

---

## Rail colors

| Option | Description | Selected |
|--------|-------------|----------|
| I'll pick colors now | User answers inline which K palette color each of badge/streak/reward_approved/level_up should use. | |
| Claude decides sensible reuse | badge→berry, streak→mango, reward_approved→sky, level_up→grape — documented in CONTEXT.md, no further discussion. | ✓ |

**User's choice:** Claude decides sensible reuse (recommended option).
**Notes:** `coins_earned`/`coins_spent` kinds exist in the schema/type but have zero current
emitters — flagged as dead/reserved, no rail color assigned.

---

## Claude's Discretion

- Exact visual treatment of the modal overlay (animation, dismiss gesture) — follow sketch
  variant D as the base.
- Whether/how the existing composer and reactions/comments UI get restyled to match new cards —
  must keep working, pixel treatment is an implementation call.
- Final exact ordering within the highlight priority ranking, if implementation evidence
  suggests reordering two adjacent kinds.

## Deferred Ideas

None raised — discussion stayed within Phase 9.1's boundary. Reactions, "подколоть" tease
replies, and kid→kid medal-as-its-own-event remain correctly scoped to Phase 9.2 per the
existing roadmap (not a new deferral from this session).
