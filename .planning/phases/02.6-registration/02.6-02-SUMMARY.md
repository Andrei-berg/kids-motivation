---
phase: 02.6-registration
plan: 02
subsystem: ui
tags: [onboarding, wizard, supabase, nextjs, confetti]

# Dependency graph
requires:
  - phase: 02.6-01
    provides: createChildWithWallet, completeOnboarding, createFamily in lib/onboarding-api.ts
provides:
  - 5-step onboarding wizard at /onboarding/v2 with real DB writes on completion
  - /onboarding root redirect to /onboarding/v2
  - Welcome/philosophy screen (Step 0) satisfying REQ-ONB-001
  - Confetti Done screen with invite code, second-parent section, per-child PIN hints
  - createFamily() updated to accept parentDisplayName and use UPSERT for parent family_members row
affects:
  - 02.6-03 (child join flow reads families/invite codes created by this wizard)
  - 02.6-04 (PIN login builds on per-child PIN hint shown in Done screen)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Inline-style isolated wizard pages (no Tailwind/globals.css classes)
    - Render-props pattern for wizard steps: parent owns all state, passes handlers down
    - Lazy-import confetti in useEffect to avoid SSR issues

key-files:
  created:
    - app/onboarding/v2/page.tsx
  modified:
    - app/onboarding/page.tsx
    - lib/onboarding-api.ts

key-decisions:
  - "createFamily() upserts parent family_members row (onConflict: user_id,family_id) — prevents duplicate parent row if auth callback pre-inserted"
  - "parentDisplayName collected in wizard Step 1 (not at /register) — this is the sole collection point for display_name (REQ-ONB-003)"
  - "onboarding/page.tsx replaced with server-side redirect() — eliminates old multi-step wizard code, keeps /onboarding URL routing from auth callback intact"
  - "confetti fired via dynamic import in useEffect on step===4 — avoids SSR bundle bloat"
  - "Task 3 (auth callback verification) is no-op — file already correct, no changes needed"

patterns-established:
  - "Wizard steps as isolated function components receiving only the props they need (not entire wizard state)"
  - "handleFinish() writes DB in order: createFamily → createChildWithWallet (sequential) → completeOnboarding"

requirements-completed:
  - REQ-ONB-001
  - REQ-ONB-002
  - REQ-ONB-003
  - REQ-ONB-004
  - REQ-ONB-005
  - REQ-ONB-006
  - REQ-ONB-008
  - REQ-FAM-008
  - REQ-FAM-010

# Metrics
duration: 18min
completed: 2026-04-12
---

# Phase 02.6 Plan 02: Onboarding Wizard Summary

**5-step registration wizard with real Supabase DB writes, confetti Done screen, invite code sharing, and UPSERT-safe parent family_members creation**

## Performance

- **Duration:** 18 min
- **Started:** 2026-04-12T08:00:00Z
- **Completed:** 2026-04-12T08:18:00Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments
- Replaced stub /onboarding/v2 wizard (console.log + window.location) with full 5-step wizard calling createFamily → createChildWithWallet → completeOnboarding
- Done screen fires confetti (REQ-ONB-008), shows invite code with copy button, "Второй родитель" section with invite sharing (REQ-ONB-006), and per-child PIN setup hints
- Updated createFamily() to accept parentDisplayName and use UPSERT for parent family_members row (duplicate-safe when auth callback pre-inserts the row)
- /onboarding/page.tsx replaced with 4-line server-side redirect — auth callback's redirect to /onboarding now lands on the real wizard
- Auth callback (/auth/callback/route.ts) verified correct: maybeSingle() + default next='/onboarding'

## Task Commits

1. **Task 1: Replace /onboarding/v2 with 5-step wizard with real DB writes** - `437162c` (feat)
2. **Task 2: Add /onboarding root redirect to /onboarding/v2** - `032e623` (feat)
3. **Task 3: Verify auth callback routes new users to /onboarding** - no-op (file already correct)

## Files Created/Modified
- `app/onboarding/v2/page.tsx` - New 5-step wizard (Welcome, Family, Children, CoinRules, Done)
- `app/onboarding/page.tsx` - Replaced with server-side redirect('/onboarding/v2')
- `lib/onboarding-api.ts` - createFamily() now accepts parentDisplayName; uses UPSERT for parent family_members

## Decisions Made
- createFamily() now uses `.upsert(..., { onConflict: 'user_id,family_id' })` for the parent family_members row. Previously used bare INSERT which would crash if auth callback pre-created the row.
- parentDisplayName passed directly to createFamily() from wizard Step 1 — avoids second DB round-trip to set display_name separately.
- onboarding/page.tsx replaced (not extended) because the old file was a 1100-line wizard stub with console.log mock save — a redirect is the correct replacement.
- Confetti uses dynamic `import('@/utils/confetti')` inside useEffect to prevent canvas-confetti from being included in SSR bundle.
- Task 3 is no-op verification: auth callback already has maybeSingle() + default='/onboarding' logic intact.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated createFamily() signature to accept parentDisplayName**
- **Found during:** Task 1 (reading lib/onboarding-api.ts before wiring handleFinish)
- **Issue:** Plan specified `createFamily(userId, { name, parentDisplayName })` but actual createFamily() only accepted `{ name }` — parentDisplayName would be silently dropped
- **Fix:** Updated createFamily() signature to `family: { name: string; parentDisplayName?: string }`, added resolution logic to use parentDisplayName param first, then fall back to user_profiles.display_name; changed family_members INSERT to UPSERT with onConflict: 'user_id,family_id'
- **Files modified:** lib/onboarding-api.ts
- **Verification:** TypeScript passes (exit 0), build succeeds
- **Committed in:** 437162c (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — bug: signature mismatch between plan spec and actual implementation)
**Impact on plan:** Required for correctness — parentDisplayName would silently not be saved without this fix. Also added UPSERT safety which was explicitly required by the plan.

## Issues Encountered
None — TypeScript and build passed cleanly on first attempt.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- /onboarding/v2 wizard fully functional with real DB writes
- Auth callback correctly routes new users to /onboarding (verified)
- Done screen shows invite code — ready for Plan 03 (child join flow via /onboarding/join)
- PIN hint text shown on Done screen — ready for Plan 04 (PIN login implementation)

---
*Phase: 02.6-registration*
*Completed: 2026-04-12*
