---
phase: 02.6-registration
plan: 03
subsystem: auth
tags: [supabase, zustand, next.js, routing, onboarding]

# Dependency graph
requires:
  - phase: 02.6-01
    provides: family_members.child_id populated with TEXT children.id slug for new families
provides:
  - Polished child/adult invite join flow at /onboarding/join with correct role-based redirects
  - Child claim sets activeMemberId to family_members.child_id (TEXT slug like 'sofia_a1b2')
  - Adult join redirects to /parent/dashboard
  - Child join redirects to /kid/day
  - Already-linked users redirected by role on return visit (no second DB query)
affects: [02.6-04, kid-screen, role-routing]

# Tech tracking
tech-stack:
  added: []
  patterns: [Read child_id from family_members row after claim rather than name-based lookup]

key-files:
  created: []
  modified:
    - app/onboarding/join/page.tsx

key-decisions:
  - "After child claim, read child_id directly from family_members row (not resolveLegacyChildId name match)"
  - "Existing-member early-exit uses role field to choose /kid/day vs /parent/dashboard — no second query needed since child_id is in the same row"

patterns-established:
  - "Join flow pattern: claim first, then read child_id from DB to set store state"

requirements-completed:
  - REQ-ONB-009
  - REQ-ONB-010
  - REQ-FAM-009
  - REQ-FAM-010

# Metrics
duration: 2min
completed: 2026-04-12
---

# Phase 02.6 Plan 03: Join Flow Fix Summary

**Removed name-based child ID lookup in /onboarding/join; child claim now reads child_id directly from family_members row and routes child→/kid/day, adult→/parent/dashboard**

## Performance

- **Duration:** 2 min
- **Started:** 2026-04-12T20:40:39Z
- **Completed:** 2026-04-12T20:41:44Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Removed `resolveLegacyChildId` (name-based lookup against legacy children table — broken for new families)
- Removed `getChildren` import from children.repo
- `handleJoinAsChild`: after `joinFamilyAsChild` RPC, queries `family_members.child_id` to set `activeMemberId`, then redirects to `/kid/day`
- `handleJoinAsAdult`: now redirects to `/parent/dashboard` (was `/dashboard`)
- Auth init useEffect: selects `child_id` and `role` columns; redirects returning child→`/kid/day` with `activeMemberId` set, parent/extended→`/parent/dashboard` (no second query)

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix post-join redirects and child_id resolution** - `3692dbd` (feat)

**Plan metadata:** _(to be added by final commit)_

## Files Created/Modified
- `app/onboarding/join/page.tsx` - Removed resolveLegacyChildId, fixed all redirects and child_id resolution

## Decisions Made
- Read `child_id` from `family_members` row after claim — direct DB field, no name matching required
- Existing-member early-exit now reads `role` and `child_id` from the same `.select()` call — avoids a second round-trip

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Self-Check: PASSED

All files exist and commit 3692dbd verified in git log.

## Next Phase Readiness
- Join flow correctly routes new users by role
- `activeMemberId` set to TEXT slug (e.g. `sofia_a1b2`) after child claim — KidLayout redirect will work
- Plan 02.6-04 can proceed (depends on working join flow)

---
*Phase: 02.6-registration*
*Completed: 2026-04-12*
