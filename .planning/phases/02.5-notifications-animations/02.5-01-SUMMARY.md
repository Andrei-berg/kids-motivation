---
phase: 02.5-notifications-animations
plan: "01"
subsystem: ui
tags: [animation, confetti, canvas-confetti, react, tailwind]

# Dependency graph
requires:
  - phase: 02.4.1-kid-screen-v2
    provides: KidDayFillForm with coinsPreview + onSaved callback; CelebrationOverlay with badge check logic
provides:
  - CoinFlyup component and useCoinAnimation hook for triggering coin fly-up animations
  - CelebrationOverlay fires triggerGoalConfetti() on new badge detection
  - CSS keyframes coin-flyup and badge-pulse in globals.css
affects: [kid-screen, 02.5-notifications-animations]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "useCoinAnimation hook: useRef counter + setTimeout cleanup for ephemeral fly-up instances"
    - "Fixed-position overlay with pointer-events:none for non-blocking animations"

key-files:
  created:
    - components/kid/CoinAnimation.tsx
  modified:
    - components/kid/KidDayFillForm.tsx
    - components/kid/CelebrationOverlay.tsx
    - app/globals.css

key-decisions:
  - "useRef for counter in useCoinAnimation (not closure variable) to survive re-renders"
  - "CoinFlyup rendered outside conditionals at bottom of KidDayFillForm JSX"
  - "triggerCoinFlyup called before onSaved to ensure animation starts immediately"

patterns-established:
  - "Coin fly-up pattern: useCoinAnimation hook returns {flyups, trigger}; CoinFlyup component renders them"

requirements-completed:
  - REQ-UX-004
  - REQ-BAD-003

# Metrics
duration: 10min
completed: 2026-04-11
---

# Phase 2.5 Plan 01: Coin & Badge Animations Summary

**CoinFlyup component with CSS keyframe fly-up + CelebrationOverlay confetti via canvas-confetti on badge earn**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-04-11T00:00:00Z
- **Completed:** 2026-04-11T00:10:00Z
- **Tasks:** 2 of 3 complete (paused at human-verify checkpoint)
- **Files modified:** 4

## Accomplishments
- Created `CoinFlyup` component and `useCoinAnimation` hook — golden coin numbers fly up and fade on save
- Wired `CoinFlyup` into `KidDayFillForm` — triggers after successful day save with `coinsPreview` amount
- Updated `CelebrationOverlay` to fire `triggerGoalConfetti()` (3-second confetti rain) when a new badge is detected
- Added CSS keyframes `coin-flyup` and `badge-pulse` to `app/globals.css`
- Badge icon now has entrance scale animation (`badge-pulse`)

## Task Commits

Each task was committed atomically:

1. **Task 1: CoinFlyup component and CSS keyframe** - `7ff7e2c` (feat)
2. **Task 2: Confetti in CelebrationOverlay** - `c5f2caf` (feat)

## Files Created/Modified
- `components/kid/CoinAnimation.tsx` - useCoinAnimation hook + CoinFlyup component
- `components/kid/KidDayFillForm.tsx` - imports hook; triggers after save; renders CoinFlyup
- `components/kid/CelebrationOverlay.tsx` - imports triggerGoalConfetti; fires on newBadge; badge-pulse style
- `app/globals.css` - coin-flyup-container, coin-flyup-number, @keyframes coin-flyup, @keyframes badge-pulse

## Decisions Made
- `useRef` for counter in `useCoinAnimation` to avoid stale closure across re-renders
- CSS `position: fixed; pointer-events: none` for coin container — does not block touch on the form
- `triggerCoinFlyup(coinsPreview)` called before `onSaved()` so animation is visible even if parent reloads quickly

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Animations are implemented and TypeScript-clean
- Awaiting human visual verification (checkpoint)
- After approval: proceed to Plan 02.5-02 (streak animations / notifications)

---
*Phase: 02.5-notifications-animations*
*Completed: 2026-04-11*
