---
phase: 02.5-notifications-animations
task: post-phase bugfixes
status: in_progress
last_updated: 2026-04-11T14:24:03.066Z
---

<current_state>
Phase 02.5 is fully executed and verified (4/4 plans). Currently doing post-deploy bugfixes
discovered during live testing with kids. Several fixes committed and pushed to prod.
Vercel webhook was broken — reconnected, now auto-deploying again.
</current_state>

<completed_work>

- Phase 02.5 executed: coin flyup animation, badge confetti, streak push notifications, reminder cron (18:00 UTC daily), missed-tasks cron (20:00 UTC)
- fix(rls): family_id added to all wallet createTransaction calls and streak inserts — was blocking authenticated sessions
- refactor: removed hardcoded adam/alim from parent-dashboard, parent/shop, ActivitiesManager, SectionsManager
- fix(types): added family_id to Wallet and WalletTransaction interfaces
- fix(cron): reminders changed from */5 * * * * to 0 18 * * * (Hobby plan limit: once/day)
- fix: grades section hidden on weekends (dayType === 'school' check added)
- feat: logout button added to KidNav (🚪 mobile top-right, desktop nav right)
- fix(auth): clear activeMemberId on logout + sync in KidInitializer
- refactor: all legacy routes (dashboard, analytics, wallet, settings, expenses, records, streaks, coach-rating, audit, parent-dashboard) replaced with server-side redirects to /parent/* — wallboard kept
</completed_work>

<remaining_work>

- BUG OPEN: Alim's phone still shows Adam after logout+login — legacy route redirect didn't fix it yet, deploy pending
- Navigation additions (on pause, to be decided later)
- /gsd:complete-milestone — milestone v2.0 ready to close
</remaining_work>

<decisions_made>

- Cron reminders: daily at 18:00 UTC (not every 5 min) due to Vercel Hobby plan limits. Pro plan needed for per-minute crons.
- Legacy routes: replaced with redirects rather than deleted, to avoid 404s from bookmarks/links. /wallboard kept intact for TV display use.
- Logout clears Zustand store (activeMemberId=null) before signOut to prevent stale state for next user.
- family_id propagated from wallet/children tables into all inserts — required by RLS family_isolation policies for authenticated sessions.
</decisions_made>

<blockers>

- Adam/Alim switch bug: after logout+login on same device, pages may briefly show previous user's data. Root cause: Zustand persist + useEffect timing. Mitigations applied but not yet confirmed working (deploy pending). If still broken after deploy, next step: check if kid is landing on old route or if KidInitializer timing is still the issue.
</blockers>

<context>
The app is live at kids-motivation.vercel.app. Family has two kids (Adam, Alim) with IDs 'adam'/'alim' in DB, family_id = 56152e30-45fd-445b-aab1-cd5636fc8d73. Wallet rows exist for both. RLS policies on wallet_transactions, streaks now have anon policies + family_id in inserts.

Next milestone planning (/gsd:complete-milestone then /gsd:new-milestone) when ready to proceed to Phase 3.x (notifications → family chat → media).
</context>

<next_action>
IMMEDIATE: Add missing sections to KidDayFillForm (option A — direct fix, no GSD plan needed):

1. Read components/kid/KidDayFillForm.tsx (full file)
2. Read components/DailyModal.tsx lines 90-460 (state + save logic for exercises/sections)
3. Add to KidDayFillForm:
   a) 🏃 Home exercises — load via flexibleApi.getExerciseTypes(), toggle + quantity input, save via saveHomeExercises()
   b) 🏫 Sections — load via getSectionsForDate(childId, date), toggle attended, progress note, coach rating (1-5 stars), save via saveSectionVisit()
   c) Award coins for coach rating via awardCoinsForSport() — same as DailyModal line 449-451
4. Wire into handleSubmit Promise.all
5. Commit + push

Auth callback fix already deployed (child → /kid, parent → /parent/dashboard).
Adam/Alim issue should be resolved — user needs to test.
</next_action>
