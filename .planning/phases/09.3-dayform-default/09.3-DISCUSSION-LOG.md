# Phase 9.3: dayform-default - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-19
**Phase:** 9.3-dayform-default
**Areas discussed:** fill_style control scope, Where the control lives, Binary vs. multi-value
mapping, Negative-coin feedback, Completion ring denominator, Re-tapping a done binary row,
Behavior row mechanic, Reuse vs. rebuild

---

## fill_style control scope

| Option | Description | Selected |
|--------|-------------|----------|
| Hide the other two for now | Control only offers sticky-summary in 9.3; tile-sheet/story-stepper appear once 9.4 ships | ✓ |
| Show all 3, mark two "coming soon" | All 3 visible, two disabled with a badge/lock | |
| Show all 3, selectable but silently falls back | Child can pick unbuilt styles now; screen renders sticky-summary anyway | |

**User's choice:** Hide the other two for now.
**Notes:** None.

---

## Where the control lives

| Option | Description | Selected |
|--------|-------------|----------|
| Kid's ProfileSheet, self-serve | Same pattern as the avatar picker — no parent gate | ✓ |
| Parent Center Settings, parent-gated | Same pattern as backfill_mode | |
| Both places | Expose in both, kid-editable + parent-visible | |

**User's choice:** Kid's ProfileSheet, self-serve.
**Notes:** None.

---

## Binary vs. multi-value mapping

| Option | Description | Selected |
|--------|-------------|----------|
| Room + Behavior tags = binary; rest = panel | Matches the sketch's literal example | |
| Also make each activity/custom-block row binary | Each individual activity and custom day-block gets its own one-tap row | ✓ |
| You decide per category during planning | No lock beyond the general principle | |

**User's choice:** Also make each activity/custom-block row binary.
**Notes:** Follow-up surfaced that "mood" (5-emoji, no coin value) doesn't fit either bucket
cleanly — resolved as its own item below.

### Follow-up: Mood row

| Option | Description | Selected |
|--------|-------------|----------|
| Inline emoji-picker row, always expanded | Show the 5 emoji directly on the row, no tap-to-open | (base, refined below) |
| Treat like the other inline panels | Collapsed by default, tap to expand | |
| Drop mood from the completion ring | Not counted toward ring % | |

**User's choice:** "сделай топ с эфектом вау" (freeform) → clarified as: always-expanded emoji
row (first option), plus a noticeable/eye-catching selection animation ("заметная анимация при
выборе").
**Notes:** Confirmed via plain-text follow-up exchange, not a second AskUserQuestion round.

---

## Negative-coin feedback

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, show negative float + total dips | Distinct loss styling (red/muted, downward drift), total genuinely decreases | ✓ |
| Only ever show positive feedback | Suppress floating animation for losses | |
| Total updates silently, no floating number either way for losses | No loss animation at all | |

**User's choice:** Yes, show negative float + total dips.
**Notes:** None.

---

## Completion ring denominator

| Option | Description | Selected |
|--------|-------------|----------|
| Only categories with something to do | Excludes unconfigured categories (no sections, no tags set up) entirely | ✓ |
| Fixed set of core categories always counted | Room/grades/behavior/reading always count; rest are bonus | |
| Every visible row on screen counts equally | Denominator = count of rendered rows | |

**User's choice:** Only categories with something to do.
**Notes:** None.

---

## Re-tapping a done binary row

| Option | Description | Selected |
|--------|-------------|----------|
| Toggles back off | Real on/off switch, freely re-tappable before save | ✓ |
| Locked once marked done | Requires a longer-press/edit affordance to undo | |

**User's choice:** Toggles back off.
**Notes:** None.

---

## Behavior row: binary vs. tag-picker

| Option | Description | Selected |
|--------|-------------|----------|
| One tap opens the tag picker inline | Row is a single tap target; expands the same inline multi-select panel used by other multi-value categories | ✓ |
| True single toggle: one default tag only | Literal on/off for one fixed tag, full picker moved elsewhere | |
| You decide during planning | No lock now | |

**User's choice:** One tap opens the tag picker inline.
**Notes:** Effectively moves "behavior" from a literal binary switch into the inline-panel
bucket mechanically, while still satisfying DAYFORM-04's "no full sheet/accordion" principle
since it's reachable directly from the main row list.

---

## Replace vs. layer the existing form

| Option | Description | Selected |
|--------|-------------|----------|
| New render layer over existing state/logic | Reuse all state/save/award-call logic; only swap the JSX render tree | ✓ |
| Fresh component, old one kept as dead code for now | Build new from scratch, old accordion left unused | |

**User's choice:** New render layer over existing state/logic.
**Notes:** Matches CLAUDE.md's "preserve original code and logic, change only what's necessary."

---

## Claude's Discretion

- Exact CSS/visual detail of the "loss" floating-feedback treatment beyond "distinct from a
  gain" (red/muted + downward drift).
- Exact animation curve/scale for the mood row's "wow" selection effect, within the existing
  reduced-motion guard pattern.
- Whether the `fill_style` migration is bundled with this phase's first plan or split out.
- Precise UI treatment of the Phase-9.3-only `fill_style` control in ProfileSheet (single-option
  confirmation vs. minimal picker vs. no visible picker yet).

## Deferred Ideas

None — `tile-sheet` and `story-stepper` (DAYFORM-02/03) are correctly deferred to Phase 9.4 per
the existing roadmap split, not a new deferral from this session.
