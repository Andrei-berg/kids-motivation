---
phase: 09.3-dayform-default
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/kid/CoinAnimation.tsx
  - app/globals.css
autonomous: true
requirements: [DAYFORM-05]

must_haves:
  truths:
    - "A coin flyup can be rendered at the position of the row that triggered it, not only at a fixed spot on screen"
    - "A negative coin delta renders visually distinct from a gain: K.danger colour and downward drift (DAYFORM-05 / D-11)"
    - "A user with prefers-reduced-motion still sees the number, with no motion"
    - "The existing save-time and award-confirmation flyups keep working unchanged"
  artifacts:
    - path: "components/kid/CoinAnimation.tsx"
      provides: "trigger(amount, anchor?) + CoinFlyup rendering gain/loss/anchored/reduced-motion variants"
      exports: ["useCoinAnimation", "CoinFlyup"]
    - path: "app/globals.css"
      provides: "coin-flyup-loss keyframes, anchored wrapper, static (reduced-motion) variant, kidMoodPop keyframes"
      contains: "coin-flyup-loss"
  key_links:
    - from: "components/kid/CoinAnimation.tsx"
      to: "app/globals.css"
      via: "className strings"
      pattern: "coin-flyup-number"
---

<objective>
Extend the ONE existing coin-feedback mechanism (`useCoinAnimation` / `CoinFlyup` +
`.coin-flyup-number` in `app/globals.css`) so it can (a) fire anchored at the tapped row's screen
position and (b) render a visually distinct negative "loss" variant — plus the shared mood-pop
keyframe the mood row needs, all behind the project's reduced-motion double-guard.

Purpose: DAYFORM-05 / D-11 / D-12. Per-row live feedback must reuse this mechanism rather than
introducing a parallel `.float-plus` system.
Output: backward-compatible `trigger(amount, anchor?)`, `CoinFlyup` gain/loss/anchored/static
rendering, and the new CSS keyframes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.3-dayform-default/09.3-CONTEXT.md
@.planning/phases/09.3-dayform-default/09.3-PATTERNS.md
@.planning/phases/09.3-dayform-default/09.3-UI-SPEC.md
@CLAUDE.md
</context>

<interfaces>
<!-- Current contract (must stay backward compatible: two existing call sites in
     components/kid/KidDayFillForm.tsx pass a single number) -->

```ts
// components/kid/CoinAnimation.tsx — as built today
interface FlyupInstance { id: number; amount: number; x: number }
export function useCoinAnimation(): { flyups: FlyupInstance[]; trigger: (amount: number) => void }
export function CoinFlyup({ flyups }: { flyups: FlyupInstance[] }): JSX.Element | null
```

Existing CSS (app/globals.css ~lines 4386-4410): `.coin-flyup-container`
(`position: fixed; bottom: 120px; left: 0; right: 0; z-index: 9999`), `.coin-flyup-number`
(`color: #fbbf24; animation: coin-flyup 1.2s ease-out forwards`), `@keyframes coin-flyup`
(rises to `translateY(-100px)`, fades out).

Reduced-motion double-guard precedent: `components/design/atoms.tsx` `Tick` — keyframes injected
inside `@media (prefers-reduced-motion: no-preference)` plus a post-mount
`window.matchMedia('(prefers-reduced-motion: reduce)').matches` check stored in state.

Palette: `K.danger` = `#D84C5E`, `K.mint` = `#12B886`, `K.berry` = `#FF6B8A`
(`components/kid/design/kidTheme.ts`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Loss / anchored / reduced-motion CSS + mood-pop keyframe</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (lines 4380-4430 — the existing `.coin-flyup-container`,
      `.coin-flyup-number`, `@keyframes coin-flyup`, and the animation section they sit in)
    - components/kid/design/kidTheme.ts (`K.danger`, `K.berry` hex values)
    - .planning/phases/09.3-dayform-default/09.3-UI-SPEC.md ("Live coin feedback (D-11, D-12)"
      and "Always-expanded row (mood only — D-08)")
  </read_first>
  <action>
    Immediately after the existing `@keyframes coin-flyup` block in `app/globals.css`, add four
    things (do not modify or reorder the existing `.coin-flyup-container` / `.coin-flyup-number` /
    `@keyframes coin-flyup` rules — legacy save-time flyups depend on them byte-for-byte):

    1. `.coin-flyup-anchored` — wrapper for row-anchored flyups:
       `position: fixed; inset: 0; pointer-events: none; z-index: 9999;`. Individual anchored
       numbers inside it get their `left`/`top` from inline styles supplied by the component, so this
       rule must not set any positioning on children.

    2. `.coin-flyup-number.loss` — colour only, outside any media query:
       `color: #D84C5E;` (`K.danger`). This guarantees the negative valence reads even when motion
       is disabled.

    3. Inside `@media (prefers-reduced-motion: no-preference) { … }`:
       `.coin-flyup-number.loss { animation: coin-flyup-loss 1.2s ease-out forwards; }` and
       `@keyframes coin-flyup-loss` with the same 3-stop shape as `coin-flyup` but drifting DOWN:
       `0% { opacity: 1; transform: translateX(-50%) translateY(0) scale(1); }`
       `30% { opacity: 1; transform: translateX(-50%) translateY(18px) scale(1.15); }`
       `100% { opacity: 0; transform: translateX(-50%) translateY(60px) scale(0.85); }`
       Also inside the same media query add `.kid-mood-pop { animation: kidMoodPop 0.45s
       cubic-bezier(.2,.9,.3,1.4); }` and `@keyframes kidMoodPop`:
       `0% { transform: scale(1); }`
       `35% { transform: scale(1.22) rotate(-4deg); box-shadow: 0 0 0 6px rgba(255,107,138,0.28); }`
       `70% { transform: scale(0.96) rotate(2deg); }`
       `100% { transform: scale(1) rotate(0deg); box-shadow: none; }`
       (`rgba(255,107,138,…)` is `K.berry` — the one-off celebration glow permitted by UI-SPEC for
       the mood pop only.)

    4. `.coin-flyup-number.static` — the reduced-motion fallback the component applies from its JS
       check: `animation: none; opacity: 1;` (specificity 0,2,0 beats the base rule, so no
       `!important`).

    Prefix the block with a comment naming Phase 09.3 / D-08 / D-11 and stating that the loss and
    mood-pop animations are double-guarded (media query + post-mount JS check), per the 05.3-02
    Stamp precedent.
  </action>
  <verify>
    <automated>grep -v '^\s*/\*' app/globals.css | grep -c "coin-flyup-loss"</automated>
    <automated>grep -v '^\s*/\*' app/globals.css | grep -c "kidMoodPop"</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*/\*' app/globals.css | grep -c "coin-flyup-loss"` returns 2 or more (the rule and
      the keyframes name).
    - `grep -v '^\s*/\*' app/globals.css | grep -c "kidMoodPop"` returns 2 or more.
    - `grep -c "coin-flyup-anchored" app/globals.css` and
      `grep -c "coin-flyup-number.static" app/globals.css` each return 1 or more.
    - `grep -c "prefers-reduced-motion: no-preference" app/globals.css` increased by at least 1
      relative to before the edit, and both `coin-flyup-loss` and `kidMoodPop` keyframes sit inside
      such a block.
    - The pre-existing `@keyframes coin-flyup` block and `.coin-flyup-number` base rule are unchanged
      (`git diff app/globals.css` shows additions only, zero deletions/modifications in that range).
    - `npm run build` completes successfully.
  </acceptance_criteria>
  <done>Loss, anchored-wrapper, reduced-motion-static, and mood-pop CSS all present; existing flyup CSS untouched.</done>
</task>

<task type="auto">
  <name>Task 2: trigger(amount, anchor?) + gain/loss/anchored/static rendering in CoinFlyup</name>
  <files>components/kid/CoinAnimation.tsx</files>
  <read_first>
    - components/kid/CoinAnimation.tsx (the full 46-line file — the current `FlyupInstance`,
      `trigger`, and `CoinFlyup` contract)
    - components/design/atoms.tsx (the `Tick` component, ~lines 258-317 — the exact
      `reducedMotion()` / post-mount `useState` guard idiom to copy)
    - app/globals.css (the classes added by task 1)
    - components/kid/KidDayFillForm.tsx (lines 816 and 631 — the two existing single-argument
      `triggerCoinFlyup(...)` call sites that must keep compiling and behaving identically)
  </read_first>
  <action>
    Extend `components/kid/CoinAnimation.tsx` additively — the two existing single-argument call
    sites in `KidDayFillForm.tsx` must keep their current behaviour with no edit:

    - `FlyupInstance` gains one optional field: `anchor?: { left: number; top: number }` (viewport
      pixels). Keep `x` for the unanchored path.
    - `trigger` signature becomes
      `(amount: number, anchor?: { left: number; top: number } | null) => void`. Keep the
      `if (amount === 0) return` early exit, the `++counter.current` id, the randomised
      `x = 30 + Math.random() * 40` for the unanchored case, and the 1200ms `setTimeout` cleanup
      exactly as they are. When `anchor` is supplied, store it on the instance.
    - `CoinFlyup` splits `flyups` into unanchored and anchored groups. Unanchored renders exactly the
      current markup (`div.coin-flyup-container` > `span.coin-flyup-number` with
      `left: ${f.x}%`) — unchanged. Anchored renders a second wrapper
      `div.coin-flyup-anchored` (also `aria-hidden="true"`) whose children are
      `span.coin-flyup-number` with inline
      `{ left: f.anchor.left, top: f.anchor.top, position: 'absolute' }`.
    - Both groups append the `loss` class when `f.amount < 0`, and the `static` class when the
      reduced-motion check is true. Build the class string as
      `'coin-flyup-number' + (f.amount < 0 ? ' loss' : '') + (reduced ? ' static' : '')`.
    - Reduced-motion detection follows the `Tick` idiom exactly: a module-level
      `function reducedMotion(): boolean` that returns false when `window`/`matchMedia` is
      unavailable and otherwise `window.matchMedia('(prefers-reduced-motion: reduce)').matches`, read
      once in a post-mount `useEffect` into `const [reduced, setReduced] = useState(false)`. Because
      `CoinFlyup` now uses hooks, move its `if (flyups.length === 0) return null` early exit to AFTER
      the hook calls (never call hooks conditionally).
    - Keep the text content rule as-is: `{f.amount > 0 ? '+' + f.amount : f.amount} 💰` (a negative
      amount already prints its own minus sign).
    - Export the anchor shape as `export interface FlyupAnchor { left: number; top: number }` so the
      consumer in plan 04 can type its ref without re-declaring it.

    Do not change the container's `z-index`, the 1200ms lifetime, or the `💰` glyph. Do not import
    `K` for the loss colour — the colour lives in CSS (task 1) so the class is the single source.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -c "FlyupAnchor" components/kid/CoinAnimation.tsx</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `components/kid/CoinAnimation.tsx` exports `useCoinAnimation`, `CoinFlyup`, and `FlyupAnchor`.
    - `grep -c "coin-flyup-anchored" components/kid/CoinAnimation.tsx` returns 1 or more and
      `grep -c "' loss'" components/kid/CoinAnimation.tsx` returns 1 or more.
    - `grep -c "prefers-reduced-motion: reduce" components/kid/CoinAnimation.tsx` returns 1
      (the JS half of the double-guard).
    - `grep -c "amount === 0" components/kid/CoinAnimation.tsx` returns 1 (the zero-delta early exit
      is preserved, so no-op fills never flash a flyup).
    - `npx tsc --noEmit` exits 0 with `KidDayFillForm.tsx`'s two existing single-argument
      `triggerCoinFlyup(...)` calls unedited.
    - `npm run build` completes successfully; `npm test` still passes.
  </acceptance_criteria>
  <done>`trigger` accepts an optional anchor, `CoinFlyup` renders gain/loss/anchored/reduced-motion variants, and the legacy call sites are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client state → DOM | Coin amounts rendered into the DOM as display-only text; no server call crosses this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-093-06 | Tampering | flyup amounts | mitigate | Flyups are presentation only — `CoinAnimation.tsx` performs no fetch, no Supabase call, and no wallet mutation. `/api/wallet/award` remains the sole crediting path (CLAUDE.md money rule); a tampered client number changes pixels, never a balance. |
| T-093-07 | Denial of Service | unbounded flyup accumulation | accept | Each instance is removed by the existing 1200ms `setTimeout`; the `amount === 0` early exit suppresses no-op events. Worst case is a handful of concurrent absolutely-positioned spans. |
| T-093-08 | Information Disclosure | anchored flyup position | accept | The anchor is a viewport rect of an element the user just tapped; it exposes nothing not already on screen. |
| T-093-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — CSS and an existing local component only. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0.
2. `npm run build` succeeds; `npm test` passes.
3. `git diff app/globals.css` shows additions only in the coin-flyup region (no modified/deleted lines).
4. `grep -c "coin-flyup-loss\|kidMoodPop\|coin-flyup-anchored\|coin-flyup-number.static" app/globals.css` is >= 4.
</verification>

<success_criteria>
- `trigger(amount)` behaves exactly as before; `trigger(amount, anchor)` renders at the anchor.
- Negative amounts render with `K.danger` colour and a downward-drift keyframe.
- Reduced-motion users get the number with `animation: none`.
- Mood-pop keyframe exists for plan 05 to consume.
</success_criteria>

<output>
Create `.planning/phases/09.3-dayform-default/09.3-02-SUMMARY.md` when done.
</output>
