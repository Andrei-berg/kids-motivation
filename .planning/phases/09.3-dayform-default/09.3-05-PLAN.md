---
phase: 09.3-dayform-default
plan: 05
type: execute
wave: 3
depends_on: [09.3-04]
files_modified:
  - components/kid/day-blocks/RoomBlock.tsx
  - components/kid/day-blocks/ActivitiesBlock.tsx
  - components/kid/day-blocks/CustomBlock.tsx
  - components/kid/KidDayFillForm.tsx
autonomous: true
requirements: [DAYFORM-04]

must_haves:
  truths:
    - "Binary categories are answered with one tap directly on their row — no sheet, no accordion (DAYFORM-04 / D-05)"
    - "Each extra activity and each custom day-block has its own row, not one shared panel (D-05)"
    - "Re-tapping a done binary row toggles it straight back off with no confirm step (D-10)"
    - "Multi-value categories (grades, exercises, sport, reading, behavior) expand a panel in place below their own row (D-06 / D-07)"
    - "The mood picker is always visible with no tap-to-open step, and selecting an emoji plays a pronounced animation that respects prefers-reduced-motion (D-08)"
    - "The single-open accordion (CollapsibleRow / openId) is gone from the day-fill form"
    - "Parent-configured block order, the dayBlocksEnabled/visibleBlocks gating, and the nothing-to-fill empty state all behave exactly as before"
  artifacts:
    - path: "components/kid/KidDayFillForm.tsx"
      provides: "sticky-summary row tree: quick rows, in-place panels, always-expanded mood"
      contains: "InlinePanelRow"
    - path: "components/kid/day-blocks/RoomBlock.tsx"
      provides: "one-tap QuickRow per room task + unchanged photo-proof affordance"
    - path: "components/kid/day-blocks/ActivitiesBlock.tsx"
      provides: "one QuickRow per extra activity with its coin tag"
    - path: "components/kid/day-blocks/CustomBlock.tsx"
      provides: "one QuickRow per custom day-block, parent-filled blocks disabled"
  key_links:
    - from: "components/kid/KidDayFillForm.tsx"
      to: "components/kid/day-fill/InlinePanelRow.tsx"
      via: "panel row per multi-value category"
      pattern: "InlinePanelRow"
    - from: "components/kid/day-blocks/RoomBlock.tsx"
      to: "components/kid/day-fill/QuickRow.tsx"
      via: "one row per room task"
      pattern: "QuickRow"
---

<objective>
Replace the single-open accordion render tree with the sticky-summary interaction model: one-tap
binary rows for room tasks / each activity / each custom block, in-place panels for the multi-value
categories, and an always-expanded mood row with a "wow" selection animation.

Purpose: DAYFORM-04 (and D-05/D-06/D-07/D-08/D-10). Per D-13 this is a render-layer change ONLY —
every `useState`, `sectionCoins`, save and `/api/wallet/award` path stays as it is.
Output: the day-fill form's rows, with `CollapsibleRow`/`openId` removed from this screen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.3-dayform-default/09.3-CONTEXT.md
@.planning/phases/09.3-dayform-default/09.3-PATTERNS.md
@.planning/phases/09.3-dayform-default/09.3-UI-SPEC.md
@.planning/phases/09.3-dayform-default/09.3-03-SUMMARY.md
@.planning/phases/09.3-dayform-default/09.3-04-SUMMARY.md
@.claude/skills/sketch-findings-kids-motivation/references/day-fill-interaction.md
@CLAUDE.md
</context>

<interfaces>
<!-- Built in wave 1 (plan 03). Consume as-is. -->

```ts
// components/kid/day-fill/QuickRow.tsx — default export
{ label: string; icon?: React.ReactNode; done: boolean;
  onToggle: (e: React.MouseEvent<HTMLElement>) => void; disabled?: boolean; trailing?: React.ReactNode }

// components/kid/day-fill/InlinePanelRow.tsx — default export
{ label: string; icon?: React.ReactNode; done: boolean; open: boolean;
  onToggle: (e: React.MouseEvent<HTMLElement>) => void; disabled?: boolean;
  trailing?: React.ReactNode; children: React.ReactNode }
```

CSS available from plan 02 (`app/globals.css`): `.kid-mood-pop` (0.45s pop, inside
`@media (prefers-reduced-motion: no-preference)`), `@keyframes kidMoodPop`.

Reduced-motion JS half of the guard (copy the idiom from `components/design/atoms.tsx` `Tick`):
`window.matchMedia('(prefers-reduced-motion: reduce)').matches`, read post-mount into state.

Anchor contract from plan 04: the form's root has one `onPointerDownCapture` handler that anchors
each coin flyup to `(e.target as HTMLElement).closest('[data-fill-row]')`. **Every row this plan
renders must sit inside an element carrying `data-fill-row`** or its flyup falls back to the raw
pointer coordinates.

Reused unchanged from `KidDayFillForm.tsx` (D-13): `toggleRoomTask`, `toggleActivity`,
`toggleCustomBlock`, `toggleExercise`, `updateExerciseQuantity`, `toggleSectionAttended`,
`updateSectionCoachRating`, `toggleTagSelection`, `handleProposeTags`, `setMood`, the
`gradesBody` / `exercisesBody` / `sectionsBody` / `readingBody` / `behaviorBody` JSX bodies, the
`done` map, `sectionCoins`, `visibleBlocks`, `dayBlocksEnabled`, `isLocked`, `nothingToday`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: One-tap binary rows — room tasks, each activity, each custom block</name>
  <files>components/kid/day-blocks/RoomBlock.tsx, components/kid/day-blocks/ActivitiesBlock.tsx, components/kid/day-blocks/CustomBlock.tsx</files>
  <read_first>
    - components/kid/day-blocks/RoomBlock.tsx (the full file — the 2-col task grid being replaced and
      the photo-proof section that must survive byte-identical)
    - components/kid/day-blocks/ActivitiesBlock.tsx (the full file — current `LedgerRow`-per-activity button)
    - components/kid/day-blocks/CustomBlock.tsx (the full file — including the
      `who_fills === 'parent'` read-only guard and the `opacity: 0.6` treatment)
    - components/kid/day-fill/QuickRow.tsx (the prop shape built in plan 03)
    - components/kid/design/kidTheme.ts (`K`)
    - .planning/phases/09.3-dayform-default/09.3-CONTEXT.md (D-05, D-10)
  </read_first>
  <action>
    These three renderers are consumed ONLY by `components/kid/KidDayFillForm.tsx` (the parent
    `DailyModal` has its own local renderers), so changing them does not touch the parent UI.
    Keep every prop signature exactly as it is — callers must not change.

    **RoomBlock.tsx** — replace the `display: 'grid', gridTemplateColumns: '1fr 1fr'` task grid with a
    `display: 'flex', flexDirection: 'column', gap: 8` list of one `QuickRow` per task:
    `label={task.name}`, `icon={task.icon ?? '🏠'}`, `done={checked[task.id] ?? false}`,
    `onToggle={() => onToggle(task.id)}` (ignore the event argument), `disabled={isLocked}`. Wrap each
    in `<div data-fill-row key={task.id}>`. Leave the photo-proof section (the `proofLocalUrl`
    preview / `takePhoto` button / hidden `<input type="file">`) completely unchanged, still rendered
    after the task list. The 60%-threshold award rule lives in `sectionCoins` and the award route —
    do not add, mirror, or reference any threshold logic here.

    **ActivitiesBlock.tsx** — replace each `LedgerRow`-bearing button with a `QuickRow`:
    `label={act.name}`, `icon={act.emoji ?? '⭐'}`, `done={checked.has(act.id)}`,
    `onToggle={() => onToggle(act.id)}`, `disabled={isLocked}`, and `trailing` = the activity's own
    coin tag when `act.coins !== 0`: an inline-flex span using `K.fNum`, `fontSize: 13`,
    `fontWeight: 700`, `color: act.coins < 0 ? K.danger : K.mintDeep`, showing
    `{act.coins > 0 ? '+' : ''}{act.coins}` followed by `<Coin size={15} />` (import `Coin` from
    `@/components/kid/design/atoms` — the same tag shape as `trailingFor()` in `KidDayFillForm`).
    Wrap each row in `<div data-fill-row key={act.id}>`. Coin values still come from the activity rows
    themselves — never hardcode an amount. `LedgerRow` may be dropped from this file's imports if it
    becomes unused.

    **CustomBlock.tsx** — replace the single button with one `QuickRow`: `label={block.name}`,
    `icon={block.icon ?? '⭐'}`, `done={done}`, `onToggle={() => onToggle(block)}`,
    `disabled={isLocked || readOnly}` (keep the existing
    `const readOnly = block.who_fills === 'parent'`), and `trailing` = the same coin-tag span shape as
    above driven by `block.price` when `hasPrice`. Preserve the read-only affordance by wrapping the
    row in `<div data-fill-row style={{ opacity: readOnly ? 0.6 : 1 }}>`. The T-056-12 comment at the
    top of the file stays.

    Allowed type scale is 13/16/20/22px at weights 700/900 — the coin tags are 13/700. Do not
    introduce 11px/12px text or weight 800 in these files.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -lc "QuickRow" components/kid/day-blocks/RoomBlock.tsx components/kid/day-blocks/ActivitiesBlock.tsx components/kid/day-blocks/CustomBlock.tsx</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All three files import `QuickRow` from `@/components/kid/day-fill/QuickRow` and render exactly
      one `QuickRow` per room task / per activity / per custom block.
    - `grep -c "data-fill-row" components/kid/day-blocks/RoomBlock.tsx components/kid/day-blocks/ActivitiesBlock.tsx components/kid/day-blocks/CustomBlock.tsx`
      returns 1 or more for each file.
    - `grep -c "gridTemplateColumns" components/kid/day-blocks/RoomBlock.tsx` returns 0 (grid replaced by a row list).
    - `grep -c "proofInputRef\|takePhoto\|retakePhoto" components/kid/day-blocks/RoomBlock.tsx` is unchanged
      from before the edit (photo-proof preserved).
    - `grep -c "who_fills === 'parent'" components/kid/day-blocks/CustomBlock.tsx` returns 1 and the
      resulting `QuickRow` receives `disabled` when it is true.
    - `grep -c "fontWeight: 800\|fontSize: 1[12]" components/kid/day-blocks/RoomBlock.tsx components/kid/day-blocks/ActivitiesBlock.tsx components/kid/day-blocks/CustomBlock.tsx`
      returns 0 for each file.
    - No prop signature changed: `git diff` shows the three `interface *Props` blocks untouched.
    - `npx tsc --noEmit` exits 0; `npm run build` succeeds; `npm test` passes.
  </acceptance_criteria>
  <done>Room tasks, activities, and custom blocks each toggle with one tap on their own row, with coin tags and the photo-proof affordance intact.</done>
</task>

<task type="auto">
  <name>Task 2: In-place panel rows + row-tree swap in KidDayFillForm</name>
  <files>components/kid/KidDayFillForm.tsx</files>
  <read_first>
    - components/kid/KidDayFillForm.tsx (lines 434-437 — `openId` / `toggleSection`;
      lines 1136-1158 — the `done` map and `trailingFor`; lines 1160-1224 — `renderSection`,
      `renderBuiltinBlock`, `renderCustomBlock`; lines 1226-1290 — the render tree, the flag-on /
      flag-off branches, the `nothingToday` empty state)
    - components/kid/day-fill/InlinePanelRow.tsx (prop shape from plan 03)
    - .planning/phases/09.3-dayform-default/09.3-UI-SPEC.md ("Layout structure",
      "Row interaction types", the D-07 "one at a time" nuance)
    - .planning/phases/09.3-dayform-default/09.3-CONTEXT.md (D-05, D-06, D-07, D-13)
  </read_first>
  <action>
    Swap the row tree, preserving structure and gating exactly:

    1. Replace `const [openId, setOpenId] = useState<string | null>(null)` and `toggleSection` with
       `const [openPanels, setOpenPanels] = useState<Set<string>>(() => new Set())` and
       `function togglePanel(id: string)` that returns a NEW `Set` with the id added or removed.
       Comment the D-07 resolution: panels are independent, and an open panel is never auto-closed by
       opening another — auto-closing would hide a half-entered grade or reading note (UI-SPEC's
       binding constraint is "in place", not "only one at a time").

    2. Add `renderPanelRow(id, title, icon, isDone, body, key?)` — same call shape as the old
       `renderSection`, a plain function (NOT a nested component, so the controlled inputs inside
       `body` never remount), returning
       `<div data-fill-row key={key ?? id}><InlinePanelRow label={title} icon={icon} done={isDone}
       open={openPanels.has(id)} onToggle={() => togglePanel(id)} trailing={trailingFor(id, isDone)}
       disabled={isLocked}>{body}</InlinePanelRow></div>`.

    3. Add `renderGroup(id, title, icon, isDone, body, key?)` for the two categories whose sub-items
       are themselves one-tap rows (room, activities — D-05 forbids hiding them under one shared
       panel). It renders a NON-interactive caption line — no button, no `aria-expanded`, no chevron —
       built from the same visual vocabulary as the rows: the 5px status rail
       (`isDone ? K.mint : K.line`), the 34×34 icon chip (`background: isDone ? K.mintSoft : K.lineSoft`,
       `borderRadius: 11`, `fontSize: 18`), the title in `K.fDisp` 16/700 `K.ink`, and
       `trailingFor(id, isDone)` on the right — followed directly by `body`, all inside a
       `flex column, gap: 8` wrapper. The caption must never hide or collapse its rows.

    4. Rewire the dispatchers, changing ONLY which row component each case produces — keep every
       existing gate condition, every `block.name`/`block.icon ?? …` fallback, the `visibleBlocks.map`
       iteration order (parent-configured block order, Phase 5.6) and the flag-off hardcoded sequence
       byte-for-byte:
       - `renderBuiltinBlock`: `'room'` → `renderGroup('room', …, roomBody, block.id)`;
         `'activity'` → `renderGroup('activity', …, extraActivitiesBody, block.id)` keeping the
         `activities.length > 0` gate; `'grade'` / `'exercise'` / `'sport'` / `'book'` →
         `renderPanelRow(...)` with their existing gates; `'behavior'` and `default` → `null` as today.
       - `renderCustomBlock` → `<div data-fill-row key={block.id}><CustomBlock … /></div>` (the
         component is already a one-tap row after task 1) — no panel wrapper.
       - the flag-off branch → `renderGroup` for room and activities, `renderPanelRow` for grades,
         exercises, sport, reading, with their existing conditions unchanged.
       - the top-level behavior row (rendered before the blocks, still gated on
         `behaviorTags.length > 0`) → `renderPanelRow('behavior', t('kidFillForm.behaviorSection'),
         '🌟', done.behavior, behaviorBody)`. Per D-06 the behavior row is a tap target that expands
         the existing multi-select tag panel — NOT a switch. Do not reintroduce a boolean
         `good_behavior` toggle: `behaviorBody` still only proposes `status='pending'` marks.
       - the `nothingToday` empty-state card stays exactly as it is.

    5. In `trailingFor`, change `fontWeight: 800` to `fontWeight: 700` (UI-SPEC caps weights at
       700/900) and leave everything else — including the `c < 0 ? K.danger : K.mintDeep` colour
       branch — unchanged. (UI-SPEC's Typography note suggesting `K.mangoDeep` for positive tags is
       superseded by its own Color section, which reserves mango for the running total and mint for
       positive coin deltas; keeping `K.mintDeep` is also the preserve-original-code choice.)

    Leave the mood row on `renderSection` for now — task 3 rebuilds it and then deletes the dead
    accordion code, so the file keeps compiling after this task.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -c "InlinePanelRow" components/kid/KidDayFillForm.tsx</automated>
    <automated>grep -c "openId" components/kid/KidDayFillForm.tsx</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "InlinePanelRow" components/kid/KidDayFillForm.tsx` is >= 2 (import + use) and
      `grep -c "renderPanelRow" components/kid/KidDayFillForm.tsx` is >= 6 (definition + behavior,
      grade, exercise, sport, book call sites).
    - `grep -c "openPanels" components/kid/KidDayFillForm.tsx` is >= 3 and
      `grep -c "setOpenId\|toggleSection(" components/kid/KidDayFillForm.tsx` returns 0.
    - `grep -c "renderGroup" components/kid/KidDayFillForm.tsx` is >= 3 (definition + room + activity).
    - `grep -c "data-fill-row" components/kid/KidDayFillForm.tsx` is >= 3.
    - `grep -c "fontWeight: 800" components/kid/KidDayFillForm.tsx` returns 0.
    - `grep -c "good_behavior" components/kid/KidDayFillForm.tsx` returns 0 (behavior stays
      propose-only) and `grep -c "nothingToday" components/kid/KidDayFillForm.tsx` is unchanged.
    - `git diff components/kid/KidDayFillForm.tsx` shows no change to `sectionCoins`, `handleSubmit`,
      `creditAwardAndFinish`, `visibleBlocks`, or any `gradesBody`/`exercisesBody`/`sectionsBody`/
      `readingBody`/`behaviorBody` body content.
    - `npx tsc --noEmit` exits 0; `npm run build` succeeds; `npm test` passes.
  </acceptance_criteria>
  <done>Multi-value categories expand in place from independent rows; room and activities render as captioned groups of one-tap rows; all gating and block ordering preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Always-expanded mood row with the "wow" animation + delete the dead accordion</name>
  <files>components/kid/KidDayFillForm.tsx</files>
  <read_first>
    - components/kid/KidDayFillForm.tsx (lines 1288-1307 — the mood `renderSection` call and the
      `MOOD_OPTIONS.map` button block; lines 1160-1178 — `renderSection`; line 30 — the
      `CollapsibleRow` import)
    - components/design/atoms.tsx (the `Tick` component's `reducedMotion()` + post-mount `useEffect`
      guard idiom to copy)
    - app/globals.css (`.kid-mood-pop` / `@keyframes kidMoodPop` added in plan 02)
    - .planning/phases/09.3-dayform-default/09.3-CONTEXT.md (D-08 and its Claude's-Discretion note)
    - .planning/phases/09.3-dayform-default/09.3-UI-SPEC.md ("Always-expanded row (mood only — D-08)")
  </read_first>
  <action>
    1. Replace the mood `renderSection(...)` call with an always-expanded group: reuse the
       `renderGroup` helper from task 2 — `renderGroup('mood', t('kidFillForm.moodSection'), '✨',
       done.mood, <the existing MOOD_OPTIONS.map block>)`. There is no tap-to-open step and no
       chevron: the 5 mood buttons render under the caption at all times (D-08). Keep all 5 options
       (`MOOD_OPTIONS_STATIC`: happy/neutral/meh/sad/tired) — do not truncate to the sketch's 4 — and
       keep each button's existing markup, copy, `minHeight: 64`, and the
       `setMood(prev => prev === m.key ? null : m.key)` toggle semantics unchanged.

    2. Add the "wow" selection animation. Introduce `const [moodPop, setMoodPop] = useState<{ key: string; n: number }>({ key: '', n: 0 })`
       and, in the mood button's existing `onClick` (after the `setMood` call), `setMoodPop(p => ({ key: m.key, n: p.n + 1 }))`.
       Apply `className={!reduced && moodPop.key === m.key ? 'kid-mood-pop' : undefined}` and give the
       button `key={`${m.key}-${moodPop.key === m.key ? moodPop.n : 0}`}` so a repeat selection
       restarts the CSS animation instead of silently not replaying it.

    3. Reduced-motion double-guard (the CSS half already lives inside
       `@media (prefers-reduced-motion: no-preference)` from plan 02): add the JS half with the exact
       `Tick` idiom — a `const [reduced, setReduced] = useState(false)` plus a post-mount
       `useEffect(() => { setReduced(window.matchMedia?.('(prefers-reduced-motion: reduce)').matches ?? false) }, [])`
       (SSR-safe: guard `typeof window` / `matchMedia` availability). When `reduced` is true the class
       is never applied, so the selected state changes instantly with no motion.

    4. Delete the now-dead accordion code from this file: the `renderSection` function, the
       `CollapsibleRow` import, and any leftover `openId`-era helper. Per D-13 the old accordion JSX
       does not need to be kept as a fallback — `sticky-summary` is the only wired style. Remove any
       import that becomes unused as a result (but leave `AnimatedNum`, `Coin`, `Tick`, `K`, `T` and
       the day-block renderers alone — they are all still used).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -c "CollapsibleRow" components/kid/KidDayFillForm.tsx</automated>
    <automated>grep -c "kid-mood-pop" components/kid/KidDayFillForm.tsx</automated>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CollapsibleRow" components/kid/KidDayFillForm.tsx` returns 0 and
      `grep -c "renderSection" components/kid/KidDayFillForm.tsx` returns 0.
    - `grep -c "kid-mood-pop" components/kid/KidDayFillForm.tsx` returns 1 and
      `grep -c "prefers-reduced-motion: reduce" components/kid/KidDayFillForm.tsx` returns 1.
    - `grep -c "MOOD_OPTIONS_STATIC" components/kid/KidDayFillForm.tsx` is unchanged and the array
      still holds 5 entries (`happy`, `neutral`, `meh`, `sad`, `tired`).
    - `grep -c "moodPop" components/kid/KidDayFillForm.tsx` is >= 3 (state, setter in onClick, key/className use).
    - `grep -c "minHeight: 64" components/kid/KidDayFillForm.tsx` returns 1 (mood button sizing preserved).
    - `npm run lint` exits 0 with no new `no-unused-vars` warning for `CollapsibleRow` or `openId`.
    - `npx tsc --noEmit` exits 0; `npm run build` succeeds; `npm test` passes.
  </acceptance_criteria>
  <done>Mood is always visible, pops on selection (unless reduced motion), and the accordion is fully gone from the day-fill form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kid taps → client form state | Row interactions mutate local state only; nothing new crosses to the server in this plan |
| client form state → existing save + `/api/wallet/award` | Untouched by this plan (render layer only, D-13) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-093-16 | Elevation of Privilege | `CustomBlock` row for `who_fills === 'parent'` | mitigate | The `QuickRow` receives `disabled` whenever `readOnly` is true, preserving the T-056-12 UI-level guard; `toggleCustomBlock`'s own `block.who_fills === 'parent'` early return is untouched and `/api/wallet/award` stays the authoritative backstop. |
| T-093-17 | Elevation of Privilege | locked past days | mitigate | Every new row receives `disabled={isLocked}` and each reused handler keeps its `if (isLocked) return` guard; the back-fill `day_fill_requests` gate in the award route is unchanged. |
| T-093-18 | Tampering | behavior row | mitigate | `behaviorBody` is reused verbatim: it only inserts `status='pending'` `behavior_marks` (RLS-enforced) and never writes `days.good_behavior` or credits coins. Asserted by `grep -c "good_behavior" = 0`. |
| T-093-19 | Tampering | room award threshold | mitigate | The 60%-threshold rule is not copied into `RoomBlock`; it stays in `sectionCoins` (preview) and in the award route (authority), so a per-task row cannot change what is credited. |
| T-093-20 | Denial of Service | panel remount losing user input | mitigate | `renderPanelRow` is a plain function with stable `key`s, so controlled inputs inside panel bodies are never remounted, and panels are never auto-closed while holding partial input. |
| T-093-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0; `npm run lint` exits 0; `npm run build` succeeds; `npm test` passes.
2. `grep -c "CollapsibleRow\|renderSection\|openId" components/kid/KidDayFillForm.tsx` returns 0.
3. `grep -rc "data-fill-row" components/kid/KidDayFillForm.tsx components/kid/day-blocks/RoomBlock.tsx components/kid/day-blocks/ActivitiesBlock.tsx components/kid/day-blocks/CustomBlock.tsx` is >= 1 per file.
4. `git diff components/kid/KidDayFillForm.tsx` shows zero changes to `sectionCoins`, `handleSubmit`, `creditAwardAndFinish`, and the `/api/wallet/award` fetch.
</verification>

<success_criteria>
- Binary categories: one tap on the row, re-tappable off, no sheet/accordion.
- Each activity and each custom block owns a row; room tasks are individually tappable rows.
- Grades, exercises, sport, reading, behavior expand panels in place from independent rows.
- Mood is always expanded, pops on selection, honours `prefers-reduced-motion`.
- Parent-configured block order, visibility gating, and the empty state are unchanged.
</success_criteria>

<output>
Create `.planning/phases/09.3-dayform-default/09.3-05-SUMMARY.md` when done.
</output>
