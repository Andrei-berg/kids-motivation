---
phase: 09.3-dayform-default
plan: 04
type: execute
wave: 2
depends_on: [09.3-01, 09.3-02, 09.3-03]
files_modified:
  - app/kid/(app)/day/page.tsx
  - components/kid/KidDayFillForm.tsx
autonomous: true
requirements: [DAYFORM-04, DAYFORM-05, DAYFORM-06]

must_haves:
  truths:
    - "The day form receives the child's fill_style, defaulting to sticky-summary when unset (DAYFORM-06)"
    - "A completion ring + live coin total stay on screen at all times while filling, in ~70px (DAYFORM-04)"
    - "The ring's denominator counts only categories that have something to do for this child today (D-09)"
    - "Filling any coin-bearing category fires a floating amount immediately at the tap position and ticks the running total (DAYFORM-05 / D-12)"
    - "Un-filling a category fires the negative inverse and the total ticks down (D-10 / D-11)"
    - "Opening an already-filled past day fires no flyups on mount"
    - "/api/wallet/award remains the only coin-crediting call and is unchanged"
  artifacts:
    - path: "components/kid/KidDayFillForm.tsx"
      provides: "fillStyle prop, StickySummaryBar mount, applicability map, per-fill coin feedback"
      contains: "StickySummaryBar"
    - path: "app/kid/(app)/day/page.tsx"
      provides: "fill_style read from the loaded child and passed to the form"
      contains: "fill_style"
  key_links:
    - from: "app/kid/(app)/day/page.tsx"
      to: "components/kid/KidDayFillForm.tsx"
      via: "fillStyle prop"
      pattern: "fillStyle="
    - from: "components/kid/KidDayFillForm.tsx"
      to: "lib/kid/day-fill-progress.ts"
      via: "computeFillProgress + diffSectionCoins import"
      pattern: "diffSectionCoins"
    - from: "components/kid/KidDayFillForm.tsx"
      to: "components/kid/CoinAnimation.tsx"
      via: "anchored triggerCoinFlyup call in the sectionCoins diff effect"
      pattern: "triggerCoinFlyup\\(delta"
---

<objective>
Put the always-on transparency layer on the Day screen: the form learns its `fill_style`, the mango
"coins today" strip becomes the sticky ring + live total bar, and every category fill fires
immediate floating coin feedback derived from the existing `sectionCoins` map.

Purpose: DAYFORM-04's persistent summary, DAYFORM-05's live feedback, DAYFORM-06's default.
Output: `KidDayFillForm` renders the sticky summary and reacts to every fill; the accordion rows are
left alone (plan 05 replaces them), so the app stays shippable after this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.3-dayform-default/09.3-CONTEXT.md
@.planning/phases/09.3-dayform-default/09.3-PATTERNS.md
@.planning/phases/09.3-dayform-default/09.3-UI-SPEC.md
@.planning/phases/09.3-dayform-default/09.3-01-SUMMARY.md
@.planning/phases/09.3-dayform-default/09.3-02-SUMMARY.md
@.planning/phases/09.3-dayform-default/09.3-03-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- Everything this plan consumes was built in waves 1. Do not re-derive it. -->

```ts
// lib/kid/day-fill-progress.ts (plan 03)
export interface FillProgress { doneCount: number; total: number; pct: number }
export function computeFillProgress(applicable: Record<string, boolean>, done: Record<string, boolean>): FillProgress
export interface CoinDelta { key: string; delta: number }
export function diffSectionCoins(prev: Record<string, number>, next: Record<string, number>): CoinDelta[]

// components/kid/CoinAnimation.tsx (plan 02)
export interface FlyupAnchor { left: number; top: number }
export function useCoinAnimation(): { flyups: FlyupInstance[]; trigger: (amount: number, anchor?: FlyupAnchor | null) => void }

// components/kid/day-fill/StickySummaryBar.tsx (plan 03) — default export
{ pct: number; coins: number; caption: string; ringLabel: string; lockedLabel?: string | null }

// lib/models/child.types.ts (plan 01)
Child.fill_style: 'tile-sheet' | 'story-stepper' | 'sticky-summary'

// public/locales/{ru,en}.json (plan 01)
kidFillForm.stickyEarnedToday, kidFillForm.ringLabel  // ringLabel interpolates {{pct}}
```

Existing, reused unchanged (D-13): every `useState` in `KidDayFillForm`, the `sectionCoins` /
`coinsPreview` `useMemo`s (lines 359-432), the `done` boolean map (lines 1136-1145),
`handleSubmit` / `creditAwardAndFinish` including both existing `triggerCoinFlyup(...)` call sites
(lines 816, 631), and the `/api/wallet/award` POST.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: fillStyle prop + day page wiring</name>
  <files>components/kid/KidDayFillForm.tsx, app/kid/(app)/day/page.tsx</files>
  <read_first>
    - components/kid/KidDayFillForm.tsx (lines 75-120 — `KidDayFillFormProps` and the component
      signature/destructuring)
    - app/kid/(app)/day/page.tsx (lines 60-150 — the `child` state and `api.getChild(activeMemberId)`
      load; lines 453-475 — the `<KidDayFillForm ... />` mount)
    - lib/models/child.types.ts (the `fill_style` field added in plan 01)
    - .planning/phases/09.3-dayform-default/09.3-CONTEXT.md (D-01, D-02, D-13)
  </read_first>
  <action>
    Add `fillStyle?: 'tile-sheet' | 'story-stepper' | 'sticky-summary'` to `KidDayFillFormProps`
    (optional, with a comment: per-child day-fill style from `children.fill_style`; only
    `sticky-summary` is implemented in Phase 9.3 per D-01, Phase 9.4 adds the other two branches
    here). Destructure it in the component signature and resolve
    `const style = fillStyle ?? 'sticky-summary'` (D-02 default). Render the sticky-summary tree
    unconditionally in this phase — do NOT add a placeholder branch or a fallback render path for the
    unbuilt styles — and expose the resolved value on the form's root `div` as
    `data-fill-style={style}` so the choice is observable in the DOM and by Phase 9.4.

    In `app/kid/(app)/day/page.tsx`, pass `fillStyle={child?.fill_style ?? 'sticky-summary'}` to the
    existing `<KidDayFillForm>` mount. `api.getChild` already selects `*`, so no new fetch, no new
    state, and no change to the load sequence is needed — only the prop on the existing element.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -c "fillStyle=" "app/kid/(app)/day/page.tsx"</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fillStyle" components/kid/KidDayFillForm.tsx` is >= 2 (prop declaration + resolution).
    - `grep -c "data-fill-style" components/kid/KidDayFillForm.tsx` returns 1.
    - `grep -c "fillStyle={child?.fill_style ?? 'sticky-summary'}" "app/kid/(app)/day/page.tsx"` returns 1.
    - `grep -c "getChild\|useState" "app/kid/(app)/day/page.tsx"` is unchanged from before the edit
      (no new fetch or state added — verify with `git diff` showing a one-line addition in that file).
    - `npx tsc --noEmit` exits 0; `npm run build` succeeds.
  </acceptance_criteria>
  <done>The form accepts and resolves `fillStyle`; the day page supplies it from the loaded child with the `sticky-summary` default.</done>
</task>

<task type="auto">
  <name>Task 2: Applicability map + StickySummaryBar replacing the mango strip</name>
  <files>components/kid/KidDayFillForm.tsx</files>
  <read_first>
    - components/kid/KidDayFillForm.tsx (lines 355-436 — `sectionCoins`, its inner `legacyVisible`
      helper and dependency array, `coinsPreview`; lines 1136-1145 — the `done` map;
      lines 1226-1256 — the render head and the mango "coins today" strip being replaced)
    - lib/kid/day-fill-progress.ts (the `computeFillProgress` contract from plan 03)
    - components/kid/day-fill/StickySummaryBar.tsx (its prop shape)
    - .planning/phases/09.3-dayform-default/09.3-UI-SPEC.md ("Completion ring
      denominator/numerator (D-09)", "Layout structure", "Color")
  </read_first>
  <action>
    1. Hoist the `legacyVisible` helper out of the `sectionCoins` `useMemo` into a single
       `useCallback` defined just above it — `(key: string) => !dayBlocksEnabled || visibleBlocks.some(b => b.legacy_key === key)`
       with deps `[dayBlocksEnabled, visibleBlocks]` — and have `sectionCoins` use the hoisted one
       (delete the inner duplicate, add `legacyVisible` to `sectionCoins`' dependency array). No other
       change to `sectionCoins`: its coin maths, its keys (`room`, `activity`, `grade`, `sport`,
       `book`, `custom:<id>`) and its WR-02 flag-on gating stay byte-identical.

    2. Add an `applicable` `useMemo` next to it — `Record<string, boolean>`, encoding D-09 exactly:
       `mood: true` always; `behavior: behaviorTags.length > 0`;
       `room: legacyVisible('room') && roomTasks.length > 0`;
       `activity: legacyVisible('activity') && activities.length > 0`;
       `grade: legacyVisible('grade') && dayType === 'school' && subjects.length > 0`;
       `exercise: legacyVisible('exercise') && exerciseTypes.length > 0`;
       `sport: legacyVisible('sport') && sections.length > 0`;
       `book: legacyVisible('book')`; and, when `dayBlocksEnabled`, one
       `custom:${b.id}: b.who_fills !== 'parent'` entry per visible custom block (a parent-filled
       block is nothing for the kid to do, so it is excluded, not counted incomplete). Comment it as
       the D-09 rule: nothing configured → excluded from BOTH numerator and denominator.

    3. Immediately after the existing `done` object literal (leave `done` itself untouched — the
       current renderers read it), build `const doneAll = { ...done, ...custom-block entries }` where
       each visible custom block contributes `` [`custom:${b.id}`]: !!customBlockDone[b.id] ``, then
       `const progress = computeFillProgress(applicable, doneAll)`.

    4. Replace the mango "coins today" strip (the `div` wrapping `kidFillForm.coinsToday`,
       `AnimatedNum`, `Coin size={17}` and the `isLocked` pill — currently the first block inside the
       root return) with a single `<StickySummaryBar pct={progress.pct} coins={coinsPreview}
       caption={t('kidFillForm.stickyEarnedToday')} ringLabel={t('kidFillForm.ringLabel', { pct: progress.pct })}
       lockedLabel={isLocked ? t('kidFillForm.locked') : null} />`. Delete the old strip — do not keep
       both, two coin totals on one screen is redundant and would blow the ~70px budget. Keep
       `<CoinFlyup flyups={flyups} />` exactly where it is. Keep the root div's
       `paddingBottom: 120, position: 'relative'` (the bar is `position: sticky`, which still works
       inside a relatively-positioned, non-scrolling ancestor).
       If `t('kidFillForm.coinsToday')` and `K.mangoSoft`/`K.mango` become unused imports/keys in this
       file, leave the i18n key in the locale files (other screens use it) but remove any now-dead
       local variable so lint stays clean.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -c "StickySummaryBar" components/kid/KidDayFillForm.tsx</automated>
    <automated>grep -c "kidFillForm.coinsToday" components/kid/KidDayFillForm.tsx</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "StickySummaryBar" components/kid/KidDayFillForm.tsx` returns 2 (import + usage).
    - `grep -c "kidFillForm.coinsToday" components/kid/KidDayFillForm.tsx` returns 0 (old strip gone).
    - `grep -c "computeFillProgress" components/kid/KidDayFillForm.tsx` returns 2 (import + call).
    - `grep -vn '^\s*//' components/kid/KidDayFillForm.tsx | grep -c "who_fills !== 'parent'"` is >= 1
      (parent-filled custom blocks excluded from the ring).
    - The `applicable` memo contains `mood: true` unconditionally and gates `behavior` on
      `behaviorTags.length > 0`, `sport` on `sections.length > 0`, `activity` on
      `activities.length > 0` (each verifiable by grep in the memo body).
    - `git diff components/kid/KidDayFillForm.tsx` shows NO change to the coin amounts computed inside
      `sectionCoins` (only the `legacyVisible` hoist) and no change to `handleSubmit` /
      `creditAwardAndFinish`.
    - `npx tsc --noEmit` exits 0; `npm run build` succeeds; `npm test` passes.
  </acceptance_criteria>
  <done>The screen shows a persistent ≤70px ring + live total whose percentage counts only today's applicable categories.</done>
</task>

<task type="auto">
  <name>Task 3: Per-fill live coin feedback wired to the sectionCoins deltas</name>
  <files>components/kid/KidDayFillForm.tsx</files>
  <read_first>
    - components/kid/KidDayFillForm.tsx (lines 116-118 — `const { flyups, trigger: triggerCoinFlyup } = useCoinAnimation()`;
      lines 359-432 — `sectionCoins`; line 816 and line 631 — the two existing save/award flyup calls
      that must stay exactly as they are; lines 1229-1232 — the root `div` and `<CoinFlyup />`)
    - components/kid/CoinAnimation.tsx (the `trigger(amount, anchor?)` + `FlyupAnchor` contract from plan 02)
    - lib/kid/day-fill-progress.ts (`diffSectionCoins` from plan 03)
    - .planning/phases/09.3-dayform-default/09.3-CONTEXT.md (D-10, D-11, D-12)
    - .planning/phases/09.3-dayform-default/09.3-UI-SPEC.md ("Live coin feedback (D-11, D-12)")
  </read_first>
  <action>
    Wire immediate per-fill feedback WITHOUT touching a single coin amount (D-12: `sectionCoins` is
    the only delta source; no new coin maths):

    1. Add three refs near the existing `useCoinAnimation()` call:
       `const lastAnchorRef = useRef<FlyupAnchor | null>(null)`,
       `const interactedRef = useRef(false)`,
       `const prevSectionCoinsRef = useRef<Record<string, number>>({})`.

    2. Add one `onPointerDownCapture` handler on the form's root `div` (the element that already
       carries `paddingBottom: 120, position: 'relative'` and now `data-fill-style`). It sets
       `interactedRef.current = true` and records the anchor: look up
       `(e.target as HTMLElement).closest('[data-fill-row]')` — when found, use its
       `getBoundingClientRect()` as `{ left: rect.left + rect.width / 2, top: rect.top }`; otherwise
       fall back to `{ left: e.clientX, top: e.clientY }`. One capture-phase handler covers every
       interaction inside the form (row toggles, grade buttons, coach stars, reading fields), so no
       per-handler plumbing is needed and plan 05 only has to tag its rows with `data-fill-row`.
       Comment it as such.

    3. Add one `useEffect` keyed on `[sectionCoins, triggerCoinFlyup]` that reads
       `prevSectionCoinsRef.current`, immediately reassigns it to the current `sectionCoins`, returns
       early when `interactedRef.current` is false, and otherwise calls
       `triggerCoinFlyup(delta, lastAnchorRef.current)` for every entry of
       `diffSectionCoins(prev, sectionCoins)`. The `interactedRef` gate is load-bearing: opening an
       already-filled day (or the async mount pre-fill of room checks / grades / reading) must NOT
       spray flyups before the child has touched anything. `trigger`'s own `amount === 0` guard
       handles no-op deltas; `CoinFlyup`'s `loss` class handles the negative styling (D-11) and
       `AnimatedNum` in the sticky bar already interpolates downward for a decreasing total.

    4. Leave both existing `triggerCoinFlyup(...)` call sites untouched: the client-estimate flyup in
       `handleSubmit` (line ~816) and the server-confirmed one in `creditAwardAndFinish` (line ~631)
       keep firing unanchored at the bottom of the screen. Do NOT fire coin feedback for behavior or
       mood: `behavior_marks` carry zero coins until a parent approves them (Phase 5.9) and mood has
       no coin value — neither has a `sectionCoins` key, so the diff naturally emits nothing. Record
       that resolution in a comment (it is the open question UI-SPEC flagged for the planner).
       Add NO new fetch, NO new write, and NO change to the `/api/wallet/award` call.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -c "diffSectionCoins" components/kid/KidDayFillForm.tsx</automated>
    <automated>grep -c "interactedRef" components/kid/KidDayFillForm.tsx</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "diffSectionCoins" components/kid/KidDayFillForm.tsx` returns 2 (import + call).
    - `grep -c "onPointerDownCapture" components/kid/KidDayFillForm.tsx` returns 1 and
      `grep -c "data-fill-row" components/kid/KidDayFillForm.tsx` returns 1 (the `closest()` lookup).
    - `grep -c "interactedRef.current" components/kid/KidDayFillForm.tsx` is >= 3
      (init, set on interaction, read as the mount gate).
    - `grep -c "triggerCoinFlyup(coinsPreview)" components/kid/KidDayFillForm.tsx` returns 1 and
      `grep -c "triggerCoinFlyup(creditedCoins)" components/kid/KidDayFillForm.tsx` returns 1 — both
      legacy call sites intact.
    - `git diff components/kid/KidDayFillForm.tsx` shows zero changes inside `handleSubmit`,
      `creditAwardAndFinish`, or any `fetch(` call; `grep -c "api/wallet/award" components/kid/KidDayFillForm.tsx`
      is unchanged at 1.
    - `npx tsc --noEmit` exits 0; `npm run build` succeeds; `npm test` passes.
  </acceptance_criteria>
  <done>Every coin-bearing fill (and un-fill) fires an anchored flyup with the exact `sectionCoins` delta, and a freshly opened filled day fires none.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kid browser (form state) → `/api/wallet/award` | The only money boundary this screen crosses; unchanged by this plan |
| loaded `Child` row → form props | `fill_style` crosses from the RLS-scoped `api.getChild` read into a render decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-093-12 | Tampering | live coin preview (`sectionCoins` → flyups → sticky total) | mitigate | Preview only. No wallet write is added: `git diff` must show zero changes in `handleSubmit`/`creditAwardAndFinish`, and `/api/wallet/award` still recomputes every amount server-side from the saved rows (CLAUDE.md money rule). A tampered client delta changes pixels, never `wallet`. |
| T-093-13 | Spoofing | `fillStyle` prop | mitigate | Read from the child's own `api.getChild` row (family-isolated by `rls.sql`); it only selects a render layer and grants no capability. A forged value cannot widen access because nothing authorizes off it. |
| T-093-14 | Elevation of Privilege | past-day lock | mitigate | `isLocked` (date < `localDateString()`) still gates every handler and is surfaced through `lockedLabel` on the sticky bar; the award route's own back-fill gating (`day_fill_requests`) remains the authoritative backstop. |
| T-093-15 | Elevation of Privilege | parent-filled custom blocks | mitigate | `applicable['custom:<id>']` is false for `who_fills === 'parent'`, so such blocks stay out of the kid's ring, complementing the existing `toggleCustomBlock` T-056-12 UI guard and the award-route backstop. |
| T-093-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0; `npm run build` succeeds; `npm test` passes.
2. `grep -c "kidFillForm.coinsToday" components/kid/KidDayFillForm.tsx` returns 0 (strip replaced, not duplicated).
3. `git diff components/kid/KidDayFillForm.tsx` shows no modification to `handleSubmit`, `creditAwardAndFinish`, or any coin amount inside `sectionCoins`.
4. `grep -c "CollapsibleRow" components/kid/KidDayFillForm.tsx` is still >= 1 — the accordion rows are intentionally still present after this plan; plan 05 removes them.
</verification>

<success_criteria>
- `fillStyle` flows child row → day page → form, defaulting to `sticky-summary`.
- A ≤70px sticky bar with ring + live total is always on screen while filling.
- Ring percentage reflects only applicable categories (D-09).
- Filling/un-filling a coin-bearing category fires an anchored positive/negative flyup and the total ticks.
- Nothing about the save/award pipeline changed.
</success_criteria>

<output>
Create `.planning/phases/09.3-dayform-default/09.3-04-SUMMARY.md` when done.
</output>
