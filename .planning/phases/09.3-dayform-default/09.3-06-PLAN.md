---
phase: 09.3-dayform-default
plan: 06
type: execute
wave: 4
depends_on: [09.3-05]
files_modified:
  - project-docs/data-model.md
  - project-docs/system-architecture.md
autonomous: false
requirements: [DAYFORM-01, DAYFORM-04, DAYFORM-05, DAYFORM-06]

must_haves:
  truths:
    - "Every gate (typecheck, lint, tests, build) is green on the finished phase"
    - "The fill_style column is still present in the live database at phase close (no drift since plan 01)"
    - "Every phase decision D-01..D-13 and every requirement DAYFORM-01/04/05/06 has traceable evidence in code"
    - "project-docs records the new per-child fill_style column and the render-layer architecture"
    - "A human has confirmed the sticky-summary experience in a real browser, including the loss feedback and reduced-motion behaviour"
  artifacts:
    - path: "project-docs/data-model.md"
      provides: "children.fill_style documented"
      contains: "fill_style"
    - path: "project-docs/system-architecture.md"
      provides: "per-child day-fill style render-layer + self-serve write path documented"
      contains: "fill_style"
  key_links:
    - from: ".planning/phases/09.3-dayform-default/09.3-06-SUMMARY.md"
      to: "code evidence per decision"
      via: "D-01..D-13 traceability matrix"
      pattern: "D-13"
---

<objective>
Close phase 9.3: prove every gate is green, prove the live schema did not drift, write the
decision/requirement traceability matrix, update `project-docs`, and get the operator's browser
sign-off on the interactions that cannot be asserted from a terminal.

Purpose: the phase's success criteria are mostly interaction-shaped (floating feedback, pop
animation, in-place panels) — automated gates prove the wiring, a human proves the feel.
Output: green gates, updated docs, an operator-approved phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.3-dayform-default/09.3-CONTEXT.md
@.planning/phases/09.3-dayform-default/09.3-UI-SPEC.md
@.planning/phases/09.3-dayform-default/09.3-01-SUMMARY.md
@.planning/phases/09.3-dayform-default/09.3-02-SUMMARY.md
@.planning/phases/09.3-dayform-default/09.3-03-SUMMARY.md
@.planning/phases/09.3-dayform-default/09.3-04-SUMMARY.md
@.planning/phases/09.3-dayform-default/09.3-05-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate sweep, live-schema re-check, and the D-01..D-13 traceability matrix</name>
  <files>(no source files — verification only; findings go in the plan SUMMARY)</files>
  <read_first>
    - .planning/phases/09.3-dayform-default/09.3-CONTEXT.md (the full D-01..D-13 decision list —
      each one needs a row in the matrix)
    - .planning/REQUIREMENTS.md (lines 29-45 — DAYFORM-01 / 04 / 05 / 06 wording)
    - .planning/phases/09.3-dayform-default/09.3-0{1,2,3,4,5}-SUMMARY.md (what each plan reported as built)
    - scripts/verify-fill-style-column.mjs (the live-DB assertion from plan 01)
  </read_first>
  <action>
    Run every gate and record the exact outputs in the SUMMARY: `npx tsc --noEmit`, `npm run lint`,
    `npm test`, `npm run build`, plus
    `node --env-file=.env.local scripts/verify-fill-style-column.mjs` and
    `node --env-file=.env.local scripts/verify-migrations-applied.mjs` (re-checked here, not trusted
    from plan 01, because "ROADMAP Complete ≠ migration ran" is a recorded failure mode in STATE.md).

    Then write a **traceability matrix** into the SUMMARY with one row per decision D-01 … D-13 and
    one row per requirement DAYFORM-01 / 04 / 05 / 06. Each row must name the file (and symbol) that
    implements it plus the grep or command that proves it. Use these as the evidence anchors:
    D-01 → the `style !== 'sticky-summary'` guard in `app/kid/actions/fill-style.ts` and
    `grep -c "tile-sheet\|story-stepper" components/kid/ProfileSheet.tsx` = 0;
    D-02/DAYFORM-06 → the column default in `supabase/migrations/2026-09-19-child-fill-style.sql`
    plus `fillStyle={child?.fill_style ?? 'sticky-summary'}` in `app/kid/(app)/day/page.tsx`;
    D-03/DAYFORM-01 → the pill in `components/kid/ProfileSheet.tsx`;
    D-04 → the `requireFamilyMember` + `authorizeChildAction` + `createAdminClient` chain in
    `app/kid/actions/fill-style.ts`;
    D-05 → `QuickRow` usage in `RoomBlock.tsx` / `ActivitiesBlock.tsx` / `CustomBlock.tsx`;
    D-06 → `renderPanelRow('behavior', …)` plus `grep -c "good_behavior" components/kid/KidDayFillForm.tsx` = 0;
    D-07 → `renderPanelRow` + `openPanels` in `KidDayFillForm.tsx` and the no-auto-close comment;
    D-08 → the mood `renderGroup` + `kid-mood-pop` + the `prefers-reduced-motion` guard;
    D-09 → the `applicable` memo + `computeFillProgress` + `tests/day-fill-progress.test.ts`;
    D-10 → `QuickRow`'s `role="switch"` toggle with no confirm step + the negative branch of
    `diffSectionCoins`;
    D-11 → `.coin-flyup-number.loss` + `@keyframes coin-flyup-loss` in `app/globals.css` and the
    `loss` class in `CoinAnimation.tsx`;
    D-12 → the `sectionCoins` diff effect in `KidDayFillForm.tsx` (`diffSectionCoins` +
    `triggerCoinFlyup(delta, …)`) with no new coin maths;
    D-13 → `git diff` proof that `sectionCoins`, `handleSubmit`, `creditAwardAndFinish` and the
    `/api/wallet/award` fetch are unchanged;
    DAYFORM-04 → `StickySummaryBar` mount + `QuickRow`/`InlinePanelRow` rows;
    DAYFORM-05 → the per-fill flyup wiring + the loss variant.

    If any row cannot be evidenced, STOP and report it as a gap rather than writing a green matrix.
    Note in the SUMMARY that `tile-sheet` / `story-stepper` (DAYFORM-02/03) are deliberately out of
    scope and belong to Phase 9.4, and that `.planning/REQUIREMENTS.md` DAYFORM-01's literal "all 3
    styles" wording is satisfied incrementally (D-01) — fully met only after 9.4.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <automated>node --env-file=.env.local scripts/verify-fill-style-column.mjs</automated>
    <automated>node --env-file=.env.local scripts/verify-migrations-applied.mjs</automated>
  </verify>
  <acceptance_criteria>
    - All six commands above exit 0, with their output quoted in the SUMMARY.
    - The SUMMARY contains a matrix with a row for each of D-01..D-13 and DAYFORM-01/04/05/06, each
      naming a file plus a proving command; no row reads "n/a" or "assumed".
    - `grep -c "CollapsibleRow" components/kid/KidDayFillForm.tsx` returns 0 (accordion retired).
    - `grep -c "coin-flyup-loss" app/globals.css` is >= 2 and `grep -c "loss" components/kid/CoinAnimation.tsx` is >= 1.
    - `npx vitest run tests/day-fill-progress.test.ts` passes.
  </acceptance_criteria>
  <done>All gates green, live schema re-verified, and every decision/requirement traced to code evidence.</done>
</task>

<task type="auto">
  <name>Task 2: project-docs update</name>
  <files>project-docs/data-model.md, project-docs/system-architecture.md</files>
  <read_first>
    - project-docs/data-model.md (the `children` / legacy-tables section around line 215 and the RLS
      section around line 238, to match the existing documentation style)
    - project-docs/system-architecture.md (find the kid-UI / Day-screen section to extend)
    - supabase/migrations/2026-09-19-child-fill-style.sql (the shipped DDL)
    - app/kid/actions/fill-style.ts (the shipped write path)
  </read_first>
  <action>
    In `project-docs/data-model.md`, document the new column where `children` is described:
    `children.fill_style TEXT NOT NULL DEFAULT 'sticky-summary'`, CHECK-constrained to
    `('tile-sheet','story-stepper','sticky-summary')` (constraint `children_fill_style_check`),
    migration `2026-09-19-child-fill-style.sql`. State that it is a per-child (not per-family) UI
    preference, that only `sticky-summary` is implemented as of Phase 9.3 with `tile-sheet` /
    `story-stepper` arriving in Phase 9.4, and that it is written exclusively by the service-role
    server action `app/kid/actions/fill-style.ts` — `children` has no client UPDATE policy for it.

    In `project-docs/system-architecture.md`, add a short subsection to the kid-UI section covering:
    the Day screen selects its render layer from `fill_style` (`app/kid/(app)/day/page.tsx` →
    `KidDayFillForm`'s `fillStyle` prop, defaulting to `sticky-summary`); the sticky-summary layer is
    a render-only change over `KidDayFillForm`'s existing state/save/award logic (D-13); the shared
    pieces live in `components/kid/day-fill/` (`StickySummaryBar`, `QuickRow`, `InlinePanelRow`) with
    the pure completion/delta maths in `lib/kid/day-fill-progress.ts`; live coin feedback reuses the
    single `components/kid/CoinAnimation.tsx` mechanism (gain, loss, anchored variants) and is
    preview-only — `/api/wallet/award` remains the sole crediting path.

    Keep both edits additive and in the surrounding prose style; do not restructure either document.
  </action>
  <verify>
    <automated>grep -c "fill_style" project-docs/data-model.md project-docs/system-architecture.md</automated>
    <automated>grep -c "children_fill_style_check" project-docs/data-model.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fill_style" project-docs/data-model.md` is >= 1 and
      `grep -c "fill_style" project-docs/system-architecture.md` is >= 1.
    - `grep -c "children_fill_style_check" project-docs/data-model.md` returns 1.
    - `grep -c "day-fill-progress\|day-fill/" project-docs/system-architecture.md` is >= 1.
    - `grep -c "api/wallet/award" project-docs/system-architecture.md` is >= 1 in the new subsection
      (the preview-only / crediting-path distinction is stated).
    - `git diff --stat project-docs/` shows additions only, no deleted lines.
  </acceptance_criteria>
  <done>The new column and the fill-style render architecture are documented in project-docs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator browser verification of the sticky-summary day fill</name>
  <files>(no files — operator verification of the running app)</files>
  <read_first>
    - .planning/phases/09.3-dayform-default/09.3-UI-SPEC.md ("Interaction Contract")
    - .planning/phases/09.3-dayform-default/09.3-CONTEXT.md (D-08, D-10, D-11)
  </read_first>
  <action>
    Start the dev server (`npm run dev`), then PAUSE and hand the 14 numbered steps in
    `<how-to-verify>` to the operator verbatim. Do not self-approve, do not simulate the browser, and
    do not mark the phase complete before the resume signal arrives. If the operator reports a
    deviation, record it in the SUMMARY as a gap citing the step number and stop — do not attempt a
    fix inside this checkpoint task.
  </action>
  <what-built>
    The full sticky-summary day-fill style: a ≤70px sticky bar with a completion ring + live coin
    total; one-tap binary rows for each room task, each extra activity, and each custom day-block;
    in-place panels for behavior, grades, exercises, sport and reading; an always-expanded mood row
    with a pop animation; per-fill floating coin feedback with a distinct negative (loss) variant;
    and a self-serve fill-style pill in the kid's ProfileSheet backed by a service-role server action.
    Everything automatable was already asserted in task 1 (typecheck, lint, tests, build, live-DB
    column + constraint, decision traceability). What remains genuinely needs eyes and a finger.
  </what-built>
  <how-to-verify>
    1. `npm run dev`, open http://localhost:3000, sign in as a child (family code + PIN at
       `/kid/login`), and go to `/kid/day` for TODAY.
    2. Sticky bar: the ring + coin total are visible immediately and STAY visible while you scroll the
       whole form. The bar looks about one ring tall (~70px) — not a two-row banner. There is exactly
       ONE coin total on screen (the old mango "МОНЕТ ЗА СЕГОДНЯ" strip should be gone).
    3. Room: each room task is its own row with a switch on the right. Tap anywhere on a row (not just
       the switch) — it flips to mint instantly. Keep tapping until the room threshold is met; a green
       "+N 💰" floats up near the row you tapped and the total ticks up.
    4. Re-tap room rows until you fall back below the threshold: a RED "-N 💰" drifts DOWNWARD and the
       total ticks DOWN. Confirm the loss reads as visually different from a gain, not just recoloured.
    5. Activities: each extra activity is its own row (not one "Доп. занятия" panel). Tapping one
       fires its own coin flyup. Same for any custom day-block; a parent-filled block stays dimmed and
       untappable.
    6. Grades: tap the grades row — the panel opens IN PLACE directly under that row (no bottom sheet,
       no page jump). Pick a good grade → positive flyup. Pick a bad grade (one with a negative coin
       value in your family's rules) → red downward flyup and the total decreases.
    7. Sport: mark a section attended, then tap a 1-star coach rating → red downward flyup.
    8. Behavior: tap the behavior row — the tag multi-select opens in place. Select a tag: the row
       shows a done/pending state and NO coin flyup fires (behavior coins are parent-approved later —
       this is correct, not a bug).
    9. Open a second panel (e.g. reading) while grades is still open: the grades panel must NOT snap
       shut and lose what you typed.
    10. Mood: the 5 emoji buttons are visible with no tap-to-open step, and selecting one plays a
        noticeably bigger pop than the other rows' feedback. Re-selecting a different emoji replays it.
    11. Save the day with the bottom button: the existing save → award → confetti/stamp flow works and
        the wallet balance matches what the server credited (not the preview).
    12. Tap your avatar in the header → ProfileSheet: a pill reads "Стиль заполнения: Сводка". Tap it
        (no error), close and reopen the sheet — it still reads the same. There is NO picker or
        "coming soon" entry for the other two styles.
    13. Reduced motion: enable macOS System Settings → Accessibility → Display → Reduce motion,
        reload `/kid/day`, then repeat steps 3-4 and 10: the numbers and selected states still change,
        but nothing animates.
    14. Past day: use the week strip to open an already-filled past day. No coin flyups fire on open,
        and the rows are locked (the sticky bar shows the "🔒 Закрыт" pill).
  </how-to-verify>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <human-check>Steps 1-14 above all behave as described.</human-check>
  </verify>
  <acceptance_criteria>
    - The operator confirms each of the 14 steps, or reports specific deviations naming the step number.
    - Any reported deviation is captured in the SUMMARY as a gap with the step number, not silently closed.
    - Step 4 (loss feedback), step 8 (no behavior flyup), step 10 (mood pop), step 13 (reduced motion)
      and step 14 (no flyups on mount) are explicitly confirmed — these are the decisions no terminal
      command can assert.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the issues by step number.</resume-signal>
  <done>Operator has confirmed the sticky-summary interaction end to end in a browser.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → prod Postgres | Read-only verification queries via `SUPABASE_DB_URL` |
| operator browser → the running app | Manual exercise of the kid session built in plans 01-05 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-093-21 | Repudiation | phase-close verification | mitigate | Gate outputs and the D-01..D-13 traceability matrix are recorded verbatim in the SUMMARY, so a later "was it actually verified?" question has evidence — the STATE.md-recorded failure mode of trusting a "Complete" marker is what task 1's live-DB re-check exists to prevent. |
| T-093-22 | Information Disclosure | project-docs update | mitigate | Documentation records column names and the write path only — no secrets, no connection strings, no keys are added to `project-docs/`. |
| T-093-23 | Tampering | verification scripts against prod | accept | `verify-fill-style-column.mjs` and `verify-migrations-applied.mjs` issue read-only catalog queries; neither writes. |
| T-093-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
1. `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build` all exit 0.
2. `node --env-file=.env.local scripts/verify-fill-style-column.mjs` exits 0.
3. `node --env-file=.env.local scripts/verify-migrations-applied.mjs` exits 0.
4. The SUMMARY holds a complete D-01..D-13 + DAYFORM-01/04/05/06 traceability matrix.
5. Operator sign-off recorded (or gaps listed by step number).
</verification>

<success_criteria>
- Every gate green and the live schema re-confirmed at phase close.
- Every phase decision and requirement traced to concrete code evidence.
- `project-docs/data-model.md` and `project-docs/system-architecture.md` document the new column and
  the render-layer architecture.
- A human has confirmed the loss feedback, mood pop, in-place panels, reduced-motion behaviour, and
  the no-flyups-on-mount rule in a real browser.
</success_criteria>

<output>
Create `.planning/phases/09.3-dayform-default/09.3-06-SUMMARY.md` when done.
</output>
