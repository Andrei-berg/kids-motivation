---
phase: 09.3-dayform-default
plan: 02
subsystem: ui
tags: [react, css, animation, reduced-motion, kid-ui]

# Dependency graph
requires:
  - phase: 09.3-dayform-default (plan 01, wave 1 sibling)
    provides: design tokens / row primitives this plan's CSS sits alongside
provides:
  - "trigger(amount, anchor?) on useCoinAnimation — row-anchored coin flyups"
  - "CoinFlyup gain/loss/anchored/reduced-motion-static rendering variants"
  - ".coin-flyup-loss / .coin-flyup-anchored / .coin-flyup-number.static / .kid-mood-pop CSS in app/globals.css"
affects: [09.3-04 (per-row live feedback consumer), 09.3-05 (mood row consumer of .kid-mood-pop)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Reduced-motion double-guard: keyframes only inside @media (prefers-reduced-motion: no-preference), plus a post-mount window.matchMedia('(prefers-reduced-motion: reduce)') JS check applying a .static class — mirrors components/design/atoms.tsx Tick"

key-files:
  created: []
  modified:
    - app/globals.css
    - components/kid/CoinAnimation.tsx

key-decisions:
  - "Reworded an inline code comment to avoid an accidental second grep match on 'prefers-reduced-motion: reduce' (acceptance criterion required exactly 1 occurrence, the literal media-query string)"

patterns-established:
  - "Anchored vs unanchored flyup groups render as two separate wrapper divs (.coin-flyup-container legacy path untouched, .coin-flyup-anchored new path) so the existing save-time/award-confirmation call sites need zero changes"

requirements-completed: [DAYFORM-05]

# Metrics
duration: 5min
completed: 2026-09-19
---

# Phase 09.3 Plan 02: Coin-Flyup Loss/Anchor/Reduced-Motion Extension Summary

**Extended the single existing `useCoinAnimation`/`CoinFlyup` mechanism (not a parallel system) to support row-anchored positioning, a `K.danger`-colored downward-drift loss variant, and a reduced-motion-static fallback, plus a shared `kidMoodPop` keyframe for the mood row.**

## Performance

- **Duration:** ~5 min (build/test verification included)
- **Started:** 2026-09-19T14:46:00+03:00 (approx, from first task commit)
- **Completed:** 2026-09-19T14:49:00+03:00
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments
- `app/globals.css` gained `.coin-flyup-anchored`, `.coin-flyup-number.loss` + `@keyframes coin-flyup-loss`, `.kid-mood-pop` + `@keyframes kidMoodPop`, and `.coin-flyup-number.static` — all additive, zero changes to the pre-existing `.coin-flyup-container`/`.coin-flyup-number`/`@keyframes coin-flyup` block
- `components/kid/CoinAnimation.tsx` `trigger` now accepts an optional `anchor?: FlyupAnchor`; `CoinFlyup` renders unanchored (legacy, byte-identical markup) and anchored groups separately, both applying `.loss` for negative amounts and `.static` under `prefers-reduced-motion: reduce`
- The two existing single-argument `triggerCoinFlyup(...)` call sites in `components/kid/KidDayFillForm.tsx` (lines 631, 816) required no edits and remain untouched — confirmed via `git diff` showing zero changes to that file

## Task Commits

Each task was committed atomically:

1. **Task 1: Loss / anchored / reduced-motion CSS + mood-pop keyframe** - `2e28b0a` (feat)
2. **Task 2: trigger(amount, anchor?) + gain/loss/anchored/static rendering in CoinFlyup** - `d8b1879` (feat)

**Plan metadata:** (this commit, made after SUMMARY.md write)

## Files Created/Modified
- `app/globals.css` - Added loss/anchored/static coin-flyup CSS and the `kidMoodPop` keyframe, additions-only (46 new lines)
- `components/kid/CoinAnimation.tsx` - `FlyupAnchor` export, optional `anchor` param on `trigger`, split unanchored/anchored rendering with loss/static class logic and the `Tick`-style reduced-motion guard

## Decisions Made
- Reworded a code comment in `CoinAnimation.tsx` that accidentally duplicated the literal string `prefers-reduced-motion: reduce`, which would have made the plan's exact-count grep acceptance criterion (`returns 1`) fail. No functional change — comment wording only.

## Deviations from Plan

None - plan executed exactly as written. The one adjustment (comment rewording, above) was a pre-commit self-correction to satisfy the plan's own stated verification command, not a deviation from the plan's intent.

## Issues Encountered
- `npm run build` initially failed in this worktree with "Your project's URL and API key are required to create a Supabase client" — the worktree checkout has no `.env.local` (git-ignored, not copied by worktree creation). Copied the existing `.env.local` from the main repo checkout (`/Users/andrei/Projects/kids-motivation/.env.local`, same developer's own local secrets, never committed) into the worktree so the build's static page-data collection (which touches `/api/cron/daily`) could complete. This is a local dev-environment fix only, not a code change, and is not part of any commit.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- `trigger(amount, anchor)` and the loss/anchored/static rendering are ready for plan 04 (per-row live feedback) to consume via `FlyupAnchor`.
- `.kid-mood-pop` / `kidMoodPop` keyframe is ready for plan 05 (mood row) to apply directly as a className.
- `npx tsc --noEmit`, `npm run build`, and `npm test` (154 passed, 72 skipped, 0 failed) all green at HEAD.

---
*Phase: 09.3-dayform-default*
*Completed: 2026-09-19*
