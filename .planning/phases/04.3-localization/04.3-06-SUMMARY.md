---
phase: 04.3-localization
plan: "06"
subsystem: ui
tags: [i18n, localization, react, nextjs, typescript]

# Dependency graph
requires:
  - phase: 04.3-01
    provides: i18n infrastructure (useT, useLanguage, locale files)
  - phase: 04.3-04
    provides: modal and wallet components localized
provides:
  - getDayType() accepts optional t() parameter for localized day-type labels
  - BADGES array with titleKey/descKey translation key fields
  - auditLogExtra.today/yesterday keys in both locale files
  - badges.* and dayType.* namespaces in both locale files
  - AuditLogViewer formatDate() uses language-aware locale strings
  - achievements page renders t(badge.titleKey) and t(badge.descKey)
  - parent shop STARTER_TEMPLATES uses English canonical strings
  - browser tab title is FamilyCoins — Kids Motivation
affects:
  - any page that renders DailyModal (dayTypeInfo label now localized)
  - kid/achievements page (badge names/descriptions now localized)
  - audit log / wallet pages (today/yesterday now localized)
  - parent shop (starter templates now English-canonical)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Optional t parameter pattern: function(date, isSick, ..., t?) — t?() ternary with Russian fallback for backward compat"
    - "titleKey/descKey pattern: service objects carry translation keys; components call t(badge.titleKey) at render time"
    - "{{time}} interpolation in auditLogExtra.today/yesterday — time formatted separately then injected"

key-files:
  created: []
  modified:
    - lib/day-type.ts
    - lib/services/badges.service.ts
    - components/DailyModal.tsx
    - components/AuditLogViewer.tsx
    - app/kid/achievements/page.tsx
    - app/parent/shop/page.tsx
    - app/layout.tsx
    - public/locales/en.json
    - public/locales/ru.json

key-decisions:
  - "getDayType optional t param: t?() ternary with Russian string fallback preserves backward compat for server-side callers"
  - "BADGES keep original title/description fields for backward compat (awardBadge still stores them in DB); new titleKey/descKey used for UI display only"
  - "STARTER_TEMPLATES use English canonical strings — since templates are written to DB as strings, English is the canonical stored value; parents can edit after loading"
  - "AuditLogViewer uses language from useLanguage() for toLocaleTimeString locale — removes hardcoded ru"

patterns-established:
  - "Optional t param on utility functions: pass t from useT() at call site when localization needed; function falls back to Russian default"
  - "Service titleKey/descKey: domain objects carry translation keys, UI calls t(key) at render time"

requirements-completed:
  - LOC-01

# Metrics
duration: 20min
completed: 2026-05-17
---

# Phase 04.3 Plan 06: Gap Closure — Remaining Russian Strings Summary

**Closed 5 localization gaps: day-type labels, badge names/descriptions, audit log today/yesterday, parent shop starter templates, and layout metadata now fully EN/RU switchable**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-05-17T03:00:00Z
- **Completed:** 2026-05-17T03:16:25Z
- **Tasks:** 2
- **Files modified:** 9

## Accomplishments
- `getDayType()` now accepts optional `t` parameter — sick/weekend/school labels render in active language when called from DailyModal
- All 10 BADGES entries have `titleKey` and `descKey` fields; achievements page renders `t(badge.titleKey)` and `t(badge.descKey)` instead of Russian hardcoded strings
- `AuditLogViewer.formatDate()` uses `t('auditLogExtra.today')` and `t('auditLogExtra.yesterday')` with `{{time}}` interpolation; locale for `toLocaleTimeString` now uses `language` variable instead of hardcoded `'ru'`
- Parent shop `STARTER_TEMPLATES` converted from Russian to English canonical strings
- `app/layout.tsx` metadata title changed from Russian to `'FamilyCoins — Kids Motivation'`
- Both locale files extended with `badges.*`, `dayType.*`, `auditLogExtra.today/yesterday`, and `parentShop.starterLoadBtn/starterLoaded` namespaces/keys

## Task Commits

1. **Task 1: Extend locale files with missing keys** - `2f52fef` (feat)
2. **Task 2: Fix Russian hardcoded strings in lib and component files** - `5c63890` (feat)

## Files Created/Modified
- `public/locales/en.json` - Added auditLogExtra.today/yesterday, badges.* namespace (10 pairs), dayType.* namespace, parentShop.starterLoadBtn/starterLoaded
- `public/locales/ru.json` - Same keys added in Russian
- `lib/day-type.ts` - getDayType() accepts optional t param; 3 labels use t('dayType.*') with Russian fallback
- `lib/services/badges.service.ts` - Badge interface extended with titleKey/descKey; all 10 BADGES entries updated
- `components/DailyModal.tsx` - getDayType call now passes t as 5th argument
- `components/AuditLogViewer.tsx` - Added useLanguage import; formatDate() uses t() for today/yesterday; locale uses language variable
- `app/kid/achievements/page.tsx` - badge.title -> t(badge.titleKey); badge.description -> t(badge.descKey) in grid and detail sheet
- `app/parent/shop/page.tsx` - STARTER_TEMPLATES replaced with English title/description strings
- `app/layout.tsx` - Metadata title/description changed to English

## Decisions Made
- Used ternary fallback pattern for `getDayType` optional `t` param to preserve backward compatibility for server-side callers that cannot use React hooks
- Kept original `title` and `description` fields in Badge interface for backward compat (awardBadge stores them in DB); added `titleKey`/`descKey` alongside for UI use
- STARTER_TEMPLATES stored as English canonical — since templates are written to DB as static strings, English is the authoritative stored value

## Deviations from Plan
None — plan executed exactly as written.

## Issues Encountered
None — TypeScript compiled with 0 errors on first attempt.

## Next Phase Readiness
- Phase 04.3 LOC-01 requirement is now fully satisfied — all visible strings switch with the language toggle
- All 5 gaps from the verification report are closed (day-type labels, badge names, audit log today/yesterday, shop templates, layout title)
- No blockers for phase 04.4 (security-compliance)

---
*Phase: 04.3-localization*
*Completed: 2026-05-17*
