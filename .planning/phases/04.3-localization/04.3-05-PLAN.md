---
phase: 04.3-localization
plan: "05"
type: execute
wave: 2
depends_on:
  - "04.3-01"
files_modified:
  - app/wallboard/page.tsx
  - app/parent/shop/page.tsx
  - app/onboarding/v2/page.tsx
  - app/onboarding/join/page.tsx
  - app/page.tsx
  - components/PotentialWidget.tsx
  - components/ExpenseModal.tsx
  - components/ExpenseCharts.tsx
  - components/ScheduleEditor.tsx
  - components/settings/CategoryManager.tsx
  - components/settings/TaskManager.tsx
  - components/settings/ScheduleEditor.tsx
  - components/settings/PeriodsManager.tsx
  - components/settings/SectionsManager.tsx
  - components/settings/SubjectsManager.tsx
  - components/settings/ActivitiesManager.tsx
  - components/settings/StreakSettings.tsx
  - components/settings/CoinRulesEditor.tsx
  - components/settings/NotificationSettings.tsx
  - components/settings/FamilyManager.tsx
  - app/actions/push-streaks.ts
  - app/actions/push.ts
  - app/actions/send-medal.ts
  - app/api/cron/reminders/route.ts
  - app/api/cron/missed-tasks/route.ts
  - app/kid/shop/actions.ts
autonomous: true
requirements:
  - LOC-01

must_haves:
  truths:
    - "Wallboard page displays all labels in the active language"
    - "Parent shop page displays all labels in the active language"
    - "All 10 settings manager components display labels in the active language"
    - "Onboarding pages display all labels in the active language"
    - "Backend push notification strings are handled appropriately"
    - "No hardcoded Russian or English strings remain anywhere in the codebase UI layer"
  artifacts:
    - path: "app/wallboard/page.tsx"
      provides: "Localized wallboard page"
      contains: "useT"
    - path: "components/settings/CategoryManager.tsx"
      provides: "Localized category manager"
      contains: "useT"
    - path: "components/settings/TaskManager.tsx"
      provides: "Localized task manager"
      contains: "useT"
    - path: "app/onboarding/v2/page.tsx"
      provides: "Localized onboarding wizard"
      contains: "useT"
  key_links:
    - from: "app/wallboard/page.tsx"
      to: "lib/i18n.tsx"
      via: "import { useT } from '@/lib/i18n'"
      pattern: "useT"
    - from: "app/actions/push-streaks.ts"
      to: "public/locales/en.json"
      via: "uses push.streaks.* translation values directly as string constants"
      pattern: "push.streaks"
---

<objective>
Apply i18n to settings components, wallboard, onboarding, remaining pages, and backend notification strings.

Purpose: This is the final wave of string replacements. Settings components contain most of the remaining Russian strings (across 10 files). Wallboard and onboarding are major user flows. Push notification server actions need to output strings in the family's preferred language.

Output: 0 hardcoded Russian strings remain anywhere in the app's UI layer. Backend notifications use language-aware string selection.
</objective>

<execution_context>
@/Users/andrei/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrei/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.3-localization/04.3-01-PLAN.md

@app/wallboard/page.tsx
@app/parent/shop/page.tsx
@components/settings/CategoryManager.tsx
@components/settings/TaskManager.tsx
@components/settings/ScheduleEditor.tsx
@components/settings/PeriodsManager.tsx
@components/settings/SectionsManager.tsx
@components/settings/SubjectsManager.tsx
@components/settings/ActivitiesManager.tsx
@components/settings/StreakSettings.tsx
@components/settings/CoinRulesEditor.tsx
@components/settings/NotificationSettings.tsx
@components/settings/FamilyManager.tsx
@app/actions/push-streaks.ts
@app/actions/push.ts
@app/api/cron/reminders/route.ts

<interfaces>
From lib/i18n.tsx (created in Plan 01):
```typescript
export function useT(): (key: string, vars?: Record<string, string | number>) => string
```

Translation key namespaces (from public/locales/en.json):
- wallboard.* — wallboard page strings
- parentShop.* — parent shop page strings
- settings.categoryManager.* — CategoryManager strings
- settings.taskManager.* — TaskManager strings
- settings.scheduleEditor.* — ScheduleEditor strings
- settings.periodsManager.* — PeriodsManager strings
- settings.sectionsManager.* — SectionsManager strings
- settings.subjectsManager.* — SubjectsManager strings
- settings.activitiesManager.* — ActivitiesManager strings
- settings.streakSettings.* — StreakSettings strings
- settings.coinRules.* — CoinRulesEditor strings
- settings.notificationSettings.* — NotificationSettings strings
- settings.familyManager.* — FamilyManager strings
- onboarding.* — onboarding pages strings
- expenseModal.* — ExpenseModal strings
- expenseCharts.* — ExpenseCharts strings
- potentialWidget.* — PotentialWidget strings
- push.streaks.* — push notification strings (server-side)
- push.reminders.* — push reminder strings (server-side)

Note on server-side files (push actions, cron routes):
- These are NOT React components — they cannot use useT() hook
- For server-side push strings: create a simple server-side helper `getServerTranslations(lang: 'ru' | 'en')` in lib/i18n.tsx or a separate lib/i18n-server.ts that imports the JSON directly and returns a t() function without React context
- For now: since push notifications go to the family's device and the family language preference is stored in the DB (not easily available in server actions), default to Russian push strings (existing behavior) and mark with TODO for future enhancement
- The cron routes and push actions are server-side — just convert their Russian string literals to English equivalents OR keep them as-is with a TODO comment. The success criteria does NOT require push notifications to be localized — only the UI layer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Localize wallboard, parent shop, onboarding, remaining client pages + components</name>
  <files>app/wallboard/page.tsx, app/parent/shop/page.tsx, app/onboarding/v2/page.tsx, app/onboarding/join/page.tsx, app/page.tsx, components/PotentialWidget.tsx, components/ExpenseModal.tsx, components/ExpenseCharts.tsx, components/ScheduleEditor.tsx</files>
  <action>
**CRITICAL:** Read each file fully before editing. Wallboard is 615 lines, onboarding/v2 is 722 lines, onboarding/join is 899 lines — read them completely. Only change string content.

For all client component files: add `import { useT } from '@/lib/i18n'` and `const t = useT()` inside each component function.

**app/wallboard/page.tsx** (53 Russian strings):

Key replacements:
- `'Загрузка...'` → `t('wallboard.loading')`
- `'⏱️ До воскресенья: {days} {dayWord}'` → `t('wallboard.untilSunday', { days: daysToSunday, dayWord: daysToSunday === 1 ? t('wallboard.day') : t('wallboard.days') })`
- `'👑 Лидерборд'` → `'👑 ' + t('nav.rating')`
- `'⭐ До {level} уровня'` → `t('wallboard.toLevel', { level: child.level + 1 })`
- `'Активная цель'` → `t('wallboard.activeGoal')`
- `'Прогресс недели'` → `t('wallboard.weekProgress')`
- Goal progress string `'{goalProgress}% • Осталось {formatMoney(goal.target - goal.current)}'` → `t('wallboard.goalProgress', { pct: goalProgress, remaining: formatMoney(goal.target - goal.current) })`
- All other Russian labels in the wallboard

**app/parent/shop/page.tsx** (41 Russian strings):

Key replacements:
- Title → `t('parentShop.title')`
- Add reward → `t('parentShop.addReward')`
- Loading → `t('parentShop.loading')`
- Form field labels → `t('parentShop.name')`, `t('parentShop.price')`, `t('parentShop.description')`
- Delete confirm → `t('parentShop.deleteConfirm')`
- Save/cancel → `t('parentShop.saveReward')`, `t('parentShop.cancel')`

**app/onboarding/v2/page.tsx** (48 Russian strings):

Read fully. This is the parent onboarding wizard (7+ steps). Key replacements:
- Step labels, button text, field labels → use `t('onboarding.*)` keys
- Navigation buttons: `t('onboarding.next')`, `t('onboarding.back')`, `t('onboarding.done')`, `t('onboarding.skip')`
- Field labels → `t('onboarding.familyName')`, `t('onboarding.childName')`, `t('onboarding.childAge')`
- Loading → `t('onboarding.loading')`
- Error states → `t('onboarding.error')`
- Any other Russian strings in the wizard steps

Note: This page may have many inline Russian strings for specific wizard content. For strings that don't fit existing keys, add new keys to both en.json and ru.json before referencing them. Key pattern: `onboarding.stepN.*` where N is the step number.

**app/onboarding/join/page.tsx** (27 Russian strings):

Read fully. This is the family join flow. Apply same pattern — `t('onboarding.*)` for generic strings, add specific keys as needed.

**app/page.tsx** (11 Russian strings):

This is the app root/redirect page. Read fully. Replace any visible Russian strings with `t()` calls. If this page only contains redirect logic, the strings may be in loading states or error messages.

**components/PotentialWidget.tsx** (50 Russian strings):

Read fully. Key replacements:
- Title → `t('potentialWidget.title')`
- Weekly/monthly labels → `t('potentialWidget.weekly')`, `t('potentialWidget.monthly')`
- Loading → `t('potentialWidget.loading')`
- All other Russian strings in this component

**components/ExpenseModal.tsx** (30 Russian strings):

Key replacements:
- Title → `t('expenseModal.title')`
- Field labels → `t('expenseModal.amount')`, `t('expenseModal.description')`, `t('expenseModal.section')`, `t('expenseModal.date')`
- Save/cancel → `t('expenseModal.save')`, `t('expenseModal.cancel')`
- Loading → `t('expenseModal.loading')`
- Error → `t('expenseModal.error')`

**components/ExpenseCharts.tsx** (24 Russian strings):

Key replacements:
- Title → `t('expenseCharts.title')`
- Chart labels → `t('expenseCharts.byCategory')`, `t('expenseCharts.byMonth')`, `t('expenseCharts.total')`
- Loading → `t('expenseCharts.loading')`

**components/ScheduleEditor.tsx** (25 Russian strings — root-level, NOT settings/):

This is the root-level `components/ScheduleEditor.tsx` (not `components/settings/ScheduleEditor.tsx`). Read fully.
- Apply `t('settings.scheduleEditor.*)` keys consistently
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All 9 client files have 0 hardcoded Russian strings in UI text. TypeScript 0 errors. New translation keys added to both en.json and ru.json where needed.</done>
</task>

<task type="auto">
  <name>Task 2: Localize all settings manager components + handle server-side push strings</name>
  <files>components/settings/CategoryManager.tsx, components/settings/TaskManager.tsx, components/settings/ScheduleEditor.tsx, components/settings/PeriodsManager.tsx, components/settings/SectionsManager.tsx, components/settings/SubjectsManager.tsx, components/settings/ActivitiesManager.tsx, components/settings/StreakSettings.tsx, components/settings/CoinRulesEditor.tsx, components/settings/NotificationSettings.tsx, components/settings/FamilyManager.tsx, app/actions/push-streaks.ts, app/actions/push.ts, app/actions/send-medal.ts, app/api/cron/reminders/route.ts, app/api/cron/missed-tasks/route.ts, app/kid/shop/actions.ts</files>
  <action>
**Settings components (10 files)** — all follow the same pattern:

For each settings component: read fully, add `import { useT } from '@/lib/i18n'` and `const t = useT()` inside the component, replace all Russian strings with `t()` calls using the appropriate `settings.*` namespace.

**components/settings/CategoryManager.tsx** (33 Russian strings):
- Title → `t('settings.categoryManager.title')`
- Add button → `t('settings.categoryManager.addCategory')`
- Field labels → `t('settings.categoryManager.name')`, `t('settings.categoryManager.emoji')`
- Save/cancel → `t('settings.categoryManager.save')`, `t('settings.categoryManager.cancel')`
- Delete confirm → `t('settings.categoryManager.deleteConfirm')`
- Active/inactive labels → `t('settings.categoryManager.active')`, `t('settings.categoryManager.inactive')`

**components/settings/TaskManager.tsx** (28 Russian strings):
- Use `settings.taskManager.*` keys
- All field labels, buttons, confirm dialogs, frequency labels

**components/settings/ScheduleEditor.tsx** (29 Russian strings):
- Use `settings.scheduleEditor.*` keys
- Item type labels: `t('settings.scheduleEditor.lesson')`, `t('settings.scheduleEditor.section')`, `t('settings.scheduleEditor.routine')`

**components/settings/PeriodsManager.tsx** (38 Russian strings):
- Use `settings.periodsManager.*` keys
- Period type labels: vacation/school

**components/settings/SectionsManager.tsx** (32 Russian strings):
- Use `settings.sectionsManager.*` keys

**components/settings/SubjectsManager.tsx** (15 Russian strings):
- Use `settings.subjectsManager.*` keys

**components/settings/ActivitiesManager.tsx** (17 Russian strings):
- Use `settings.activitiesManager.*` keys

**components/settings/StreakSettings.tsx** (15 Russian strings):
- Use `settings.streakSettings.*` keys

**components/settings/CoinRulesEditor.tsx** (11 Russian strings):
- Use `settings.coinRules.*` keys
- The grade/rule labels match the existing English labels in en.json

**components/settings/NotificationSettings.tsx** (26 Russian strings):
- Use `settings.notificationSettings.*` keys
- Test notification button → `t('settings.notificationSettings.testButton')`

**components/settings/FamilyManager.tsx** (25 Russian strings):
- Use `settings.familyManager.*` keys

**Server-side files (push actions + cron routes)** — these cannot use React hooks.

Strategy: These files send push notifications to devices. The success criteria requires UI to be localized — push notification content is a separate concern. Apply this approach:

For `app/actions/push-streaks.ts`, `app/actions/push.ts`, `app/actions/send-medal.ts`:
- Keep the existing Russian strings as-is (they are push notification content, not UI)
- Add a comment: `// TODO: Phase 4.3+ — localize push notification strings based on family language preference`
- The files already work; this change prevents regression

For `app/api/cron/reminders/route.ts` and `app/api/cron/missed-tasks/route.ts`:
- Same approach — keep Russian strings with TODO comments

For `app/kid/shop/actions.ts` (2 Russian strings):
- These are server actions. If the strings are error messages returned to the client, convert to English equivalents directly (no hook needed in server actions — just use English string literals)
- Pattern: `'Ошибка'` → `'Error'`, `'Недостаточно монет'` → `'Insufficient coins'`

**Final verification pass:**
After completing all replacements, run a Cyrillic detection check:
```bash
find app components -name "*.tsx" -o -name "*.ts" | xargs python3 -c "
import sys, os
files = sys.argv[1:]
for f in files:
    try:
        content = open(f).read()
        lines = [(i+1, l) for i, l in enumerate(content.split('\n')) if any('Ѐ' <= c <= 'ӿ' for c in l) and not l.strip().startswith('//') and not l.strip().startswith('*')]
        if lines:
            print(f'{f}: {len(lines)} lines with Cyrillic')
            for n, l in lines[:3]:
                print(f'  {n}: {l.strip()[:80]}')
    except: pass
" -- $(find app components -name "*.tsx" -o -name "*.ts" | grep -v node_modules | grep -v DailyModal-old)
```

Review output and fix any remaining Cyrillic in JSX text / string literals used as UI (ignore comments, data strings from DB, or intentionally Russian values).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>All 10 settings components have 0 hardcoded Russian strings in UI text. Server-side push files have TODO comments. TypeScript 0 errors. Production build succeeds.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- `npm run build` succeeds (no build-time errors)
- Run Cyrillic detection script from Task 2 — output should show 0 UI-layer files with Russian strings (only push action files and data strings acceptable)
- All 10 settings components import useT and call t()
- Wallboard, parent shop, and onboarding pages render English labels in 'en' mode
- Language toggle in NavBar/KidNav switches all settings labels language
</verification>

<success_criteria>
Phase 4.3 localization is complete when:
1. Language set to 'en': every page, modal, nav, and settings component displays English
2. Language set to 'ru': every page, modal, nav, and settings component displays Russian
3. The language toggle in the nav switches the language immediately without page reload
4. TypeScript compiles with 0 errors
5. Production build succeeds
6. The only remaining Russian strings are in: push notification server actions (marked TODO), code comments, and strings that come from the database (child names, reward names, etc. — user-entered data)
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-localization/04.3-05-SUMMARY.md`
</output>
