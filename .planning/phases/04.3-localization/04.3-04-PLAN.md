---
phase: 04.3-localization
plan: "04"
type: execute
wave: 2
depends_on:
  - "04.3-01"
files_modified:
  - components/DailyModal.tsx
  - components/CoachRatingModal.tsx
  - components/P2PTransferModal.tsx
  - components/GoalsModal.tsx
  - components/ShopModal.tsx
  - components/ExchangeModal.tsx
  - components/WithdrawModal.tsx
  - components/BulkModal.tsx
  - components/WalletDashboard.tsx
  - components/AuditLogViewer.tsx
autonomous: true
requirements:
  - LOC-01

must_haves:
  truths:
    - "DailyModal displays all section labels, save status, and error messages in the active language"
    - "CoachRatingModal displays all labels in the active language"
    - "P2PTransferModal (Gift Coins) displays all labels in the active language"
    - "Shop, Exchange, Withdraw modals display all labels in the active language"
    - "GoalsModal and BulkModal display all labels in the active language"
    - "WalletDashboard and AuditLogViewer display all labels in the active language"
  artifacts:
    - path: "components/DailyModal.tsx"
      provides: "Localized daily fill modal (81 strings)"
      contains: "useT"
    - path: "components/CoachRatingModal.tsx"
      provides: "Localized coach rating modal (78 strings)"
      contains: "useT"
    - path: "components/P2PTransferModal.tsx"
      provides: "Localized P2P transfer modal (114 strings — largest file)"
      contains: "useT"
    - path: "components/AuditLogViewer.tsx"
      provides: "Localized audit log viewer (83 strings)"
      contains: "useT"
  key_links:
    - from: "components/DailyModal.tsx"
      to: "lib/i18n.tsx"
      via: "import { useT } from '@/lib/i18n'"
      pattern: "useT"
    - from: "components/P2PTransferModal.tsx"
      to: "lib/i18n.tsx"
      via: "import { useT } from '@/lib/i18n'"
      pattern: "useT"
---

<objective>
Apply i18n to modal components and wallet management UI — the transactional layer of the app.

Purpose: Modals are high-interaction surfaces. P2PTransferModal (114 Russian lines), DailyModal (81), CoachRatingModal (78), and AuditLogViewer (83) are the densest Russian string sources remaining.

Output: All 10 modal/wallet components use `t()` instead of hardcoded Russian strings.
</objective>

<execution_context>
@/Users/andrei/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrei/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.3-localization/04.3-01-PLAN.md

@components/DailyModal.tsx
@components/CoachRatingModal.tsx
@components/P2PTransferModal.tsx
@components/AuditLogViewer.tsx
@components/GoalsModal.tsx
@components/ShopModal.tsx
@components/ExchangeModal.tsx
@components/WithdrawModal.tsx
@components/BulkModal.tsx
@components/WalletDashboard.tsx

<interfaces>
From lib/i18n.tsx (created in Plan 01):
```typescript
export function useT(): (key: string, vars?: Record<string, string | number>) => string
```

Translation key namespaces (from public/locales/en.json):
- dailyModal.* — DailyModal strings
- coachRating.* — CoachRatingModal strings
- p2pTransfer.* — P2PTransferModal strings
- goals.* — GoalsModal strings
- shopModal.* — ShopModal strings
- exchangeModal.* — ExchangeModal strings
- withdrawModal.* — WithdrawModal strings
- bulkModal.* — BulkModal strings
- walletDashboard.* — WalletDashboard strings
- auditLog.* — AuditLogViewer strings
- common.loading, common.save, common.cancel, common.error — shared strings

Variable interpolation syntax: t('key', { varName: value })
Examples:
  t('shopModal.buyCoinsConfirm', { title: reward.title, price: reward.price_coins })
  t('exchangeModal.rate', { rate: exchangeRate.toFixed(1) })
  t('withdrawModal.confirmWithdraw', { amount: withdrawAmount.toLocaleString('ru-RU') })
  t('goals.remaining', { amount: (target - current).toLocaleString() })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Localize DailyModal and CoachRatingModal</name>
  <files>components/DailyModal.tsx, components/CoachRatingModal.tsx</files>
  <action>
**CRITICAL:** Read each file fully before editing. DailyModal is 1,142 lines — read it completely. Only change string content, preserve all logic, styles, and data handling.

**components/DailyModal.tsx** (81 Russian strings):

Add `import { useT } from '@/lib/i18n'` and `const t = useT()` inside the component function (not at module scope).

Key replacement patterns:
- Section headers: `'Учёба'` → `t('dailyModal.study')`, `'Комната'` → `t('dailyModal.room')`, `'День'` → `t('dailyModal.day')`, `'Спорт'` → `t('dailyModal.sport')`, `'Секции'` → `t('dailyModal.sections')`, `'Чтение'` → `t('dailyModal.reading')`
- Extra sections: `'Доп. занятия'` → `t('dailyModal.extraActivities')`, `'Помощь по дому'` → `t('dailyModal.homeHelp')`, `'Домашнее задание'` → `t('dailyModal.homework')`
- Modal title: `'Заполнить день'` → `t('dailyModal.title')`
- Loading: `'Загрузка...'` → `t('dailyModal.loading')`
- Saving status: `'Сохранение...'` → `t('dailyModal.saving')`, `'✅ Сохранено!'` → `t('dailyModal.saved')`, `'🎉 Готово! Получен бейдж!'` → `t('dailyModal.savedBadge')`
- Sick day: `'День болезни'` → `t('dailyModal.sickDay')`, note → `t('dailyModal.sickDayNote')`
- Schedule alert: `'На этот день нет расписания'` → `t('dailyModal.noSchedule')` — this is in an `alert()` call
- Reading fields: book → `t('dailyModal.book')`, placeholder → `t('dailyModal.bookPlaceholder')`, pages label → `t('dailyModal.pages')`, СТРАНИЦ → `t('dailyModal.pagesUnit')`, minutes → `t('dailyModal.minutes')`, МИНУТ → `t('dailyModal.minutesUnit')`
- Book finished → `t('dailyModal.bookFinished')`
- Reading note placeholder → `t('dailyModal.readingNote')`
- Unit `'раз'` → `t('dailyModal.unit')` (used in exercise repetition counts)
- Day type `'Учебный'` initial state → `t('kidDay.schoolDay')` (same string as kid day page)
- The comments in Russian (lines like `// УЧЁБА (school days)`) — leave as-is, these are code comments not UI strings

**components/CoachRatingModal.tsx** (78 Russian strings):

Read the full file before editing. This modal has many Russian strings for rating options, labels, and UI text.

Add `import { useT } from '@/lib/i18n'` and `const t = useT()` inside the component.

Key replacements:
- Modal title → `t('coachRating.title')`
- Loading → `t('coachRating.loading')`
- Save button → `t('coachRating.save')`
- Cancel → `t('coachRating.cancel')`
- Rating label → `t('coachRating.rating')`
- Comment label → `t('coachRating.comment')`
- Comment placeholder → `t('coachRating.commentPlaceholder')`
- Any other Russian labels in this modal
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>DailyModal.tsx and CoachRatingModal.tsx have 0 hardcoded Russian strings in UI text. TypeScript 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Localize P2PTransfer, Goals, Shop, Exchange, Withdraw, Bulk modals + WalletDashboard + AuditLogViewer</name>
  <files>components/P2PTransferModal.tsx, components/GoalsModal.tsx, components/ShopModal.tsx, components/ExchangeModal.tsx, components/WithdrawModal.tsx, components/BulkModal.tsx, components/WalletDashboard.tsx, components/AuditLogViewer.tsx</files>
  <action>
**CRITICAL:** Read each file fully before editing. P2PTransferModal is 386 lines, AuditLogViewer is 330 lines — read them fully. Only change string content.

For all 8 files: add `import { useT } from '@/lib/i18n'` and `const t = useT()` inside each component function.

**components/P2PTransferModal.tsx** (114 Russian strings — largest file in this plan):

Read fully. This modal handles coin gifting between family members. Key replacements:
- Modal title → `t('p2pTransfer.title')`
- Field labels: from/to/amount → `t('p2pTransfer.from')`, `t('p2pTransfer.to')`, `t('p2pTransfer.amount')`
- Buttons: send/cancel → `t('p2pTransfer.send')`, `t('p2pTransfer.cancel')`
- Loading → `t('p2pTransfer.loading')`
- Success/error messages → `t('p2pTransfer.success')`, `t('p2pTransfer.error')`
- Insufficient funds → `t('p2pTransfer.insufficientFunds')`
- Amount placeholder → `t('p2pTransfer.amountPlaceholder')`
- Note field → `t('p2pTransfer.note')`, `t('p2pTransfer.notePlaceholder')`
- Child selector → `t('p2pTransfer.selectChild')`
- All other Russian labels in this file

**components/GoalsModal.tsx** (36 Russian strings):

- Modal title → `t('goals.title')`
- Add goal button → `t('goals.addGoal')`
- Loading → `t('goals.loading')`
- Form fields: name/target/deadline → `t('goals.name')`, `t('goals.target')`, `t('goals.deadline')`
- Save/cancel → `t('goals.save')`, `t('goals.cancel')`
- No goals → `t('goals.noGoals')`
- Delete confirm → `t('goals.deleteConfirm')`
- Remaining → `t('goals.remaining', { amount: ... })`

**components/ShopModal.tsx** (22 Russian strings):

- Title → `t('shopModal.title')`
- Balance labels → `t('shopModal.coinsLabel')`, `t('shopModal.moneyLabel')`
- Tab labels → `t('shopModal.forCoins')`, `t('shopModal.forMoney')`
- Loading → `t('shopModal.loading')`
- Empty states → `t('shopModal.noCoinsRewards')`, `t('shopModal.noMoneyRewards')`
- Buy confirm (in alert/confirm calls) → `t('shopModal.buyCoinsConfirm', { title: ..., price: ... })`
- Buy success (in alert) → `t('shopModal.bought', { title: ... })`
- Error messages → `t('shopModal.insufficientCoins')`, `t('shopModal.insufficientMoney')`, `t('shopModal.buyError')`

**components/ExchangeModal.tsx** (28 Russian strings):

- Title → `t('exchangeModal.title')`
- Balance labels → `t('exchangeModal.coinsLabel')`, `t('exchangeModal.moneyLabel')`
- Rate section → `t('exchangeModal.rateTitle')`, `t('exchangeModal.rate', { rate: exchangeRate.toFixed(1) })`
- Bonus → `t('exchangeModal.bonus', { pct: bonus })`
- Tips based on coin count → `t('exchangeModal.tip100')`, `t('exchangeModal.tip500', { amount: 500 - coins })` etc.
- Exchange/cancel buttons → `t('exchangeModal.exchange')`, `t('exchangeModal.cancel')`
- Error messages → `t('exchangeModal.loading')`, `t('exchangeModal.insufficientCoins')`, `t('exchangeModal.error')`

**components/WithdrawModal.tsx** (23 Russian strings):

- Title → `t('withdrawModal.title')`
- Available label → `t('withdrawModal.available')`
- How much label → `t('withdrawModal.howMuch')`
- Withdraw/cancel → `t('withdrawModal.withdraw')`, `t('withdrawModal.cancel')`
- Confirm (in confirm() call) → `t('withdrawModal.confirmWithdraw', { amount: withdrawAmount.toLocaleString('ru-RU') })`
- Success (in alert) → `t('withdrawModal.success')`
- Errors → `t('withdrawModal.insufficientFunds')`, `t('withdrawModal.error')`

**components/BulkModal.tsx** (32 Russian strings):

- Title → `t('bulkModal.title')`
- Loading → `t('bulkModal.loading')`
- Save/cancel → `t('bulkModal.save')`, `t('bulkModal.cancel')`
- Action labels → `t('bulkModal.addCoins')`, `t('bulkModal.removeCoins')`
- Amount/reason fields → `t('bulkModal.amount')`, `t('bulkModal.reason')`, `t('bulkModal.reasonPlaceholder')`
- Success/error → `t('bulkModal.success')`, `t('bulkModal.error')`

**components/WalletDashboard.tsx** (13 Russian strings):

- Title → `t('walletDashboard.title')`
- Loading → `t('walletDashboard.loading')`
- Balance/coins/money labels → `t('walletDashboard.balance')` etc.
- Action buttons → `t('walletDashboard.addCoins')`, `t('walletDashboard.exchange')` etc.
- No transactions → `t('walletDashboard.noTransactions')`

**components/AuditLogViewer.tsx** (83 Russian strings):

Read fully. Key replacements:
- Title → `t('auditLog.title')`
- Loading → `t('auditLog.loading')`
- No events → `t('auditLog.noEvents')`
- Filter labels → `t('auditLog.filterAll')`, `t('auditLog.filterCoins')` etc.
- Column headers → `t('auditLog.dateLabel')`, `t('auditLog.actionLabel')`, `t('auditLog.actorLabel')`
- All other Russian labels in this file (action descriptions, event type labels)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All 8 component files have 0 hardcoded Russian strings in UI text. TypeScript 0 errors. All t() calls use keys defined in public/locales/en.json.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- All 10 files import useT and call t() for UI strings
- DailyModal section headers switch language when language store changes
- ShopModal buy confirm/success messages use t() with variable interpolation
- AuditLogViewer filter labels and event descriptions switch language
</verification>

<success_criteria>
All 10 modal/wallet/audit components display entirely in English when language is 'en'. No hardcoded Russian strings remain in JSX text content or string literals used as UI labels in these files.
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-localization/04.3-04-SUMMARY.md`
</output>
