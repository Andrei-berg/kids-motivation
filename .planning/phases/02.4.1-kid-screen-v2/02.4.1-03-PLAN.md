---
phase: 02.4.1-kid-screen-v2
plan: "03"
type: execute
wave: 3
depends_on:
  - "02.4.1-01"
  - "02.4.1-02"
files_modified:
  - app/kid/day/page.tsx
autonomous: true
requirements:
  - REQ-KID-001
  - REQ-KID-002
  - REQ-KID-003

must_haves:
  truths:
    - "Child sees 3 tabs on /kid/day: Сегодня, Неделя, Расходы"
    - "Сегодня tab: hero with coin balance + XP bar, 4 stat cards with coin deltas, fill form button or read-only summary"
    - "Неделя tab: 7-day horizontal calendar strip with coin total per day; today highlighted"
    - "Расходы tab: list of active sections with monthly cost, total monthly spend on child"
    - "Tapping Заполнить сегодня opens KidDayFillForm inline (not modal)"
    - "After form save, celebration overlay shown with total coins earned"
    - "SVG ring removed — replaced by 7-day calendar strip"
  artifacts:
    - path: "app/kid/day/page.tsx"
      provides: "Tabbed /kid/day page (Сегодня | Неделя | Расходы)"
      min_lines: 300
  key_links:
    - from: "app/kid/day/page.tsx"
      to: "components/kid/KidDayFillForm.tsx"
      via: "import and render KidDayFillForm"
      pattern: "KidDayFillForm"
    - from: "app/kid/day/page.tsx"
      to: "weekRaw.days + weekRaw.grades"
      via: "inline coin computation using GRADE_COINS constant per day"
      pattern: "GRADE_COINS"
    - from: "app/kid/day/page.tsx"
      to: "lib/repositories/expenses.repo.ts"
      via: "getSectionsForChildExpenses for expenses tab"
      pattern: "getSectionsForChildExpenses"
    - from: "app/kid/day/page.tsx"
      to: "lib/repositories/wallet.repo.ts"
      via: "getWallet for coin balance in hero"
      pattern: "getWallet"
---

<objective>
Complete redesign of /kid/day into a 3-tab page: Сегодня (today summary + fill form), Неделя (7-day calendar strip), Расходы (family spending on this child). Replace the SVG ring with the calendar strip.

Purpose: Makes the kid experience engaging with immediate coin feedback and personal context (family investment in them).
Output: `app/kid/day/page.tsx` rewritten with tabbed layout using existing kid CSS classes.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.4.1-kid-screen-v2/02.4.1-01-SUMMARY.md
@.planning/phases/02.4.1-kid-screen-v2/02.4.1-02-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts the executor needs -->

KidDayFillForm (from Plan 02):
```typescript
// components/kid/KidDayFillForm.tsx
interface KidDayFillFormProps {
  childId: string
  date: string
  fillMode: 1 | 2 | 3
  dayType: 'school' | 'weekend' | 'vacation'
  existingDay: DayData | null
  onSaved: (coinsEarned: number) => void
}
```

From lib/repositories/wallet.repo.ts:
```typescript
getWallet(childId: string): Promise<Wallet>
// Wallet has: coins: number, money: number
```

From lib/repositories/expenses.repo.ts (added in Plan 01):
```typescript
getSectionsForChildExpenses(childId: string): Promise<Array<{
  id: string; name: string; cost: number | null; schedule_days: string[];
}>>
```

From lib/repositories/children.repo.ts:
```typescript
getChild(childId: string): Promise<Child>
// Child now has kid_fill_mode: 1 | 2 | 3
getDay(childId: string, date: string): Promise<DayData | null>
```

From lib/api.ts (re-export wrappers):
```typescript
api.getWeekData(childId, weekStart): Promise<{
  days: DayData[], grades: SubjectGrade[], sports: any[], weekRecord: any
}>
```

From @/utils/helpers:
```typescript
getWeekRange(weekStart: string): { start: string, end: string }
normalizeDate(date: string | Date): string
```

CelebrationOverlay (already exists in components/kid/):
```typescript
// components/kid/CelebrationOverlay.tsx — already mounted in KidLayout
// To trigger it: dispatch a custom event or use a flag via localStorage
// Existing pattern: checks localStorage 'last_celebration_check' timestamp
// For this page: show an inline celebration panel instead of relying on CelebrationOverlay
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite /kid/day with 3 tabs and all sub-sections</name>
  <files>app/kid/day/page.tsx</files>
  <action>
Fully rewrite `app/kid/day/page.tsx`. Keep `'use client'` at top. Preserve existing helpers (LEVEL_TITLES, getLevelTitle, gradeChipClass, streakLabel) — reuse them.

**New state:**
- `tab: 'today' | 'week' | 'expenses'` — default 'today'
- `wallet`: Wallet | null
- `sections`: Array<{id, name, cost, schedule_days}> — for expenses tab
- `weekDayCoins`: Record<string, number> — maps YYYY-MM-DD → total coins for 7-day strip
- `showFillForm`: boolean — false by default
- `lastCoinsEarned`: number | null — coins from last submission (for celebration)

**Data loading in useEffect:**
Load in parallel:
- `api.getChild(activeMemberId)` → child (with kid_fill_mode)
- `api.getDay(activeMemberId, today)` → todayDay
- `api.getSubjectGradesForDate(activeMemberId, today)` → todayGrades
- `api.getWeekData(activeMemberId, weekStart)` → weekRaw
- `api.getStreaks(activeMemberId)` → streaks
- `api.getGoals(activeMemberId)` → goals
- `getWallet(activeMemberId)` → wallet
- `getSectionsForChildExpenses(activeMemberId)` → sections

After loading weekRaw, compute `weekDayCoins` inline (do NOT call getWeekScore — compute directly from raw data to avoid extra round-trip):
```typescript
const GRADE_COINS: Record<number, number> = { 5: 5, 4: 3, 3: -3, 2: -5, 1: -10 }
const dayCoins: Record<string, number> = {}
weekRaw.days.forEach(d => {
  const base = (d.room_ok ? 3 : 0) + (d.good_behavior ? 5 : 0)
  dayCoins[d.date] = base
})
weekRaw.grades.forEach(g => {
  dayCoins[g.date] = (dayCoins[g.date] ?? 0) + (GRADE_COINS[g.grade] ?? 0)
})
setWeekDayCoins(dayCoins)
```

**Helper: buildWeekDays()**
Returns array of 7 date strings for Mon–Sun of current week (use getWeekRange result):
```typescript
function buildWeekDays(weekStart: string): string[] {
  return Array.from({ length: 7 }, (_, i) => {
    const d = new Date(weekStart)
    d.setDate(d.getDate() + i)
    return d.toISOString().slice(0, 10)
  })
}
```

**Tab bar** (render at top of page, below hero):
3 pill buttons: Сегодня | Неделя | Расходы
Active pill: `bg-amber-400 text-white font-bold rounded-full px-4 py-1.5`
Inactive: `text-gray-500 px-4 py-1.5 rounded-full`

---

**TODAY tab (`tab === 'today'`):**

A. **Hero banner** (same as existing but ADD coin balance):
- Greeting + level badge (keep existing)
- Add coin balance row: `🪙 {wallet?.coins ?? 0} монет`
- XP bar (keep existing)

B. **4 stat cards** (replace the existing 3-card grid with 4):
- Card 1: Room status (✅/❌) + `+{room_ok ? 3 : 0}💰`
- Card 2: Behavior (👍/👎) + `+{good_behavior ? 5 : 0}💰`
- Card 3: Study grades count with average
- Card 4: Streaks count (active streaks, fire emoji)
Grid: `grid grid-cols-2 gap-3` (2x2)

C. **Fill form or summary:**
- If `todayDay?.filled_by === 'child'` → show read-only summary "День заполнен ✅"
- Else if `!showFillForm && child?.kid_fill_mode` → show CTA button:
  `<button onClick={() => setShowFillForm(true)}>✏️ Заполнить сегодня</button>`
  Style: full-width, amber gradient, rounded-2xl, py-3 font-bold
- If `showFillForm`:
  Render `<KidDayFillForm childId={activeMemberId} date={today} fillMode={child.kid_fill_mode} dayType={dayType} existingDay={todayDay} onSaved={handleFormSaved} />`

D. **Celebration panel** (shown after save, `lastCoinsEarned !== null`):
Full-width card with amber/yellow gradient:
`🎉 Отлично! Ты заработал {lastCoinsEarned} монет!`
`[Посмотреть кошелёк →]` button linking to /kid/wallet
Auto-dismiss after 8 seconds.

E. **Active streaks** (keep existing streaks row, same code)
F. **Active goal widget** (keep existing, same code)

---

**WEEK tab (`tab === 'week'`):**

A. **7-day calendar strip** (replaces SVG ring):
Horizontal scroll row of 7 day cells.

Each cell is a rounded card (`rounded-2xl p-2 text-center min-w-[44px]`):
- Day letter (П/В/С/Ч/П/С/В — short Russian weekday)
- Day number (e.g. "10")
- Coins badge: if date in weekDayCoins → show `+{coins}💰` in green text; else show `—`
- Today: highlighted with `bg-amber-400 text-white`
- Past filled days: `bg-emerald-50 border border-emerald-200`
- Future/unfilled: `bg-white border border-gray-100`

The Russian day letters for Mon=П, Tue=В, Wed=С, Thu=Ч, Fri=П, Sat=С, Sun=В.
Use `['П','В','С','Ч','П','С','В']` indexed by `(dayOfWeek + 6) % 7` for Mon-first ordering.

B. **Week summary cards** (below strip):
3 small cards in a row:
- 🏠 Комната: {roomOkDays}/7 дн.
- 😊 Поведение: {behaviorDays}/7 дн.
- 📖 Оценки: {gradedDays}/7 дн.
These are already computed from weekRaw — reuse.

C. **Week total** (single bold line):
`Итого за неделю: {sum of weekDayCoins values}💰`

---

**РАСХОДЫ tab (`tab === 'expenses'`):**

A. **Header card** (amber gradient):
`Семья тратит на тебя`
Subtitle: `Это инвестиция в твоё будущее 💛`

B. **Sections list:**
For each section in `sections`:
- Row: `{name}` + `{cost ? cost + ' ₽/мес' : 'бесплатно'}`
- Schedule days shown as small pills if schedule_days.length > 0
- Styled as simple list items in a kid-card

C. **Total monthly:**
`Итого в месяц: {sum of all section costs} ₽`
If no sections: show empty state "Секций не найдено — спроси родителей"

---

**Existing code to preserve/reuse:**
- LEVEL_TITLES map and getLevelTitle helper — keep verbatim
- gradeChipClass helper — keep
- streakLabel helper — keep
- Loading skeleton JSX — keep (adjust grid to 4 cards)
- "no activeMemberId" guard — keep

**Import additions needed:**
```typescript
import { getWallet } from '@/lib/repositories/wallet.repo'
import { getSectionsForChildExpenses } from '@/lib/repositories/expenses.repo'
import KidDayFillForm from '@/components/kid/KidDayFillForm'
import type { Wallet } from '@/lib/models/wallet.types'
import { getWeekRange } from '@/utils/helpers'
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compiles cleanly. /kid/day renders 3 tabs. SVG ring code removed. KidDayFillForm imported and rendered in today tab. getSectionsForChildExpenses called for expenses tab. getWallet called for hero coin balance.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no new errors
- app/kid/day/page.tsx no longer contains SVG ring code (`RADIUS`, `CIRCUMFERENCE`, `dashOffset`)
- app/kid/day/page.tsx imports KidDayFillForm
- app/kid/day/page.tsx has `tab` state with 'today' | 'week' | 'expenses'
- app/kid/day/page.tsx imports getWallet and getSectionsForChildExpenses
- app/kid/day/page.tsx contains `GRADE_COINS` constant for inline coin computation
- `npm run build` completes without errors (run lint check: `npm run lint`)
</verification>

<success_criteria>
/kid/day is fully tabbed with a gamified today view, a 7-day coin calendar, and a family expenses view. The old SVG ring is gone. Kids can tap "Заполнить сегодня" to fill in their day and see coins earned immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/02.4.1-kid-screen-v2/02.4.1-03-SUMMARY.md`
</output>
