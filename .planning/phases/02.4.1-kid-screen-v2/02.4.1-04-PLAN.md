---
phase: 02.4.1-kid-screen-v2
plan: "04"
type: execute
wave: 4
depends_on:
  - "02.4.1-01"
  - "02.4.1-03"
files_modified:
  - app/parent/settings/page.tsx
autonomous: false
requirements:
  - REQ-DAY-001
  - REQ-KID-001
  - REQ-KID-002
  - REQ-KID-003

must_haves:
  truths:
    - "Parent settings page has a new tab for kid fill-mode configuration"
    - "Parent can select Mode 1, 2, or 3 per child"
    - "Saving the mode updates children.kid_fill_mode in the database"
    - "Human tester can visit /kid/day and see the fill form with correct mode"
  artifacts:
    - path: "app/parent/settings/page.tsx"
      provides: "Settings page with kid fill-mode tab"
      contains: "kid_fill_mode"
  key_links:
    - from: "app/parent/settings/page.tsx"
      to: "children table"
      via: "Supabase update on children.kid_fill_mode"
      pattern: "kid_fill_mode"
---

<objective>
Add a "Режим ребёнка" tab to parent settings for configuring each child's self-fill mode (1/2/3). Then pause for human verification of the full 2.4.1 feature set.

Purpose: Parents control how much children can fill in themselves. Without this setting the form defaults to mode 1 (mood only) but parents need a UI to upgrade it.
Output: New "Ребёнок" tab in /parent/settings + human checkpoint.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.4.1-kid-screen-v2/02.4.1-01-SUMMARY.md
@.planning/phases/02.4.1-kid-screen-v2/02.4.1-03-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts the executor needs -->

From app/parent/settings/page.tsx (current tab structure):
```typescript
type Tab = 'family' | 'subjects' | 'coins' | 'audit'
const TABS = [
  { id: 'family', label: '👨‍👩‍👧 Семья' },
  { id: 'subjects', label: '📚 Предметы' },
  { id: 'coins', label: '🪙 Монеты' },
  { id: 'audit', label: '📋 Журнал' },
]
// Add: { id: 'kids', label: '🧒 Ребёнок' }
```

From lib/repositories/children.repo.ts:
```typescript
getChildren(): Promise<Child[]>
// Child now has kid_fill_mode: 1 | 2 | 3
```

Supabase direct update pattern (used in settings page already):
```typescript
const supabase = createClient() // from @/lib/supabase/browser
await supabase.from('children').update({ kid_fill_mode: mode }).eq('id', childId)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Ребёнок tab to parent settings</name>
  <files>app/parent/settings/page.tsx</files>
  <action>
Open `app/parent/settings/page.tsx` and add a new "Ребёнок" tab for configuring kid fill modes.

**Changes:**

1. Extend the `Tab` type: add `'kids'` to the union.

2. Add to `TABS` array:
```typescript
{ id: 'kids', label: '🧒 Ребёнок' }
```

3. Add state:
```typescript
const [children, setChildren] = useState<Child[]>([])
const [kidFillModes, setKidFillModes] = useState<Record<string, 1 | 2 | 3>>({})
const [savingKidMode, setSavingKidMode] = useState<Record<string, boolean>>({})
```

4. In the `useEffect` that loads settings data, add `getChildren()` and populate `kidFillModes` from each child's `kid_fill_mode` field (default to 1 if null).

5. Add handler:
```typescript
async function handleSaveKidFillMode(childId: string) {
  setSavingKidMode(prev => ({ ...prev, [childId]: true }))
  try {
    const { createClient } = await import('@/lib/supabase/browser')
    const supabase = createClient()
    await supabase.from('children').update({ kid_fill_mode: kidFillModes[childId] ?? 1 }).eq('id', childId)
    // Show toast or success indicator
  } finally {
    setSavingKidMode(prev => ({ ...prev, [childId]: false }))
  }
}
```

6. Add `kids` tab JSX in the tab content switch/conditional area:

```tsx
{tab === 'kids' && (
  <div className="space-y-4">
    <p className="text-sm text-gray-500">
      Выбери, что ребёнок может заполнять сам. Оценки всегда вводит родитель.
    </p>

    {children.map(child => (
      <div key={child.id} className="bg-gray-800 rounded-xl p-4 space-y-3">
        <div className="flex items-center gap-2">
          <span className="text-2xl">{child.emoji}</span>
          <span className="text-white font-bold">{child.name}</span>
        </div>

        <div className="space-y-2">
          {[
            { mode: 1, label: 'Режим 1', desc: 'Только настроение и доп. занятия' },
            { mode: 2, label: 'Режим 2', desc: 'Комната + настроение + доп. занятия' },
            { mode: 3, label: 'Режим 3', desc: 'Комната + настроение + спорт + занятия' },
          ].map(({ mode, label, desc }) => (
            <button
              key={mode}
              onClick={() => setKidFillModes(prev => ({ ...prev, [child.id]: mode as 1 | 2 | 3 }))}
              className={`w-full text-left p-3 rounded-lg border transition ${
                kidFillModes[child.id] === mode
                  ? 'border-amber-400 bg-amber-400/10 text-amber-300'
                  : 'border-gray-700 text-gray-400'
              }`}
            >
              <span className="font-semibold">{label}</span>
              <span className="ml-2 text-xs opacity-70">{desc}</span>
            </button>
          ))}
        </div>

        <button
          onClick={() => handleSaveKidFillMode(child.id)}
          disabled={savingKidMode[child.id]}
          className="w-full py-2 bg-amber-500 hover:bg-amber-400 text-white font-bold rounded-lg disabled:opacity-50"
        >
          {savingKidMode[child.id] ? 'Сохраняю...' : 'Сохранить'}
        </button>
      </div>
    ))}

    {children.length === 0 && (
      <p className="text-gray-500 text-sm">Нет детей в семье</p>
    )}
  </div>
)}
```

Import `getChildren` from `@/lib/repositories/children.repo` at the top of the file (check if already imported; if so, just add to the destructured imports from existing import line).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compiles. Parent settings page has a "Ребёнок" tab. Mode selection buttons render per child. Save handler calls Supabase update on children.kid_fill_mode.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 2.4.1 complete:
    - DB migration SQL file ready (not yet applied to Supabase)
    - KidDayFillForm component: room checklist, mood picker, extra activities, live coin counter
    - /kid/day redesigned: 3 tabs (Сегодня/Неделя/Расходы), coin balance in hero, 7-day calendar strip, fill form integration, celebration panel
    - Parent settings: new "Ребёнок" tab for configuring fill mode per child
  </what-built>
  <how-to-verify>
    1. Run Supabase migration: open Supabase SQL Editor, paste contents of `supabase-migration-kid-fill-v2.sql`, click Run
    2. Run: `npm run dev`
    3. Log in as a parent → go to /parent/settings → click "Ребёнок" tab → set a child to Mode 2 → Save
    4. Open /kid/day (or preview via parent Kid View button)
       - Verify: hero shows coin balance + XP bar
       - Verify: 4 stat cards visible (Room, Behavior, Grades, Streaks)
       - Verify: 3 tabs at top (Сегодня / Неделя / Расходы)
       - Verify: "Заполнить сегодня" button visible
    5. Tap "Заполнить сегодня":
       - Verify: room checklist (5 items) shown (Mode 2+)
       - Verify: mood picker (5 emoji) shown
       - Verify: live coin counter updates when tapping room items
       - Verify: submit saves and celebration panel shows coins earned
    6. Tap "Неделя" tab:
       - Verify: 7-day horizontal strip shown with day letters and coin totals
       - Verify: today cell highlighted in amber
       - Verify: SVG ring is GONE
    7. Tap "Расходы" tab:
       - Verify: sections list shown with monthly costs (or empty state if none configured)
    8. Run: `npm run build` — verify no build errors
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no new errors
- Parent settings has "Ребёнок" tab that renders per-child mode selectors
- Full phase human verification passed per checkpoint above
</verification>

<success_criteria>
Parent can configure kid fill mode per child. The kid sees the appropriate form on /kid/day. The 7-day calendar strip replaced the SVG ring. Family expenses are visible in the Расходы tab. Human verified the full flow end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/02.4.1-kid-screen-v2/02.4.1-04-SUMMARY.md`
</output>
