---
phase: 04.2-ux-animations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - components/kid/KidNav.tsx
  - components/parent/ParentNav.tsx
autonomous: true
requirements:
  - UX-02

must_haves:
  truths:
    - "Every button, link, and interactive element in the app is at least 44×44px tap area on a 375px-wide screen"
    - "KidNav bottom tab links are visually at least 44px tall (not just invisible padding)"
    - "ParentNav bottom mobile links are at least 44px tall"
    - "Grade buttons (1–5) have a visible ripple feedback when tapped on mobile"
    - "Logout button in KidNav top-right grows from 36px to 44px"
    - "No existing visual appearance regresses — only tap areas grow or pills get a height bump"
  artifacts:
    - path: "app/globals.css"
      provides: "Updated .btn (min-height 44px), .pill (min-height 44px), .premium-grade-btn with ripple, .touch-target utility class"
      contains: "min-height: 44px"
    - path: "components/kid/KidNav.tsx"
      provides: "KidNav Link items with minHeight: 44px; logout button bumped to 44px"
    - path: "components/parent/ParentNav.tsx"
      provides: "ParentNav mobile Link+button items with min-height: 44px"
  key_links:
    - from: "app/globals.css"
      to: ".btn, .pill"
      via: "min-height: 44px; display: inline-flex; align-items: center"
      pattern: "min-height.*44"
    - from: "components/kid/KidNav.tsx"
      to: "Link (bottom nav) + logout button"
      via: "minHeight: 44px in inline style"
      pattern: "minHeight.*44"
---

<objective>
Fix all touch targets throughout the app to meet the 44×44px minimum. Apply CSS fixes to `.btn`, `.pill`, and `.premium-grade-btn` in globals.css. Update KidNav and ParentNav mobile bottom bars for visible height bumps. Add ripple feedback to grade buttons.

Purpose: Children tap grade buttons many times per session — missed taps are frustrating. Adults navigating on phones should never miss a tap target.
Output: Updated globals.css + KidNav.tsx + ParentNav.tsx with all interactive elements at minimum 44px.
</objective>

<execution_context>
@/Users/andrei/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrei/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.2-ux-animations/04.2-CONTEXT.md

<interfaces>
<!-- Current sizes that need fixing: -->

/* .btn — padding: 10px 18px → ~36px height. Needs: min-height: 44px */
/* .pill — padding: 8px 16px → ~32px height. Needs: min-height: 44px (visible bump per user decision) */
/* .premium-grade-btn — padding: 16px, font-size 24px → already ~56px height, OK. Add ripple animation. */

/* KidNav bottom tab Link items: padding: '6px 2px' → ~42px height. Close but need explicit minHeight: 44px */
/* KidNav logout button: width: 36, height: 36 → TOO SMALL. Bump to 44px */
/* ParentNav mobile Link+button: py-1.5 px-2 → ~34px height. Needs min-height or py bump */

<!-- Per user decision from CONTEXT.md: -->
/* Nav pills: give VISIBLE size bump to ~44px height (intentional visual improvement, not just tap area) */
/* Other elements: use invisible padding / ::after pseudo-element so visual size unchanged */
/* Grade buttons: add subtle ripple on tap (CSS ::after approach) */
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix .btn and .pill min-height in globals.css; add ripple to grade buttons</name>
  <files>app/globals.css</files>
  <action>
    Make the following targeted CSS edits to `app/globals.css`. IMPORTANT: Only change what is specified — do NOT remove or restructure any other CSS.

    **1. Fix `.btn` min-height (around line 197):**
    Add `min-height: 44px; display: inline-flex; align-items: center; justify-content: center;` to `.btn` rule. The button already uses `padding: 10px 18px` which on some text sizes gives ~36px — min-height: 44px enforces the floor without changing visual appearance for large buttons.

    **2. Fix `.pill` min-height (around line 280):**
    Add `min-height: 44px; display: inline-flex; align-items: center; justify-content: center;` to `.pill` rule. This is the visible size bump per user decision — pills will appear 44px tall. Also change `padding: 8px 16px` to `padding: 10px 16px` to achieve a nicer look at 44px height.

    **3. Add ripple animation to `.premium-grade-btn` (around line 1155):**
    The grade button already has `padding: 16px; font-size: 24px` which makes it ~56px — tap area is fine. Add CSS ripple feedback on active state (same pattern as `.btn::after` which already exists):

    After the existing `.premium-grade-btn.active` rule, add:
    ```css
    /* Ripple feedback for grade buttons */
    .premium-grade-btn {
      position: relative;
      overflow: hidden;
    }
    .premium-grade-btn::after {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 0;
      height: 0;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.4);
      transform: translate(-50%, -50%);
      transition: width 0.3s ease-out, height 0.3s ease-out, opacity 0.3s ease-out;
      opacity: 0;
    }
    .premium-grade-btn:active::after {
      width: 160px;
      height: 160px;
      opacity: 1;
    }
    ```

    **4. Add `.touch-target` utility class** — for elements that need 44px tap area without visual size change. Add near the end of globals.css before the last comment block:
    ```css
    /* Touch target utility — use on small interactive elements */
    .touch-target {
      position: relative;
      min-height: 44px;
      min-width: 44px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
    }
    .touch-target::after {
      content: '';
      position: absolute;
      inset: -6px;
    }
    ```

    IMPORTANT: The `.btn::after` ripple already exists at line 258 — do NOT duplicate it. Only add the `.premium-grade-btn::after` ripple.

    IMPORTANT: Do NOT change `.btn.primary`, `.btn.danger`, `.btn.ghost`, `.btn:hover`, or any variant rules — only add properties to the base `.btn` rule.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>
    - `.btn` has `min-height: 44px; display: inline-flex; align-items: center; justify-content: center;`
    - `.pill` has `min-height: 44px; display: inline-flex; align-items: center; justify-content: center;` and `padding: 10px 16px`
    - `.premium-grade-btn::after` and `.premium-grade-btn:active::after` rules exist for ripple
    - `.touch-target` utility class exists
    - TypeScript compiles with 0 errors (CSS file changes do not affect TS)
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix KidNav and ParentNav mobile tap targets</name>
  <files>components/kid/KidNav.tsx, components/parent/ParentNav.tsx</files>
  <action>
    **KidNav.tsx changes:**

    1. **Logout button (top-right, line ~136–146):** Change `width: 36, height: 36` to `width: 44, height: 44`. This is the explicit 44px bump.

    2. **Bottom nav Link items (line ~113–130):** Add `minHeight: 44` to the Link's inline style object. Current style is `{ flex: 1, padding: '6px 2px', ... }`. Change to `{ flex: 1, padding: '6px 2px', minHeight: 44, ... }`. This ensures the tap zone is at least 44px tall. The visual tab with icon + label is ~42px currently — `minHeight: 44` adds a 1px floor invisibly.

    **ParentNav.tsx changes:**

    The mobile bottom tab bar (`md:hidden fixed bottom-0...`) uses `py-1.5` (~6px top+bottom = 12px padding) plus `h-5 w-5` icons (20px) plus text (~12px) = ~44px total, but varies. Add explicit `min-h-[44px]` to each Link and the sign-out button to guarantee the floor.

    For the mobile `<Link>` elements (line ~124–135): change className from `flex flex-col items-center gap-0.5 px-2 py-1.5 rounded-lg...` to `flex flex-col items-center gap-0.5 px-2 py-1.5 min-h-[44px] rounded-lg...`

    For the mobile sign-out `<button>` (line ~138–146): add `min-h-[44px]` to className.

    Do NOT change any desktop nav styles (they are inside `hidden md:flex` wrappers).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>
    - KidNav logout button: width and height both 44
    - KidNav bottom Link items: minHeight: 44 in inline style
    - ParentNav mobile Link+button: min-h-[44px] in className
    - TypeScript compiles with 0 errors
    - No desktop nav styles changed
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` — 0 errors
2. Open DevTools → device mode (375px width iPhone SE) → inspect `.btn` elements — computed height ≥ 44px
3. Inspect `.pill` elements — computed height ≥ 44px
4. Tap a grade button (1–5) on mobile — see ripple feedback spread from center
5. KidNav bottom tabs are visibly taller (44px)
6. KidNav logout button (top-right) is 44×44px
</verification>

<success_criteria>
- globals.css: .btn and .pill have min-height: 44px
- globals.css: .premium-grade-btn has ::after ripple animation
- globals.css: .touch-target utility class exists
- KidNav: logout button 44×44; bottom Links minHeight: 44
- ParentNav: mobile Links and sign-out button have min-h-[44px]
- TypeScript: 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-ux-animations/04.2-02-SUMMARY.md` with what was built, key decisions, and any deviations from this plan.
</output>
