---
phase: 05.6-day-blocks
plan: 06
subsystem: ui
tags: [react, day-blocks, kid-day-form, regression-fix]

# Dependency graph
requires:
  - phase: 05.6-day-blocks
    provides: day_blocks_enabled flag, renderBuiltinBlock('room') flag-on rendering, roomBody constant
provides:
  - Room checklist FillSection restored in KidDayFillForm's flag-off render branch
  - Flag-off/default families (day_blocks_enabled=false) regain room UI and correct roomChecked state on save
affects: [05.6-day-blocks verification, room coin award path]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified: [components/kid/KidDayFillForm.tsx]

key-decisions:
  - "Placed the Room FillSection as the first item inside the flag-off `<>` branch (right after the ternary's else, before Extra activities) rather than literally before the always-rendered Mood section — Mood was hoisted outside the ternary during 5.6 and is now flag-agnostic, so re-inserting Room ahead of it would render Room on both flag states and duplicate the flag-on renderBuiltinBlock('room') section. This matches the review's own clarification: 'inside the flag-off <> branch, before or after Mood as pre-5.6 (it was before Mood)'."

patterns-established: []

requirements-completed: []

# Metrics
duration: 6min
completed: 2026-07-14
---

# Phase 05.6 Plan 06: Restore Room FillSection in Flag-Off Branch Summary

**Closed CR-01: the kid day form's flag-off branch (the default for every existing family) now renders the Room checklist again, using the same shared `roomBody` constant as the flag-on path — no duplication, no changes to `handleSubmit` or the room dual-write.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-07-14T19:39:00Z
- **Completed:** 2026-07-14T19:45:27Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Room `FillSection` (title/icon/sub matching `renderBuiltinBlock('room')` exactly) now renders inside the flag-off `<>` branch, as the first section before Extra activities — restoring the pre-5.6 UI for the default flag-off population.
- `roomBody` remains a single shared constant referenced in exactly 3 places (definition, flag-on `renderBuiltinBlock('room')`, new flag-off usage) — no duplicated JSX/logic.
- Flag-on rendering, `handleSubmit`, and the room dual-write are byte-unchanged (`git diff` confirms only the 6 additive lines in the flag-off branch).

## Task Commits

Each task was committed atomically:

1. **Task 1: Render the Room FillSection in the flag-off branch (D-07 parity)** - `07d8a71` (fix)

## Files Created/Modified
- `components/kid/KidDayFillForm.tsx` - Added a `FillSection` wrapping the existing `roomBody` constant as the first item in the flag-off render branch, with `sub` counting checked room tasks.

## Decisions Made
- Placement resolved per review clarification: room section goes at the top of the flag-off-specific block list (inside the `<>` branch), not literally before the flag-agnostic Mood section which now sits outside the ternary. See `key-decisions` above.

## Deviations from Plan

None - plan executed exactly as written. The one placement ambiguity in the plan text ("before the Mood section... i.e. render it near the top of the flag-off block list") was resolved using the plan's own `<read_first>` pointer to `05.6-REVIEW.md`'s CR-01 code sample, which explicitly says "inside the flag-off `<>` branch, before or after Mood as pre-5.6" — this is not a deviation, it's following the plan's own disambiguation.

## Issues Encountered
- `npm run build` initially failed in this worktree with a missing Supabase env var error (`.env.local` does not exist in the worktree checkout, only in the main repo). This is a worktree environment gap, not caused by this change. Temporarily copied `.env.local` from the main repo (gitignored, not committed) to run the build verification, then removed it after. Build and `npx tsc --noEmit` both passed clean with the fix applied.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- CR-01 (Room checklist regression, D-07 flag-off parity) is closed. Flag-off families (the default for the entire existing user base) regain the room checklist UI and correct `roomChecked` persistence, unblocking room coin awards.
- Remaining 05.6 gap-closure plans (07, 08 — CR-02 idempotency and others per `05.6-REVIEW.md`) are unaffected by this change and can proceed independently.
- Manual verification recommended by the plan (load `/kid/day` on a flag-off family, confirm room checklist renders/toggles, save, confirm room coins awarded) was not performed in this automated run — no live DB session available in this worktree. Recommend as a follow-up smoke check before closing the phase.

---
*Phase: 05.6-day-blocks*
*Completed: 2026-07-14*
