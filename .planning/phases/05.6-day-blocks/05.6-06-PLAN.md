---
phase: 05.6-day-blocks
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - components/kid/KidDayFillForm.tsx
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "On a flag-off family (day_blocks_enabled=false, the default), the kid day form renders the Room checklist FillSection so the child can see and toggle room tasks"
    - "Saving a flag-off day no longer silently zeroes room state — the room dual-write in handleSubmit reflects what the kid actually toggled in the UI"
  artifacts:
    - path: "components/kid/KidDayFillForm.tsx"
      provides: "Room FillSection rendered inside the flag-off render branch using the existing roomBody constant"
      contains: "roomBody"
  key_links:
    - from: "components/kid/KidDayFillForm.tsx flag-off <> branch (~line 1079)"
      to: "roomBody constant (defined ~line 619)"
      via: "FillSection wrapping roomBody, rendered unconditionally in the flag-off branch"
      pattern: "roomBody"
---

<objective>
Close CR-01: restore the Room checklist section to the kid day form's flag-off
render branch. Pre-5.6 the room `FillSection` rendered unconditionally; after 5.6
`roomBody` became reachable only via `renderBuiltinBlock('room')`, which fires
only on the flag-on path. Because `families.day_blocks_enabled` defaults `false`
for every existing family, every current kid lost the room UI, and the
unconditional `handleSubmit` re-persists stale all-false `roomChecked` state,
zeroing `days.room_*` and `room_checks` and permanently breaking room coins.

Purpose: D-07 flag-off byte-parity — flag-off families must behave exactly as
pre-5.6. This is a live production regression for the entire existing user base.
Output: The room FillSection renders in the flag-off branch, restoring the
checklist UI and its coin path with no change to flag-on behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.6-day-blocks/05.6-VERIFICATION.md
@.planning/phases/05.6-day-blocks/05.6-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the Room FillSection in the flag-off branch (D-07 parity)</name>
  <read_first>
    - components/kid/KidDayFillForm.tsx — the file being modified. Read: the
      `roomBody` constant definition (~line 619-664), the `renderBuiltinBlock`
      switch case `'room'` (~line 940-945) which shows the exact FillSection
      props already used for room (title `block.name`/`t('kidFillForm.roomSection')`,
      icon `🏠`, sub = filled/total count), and the flag-off `<>` branch
      (~line 1078-1357) where the room section is currently absent.
    - components/DailyModal.tsx — PARITY REFERENCE ONLY (do not modify). Its
      flag-off branch still renders `roomBody` (~line 1179) correctly; the parent
      form does NOT have this bug. Use it to confirm the intended flag-off shape.
  </read_first>
  <action>
    In `components/kid/KidDayFillForm.tsx`, inside the flag-off render branch
    (the `: (` / `<>` alternative of the `dayBlocksEnabled && visibleBlocks.length > 0`
    ternary at ~line 1074-1078), render a `FillSection` that wraps the existing
    `roomBody` constant, exactly as pre-5.6. Use the same props the `'room'` case
    of `renderBuiltinBlock` uses so flag-on and flag-off render identically: title
    from the room locale key (`t('kidFillForm.roomSection')`), icon `🏠`, and
    `sub` = `` `${Object.values(roomChecked).filter(Boolean).length}/${roomTasks.length}` ``.
    Place it at the same position it occupied pre-5.6 — before the Mood section per
    CR-01 — i.e. render it near the top of the flag-off block list, not after
    reading. Do NOT introduce a new `roomBody` copy: reuse the single existing
    constant so flag-on/flag-off share the body (the IN-01 duplication hazard that
    caused this bug). Do NOT touch the flag-on branch, `renderBuiltinBlock`,
    `handleSubmit`, the room dual-write, or `roomBody` itself. Change only the
    flag-off JSX.
  </action>
  <verify>
    <automated>grep -n "roomBody" components/kid/KidDayFillForm.tsx | wc -l | grep -qv '^ *1$' && echo "OK roomBody referenced in 2+ places (def + flag-on + flag-off)" || (echo "FAIL roomBody not rendered in a second place" && exit 1)</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "roomBody" components/kid/KidDayFillForm.tsx` shows the constant
      referenced in at least 3 locations: its definition (~619), the flag-on
      `renderBuiltinBlock('room')` case (~943), and the new flag-off branch usage.
    - The flag-off branch (the `<>` after `: (`) contains a `FillSection` whose
      child is `{roomBody}` and whose `sub` prop counts checked room tasks
      (`Object.values(roomChecked).filter(Boolean).length`).
    - No new inline copy of the room checklist JSX was added (a single `roomBody`
      constant remains the only source of the room button grid + photo-proof UI).
    - `npx tsc --noEmit` exits 0; `npm run build` exits 0.
    - The flag-on branch, `handleSubmit`, and the room dual-write blocks are
      byte-unchanged (diff shows only additive flag-off JSX).
  </acceptance_criteria>
  <done>
    Flag-off families render the room checklist FillSection again; the kid can
    toggle room tasks; on save `roomChecked` reflects real UI state instead of the
    all-false default, so room coins award correctly. Flag-on behavior is
    untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kid browser → form state | The kid toggles room tasks; `roomChecked` drives the dual-write in `handleSubmit`. If the UI cannot express room state, the submit path writes a stale/empty state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.6.06-01 | Tampering (self-inflicted data loss) | `KidDayFillForm.handleSubmit` room dual-write | mitigate | Restore the room FillSection so `roomChecked` reflects real input; the unconditional dual-write then persists true state instead of overwriting saved room rows with all-false. UI-only change; no new money mutation path introduced. |
| T-05.6.06-02 | Denial of Service (feature loss) | flag-off render branch | mitigate | Room checklist and its coin path are restored for the default (flag-off) population; no server or RLS surface touched. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm run build` succeeds.
- Manual (optional, non-blocking): load `/kid/day` on a flag-off family and
  confirm the Room checklist renders and toggles; save and confirm room coins are
  awarded (this fix is UI/logic; the flag-on assembled path is unaffected).
</verification>

<success_criteria>
- Room FillSection renders in the flag-off branch of `KidDayFillForm.tsx`.
- `roomBody` remains a single shared constant (no duplication).
- Type check and build pass.
- Flag-on rendering and `handleSubmit` unchanged.
</success_criteria>

<output>
Create `.planning/phases/05.6-day-blocks/05.6-06-SUMMARY.md` when done.
</output>
