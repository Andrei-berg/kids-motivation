---
phase: 05.6-day-blocks
plan: 04
type: execute
wave: 3
depends_on: ["05.6-01", "05.6-02"]
files_modified:
  - app/kid/day/page.tsx
  - components/kid/KidDayFillForm.tsx
  - components/DailyModal.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "When day_blocks_enabled is on, the kid day form renders the assembled block list from config instead of the hardcoded FillSection list (SC2, D-02)"
    - "On a vacation day the school blocks disappear and schedule-driven blocks (section training, extra lesson) appear only on their scheduled days (SC2, D-08)"
    - "The parent DailyModal renders the identical assembled block list — the two forms never show a different day (D-06)"
    - "Custom blocks render as a native generic 'did it / didn't' toggle and save to day_block_entries; who_fills controls which role can toggle (D-02)"
    - "When the flag is off, both forms render exactly the current hardcoded sections unchanged (D-07 parity)"
  artifacts:
    - path: "components/kid/KidDayFillForm.tsx"
      provides: "flag-on assembled renderer + custom-block toggle + day_block_entries save"
      contains: "assembleDayBlocks"
    - path: "components/DailyModal.tsx"
      provides: "parallel assembled renderer (parent side), same assembly call"
      contains: "assembleDayBlocks"
    - path: "app/kid/day/page.tsx"
      provides: "loads the flag + block list and passes them to KidDayFillForm"
      contains: "getFamilyDayBlocksEnabled"
  key_links:
    - from: "components/kid/KidDayFillForm.tsx + components/DailyModal.tsx"
      to: "lib/day-blocks.ts assembleDayBlocks"
      via: "shared assembly call (no re-implementation)"
      pattern: "assembleDayBlocks"
    - from: "custom-block toggles"
      to: "day_block_entries"
      via: "saveDayBlockEntries repo"
      pattern: "saveDayBlockEntries"
---

<objective>
Render the assembled block list in BOTH day-fill forms behind the feature flag.
Flag-off keeps today's hardcoded forms byte-for-byte; flag-on drives the section
list from day_blocks config filtered by the shared assembleDayBlocks, with custom
blocks rendered as native toggles that save to day_block_entries. Both forms call
the same assembly function so they can never show a different day (D-06).

Purpose: close SC2 — the day is assembled from data, school blocks vanish on
vacation, schedule blocks appear only on their days.
Output: kid page loads flag+blocks; KidDayFillForm and DailyModal render assembled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.6-day-blocks/05.6-CONTEXT.md
@.planning/phases/05.6-day-blocks/05.6-PATTERNS.md

<interfaces>
<!-- From lib/day-blocks.ts (Plan 02): assembleDayBlocks(blocks, dayType, date): DayBlock[] -->
<!-- From lib/repositories/day-blocks.repo.ts (Plan 01):
getDayBlocks(familyId, {childId?, activeOnly?}), getDayBlockEntries(childId, date),
saveDayBlockEntries(childId, date, entries: {blockId, done}[]), getFamilyDayBlocksEnabled(familyId) -->
<!-- KidDayFillFormProps (components/kid/KidDayFillForm.tsx:34-41):
childId, date, fillMode: 1|2|3, dayType: 'school'|'weekend'|'vacation', existingDay, onSaved -->
<!-- FillSection wrapper (lines 59-71) — reuse verbatim for every block for visual parity.
Generic toggle-button pattern (extra-activities block, lines 654-679) — template for custom blocks. -->
<!-- DailyModal (components/DailyModal.tsx): dayTypeInfo state (line 103), day-type recompute effect
(lines 169-172), familyCalendar already in state; props {isOpen,onClose,childId,date,onSave} line 89. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kid day page loads flag + blocks; KidDayFillForm renders assembled list (flag-on)</name>
  <files>app/kid/day/page.tsx, components/kid/KidDayFillForm.tsx</files>
  <read_first>
    - app/kid/day/page.tsx lines 51-120 (the getDayType load-and-pass Promise.all flow + the prop-drilling into KidDayFillForm)
    - components/kid/KidDayFillForm.tsx (WHOLE file — the 7 FillSection blocks lines 586-928, the load Promise.all lines 153-165, the FillSection wrapper lines 59-71, the extra-activities toggle template lines 654-679, isLocked lines 147-150, saveDay flow)
    - lib/day-blocks.ts (assembleDayBlocks — Plan 02)
    - lib/repositories/day-blocks.repo.ts (getDayBlocks/getDayBlockEntries/saveDayBlockEntries/getFamilyDayBlocksEnabled — Plan 01)
  </read_first>
  <action>
    In app/kid/day/page.tsx: extend the existing Promise.all/day-type resolution to also read getFamilyDayBlocksEnabled(familyId) and, when enabled, getDayBlocks(familyId, { childId: resolvedId, activeOnly: true }). Pass two new props to KidDayFillForm: dayBlocksEnabled: boolean and dayBlocks: DayBlock[] (empty when flag-off). Follow the existing prop-drilling convention; do not remove any current prop.

    In components/kid/KidDayFillForm.tsx: add dayBlocksEnabled and dayBlocks to KidDayFillFormProps. Branch the render:
      - Flag OFF (dayBlocksEnabled false OR dayBlocks empty): render the EXISTING 7 FillSection blocks completely unchanged (no refactor) — D-07 parity.
      - Flag ON: compute visible = assembleDayBlocks(dayBlocks, dayType, date) (import from lib/day-blocks). Render one section per visible block, each wrapped in the existing <FillSection title icon> for visual parity, ordered by the assembled order. Built-in blocks (legacy_key non-null) reuse their EXISTING inner JSX (room checklist grid, grade entry, coach rating, reading, home exercises, extra activities) — drive each from the block config's presence/order instead of the hardcoded call site; keep loading room_tasks/subjects/sections/etc. as today for the inner renderers. Custom blocks (legacy_key null) render with the generic toggle-button pattern (copy the extra-activities button markup lines 654-679: height 48, checkmark SVG, T.teal/T.tealSoft on-state) showing name/icon/price and a checkmark when done. Enforce who_fills: on the kid form, a block with who_fills === 'parent' renders read-only/greyed (kid cannot toggle it).
      - On save (flag-on): in addition to the existing saveDay/saveRoomChecks/etc., collect the custom-block toggle states and call saveDayBlockEntries(childId, date, entries) with ALL rendered custom blocks (done true/false) so unchecking persists; then POST /api/wallet/award as today (client still sends only {childId,date}; coins are server-computed).
    Keep isLocked (past-date lock) applied to the assembled toggles too.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "KidDayFillForm|kid/day/page" || echo "tsc-clean"; grep -c "assembleDayBlocks\|saveDayBlockEntries\|dayBlocksEnabled" components/kid/KidDayFillForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - KidDayFillFormProps includes dayBlocksEnabled and dayBlocks; app/kid/day/page.tsx passes both (grep finds getFamilyDayBlocksEnabled in page.tsx).
    - Flag-off path renders the original 7 FillSection blocks unchanged (diff shows they moved into an `if`/`else`, not rewritten).
    - Flag-on path calls assembleDayBlocks and renders custom blocks with the extra-activities toggle markup; who_fills==='parent' blocks are not kid-toggleable.
    - Save path calls saveDayBlockEntries for custom blocks then POSTs /api/wallet/award (no client-side coin credit).
    - `npx tsc --noEmit` reports no new errors in the two files.
  </acceptance_criteria>
  <done>Kid form renders the assembled, day-type/schedule-filtered block list when flag-on; unchanged when flag-off; custom blocks toggle and persist.</done>
</task>

<task type="auto">
  <name>Task 2: DailyModal renders the identical assembled block list (parent side, D-06 parity)</name>
  <files>components/DailyModal.tsx</files>
  <read_first>
    - components/DailyModal.tsx (WHOLE file — dayTypeInfo state line 103, day-type recompute effect lines 169-172, familyCalendar in state, the parallel hardcoded block structure, props line 89)
    - components/kid/KidDayFillForm.tsx (the Task 1 assembled-render implementation — DailyModal must call the SAME assembleDayBlocks + getDayBlocks and produce the same visible set; cross-reference for parity)
    - lib/day-blocks.ts, lib/repositories/day-blocks.repo.ts
  </read_first>
  <action>
    In components/DailyModal.tsx: load the flag + blocks in the same effect that computes dayTypeInfo (reuse the familyCalendar already in state; add getFamilyDayBlocksEnabled + getDayBlocks to the load, keyed off dayTypeInfo.type like the existing recompute effect). Branch the render exactly as KidDayFillForm:
      - Flag OFF: render the current hardcoded parent sections unchanged (D-07 parity).
      - Flag ON: visible = assembleDayBlocks(blocks, dayTypeInfo.type, date) — MUST be the same function and same inputs as the kid form so the two forms never diverge (D-06). Render built-in blocks with their existing parent inner JSX and custom blocks with the generic toggle. On the parent side, who_fills is not a restriction (parents fill anything). Save custom-block toggles via saveDayBlockEntries then trigger the award POST as the modal does today.
    Do NOT re-implement the day-type/schedule filtering locally — import and call assembleDayBlocks (the D-06 structural requirement).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "DailyModal" || echo "tsc-clean"; grep -c "assembleDayBlocks\|saveDayBlockEntries" components/DailyModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DailyModal imports and calls assembleDayBlocks (grep finds it) — it does NOT contain its own day_types/days_of_week filtering loop.
    - Flag-off path renders the original parent sections unchanged.
    - The assembled visible set is produced by the same assembleDayBlocks(blocks, dayType, date) call the kid form uses (same inputs → identical block list, D-06).
    - Custom-block toggles save via saveDayBlockEntries; coins remain server-computed.
    - `npx tsc --noEmit` reports no new errors in DailyModal.tsx.
  </acceptance_criteria>
  <done>Parent modal renders the identical assembled block list via the shared assembler; flag-off unchanged; D-06 parity holds structurally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kid form → who_fills enforcement | UI must not let a kid toggle a parent-only block; RLS/award are the real backstop |
| two forms → shared assembler | divergence would let a block be earnable in one form but hidden in the other |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-056-12 | Elevation | kid toggles a parent-filled block | mitigate | Kid form renders who_fills==='parent' blocks read-only; award route (Plan 03) also skips them for member.role==='child' (defense in depth); day_block_entries writes are parent/extended-only by RLS (Plan 01). |
| T-056-13 | Tampering | forms diverge from award visibility | mitigate | Both forms and the award route call the same assembleDayBlocks — visibility == earnability (D-06/D-08). |
| T-056-14 | Information Disclosure | client sees other families' blocks | accept | getDayBlocks is family-scoped and RLS-filtered; no cross-family exposure. |
</threat_model>

<verification>
- tsc clean; both forms call assembleDayBlocks (no local filter duplication).
- Manual (operator, flag-on family): vacation day hides grade/exercise blocks; a section-training block shows only on its scheduled weekday; kid and parent forms show the same list; a custom block toggles and its coins appear after save.
</verification>

<success_criteria>
SC2 met: the kid day form renders the assembled block list; on a vacation day school blocks disappear and schedule-driven blocks appear only on their scheduled days; the parent form shows the identical list (D-06). Flag-off forms are unchanged (D-07).
</success_criteria>

<output>
Create `.planning/phases/05.6-day-blocks/05.6-04-SUMMARY.md` when done
</output>
