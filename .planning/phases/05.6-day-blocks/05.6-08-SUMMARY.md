---
phase: 05.6-day-blocks
plan: 08
subsystem: db
tags: [rls, supabase, security, wallet, idempotency, gap-closure]

# Dependency graph
requires:
  - phase: 05.6-day-blocks
    provides: "Deterministic (date + block_id) custom_block sourceId in app/api/wallet/award/route.ts (05.6-07)"
provides:
  - "day_block_entries with no client-reachable DELETE (CR-02 defense-in-depth) and INSERT/UPDATE WITH CHECK tying child_id to the row's family_id (WR-01)"
  - "seed_default_day_blocks gated to parent/extended family members (WR-07)"
  - "custom_block wallet_transactions source_id backfilled to the date:block_id natural key so the Plan 07 key-format switch cannot re-award a credited day/block"
  - "verify-day-blocks-award.mjs entry-rotation exploit probe proving CR-02 stays closed at the DB layer"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "RLS WITH CHECK closes cross-family injection by requiring the target child_id to exist in public.children WHERE family_id = <row's family_id>, not just family membership alone"
    - "SECURITY DEFINER config-seeding functions gated by an explicit family_members role IN ('parent','extended') RAISE guard, mirroring the day_blocks write-policy role clause"
    - "Idempotency-key-format migrations require a collision-guarded backfill UPDATE (NOT EXISTS on the target key) alongside the RLS/route fix, or already-credited rows silently become re-awardable once"

key-files:
  created:
    - supabase/migrations/05.6-02-day-block-entries-hardening.sql
  modified:
    - scripts/verify-day-blocks-award.mjs

key-decisions:
  - "Confirmed lib/onboarding-api.ts createFamily() upserts the creator's family_members row (role='parent') before calling seed_default_day_blocks — the WR-07 membership guard does not break onboarding; no re-ordering needed."
  - "Backfill left rows with no day_block_entries join match (entry already deleted) on their old UUID source_id — documented as harmless historical credits given day_blocks_enabled is currently true for only the operator's own family."
  - "Rotation probe inserts a synthetic custom day_block + entry (past test date 1999-12-31) rather than reusing a real family's data, so it is fully self-cleaning and safe to re-run in CI/prod."

patterns-established: []

requirements-completed: []

# Metrics
duration: 25min
completed: 2026-07-14
---

# Phase 05.6 Plan 08: day_block_entries DB hardening + idempotency continuity Summary

**Migration `05.6-02-day-block-entries-hardening.sql` (applied live) drops the day_block_entries DELETE policy, ties INSERT/UPDATE WITH CHECK to child_id↔family_id, gates seed_default_day_blocks to parent/extended members, and backfills already-awarded custom_block source_ids to the new date:block_id natural key; the verify script now exercises the actual entry-rotation exploit path.**

## Performance

- **Duration:** 25 min
- **Started:** 2026-07-14T19:29:00Z (approx, continuing from wave 1 context)
- **Completed:** 2026-07-14T19:53:11Z
- **Tasks:** 3
- **Files modified:** 2 (1 created, 1 modified)

## Accomplishments

- **CR-02 defense-in-depth:** `day_block_entries_delete_own_family` policy dropped and not recreated — no client role (kid/parent/extended) can DELETE entry rows anymore, removing the UUID-rotation primitive at the database layer, independent of the application-layer natural-key fix landed in Plan 07.
- **WR-01 (RLS half):** `day_block_entries_insert_own_family` and `_update_own_family` policies' `WITH CHECK` now require `child_id IN (SELECT id FROM public.children WHERE family_id = day_block_entries.family_id)` in addition to the existing family-membership check, closing the cross-family `child_id` injection half of WR-01 (the read-side half was closed in Plan 07's `family_id` filter on the award route's entries query).
- **WR-07:** `seed_default_day_blocks(p_family_id)` now RAISEs unless the caller is a `parent`/`extended` member of `p_family_id`, closing the any-authenticated-user config-write gap. Verified onboarding order is safe (see Decisions).
- **Idempotency continuity:** a collision-guarded `UPDATE ... FROM day_block_entries` backfill rewrites existing `custom_block` `wallet_transactions.source_id` from the old entry-UUID format to the new `date:block_id` natural key (Plan 07's format), so the key-format switch itself cannot re-award an already-credited day/block. `NOT EXISTS` guard prevents violating the `(child_id, source_type, source_id)` unique index.
- Migration applied live via the documented `pg` one-liner; live check confirms zero rows in `pg_policies` for `day_block_entries_delete_own_family`; re-running the migration is confirmed idempotent (no-op, ran twice).
- `verify-day-blocks-award.mjs` extended with a new self-cleaning probe (synthetic day_block + entry, past test date) that inserts a natural-key award, deletes+re-inserts the entry (new UUID), and asserts the second same-natural-key award insert fails with `23505` — this is the actual double-credit-prevention assertion, not just a literal-duplicate check. Ran twice back-to-back to confirm it stays green.

## Task Commits

Each task was committed atomically:

1. **Task 1: Write the entry-RLS + seed-guard + backfill migration** - `81545df` (feat)
2. **Task 2: Apply the migration to the live database** - verification-only, no new file changes (migration file from Task 1 applied via `pg`; live `pg_policies` check confirmed)
3. **Task 3: Extend the verify script with the entry-rotation exploit probe and run the suite** - `d3e50d5` (test)

**Plan metadata:** (this SUMMARY commit)

## Files Created/Modified

- `supabase/migrations/05.6-02-day-block-entries-hardening.sql` (new) - drops the `day_block_entries` DELETE policy; recreates INSERT/UPDATE policies with `WITH CHECK` tying `child_id` to `family_id`; recreates `seed_default_day_blocks` with a parent/extended membership guard (body otherwise byte-identical to 05.6-01); backfills already-awarded `custom_block` `wallet_transactions.source_id` to the `date:block_id` natural key.
- `scripts/verify-day-blocks-award.mjs` - added a self-cleaning entry-rotation exploit probe (step 4): synthetic `day_blocks`/`day_block_entries` rows, natural-key award insert, delete+reinsert to rotate the entry UUID, and an assertion that a second same-natural-key award is rejected with `23505`.

## Decisions Made

- Confirmed via reading `lib/onboarding-api.ts` `createFamily()` (Step 3 upserts the creator into `family_members` with `role='parent'`, Step 5c later calls `seed_default_day_blocks`) that the WR-07 guard does not break onboarding — no re-ordering was needed, and the migration's header comment documents this verification for future readers.
- Backfill intentionally leaves rows whose underlying `day_block_entries` row was already deleted (no join match) on their old UUID `source_id` rather than attempting to reconstruct a natural key from a now-gone row — documented as harmless historical credits, and the practical volume is near zero since `day_blocks_enabled` is currently true for only one family.
- The rotation probe runs at the service-role level (bypasses RLS by design, since verify scripts always use the service-role key) so it can still simulate the raw delete+reinsert rotation mechanics even after RLS denies DELETE to every client role — this proves the natural key itself (not just RLS) is what keeps idempotency intact, satisfying the plan's "actual exploit path" requirement.

## Deviations from Plan

None - plan executed exactly as written. All four `must_haves.truths` and all `key_links` patterns are present in the applied migration; the verify probe exercises entry rotation as specified.

## Issues Encountered

- Same as Plan 07: `.env.local` is git-ignored and not present in the worktree checkout. All live-DB commands (`pg` migration apply, verify scripts, this plan's verification queries) were run with `--env-file=/Users/andrei/Projects/kids-motivation/.env.local` pointing at the main repo's copy, per the Wave 1 note in this plan's instructions. Not a deviation from the plan — just how the documented commands were executed from within an isolated worktree. `pg` itself resolved from the main repo's hoisted `node_modules` without needing `npm i pg --no-save` in the worktree.

## User Setup Required

None - migration applied to production during this plan; no external service configuration required.

## Full Verification Suite Output (live DB)

- `node --env-file=... scripts/verify-day-blocks-award.mjs` — PASS (schema checks + literal-duplicate check + new entry-rotation probe), run twice, both green (self-cleaning confirmed).
- `node --env-file=... scripts/verify-award-idempotency.mjs` — PASS.
- `node --env-file=... scripts/verify-award-reads.mjs` — PASS.
- `npx tsc --noEmit` — clean, exit 0.
- `npx vitest run tests/day-blocks.test.ts` — 15/15 passed.
- Live `pg_policies` check: `day_block_entries_delete_own_family` count = 0 (DELETE policy confirmed gone in production).
- Migration re-applied a second time to confirm idempotency — no errors, `applied` printed both times.

## Next Phase Readiness

- CR-02 and WR-01/WR-07 are closed at both the application layer (Plan 07) and the database layer (this plan). SC3/D-09 idempotency is now provably un-rotatable (entry-rotation probe) and continuous across the key-format switch (backfill).
- Remaining deferred warnings from the threat model (WR-02 through WR-08 minus WR-01/07, IN-01..07) are explicitly out of scope for this gap-closure plan per the plan's own threat model — none affect the default flag-off population or the money-minting/regression must-haves.

---
*Phase: 05.6-day-blocks*
*Completed: 2026-07-14*
