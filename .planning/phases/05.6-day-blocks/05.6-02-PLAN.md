---
phase: 05.6-day-blocks
plan: 02
type: execute
wave: 2
depends_on: ["05.6-01"]
files_modified:
  - lib/day-blocks.ts
  - lib/day-blocks.test.ts
  - app/api/wallet/_lib.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Given a set of day_blocks + a resolved day type + a date, a single shared pure function returns exactly the blocks visible that day (D-06 shared, D-08 visibility)"
    - "On a vacation day, blocks whose day_types exclude 'vacation' (e.g. grade, exercise) are dropped; schedule-driven blocks appear only on their scheduled weekdays (D-08, SC2)"
    - "The server can read a family's day_blocks_enabled flag to branch award behavior (D-07, D-09)"
  artifacts:
    - path: "lib/day-blocks.ts"
      provides: "assembleDayBlocks(blocks, dayType, date) visibility/ordering + resolveBlockPrice + resolveMultiplier helpers"
      exports: ["assembleDayBlocks", "resolveBlockPrice", "resolveMultiplier"]
    - path: "lib/day-blocks.test.ts"
      provides: "vitest coverage of day-type + schedule-day filtering and price/multiplier resolution"
    - path: "app/api/wallet/_lib.ts"
      provides: "loadFeatureFlag(admin, familyId) server-side flag read"
      exports: ["loadFeatureFlag"]
  key_links:
    - from: "lib/day-blocks.ts assembleDayBlocks"
      to: "day_types + days_of_week filtering"
      via: "reuse of getActivitiesForDay / getSectionsForDate semantics"
      pattern: "day_types|days_of_week"
    - from: "app/api/wallet/_lib.ts loadFeatureFlag"
      to: "families.day_blocks_enabled"
      via: "admin service client select"
      pattern: "day_blocks_enabled"
---

<objective>
Build the single shared block-assembly contract that BOTH day-fill forms and the
award route use to decide which blocks are visible on a given day, plus the price
and multiplier resolution helpers, plus the server-side feature-flag loader. This
is the interface-first plan: it defines the pure logic downstream plans (03 award,
04 forms) call so neither re-implements filtering (the D-06 parity guarantee).

Purpose: one source of truth for block visibility (D-08) and price/multiplier
math (D-04, D-05); a server-side flag read for the D-09 award branch.
Output: lib/day-blocks.ts (tested) + loadFeatureFlag in _lib.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.6-day-blocks/05.6-CONTEXT.md
@.planning/phases/05.6-day-blocks/05.6-PATTERNS.md

<interfaces>
<!-- From lib/models/day-block.types.ts (created in Plan 01) -->
DayBlock: { id, family_id, child_id: string|null, legacy_key: 'room'|'behavior'|'grade'|'sport'|'activity'|'book'|'exercise'|null, name, icon, price: number|null, day_types: string[], days_of_week: number[], schedule_link: string|null, who_fills: 'kid'|'parent'|'both', multipliers: Record<string, number>, sort_order, is_active, created_at }

<!-- From lib/day-type.ts -->
getDayType(date, isSick, vacationPeriods, childId?, t?, familyCalendar?) → { type: 'school'|'weekend'|'vacation'|'sick', label, emoji }

<!-- getActivitiesfor-day day_types convention (lib/repositories/expenses.repo.ts:584-609):
empty array = all days; 'always' sentinel = all days; else must include currentDayType.
JS day → system day: jsDay===0 ? 6 : jsDay-1 (0=Mon..6=Sun). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: assembleDayBlocks visibility/ordering + price/multiplier resolution (pure, tested)</name>
  <files>lib/day-blocks.ts, lib/day-blocks.test.ts</files>
  <read_first>
    - lib/repositories/expenses.repo.ts lines 529-609 (getSectionsForDate DAY_KEYS/dayOfWeek matching + getActivitiesForDay day_types filter — the exact semantics to reuse)
    - lib/day-type.ts (getDayType output shape; do NOT re-derive day type inside the assembler — accept the resolved type as input)
    - lib/models/day-block.types.ts (DayBlock shape — created in Plan 01)
    - app/api/wallet/_lib.ts SETTINGS_DEFAULTS (the wallet_settings keys the price fallback maps to per legacy_key)
  </read_first>
  <behavior>
    - assembleDayBlocks(blocks, dayType, date): returns is_active blocks sorted by sort_order, filtered so that (a) a block with empty day_types OR day_types containing 'always' shows on any day; (b) otherwise the block shows only if day_types includes dayType; (c) a block with non-empty days_of_week shows only when the date's system weekday (jsDay===0?6:jsDay-1) is included. Test: grade block (day_types ['school']) hidden when dayType='vacation', shown when 'school'. Test: a block with days_of_week [1,3] (Tue/Thu) shown only on those weekdays. Test: room block (day_types []) shown on every day type. Test: output preserves sort_order ascending.
    - resolveBlockPrice(block, settings): returns block.price when non-null, else the wallet_settings fallback for the block's legacy_key (room→coins_per_room_task, behavior→coins_per_good_behavior, book→coins_per_book, exercise→coins_per_exercise; grade/sport/activity have per-value pricing so return null/0 to signal "use the existing per-row computation" — document this). Test: NULL price + legacy_key 'room' → settings.coins_per_room_task; explicit price 7 → 7.
    - resolveMultiplier(block, dayType): returns block.multipliers[dayType] when present and > 0, else 1. Test: multipliers {vacation: 2} with dayType 'vacation' → 2; missing key → 1; dayType 'school' → 1.
  </behavior>
  <action>
    Create lib/day-blocks.ts (pure, no Supabase imports) exporting assembleDayBlocks(blocks: DayBlock[], dayType: string, date: string): DayBlock[], resolveBlockPrice(block: DayBlock, settings: Record<string, number>): number|null, resolveMultiplier(block: DayBlock, dayType: string): number. Reuse the getActivitiesForDay day_types + 'always' + empty-means-all convention and the getSectionsForDate weekday math (system weekday = jsDay===0?6:jsDay-1) verbatim — do NOT invent new filter semantics. Treat 'sick' dayType like the forms do today (callers pass the resolved getDayType type; the assembler just filters on the string). Create lib/day-blocks.test.ts (vitest) covering every case in <behavior>. Run `npm test -- day-blocks` — all green before finishing.
  </action>
  <verify>
    <automated>npm test -- day-blocks 2>&1 | grep -E "passed|PASS|✓" | head</automated>
  </verify>
  <acceptance_criteria>
    - lib/day-blocks.ts exports assembleDayBlocks, resolveBlockPrice, resolveMultiplier and imports nothing from lib/supabase.
    - lib/day-blocks.test.ts exists; `npm test -- day-blocks` passes with tests covering: grade-hidden-on-vacation, days_of_week filter, empty-day_types-shows-always, sort_order preserved, price override vs fallback, multiplier present/missing.
    - assembleDayBlocks output for a vacation day excludes any block whose day_types = ['school'].
  </acceptance_criteria>
  <done>Pure, tested assembly + price + multiplier helpers exist; the D-08 visibility rule and D-04/D-05 resolution live in one shared module.</done>
</task>

<task type="auto">
  <name>Task 2: Server-side feature-flag loader in wallet _lib</name>
  <files>app/api/wallet/_lib.ts</files>
  <read_first>
    - app/api/wallet/_lib.ts (loadSettings pattern: admin.from(...).eq('family_id').maybeSingle() with a defaults merge — mirror this exact shape)
    - supabase/migrations/05.6-01-day-blocks.sql (families.day_blocks_enabled column — created in Plan 01)
  </read_first>
  <action>
    Add exported async function loadFeatureFlag(admin: Admin, familyId: string): Promise<boolean> to app/api/wallet/_lib.ts, following the loadSettings shape: select day_blocks_enabled from families where id = familyId via the admin client, maybeSingle(), return (data?.day_blocks_enabled ?? false). Default false so a missing/legacy row is treated as flag-off (D-09 byte-parity). Do not modify creditAwards, AwardIntent, or loadSettings.
  </action>
  <verify>
    <automated>grep -n "export async function loadFeatureFlag" app/api/wallet/_lib.ts && npx tsc --noEmit 2>&1 | grep -E "_lib" || echo "ok"</automated>
  </verify>
  <acceptance_criteria>
    - app/api/wallet/_lib.ts exports loadFeatureFlag(admin, familyId) returning a boolean, default false.
    - It reads families.day_blocks_enabled via the admin client (grep finds `day_blocks_enabled`).
    - `npx tsc --noEmit` reports no new errors in _lib.ts.
    - creditAwards / AwardIntent / loadSettings signatures are unchanged.
  </acceptance_criteria>
  <done>loadFeatureFlag exported; award route (Plan 03) can branch on it server-side.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared assembly logic → forms + award | if forms and award used different filtering, a block could be earnable while hidden (or vice-versa) |
| flag read → award branch | the server, not the client, decides flag-on/off |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-056-05 | Spoofing | flag source | mitigate | loadFeatureFlag reads families.day_blocks_enabled server-side via the admin client; the client-supplied request body cannot set the flag. |
| T-056-06 | Tampering | divergent filter logic | mitigate | Single assembleDayBlocks module consumed by both forms and award; unit-tested so visibility == earnability (closes the D-06 drift risk). |
</threat_model>

<verification>
- `npm test -- day-blocks` green; tsc clean.
- assembleDayBlocks drops school-only blocks on vacation and respects days_of_week.
- loadFeatureFlag returns false for a family with the default flag.
</verification>

<success_criteria>
The shared visibility + price + multiplier contract (D-04, D-05, D-08) and the server flag read (D-07, D-09) exist and are unit-tested, ready for the award route and both forms to consume without re-implementation (D-06).
</success_criteria>

<output>
Create `.planning/phases/05.6-day-blocks/05.6-02-SUMMARY.md` when done
</output>
