---
phase: 05.6-day-blocks
plan: 05
type: execute
wave: 2
depends_on: ["05.6-01"]
files_modified:
  - components/settings/DayBlocksManager.tsx
  - components/parent-center/screens/Settings.tsx
  - public/locales/en.json
  - public/locales/ru.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "A parent can, in Parent Center → Settings, see the family's day blocks and toggle each block enabled/disabled (SC1 enabled flag)"
    - "A parent can create a first-class custom block with a name, icon, and coin price (D-02, D-03)"
    - "The 3 seeded 'previously-free' blocks (home help, weekend homework, extra reading) appear here as ordinary editable/deletable custom rows so a parent can re-price or disable them (D-03)"
    - "A parent can set a built-in block's price override and its day-type multiplier at a minimal level (D-04, D-05)"
    - "Built-in (legacy) blocks cannot be hard-deleted, only deactivated — the DB guard surfaces a 'deactivate instead' message (D-02 built-ins preserved)"
    - "The manager writes via the repo under parent-only RLS; a kid role has no write path (D-07 backstop)"
  artifacts:
    - path: "components/settings/DayBlocksManager.tsx"
      provides: "minimal per-family day_blocks editor (list, toggle, price, multiplier, add/delete custom, reorder)"
      contains: "getDayBlocks"
    - path: "components/parent-center/screens/Settings.tsx"
      provides: "a 'blocks' sub-tab mounting DayBlocksManager"
      contains: "DayBlocksManager"
  key_links:
    - from: "components/settings/DayBlocksManager.tsx"
      to: "lib/repositories/day-blocks.repo.ts CRUD"
      via: "addDayBlock/updateDayBlock/setDayBlockActive/reorderDayBlocks/deleteDayBlock"
      pattern: "addDayBlock|setDayBlockActive"
    - from: "components/parent-center/screens/Settings.tsx SUB_TABS"
      to: "DayBlocksManager"
      via: "new 'blocks' sub-tab render"
      pattern: "sub === 'blocks'"
---

<objective>
Ship the minimal parent management surface needed to configure day blocks at all
in 5.6 (the full Day Constructor is Phase 5.8): list the family's blocks, toggle
enabled, set a built-in's price/multiplier, and create/delete first-class custom
blocks. Mirrors the existing RoomTasksManager settings editor exactly.

Purpose: without this, D-02's "a parent can create an arbitrary block", D-03's
seeded "previously-free" blocks becoming editable, and D-04/D-05 per-block
price/multiplier are unreachable in 5.6. Keep it minimal — rich editing is 5.8.
Output: DayBlocksManager component + a Settings sub-tab + i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.6-day-blocks/05.6-CONTEXT.md
@.planning/phases/05.6-day-blocks/05.6-PATTERNS.md

<interfaces>
<!-- From lib/repositories/day-blocks.repo.ts (Plan 01):
getDayBlocks(familyId, {childId?, activeOnly?}), addDayBlock({familyId, legacyKey:null, name, icon?, price?, dayTypes?, whoFills?, sortOrder?}),
updateDayBlock(id, fields), setDayBlockActive(id, isActive), reorderDayBlocks(orderedIds), deleteDayBlock(id) -->
<!-- DayBlock type: legacy_key non-null = built-in (undeletable), null = custom (deletable). The 3 seeded
"previously-free" blocks (home_help/weekend_homework/reading_extra) have legacy_key null → they list as custom rows. -->
<!-- RoomTasksManager (components/settings/RoomTasksManager.tsx): default export, useAppStore() familyId,
add/rename/toggle/reorder/delete, systemTask lock UI (line 234), deleteConfirm (line 117) — the exact analog. -->
<!-- Settings mount (components/parent-center/screens/Settings.tsx): SUB_TABS array ~line 375, render `{sub === 'room' && <RoomTasksManager/>}` line 410. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DayBlocksManager component (list, toggle, price, multiplier, add/delete custom)</name>
  <files>components/settings/DayBlocksManager.tsx, public/locales/en.json, public/locales/ru.json</files>
  <read_first>
    - components/settings/RoomTasksManager.tsx (WHOLE file — the exact editor shape: useAppStore() familyId, load list, add input, rename, toggle active, reorder, delete with confirm + systemTask lock UI, i18n key usage under settings.roomTasksManager.*)
    - lib/repositories/day-blocks.repo.ts (the CRUD functions to call — Plan 01)
    - lib/models/day-block.types.ts (DayBlock shape, legacy_key non-null = built-in)
    - public/locales/en.json and public/locales/ru.json (the settings.roomTasksManager.* block to mirror for settings.dayBlocksManager.*)
  </read_first>
  <action>
    Create components/settings/DayBlocksManager.tsx (default export, 'use client'), mirroring RoomTasksManager. Resolve familyId via useAppStore(). Load getDayBlocks(familyId, { activeOnly: false }) so disabled blocks are still shown for re-enabling. For each block render a row showing icon + name + a coin-price input + an is_active toggle. Behavior:
      - Built-in blocks (legacy_key non-null): show a lock/system badge (reuse the RoomTasksManager systemTask lock UI); allow editing price (updateDayBlock(id, { price })) and a minimal multiplier control — a single "vacation ×" number input that writes updateDayBlock(id, { multipliers: { ...existing, vacation: value } }) (minimal per D-05; rich multi-day-type editing is 5.8). Allow toggle active (setDayBlockActive). Do NOT allow delete — deleteDayBlock will throw the DB guard error; catch it and show settings.dayBlocksManager.cannotDeleteBuiltin.
      - Custom blocks (legacy_key null): full edit — rename (updateDayBlock name), icon, price, who_fills select (kid/parent/both), day_types minimal selector (checkboxes school/weekend/vacation, empty = all), toggle active, and delete (with confirm) via deleteDayBlock. NOTE: the 3 seeded "previously-free" blocks (home_help / weekend_homework / reading_extra, seeded in Plan 01 with legacy_key null) surface here as ordinary custom rows — a parent can re-price, re-name, disable, or delete them just like any custom block (this is how D-03's blocks are configured in 5.6).
      - Add-custom form: name + icon + price inputs → addDayBlock({ familyId, legacyKey: null, name, icon, price, whoFills: 'both', sortOrder: <next> }).
      - Reorder: up/down buttons calling reorderDayBlocks(orderedIds) (loop pattern, like RoomTasksManager).
    All labels via t('settings.dayBlocksManager.*'). Do NOT hardcode coin values or Russian strings inline — use i18n keys (custom block names are user data, stored as-is).

    Add a settings.dayBlocksManager.* i18n block to BOTH public/locales/en.json and public/locales/ru.json, mirroring the existing settings.roomTasksManager.* keys: title, subtitle, addPlaceholder/namePlaceholder, pricePlaceholder, empty, systemBlock, cannotDeleteBuiltin, deleteConfirm, whoFills (kid/parent/both labels), dayTypes (school/weekend/vacation labels), vacationMultiplier. Keep en.json and ru.json key sets identical (parity — a missing key in one locale is a defect per the 04.3 i18n convention).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "DayBlocksManager" || echo "tsc-clean"; node -e "const a=Object.keys(require('./public/locales/en.json').settings?.dayBlocksManager||{}).sort();const b=Object.keys(require('./public/locales/ru.json').settings?.dayBlocksManager||{}).sort();console.log('en',a.length,'ru',b.length,'match',JSON.stringify(a)===JSON.stringify(b))"</automated>
  </verify>
  <acceptance_criteria>
    - components/settings/DayBlocksManager.tsx exists (default export) and calls getDayBlocks + at least addDayBlock, updateDayBlock, setDayBlockActive, deleteDayBlock.
    - Built-in rows show a system/lock badge and have no delete affordance; custom rows (incl. the 3 seeded "previously-free" blocks) have delete + who_fills + day_types controls.
    - deleteDayBlock on a built-in is wrapped in try/catch surfacing settings.dayBlocksManager.cannotDeleteBuiltin.
    - The node parity check prints `match true` (en.json and ru.json dayBlocksManager key sets identical and non-empty).
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>Minimal per-family day-blocks editor exists: list, toggle, price + vacation multiplier, create/delete custom (incl. re-pricing the 3 seeded D-03 blocks), built-in delete-guarded; i18n complete in both locales.</done>
</task>

<task type="auto">
  <name>Task 2: Mount DayBlocksManager as a Settings sub-tab</name>
  <files>components/parent-center/screens/Settings.tsx</files>
  <read_first>
    - components/parent-center/screens/Settings.tsx (SUB_TABS array ~line 375 and the `{sub === 'room' && <RoomTasksManager/>}` render at line 410 — the exact mount pattern to mirror)
    - public/locales/en.json / ru.json (settings.tabs.* keys for the tab label)
  </read_first>
  <action>
    In components/parent-center/screens/Settings.tsx: import DayBlocksManager from '@/components/settings/DayBlocksManager'. Add a SUB_TABS entry { id: 'blocks', icon: '🧩', label: t('settings.tabs.blocks') } (place near 'room'/'activities'). Add the render branch `{sub === 'blocks' && <DayBlocksManager/>}` alongside the existing room branch. Add settings.tabs.blocks to both public/locales/en.json and ru.json ("Day Blocks" / "Блоки дня").
  </action>
  <verify>
    <automated>grep -n "DayBlocksManager\|sub === 'blocks'\|id: 'blocks'" components/parent-center/screens/Settings.tsx && npx tsc --noEmit 2>&1 | grep -E "Settings.tsx" || echo "ok"</automated>
  </verify>
  <acceptance_criteria>
    - Settings.tsx imports DayBlocksManager, has a 'blocks' SUB_TABS entry, and renders `{sub === 'blocks' && <DayBlocksManager/>}`.
    - settings.tabs.blocks exists in both en.json and ru.json.
    - `npx tsc --noEmit` reports no new errors in Settings.tsx.
  </acceptance_criteria>
  <done>Day Blocks editor reachable from Parent Center → Settings via a new sub-tab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manager writes → day_blocks | prices/config feed money; only parents may write |
| kid role → this screen | Settings is parent-guarded; RLS is the real backstop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-056-15 | Elevation | kid edits block prices | mitigate | DayBlocksManager writes go through the browser client under parent-only RLS on day_blocks (Plan 01); Parent Center is middleware parent-guarded; a kid session's config writes are DB-denied even if the UI were reached. |
| T-056-16 | Tampering | hard-delete of a built-in block breaks the award mapping | mitigate | DB legacy-delete guard (Plan 01) rejects built-in deletes; UI hides the delete affordance and surfaces "deactivate instead". Custom rows (incl. seeded D-03 blocks) delete freely — they carry no legacy_key mapping. |
| T-056-17 | Denial of Service | negative/absurd price or multiplier entered | mitigate | Award route (Plan 03) clamps every block coin; a bad config cannot mint unbounded coins even if entered here. |
</threat_model>

<verification>
- tsc clean; i18n parity check prints match true.
- Manual (operator): a parent can add a custom block, toggle a built-in off/on, set a price + vacation multiplier, and re-price/disable a seeded "previously-free" block; deleting a built-in shows the "deactivate instead" message.
</verification>

<success_criteria>
The minimal management surface required to configure blocks at all in 5.6 exists (D-02 custom creation, D-03 seeded blocks editable/re-priceable, D-04 price, D-05 minimal multiplier, SC1 enabled toggle) — full Day Constructor remains Phase 5.8.
</success_criteria>

<output>
Create `.planning/phases/05.6-day-blocks/05.6-05-SUMMARY.md` when done
</output>
</output>
