---
phase: 04.1-pwa
plan: "01"
subsystem: ui
tags: [pwa, manifest, service-worker, ios, android, install-prompt, meta-tags]

# Dependency graph
requires: []
provides:
  - PWA manifest with 3 icon entries (192 maskable, 512 any, 512 maskable)
  - Apple PWA meta tags in layout head (apple-mobile-web-app-capable, statusBarStyle)
  - InstallPrompt component for Android (beforeinstallprompt) and iOS (Share sheet guidance)
  - Viewport export with themeColor
affects: [04.1-02, 04.1-03]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "PWA install prompt: detect platform (iOS vs Android) in useEffect, show platform-specific banner"
    - "SSR safety: all window/navigator checks inside useEffect"
    - "Permanent dismiss: localStorage keys pwa_install_dismissed / pwa_ios_dismissed"

key-files:
  created:
    - components/InstallPrompt.tsx
  modified:
    - app/layout.tsx
    - app/manifest.ts

key-decisions:
  - "Used Next.js Metadata API appleWebApp field for Apple meta tags (not manual <head> tags)"
  - "InstallPrompt uses inline styles for reliability across all page contexts"
  - "iOS banner triggers on any iOS + non-standalone, regardless of browser — simpler and covers all cases"

patterns-established:
  - "InstallPrompt pattern: check standalone mode first, then iOS, then Android via event listener"

requirements-completed: [PWA-01]

# Metrics
duration: 2min
completed: 2026-04-26
---

# Phase 4.1 Plan 01: PWA Installability Summary

**Apple meta tags, 3-icon manifest, and dismissible Android/iOS install banners enabling standalone PWA installation**

## Performance

- **Duration:** 2 min
- **Started:** 2026-04-26T17:59:44Z
- **Completed:** 2026-04-26T18:01:50Z
- **Tasks:** 2
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments
- Added Apple PWA meta tags via Next.js Metadata API (apple-mobile-web-app-capable, statusBarStyle: black-translucent)
- Updated manifest.ts with third icon entry (512px maskable) so both iOS and Android adaptive icons work correctly
- Created InstallPrompt component: Android shows native install banner via beforeinstallprompt, iOS shows Share sheet guidance
- Both banners are permanently dismissible via localStorage and hidden when already in standalone mode

## Task Commits

Each task was committed atomically:

1. **Task 1: Apple meta tags in layout + manifest icon fix** - `0fada5a` (feat)
2. **Task 2: InstallPrompt component for Android + iOS** - `f9c4428` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `components/InstallPrompt.tsx` - Platform-detecting install banner (Android + iOS), SSR-safe, dismissible
- `app/layout.tsx` - Added Apple PWA metadata, Viewport export, InstallPrompt mount
- `app/manifest.ts` - Added third icon entry for 512px maskable purpose

## Decisions Made
- Used Next.js `Metadata` API `appleWebApp` field rather than manual `<head>` meta tags — stays idiomatic with App Router
- InstallPrompt styled with inline styles for zero dependency on Tailwind (layout renders before CSS hydration)
- iOS detection kept simple: `/iPad|iPhone|iPod/.test(navigator.userAgent)` — matches all iOS Safari variants

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Plan 02 (OfflineBanner + service worker caching) can now proceed — it requires this plan's layout.tsx updates
- Plan 03 (push notification opt-in) also builds on the standalone install foundation established here
- InstallPrompt and Apple meta tags are live on next deploy to Vercel

---
*Phase: 04.1-pwa*
*Completed: 2026-04-26*
