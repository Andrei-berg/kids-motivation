---
phase: 09.6-boost-styles
plan: 05
type: execute
wave: 5
depends_on: [09.6-01, 09.6-02, 09.6-03, 09.6-04]
files_modified:
  - project-docs/data-model.md
  - project-docs/system-architecture.md
autonomous: false
requirements: [BOOST-04, BOOST-05]

must_haves:
  truths:
    - "Every gate (typecheck, lint, tests, build) is green on the finished phase"
    - "Every phase decision D-00..D-16 has traceable code evidence"
    - "BOOST-04 and BOOST-05 each have named code evidence, closing out the two Pending rows in REQUIREMENTS.md's phase-mapping table"
    - "project-docs documents both new boost-detail styles and the completed 3-way picker"
    - "A human has confirmed both new styles in a real browser, including their penalty states, popover interaction and reduced motion"
  artifacts:
    - path: "project-docs/system-architecture.md"
      provides: "quest-checklist + ring-badges detail bodies documented beside segmented-bar"
      contains: "QuestChecklistBody"
  key_links:
    - from: ".planning/phases/09.6-boost-styles/09.6-05-SUMMARY.md"
      to: "code evidence per decision"
      via: "D-00..D-16 traceability matrix"
      pattern: "D-16"
---

<objective>
Close phase 9.6: prove every gate is green, trace every decision and requirement to concrete code,
document the two new boost-detail styles and the completed picker in `project-docs`, and get the
operator's browser sign-off.

Purpose: this phase's success criteria are interaction- and data-accuracy-shaped (every condition is
its own row/badge with a real threshold; a popover explains a real next-tier gap), so automated gates
prove the wiring and a human proves the feel. This mirrors the phase-close pattern already used at
[Phase 09.3-06], [Phase 09.4-05] and [Phase 09.5-05], recorded in STATE.md specifically because a
ROADMAP "Complete" marker has previously not matched reality in this project (the Phase 1.3 migration
incident) — this phase adds no migration, but the same "don't trust a green build alone" discipline
applies to the copy/data-binding surface here.
Output: green gates, a complete D-00..D-16 traceability matrix, a BOOST-04/BOOST-05 requirements
matrix, corrected docs, and an operator-approved phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09.6-boost-styles/09.6-CONTEXT.md
@.planning/phases/09.6-boost-styles/09.6-UI-SPEC.md
@.planning/phases/09.6-boost-styles/09.6-01-SUMMARY.md
@.planning/phases/09.6-boost-styles/09.6-02-SUMMARY.md
@.planning/phases/09.6-boost-styles/09.6-03-SUMMARY.md
@.planning/phases/09.6-boost-styles/09.6-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate sweep, traceability matrix, project-docs correction</name>
  <files>project-docs/data-model.md, project-docs/system-architecture.md</files>
  <read_first>
    - project-docs/system-architecture.md, the section documenting `fill_style`/`KidDayFillForm`
      and the section Phase 9.5 added for `boost_style`/`BoostDetailSheet`/segmented-bar (the exact
      wording/placement style this task's addition mirrors)
    - project-docs/data-model.md, the `children.boost_style` paragraph Phase 9.5 added (the
      three-CHECK-values wording this task updates from "only segmented-bar selectable" to "all
      three selectable")
    - .planning/phases/09.6-boost-styles/09.6-CONTEXT.md (the full D-00..D-16 decision list)
    - .planning/REQUIREMENTS.md lines 44-62 and 100-110 (BOOST-04/BOOST-05 wording and the phase
      mapping table whose status column this phase's requirements move out of `Pending`)
    - the four SUMMARY files from plans 01-04 (the as-built record the matrix cites)
  </read_first>
  <action>
    Run the full gate sweep and record each exit code in the summary: `npx tsc --noEmit`,
    `npm run lint`, `npm test`, `npm run build`.

    Build a D-00..D-16 traceability matrix in the summary with one row per decision: decision ID,
    one-line restatement, the file(s) and symbol(s) implementing it, and the grep or acceptance
    criterion that proves it. Cover at minimum: D-00 (row/badge/popover copy ordering — title before
    coin amount, `K.ink3`/secondary-position reward styling), D-01/D-02 (3+3 independent quest rows),
    D-03 (superseded-still-checked, `tier.reached` independent booleans), D-04 (`questNote` +
    `v.total` as the real, non-summed total), D-06 (3 grade-only tier badges), D-07 (week-dot strip +
    reused streak chip, not overloaded), D-08 (`week.total / week.max` ring fill, no blended metric),
    D-09 (`nextTierIndex` sky highlight), D-10 (`openTierId` single-state toggle), D-11 (transparent
    backdrop `onClick` scoped to the sheet body, distinct from `BottomSheet`'s own close rule), D-12
    (3-variant popover copy, penalized reuses not-yet-reached), D-13 (no shared wrapper forced across
    styles — `SegmentedBarBody`/`QuestChecklistBody`/`RingBadgesBody` stay visually distinct), D-14
    (identical `BottomSheet` title/icon across all 3), D-15 (danger treatment in both new styles'
    grade rows/badges), D-16 (`K` token reuse only, grep for any literal hex in the new bodies).

    Add a second matrix mapping BOOST-04 and BOOST-05 to named code evidence (component + test/grep),
    and confirm BOOST-06 (already Phase 9.5 scope) still holds for both new styles — cite the
    `lib/kid/boost-detail.ts` no-sketch-literal grep from plan 09.6-01/02.

    In `project-docs/data-model.md`, update the existing `children.boost_style` paragraph to state
    that all three values are now selectable from the kid's own `ProfileSheet` (previously only
    `segmented-bar` was reachable pending Phase 9.6).

    In `project-docs/system-architecture.md`, extend the existing `boost_style`/`BoostDetailSheet`
    section with the two new bodies: `QuestChecklistBody` (6 independent condition rows + combined
    total, reading `BoostDetailView.grades.tiers`/`consistency.streak2Done`/`streak3Done`) and
    `RingBadgesBody` (ring + 3 tier badges + single-open popover + week-dot strip, reading
    `grades.nextTierIndex`/`consistency.filledDayFlags`) — state again that every threshold/amount
    traces to `lib/kid/boost-rules.ts`, never a literal in either new view.

    Do not edit `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` or `.planning/STATE.md` in this
    task — the phase-close workflow owns those updates after the operator approves in task 2.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All four commands above exit 0, each exit status recorded in the summary.
    - `grep -c "boost_style" project-docs/data-model.md` returns at least 2.
    - `grep -c "QuestChecklistBody" project-docs/system-architecture.md` returns at least 1 and
      `grep -c "RingBadgesBody" project-docs/system-architecture.md` returns at least 1.
    - The summary contains a table with one row per decision covering all 16 of D-00 through D-16
      (skipping D-05, which is a Phase 9.5 decision extended, not re-declared, by D-15 — note this
      explicitly rather than silently omitting it), each citing a file path and a proving grep/test.
    - The summary contains a second table covering BOOST-04 and BOOST-05, and confirms BOOST-06
      still holds for both new styles.
    - `git status --porcelain .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md`
      returns empty for this task.
  </acceptance_criteria>
  <done>All gates green, both matrices written, project-docs updated for both new styles and the completed picker.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator browser sign-off</name>
  <what-built>
    Two new weekly-boost detail-view styles — `quest-checklist` (a plain list of every boost
    condition as its own row with its own reward, plus one combined total) and `ring-badges` (a
    ring for the current total plus 3 tappable grade-tier badges opening a popover) — plus a
    completed 3-way `boost_style` picker in the kid profile sheet. Every threshold and coin amount is
    read live from `lib/kid/boost-rules.ts`; the inline `BoostMeter` itself is unchanged.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and sign in as a child (or as a parent with `?preview=true`).

    1. Open the profile sheet (tap the avatar in the kid header). Scroll to "Стиль буста недели" —
       confirm it now shows 3 options ("Полосы" / "Квесты" / "Кольцо"), not 1.
    2. Tap "Квесты". No error appears below the picker. Reload the page and reopen the profile sheet
       — "Квесты" is still shown active.
    3. Tap the boost meter (in the profile sheet, or on `/kid/day`). Confirm the sheet now shows a
       plain list of 6 rows: 3 grade-tier rows (with a checkmark or fraction, a title, and a smaller
       "+N" reward on the right) and 3 consistency rows (full week / streak×2 / streak×3), followed
       by a note about the best tier not being summed, then a "Всего за неделю" total row matching
       the number on the inline meter, then a progress bar.
    4. Confirm every row's title (what was done/needed) reads BEFORE its coin amount, and the coin
       amount is visually smaller/secondary — never the first or biggest thing on the row.
    5. Tap "Кольцо" in the profile sheet, then reopen the boost meter. Confirm a ring appears with
       the same "+N" total as before, 3 tier badges (🥉🥈🥇) below it, and a 7-dot week strip plus a
       streak chip beneath that.
    6. Tap the middle tier badge. A dark popover appears above it with a headline (what's needed or
       what was earned) and a smaller coin line beneath. Tap the third badge — the first popover
       closes and the third one's popover opens. Tap anywhere else in the sheet (the ring, the
       week-dot area) — the open popover closes.
    7. Confirm exactly one badge (the next unreached one, if any) has a visibly different highlight
       (a blue outline/glow) from the plain reached (orange) and not-reached (grey) badges.
    8. Penalty state: as a parent, enter a bad grade (a "2") for this child on any day of the current
       week. Reopen the kid detail sheet in "Квесты" style — all 3 grade rows should render in a
       red-tinted panel with a ⚠, and the shared "Одна двойка обнулила..." hint appears once beneath
       the group, not on every row. Switch to "Кольцо" style — all 3 grade badges show the same
       red-tinted ⚠ treatment, and tapping one shows the not-yet-reached popover copy (not a claim of
       having earned the tier). Remove the grade afterwards.
    9. Reduced motion: enable macOS System Settings → Accessibility → Display → Reduce motion, then
       reopen both new styles — no sweeping/pulsing animation anywhere, everything still legible and
       fully rendered in its final state including the tier-badge popover (appears instantly, no
       fade).
    10. Switch the app language to English and reopen both new styles — every string is English,
        including the note, the popover copy and the two new picker option labels ("Quests"/"Ring").
    11. Confirm no number anywhere in either new style is 150, 300, 500, 3, 5, or 7 unless those are
        genuinely this family's configured `wallet_settings` boost amounts/thresholds.
    12. Widen the window past 1024px (desktop layout) and repeat step 3 and step 5 from the Day hero
        — both new styles render correctly there too.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what you saw that differs from the steps above.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → future implementers | `project-docs` is the stated source of truth; a wrong statement there becomes a future defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-096-10 | Repudiation | undocumented new detail-view bodies | mitigate | `project-docs/data-model.md` and `project-docs/system-architecture.md` are updated in the same plan that closes the phase, with grep-asserted acceptance criteria. |
| T-096-SC | Tampering | npm/pip/cargo installs | accept | No dependency added or upgraded across the whole phase. |
</threat_model>

<verification>
1. All four automated commands in task 1 exit 0, each recorded in the summary.
2. The D-00..D-16 matrix has one row per decision (16 rows, with D-05 explicitly noted as a Phase
   9.5 decision extended by D-15), each with a file path and a proving grep or test.
3. The requirements matrix covers BOOST-04 and BOOST-05 and reconfirms BOOST-06.
4. The operator has typed "approved" for the 12-step browser checklist.
</verification>

<success_criteria>
- Typecheck, lint, tests and build are all green on the finished phase.
- Every decision D-00 through D-16 and every requirement BOOST-04/BOOST-05 traces to named code
  evidence.
- `project-docs` documents both new styles and the completed picker.
- Operator sign-off recorded, including both penalty states, the popover interaction and reduced
  motion.
</success_criteria>

<output>
Create `.planning/phases/09.6-boost-styles/09.6-05-SUMMARY.md` when done
</output>
