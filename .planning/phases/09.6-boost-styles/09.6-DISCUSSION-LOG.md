# Phase 9.6: boost-styles - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-20
**Phase:** 9.6-boost-styles
**Areas discussed:** Quest-checklist row breakdown, Ring-badges tier mapping, Popover
interaction, Visual consistency vs sketch fidelity

---

## Quest-checklist row breakdown

| Option | Description | Selected |
|--------|-------------|----------|
| 3 separate rows | One row per grade tier (t1/t2/t3), matching the sketch's example rows | ✓ |
| 1 collapsed row | Single "grades" row with current tier + next hint | |

**User's choice:** 3 separate rows.

| Option | Description | Selected |
|--------|-------------|----------|
| 3 rows: full-week + streak×2 + streak×3 | Mirrors the grades treatment, consistent granularity | ✓ |
| 2 rows: full-week + one combined streak row | Less granular, one row for both streak tiers | |

**User's choice:** 3 rows (full-week + streak×2 + streak×3).

| Option | Description | Selected |
|--------|-------------|----------|
| Lower tier shows checked, normal done state | Reuses sketch's `.quest-item.done` style verbatim | ✓ |
| Distinct "superseded" style | New third visual state | |

**User's choice:** Lower tier shows checked, normal done state.

| Option | Description | Selected |
|--------|-------------|----------|
| Explicit note + combined total ≠ sum of rows | Bottom total = real `week.total`, note explains best-tier-wins | ✓ |
| Show only incremental delta on not-yet-reached rows | Different treatment per row state | |

**User's choice:** Explicit note + combined total ≠ sum of rows.

**Notes:** These four answers became D-01 through D-04.

---

## Ring-badges tier mapping

| Option | Description | Selected |
|--------|-------------|----------|
| Grade tiers only (3 badges) + 7-dot week strip | Matches validated sketch composition exactly | ✓ |
| All 5 tiers as badges (3 grade + 2 streak) | Departs from sketch, more badge copy needed | |

**User's choice:** Grade tiers only + 7-dot week strip.

| Option | Description | Selected |
|--------|-------------|----------|
| 7 dots for full-week + a small streak chip alongside | Dots = literal days, chip = streak tier status | ✓ |
| Fold streak into the dot strip visually | Overloads dots with two meanings | |

**User's choice:** 7 dots + separate streak chip.

| Option | Description | Selected |
|--------|-------------|----------|
| week.total / week.max | Same total/max the inline BoostMeter already uses | ✓ |
| Average of grade-tier % and consistency % | New blended metric, inconsistent with BOOST-06 | |

**User's choice:** week.total / week.max.

| Option | Description | Selected |
|--------|-------------|----------|
| Distinct "next" highlight | Outline/glow on the immediately-next badge | ✓ |
| Binary only — reached vs not-yet | Matches sketch exactly, no extra state | |

**User's choice:** Distinct "next" highlight.

**Notes:** These four answers became D-06 through D-09. Mid-area, the user interjected (via
free-text "Other" on the continue-check) with guidance that money is a tool, not the goal, and
the UI should not put coin amounts front-and-center — this became a new cross-cutting decision
D-00 (motivation framing), applied to both quest-checklist and ring-badges copy.

---

## Popover interaction

| Option | Description | Selected |
|--------|-------------|----------|
| Closes automatically, only one open at a time | Matches sketch's `.tier-badge.open` toggle | ✓ |
| Each badge toggles independently | Risk of overlapping popovers | |

**User's choice:** Single-open-at-a-time.

| Option | Description | Selected |
|--------|-------------|----------|
| Tap-outside/backdrop closes it too | Standard lightweight-popover behavior | ✓ |
| Only tapping the same badge again closes it | Fully explicit, matches BottomSheet's own rule | |

**User's choice:** Tap-outside also closes.

| Option | Description | Selected |
|--------|-------------|----------|
| Reached: what was earned (behavior-first) · Not-yet: what's needed | Mirrors D-00's framing | ✓ |
| Same template for both, reached/not-yet toggle only | Less behavior-first distinction | |

**User's choice:** Behavior-first, state-specific copy.

| Option | Description | Selected |
|--------|-------------|----------|
| Same reached template, no special maxed framing | Simplest, no new copy branch | ✓ |
| Special "maxed out this week" framing for the top tier | Extra celebratory copy string | |

**User's choice:** Same reached template, no special maxed framing.

**Notes:** These four answers became D-10 through D-12.

---

## Visual consistency vs sketch fidelity

| Option | Description | Selected |
|--------|-------------|----------|
| Keep each style's own sketch composition | Styles stay visually distinct, matches validated sketch | ✓ |
| Wrap all three in the same gradient hero card | Unified chrome, departs from sketch fidelity | |

**User's choice:** Keep each style's own sketch composition.

| Option | Description | Selected |
|--------|-------------|----------|
| Same title/icon for all three styles | One i18n key, recognizable as same feature | ✓ |
| Style-specific title | More personality, more i18n strings | |

**User's choice:** Same title/icon for all three.

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — same distinct penalty treatment in both | Mirrors 9.5's D-05 transparency principle | ✓ |
| Treat penalized as plain "not reached" | Simpler, departs from transparency principle | |

**User's choice:** Same distinct penalty treatment in both new styles.

| Option | Description | Selected |
|--------|-------------|----------|
| Standard K token reuse, no exceptions | Same palette as every other kid-UI component | ✓ |
| Something specific to flag | User has a specific exception in mind | |

**User's choice:** Standard K token reuse, no exceptions.

**Notes:** These four answers became D-13 through D-16.

---

## Claude's Discretion

- Exact spacing/typography of quest-checklist rows and ring-badges layout beyond the sketch's
  `.quest-item`/`.ring-big`/`.tier-badge` CSS.
- Exact wording of the "best tier counts, not summed" note (D-04) and behavior-first popover
  copy (D-12), within existing i18n/copy patterns and honoring D-00.
- Exact highlight treatment for the "next" badge (D-09) — outline vs glow vs pulse.
- Whether the streak status chip (D-07) sits directly under the dot strip or beside it.

## Deferred Ideas

None raised this session. The loot-box/crate reward mechanic (deferred in Phases 9.4 and 9.5)
was not re-raised.
