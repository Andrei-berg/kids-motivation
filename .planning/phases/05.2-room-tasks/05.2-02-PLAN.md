---
phase: 05.2-room-tasks
plan: 02
type: execute
wave: 2
depends_on: [05.2-01]
files_modified:
  - lib/models/room.types.ts
  - lib/repositories/room.repo.ts
  - lib/room-api.ts
  - lib/onboarding-api.ts
autonomous: true
requirements: [RT-SC1, RT-SC4]   # new-family seed (SC1) + repo layer that the Settings editor & forms consume (SC4/SC2)

must_haves:
  truths:
    - "A new family created via createFamily gets the 5 default room tasks seeded"
    - "getRoomTasks returns a family's active tasks in sort_order"
    - "saveRoomChecks upserts one room_checks row per task for a child+date"
    - "Parent CRUD (add/rename/toggle/reorder/delete) writes room_tasks under family RLS"
  artifacts:
    - path: "lib/repositories/room.repo.ts"
      provides: "getRoomTasks, getRoomChecks, saveRoomChecks + parent CRUD (addRoomTask, updateRoomTask, setRoomTaskActive, reorderRoomTasks, deleteRoomTask)"
      exports: ["getRoomTasks", "saveRoomChecks", "addRoomTask"]
      min_lines: 60
    - path: "lib/models/room.types.ts"
      provides: "RoomTask, RoomCheck types"
      contains: "legacy_key"
    - path: "lib/onboarding-api.ts"
      provides: "seed_default_room_tasks RPC call inside createFamily"
      contains: "seed_default_room_tasks"
  key_links:
    - from: "createFamily"
      to: "seed_default_room_tasks RPC"
      via: "non-fatal try/catch after seedDefaultCategories"
      pattern: "rpc\\('seed_default_room_tasks'"
    - from: "saveRoomChecks"
      to: "room_checks table"
      via: "upsert onConflict child_id,date,task_id"
      pattern: "onConflict"
---

<objective>
Add the browser data layer for room tasks/checks and wire the new-family seed. This is the contract that
the kid form, parent modal, award tests, and the Settings editor all build against. `room_tasks`/`room_checks`
are family-config / day-data (NOT money tables) so writes go through the standard browser supabase client
under family RLS — the same pattern as categories/activities.

Purpose: One repo surface for reads (checklist render), day-data writes (dual-write), and parent CRUD.
Output: lib/repositories/room.repo.ts + types + re-export wrapper + createFamily seeding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-room-tasks/05.2-CONTEXT.md
@CLAUDE.md

<read_first>
- lib/repositories/children.repo.ts — the repo idiom to MIRROR: `import { supabase } from '../supabase'`, `.from(...).select/upsert`, `onConflict`, throw on error, TEXT child ids, family_id fetched from children when needed.
- lib/repositories/categories.repo.ts (seedDefaultCategories, ~line 129) — the `supabase.rpc('seed_default_categories', { p_family_id })` shape to mirror for room tasks.
- lib/onboarding-api.ts createFamily (~lines 130-200) — Step 5 already calls `seedDefaultCategories(familyRow.id)` inside a non-fatal try/catch; add the room seed the same way, right after it.
- lib/expenses-api.ts / lib/*.ts wrappers — the backward-compat re-export pattern for lib/room-api.ts.
- Plan 05.2-01 &lt;interfaces&gt; — the exact column contract for room_tasks/room_checks.
</read_first>

<interfaces>
<!-- Types this plan defines and downstream plans import. -->
RoomTask  { id: string; family_id: string; name: string; icon: string | null; legacy_key: 'bed'|'floor'|'desk'|'closet'|'trash'|null; sort_order: number; is_active: boolean; created_at: string }
RoomCheck { id: string; child_id: string; family_id: string; date: string; task_id: string; done: boolean; created_at: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: room.types.ts + room.repo.ts (reads, day-data writes, parent CRUD)</name>
  <files>lib/models/room.types.ts, lib/repositories/room.repo.ts, lib/room-api.ts</files>
  <read_first>lib/repositories/children.repo.ts (saveDay upsert + family_id fetch); lib/repositories/categories.repo.ts</read_first>
  <action>
    Define `RoomTask`/`RoomCheck` in lib/models/room.types.ts per &lt;interfaces&gt;. In lib/repositories/room.repo.ts
    (import the browser `supabase` singleton like children.repo.ts) implement:
    - `getRoomTasks(familyId, { activeOnly = true } = {})` → room_tasks filtered by family_id (and is_active when activeOnly), ordered by sort_order asc.
    - `getRoomChecks(childId, date)` → room_checks for child+date.
    - `saveRoomChecks(childId, date, checks: {taskId: string; done: boolean}[])` → resolve family_id from children (mirror saveDay's family_id fetch), then upsert one room_checks row per task with `onConflict: 'child_id,date,task_id'`. Write ALL passed tasks (done true/false) so unchecking persists.
    - Parent CRUD: `addRoomTask({familyId, name, icon, sortOrder})` (legacy_key stays NULL for custom), `updateRoomTask(id, {name?, icon?})`, `setRoomTaskActive(id, isActive)`, `reorderRoomTasks(orderedIds: string[])` (update sort_order per index), `deleteRoomTask(id)` (relies on the DB delete-guard trigger to reject legacy tasks — surface the thrown error).
    Add lib/room-api.ts re-exporting from the repo (backward-compat import path convention).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` clean.
    - room.repo.ts exports getRoomTasks, getRoomChecks, saveRoomChecks, addRoomTask, updateRoomTask, setRoomTaskActive, reorderRoomTasks, deleteRoomTask (grep confirms all 8 `export`).
    - saveRoomChecks uses `onConflict: 'child_id,date,task_id'` (grep).
    - No import of `@/lib/supabase/admin` (this is a browser/client repo, not a money-service module).
  </acceptance_criteria>
  <done>Repo + types + wrapper compile; 8 exports present; day-data writes go through the browser client under RLS.</done>
</task>

<task type="auto">
  <name>Task 2: Seed default room tasks on new-family creation</name>
  <files>lib/onboarding-api.ts</files>
  <read_first>lib/onboarding-api.ts createFamily Step 5 (seedDefaultCategories non-fatal try/catch)</read_first>
  <action>
    In createFamily, immediately after the existing `seedDefaultCategories(familyRow.id)` try/catch, add a second
    non-fatal try/catch calling `supabase.rpc('seed_default_room_tasks', { p_family_id: familyRow.id })`. Log a
    `console.warn('seedDefaultRoomTasks failed (non-fatal):', err)` on failure — a new family must still be created
    if seeding hiccups (the migration's idempotent backfill / lazy retry covers it). Do not change return shape.
  </action>
  <verify>
    <automated>grep -q "seed_default_room_tasks" lib/onboarding-api.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - createFamily calls `supabase.rpc('seed_default_room_tasks', { p_family_id: ... })` inside a try/catch that logs and continues.
    - Return type FamilyCreateResult unchanged; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>New families seed the 5 defaults; failure is non-fatal; types unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kid/parent client → room.repo.ts → room_tasks/room_checks | Browser writes under family RLS; caller identity is the JWT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-052-06 | Tampering | saveRoomChecks writing another family's child rows | mitigate | family_id resolved from the children row; RLS INSERT/UPDATE WITH CHECK (from Plan 01) rejects cross-family writes regardless of client-supplied ids |
| T-052-07 | Elevation of Privilege | deleteRoomTask removing a legacy/system task via the repo | mitigate | DB BEFORE DELETE trigger (Plan 01) rejects legacy_key-not-null deletes; repo surfaces the error rather than swallowing it |
| T-052-08 | Denial of Service | seed RPC failure aborting family creation | accept | Wrapped in non-fatal try/catch; migration backfill + idempotent seed provide recovery; family creation never blocked |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm test` still green (no route/money code touched).
- Manual repo smoke (optional): with .env.local, a node one-liner calling getRoomTasks(existingFamilyId) returns 5 ordered tasks.
</verification>

<success_criteria>
The data layer + new-family seed exist so Plans 03 (award), 04/05 (forms), and 06 (settings editor) can
build without exploring the schema. SC1 (new-family seed) satisfied.
</success_criteria>

<output>
Create `.planning/phases/05.2-room-tasks/05.2-02-SUMMARY.md` when done.
</output>
