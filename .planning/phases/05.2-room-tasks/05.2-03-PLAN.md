---
phase: 05.2-room-tasks
plan: 03
type: tdd
wave: 2
depends_on: [05.2-01]
files_modified:
  - app/api/wallet/award/route.ts
  - tests/integration/room-award.test.ts
autonomous: true
requirements: [RT-SC3]   # award computes room coins from room_checks with fallback; verify/tests pass

must_haves:
  truths:
    - "For a default 5-task family, 3+ done room_checks credit coins_per_room_task exactly once (unchanged economics)"
    - "2 of 5 done credits nothing"
    - "When a child has NO room_checks rows for the date, award falls back to day.room_ok (legacy path unchanged)"
    - "A second identical award POST credits 0 (idempotency per (child_id,'room',day.id) preserved)"
  artifacts:
    - path: "app/api/wallet/award/route.ts"
      provides: "room award computed from room_checks with legacy room_ok fallback"
      contains: "room_checks"
    - path: "tests/integration/room-award.test.ts"
      provides: "parity + threshold + fallback + idempotency integration tests against live DB"
      contains: "room_checks"
  key_links:
    - from: "award route room block"
      to: "room_checks table"
      via: "admin query on (child_id,date) joined to active room_tasks"
      pattern: "from\\('room_checks'\\)"
    - from: "room award intent"
      to: "wallet_transactions"
      via: "sourceType 'room', sourceId day.id (idempotent)"
      pattern: "sourceType: 'room'"
---

<objective>
Move the room coin computation in `/api/wallet/award` from the flat `day.room_ok` boolean to a count of
done `room_checks` among the family's ACTIVE tasks — while keeping the economics and idempotency identical:
still a flat `coins_per_room_task` credited once per day, keyed `sourceType 'room'` / `sourceId = day.id`.
Add a FALLBACK to `day.room_ok` when the child has no room_checks rows for that date, so families that have
not yet dual-written (or legacy days) behave exactly as today. Prove all of it with new integration tests
extending the 05.1 harness.

Purpose: Award reads the flexible source, with zero behavior change for the default 5-task / legacy case.
Output: award route room block rewritten; tests/integration/room-award.test.ts green against the live DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-room-tasks/05.2-CONTEXT.md
@CLAUDE.md

<read_first>
- app/api/wallet/award/route.ts (lines 60-96) — the CURRENT room block: `day.room_ok && settings.coins_per_room_task !== 0` → push intent {coins, sourceType:'room', sourceId: day.id}. Preserve this intent shape EXACTLY; only change the CONDITION that decides whether to push it.
- app/api/wallet/_lib.ts — loadSettings (coins_per_room_task default 3), creditAwards (idempotency pre-check on (child_id, source_type, source_id)). Do NOT change creditAwards.
- tests/integration/family-fixture.ts — createTestFamily/destroyTestFamily/serviceClient/hasIntegrationEnv.
- tests/integration/award.test.ts — the requireFamilyMember partial-mock + direct-POST-invocation pattern to reuse; note the KEY FACT: `days.room_ok` is DB-trigger-derived — seed the 5 room_* booleans, not room_ok directly.
- .planning/phases/05.1-launch-prep/05.1-05-SUMMARY.md — room_ok trigger discovery + streak-default fallback assertion pattern.
</read_first>

<interfaces>
<!-- From Plan 05.2-01. The award route uses createAdminClient (service role), reads tables directly. -->
room_tasks:  family_id, is_active, sort_order, legacy_key
room_checks: child_id, date, task_id, done
Threshold rule (Claude's discretion per CONTEXT decision 4): threshold = max(1, ceil(0.6 * activeTaskCount)).
For the default 5 active tasks → ceil(3.0)=3 → EXACT parity with today's ≥3-of-5.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Award room coins from room_checks with legacy fallback</name>
  <files>app/api/wallet/award/route.ts</files>
  <read_first>app/api/wallet/award/route.ts room block (lines 60-96); app/api/wallet/_lib.ts loadSettings/creditAwards</read_first>
  <behavior>
    - 5 active tasks, 3 done room_checks rows → one 'room' intent for coins_per_room_task (parity with legacy 3/5).
    - 5 active tasks, 2 done → no 'room' intent.
    - N active tasks, done_count ≥ max(1, ceil(0.6*N)) → one 'room' intent; else none.
    - Zero room_checks rows for (child_id,date) → fall back to `day.room_ok` (existing legacy condition) unchanged.
    - Re-POST same day → creditAwards idempotency (source 'room', sourceId day.id) → 0 new coins.
  </behavior>
  <action>
    Replace ONLY the room condition inside the `if (day) { ... }` block. After loading `day`, query
    `room_checks` for (childId, date) and the family's ACTIVE `room_tasks` (is_active=true) via the existing
    `admin` client. Compute `activeCount` and `doneCount` (done room_checks whose task_id is an active task).
    If `room_checks` returned zero rows for the date → keep the CURRENT behavior: push the room intent when
    `day.room_ok` is true. If room_checks rows exist → push the room intent when
    `doneCount >= Math.max(1, Math.ceil(0.6 * activeCount))`. The pushed intent is byte-identical to today's
    (coins: settings.coins_per_room_task, description 'Убрана комната', icon '🏠', sourceType 'room',
    sourceId day.id) and still gated by `settings.coins_per_room_task !== 0`. Do NOT change behavior, streak,
    grade, sport, activity, or book blocks. Add a short comment documenting the threshold rule and the fallback.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; grep -q "from('room_checks')" app/api/wallet/award/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - Route queries `room_checks` and `room_tasks` (grep both) via the admin client.
    - Room intent still uses `sourceType: 'room'`, `sourceId: day.id`, `settings.coins_per_room_task` (unchanged shape).
    - Fallback branch references `day.room_ok`.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Award computes room completeness from room_checks with legacy fallback; idempotency source key unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests — parity, threshold, fallback, idempotency</name>
  <files>tests/integration/room-award.test.ts</files>
  <read_first>tests/integration/award.test.ts (mock + POST pattern, room_* seeding); tests/integration/family-fixture.ts</read_first>
  <behavior>
    - Test A (parity): seed 5 active room_tasks (or use the family's seeded defaults) + 3 done room_checks + a days row (set 3 of room_bed/floor/desk so room_ok is also true) → POST award → exactly one 'room' wallet_transaction with coins_change = coins_per_room_task default (3). Second POST → 0 new room tx (idempotent).
    - Test B (below threshold): fresh day, 2 done room_checks (5 active) + a days row with only 2 room_* booleans (room_ok false) → POST → zero 'room' transactions.
    - Test C (fallback): a days row with 3 room_* booleans true (room_ok true) and ZERO room_checks rows for the date → POST → one 'room' transaction (legacy path still pays).
  </behavior>
  <action>
    Create tests/integration/room-award.test.ts under `describe.skipIf(!hasIntegrationEnv)`, reusing
    createTestFamily/destroyTestFamily and the requireFamilyMember partial-mock + direct `POST(new NextRequest(...))`
    pattern from award.test.ts. The test family already has the 5 seeded tasks (from Plan 01 backfill); fetch its
    room_tasks via serviceClient to get task_ids, then seed room_checks rows accordingly. Assert on
    `wallet_transactions` filtered by `source_type='room'`. Use distinct dates per test to keep idempotency keys
    independent. Numeric columns from supabase-js are strings — wrap coins assertions with Number() only where the
    column is numeric (coins_change is integer → native number; confirm as in 05.1-06). Ensure teardown leaves zero rows.
  </action>
  <verify>
    <automated>node --env-file=.env.local ./node_modules/vitest/vitest.mjs run tests/integration/room-award.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:integration` (or the scoped vitest command above) runs room-award.test.ts green against the live DB.
    - Tests A/B/C all present and passing; Test A asserts idempotency (second POST → 0 new 'room' tx).
    - `npm test` (no env) still green — the suite is skipped via describe.skipIf(!hasIntegrationEnv).
    - Post-run: zero `__test__` families/room_checks/wallet_transactions left for the test child.
  </acceptance_criteria>
  <done>Award-from-room_checks parity + threshold + fallback + idempotency proven against live DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kid client → /api/wallet/award (server, service-role) | Child may trigger award only for their own day (existing role guard) |
| client-written room_checks → server award computation | Untrusted day-data feeds a money computation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-052-09 | Elevation of Privilege | Child inflating room coins by writing extra room_checks | mitigate | Room award is a FLAT `coins_per_room_task` credited AT MOST ONCE per day (sourceType 'room', sourceId day.id) — extra checks cannot exceed the single flat award a 3/5 legacy check already grants; idempotency pre-check in creditAwards blocks re-award |
| T-052-10 | Tampering | Fallback abuse: no room_checks but forged room_ok | mitigate | room_ok is DB-trigger-derived from the 5 room_* booleans (cannot be set directly); fallback only reproduces the already-shipped legacy behavior — no new attack surface |
| T-052-11 | Tampering | Cross-child/date room_checks feeding award | mitigate | Award queries room_checks strictly by the authorized (childId, date); child role guard restricts childId to the caller's own child |
| T-052-12 | Repudiation | Silent behavior drift for default families | mitigate | Integration Test A pins exact parity (coins_change=3, one tx, idempotent) so any economic drift fails CI |
</threat_model>

<verification>
- `npm run test:integration` green (award.test.ts + purchase + exchange-withdraw + new room-award.test.ts all pass together).
- `npm test` green with no env (all integration suites skip).
- `npx tsc --noEmit` clean.
- Optionally re-run `node --env-file=.env.local scripts/verify-award-idempotency.mjs` — still passes (idempotency key unchanged).
</verification>

<success_criteria>
ROADMAP 5.2 SC3 met: `/api/wallet/award` computes room coins from room_checks with fallback to legacy
columns, economics unchanged for default/legacy families, covered by new integration tests.
</success_criteria>

<output>
Create `.planning/phases/05.2-room-tasks/05.2-03-SUMMARY.md` when done.
</output>
