---
phase: 05.2-room-tasks
plan: 05
type: execute
wave: 3
depends_on: [05.2-02]
files_modified:
  - components/DailyModal.tsx
autonomous: false   # ends with a human-verify checkpoint (parent modal renders + dual-write)
requirements: [RT-SC2]

must_haves:
  truths:
    - "The parent DailyModal Room section renders items from room_tasks (active, ordered), not the hardcoded 5-item array"
    - "Saving writes room_checks rows for every rendered task (done true/false)"
    - "For legacy_key tasks, saving ALSO writes the matching days.room_* column (dual-write)"
    - "Sick-day grace behavior is preserved (room section shows grace note, no penalty)"
  artifacts:
    - path: "components/DailyModal.tsx"
      provides: "checklist rendered from room_tasks + dual-write on save"
      contains: "getRoomTasks"
  key_links:
    - from: "DailyModal room section"
      to: "getRoomTasks(familyId)"
      via: "load in useEffect, render list"
      pattern: "getRoomTasks"
    - from: "handleSave"
      to: "saveRoomChecks + api.saveDay legacy columns"
      via: "dual-write"
      pattern: "saveRoomChecks"
---

<objective>
Mirror Plan 04 for the parent-side day editor: replace DailyModal's hardcoded 5 room booleans with a list
rendered from `room_tasks` (active, ordered), dual-writing `room_checks` + the legacy `days.room_*` columns on
save. Preserve the sick-day grace path and the existing coin-preview chips.

Purpose: Both day-fill surfaces read the flexible checklist; legacy consumers keep working.
Output: DailyModal renders from the table and dual-writes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-room-tasks/05.2-CONTEXT.md
@CLAUDE.md

<read_first>
- components/DailyModal.tsx — CURRENT room implementation to replace: room booleans state (lines 113-118), pre-fill from dayData (lines 205-210), the hardcoded checklist render (lines ~1010-1027, keys bed/floor/desk/closet/trash with setters), roomScore/roomOk/roomCoins preview (lines 499-501), and the api.saveDay call in handleSave (lines 404-413) that passes roomBed/roomFloor/roomDesk/roomCloset/roomTrash.
- lib/repositories/room.repo.ts (Plan 02) — getRoomTasks, saveRoomChecks, getRoomChecks.
- Note DailyModal's closet emoji is 👕 today vs the kid form's 🚪; after this change BOTH render task.icon from the table (the seed uses 🚪), so they converge — this is expected and acceptable.
- CONTEXT decision 3 (dual-write) — legacy columns keep being written for legacy_key tasks.
</read_first>

<interfaces>
RoomTask.legacy_key ∈ {'bed','floor','desk','closet','trash'} → api.saveDay params roomBed/roomFloor/roomDesk/roomCloset/roomTrash.
getRoomTasks(familyId) → RoomTask[] active, sort_order asc.
saveRoomChecks(childId, date, [{taskId, done}]).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render DailyModal Room checklist from room_tasks and dual-write on save</name>
  <files>components/DailyModal.tsx</files>
  <read_first>components/DailyModal.tsx (room state/render/save lines noted above); lib/repositories/room.repo.ts</read_first>
  <action>
    Load the child's family_id and active room_tasks in the modal's data-load effect; store checked-state as
    `Record&lt;taskId, boolean&gt;`, pre-filled from getRoomChecks(childId,date) (fall back to the legacy dayData.room_*
    booleans mapped by legacy_key when no room_checks rows exist, so an already-legacy-saved day pre-checks correctly).
    Replace the hardcoded checklist array render with a map over the loaded tasks (task.icon + task.name), keeping the
    `premium-checklist`/`premium-checkbox` markup and the sick-day grace branch untouched. Update roomScore/roomOk/roomCoins
    preview to derive from the dynamic checked-state and taskCount (roomOk when doneCount ≥ max(1, ceil(0.6*taskCount)),
    which is 3/5 for defaults). In handleSave, DUAL-WRITE: call saveRoomChecks(childId, date, tasks.map(t =&gt; ({taskId:t.id, done: checked[t.id] ?? false}))) alongside the existing api.saveDay call, and derive the api.saveDay
    roomBed/…/roomTrash params from checked-state via each task's legacy_key (default false). Keep the /api/wallet/award
    POST and every non-room field/behavior unchanged. Preserve sick-day grace (no room penalty).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; grep -q "getRoomTasks" components/DailyModal.tsx &amp;&amp; grep -q "saveRoomChecks" components/DailyModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Room checklist maps over getRoomTasks output (no hardcoded 5-element room array for rendering).
    - handleSave calls saveRoomChecks with one entry per rendered task and still calls api.saveDay with legacy room params derived from legacy_key.
    - Sick-day branch (grace note, no penalty) preserved.
    - `npx tsc --noEmit` clean; `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Parent DailyModal Room checklist is data-driven; save dual-writes room_checks + legacy columns.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>DailyModal renders the Room checklist from room_tasks and dual-writes to room_checks + the legacy days.room_* columns; sick-day grace preserved.</what-built>
  <how-to-verify>
    1. `npm run dev`, open Parent Center → a child → daily input (DailyModal) for today.
    2. Confirm the Room section shows the 5 seeded items with emoji in order; the badge shows doneCount/5.
    3. Check 3, save. Confirm room coins chip shows and the day saves without error.
    4. Service-role query: room_checks has 5 rows (3 done) and days has 3 room_* booleans true (room_ok true).
    5. Open a SICK day: confirm the Room grace note shows and no room penalty is applied.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what rendered/persisted incorrectly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent browser → room_checks + days (RLS) | Parent writes a child's day data under family isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-052-16 | Information Disclosure | Rendering another family's tasks in the modal | mitigate | getRoomTasks filters by the child's family_id; RLS SELECT enforces isolation |
| T-052-17 | Tampering | Divergent legacy vs room_checks state on save | mitigate | Both derived from the same checked-state map in handleSave; legacy_key mapping is the single source for the room_* params |
| T-052-18 | Repudiation | Sick-day grace silently lost | mitigate | Human-verify step 5 explicitly checks the sick-day grace branch |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run lint` exits 0.
- Human-verify checkpoint approved (checklist from table; room_checks + legacy columns written; sick-day grace intact).
</verification>

<success_criteria>
ROADMAP 5.2 SC2 met for the parent modal as well — both day-fill surfaces render from room_tasks and dual-write.
</success_criteria>

<output>
Create `.planning/phases/05.2-room-tasks/05.2-05-SUMMARY.md` when done.
</output>
