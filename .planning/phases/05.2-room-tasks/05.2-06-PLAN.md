---
phase: 05.2-room-tasks
plan: 06
type: execute
wave: 3
depends_on: [05.2-02]
files_modified:
  - components/settings/RoomTasksManager.tsx
  - components/parent-center/screens/Settings.tsx
  - public/locales/en.json
  - public/locales/ru.json
# ends with a human-verify checkpoint (SC4: rename visible on kid screen)
autonomous: false
requirements: [RT-SC4]

must_haves:
  truths:
    - "Parent can add a custom room task (name + emoji) and it appears in the checklist"
    - "Parent can rename any task, toggle active, and reorder tasks"
    - "Custom tasks can be deleted; the 5 legacy/system tasks are non-deletable (deactivate-only) with a lock hint"
    - "A rename saved here shows on the kid screen on next load (SC4 end-to-end)"
  artifacts:
    - path: "components/settings/RoomTasksManager.tsx"
      provides: "Room checklist manager: add/rename/toggle/reorder/delete with legacy lock"
      min_lines: 80
    - path: "components/parent-center/screens/Settings.tsx"
      provides: "RoomTasksManager mounted as a Schedule sub-tab"
      contains: "RoomTasksManager"
  key_links:
    - from: "RoomTasksManager"
      to: "room.repo.ts CRUD (addRoomTask/updateRoomTask/setRoomTaskActive/reorderRoomTasks/deleteRoomTask)"
      via: "manager component pattern"
      pattern: "addRoomTask|updateRoomTask"
    - from: "Settings ScheduleTab SUB_TABS"
      to: "RoomTasksManager"
      via: "new 'room' sub-tab"
      pattern: "sub === 'room'"
---

<objective>
Add a Room checklist manager to Parent Center Settings so a family can add, rename, toggle, reorder and
delete checklist items. Mount it as a new sub-tab in the Schedule tab (alongside Subjects/Sections/
Vacations/Activities). Enforce the dual-write-era rule: custom tasks (no legacy_key) hard-delete; the 5
legacy/system tasks can only be deactivated, shown with a small lock hint. This closes SC4 — a task renamed
here appears on the kid screen immediately (next load).

Purpose: The checklist is now fully family-editable end to end.
Output: RoomTasksManager component + Settings wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-room-tasks/05.2-CONTEXT.md
@CLAUDE.md

<read_first>
- components/settings/ActivitiesManager.tsx — the manager-component pattern to MIRROR: 'use client', useState/useEffect, useFamilyMembers()/family context, list + add form + edit/toggle/delete rows, EMOJIS palette, useT(). RoomTasksManager should be visually and structurally consistent with it (parent-center dark tokens).
- components/settings/SectionsManager.tsx — a simpler CRUD manager if a lighter template fits better.
- lib/repositories/room.repo.ts (Plan 02) — getRoomTasks (activeOnly:false here to show inactive too), addRoomTask, updateRoomTask, setRoomTaskActive, reorderRoomTasks, deleteRoomTask. RoomTask.legacy_key distinguishes system vs custom.
- components/parent-center/screens/Settings.tsx ScheduleTab (lines 356-395) — SUB_TABS array + the `sub === 'x' && <Manager/>` render switch to extend with a 'room' entry.
- lib/store.ts — resolve familyId via `const { familyId } = useAppStore()` (the pattern of SectionsManager.tsx). Do NOT use useFamilyMembers() for familyId — it returns only { members, loading, error } (checker-verified 2026-07-06).
- CONTEXT decision 5 (settings editor scope) + specifics (system tasks visibly non-deletable, a small lock hint is enough).
</read_first>

<interfaces>
RoomTask.legacy_key !== null ⇒ system/legacy task: render a lock hint, DISABLE delete (deactivate-only), keep rename + reorder allowed.
deleteRoomTask on a legacy task throws (DB guard) — never expose the delete control for those; only call delete for custom tasks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RoomTasksManager component (add/rename/toggle/reorder/delete + legacy lock)</name>
  <files>components/settings/RoomTasksManager.tsx</files>
  <read_first>components/settings/ActivitiesManager.tsx (structure, EMOJIS, tokens); lib/repositories/room.repo.ts</read_first>
  <action>
    Create a 'use client' RoomTasksManager mirroring ActivitiesManager's shape. Resolve familyId via
    `const { familyId } = useAppStore()` (SectionsManager pattern) — NOT via useFamilyMembers(), which has no familyId field. On mount, getRoomTasks(familyId, {activeOnly:false}) → render the list ordered by
    sort_order. Per row: emoji + editable name (rename via updateRoomTask on blur/save), an active toggle
    (setRoomTaskActive), reorder controls (up/down buttons calling reorderRoomTasks with the new id order — buttons,
    not drag, keep it simple), and a delete control ONLY for custom tasks (legacy_key === null → deleteRoomTask). For
    legacy tasks (legacy_key !== null) render a small lock hint (e.g. 🔒 "системная") and NO delete button — only the
    active toggle + rename. An add form at the top: name input + an emoji picker (reuse an EMOJIS palette) →
    addRoomTask({familyId, name, icon, sortOrder: nextIndex}). Refetch after each mutation so the list reflects DB
    state. Use useT() for the manager's own chrome labels; task names themselves are user data (render raw, no i18n).
    Show a lightweight error toast/message if a mutation throws (e.g. attempting to delete a legacy task).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; grep -q "deleteRoomTask" components/settings/RoomTasksManager.tsx &amp;&amp; grep -q "legacy_key" components/settings/RoomTasksManager.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Component calls addRoomTask, updateRoomTask, setRoomTaskActive, reorderRoomTasks, deleteRoomTask (grep each).
    - Delete control is rendered only when `legacy_key === null`; legacy rows show a lock hint instead.
    - `npx tsc --noEmit` clean; `npm run lint` exits 0.
  </acceptance_criteria>
  <done>RoomTasksManager supports add/rename/toggle/reorder; custom-only delete; legacy tasks locked.</done>
</task>

<task type="auto">
  <name>Task 2: Mount RoomTasksManager as a Schedule sub-tab in Settings</name>
  <files>components/parent-center/screens/Settings.tsx</files>
  <read_first>components/parent-center/screens/Settings.tsx ScheduleTab (SUB_TABS + render switch)</read_first>
  <action>
    Import RoomTasksManager. Add a `{ id: 'room', icon: '🏠', label: t('settings.tabs.room') }` entry to the
    ScheduleTab SUB_TABS array (place it after 'sections' or wherever reads naturally) and a
    `{sub === 'room' && <RoomTasksManager/>}` line in the render switch. Add the `settings.tabs.room` key to the `settings.tabs` object in BOTH public/locales/en.json ("Room") and
    public/locales/ru.json ("Комната") — mirror the existing settings.tabs.children/schedule/activities keys. Do not disturb the other sub-tabs.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; grep -q "sub === 'room'" components/parent-center/screens/Settings.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ScheduleTab SUB_TABS includes a 'room' entry and the render switch mounts RoomTasksManager.
    - `settings.tabs.room` exists in both en.json and ru.json (grep both).
    - `npx tsc --noEmit` clean; `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Room manager reachable from Parent Center → Settings → Schedule → Room.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: rename in Settings appears on kid screen immediately (SC4)</name>
  <action>Operator renames a task in RoomTasksManager and confirms the new name renders on the kid day form after reload.</action>
  <what-built>A Room checklist manager in Parent Center Settings (Schedule → Room) with add/rename/toggle/reorder/delete; legacy tasks are locked from deletion. This closes SC4 end-to-end with the kid form (Plan 04).</what-built>
  <how-to-verify>
    1. `npm run dev` → Parent Center → Settings → Schedule → Room. Confirm the 5 seeded tasks show, with a 🔒 lock hint and no delete button on the system tasks.
    2. Rename "кровать" → "Заправить кровать", save.
    3. Add a custom task ("Полить цветы" 🌱). Confirm it appears and HAS a delete button; the system tasks do not.
    4. Reorder a task up/down; toggle one system task inactive.
    5. Open the KID day screen (Plan 04) and reload: confirm the renamed task shows its new name, the custom task appears, the deactivated task is gone. (SC4: rename visible on kid screen immediately.)
    6. Try deleting a custom task → succeeds. Confirm you cannot delete a system task (no control; if forced via repo it errors).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what did not work.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent browser → room_tasks (RLS) | Parent edits family checklist config directly under family isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-052-19 | Elevation of Privilege | Deleting a legacy/system task breaks dual-write for the whole family | mitigate | UI hides the delete control for legacy_key tasks AND the DB BEFORE DELETE trigger (Plan 01) rejects the delete server-side — defense in depth |
| T-052-20 | Tampering | Editing another family's tasks | mitigate | Manager scopes all reads/writes to the resolved familyId; RLS on room_tasks enforces isolation regardless of client input |
| T-052-21 | Denial of Service | Reorder writing conflicting sort_order | accept | reorderRoomTasks rewrites the full ordered id list; last-write-wins within a single family is acceptable for config data |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run lint` exits 0.
- Human-verify checkpoint approved — SC4 demonstrated: a rename in Settings shows on the kid screen on reload.
</verification>

<success_criteria>
ROADMAP 5.2 SC4 met: parent edits the task list in Settings; a renamed task appears on the kid screen. With
Plans 01-05, all four 5.2 success criteria are satisfied.
</success_criteria>

<output>
Create `.planning/phases/05.2-room-tasks/05.2-06-SUMMARY.md` when done.
</output>
