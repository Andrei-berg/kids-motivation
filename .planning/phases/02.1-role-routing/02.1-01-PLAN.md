---
phase: 02.1-role-routing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/parent/dashboard/page.tsx
  - app/kid/day/page.tsx
  - app/family/page.tsx
autonomous: true
requirements:
  - REQ-ROLE-001
  - REQ-ROLE-002
  - REQ-ROLE-003
  - REQ-ROLE-004

must_haves:
  truths:
    - "Route /parent/dashboard renders a valid React page (no 404)"
    - "Route /kid/day renders a valid React page (no 404)"
    - "Route /family renders a valid React page (no 404)"
    - "Each placeholder page clearly identifies its intended role (text label visible)"
  artifacts:
    - path: "app/parent/dashboard/page.tsx"
      provides: "Placeholder parent dashboard page"
      exports: ["default"]
    - path: "app/kid/day/page.tsx"
      provides: "Placeholder kid day page"
      exports: ["default"]
    - path: "app/family/page.tsx"
      provides: "Placeholder family shared page"
      exports: ["default"]
  key_links:
    - from: "middleware.ts"
      to: "app/parent/dashboard/page.tsx"
      via: "redirect after role check"
      pattern: "/parent/dashboard"
    - from: "middleware.ts"
      to: "app/kid/day/page.tsx"
      via: "redirect after role check"
      pattern: "/kid/day"
---

<objective>
Create skeleton route pages for /parent/dashboard, /kid/day, and /family so middleware redirects have valid destinations before Phase 2.2 and 2.3 build the full UI.

Purpose: Middleware in Plan 02 will redirect parents to /parent/dashboard and children to /kid/day. These pages must exist and render without error before that middleware is activated.
Output: Three minimal 'use client' page files — each renders a centered placeholder with role label and link back to /dashboard.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- No imports from existing lib/ are needed — these are pure placeholder pages. -->
<!-- Next.js App Router page convention: -->
<!-- app/parent/dashboard/page.tsx → route /parent/dashboard -->
<!-- app/kid/day/page.tsx         → route /kid/day -->
<!-- app/family/page.tsx          → route /family -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create /parent/dashboard placeholder page</name>
  <files>app/parent/dashboard/page.tsx</files>
  <action>
    Create the directory app/parent/dashboard/ and create app/parent/dashboard/page.tsx.

    The file must:
    1. Start with 'use client' directive
    2. Import { useRouter } from 'next/navigation'
    3. Export a default function ParentDashboardPage()
    4. Render a full-screen centered div with:
       - A heading: "Parent Dashboard" (role label for visibility)
       - Subtext: "Phase 2.2 will build the full parent UI here."
       - A button "Go to legacy dashboard" that calls router.push('/dashboard') — allows navigation during development
    5. Use inline styles only (no Tailwind, no imports from globals.css) — this is a pure scaffold, no styling dependencies
    6. No data fetching, no Supabase calls, no imports from lib/

    This page is intentionally minimal. Its only purpose is to give middleware a valid redirect target.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "app/parent/dashboard" || echo "No TS errors in parent/dashboard"</automated>
  </verify>
  <done>app/parent/dashboard/page.tsx exists, exports a default React component, TypeScript compiles cleanly. Route /parent/dashboard is accessible (returns 200, not 404).</done>
</task>

<task type="auto">
  <name>Task 2: Create /kid/day and /family placeholder pages</name>
  <files>app/kid/day/page.tsx, app/family/page.tsx</files>
  <action>
    Create app/kid/day/page.tsx and app/family/page.tsx using the same pattern as Task 1.

    For app/kid/day/page.tsx:
    1. 'use client' directive
    2. Import { useRouter } from 'next/navigation'
    3. Export default function KidDayPage()
    4. Render centered div with heading "My Day" and subtext "Phase 2.3 will build the full kid UI here."
    5. Button "Go to legacy dashboard" → router.push('/dashboard')
    6. Inline styles only, no lib/ imports

    For app/family/page.tsx:
    1. 'use client' directive
    2. Import { useRouter } from 'next/navigation'
    3. Export default function FamilyPage()
    4. Render centered div with heading "Family" and subtext "Shared family space — accessible to all members."
    5. Button "Go to dashboard" → router.push('/dashboard')
    6. Inline styles only, no lib/ imports

    Note: app/kid/ directory already has a legacy page.tsx (old hardcoded kid screen). The new file goes in app/kid/day/page.tsx (a subdirectory), which creates route /kid/day. Do NOT modify or delete app/kid/page.tsx — that is a separate legacy route left intact until Phase 2.3 replaces it.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "app/kid/day|app/family" || echo "No TS errors in kid/day or family"</automated>
  </verify>
  <done>app/kid/day/page.tsx and app/family/page.tsx both exist and export valid default React components. TypeScript compiles cleanly for both files. Routes /kid/day and /family return 200.</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run: ls app/parent/dashboard/page.tsx app/kid/day/page.tsx app/family/page.tsx — all three must exist
2. Run: npx tsc --noEmit 2>&1 | grep -E "parent/dashboard|kid/day|app/family" — must return no errors
3. Run: npm run build 2>&1 | tail -20 — build must succeed (no new errors from these files)
</verification>

<success_criteria>
- Three placeholder pages exist at the exact paths middleware will redirect to
- No TypeScript errors introduced by the new files
- Production build still passes
- Each page renders a human-readable role label (visible in browser)
- No imports from lib/, no data fetching — pure scaffold only
</success_criteria>

<output>
After completion, create .planning/phases/02.1-role-routing/02.1-01-SUMMARY.md with:
- Files created (exact paths)
- Any deviations from this plan
- Confirmation that npm run build passes
</output>
