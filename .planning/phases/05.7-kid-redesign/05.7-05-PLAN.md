---
phase: 05.7-kid-redesign
plan: 05
type: execute
wave: 2
depends_on: ["05.7-01", "05.7-03"]
files_modified:
  - app/kid/wallet/page.tsx
  - components/kid/GoalsPanel.tsx
autonomous: true
requirements: [05.7-SC1, 05.7-SC3]

must_haves:
  truths:
    - "The wallet screen shows a hero сберкнижка balance as a large gold Amount with count-up (D-16)"
    - "A weekly +earned / -spent summary line appears under the hero (D-16)"
    - "Goals render as вклады (deposits) with progress toward target using Amount/LedgerRow (D-16)"
    - "Goal-reached celebration uses the Stamp ceremony ('ЦЕЛЬ ДОСТИГНУТА'), not confetti (D-20)"
    - "Transaction history stays on LedgerRow theme=paper (5.3 pilot preserved)"
  artifacts:
    - path: "app/kid/wallet/page.tsx"
      provides: "Hero balance + weekly summary + LedgerRow history on paper"
      contains: "useCountUp"
    - path: "components/kid/GoalsPanel.tsx"
      provides: "Deposits framing + Stamp on goal reached"
      contains: "Stamp"
  key_links:
    - from: "app/kid/wallet/page.tsx hero"
      to: "useCountUp + Amount(money)"
      via: "count-up gold balance"
      pattern: "useCountUp"
    - from: "components/kid/GoalsPanel.tsx"
      to: "Stamp ceremony"
      via: "replaces triggerGoalConfetti"
      pattern: "Stamp"
---

<objective>
Restyle the wallet as the hero "сберкнижка" (large gold count-up balance + weekly earned/spent
line) and reframe goals as "вклады" with a Stamp ceremony on completion, retiring the goal
confetti (D-16, D-20).

Purpose: The wallet is the emotional center of the family-bank concept; D-16 makes the balance
the signature gold hero. GoalsPanel lives on this screen, so its D-16 restyle and D-20 stamp
land together here (avoids two plans owning the same file).
Output: reskinned wallet page (hero + summary + LedgerRow history) and GoalsPanel (deposits + Stamp).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md
@.planning/phases/05.7-kid-redesign/05.7-PATTERNS.md
@CLAUDE.md

<interfaces>
Atoms: Amount, LedgerRow, useCountUp, Stamp from '@/components/design/atoms';
ScreenHeader default from '@/components/kid/design/ScreenHeader'; useDesktop from '@/lib/hooks/useDesktop'.
Already-shipped pilot (app/kid/wallet/page.tsx:224-244 TxnRow — reuse verbatim):
  <LedgerRow theme="paper" name={label} sub={time} amount={x.coins_change} tone={isIn?'earn':'spend'}/>
Empty-state copy: t('wallet.emptyHeading'), t('wallet.emptyBody').
getWeekScore-based "earned this week" already computed on this page (~line 70) — extend to a
+earned / -spent pair from the existing transaction data (render-only; no new money math).
GoalsPanel goal-reached today: triggerGoalConfetti() inside refresh() (GoalsPanel.tsx:31) → replace with Stamp.
UI-SPEC Typography Atom-Owned Type: hero may extend Amount with size="xl" (extend, don't fork) if 22px lg is too small.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wallet hero + weekly summary + LedgerRow history</name>
  <files>app/kid/wallet/page.tsx</files>
  <read_first>
    - app/kid/wallet/page.tsx (full — hero block 96-152, TxnRow 224-244, weekly chip ~70, local useDesktop 17-26)
    - components/design/atoms.tsx (Amount sizes 79-83; useCountUp 215-240; if size="xl" needed, extend the sizes map)
    - .planning/phases/05.7-kid-redesign/05.7-CONTEXT.md (D-16 hero + weekly line + вклады; D-13 header; D-15 desktop layout)
    - .planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md (Color gold-money invariant; Typography Atom-Owned Type xl exception)
  </read_first>
  <action>
    Replace the hand-rolled gradient/glass hero (lines 96-152) with a `<ScreenHeader>` +
    a hero balance block: `useCountUp(wallet.coins)` fed into `<Amount value={...} theme="paper"
    money size="lg"/>` (or add `xl: { fs: 30, fw: 800 }` to the Amount sizes map and use size="xl"
    if the hero needs more weight — extend the atom, do not inline a bespoke gold number). Add a
    weekly summary line beneath: `+{earned} / −{spent}` derived from the existing transaction/week
    data, earned via gold `Amount tone-equivalent`, spent neutral — no new money computation.
    Keep the transaction history on the existing `LedgerRow theme="paper"` pilot; add the empty
    state using `wallet.emptyHeading`/`wallet.emptyBody`. Swap the local useDesktop for the shared
    hook; preserve the 4.5 sticky-column desktop layout (D-15). Gold appears ONLY on money figures.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "useCountUp" app/kid/wallet/page.tsx && grep -q "ScreenHeader" app/kid/wallet/page.tsx && grep -q "emptyHeading" app/kid/wallet/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Hero balance uses `useCountUp` feeding a money `Amount` (size lg or xl); no bespoke gold-colored inline number
    - Weekly `+earned / −spent` line present, derived from existing data (no new money math added)
    - Transaction rows still use `LedgerRow theme="paper"`; empty state uses the wallet.empty* keys
    - `useDesktop` imported from the shared hook; no local copy remains in the file
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Wallet hero сберкнижка with count-up gold balance + weekly summary + ledger history + empty state.</done>
</task>

<task type="auto">
  <name>Task 2: Goals as вклады with Stamp ceremony</name>
  <files>components/kid/GoalsPanel.tsx</files>
  <read_first>
    - components/kid/GoalsPanel.tsx (full — goal card + ProgressRing ~91-96; triggerGoalConfetti at 31; inline create sheet ~127-168)
    - components/design/atoms.tsx (Amount, LedgerRow, Stamp)
    - .planning/phases/05.7-kid-redesign/05.7-CONTEXT.md (D-16 вклады framing; D-20 goal stamp 'ЦЕЛЬ ДОСТИГНУТА')
    - .planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md (Motion Contract: goal reached → Stamp -8° 450ms)
  </read_first>
  <action>
    Restyle each goal as a "вклад" (deposit): progress toward target rendered with `Amount`
    (gold current/target coin figures) and a progress bar/ring on paper tones, replacing the raw
    `current.toLocaleString('ru-RU')` strings. Replace the `triggerGoalConfetti()` call in
    refresh() with a `<Stamp>` ceremony showing `t('stamp.goalReached')` ('ЦЕЛЬ ДОСТИГНУТА') on
    the completed goal — reuse the Stamp atom's built-in reduced-motion guard (do NOT add a third
    guard, do NOT import canvas-confetti here). Remove the now-unused triggerGoalConfetti import
    if nothing else in the file uses it. Keep the create/edit sheet behavior unchanged (no delete
    action in scope per UI-SPEC).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "Stamp" components/kid/GoalsPanel.tsx && ! grep -q "triggerGoalConfetti" components/kid/GoalsPanel.tsx && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `components/kid/GoalsPanel.tsx` no longer calls `triggerGoalConfetti`
    - Goal completion renders a `Stamp` with `t('stamp.goalReached')`
    - Coin figures render via `Amount` (gold), not raw `toLocaleString` strings
    - No `canvas-confetti` import remains in the file
    - `npx tsc --noEmit` and `npm test` exit 0
  </acceptance_criteria>
  <done>Goals reframed as deposits with a Stamp ceremony; goal confetti retired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Render-only reskin over existing RLS-scoped wallet/goal reads. No writes changed. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-057-10 | Tampering | weekly summary figures | accept | Derived from already-loaded transaction data client-side for display only; the authoritative balance is server-side (money tables RLS SELECT-only). |
| T-057-SC | Tampering | package installs | accept | N/A — no packages installed. |
</threat_model>

<verification>
`npx tsc --noEmit` + `npm test` green; hero count-up gold balance; goals use Stamp not confetti; gold only on money.
</verification>

<success_criteria>
Wallet is the hero сберкнижка; goals are вклады with a Stamp ceremony; ledger history preserved.
</success_criteria>

<output>
Create `.planning/phases/05.7-kid-redesign/05.7-05-SUMMARY.md` when done.
</output>
