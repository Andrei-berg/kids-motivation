---
phase: 05.7-kid-redesign
plan: 06
type: execute
wave: 2
depends_on: ["05.7-01", "05.7-02", "05.7-03"]
files_modified:
  - app/kid/achievements/page.tsx
  - app/kid/leaderboard/page.tsx
autonomous: true
requirements: [05.7-SC1, 05.7-SC2]

must_haves:
  truths:
    - "Awards screen has exactly 3 tabs: Бейджи / Стрики / Рейтинг, with the XP bar + level in the shared header above them (D-05)"
    - "The Rating tab ranks ALL children (N>=1) via rankChildren: top-3 podium + list (D-07, not a hardcoded 2-child compare)"
    - "A single-child family sees the 'Ты против себя' this-week-vs-last-week view instead of an empty leaderboard (D-08)"
    - "/kid/leaderboard returns 404 (route deleted, no redirect) and no internal link points to it (D-06)"
  artifacts:
    - path: "app/kid/achievements/page.tsx"
      provides: "3-tab Awards screen (badges/streaks/rating) with XP header"
      contains: "tabRating"
    - path: "app/kid/leaderboard/page.tsx"
      provides: "DELETED (route removed)"
  key_links:
    - from: "app/kid/achievements/page.tsx Rating tab"
      to: "rankChildren"
      via: "N-children podium/list"
      pattern: "rankChildren"
    - from: "app/kid/achievements/page.tsx"
      to: "Tabs atom"
      via: "theme-aware 3-tab shell"
      pattern: "Tabs"
---

<objective>
Merge the leaderboard into a 3-tab Awards screen (Badges / Streaks / Rating), rebuild the
Rating tab to rank N children via the podium+list logic (with the single-child "я против себя"
fallback), and delete the standalone /kid/leaderboard route (D-05, D-06, D-07, D-08).

Purpose: SC2 requires the leaderboard to live inside Awards and the nav to drop to 5 items.
This plan owns the achievements page and the leaderboard deletion.
Output: rebuilt achievements page with the 3-tab shell, deleted leaderboard route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md
@.planning/phases/05.7-kid-redesign/05.7-PATTERNS.md
@CLAUDE.md

<interfaces>
Atoms/logic: Tabs, Amount from '@/components/design/atoms'; ScreenHeader from '@/components/kid/design/ScreenHeader';
rankChildren from '@/lib/kid/rating-rank'; levelForXp from '@/lib/kid/level'; useDesktop from '@/lib/hooks/useDesktop'.
Tab labels: t('achievements.tabBadges'|'tabStreaks'|'tabRating'); solo copy: t('achievements.soloCompareHeading'|'soloCompareEmpty').
N-children query to REUSE (app/kid/leaderboard/page.tsx:42-60) — fetches all role='child' members,
then per-child getChild/getWallet/getWeekScore/getChildBadges/getStreaks. DO NOT reuse the 2-child
`find(e => e.id !== activeMemberId)` compare (lines ~69-70) — feed all entries into rankChildren instead.
D-08 solo: api.getWeekScore(childId, thisWeekStart) vs api.getWeekScore(childId, lastWeekStart).
XP header: levelForXp + xpInLevel = xp % 1000 (already echoed at achievements page ~line 123).
Color rule (UI-SPEC): XP bar, level number, badge icons are NOT money → never gold; podium avatars use their own colors.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3-tab Awards shell (Badges/Streaks) + XP header</name>
  <files>app/kid/achievements/page.tsx</files>
  <read_first>
    - app/kid/achievements/page.tsx (full — badge grid + streak cards ~145-301; xpInLevel ~123; local useDesktop ~6-15)
    - components/design/atoms.tsx (Tabs signature)
    - components/kid/design/ScreenHeader.tsx (from Plan 03)
    - .planning/phases/05.7-kid-redesign/05.7-CONTEXT.md (D-05 3 tabs + XP in header)
    - .planning/phases/05.7-kid-redesign/05.7-UI-SPEC.md (Color: XP/level/badges never gold; Typography)
  </read_first>
  <action>
    Rebuild the page around `<ScreenHeader title={t('kidHeader.awards')} .../>` with the XP bar +
    level (via `levelForXp(child.xp)` and `xp % 1000`) shown in/under the header (D-05), then a
    `<Tabs theme="paper" tabs={[badges,streaks,rating]} value onChange/>` controlling a tab state.
    Move the existing badge-grid content under the Badges tab and the streak cards under the Streaks
    tab, reskinned to paper tones/atoms (reuse the existing content, restyle only). Swap the local
    useDesktop for the shared hook. The XP bar, level number, and badge icons must NOT use gold
    (gold = money only). The Rating tab body is Task 2.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "Tabs" app/kid/achievements/page.tsx && grep -q "levelForXp" app/kid/achievements/page.tsx && grep -q "tabRating" app/kid/achievements/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Page renders a 3-item `Tabs` (badges/streaks/rating) with `t('achievements.tabBadges'|'tabStreaks'|'tabRating')`
    - XP bar + level derived via `levelForXp`; not colored with goldText
    - `ScreenHeader` mounted with the awards title; `useDesktop` from the shared hook
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Awards screen has the 3-tab shell + XP header; badges/streaks tabs reskinned.</done>
</task>

<task type="auto">
  <name>Task 2: Rating tab (N-children podium+list + solo fallback) + delete leaderboard route</name>
  <files>app/kid/achievements/page.tsx, app/kid/leaderboard/page.tsx</files>
  <read_first>
    - app/kid/leaderboard/page.tsx (full — N-children query 42-60; the 2-child compare 69-70 to NOT port; me-vs-sibling stat block)
    - lib/kid/rating-rank.ts (rankChildren signature from Plan 02)
    - lib/api.ts (getWeekScore signature; getWeekRange/addDays helpers if present in utils)
    - .planning/phases/05.7-kid-redesign/05.7-RESEARCH.md (Pitfall 5 N-children; Caveat: getWeekScore is an approximation — do NOT "fix" it)
    - .planning/phases/05.7-kid-redesign/05.7-CONTEXT.md (D-06 delete route, D-07 podium, D-08 solo)
  </read_first>
  <action>
    Build the Rating tab: reuse the leaderboard's all-children query, map each child to a
    `RankEntry` ({ id, name, coinsWeek, avatarUrl, ...secondary stats }), pass the full array to
    `rankChildren` and render top-3 as a podium (avatars; must look good with only 2 children) and
    the remainder as a list, secondary stats as chips. When `entries.length === 1`, render the
    single-child "я против себя" view: `t('achievements.soloCompareHeading')` with this-week vs
    last-week from two `api.getWeekScore` calls (current week start and start-7d); if no prior-week
    data, show `t('achievements.soloCompareEmpty')`. Gold only on coin figures (via `Amount`);
    podium ranks/medals use non-gold accent/success tones. Then DELETE `app/kid/leaderboard/page.tsx`
    (remove the file so the route 404s — no redirect). Grep for any remaining `/kid/leaderboard`
    href/router.push references in the files THIS plan owns (the achievements page) and remove them.
    KidNav's `/kid/leaderboard` removal is owned by Plan 08 (do NOT touch KidNav here); Plan 12's
    Wave-4 phase-wide remnant sweep catches any straggler links across the whole kid surface. The
    automated verify below is scoped to `app/kid/achievements` only so it stays independent of the
    Plan 08 KidNav rewrite (same Wave 2, no depends_on edge between 06 and 08).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "rankChildren" app/kid/achievements/page.tsx && grep -q "soloCompareHeading" app/kid/achievements/page.tsx && test ! -f app/kid/leaderboard/page.tsx && ! grep -rq "kid/leaderboard" app/kid/achievements && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Rating tab feeds all children into `rankChildren`; no `.find(e => e.id !==` 2-child compare remains
    - Single-child case renders `t('achievements.soloCompareHeading')` using two `getWeekScore` calls
    - `app/kid/leaderboard/page.tsx` no longer exists; no `/kid/leaderboard` reference in `app/kid/achievements` (KidNav is Plan 08's; Plan 12 sweeps the rest phase-wide)
    - Coin figures via `Amount` (gold); podium medals non-gold
    - `npx tsc --noEmit` and `npm test` exit 0
  </acceptance_criteria>
  <done>Rating tab ranks N children with a solo fallback; leaderboard route deleted with no dangling links.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client(child) → other children's weekly scores | Rating reads sibling scores within the same family under existing family-RLS. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-057-11 | Information Disclosure | Rating cross-child reads | mitigate | Query scopes to role='child' members of the child's own family; existing RLS on children/wallet/streaks confines to the family — no cross-family exposure. |
| T-057-12 | Tampering | rating rank ordering | accept | Ranking is client-side display over RLS-scoped reads; no money moved, no writes. |
| T-057-SC | Tampering | package installs | accept | N/A — no packages installed. |
</threat_model>

<verification>
`npx tsc --noEmit` + `npm test` green; 3-tab Awards; N-children podium + solo fallback; leaderboard route gone; no dangling links.
</verification>

<success_criteria>
Leaderboard folded into a 3-tab Awards Rating view (N children + solo fallback); standalone route deleted.
</success_criteria>

<output>
Create `.planning/phases/05.7-kid-redesign/05.7-06-SUMMARY.md` when done.
</output>
