---
phase: pre-02.1-bugfix
task: 1
total_tasks: 2
status: blocked
last_updated: 2026-03-08T19:50:42.636Z
---

<current_state>
СТОП — app сломан. Нужно починить ПЕРЕД тем как планировать Phase 2.1 (Coin Engine).

Основная проблема: useFamilyMembers читает из старой таблицы `children` (id = 'adam'/'alim'),
а вся новая система использует `family_members` (UUID). После Phase 1.4 мигрировали на activeMemberId (UUID),
но useFamilyMembers остался на старом API.
</current_state>

<completed_work>

- lib/ рефакторинг ПОЛНОСТЬЮ завершён и закоммичен:
  - lib/models/ — 7 файлов типов (child, wallet, flexible, expense, category, schedule, family)
  - lib/repositories/ — 6 репозиториев (children, grades, wallet, schedule, expenses, categories)
  - lib/services/ — 3 сервиса (coins, badges, streaks)
  - lib/*.ts — все стали thin re-export wrappers
  - npx tsc --noEmit → 0 ошибок
- ai-context/constraints.md обновлён (lib/ refactor status = COMPLETE)
- MEMORY.md обновлён

НО: изменения ещё НЕ закоммичены в git (untracked: lib/models/, lib/repositories/, lib/services/)

</completed_work>

<remaining_work>

1. СРОЧНО: закоммитить lib/ рефакторинг (все новые файлы untracked)
2. СРОЧНО: починить useFamilyMembers — переписать на family_members таблицу
3. СРОЧНО: починить dashboard — api.getChild(activeMemberId) падает потому что ищет UUID в таблице children (где id = 'adam'/'alim')
4. После фикса: планировать Phase 2.1 (/gsd:plan-phase 2.1)

</remaining_work>

<decisions_made>

- lib/ рефакторинг: old lib/*.ts → thin wrappers, implementations в repositories/ и services/
- Backward compat полный: все старые import paths работают
- useFamilyMembers НЕ трогали (это следующая задача)

</decisions_made>

<blockers>

КРИТИЧЕСКИЙ: useFamilyMembers (lib/hooks/useFamilyMembers.ts) вызывает api.getChildren()
→ таблица `children` (legacy, id = 'adam'/'alim')
→ новые пользователи не имеют записей в children → возвращает []
→ activeMemberId = null → dashboard не загружается

dashboard/page.tsx: api.getChild(activeMemberId) — ищет по UUID в children, но там TEXT id.

FIX PLAN для useFamilyMembers:
- Вместо api.getChildren() использовать onboarding-api.ts: getChildMembers(familyId)
  (функция из lib/onboarding-api.ts, читает из family_members WHERE role='child')
- familyId получать из store (useAppStore) или из supabase session

FIX PLAN для dashboard:
- Вместо api.getChild(activeMemberId) использовать данные из useFamilyMembers (members[])
- Т.е. активный child = members.find(m => m.id === activeMemberId)
- Либо создать getChildMember(memberId) в onboarding-api.ts

</blockers>

<context>
Весь день работали над Phase 1.x (auth, onboarding, categories, dashboard refactor).
Phase 1.4 мигрировала 8 страниц на activeMemberId (UUID из family_members).
useFamilyMembers должен был тоже мигрироваться но остался на старом api.getChildren().
Это создаёт несоответствие: store хранит UUID, а hook загружает данные по старому TEXT id.

Пользователь не может попасть в dashboard потому что:
1. Middleware: если нет записи в family_members → redirect /onboarding (OK для новых)
2. Если есть family_members НО dashboard падает при загрузке → белый экран

Редирект с / на /dashboard работает (middleware корректный).
Проблема в самом dashboard: он падает при api.getChild(UUID) т.к. ищет UUID в children table.
</context>

<next_action>
1. Сначала закоммитить lib/ рефакторинг:
   git add lib/models/ lib/repositories/ lib/services/ lib/api.ts lib/wallet-api.ts lib/flexible-api.ts lib/expenses-api.ts lib/categories-api.ts lib/schedule-api.ts lib/badges.ts lib/streaks.ts ai-context/
   git commit -m "refactor(lib): extract models/, repositories/, services/ — thin wrappers"

2. Переписать lib/hooks/useFamilyMembers.ts:
   - Использовать createClient() из lib/supabase/client.ts
   - Читать из family_members WHERE role='child' AND family_id = <familyId из store>
   - familyId: добавить в store.ts (useAppStore) или получать из supabase session metadata

3. Починить dashboard: убрать api.getChild(), использовать данные из useFamilyMembers
</next_action>
