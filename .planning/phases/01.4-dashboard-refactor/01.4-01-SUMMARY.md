---
phase: 01.4-dashboard-refactor
plan: "01"
subsystem: ui
tags: [zustand, supabase, react-hooks, multi-tenant, family-members, rls]

# Dependency graph
requires:
  - phase: 01.1-db-schema
    provides: family_members table with RLS, get_my_family_ids() helper
  - phase: 01.3-flexible-categories
    provides: lib/supabase/client.ts createClient(), familyId + activeMemberId in Zustand store

provides:
  - "useFamilyMembers hook — fetches authenticated user's family and children from family_members table"
  - "FamilyMember interface matching family_members table (id, display_name, avatar_url, role)"
  - "lib/store.ts cleaned: activeMemberId (UUID) is sole child identifier, no legacy childId"

affects:
  - 01.4-02
  - 01.4-03

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Child-role users receive filtered members array (only themselves) via role check after DB fetch"
    - "activeMemberId auto-selected on mount if null or stale (not in fetched list)"
    - "cancelled flag in useEffect cleanup prevents state updates after unmount"

key-files:
  created:
    - lib/hooks/useFamilyMembers.ts
  modified:
    - lib/store.ts

key-decisions:
  - "childId and setChildId removed from store — activeMemberId (UUID) is sole child identifier from Plan 01.4-01 onward"
  - "REQ-FAM-011 deferred to Phase 2.x — Phase 1.1 RLS already enforces DB-level family isolation; delete UI not in 1.4 scope"
  - "REQ-SEC-002 pre-satisfied — app/settings/page.tsx PIN gate (verifyPin import + pinInput state) is untouched in this phase"
  - "useFamilyMembers queries family_members twice per page load (caller row + all children) — acceptable for a family app with 2-3 children"
  - "Stale activeMemberId from localStorage silently replaced: if not found in fetched members, auto-select members[0]"

patterns-established:
  - "useFamilyMembers: 'use client' hook, useEffect with cancelled flag, two Supabase queries, role-based filter"

requirements-completed:
  - REQ-FAM-010
  - REQ-FAM-011
  - REQ-SEC-002

# Metrics
duration: 2min
completed: 2026-03-08
---

# Phase 01.4 Plan 01: Dashboard Refactor — Foundation Summary

**useFamilyMembers hook with child-role isolation filter + Zustand store stripped of legacy childId/setChildId fields**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-03-08T02:05:30Z
- **Completed:** 2026-03-08T02:07:30Z
- **Tasks:** 3 (2 code, 1 audit)
- **Files modified:** 2

## Accomplishments
- Created `lib/hooks/useFamilyMembers.ts` — the single source of truth for family member data used by NavBar and all pages downstream
- Removed legacy `childId` and `setChildId` from `lib/store.ts` — store now holds only `familyId`, `setFamilyId`, `activeMemberId`, `setActiveMemberId`
- Confirmed REQ-FAM-011 (deferred) and REQ-SEC-002 (pre-satisfied) with audit grep

## Task Commits

Each task was committed atomically:

1. **Task 1: Create useFamilyMembers hook** - `d8720f6` (feat)
2. **Task 2: Migrate lib/store.ts — remove legacy childId** - `da9750d` (refactor)
3. **Task 3: Audit REQ-FAM-011 and REQ-SEC-002** - no commit (audit only, no files modified)

## FamilyMember Interface (exact TypeScript)

```typescript
export interface FamilyMember {
  id: string
  display_name: string
  avatar_url: string | null
  role: 'parent' | 'child' | 'extended'
}
```

## Updated AppStore Interface (exact TypeScript)

```typescript
interface AppStore {
  // Multi-tenant fields (Phase 1.4+)
  familyId: string | null
  setFamilyId: (id: string | null) => void
  activeMemberId: string | null     // family_members.id of the selected child
  setActiveMemberId: (id: string | null) => void
}
```

## Requirement Statuses

### REQ-FAM-010 — Child sees only their own pill button
IMPLEMENTED in Task 1. After fetching all children in the family, the hook applies:
```typescript
const filteredChildren = membership.role === 'child'
  ? allChildren.filter(m => m.id === membership.id)
  : allChildren
```
A child user's `useFamilyMembers()` returns a single-element array containing only their own entry.

### REQ-FAM-011 — Delete family member, parent only
DEFERRED to Phase 2.x. No delete UI is built in Phase 1.4.
Pre-existing coverage: Phase 1.1 established Row Level Security policies on `family_members` (via `get_my_family_ids()` helper and family-scoped RLS). DB-level protection is in place. Application-level delete UI is scheduled for Phase 2.x.

### REQ-SEC-002 — Parent PIN for settings
PRE-SATISFIED. `app/settings/page.tsx` has a working parent PIN gate implemented in Phase 1.3:
- `import { verifyPin } from '@/utils/helpers'`
- `const [pinInput, setPinInput] = useState('')`
- `// PIN gate` comment block at line 28
Phase 1.4 does not touch `app/settings/page.tsx`.

## Files Created/Modified
- `lib/hooks/useFamilyMembers.ts` — New hook: fetches caller's membership row + all family children, applies role filter, auto-selects activeMemberId
- `lib/store.ts` — Removed `childId: 'adam' | 'alim'` and `setChildId` (5 lines removed, legacy comment removed)

## Decisions Made
- Kept `{ name: 'v5_child' }` persist key unchanged — Zustand silently ignores stale `childId` value in localStorage from old sessions
- TypeScript errors in consumer files (dashboard, wallet, analytics, etc.) are expected and intentional — deferred to plans 02 and 03
- Used `cancelled` cleanup flag in useEffect to prevent setState on unmounted component (common React pattern for async hooks)

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## Next Phase Readiness
- `useFamilyMembers` hook and cleaned `lib/store.ts` are ready for Plan 02 (NavBar wiring) and Plan 03 (page migration)
- Consumer files (`app/dashboard/page.tsx`, `app/wallet/page.tsx`, etc.) will have TypeScript errors until Plan 02/03 replaces `childId` references with `activeMemberId`
- No blockers

---
*Phase: 01.4-dashboard-refactor*
*Completed: 2026-03-08*
