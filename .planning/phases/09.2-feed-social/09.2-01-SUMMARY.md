---
phase: 09.2-feed-social
plan: 01
subsystem: api
tags: [feed, tease, feed.repo, pure-module, vitest]

# Dependency graph
requires:
  - phase: 09.1-feed-recognition
    provides: family_events / family_event_comments schema, FeedEventKind union, RAIL_COLOR_MAP pure-lookup-table convention (lib/kid/feed-highlights.ts)
provides:
  - lib/kid/tease-phrases.ts — locked 17-phrase D-04 tease copy, kind→group resolver with Routine fallback, isKnownTeasePhrase anti-forgery check
  - lib/repositories/feed.repo.ts tease helpers — addTeaseReply, getTeaseRepliesByFamily, isTeaseComment, teaseTextOf, hasTeased, TEASE_PREFIX convention
  - tease-blind getComments/getCommentCounts (teases never appear in plain comment thread or inflate the 💬 count)
affects: [09.2-04 (FamilyFeed.tsx TeaseButton/TeaseTray UI consumes these exports)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Reserved body-prefix tagging on an existing table (TEASE_PREFIX = '[[tease]] ') instead of a new column/table — zero migration, zero new RLS policy"
    - "Tag validity requires prefix AND exact membership in a locked phrase Set — prevents hand-typed forgery of the tag"

key-files:
  created:
    - lib/kid/tease-phrases.ts
    - tests/tease-phrases.test.ts
  modified:
    - lib/repositories/feed.repo.ts

key-decisions:
  - "TEASE_PREFIX = '[[tease]] ' (ASCII marker + trailing space), declared as a repo export next to the comment functions"
  - "addTeaseReply delegates to the existing addComment (single insert code path) rather than duplicating the Supabase call"
  - "LEGACY_TEASE_PHRASES kept as an empty array now, per the module's own documented removal convention, rather than omitted"

patterns-established:
  - "Pure lookup module for locked, non-freeform copy (tease-phrases.ts) mirrors feed-highlights.ts's Partial<Record<FeedEventKind,X>> + explicit-fallback shape"

requirements-completed: [FEED-06]

# Metrics
duration: ~25min
completed: 2026-09-19
---

# Phase 9.2 Plan 01: Tease Foundations (Non-UI) Summary

**Locked 17-phrase tease copy module + `[[tease]] `-prefix comment-tagging storage layer on `family_event_comments`, with tease rows filtered out of the plain comment thread/count — no UI, no migration.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-09-19T03:59:00Z (approx.)
- **Completed:** 2026-09-19T04:02:09Z
- **Tasks:** 2/2 completed
- **Files modified:** 3 (2 created, 1 modified)

## Accomplishments
- `lib/kid/tease-phrases.ts` — three frozen phrase arrays matching CONTEXT.md D-04 character-for-character (achievement 6 / routine 6 / purchase 5), `TEASE_PHRASES_BY_KIND` kind→group map, `teasePhrasesFor()` with a locked Routine fallback that can never return empty, `isKnownTeasePhrase()` exact-membership anti-forgery check, and a documented `LEGACY_TEASE_PHRASES` removal convention
- `lib/repositories/feed.repo.ts` gained a validated write path (`addTeaseReply`), a grouped read path (`getTeaseRepliesByFamily`), pure helpers (`isTeaseComment`, `teaseTextOf`, `hasTeased`), and made `getComments`/`getCommentCounts` tease-blind — all riding on the existing `family_event_comments` table with zero schema change

## Task Commits

Each task was committed atomically:

1. **Task 1: lib/kid/tease-phrases.ts — D-04 locked phrase groups + resolver** - `6da55c4` (feat)
2. **Task 2: feed.repo.ts — tease storage, read, and comment-stream separation (D-06, D-07)** - `c30ed91` (feat)

_No plan-metadata commit — worktree mode; orchestrator handles shared STATE.md/ROADMAP.md updates after wave merge._

## Files Created/Modified
- `lib/kid/tease-phrases.ts` - New pure lookup module: 17 locked phrases, kind→group map, `teasePhrasesFor`, `isKnownTeasePhrase`
- `tests/tease-phrases.test.ts` - Regression suite (group lengths, fallback identity, membership test)
- `lib/repositories/feed.repo.ts` - Appended tease storage/read helpers; `getComments`/`getCommentCounts` made tease-blind; `addComment`/`addReaction`/`removeReaction`/`getFeed`/`getFeedUnreadCount`/`subscribeFeed`/localStorage helpers untouched (CLAUDE.md preserve-original-code rule)

## Decisions Made
- Followed the plan's locked technical choices exactly: reserved body-prefix (`TEASE_PREFIX`) over a new `metadata` column, Routine-group fallback for uncategorized kinds, `addTeaseReply` delegating to `addComment` rather than a parallel insert.
- `LEGACY_TEASE_PHRASES` implemented as an empty array (not omitted) so the module's own documented "move a removed phrase here" convention has a concrete landing spot from day one, per the header-comment requirement in the plan's Task 1 action.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required. No migration, no RLS change, no new package (`git diff --numstat package.json package-lock.json` and `git diff --numstat supabase/migrations/` both empty, matching the plan's verification block).

## Known Stubs

None — both new exports (`tease-phrases.ts`, the `feed.repo.ts` tease helpers) are fully wired, pure/testable, and covered by `tests/tease-phrases.test.ts`. No UI consumes them yet by design (plan 09.2-04 owns `TeaseButton`/`TeaseTray`); `grep -rn "QUICK_EMOJI\|TeaseButton" components/feed/FamilyFeed.tsx` confirms no tease UI exists yet, matching this plan's stated non-UI scope.

## Next Phase Readiness

- Plan 09.2-04 can import `teasePhrasesFor`, `isKnownTeasePhrase`, `addTeaseReply`, `getTeaseRepliesByFamily`, `hasTeased`, and `teaseTextOf` directly to build the `TeaseButton`/`TeaseTray`/tease-reply-bubble UI with no further repo/data-layer work needed.
- No blockers. `npx tsc --noEmit`, `npm run lint`, and `npm test` all exit 0 on the final state of this plan's branch.

---
*Phase: 09.2-feed-social*
*Completed: 2026-09-19*

## Self-Check: PASSED

- FOUND: lib/kid/tease-phrases.ts
- FOUND: tests/tease-phrases.test.ts
- FOUND: .planning/phases/09.2-feed-social/09.2-01-SUMMARY.md
- FOUND: 6da55c4 (Task 1 commit)
- FOUND: c30ed91 (Task 2 commit)
- FOUND: d4d348a (SUMMARY commit)
