---
phase: 09.2-feed-social
plan: 04
subsystem: testing
tags: [vitest, supabase, integration-test, medals, feed, rls]

# Dependency graph
requires:
  - phase: 09.2-feed-social (plan 02)
    provides: "app/actions/send-kid-medal.ts (sendKidMedal), lib/kid/medal-phrases.ts, medals.sender_role/sender_member_id + medals_recipient_day_role/medals_kid_sender_day unique indexes"
provides:
  - "tests/integration/kid-medal.test.ts — 9-test live-DB regression suite for sendKidMedal"
affects: [09.2-feed-social, feed-social-verification]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Mock only requireFamilyMember at the auth boundary; run createAdminClient/assertChildInFamily/the medals insert/emitFeedEvent for real against a disposable __test__ family (house pattern from behavior-approval.test.ts)"

key-files:
  created: [tests/integration/kid-medal.test.ts]
  modified: []

key-decisions:
  - "Split the plan's two tasks into two commits against the same file (Task 1: scaffolding + happy path + money-safety; Task 2: guard/cap matrix), matching the plan's task boundaries exactly"
  - "Every rejection-case assertion checks the DB row count is unchanged, not just success:false, per the plan's explicit anti-pattern warning (a guard that inserts then errors would otherwise pass)"

requirements-completed: [FEED-07]

# Metrics
duration: 4min
completed: 2026-09-19
---

# Phase 09.2 Plan 04: Kid Medal Integration Tests Summary

**9-test live-DB suite proving sendKidMedal's coins-0 economy safety, sender-named feed event, and all five rejection/cap paths (D-08..D-11), backstopped at the Postgres unique-index level (23505)**

## Performance

- **Duration:** 4 min (commit-to-commit; environment setup — worktree base correction, `npm ci`, `.env.local` copy — took longer but is one-time infra, not plan work)
- **Started:** 2026-09-19T07:15:36+03:00 (first task commit)
- **Completed:** 2026-09-19T07:16:57+03:00 (second task commit)
- **Tasks:** 2/2 completed
- **Files modified:** 1 created

## Accomplishments

- `tests/integration/kid-medal.test.ts` proves, against the live DB: a coins-0 medal write with the exact sender-named feed event shape (D-12); zero wallet/wallet_transactions movement (D-08); rejection of off-list phrases (D-11), self-sends, non-child callers, and cross-family targets, each with zero rows written; the sender cap is per-day-total across recipients, not per-recipient (D-10); the recipient cap is per-sender-role so a same-day parent medal and kid medal coexist (D-09); and a raced duplicate insert hits the DB-level unique index with Postgres error `23505`.
- Suite is fully self-provisioning (two `__test__` families, a hand-rolled second sibling, and a real synthetic auth user for the sender child) and fully self-cleaning — verified the `__test__` family count returns to 0 after every run.
- `describe.skipIf(!hasIntegrationEnv)` keeps `npm test` green (154 passed, 66 skipped, including this file's 9 tests) when no `.env.local` is loaded, and all 9 tests pass when integration env is present.

## Task Commits

Each task was committed atomically:

1. **Task 1: Suite scaffolding + happy path + money-safety assertions (D-08, D-12)** - `0cbc1a4` (test)
2. **Task 2: Guard and cap matrix (D-09, D-10, D-11, sibling/self/cross-family)** - `5a4a4b5` (test)

**Plan metadata:** this summary's commit (docs: complete plan)

## Files Created/Modified

- `tests/integration/kid-medal.test.ts` - Live-DB integration suite for `sendKidMedal`: fixture setup (two `__test__` families, second sibling, auth-backed sender child + `family_members` row), `asSender()` helper, 2 happy-path/money-safety tests (Task 1), and a `describe('guards and caps')` block with 7 rejection/cap cases (Task 2)

## Decisions Made

- Split the single plan file's two tasks into two separate commits, both touching `tests/integration/kid-medal.test.ts`, matching the plan's stated task boundary (Task 1 = scaffolding + 2 tests; Task 2 = 7 more tests appended to the same file) rather than one combined commit.
- Every guard/cap rejection test asserts the DB row count for the scoped `family_id`/`child_id` stays at 0, not merely `success === false`, per the plan's explicit warning that a guard which inserts and then returns an error would otherwise pass silently.
- Recipient-cap coexistence test (D-09) inserts the parent-style medal row directly via the service client rather than calling the parent `sendMedal` action, avoiding any dependency on a parent session or the wallet credit path (as instructed by the plan).

## Deviations from Plan

None - plan executed exactly as written. Two environment-setup items were needed to make execution possible in this fresh worktree, both explicitly pre-authorized by the parallel-execution instructions and not scope changes to the plan itself:
- Copied `.env.local` from the main checkout into the worktree (gitignored, not committed) — the parallel-execution prompt explicitly anticipated this for a fresh worktree.
- Ran `npm ci` to restore `node_modules` from the committed `package-lock.json` (the fresh worktree had none) — a dependency restoration from the existing lockfile, not a new package install, so it does not fall under the Rule 3 package-manager-install exclusion (no new/renamed package was added).

## Issues Encountered

- The worktree's initial base commit (`79a2387`) predated the wave-1 merges that landed Plan 09.2-02's `sendKidMedal` action and the `medal-sender.sql` migration this plan's tests depend on. The mandatory pre-execution `<worktree_branch_check>` step detected the mismatch (`git merge-base HEAD <expected-base>` != expected base) and corrected it via `git reset --hard` to the orchestrator-specified base commit before any task work began — resolved per the documented protocol, not an ad hoc fix.
- Verified the `2026-09-19-medal-sender.sql` migration (`sender_role`, `sender_member_id` columns + `medals_recipient_day_role`/`medals_kid_sender_day` unique indexes) was actually applied to the live DB before writing tests against it — confirmed via a live `select` query, avoiding the "ROADMAP complete ≠ migration ran" class of bug noted in project history.

## User Setup Required

None - no external service configuration required. No production files were modified (`git status --porcelain app lib components supabase` is empty for both task commits).

## Next Phase Readiness

- FEED-07's kid-medal path now has a standing live-DB regression guard covering the full security-relevant surface (economy safety, sibling-only targeting, cross-family isolation, content allow-list, both independent daily caps, and the DB-level race backstop).
- No blockers for subsequent Phase 09.2 plans; this plan added test coverage only and touched no production code.

---
*Phase: 09.2-feed-social*
*Completed: 2026-09-19*
