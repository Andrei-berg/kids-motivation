---
phase: 09.2-feed-social
plan: 02
subsystem: api
tags: [supabase, postgres, server-actions, medals, family-feed, rls]

# Dependency graph
requires:
  - phase: 09.1-feed-recognition
    provides: family_events schema, emitFeedEvent(), FeedEventKind, 9.1 D-01 (no money on feed cards)
provides:
  - medals.sender_role / medals.sender_member_id columns + role CHECK on the live DB
  - medals_recipient_day_role (D-09) and medals_kid_sender_day (D-10) unique indexes
  - lib/kid/medal-phrases.ts — locked 8-phrase MEDAL_PHRASES + isKnownMedalPhrase server allow-list
  - app/actions/send-kid-medal.ts — sendKidMedal server action (coins=0, sibling-guarded, dual daily cap, sender-distinguishing feed emit)
  - app/actions/send-medal.ts made sender-role aware (independent parent-cap query/insert)
affects: [09.2-06 (medal composer UI consumes sendKidMedal), 09.2-07 (money-boundary re-check)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Sibling-target auth guard is module-local/non-exported in a 'use server' file — every exported function becomes a callable endpoint, so the guard itself must not be exported"
    - "Independent daily caps via two Postgres unique indexes (full + partial), with 23505 mapped back to the matching cap message so a concurrent-tap race degrades gracefully instead of a raw DB error"

key-files:
  created:
    - supabase/migrations/2026-09-19-medal-sender.sql
    - lib/kid/medal-phrases.ts
    - app/actions/send-kid-medal.ts
  modified:
    - app/actions/send-medal.ts

key-decisions:
  - "sender_role defaults to 'parent' so every pre-existing medals row backfills correctly without a data migration step"
  - "medals_kid_sender_day is a partial unique index (WHERE sender_role='child') and is never used as a PostgREST upsert conflict target — avoids the family_events_dedup partial-index-vs-upsert mistake from 2026-09-11"

requirements-completed: [FEED-07]

# Metrics
duration: ~25min
completed: 2026-09-19
---

# Phase 09.2 Plan 02: Sender-aware medal schema + kid-initiated medal send Summary

**New `sendKidMedal` server action lets a child send a sibling a zero-coin, phrase-locked medal behind a service-role guard, backed by a live-DB migration that makes the medal daily cap sender-aware (independent parent/kid caps via two Postgres unique indexes).**

## Performance

- **Duration:** ~25 min
- **Completed:** 2026-09-19T04:06Z
- **Tasks:** 3/3 completed
- **Files modified:** 4 (3 created, 1 modified)

## Accomplishments

- `medals` table on the live DB now has `sender_role` (NOT NULL, default `'parent'`, CHECK `IN ('parent','child')`), `sender_member_id`, and two cap-enforcing unique indexes — verified against prod catalogs, not just the migration file's presence
- 8 locked medal phrases (D-11) exist in a pure, server-importable module with an exact-match allow-list check
- `sendKidMedal` lets a child recognize a sibling at coins=0 (D-08), guarded against non-child callers, self-send, and cross-family targets, with independent sender-side (D-10) and recipient-side (D-09) daily caps and a sender-distinguishing feed event (D-12)
- `sendMedal` (parent path) made sender-role aware with a 2-insertion/1-deletion diff — its own daily cap and coin-crediting behavior are otherwise untouched

## Task Commits

1. **Task 1: Migration for sender-aware medal caps + D-11 phrase module** - `adb8bf2` (feat)
2. **Task 2: [BLOCKING] Apply the migration to the live DB and prove it landed** - no code commit (live-DB action only; see Migration Verification below)
3. **Task 3: sendKidMedal server action + sender-aware parent cap** - `e5b4894` (feat)

_Task 2 modified no files (the migration file from Task 1 was applied to the live database, not re-written), so there is no separate commit for it — the plan's `files_modified` entry for Task 2 is the same file already committed in Task 1._

## Files Created/Modified

- `supabase/migrations/2026-09-19-medal-sender.sql` - Adds `sender_role`/`sender_member_id` to `medals`, the `medals_sender_role_chk` CHECK, and the two cap-enforcing unique indexes; no new RLS policy
- `lib/kid/medal-phrases.ts` - `MEDAL_PHRASES` (8 locked strings) + `isKnownMedalPhrase()` exact-match allow-list
- `app/actions/send-kid-medal.ts` - `sendKidMedal()` server action: phrase allow-list → child-role/self-send/cross-family guard → server-derived sender identity → dual daily-cap check → insert at coins=0 → feed emit → push
- `app/actions/send-medal.ts` - Daily-cap query gained `.eq('sender_role', 'parent')`; insert gained `sender_role: 'parent'` (2 lines changed total, verified via `git diff --stat`)

## Migration Verification (Task 2)

**Pre-flight duplicate check** (against prod, before applying):
```sql
SELECT child_id, date, count(*) FROM medals GROUP BY 1,2 HAVING count(*) > 1;
```
Returned **0 rows** — safe to create `medals_recipient_day_role`.

**Applied:** 2026-09-19T04:03:02.541Z, via the documented `pg` one-liner (`npm i pg --no-save`; `SUPABASE_DB_URL` was present in `.env.local`).

**Post-apply catalog verification (against prod, not the file):**
- `information_schema.columns` for `medals`:
  ```json
  [
    { "column_name": "sender_member_id", "is_nullable": "YES", "column_default": null },
    { "column_name": "sender_role", "is_nullable": "NO", "column_default": "'parent'::text" }
  ]
  ```
- `pg_constraint`: `medals_sender_role_chk` present (1 row)
- `pg_indexes` for `medals`: `medals_recipient_day_role`, `medals_kid_sender_day`, `medals_pkey` — both new indexes present
- `SELECT count(*) FROM medals WHERE sender_role IS NULL` → `0` (backfill complete)
- `node --env-file=.env.local scripts/verify-migrations-applied.mjs` → exit 0, `26 tables / 47 columns / 20 functions / 105 policies (15 superseded, ok) / 38 indexes`, all 0 missing. No partial-index parser issue observed — the script's own regex captured `medals_kid_sender_day` correctly (its `create unique index ... on ...` clause is captured up to `on `, independent of the trailing `WHERE` filter).
- `git diff --numstat package.json package-lock.json` → no output (`pg` installed with `--no-save`)

## Decisions Made

- Sibling-target guard (`assertSiblingTarget`) written as a module-local, non-exported async function inside `send-kid-medal.ts` rather than reused/exported — matches the plan's explicit instruction that exporting it would publish a second server endpoint in a `'use server'` file
- `medals_kid_sender_day` kept as a genuinely partial index (not full) since nothing ever targets it as a PostgREST upsert conflict — the medal insert path uses `.insert()`, not `.upsert()`, so the `family_events_dedup` full-index lesson does not apply here
- `.env.local` copied from the main repo checkout into this worktree (git-ignored either way) so `SUPABASE_DB_URL` was available for the Task 2 DDL apply + verification scripts, per CLAUDE.md's documented migration path

## Deviations from Plan

### Notes (no code changes required)

**1. Three acceptance-criteria grep counts specified `returns 1` but the real count is 2, due to a necessary import line + a necessary usage line both matching the same identifier**
- **Found during:** Task 3 verification
- **Detail:** `grep -c "assertChildInFamily" app/actions/send-kid-medal.ts` → 2 (line 10 import, line 50 call). The pre-existing `app/actions/send-medal.ts` has the identical pattern for `requireParent` (`grep -c` → 2: import + call) — confirmed this is not something Task 3 introduced, it is structurally unavoidable whenever a named import is also invoked once, and `grep -c` counts matching *lines* not occurrences.
- **Action taken:** No code change — the underlying functional requirement (guard called exactly once, correctly) is met and verified by reading the call site directly (`grep -n`). Fixed the one instance that *was* a real over-count from my own added comment (`amount: null` comment text, Task 3) by rewording the comment so the real acceptance criterion (`returns 1`) now passes exactly.
- **Impact:** None on behavior. Documented so a reviewer isn't surprised the exact grep count differs from the plan's literal criterion for `assertChildInFamily` (send-kid-medal.ts) and `requireParent` (pre-existing in send-medal.ts).

**2. `npm run lint` (`next lint`) fails with a plugin-conflict error unrelated to this plan's code**
- **Found during:** Task 3 verification
- **Detail:** `next lint` reports: `Plugin "@next/next" was conflicted between ".eslintrc.json » eslint-config-next/core-web-vitals" and "../../../.eslintrc.json » eslint-config-next/core-web-vitals"`. Root cause: this worktree lives at `.claude/worktrees/agent-.../` nested inside the main repo's own directory tree, so ESLint's flat-config-adjacent resolution walks up and finds the main repo's `.eslintrc.json` in addition to the worktree's own copy, producing a duplicate-plugin conflict. This reproduces on a completely clean worktree with zero code changes (structural to the worktree nesting, not caused by this plan).
- **Action taken:** Verified the actual changed/created files are lint-clean directly: `npx eslint app/actions/send-kid-medal.ts app/actions/send-medal.ts lib/kid/medal-phrases.ts --no-eslintrc -c .eslintrc.json` → 0 errors, 0 warnings. Did not modify any lint/eslint config (out of scope for this task; a config fix would need to address the nested-worktree path issue, not this plan's files).
- **Impact:** None on code correctness. Flagging for whichever plan/task owns worktree tooling health, since every worktree-isolated plan in this wave will hit the same `npm run lint` failure.

---

**Total deviations:** 0 code deviations (Rules 1-4 not triggered); 2 verification notes documented above for reviewer context.
**Impact on plan:** None — all `<done>` and `<acceptance_criteria>` substantive requirements met; the two notes above are grep-count/tooling artifacts, not functional gaps.

## Issues Encountered

- `.env.local` was not present in this git worktree (it's git-ignored, and worktrees don't inherit untracked files from the main checkout) — copied it from `/Users/andrei/Projects/kids-motivation/.env.local` so Task 2's `pg`-based migration apply and verification scripts had `SUPABASE_DB_URL`. The copy is itself git-ignored and was not committed.

## User Setup Required

None - no external service configuration required. The migration was applied directly to the live database as required by Task 2 (a blocking gate task); no manual Supabase SQL Editor step was needed since `SUPABASE_DB_URL` was available.

## Next Phase Readiness

- `sendKidMedal` is ready to be wired into a composer UI in plan 09.2-06 (`grep -rn "sendKidMedal" app components --include="*.tsx"` currently returns nothing, confirming no premature UI coupling)
- The live `medals` schema change is already in prod, so 09.2-06 can build directly against it without a separate migration step
- Money-boundary re-check (plan 09.2-07, per the threat register's T-092-11) can grep this file for wallet imports and find none

## Self-Check: PASSED

- FOUND: supabase/migrations/2026-09-19-medal-sender.sql
- FOUND: lib/kid/medal-phrases.ts
- FOUND: app/actions/send-kid-medal.ts
- FOUND: app/actions/send-medal.ts (modified)
- FOUND commit: adb8bf2
- FOUND commit: e5b4894

---
*Phase: 09.2-feed-social*
*Completed: 2026-09-19*
