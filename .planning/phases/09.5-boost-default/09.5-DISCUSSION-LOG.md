# Phase 9.5: boost-default - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-20
**Phase:** 9.5-boost-default
**Areas discussed:** Tap target & container, Segmented-bar content mapping, Empty/zero-week state, boost_style control placement & default

---

## Tap target & container

| Option | Description | Selected |
|--------|-------------|----------|
| Все три BoostMeter | Day hero full + Day hero compact + ProfileSheet all tappable | ✓ |
| Только Day hero (обе версии) | ProfileSheet stays non-tappable | |
| Только полная версия на Day hero | Narrowest option | |

**User's choice:** Все три инстанса.

| Container option | Description | Selected |
|--------|-------------|----------|
| Bottom sheet (reuse 9.4's BottomSheet) | Content-agnostic shell | ✓ (after clarification) |
| Отдельный полноэкранный роут (/kid/boost) | New route + nav | |
| Модальная карточка по центру | New pattern, not in codebase | |

**User's choice:** initially "делай так чтобы в будущем не переделывать с запасом" (free text,
future-proofing ask). Claude proposed BottomSheet as the content-agnostic answer (container stays
identical across 9.5/9.6, only the body swaps) and the user confirmed.

**Close behavior:** Явное закрытие (X / swipe-down), matching Phase 9.4's `BottomSheet` — selected
directly, no alternative considered further.

---

## Segmented-bar content mapping

| Question | Options | Selected |
|----------|---------|----------|
| Grades sub-bar fill basis | По доле до следующего тира (topGradeCount/порог) vs. По достигнутому тиру (0/33/66/100%) | По доле до следующего тира |
| Penalty-disqualified state | Отдельный визуальный статус (приглушённый/красный + hint) vs. Просто пустой бар 0% | Отдельный визуальный статус |
| Consistency bar structure | Один бар, два сегмента внутри vs. Три отдельных бара (grades/full-week/streak) | Один бар, два сегмента (free text: "не знаю делай топ так чтобы вау" — Claude proposed two-zone bar with spring easing + completion glow-pulse; user accepted implicitly by moving on) |
| Overall total row | Да, итоговая строка внизу vs. Нет, двух баров достаточно | Да, итоговая строка внизу |

**Notes:** For the consistency-bar "вау" request, Claude proposed: single bar, two independently
filling zones (full-week + streak-tier), each with its own status chip underneath, using the
sketch's validated spring easing plus a completion glow-pulse (reduced-motion guarded). Captured
as D-06 with exact pulse tuning left to implementation.

---

## Empty/zero-week state

| Option | Description | Selected |
|--------|-------------|----------|
| Обычные пустые бары + полный hint | No new state, existing hint text self-explanatory | ✓ (Claude's pick after user deferred: "верю тебе сделай чтобы вау") |
| Отдельное "start of week" сообщение | New UI state not in sketch | |

**Notes:** User deferred to Claude ("ещё бы понять визуально давай верю тебе сделай чтобы вау").
Claude chose the no-new-state option plus an optional implementer-level shimmer/pulse touch, to
avoid inventing a new state machine while still keeping the "wow" ask in scope as a CSS-level
detail.

---

## boost_style control placement & default

| Option | Description | Selected |
|--------|-------------|----------|
| Да, полностью тот же паттерн (own radiogroup, mirrors fill-style.ts) | Separate block from fill_style | |
| Объединить в один контрол с fill_style | Single "Style" section, two picker rows | ✓ |

**User's choice:** Объединить в один контрол с fill_style — one "Style" section in `ProfileSheet`
with two radiogroups (fill style + boost style) instead of two separate blocks.

**Default:** `segmented-bar` (only real option this phase) — stated directly, not asked as a
question (no genuine second choice existed yet).

---

## Claude's Discretion

- Exact glow-pulse timing/intensity for the consistency-bar segment completion (D-06)
- Exact shimmer/pulse treatment (if any) for the empty-week state (D-08)
- Exact wording of the penalty-disqualified hint text (D-05)
- Whether the `boost_style` migration is bundled with this phase's first plan or split out

## Deferred Ideas

- **Loot-box/crate reward mechanic** (Brawl Stars-style weekly reward crate, variable
  rarity/bonus contents) — raised again during this discussion (user asked Claude to research
  options online). Redirected: this is a new economy/reward-delivery capability, not a
  boost-detail visualization style, and was already logged as a deferred idea in Phase 9.4's
  context. Not researched in this session per the scope guardrail. Candidate for a dedicated
  future phase (schema implications for `wallet_settings`/`rewards`, drop-rate transparency vs.
  the app's "every consequence visible" principle, COPPA-adjacent considerations).
