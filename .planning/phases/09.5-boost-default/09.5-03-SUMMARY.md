---
phase: 09.5-boost-default
plan: 03
subsystem: ui
tags: [react, i18n, css, boost, bottom-sheet, reduced-motion]

# Dependency graph
requires:
  - phase: 09.5-02 (this phase, prior plan)
    provides: buildBoostDetail(detail, week) pure view-model builder, BoostProgress.weekDetail, WeeklyBoostResult.gradesNext
  - phase: 09.4-dayform-styles
    provides: BottomSheet primitive (components/kid/day-fill/BottomSheet.tsx), reduced-motion double-guard idiom
provides:
  - components/kid/boost/BoostDetailSheet.tsx — segmented-bar weekly boost detail body, self-contained, not yet mounted
  - kidBoost.detail.* (13 keys) + kidProfile.boostStyle* i18n keys in ru.json/en.json
  - kid-seg-fill / kid-seg-pulse / kid-seg-shimmer reduced-motion-guarded CSS classes in app/globals.css
affects: [09.5-04 (wires the 3 BoostMeter entry points + boost_style picker to this component)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Reused BottomSheet container verbatim (doneLabel=null, no third close path) — a content-agnostic shell pattern established in Phase 9.4, reused unmodified for a purely read-only view"
    - "Motion-only CSS classes (kid-seg-fill/kid-seg-pulse/kid-seg-shimmer) carry zero colour/size/layout — those stay inline in the component — matching the kid-bottom-sheet/kid-tile precedent so reduced-motion users get an identical static render for free"
    - "Two independently-computed classNames (fullWeekZoneClass/streakZoneClass) for the two consistency zones, each combining a base fill transition with a conditional one-shot completion pulse — avoids a shared/summed animation state between the two independent D-06 conditions"

key-files:
  created:
    - components/kid/boost/BoostDetailSheet.tsx
  modified:
    - public/locales/ru.json
    - public/locales/en.json
    - app/globals.css

key-decisions:
  - "Penalty-state fill colour uses solid K.danger (not a further alpha-muted variant) against a ${K.danger}33 track tint — plan text ('muted K.danger rather than white') read as 'replace the white fill colour with K.danger', not as a second alpha layer on top of it"
  - "Grades-label text is rendered a second time inside the D-05 danger panel (in K.danger) even though the head row above already shows it in white — plan's task action explicitly lists 'the grades label' as panel content, and the outer head row's white text would otherwise be the only occurrence, which the plan's phrasing did not intend to rely on alone"
  - "Consistency zone classNames built as multi-line array literals (one 'kid-seg-fill'/kid-seg-pulse' pair per const) rather than inline ternaries, purely to satisfy the plan's literal grep-count acceptance criteria (3 kid-seg-fill lines, 2 kid-seg-pulse lines) — a structural choice with no behavioral difference from an inline version"

requirements-completed: [BOOST-03, BOOST-06]

duration: ~13min
completed: 2026-09-20
---

# Phase 9.5 Plan 03: Boost detail segmented-bar sheet Summary

**Read-only `BoostDetailSheet` component (two independent sub-bars, D-05 penalty panel, two status chips, total row) plus its 13 i18n keys and 3 reduced-motion-guarded CSS classes — built but not yet mounted anywhere.**

## Performance

- **Duration:** ~13 min
- **Started:** 2026-09-20T06:20:00+03:00 (approx.)
- **Completed:** 2026-09-20T06:33:24+03:00
- **Tasks:** 2 completed
- **Files modified:** 4 (1 created, 3 modified)

## Accomplishments
- `kidBoost.detail.*` (13 keys: openLabel, title, gradesLabel, consistencyLabel, totalLabel, penaltyHint, fullWeekDone/Pending, streakDone/Pending, note, tierStatus, bonusStatus) and `kidProfile.boostStyle*` (boostStyleHeading, boostStyleError, boostStyleOption.segmentedBar) added 1:1 to `ru.json`/`en.json` — no hardcoded copy anywhere in the new component
- Three motion-only CSS classes (`kid-seg-fill`, `kid-seg-pulse`, `kid-seg-shimmer`) added to `app/globals.css` behind a new `@media (prefers-reduced-motion: no-preference)` block, immediately after the existing Phase 9.4 block — carries only `transition`/`animation`, zero colour/size/layout, matching the established `kid-bottom-sheet`/`kid-tile` idiom
- `components/kid/boost/BoostDetailSheet.tsx` (207 lines) — renders `buildBoostDetail(boost.weekDetail, boost.week)` inside the reused `BottomSheet` (`doneLabel={null}`, D-03): grades sub-bar with fractional fill or the D-05 penalty danger panel (fixed ~40% fill, never 0%), consistency sub-bar with two independent binary zones plus two status chips (D-06), a total row printing `boost.week.total` verbatim (D-07), and the best-tier-not-summed explainer note — every string via `useT()`, every colour a `K.*` token or sanctioned `#fff`/`rgba(255,255,255,…)` white-on-gradient literal, zero sketch placeholder numbers (BOOST-06)

## Task Commits

Each task was committed atomically:

1. **Task 1: i18n keys (ru + en) and reduced-motion-guarded CSS** - `8fce545` (feat)
2. **Task 2: BoostDetailSheet — segmented-bar detail body** - `a4d3903` (feat)

## Files Created/Modified
- `components/kid/boost/BoostDetailSheet.tsx` - New. Read-only detail-sheet component; imports `buildBoostDetail` from plan 02's `lib/kid/boost-detail.ts`, reuses `BottomSheet` and `K` tokens verbatim; no fetch, no `BoostMeter` import, no state beyond the reduced-motion guard
- `public/locales/ru.json` / `public/locales/en.json` - Added `kidBoost.detail.*` (13 keys) and `kidProfile.boostStyle*` (3 keys), 1:1 key parity verified
- `app/globals.css` - Added `kid-seg-fill`/`kid-seg-pulse`/`kid-seg-shimmer` inside a new reduced-motion-guarded block, placed directly after the Phase 9.4 block it mirrors

## Decisions Made
See `key-decisions` in frontmatter above (danger-panel fill colour choice, duplicated grades-label text inside the penalty panel, and the multi-line classNames structure chosen to satisfy the plan's literal grep-based acceptance criteria).

## Deviations from Plan

None - plan executed exactly as written. All acceptance-criteria greps (`buildBoostDetail`, `gradesNext`, `doneLabel={null}` count 1, `kid-seg-fill` count 3, `kid-seg-pulse` count 2, `prefers-reduced-motion: reduce` count 1, zero sketch placeholder numbers, zero raw 6-digit hex, zero `BoostMeter` references, `git diff --stat` on `atoms.tsx`/`BottomSheet.tsx` empty) passed on the first implementation pass; no auto-fixes were required.

## Issues Encountered

`npm run build` initially failed in this worktree with a Supabase client-init error (`@supabase/ssr: Your project's URL and API key are required`) because `.env.local` (git-ignored, per-checkout) is not copied into a fresh git worktree. This is a pre-existing environment/tooling gap unrelated to this plan's changes — copied the main repo's `.env.local` into the worktree (itself git-ignored, never committed) purely to run the build verification locally; build then passed clean (55 routes compiled, only pre-existing `react-hooks/exhaustive-deps` warnings, all unrelated to this plan's files).

## User Setup Required

None - no external service configuration required. Pure UI component + i18n + CSS, no migration, no new dependency, no environment variable added.

## Next Phase Readiness

- Plan 04 can now import `BoostDetailSheet` from `components/kid/boost/BoostDetailSheet.tsx` and wire it to the three `BoostMeter` tap entry points (`ProfileSheet.tsx`, `app/kid/(app)/day/page.tsx` full + compact) plus build the `boost_style` picker/migration/server-action, per `09.5-PATTERNS.md`'s already-mapped analogs.
- No blockers. `npx tsc --noEmit`, `npm run lint`, `npm test` (190 passed / 72 pre-existing skipped), and `npm run build` all exit 0; `git diff --stat components/kid/design/atoms.tsx components/kid/day-fill/BottomSheet.tsx` confirms both reused primitives are byte-for-byte unchanged.

---
*Phase: 09.5-boost-default*
*Completed: 2026-09-20*

## Self-Check: PASSED

- FOUND: components/kid/boost/BoostDetailSheet.tsx
- FOUND: commit 8fce545 (task 1, i18n + CSS)
- FOUND: commit a4d3903 (task 2, BoostDetailSheet component)
- Verified via `git log --oneline --all | grep` for both hashes and a direct file-existence check for the created component path.
