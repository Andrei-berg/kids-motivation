---
phase: 09.5-boost-default
plan: 05
type: execute
wave: 4
depends_on: [09.5-04]
files_modified:
  - project-docs/data-model.md
  - project-docs/system-architecture.md
autonomous: false
requirements: [BOOST-01, BOOST-02, BOOST-03, BOOST-06]

must_haves:
  truths:
    - "Every gate (typecheck, lint, tests, build) is green on the finished phase"
    - "children.boost_style is re-confirmed present in the LIVE database at phase close, not just at plan 01"
    - "Every phase decision D-01..D-10 has traceable code evidence"
    - "BOOST-01, BOOST-02, BOOST-03 and BOOST-06 each have named code evidence"
    - "project-docs documents boost_style and the boost detail view"
    - "A human has confirmed the detail view in a real browser, including the penalty state and reduced motion"
  artifacts:
    - path: "project-docs/data-model.md"
      provides: "boost_style column paragraph beside the fill_style one"
      contains: "boost_style"
    - path: "project-docs/system-architecture.md"
      provides: "weekly boost detail view + boost_style write path documented"
      contains: "BoostDetailSheet"
  key_links:
    - from: ".planning/phases/09.5-boost-default/09.5-05-SUMMARY.md"
      to: "code evidence per decision"
      via: "D-01..D-10 traceability matrix"
      pattern: "D-10"
    - from: "scripts/verify-boost-style-column.mjs"
      to: "prod Postgres"
      via: "phase-close live-schema re-check"
      pattern: "children_boost_style_check"
---

<objective>
Close phase 9.5: prove every gate is green, re-confirm the migration is still live in prod, trace
every decision and requirement to concrete code, document the new column and view in `project-docs`,
and get the operator's browser sign-off.

Purpose: this phase's success criteria are interaction- and data-accuracy-shaped (a tap opens a real
breakdown; every number traces to `boost-rules`), so automated gates prove the wiring and a human
proves the feel. This mirrors the phase-close pattern recorded as decisions [Phase 09.3-06] and
[Phase 09.4-05] in STATE.md, which exists specifically because a ROADMAP "Complete" marker has
previously not matched reality in this project (the Phase 1.3 migration incident).
Output: green gates, a live-schema re-check, a complete traceability matrix, corrected docs, and an
operator-approved phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09.5-boost-default/09.5-CONTEXT.md
@.planning/phases/09.5-boost-default/09.5-UI-SPEC.md
@.planning/phases/09.5-boost-default/09.5-01-SUMMARY.md
@.planning/phases/09.5-boost-default/09.5-02-SUMMARY.md
@.planning/phases/09.5-boost-default/09.5-03-SUMMARY.md
@.planning/phases/09.5-boost-default/09.5-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate sweep, live-schema re-check, traceability matrix, project-docs correction</name>
  <files>project-docs/data-model.md, project-docs/system-architecture.md</files>
  <read_first>
    - project-docs/data-model.md around line 224 (the `children.fill_style` paragraph — the exact
      wording and placement the `boost_style` paragraph mirrors)
    - project-docs/system-architecture.md around lines 128-140 (the `fill_style` → `KidDayFillForm`
      section — where the boost detail view's equivalent description belongs)
    - .planning/phases/09.5-boost-default/09.5-CONTEXT.md (the full D-01..D-10 decision list the
      matrix must cover)
    - .planning/REQUIREMENTS.md lines 44-62 and 100-110 (BOOST-01..06 wording and the phase mapping
      table whose status column this phase's requirements move out of `Pending`)
    - the four SUMMARY files from plans 01-04 (the as-built record the matrix cites)
  </read_first>
  <action>
    Run the full gate sweep and record each exit code in the summary: `npx tsc --noEmit`,
    `npm run lint`, `npm test`, `npm run build`.

    Re-run the live-schema check at phase close, not just at plan 01:
    `node --env-file=.env.local scripts/verify-boost-style-column.mjs` and
    `node --env-file=.env.local scripts/verify-migrations-applied.mjs`. If either cannot connect,
    report BLOCKED with the exact command and error rather than recording the phase as closed —
    a green build says nothing about whether the column exists in prod.

    Build a D-01..D-10 traceability matrix in the summary with one row per decision: decision ID,
    one-line restatement, the file(s) and symbol(s) implementing it, and the grep or test that
    proves it. Cover at minimum: D-01 (three wrapped `BoostMeter` mounts), D-02 (`BottomSheet`
    reuse), D-03 (`doneLabel={null}`), D-04 (fractional fill, `tests/boost-detail.test.ts`),
    D-05 (`PENALTY_FILL_PCT` + danger panel + `penaltyHint`), D-06 (two zones + two chips +
    `kid-seg-pulse` behind the reduced-motion guard), D-07 (total row = `boost.week.total`),
    D-08 (no new state branch; `kid-seg-shimmer` class swap only), D-09 (`boost-style.ts` guard chain
    + `ProfileSheet` row placement), D-10 (single `segmented-bar` option + schema default).

    Add a second matrix mapping BOOST-01, BOOST-02, BOOST-03 and BOOST-06 to named code evidence,
    and explicitly record that BOOST-04 and BOOST-05 remain Phase 9.6 scope and were deliberately not
    built. Also record the CONTEXT `<deferred>` item (loot-box/crate mechanic) as still deferred — no
    new deferral entry is needed.

    In `project-docs/data-model.md`, add a `children.boost_style TEXT NOT NULL DEFAULT 'segmented-bar'`
    paragraph immediately after the existing `fill_style` paragraph, in the same wording style: the
    three CHECK values, that all three are pre-declared while only `segmented-bar` is selectable until
    Phase 9.6, that it is self-serve/kid-controlled (not a parent policy field like `backfill_mode`),
    and that `app/kid/actions/boost-style.ts` is the only writer.

    In `project-docs/system-architecture.md`, add a short section beside the `fill_style` one
    describing the weekly-boost detail view: the three `BoostMeter` entry points, the reused
    `BottomSheet` container, `components/kid/boost/BoostDetailSheet.tsx` as the `segmented-bar` body,
    and — importantly — that every threshold and amount comes from `lib/kid/boost-rules.ts` via
    `BoostProgress.weekDetail` and the pure `lib/kid/boost-detail.ts` builder, never from a literal
    in the view. State that `/api/wallet/award` remains the sole authority for what actually credits.

    Do not edit `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` or `.planning/STATE.md` in this
    task — the phase-close workflow owns those updates after the operator approves in task 2.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <automated>node --env-file=.env.local scripts/verify-boost-style-column.mjs</automated>
    <automated>node --env-file=.env.local scripts/verify-migrations-applied.mjs</automated>
  </verify>
  <acceptance_criteria>
    - All six commands above exit 0, with each exit status recorded in the summary.
    - `grep -c "boost_style" project-docs/data-model.md` returns at least 2.
    - `grep -c "BoostDetailSheet" project-docs/system-architecture.md` returns at least 1 and
      `grep -c "boost-rules" project-docs/system-architecture.md` returns at least 1.
    - The summary contains a table with one row per decision covering all ten of D-01 through D-10,
      each citing a file path and a proving grep or test name.
    - The summary contains a second table covering BOOST-01, BOOST-02, BOOST-03 and BOOST-06, and an
      explicit line that BOOST-04/BOOST-05 are Phase 9.6 scope.
    - `git status --porcelain .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md`
      returns empty for this task.
  </acceptance_criteria>
  <done>All gates green, prod schema re-confirmed, both matrices written, project-docs updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator browser sign-off</name>
  <what-built>
    A tappable weekly-boost detail view in the `segmented-bar` style, opening from all three inline
    `BoostMeter` instances, plus a persisted per-child `boost_style` preference control in the kid
    profile sheet. Every threshold and coin amount is read live from `lib/kid/boost-rules.ts` /
    `wallet_settings`; the inline meter itself is visually unchanged.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and sign in as a child (or as a parent with `?preview=true`), then:

    1. Open `/kid/day` on a narrow (mobile) viewport. The boost meter under the XP bar looks exactly
       as it did before — no chevron, no new icon, same colours and spacing.
    2. Tap that meter. A bottom sheet titled "🚀 Буст недели" slides up.
    3. Confirm the sheet shows TWO separate bars: "📚 Оценки" and "🔥 Стабильность", each with its
       own status text on the right and its own hint line.
    4. Confirm the "Итого за неделю" number at the bottom of the sheet equals the "+N 🪙" number on
       the inline meter behind it.
    5. Confirm the hint under "📚 Оценки" matches the hint line on the inline meter's own copy
       (same "ещё N пятёрок → +X" wording, same numbers) — no second, different set of numbers.
    6. Close the sheet with the × button. Then reopen it and close it by tapping the dark scrim.
       Both work; nothing else on the page closes with it.
    7. Widen the window past 1024px (desktop layout) and tap the boost meter in the left hero column.
       The same sheet opens.
    8. Open the profile sheet (tap the avatar in the kid header). Tap the boost meter inside the
       profile sheet — the same detail sheet opens ON TOP of the profile sheet. Close it with the
       scrim: only the detail sheet closes, the profile sheet stays open.
    9. Still in the profile sheet, scroll to the style controls: "Стиль заполнения дня" with its
       three options, and directly below it "Стиль буста недели" with a single "Полосы ✓" option
       in the blue active treatment. Tap it — no error message appears below it.
    10. Reload the page, reopen the profile sheet, confirm "Полосы" is still shown as active.
    11. Consistency chips: confirm the two pills under the "🔥 Стабильность" bar read sensibly for
        this child's real week (e.g. "4/7 дней" in neutral grey, or "7/7 дней ✓" in mint) and that
        they match the bar's two zones — a filled left zone must correspond to a mint "7/7 дней ✓".
    12. Penalty state (D-05): as a parent, enter a bad grade (a "2") for this child on any day of the
        current week, then reopen the kid detail sheet. The "📚 Оценки" block must render as a red-
        tinted panel with a ⚠ and the text "Одна двойка обнулила бонус за оценки на этой неделе",
        and its bar must be partly filled — NOT an empty 0% bar. Remove the grade afterwards.
    13. Reduced motion: enable macOS System Settings → Accessibility → Display → Reduce motion, then
        reopen the sheet. The sheet appears with no slide-up, the bars appear already at their final
        width with no sweep or glow, and everything is still fully legible. Turn it back off.
    14. Switch the app language to English and reopen the sheet — every string is English (no
        Russian leaking through), including the chips and the bottom note.
    15. Confirm no number anywhere in the sheet is 150, 300 or 500 unless those are genuinely this
        family's configured `wallet_settings` boost amounts.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what you saw that differs from the steps above.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → prod Postgres | The phase-close verify scripts read the live schema with `SUPABASE_DB_URL` |
| documentation → future implementers | `project-docs` is the stated source of truth; a wrong statement there becomes a future defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-095-18 | Repudiation | a ROADMAP "Complete" marker that does not match prod | mitigate | Phase close re-runs `scripts/verify-boost-style-column.mjs` and `scripts/verify-migrations-applied.mjs` against the live database and records the exit codes in the summary, instead of trusting plan 01's earlier run. This is the direct countermeasure to the Phase 1.3 incident in STATE.md. |
| T-095-19 | Repudiation | undocumented new column | mitigate | `project-docs/data-model.md` and `project-docs/system-architecture.md` are updated in the same plan that closes the phase, with grep-asserted acceptance criteria. |
| T-095-20 | Information Disclosure | verify scripts printing connection details | accept | Both scripts print only column metadata, constraint definitions and counts; `SUPABASE_DB_URL` is read from git-ignored `.env.local` and never echoed. Existing, already-shipping behaviour. |
| T-095-SC | Tampering | npm/pip/cargo installs | accept | No new dependency; the phase-close scripts reuse the `pg` driver installed non-persistently in plan 01. Nothing for the Package Legitimacy Gate to audit. |
</threat_model>

<verification>
1. All six automated commands in task 1 exit 0, each recorded in the summary.
2. The D-01..D-10 matrix has ten rows, each with a file path and a proving grep or test.
3. The requirements matrix covers BOOST-01/02/03/06 and explicitly scopes BOOST-04/05 to Phase 9.6.
4. The operator has typed "approved" for the 15-step browser checklist.
</verification>

<success_criteria>
- Typecheck, lint, tests and build are all green on the finished phase.
- `children.boost_style` + `children_boost_style_check` re-confirmed in the LIVE database at close.
- Every decision D-01..D-10 and every requirement BOOST-01/02/03/06 traces to named code evidence.
- `project-docs` documents the column and the detail view, including the "no hardcoded numbers" rule.
- Operator sign-off recorded, including the penalty state and the reduced-motion check.
</success_criteria>

<output>
Create `.planning/phases/09.5-boost-default/09.5-05-SUMMARY.md` when done
</output>
