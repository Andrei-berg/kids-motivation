---
phase: 09.5-boost-default
plan: 03
type: execute
wave: 2
depends_on: [09.5-02]
files_modified:
  - public/locales/ru.json
  - public/locales/en.json
  - app/globals.css
  - components/kid/boost/BoostDetailSheet.tsx
autonomous: true
requirements: [BOOST-03, BOOST-06]

must_haves:
  truths:
    - "The detail view renders inside the Phase 9.4 BottomSheet with no new modal pattern, closing only via the explicit X/swipe control and never a backdrop tap (D-02, D-03)"
    - "The grades sub-bar and the consistency sub-bar are two visually independent bars, each with its own hint"
    - "A penalty week renders a danger panel with an explicit explanation, never a bare 0% bar"
    - "The consistency bar shows two independent fill zones plus two status chips"
    - "The total row shows exactly boost.week.total — the same number as the inline BoostMeter"
    - "Every threshold and amount displayed traces to boost-rules settings, no sketch placeholder numbers appear"
    - "Every string in the component comes from useT(), no hardcoded Russian or English copy"
    - "A reduced-motion user gets the identical static appearance with no pulse and no shimmer (D-06, D-08)"
  artifacts:
    - path: "components/kid/boost/BoostDetailSheet.tsx"
      provides: "segmented-bar weekly boost detail body inside BottomSheet"
      exports: ["default"]
      min_lines: 120
    - path: "app/globals.css"
      provides: "kid-seg-fill / kid-seg-pulse / kid-seg-shimmer behind prefers-reduced-motion: no-preference"
      contains: "kid-seg-pulse"
    - path: "public/locales/ru.json"
      provides: "kidBoost.detail.* and kidProfile.boostStyle* keys"
      contains: "boostStyleHeading"
    - path: "public/locales/en.json"
      provides: "kidBoost.detail.* and kidProfile.boostStyle* keys"
      contains: "boostStyleHeading"
  key_links:
    - from: "components/kid/boost/BoostDetailSheet.tsx"
      to: "components/kid/day-fill/BottomSheet.tsx"
      via: "container reuse, doneLabel null"
      pattern: "<BottomSheet"
    - from: "components/kid/boost/BoostDetailSheet.tsx"
      to: "lib/kid/boost-detail.ts buildBoostDetail"
      via: "view model built from boost.weekDetail + boost.week"
      pattern: "buildBoostDetail"
    - from: "components/kid/boost/BoostDetailSheet.tsx"
      to: "lib/kid/boost-rules.ts gradesNext"
      via: "grades hint copy reused, no second copy-generation path"
      pattern: "gradesNext"
---

<objective>
Build `BoostDetailSheet` — the `segmented-bar` weekly-boost detail view — plus the i18n keys and the
reduced-motion-guarded CSS it needs. The component is self-contained and not yet mounted anywhere;
plan 04 wires the three entry points.

Purpose: BOOST-03, BOOST-06 and CONTEXT decisions D-02 (reuse Phase 9.4's `BottomSheet`), D-03
(explicit close only, no `doneLabel`), D-04 (fractional grades fill), D-05 (penalty danger panel),
D-06 (two-zone consistency bar + two status chips + completion pulse), D-07 (total row), D-08 (no
separate empty-state branch).
Output: one new component, two locale blocks, one CSS block. No existing component's render output
changes in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.5-boost-default/09.5-CONTEXT.md
@.planning/phases/09.5-boost-default/09.5-UI-SPEC.md
@.planning/phases/09.5-boost-default/09.5-PATTERNS.md
@.planning/phases/09.5-boost-default/09.5-02-SUMMARY.md
@.claude/skills/sketch-findings-kids-motivation/references/progress-and-boost.md
@CLAUDE.md

<interfaces>
<!-- Built in plan 02 — consume as-is, do not re-declare or fork. -->
```typescript
// lib/kid/boost-detail.ts
export const PENALTY_FILL_PCT = 40
export interface BoostWeekDetail { settings: BoostSettings; grades: WeeklyGradeStats; consistency: WeeklyConsistencyStats }
export interface BoostDetailView {
  grades: { penalized: boolean; pct: number; tierReached: 0|1|2|3; tierTotal: 3
            nextThreshold: number | null; nextThresholdUnit: 'top-grades'|'graded-days'|null
            topGradeCount: number; coins: number }
  consistency: { fullWeekDone: boolean; filledDays: number; fullWeekTarget: 7
                 streakDone: boolean; streaksAtThreshold: number; streakTarget: number
                 bonusesEarned: 0|1|2; bonusesTotal: 2; coins: number }
  total: number
}
export function buildBoostDetail(detail: BoostWeekDetail, week: WeeklyBoostResult): BoostDetailView

// lib/kid/boost.ts
export interface BoostProgress { week: WeeklyBoostResult; weekDetail: BoostWeekDetail; milestones: Array<{...}> }

// lib/kid/boost-rules.ts
export interface WeeklyBoostResult { grades: number; consistency: number; total: number; max: number; nextLabel: string; gradesNext: string | null }
```

<!-- Existing, reuse verbatim — components/kid/day-fill/BottomSheet.tsx -->
```typescript
interface BottomSheetProps {
  open: boolean; title: string; icon?: string
  onClose: () => void; closeLabel: string; doneLabel?: string | null
  children: React.ReactNode
}
```

NEW in this plan — components/kid/boost/BoostDetailSheet.tsx:
```typescript
interface BoostDetailSheetProps {
  open: boolean
  boost: BoostProgress | null
  onClose: () => void
}
export default function BoostDetailSheet(props: BoostDetailSheetProps): JSX.Element | null
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n keys (ru + en) and reduced-motion-guarded CSS</name>
  <files>public/locales/ru.json, public/locales/en.json, app/globals.css</files>
  <read_first>
    - public/locales/ru.json lines 1995-2022 (the `kidProfile` tail with `fillStyleHeading` /
      `fillStyleError` / `fillStyleOption.*`, and the `kidBoost` block with `milestone.*`)
    - public/locales/en.json lines 1995-2022 (the same two blocks — the keys must stay 1:1)
    - app/globals.css lines 4373-4410 (the Phase 09.4 `@media (prefers-reduced-motion: no-preference)`
      block containing `kid-slide-up` / `.kid-bottom-sheet` / `.kid-tile` / `.kid-dot-fill` — the new
      rules go in the same idiom)
    - app/globals.css around line 4220 (`@keyframes kid-shimmer` — the existing shimmer idiom to
      model the empty-track shimmer on)
    - .planning/phases/09.5-boost-default/09.5-UI-SPEC.md section "Copywriting Contract" (the exact
      key names and RU/EN copy, which are binding)
  </read_first>
  <action>
    Add to the `kidBoost` object in BOTH `public/locales/ru.json` and `public/locales/en.json` a new
    nested `detail` object (sibling of the existing `milestone` object) with exactly these keys and
    the UI-SPEC's binding copy:
    `openLabel` ("Открыть детали буста недели" / "Open weekly boost details"),
    `title` ("Буст недели" / "Weekly boost"),
    `gradesLabel` ("📚 Оценки" / "📚 Grades"),
    `consistencyLabel` ("🔥 Стабильность" / "🔥 Consistency"),
    `totalLabel` ("Итого за неделю" / "Total this week"),
    `penaltyHint` ("Одна двойка обнулила бонус за оценки на этой неделе" / "One low grade zeroed this week's grade bonus"),
    `fullWeekDone` ("7/7 дней ✓" / "7/7 days ✓"),
    `fullWeekPending` ("{{filled}}/7 дней" / "{{filled}}/7 days"),
    `streakDone` ("Стрик×{{count}} ✓" / "Streak×{{count}} ✓"),
    `streakPending` ("Стрик" / "Streak"),
    `note` ("Итог = лучший тир по оценкам (не суммируется) + бонусы за стабильность" / "Total = best grade tier (not summed) + consistency bonuses").
    Additionally add `tierStatus` ("Тир {{tier}} из {{total}}" / "Tier {{tier}} of {{total}}") and
    `bonusStatus` ("{{earned}} из {{total}} бонусов" / "{{earned}} of {{total}} bonuses") — these are
    the two `seg-head` right-side status strings the UI-SPEC leaves to the implementer, expressed as
    interpolated keys so no number is baked into copy.

    Add to the `kidProfile` object in BOTH locale files, as siblings of the existing `fillStyle*`
    keys: `boostStyleHeading` ("Стиль буста недели" / "Weekly boost style"),
    `boostStyleError` ("Не удалось сохранить стиль" / "Couldn't save style"), and a nested
    `boostStyleOption` object with `segmentedBar` ("Полосы" / "Bars"). Plan 04 consumes these.

    Do NOT add a second close-label key — `kidFillForm.sheetClose` already exists in both files
    (line 1279) and is reused verbatim per the UI-SPEC.

    In `app/globals.css`, append a new commented block immediately after the existing Phase 09.4
    `@media (prefers-reduced-motion: no-preference)` block, headed
    `/* ── Phase 09.5 / D-06 / D-08 — boost detail sub-bars ── */`, containing a single
    `@media (prefers-reduced-motion: no-preference)` wrapper with:
    - `.kid-seg-fill { transition: width .8s cubic-bezier(.2,.9,.3,1.1); }` — the sketch's exact
      easing for the sub-bar fills.
    - `@keyframes kid-seg-pulse` plus `.kid-seg-pulse { animation: kid-seg-pulse .9s ease-out 1; }` —
      a one-shot glow on a completed consistency zone: a `box-shadow` going from
      `0 0 0 0 rgba(255,255,255,0)` through `0 0 10px 3px rgba(255,255,255,0.75)` at 50% and back to
      transparent, with no transform and no opacity change (the zone must stay legible throughout).
    - `@keyframes kid-seg-shimmer` plus `.kid-seg-shimmer { ... }` — a subtle left-to-right
      `background-position` sweep on an empty track, modeled on the existing `kid-shimmer` keyframe,
      running `infinite` at a slow duration (2s or longer) and low contrast so an empty week reads as
      "waiting for you", not as a loading skeleton.
    All three classes carry only motion — every colour, size and layout value stays in the component's
    inline styles, so a reduced-motion user (for whom none of these rules exist) sees the identical
    static appearance. This is the same split already used by `.kid-bottom-sheet` / `.kid-tile`.
  </action>
  <verify>
    <automated>node -e "const r=require('./public/locales/ru.json'),e=require('./public/locales/en.json');const ks=Object.keys(r.kidBoost.detail);if(JSON.stringify(ks)!==JSON.stringify(Object.keys(e.kidBoost.detail)))throw new Error('kidBoost.detail key mismatch');if(JSON.stringify(Object.keys(r.kidProfile.boostStyleOption))!==JSON.stringify(Object.keys(e.kidProfile.boostStyleOption)))throw new Error('boostStyleOption mismatch');console.log('locales ok',ks.length)"</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - The node check above exits 0 and prints `locales ok 13`.
    - `node -e "const r=require('./public/locales/ru.json');['openLabel','title','gradesLabel','consistencyLabel','totalLabel','penaltyHint','fullWeekDone','fullWeekPending','streakDone','streakPending','note','tierStatus','bonusStatus'].forEach(k=>{if(!r.kidBoost.detail[k])throw new Error('missing '+k)});if(!r.kidProfile.boostStyleHeading||!r.kidProfile.boostStyleError||!r.kidProfile.boostStyleOption.segmentedBar)throw new Error('missing kidProfile key')"` exits 0.
    - `grep -c "kid-seg-fill\|kid-seg-pulse\|kid-seg-shimmer" app/globals.css` returns at least 5
      (three class rules plus two keyframe declarations).
    - Every one of `kid-seg-fill`, `kid-seg-pulse`, `kid-seg-shimmer` appears only at a line number
      greater than the last `@media (prefers-reduced-motion: no-preference) {` opening line —
      verify by reading the appended block, which must be entirely inside that media query.
    - `grep -c "sheetClose" public/locales/ru.json` still returns 1 (no duplicate close key added).
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>All 13 `kidBoost.detail.*` keys plus the three `kidProfile.boostStyle*` keys exist in both locales; three motion-only CSS classes live behind the reduced-motion guard.</done>
</task>

<task type="auto">
  <name>Task 2: BoostDetailSheet — segmented-bar detail body</name>
  <files>components/kid/boost/BoostDetailSheet.tsx</files>
  <read_first>
    - components/kid/day-fill/BottomSheet.tsx in full (the container's exact props, its own
      reduced-motion double-guard at lines 34-39, and its D-18 close-path rule)
    - components/kid/design/atoms.tsx lines 474-509 (`BoostMeter` — the track/fill nesting idiom and
      the gradient this detail view inverts; this file must NOT be modified)
    - components/kid/design/kidTheme.ts (the `K` token object — every colour must come from here)
    - components/kid/day-fill/TileCard.tsx lines 20-35 (the `${K.danger}14` / `${K.danger}40`
      soft-surface loss-tint idiom the penalty panel adapts)
    - components/kid/ProfileSheet.tsx lines 156-172 (the `STREAK_META` chip: `padding: '7px 12px'`,
      `borderRadius: 999`, `display: 'inline-flex'` — the exact chip shape to reuse)
    - lib/kid/boost-detail.ts and lib/kid/boost.ts (as built in plan 02)
    - .claude/skills/sketch-findings-kids-motivation/sources/003-weekly-boost/index.html, the
      `#variant-a` block and its `.boost-hero`/`.seg-block`/`.seg-head`/`.seg-track`/`.seg-fill`/
      `.seg-hint`/`.section-note` CSS (lines 37-49, 106) — the validated markup this component ports
    - .planning/phases/09.5-boost-default/09.5-UI-SPEC.md sections "Color", "Typography",
      "Interaction Contract — Segmented-Bar Detail View" and "Data → visual mapping"
  </read_first>
  <action>
    Create `components/kid/boost/BoostDetailSheet.tsx` as a `'use client'` module with a default
    export matching this plan's `<interfaces>` `BoostDetailSheetProps`. Return `null` when
    `!props.open || props.boost === null`. Otherwise compute
    `const v = buildBoostDetail(boost.weekDetail, boost.week)` and render a `<BottomSheet>` with
    `open`, `title={t('kidBoost.detail.title')}`, `icon="🚀"`, `onClose={props.onClose}`,
    `closeLabel={t('kidFillForm.sheetClose')}` and `doneLabel={null}` — no third close path (D-03).
    Do not modify `BottomSheet` itself.

    Declare the reduced-motion double-guard exactly as `BottomSheet.tsx` lines 34-39 does: a
    `const [reduced, setReduced] = useState(false)` plus a mount `useEffect` reading
    `window.matchMedia('(prefers-reduced-motion: reduce)').matches` behind `typeof window` and
    `typeof window.matchMedia` checks. Every animated class from task 1 is applied as
    `className={!reduced ? 'kid-seg-...' : undefined}`.

    Sheet body, top to bottom:

    (1) Hero card — `borderRadius: 20`, `padding: 16`, `color: '#fff'`,
    `background: linear-gradient(135deg, ${K.mango}, ${K.berry})`, containing two blocks separated by
    an 8px gap.

    (2) Grades block. Head row: `display: flex`, `justifyContent: 'space-between'`, left
    `t('kidBoost.detail.gradesLabel')`, right `t('kidBoost.detail.tierStatus', { tier: v.grades.tierReached, total: v.grades.tierTotal })`; both at `K.fBody`/13px/700 with `opacity: 0.95`.
    Then a mutually exclusive two-way branch:
    - When `v.grades.penalized` (D-05): render a panel with `background: ${K.danger}1F`,
      `border: 1.5px solid ${K.danger}55`, `borderRadius: 12`, `padding: 8`, containing a `⚠` glyph
      (`aria-hidden`), the grades label, a track whose fill width is `${v.grades.pct}%` (which is
      `PENALTY_FILL_PCT`, never 0) rendered in a muted `${K.danger}` rather than white, and
      `t('kidBoost.detail.penaltyHint')` as the hint in solid `K.danger`. Never combine this panel
      with the normal white-on-gradient treatment in the same render.
    - Otherwise: track `height: 10`, `borderRadius: 999`, `overflow: 'hidden'`,
      `background: 'rgba(255,255,255,0.28)'`; fill `width: ${v.grades.pct}%`, `height: '100%'`,
      `background: '#fff'`, `borderRadius: 999`, `className` `kid-seg-fill` (plus `kid-seg-shimmer`
      on the TRACK when `v.grades.pct === 0`, per D-08 — this is a class swap only, no new state
      variable and no separate empty-state branch). Hint line below: render
      `boost.week.gradesNext` verbatim when it is non-null, otherwise render nothing. Do NOT
      construct a second "ещё N пятёрок → +X" string anywhere — `gradesNext` is the single source of
      that copy (BOOST-06).

    (3) Consistency block (D-06). Head row: left `t('kidBoost.detail.consistencyLabel')`, right
    `t('kidBoost.detail.bonusStatus', { earned: v.consistency.bonusesEarned, total: v.consistency.bonusesTotal })`.
    Track: one 10px-tall `display: flex`, `gap: 3` row of TWO sibling zone containers, each
    `flex: 1`, `borderRadius: 999`, `overflow: 'hidden'`, `background: 'rgba(255,255,255,0.28)'`.
    Inside each zone one fill div with `height: '100%'`, `background: '#fff'`, `borderRadius: 999`,
    `className` `kid-seg-fill` and `width` of `'100%'` when that zone's boolean is true else `'0%'` —
    binary per zone, never proportional to the other zone and never summed into one percentage. Left
    zone uses `v.consistency.fullWeekDone`, right zone uses `v.consistency.streakDone`. When a zone's
    boolean is true and `!reduced`, also apply `kid-seg-pulse` to that zone's fill so the earned zone
    glows once each time the sheet opens (this is the implementer's-discretion resolution of D-06's
    "completion feedback" — a one-shot pulse on open, not a persisted since-last-view diff, which
    would need storage this phase does not have).

    (4) Two status chips below the track (D-06, required — they exist so neither condition reads as
    swallowed into one percentage; do not omit them even though the track already has two zones).
    Reuse the `ProfileSheet` `STREAK_META` chip shape exactly: `display: 'inline-flex'`,
    `alignItems: 'center'`, `gap: 6`, `padding: '7px 12px'`, `borderRadius: 999`, 8px gap between the
    two chips, text at `K.fBody`/13px/700.
    - Full-week chip: `t('kidBoost.detail.fullWeekDone')` when `fullWeekDone`, else
      `t('kidBoost.detail.fullWeekPending', { filled: v.consistency.filledDays })`. Done state uses
      `background: K.mintSoft`, `border: 1.5px solid ${K.mint}`, `color: K.ink`; pending uses
      `background: K.card`, `border: 1.5px solid ${K.line}`, `color: K.ink2`.
    - Streak chip: `t('kidBoost.detail.streakDone', { count: v.consistency.streaksAtThreshold })` when
      `streakDone`, else `t('kidBoost.detail.streakPending')`. Done state uses
      `background: K.mangoSoft`, `border: 1.5px solid ${K.mango}`, `color: K.mangoDeep`; pending uses
      the same neutral treatment as above.

    (5) Total row (D-07), OUTSIDE the hero card, `padding: '8px 4px'`, `display: flex`,
    `justifyContent: 'space-between'`, `alignItems: 'baseline'`: left `t('kidBoost.detail.totalLabel')`
    at `K.fBody`/13px/700/`K.ink3`; right `+{v.total.toLocaleString('ru-RU')} 🪙` at
    `K.fDisp`/20px/900/`K.mangoDeep`. `v.total` is `boost.week.total` — the identical number the
    inline `BoostMeter` shows. Never recompute it here.

    (6) Explainer note: `t('kidBoost.detail.note')` at `K.fBody`/13px/600/`K.ink3`.

    Hard constraints: every string goes through `useT()` (no hardcoded Russian or English literal,
    with the sole exceptions of the emoji glyphs `🚀`/`⚠`/`🪙` and the `#fff`/`rgba(255,255,255,…)`
    white-on-gradient literals the UI-SPEC explicitly sanctions). Every colour is a `K.*` token or an
    established `${K.hex}alpha` tint — introduce no new hex value and no `K.white` constant. Do not
    import or render `BoostMeter`. Do not touch `components/kid/design/atoms.tsx`. Do not read the
    database, call `getBoostProgress`, or hold any state other than `reduced` — the component is a
    pure render of the `boost` prop. Do not mount this component anywhere yet; plan 04 does the wiring.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "buildBoostDetail" components/kid/boost/BoostDetailSheet.tsx` returns at least 1.
    - `grep -c "gradesNext" components/kid/boost/BoostDetailSheet.tsx` returns at least 1 and
      `grep -c "пятёр\|ещё \|Тир \|бонус" components/kid/boost/BoostDetailSheet.tsx` returns 0 —
      no copy is generated in the component.
    - `grep -c "doneLabel={null}" components/kid/boost/BoostDetailSheet.tsx` returns 1 (D-03).
    - `grep -c "kid-seg-fill" components/kid/boost/BoostDetailSheet.tsx` returns 3 (grades fill plus
      the two consistency zone fills).
    - `grep -c "kid-seg-pulse" components/kid/boost/BoostDetailSheet.tsx` returns 2 (one per zone).
    - `grep -c "prefers-reduced-motion: reduce" components/kid/boost/BoostDetailSheet.tsx` returns 1.
    - `grep -c "150\|300\|500\|66%" components/kid/boost/BoostDetailSheet.tsx` returns 0 — no sketch
      placeholder numbers (BOOST-06).
    - `grep -c "#[0-9A-Fa-f]\{6\}" components/kid/boost/BoostDetailSheet.tsx` returns 0 — no raw hex
      other than the permitted `#fff` shorthand (verify by reading that only `#fff` appears).
    - `grep -rc "BoostMeter" components/kid/boost/BoostDetailSheet.tsx` returns 0.
    - `git diff --stat components/kid/design/atoms.tsx` shows no change.
    - `npx tsc --noEmit`, `npm run lint` and `npm run build` all exit 0.
  </acceptance_criteria>
  <done>BoostDetailSheet renders the two independent sub-bars, the penalty panel, two status chips, the total row and the note — all from real settings-derived data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `BoostProgress` prop → rendered DOM | Data already fetched under family-isolation RLS by `getBoostProgress` crosses into the view |
| i18n catalogue → rendered DOM | `{{filled}}` / `{{count}}` / `{{tier}}` interpolation values originate from the child's own stats |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-095-10 | Information Disclosure | `BoostDetailSheet` rendering another child's stats | mitigate | The component holds no state and performs no fetch — it renders only the `boost` prop its parent already scoped to the active child via `getBoostProgress(activeMemberId)` under family-isolation RLS. Asserted by the acceptance criterion that the component contains no `getBoostProgress` call and no Supabase import. |
| T-095-11 | Tampering | displayed coin totals diverging from the real payout | mitigate | The total row prints `v.total`, copied verbatim from `boost.week.total` (plan 02, T-095-06), and the grades hint prints `week.gradesNext` verbatim. No copy or amount is constructed in the component — enforced by the `grep` criteria returning 0 for copy fragments and sketch numbers. |
| T-095-12 | Spoofing | a penalty week presented as "no progress yet" | mitigate | D-05 danger panel with `PENALTY_FILL_PCT` fill and an explicit `penaltyHint` — never a bare 0% bar. This is a transparency requirement from PROJECT.md ("every consequence must be visible and honest"), treated as a repudiation-class risk: a kid must be able to see why the bonus was zeroed. |
| T-095-13 | Denial of Service | infinite shimmer animation on low-end devices | accept | `kid-seg-shimmer` is a single `background-position` sweep on one 10px element, only when the track is empty, and is entirely absent for reduced-motion users. Same cost profile as the already-shipping `kid-shimmer`. |
| T-095-SC | Tampering | npm/pip/cargo installs | accept | No new dependency — the component imports only React, the existing `K` tokens, the existing `BottomSheet`, `useT`, and plan 02's pure module. Nothing for the Package Legitimacy Gate to audit. |
</threat_model>

<verification>
1. `npx tsc --noEmit`, `npm run lint`, `npm test` and `npm run build` all exit 0.
2. The locale parity check in task 1 exits 0 with `locales ok 13`.
3. `grep -c "150\|300\|500\|66%" components/kid/boost/BoostDetailSheet.tsx` returns 0 (BOOST-06).
4. `git diff --stat components/kid/design/atoms.tsx components/kid/day-fill/BottomSheet.tsx` shows
   zero changes — the inline meter and the sheet primitive were reused, not modified.
</verification>

<success_criteria>
- `BoostDetailSheet` renders inside the Phase 9.4 `BottomSheet` with `doneLabel={null}` (D-02, D-03).
- Grades sub-bar shows fractional fill with `gradesNext` as its hint, or the danger panel with
  `penaltyHint` and a non-zero fill when penalized (D-04, D-05).
- Consistency sub-bar shows two independent binary zones plus the two required status chips (D-06).
- The total row shows `boost.week.total` (D-07); the explainer note states best-tier-not-summed.
- Zero-week renders through the same code path with an empty-track shimmer and no new state (D-08).
- Every string is a `useT()` lookup; every colour is a `K` token; reduced-motion users see no motion.
</success_criteria>

<output>
Create `.planning/phases/09.5-boost-default/09.5-03-SUMMARY.md` when done
</output>
