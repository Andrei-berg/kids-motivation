---
phase: 09.5-boost-default
plan: 02
subsystem: ui
tags: [vitest, typescript, boost, view-model, tdd]

# Dependency graph
requires:
  - phase: 09.4-dayform-styles
    provides: BottomSheet primitive (reused by plan 03, not this plan)
provides:
  - Pure buildBoostDetail(detail, week) view-model builder (lib/kid/boost-detail.ts) — fractional grades fill, distinct penalty state, two independent consistency zone booleans, coins/total copied verbatim from WeeklyBoostResult
  - WeeklyBoostResult.gradesNext — surfaces gradesBoost()'s private "next" hint string without exporting the helper
  - BoostProgress.weekDetail — raw BoostSettings/WeeklyGradeStats/WeeklyConsistencyStats inputs, additive, no extra DB round trip
affects: [09.5-03 (segmented-bar detail sheet component, binds to buildBoostDetail + weekDetail)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pure view-model builder pattern: lib/kid/boost-detail.ts has zero React/Supabase imports so it is independently unit-testable, mirroring the existing lib/kid/boost-rules.ts pure-function convention"
    - "Additive-only interface extension: new fields (gradesNext, weekDetail) appended to existing return objects/interfaces without touching any existing field, verified via git diff --stat showing only the single return-line rewrite in boost.ts"

key-files:
  created:
    - lib/kid/boost-detail.ts
    - tests/boost-detail.test.ts
  modified:
    - lib/kid/boost-rules.ts
    - lib/kid/boost.ts

key-decisions:
  - "Grades branch precedence order mirrors gradesBoost() exactly: penalty > perfect > t2 > t1 > base — required so the detail view's tierReached/pct can never disagree with the coin amount computeWeeklyBoost actually pays"
  - "clampPct() returns 0 for non-finite input (misconfigured 0 threshold) instead of throwing — a defensive guard not specified numerically in the plan but required for correctness with settings.boost_grades_t1_count possibly editable to 0 by a parent"
  - "STREAK_BONUS_THRESHOLD = 2 declared as a local const (not re-imported from boost-rules) with a comment tying it to consistencyBoost()'s boost_streaks_2 branch, per plan instruction"

requirements-completed: [BOOST-03, BOOST-06]

duration: 3min
completed: 2026-09-20
---

# Phase 9.5 Plan 02: Boost detail data layer Summary

**Pure, unit-tested `buildBoostDetail` view-model builder plus additive `gradesNext`/`weekDetail` surfacing on the existing boost pipeline — zero recomputation, zero new DB reads.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-09-20T06:20:34+03:00
- **Completed:** 2026-09-20T06:22:46+03:00
- **Tasks:** 2 completed
- **Files modified:** 4 (2 created, 2 modified)

## Accomplishments
- `lib/kid/boost-detail.ts` — pure `buildBoostDetail(detail, week)` builder producing fractional grades fill (never stepped 0/33/66/100, D-04), a distinct penalty state fixed at `PENALTY_FILL_PCT = 40` (never a bare 0%, D-05), and two independent consistency zone booleans plus a `0..2` `bonusesEarned` count (never summed into one percentage, D-06)
- `tests/boost-detail.test.ts` — 12 tests covering every case in the plan's `<behavior>` block, written first and confirmed failing (module didn't exist) before implementation
- `WeeklyBoostResult.gradesNext` — the grades-only "what unlocks the next tier" hint, surfaced from the private `gradesBoost()` helper without exporting it
- `BoostProgress.weekDetail` — the raw `BoostSettings`/`WeeklyGradeStats`/`WeeklyConsistencyStats` inputs `getBoostProgress` already computes, so plan 03's detail sheet component has real data to bind fractional fills/chip states to, with zero extra database round trip

## Task Commits

Each task was committed atomically (TDD RED/GREEN for Task 1):

1. **Task 1: Pure buildBoostDetail view-model builder + tests**
   - `bf6a761` (test) — RED: failing test file, module did not yet exist
   - `97b0c62` (feat) — GREEN: implementation, all 12 tests pass
2. **Task 2: Surface gradesNext on WeeklyBoostResult and weekDetail on BoostProgress** - `8fcda77` (feat)

_TDD gate sequence verified in git log: `test(...)` commit exists before the `feat(...)` commit that makes it pass (bf6a761 → 97b0c62)._

## Files Created/Modified
- `lib/kid/boost-detail.ts` - Pure view-model builder (`buildBoostDetail`, `PENALTY_FILL_PCT`, `BoostWeekDetail`/`BoostDetailView` types); no React/Supabase import
- `tests/boost-detail.test.ts` - 12-case regression guard for fractional fill, penalty state, independent zones, and total/coins passthrough
- `lib/kid/boost-rules.ts` - Added `gradesNext: string | null` to `WeeklyBoostResult` + return-object entry; `gradesBoost`/`consistencyBoost` remain unexported; all other fields byte-for-byte unchanged
- `lib/kid/boost.ts` - Added `weekDetail: BoostWeekDetail` to `BoostProgress` + return-object entry (settings/gradeStats/consistencyStats already computed in the function body, no re-fetch/re-derive)

## Decisions Made
- See `key-decisions` in frontmatter above (grades-branch precedence order, `clampPct` non-finite guard, `STREAK_BONUS_THRESHOLD` local const).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Updated tests/boost-detail.test.ts's `makeWeek()` helper for the new required `gradesNext` field**
- **Found during:** Task 2 (after adding `gradesNext: string | null` to `WeeklyBoostResult`)
- **Issue:** Task 1's test helper `makeWeek()` built `WeeklyBoostResult` object literals without `gradesNext`; once Task 2 made that field required and non-optional, `npx tsc --noEmit` failed with "Type 'string | null | undefined' is not assignable to type 'string | null'" (the spread-default pattern surfaces `undefined` when a field is entirely absent from the base object).
- **Fix:** Added `gradesNext: null` to the `makeWeek()` default object.
- **Files modified:** tests/boost-detail.test.ts
- **Verification:** `npx tsc --noEmit` exits 0; `npx vitest run tests/boost-detail.test.ts` still 12/12 passing; `npm test` full suite 183 passed.
- **Committed in:** `8fcda77` (part of Task 2 commit, same file group as the interface change that caused it)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary consequence of Task 2's additive interface change on Task 1's test fixtures. No scope creep — same file, same plan, immediately caused by the plan's own task sequence.

## Issues Encountered
None beyond the deviation above.

## User Setup Required

None - no external service configuration required. Pure TypeScript logic, no migration, no new dependency.

## Next Phase Readiness

- Plan 03 (segmented-bar detail sheet component) can now import `buildBoostDetail` from `lib/kid/boost-detail.ts` and read `boost.weekDetail` from `getBoostProgress()`'s existing three call sites (`app/kid/(app)/day/page.tsx`, `app/kid/(app)/achievements/page.tsx`, `components/kid/ProfileSheet.tsx`) without any additional data plumbing.
- No blockers. All three existing callers of `getBoostProgress` continue to compile and pass tests untouched (verified via `npx tsc --noEmit` and `npm test` full-suite green, plus `git diff --stat lib/kid/boost.ts` showing only the intended additive lines).

---
*Phase: 09.5-boost-default*
*Completed: 2026-09-20*
