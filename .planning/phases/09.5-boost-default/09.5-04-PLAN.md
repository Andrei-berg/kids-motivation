---
phase: 09.5-boost-default
plan: 04
type: execute
wave: 3
depends_on: [09.5-01, 09.5-03]
files_modified:
  - components/kid/ProfileSheet.tsx
  - app/kid/(app)/day/page.tsx
autonomous: true
requirements: [BOOST-01, BOOST-02]

must_haves:
  truths:
    - "All three existing BoostMeter instances are tappable and open the same detail view"
    - "The inline BoostMeter itself is visually unchanged — no chevron, no new prop, atoms.tsx untouched"
    - "ProfileSheet shows a boost-style control directly below the fill-style control"
    - "The boost-style control offers segmented-bar only and shows it as the active choice"
    - "Selecting a boost style persists through the server action and survives a reload"
    - "Closing the boost detail sheet from inside ProfileSheet does not also close ProfileSheet"
    - "No client-side write to the children table is introduced"
  artifacts:
    - path: "components/kid/ProfileSheet.tsx"
      provides: "tap-to-open wrapper, boost_style radiogroup, mounted BoostDetailSheet"
      contains: "updateChildBoostStyle"
    - path: "app/kid/(app)/day/page.tsx"
      provides: "tap-to-open wrappers on both hero meters and a single mounted BoostDetailSheet"
      contains: "BoostDetailSheet"
  key_links:
    - from: "components/kid/ProfileSheet.tsx"
      to: "app/kid/actions/boost-style.ts updateChildBoostStyle"
      via: "handleBoostStyleTap"
      pattern: "updateChildBoostStyle"
    - from: "app/kid/(app)/day/page.tsx"
      to: "components/kid/boost/BoostDetailSheet.tsx"
      via: "single mount shared by both hero entry points"
      pattern: "<BoostDetailSheet"
    - from: "components/kid/ProfileSheet.tsx"
      to: "components/kid/boost/BoostDetailSheet.tsx"
      via: "third entry point, same sheet"
      pattern: "<BoostDetailSheet"
---

<objective>
Wire the phase together: make all three existing `BoostMeter` instances tappable so each opens the
`segmented-bar` detail sheet, and add the `boost_style` picker to `ProfileSheet` beside the existing
`fill_style` picker.

Purpose: BOOST-02 (tapping the inline meter opens the detail view, meter unchanged), BOOST-01
(changeable per-child preference control), and CONTEXT decisions D-01 (all three instances, no
inconsistent subset), D-09 (unified Style area in `ProfileSheet`, server-action write), D-10
(`segmented-bar` is the only selectable option this phase).
Output: three entry points, one picker row, zero changes to `atoms.tsx` or `BottomSheet.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.5-boost-default/09.5-CONTEXT.md
@.planning/phases/09.5-boost-default/09.5-UI-SPEC.md
@.planning/phases/09.5-boost-default/09.5-PATTERNS.md
@.planning/phases/09.5-boost-default/09.5-01-SUMMARY.md
@.planning/phases/09.5-boost-default/09.5-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Built in plans 01 and 03 — consume as-is, do not re-declare or fork. -->
```typescript
// app/kid/actions/boost-style.ts  (plan 01)
export async function updateChildBoostStyle(
  childId: string,
  style: 'segmented-bar' | 'quest-checklist' | 'ring-badges',
): Promise<{ ok: true }>

// components/kid/boost/BoostDetailSheet.tsx  (plan 03)
interface BoostDetailSheetProps { open: boolean; boost: BoostProgress | null; onClose: () => void }
export default function BoostDetailSheet(props: BoostDetailSheetProps): JSX.Element | null

// lib/models/child.types.ts  (plan 01)
boost_style: 'segmented-bar' | 'quest-checklist' | 'ring-badges'
```

<!-- Existing, unchanged — components/kid/design/atoms.tsx -->
```typescript
export function BoostMeter(props: { earned: number; max: number; label: string; compact?: boolean }): JSX.Element
```

<!-- i18n keys added in plan 03, ready to use -->
`kidBoost.detail.openLabel`, `kidProfile.boostStyleHeading`, `kidProfile.boostStyleError`,
`kidProfile.boostStyleOption.segmentedBar`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProfileSheet — tap-to-open wrapper, boost_style picker, mounted detail sheet</name>
  <files>components/kid/ProfileSheet.tsx</files>
  <read_first>
    - components/kid/ProfileSheet.tsx in full, with attention to: the `savingFillStyle` /
      `fillStyleError` state pair (lines 45-46), `load()` (lines 48-60), `handleFillStyleTap`
      (lines 70-83), the root dialog div with `onClick={onClose}` and its `stopPropagation` inner
      panel (lines 89-107), the `BoostMeter` mount (lines 143-147), the `STREAK_META` chips
      (lines 156-172), the `fill_style` heading + radiogroup + error line (lines 183-222), and the
      trailing `pickerOpen && <AvatarPicker .../>` block (lines 237-243)
    - components/kid/AvatarPicker.tsx lines 80-95 (the nested-overlay idiom inside ProfileSheet:
      its own `position: fixed` scrim at `zIndex: 260` plus a `stopPropagation` inner panel)
    - components/kid/day-fill/BottomSheet.tsx (its scrim is `zIndex: 230` and its `onClick` calls
      `onClose` — the event still bubbles up the React tree, which is why the mount below needs its
      own `stopPropagation` wrapper)
    - .planning/phases/09.5-boost-default/09.5-PATTERNS.md section `components/kid/ProfileSheet.tsx`
    - .planning/phases/09.5-boost-default/09.5-UI-SPEC.md section "`boost_style` control"
  </read_first>
  <action>
    Add three pieces of state next to the existing `savingFillStyle`/`fillStyleError` pair:
    `savingBoostStyle`, `boostStyleError`, and `boostDetailOpen` (all `useState(false)`).

    Add `handleBoostStyleTap(value: 'segmented-bar')` as a sibling of `handleFillStyleTap`, with the
    identical body shape: early-return when `!child?.id || savingBoostStyle`, clear the error, set
    saving, `await updateChildBoostStyle(child.id, value)`, `await load()`, `catch` logs
    `'updateChildBoostStyle failed'` and sets `boostStyleError`, `finally` clears saving. Import
    `updateChildBoostStyle` from `@/app/kid/actions/boost-style` alongside the existing
    `updateChildFillStyle` import (line 21).

    Entry point (D-01): wrap the EXISTING `{boost && (<div style={{ marginTop: 14 }}><BoostMeter .../></div>)}`
    block (lines 143-147) in a `<button type="button">` with
    `aria-label={t('kidBoost.detail.openLabel')}` and `onClick={() => setBoostDetailOpen(true)}`.
    Reset the button chrome with explicit properties rather than `all: 'unset'` (whose cascade order
    inside a React style object is fragile): `display: 'block'`, `width: '100%'`, `padding: 0`,
    `margin: 0`, `border: 'none'`, `background: 'transparent'`, `textAlign: 'left'`, `font: 'inherit'`,
    `cursor: 'pointer'`. Do not change a single byte of the `BoostMeter` call itself, do not add an
    `onClick` prop inside the component, do not add a chevron or any other affordance to the meter —
    CONTEXT is explicit that the inline meter stays visually unchanged.

    Picker (D-09, D-10): directly below the existing `fillStyleError` block (after line 222), add a
    heading `div` reusing the exact style object of the `fillStyleHeading` div
    (`K.fBody`, 12px, 700, `K.ink3`, `marginTop: 16`, `marginBottom: 8`) with
    `{t('kidProfile.boostStyleHeading')}`. Add NO new outer "Style" section wrapper — the two rows
    read as one grouped area through proximity alone. Below it render a
    `<div role="radiogroup" style={{ display: 'flex', gap: 8, opacity: savingBoostStyle ? 0.7 : 1 }}>`
    containing exactly ONE full-width `<button type="button" role="radio" aria-checked>` for
    `'segmented-bar'` (not a 3-item row with disabled slots — `quest-checklist` and `ring-badges`
    appear only once Phase 9.6 ships). Give it the exact ACTIVE visual treatment of a `fill_style`
    option: `flex: 1`, `minHeight: 44`, `borderRadius: 14`, `padding: '0 10px'`,
    `background: K.skySoft`, `border: 1.5px solid ${K.sky}`, `color: K.skyDeep`, centred flex row
    with `gap: 6`, `K.fBody`/13px/700, label `{t('kidProfile.boostStyleOption.segmentedBar')}` plus
    the `<span aria-hidden style={{ color: K.mint }}>✓</span>` checkmark. Compute
    `const boostActive = child?.boost_style === 'segmented-bar'` and pass it to `aria-checked` so the
    control reports real persisted state rather than a hardcoded `true`; keep the visual treatment
    active regardless, since `segmented-bar` is both the default and the only option. `onClick`
    calls `handleBoostStyleTap('segmented-bar')`, `disabled={savingBoostStyle}`. Below the
    radiogroup add the error line mirroring `fillStyleError`'s exactly, gated on `boostStyleError`
    and showing `{t('kidProfile.boostStyleError')}`.

    Mount (D-02): render `<BoostDetailSheet open={boostDetailOpen} boost={boost} onClose={() => setBoostDetailOpen(false)} />`
    as the LAST child of the root dialog div, after the `pickerOpen && <AvatarPicker .../>` block,
    wrapped in a plain `<div onClick={(e) => e.stopPropagation()}>`. The wrapper is required: the
    root dialog div has `onClick={onClose}`, and `BottomSheet`'s scrim click would otherwise bubble
    up and dismiss the whole ProfileSheet along with the detail sheet. Mount it exactly once.

    Do not add any Supabase call, any `.from('children')` write, or any new data fetch — `load()`
    already refreshes `child` (and therefore `boost_style`) after a successful save.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "updateChildBoostStyle" components/kid/ProfileSheet.tsx` returns 2 (import + call).
    - `grep -c "<BoostDetailSheet" components/kid/ProfileSheet.tsx` returns 1.
    - `grep -c "kidBoost.detail.openLabel" components/kid/ProfileSheet.tsx` returns 1.
    - `grep -c "quest-checklist\|ring-badges" components/kid/ProfileSheet.tsx` returns 0 (D-10 —
      no dead-end UI for unbuilt styles).
    - `grep -c "from('children')" components/kid/ProfileSheet.tsx` returns 0.
    - `grep -c "boost_style === 'segmented-bar'" components/kid/ProfileSheet.tsx` returns 1.
    - `grep -c "all: 'unset'" components/kid/ProfileSheet.tsx` returns 0.
    - `git diff components/kid/ProfileSheet.tsx` shows the `<BoostMeter earned={boost.week.total}
      max={boost.week.max} label={boost.week.nextLabel} />` line itself unmodified — only wrapped.
    - `npx tsc --noEmit` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>ProfileSheet has a tappable meter, a persisted boost-style picker, and one mounted detail sheet.</done>
</task>

<task type="auto">
  <name>Task 2: Day page — tap-to-open on both hero meters, one shared detail sheet</name>
  <files>app/kid/(app)/day/page.tsx</files>
  <read_first>
    - app/kid/(app)/day/page.tsx, specifically: the import block (lines 19-21), the UI-state hooks
      around line 80 (`boost`, `notice`, `selectedDate`), the desktop hero `BoostMeter` mount
      (line 335), the mobile compact `BoostMeter` mount (lines 402-406), and the component's closing
      JSX (the last ~10 lines — where the single shared sheet mounts)
    - components/kid/boost/BoostDetailSheet.tsx (built in plan 03 — its props)
    - components/kid/ProfileSheet.tsx as modified in task 1 (the exact button-reset style object to
      reuse verbatim, so the three entry points are identical)
    - .planning/phases/09.5-boost-default/09.5-PATTERNS.md section `app/kid/(app)/day/page.tsx`
    - .planning/phases/09.5-boost-default/09.5-CONTEXT.md decision D-01
  </read_first>
  <action>
    Add `const [boostDetailOpen, setBoostDetailOpen] = useState(false)` beside the existing
    `const [boost, setBoost] = useState<BoostProgress | null>(null)` (line 80). Add
    `import BoostDetailSheet from '@/components/kid/boost/BoostDetailSheet'` to the import block.

    Wrap BOTH existing `BoostMeter` mounts in the same `<button type="button">` used in task 1 —
    identical `aria-label={t('kidBoost.detail.openLabel')}`, identical
    `onClick={() => setBoostDetailOpen(true)}`, identical explicit button-reset style object
    (`display: 'block'`, `width: '100%'`, `padding: 0`, `margin: 0`, `border: 'none'`,
    `background: 'transparent'`, `textAlign: 'left'`, `font: 'inherit'`, `cursor: 'pointer'`).
    For the desktop mount at line 335 the button replaces the bare `{boost && <BoostMeter .../>}`
    expression's child. For the mobile mount at lines 402-406 the button wraps the existing
    `<div style={{ padding: '10px 16px 0' }}>` — keep that padding div and its value exactly as is
    so the mobile hero spacing does not shift. Leave every `BoostMeter` prop byte-for-byte unchanged
    (including `compact` on the mobile one) and do not touch `components/kid/design/atoms.tsx`.

    Render `<BoostDetailSheet open={boostDetailOpen} boost={boost} onClose={() => setBoostDetailOpen(false)} />`
    exactly ONCE, as the last child inside the component's outermost wrapper div (immediately before
    its closing tag at the end of the return), so both hero entry points open the same sheet
    instance. Do not duplicate it per mount and do not place it inside either the desktop-only or
    mobile-only branch — it must mount in both layouts.

    Change nothing else: no new fetch, no change to `getBoostProgress(resolvedId)` in the existing
    `Promise.all` (line 114), no change to `showForm`, `editMode`, `selectedDate` or any save path.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<BoostDetailSheet" "app/kid/(app)/day/page.tsx"` returns 1.
    - `grep -c "setBoostDetailOpen(true)" "app/kid/(app)/day/page.tsx"` returns 2 (one per hero mount).
    - `grep -c "kidBoost.detail.openLabel" "app/kid/(app)/day/page.tsx"` returns 2.
    - `grep -c "<BoostMeter" "app/kid/(app)/day/page.tsx"` still returns 2 — no mount added or removed.
    - `grep -c "padding: '10px 16px 0'" "app/kid/(app)/day/page.tsx"` returns 1 — the mobile hero
      spacing div survived the wrap.
    - `git diff --stat components/kid/design/atoms.tsx components/kid/day-fill/BottomSheet.tsx` shows
      zero changes.
    - `npx tsc --noEmit`, `npm run lint`, `npm run build` and `npm test` all exit 0.
  </acceptance_criteria>
  <done>Both Day-hero meters open the single shared BoostDetailSheet; the meters themselves are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kid browser → `'use server'` action | `handleBoostStyleTap` sends `childId` + `style` over the wire to `updateChildBoostStyle` |
| kid browser → Postgres (`children`) | Read-only via the anon client under family-isolation RLS; no write path exists on the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-095-14 | Elevation of Privilege | `handleBoostStyleTap` passing an arbitrary `childId` | mitigate | The component passes only `child.id`, the already-loaded active member's child, and the server action re-verifies independently with `requireFamilyMember()` + `authorizeChildAction()` (plan 01, T-095-02). The client is not trusted to scope the write. |
| T-095-15 | Tampering | a client-side `children` write sneaking in | mitigate | Asserted by the `grep -c "from('children')" components/kid/ProfileSheet.tsx` = 0 acceptance criterion; the only write path added is the server action import. |
| T-095-16 | Tampering | unbuilt styles reachable from the UI | mitigate | The picker renders a single `segmented-bar` button and the `grep -c "quest-checklist\|ring-badges"` = 0 criterion proves no dead-end control exists; the DB CHECK and the action whitelist remain the authoritative boundary if a value is ever posted directly. |
| T-095-17 | Denial of Service | nested-overlay click handling dismissing the wrong surface | accept | Cosmetic, not a security issue, but explicitly mitigated anyway by the `stopPropagation` wrapper around the ProfileSheet mount so a scrim tap closes only the detail sheet. |
| T-095-SC | Tampering | npm/pip/cargo installs | accept | No new dependency — this plan only wires existing modules. Nothing for the Package Legitimacy Gate to audit. |
</threat_model>

<verification>
1. `npx tsc --noEmit`, `npm run lint`, `npm test` and `npm run build` all exit 0.
2. `grep -c "<BoostDetailSheet" components/kid/ProfileSheet.tsx "app/kid/(app)/day/page.tsx"` reports
   1 for each file — three entry points, two mounted sheet instances (one per screen), never a
   duplicate within a screen.
3. `grep -rc "from('children')" components/kid/ProfileSheet.tsx "app/kid/(app)/day/page.tsx"` returns
   0 for both.
4. `git diff --stat components/kid/design/atoms.tsx` shows zero changes — the inline meter is untouched.
</verification>

<success_criteria>
- All three `BoostMeter` instances (ProfileSheet, Day hero full, Day hero compact) are wrapped in an
  accessible button that opens the detail sheet (D-01, BOOST-02).
- `components/kid/design/atoms.tsx` is byte-for-byte unchanged.
- `ProfileSheet` shows a `boost_style` row directly below the `fill_style` row, with `segmented-bar`
  as the only option, rendered in the active treatment, persisting through `updateChildBoostStyle`
  and surviving a reload (D-09, D-10, BOOST-01).
- Closing the detail sheet from inside `ProfileSheet` leaves `ProfileSheet` open.
</success_criteria>

<output>
Create `.planning/phases/09.5-boost-default/09.5-04-SUMMARY.md` when done
</output>
