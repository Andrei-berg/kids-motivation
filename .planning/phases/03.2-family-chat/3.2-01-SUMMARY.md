---
phase: 03.2-family-chat
plan: "01"
subsystem: database
tags: [supabase, postgres, realtime, typescript, rls]

# Dependency graph
requires: []
provides:
  - chat_messages SQL migration (DDL, RLS, Realtime)
  - ChatMessage TypeScript interface and MessageType union
  - chat.repo.ts with getMessages, sendMessage, subscribeToMessages
affects:
  - 3.2-02-chat-ui
  - 3.2-03-stickers
  - 3.2-04-notifications

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Supabase Realtime postgres_changes on INSERT filtered by family_id
    - Named exports only in repository modules (no default export)
    - Repository returns cleanup function from subscribeToMessages

key-files:
  created:
    - supabase-schema-chat.sql
    - lib/models/chat.types.ts
    - lib/repositories/chat.repo.ts
  modified: []

key-decisions:
  - "subscribeToMessages returns () => void cleanup — callers must call it on unmount to prevent channel leaks"
  - "ALTER PUBLICATION wrapped in idempotent DO block — safe to run migration multiple times"
  - "sender_id is TEXT not UUID — matches existing family_members.id convention in this codebase"

patterns-established:
  - "Chat repo pattern: browser-only (no server imports), named exports, supabase from @/lib/supabase"
  - "Realtime subscription: supabase.channel(`family-chat-${familyId}`).on('postgres_changes', ...).subscribe()"

requirements-completed: [CHAT-01]

# Metrics
duration: 1min
completed: 2026-04-14
---

# Phase 3.2 Plan 01: DB Schema Summary

**chat_messages table with RLS + Supabase Realtime, TypeScript ChatMessage types, and browser repository with getMessages/sendMessage/subscribeToMessages**

## Performance

- **Duration:** ~1 min
- **Started:** 2026-04-14T09:16:51Z
- **Completed:** 2026-04-14T09:17:43Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- SQL migration creates chat_messages table with all required columns plus composite index for efficient family message pagination
- RLS policies restrict read and insert to authenticated family members (matching existing medals table pattern)
- Supabase Realtime enabled via idempotent DO block — safe to re-run migration
- TypeScript types (ChatMessage, MessageType) define the data contract for all downstream UI plans
- Repository exports getMessages, sendMessage, subscribeToMessages — all browser-side, no server imports

## Task Commits

1. **Task 1: SQL migration** - `75d7f45` (feat)
2. **Task 2: TypeScript types and chat repository** - `1b73df9` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `supabase-schema-chat.sql` - DDL for chat_messages + 2 RLS policies + idempotent Realtime publication
- `lib/models/chat.types.ts` - MessageType union and ChatMessage interface
- `lib/repositories/chat.repo.ts` - getMessages, sendMessage, subscribeToMessages (browser-only)

## Decisions Made

- sender_id is TEXT to match existing family_members.id convention (not UUID)
- ALTER PUBLICATION wrapped in DO block for idempotent re-runs
- subscribeToMessages returns cleanup () => void — callers responsible for calling on unmount

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

**Manual step required before Phase 3.2 UI plans can function:**

Run `supabase-schema-chat.sql` in the Supabase SQL Editor (same process as previous migration files). This creates the chat_messages table, enables RLS, and registers the table for Realtime events.

## Next Phase Readiness

- Data contract fully established: ChatMessage shape and three repository functions are the sole interface for all 3.2 UI plans
- 3.2-02 (Chat UI) can import directly from `@/lib/repositories/chat.repo` and `@/lib/models/chat.types`
- No blockers

---
*Phase: 03.2-family-chat*
*Completed: 2026-04-14*
