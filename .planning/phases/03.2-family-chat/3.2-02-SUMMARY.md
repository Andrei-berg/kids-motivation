---
phase: 03.2-family-chat
plan: "02"
subsystem: ui
tags: [nextjs, supabase, realtime, react, tailwind, chat]

# Dependency graph
requires:
  - phase: 03.2-family-chat-01
    provides: chat_messages table, ChatMessage types, chat.repo.ts (getMessages/sendMessage/subscribeToMessages)
provides:
  - ChatThread component with Realtime subscription, optimistic sends, auto-scroll
  - SendBox component with text input and trimmed send
  - /parent/chat page (auth-gated, dark theme)
  - /kid/chat page (store-based member resolution, amber theme)
  - Chat nav entries in ParentNav and KidNav
affects:
  - 3.2-03-stickers
  - 3.2-04-notifications

# Tech tracking
tech-stack:
  added: []
  patterns:
    - ChatThread resolves member context via props (familyId, currentMemberId, senderName, senderRole)
    - Optimistic update pattern: insert temp message immediately, remove on sendMessage error
    - Duplicate prevention in Realtime handler: skip if msg.id already in state
    - Parent page uses createClient from lib/supabase/client for browser auth
    - Kid page uses supabase singleton from lib/supabase + activeMemberId from Zustand store

key-files:
  created:
    - components/chat/ChatThread.tsx
    - components/chat/SendBox.tsx
    - app/parent/chat/page.tsx
    - app/kid/chat/page.tsx
  modified:
    - components/parent/ParentNav.tsx
    - components/kid/KidNav.tsx

key-decisions:
  - "ChatThread accepts props not context — makes it reusable for sticker extension in plan 3.2-03"
  - "Optimistic messages use id prefix 'optimistic-{timestamp}' — Realtime dedup skips real msg with same id"
  - "Duplicate guard in subscribeToMessages callback prevents double-render after optimistic insert"

patterns-established:
  - "Chat page pattern: resolve familyId + memberId on mount, pass to ChatThread when ready, show spinner while loading"
  - "KidNav tab shape requires 4 fields: href, label, icon, activeColor, activeBg"

requirements-completed: [CHAT-01]

# Metrics
duration: 2min
completed: 2026-04-14
---

# Phase 3.2 Plan 02: Chat UI Summary

**Real-time family chat UI with ChatThread (Realtime subscription + optimistic updates), SendBox, /parent/chat and /kid/chat route pages, and nav entries in both ParentNav and KidNav**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-04-14T09:19:25Z
- **Completed:** 2026-04-14T09:21:07Z
- **Tasks:** 2 of 3 (Task 3 is checkpoint:human-verify, awaiting verification)
- **Files modified:** 6

## Accomplishments

- ChatThread component: loads messages from DB, subscribes to Supabase Realtime, auto-scrolls, renders bubbles with correct alignment (own messages right, others left), handles system messages
- SendBox component: controlled text input, trims whitespace, calls onSend, clears on submit
- /parent/chat: auth-gated via auth.getUser(), queries family_members for parent member, dark theme
- /kid/chat: uses activeMemberId from Zustand store, queries family_members for family_id, amber theme
- Chat link added to ParentNav (between Shop and Settings) and KidNav (after Leaderboard)

## Task Commits

1. **Task 1: ChatThread and SendBox shared components** - `1df521e` (feat)
2. **Task 2: Route pages and nav entries** - `1b762f3` (feat)

**Plan metadata:** (docs commit follows after human verification)

## Files Created/Modified

- `components/chat/ChatThread.tsx` - Message list with Realtime subscription, optimistic send, auto-scroll
- `components/chat/SendBox.tsx` - Text input + send button
- `app/parent/chat/page.tsx` - Parent chat entry point, auth-gated, dark theme
- `app/kid/chat/page.tsx` - Kid chat entry point, Zustand store, amber theme
- `components/parent/ParentNav.tsx` - Added Чат link at /parent/chat
- `components/kid/KidNav.tsx` - Added Чат tab at /kid/chat

## Decisions Made

- ChatThread accepts props (not context/hooks) for familyId and member info — keeps it portable for plan 3.2-03 sticker extension
- Optimistic update uses `id: optimistic-{timestamp}` prefix; Realtime dedup checks `prev.some(m => m.id === msg.id)` to skip when real message arrives with same content but different ID
- Duplicate prevention guard added in subscribeToMessages callback (not in plan) — required for correct behavior with optimistic inserts

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Added duplicate guard in Realtime subscription callback**
- **Found during:** Task 1 (ChatThread component)
- **Issue:** Without dedup, optimistic message + Realtime INSERT would show the same message twice
- **Fix:** Added `if (prev.some((m) => m.id === msg.id)) return prev` check in subscription handler
- **Files modified:** components/chat/ChatThread.tsx
- **Verification:** Logic prevents double-render for optimistic messages; TypeScript clean
- **Committed in:** 1df521e (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 missing critical for correctness)
**Impact on plan:** Necessary for correct message display. No scope creep.

## Issues Encountered

None.

## User Setup Required

**Before human verification (Task 3 checkpoint), run `supabase-schema-chat.sql` in Supabase SQL Editor** if not already done from plan 3.2-01. This creates the chat_messages table and enables Realtime.

## Next Phase Readiness

- Chat UI fully built and committed — awaiting human verification of Realtime delivery
- 3.2-03 (stickers) can extend ChatThread via new message_type handling — props-based design supports this
- 3.2-04 (notifications) can hook into sendMessage in chat.repo.ts

---
*Phase: 03.2-family-chat*
*Completed: 2026-04-14*
