---
phase: 05.9-rules-presets
plan: 09
type: execute
wave: 6
depends_on: [05.9-02, 05.9-04, 05.9-05, 05.9-07]
files_modified:
  - components/parent-center/screens/Settings.tsx
autonomous: false
requirements: [05.9-SC1, 05.9-SC2, 05.9-SC3]
must_haves:
  truths:
    - "A parent picks one of three preset cards, sees a per-field old->new diff preview, and only writes wallet_settings on confirm"
    - "A parent switches grade_scale via 3-way Tabs and edits a data-driven grade_coin_map row list"
    - "The Behavior Tags manager and the Behavior approval queue are mounted in Settings"
  artifacts:
    - path: "components/parent-center/screens/Settings.tsx"
      provides: "CoinsRulesTab: preset cards + diff-preview + grade-scale editor + mounted managers"
      contains: "getPresetValues"
  key_links:
    - from: "CoinsRulesTab apply-preset"
      to: "PATCH /api/wallet/settings"
      via: "updateWalletSettingsApi after diff-preview confirm"
      pattern: "updateWalletSettingsApi"
---

<objective>
Assemble the parent-facing rules surface inside CoinsRulesTab: three preset cards -> diff-preview -> confirm (server write), a 3-way grade-scale Tabs driving a data-driven grade_coin_map row list, and mount the Behavior Tags manager (plan 05) + Behavior approval queue (plan 07). All writes go through the existing server PATCH — never a client wallet_settings write.

Purpose: The single Settings screen where a parent tunes the rule philosophy, grading scale, and behavior tags.
Output: Extended CoinsRulesTab in Settings.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.9-rules-presets/05.9-PATTERNS.md
@.planning/phases/05.9-rules-presets/05.9-UI-SPEC.md
@.planning/phases/05.9-rules-presets/05.9-02-SUMMARY.md
@.planning/phases/05.9-rules-presets/05.9-04-SUMMARY.md
@.planning/phases/05.9-rules-presets/05.9-05-SUMMARY.md
@.planning/phases/05.9-rules-presets/05.9-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CoinsRulesTab — preset cards + diff-preview + grade-scale editor</name>
  <files>components/parent-center/screens/Settings.tsx</files>
  <read_first>
    - components/parent-center/screens/Settings.tsx (CoinsRulesTab lines 160-267: settings load/save, up() setter, save() via updateWalletSettingsApi + insertAuditEvent, RuleRow editor ~lines 191-204, fixed grade rows ~lines 212-216; ChildrenTab card-select idiom ~lines 327-378 for the preset-card interaction)
    - components/settings/PeriodsManager.tsx (handleApplyPreset ~lines 174-198; picker UI shell ~lines 359-390 — state machine only: selectedId -> disabled -> apply -> reload)
    - lib/presets.ts (getPresetValues, PRESET_IDS, GRADE_SCALE_VALUES, defaultGradeCoinMap)
    - components/design/atoms.tsx (Amount, Tabs — confirm which Tabs export Settings.tsx already imports and reuse it; do NOT introduce a second Tabs)
    - .planning/phases/05.9-rules-presets/05.9-UI-SPEC.md §"Copywriting Contract" + §"Color"/"Typography"/"Spacing" (3 equal-weight preset cards with selected indigo border+glow; "Apply preset" disabled until selected; diff-preview heading + confirm/cancel copy; "Grading scale" Tabs; forward-only switch toast; Amount for all coin values)
  </read_first>
  <action>
    Preset cards: render PRESET_IDS as 3 equal-weight cards (Classic / No Penalties / Bonuses Only) using the ChildrenTab card-select idiom (map options, active boolean drives border/background, click sets selectedPresetId), styled per UI-SPEC (selected = T.indigo border + subtle glow). The "Apply preset" primary Btn is disabled until a card is selected (mirror PeriodsManager's disabled={!selectedPresetId || applyingPreset}).
    Diff-preview (D-03): on Apply, compute patch = getPresetValues(selectedPresetId) and render a per-field old->new table over D-02's field list BEFORE writing — each changed field as a LedgerRow-style row with the new value in Amount (penalty/earn tone by sign so direction is obvious). Confirm ("Apply these changes") calls await updateWalletSettingsApi({ ...settings, ...patch }) then reloads + toasts; Cancel ("Keep current rules") dismisses without writing. NEVER write wallet_settings from the client directly — updateWalletSettingsApi is the server PATCH wrapper.
    Grade-scale editor: a 3-way Tabs ("5-point"/"12-point"/"A–F") bound to settings.grade_scale. Switching the tab sets grade_scale and, if grade_coin_map lacks keys for the new scale, seeds it from defaultGradeCoinMap(scale); show the forward-only informational toast (not a blocking modal — D-08). Below the Tabs, render the grade_coin_map rows data-driven: map over GRADE_SCALE_VALUES[grade_scale], one editable row per value (generalize the fixed 5-row RuleRow block into a .map()), each value shown/edited and displayed via Amount size="md". Saving persists grade_scale + grade_coin_map through updateWalletSettingsApi (the plan-04 validator guards the map).
    All new strings via useT() (keys from plan 02). Row padding 8px 16px.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "getPresetValues" components/parent-center/screens/Settings.tsx && grep -q "GRADE_SCALE_VALUES\|grade_coin_map" components/parent-center/screens/Settings.tsx && grep -q "updateWalletSettingsApi" components/parent-center/screens/Settings.tsx</automated>
  </verify>
  <done>CoinsRulesTab shows 3 preset cards, a per-field diff-preview gated behind an explicit confirm, a 3-way grade-scale Tabs, and a data-driven grade_coin_map row list; every write goes through updateWalletSettingsApi; tsc clean.</done>
  <acceptance_criteria>
    - Apply is disabled until a preset card is selected
    - A diff-preview (old->new per D-02 field) renders BEFORE any write; Cancel writes nothing
    - Grade rows are mapped from GRADE_SCALE_VALUES (no fixed 5-row block remains)
    - Coin values render via Amount (no raw span coin values)
    - No direct client `supabase.from('wallet_settings')` write appears in the file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mount BehaviorTagsManager + BehaviorApprovalQueue in Settings</name>
  <files>components/parent-center/screens/Settings.tsx</files>
  <read_first>
    - components/parent-center/screens/Settings.tsx (how CoinsRulesTab / other tabs are laid out and where RoomTasksManager is mounted for a placement precedent)
    - components/settings/BehaviorTagsManager.tsx (plan 05 export)
    - components/parent-center/screens/BehaviorApprovalQueue.tsx (plan 07 export)
    - .planning/phases/05.9-rules-presets/05.9-UI-SPEC.md §"Spacing Scale" (lg=24px vertical rhythm between major settings sections)
  </read_first>
  <action>
    Mount BehaviorTagsManager and BehaviorApprovalQueue inside the Coin Rules area of Settings (same region as the presets/grade-scale editor, or an adjacent Behavior section header) with 24px (lg) vertical rhythm between sections. The approval queue is the canonical parent review surface (resolved OQ3). Import both components; render them with the correct ink theme. Do not duplicate their internal logic here — just place and title them (section headings via useT()). Keep the existing tab structure intact.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "BehaviorTagsManager" components/parent-center/screens/Settings.tsx && grep -q "BehaviorApprovalQueue" components/parent-center/screens/Settings.tsx</automated>
  </verify>
  <done>Both new components are mounted and reachable from Parent Center Settings; tsc clean.</done>
  <acceptance_criteria>
    - BehaviorTagsManager and BehaviorApprovalQueue are both imported and rendered in Settings.tsx
    - Section spacing uses the 24px lg rhythm
    - Existing Settings tabs still compile and render
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the Settings rules surface</name>
  <what-built>CoinsRulesTab now has 3 preset cards, a diff-preview-gated apply, a 3-way grade-scale editor with data-driven rows, and mounts the Behavior Tags manager + approval queue.</what-built>
  <how-to-verify>
    1. Open Parent Center -> Settings -> Coin Rules.
    2. Select a preset card; confirm "Apply preset" enables only after selection; click it and confirm a per-field old->new diff table appears (e.g. "Grade 3 · -3 -> 0") with values in gold Amount and direction obvious; click "Keep current rules" and confirm nothing changed; re-apply and "Apply these changes" and confirm the coin rules update.
    3. Switch the Grading scale Tabs between 5-point / 12-point / A–F; confirm the grade row list re-renders with the correct number of rows/labels and the forward-only notice toast appears; edit a value and save; confirm it persists.
    4. In Behavior Tags, add a tag with a negative price, rename/toggle/reorder; confirm the seeded "🔒 Default tag" cannot be deleted.
    5. Confirm the approval queue section renders (empty state if no pending marks).
  </how-to-verify>
  <action>Operator exercises the five interactions in how-to-verify against a running dev server and confirms the Settings rules surface behaves per the UI-SPEC contract.</action>
  <verify>
    <human-check>Operator confirms all five interactions behave per the UI-SPEC copy/color/spacing contract.</human-check>
  </verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent client -> updateWalletSettingsApi (PATCH) | untrusted body; presets/grade_coin_map validated server-side (plan 04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-059-02 | Tampering | preset/grade_coin_map write | mitigate | All writes route through PATCH /api/wallet/settings (requireParent + clampGradeCoinMap from plan 04); the client never writes wallet_settings directly. |
| T-059-15 | Tampering | preset silently overwrites out-of-scope settings | mitigate | getPresetValues (plan 02, unit-tested) touches only D-02's field list; the diff-preview shows exactly which fields change before confirm. |
</threat_model>

<verification>
- npx tsc --noEmit clean.
- Full build green (npm run build) before the human-verify checkpoint.
- Operator sign-off on the five interactions.
</verification>

<success_criteria>
A parent can apply a rule preset (with a visible diff), switch grading scale (data-driven rows, forward-only), manage behavior tags, and review the approval queue — all from one Settings surface, all through the server write path.
</success_criteria>

<output>
Create `.planning/phases/05.9-rules-presets/05.9-09-SUMMARY.md` when done.
</output>
