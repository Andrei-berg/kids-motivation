---
phase: 05.9-rules-presets
plan: 03
type: execute
wave: 2
depends_on: [05.9-01]
files_modified: []
autonomous: false
requirements: [05.9-SC2, 05.9-SC3]
must_haves:
  truths:
    - "All three 05.9 migrations are applied to the live prod DB in order"
    - "wallet_settings has grade_scale + grade_coin_map; subject_grades.grade is TEXT with no numeric CHECK; behavior_tags/behavior_marks exist with the status='pending' INSERT guard"
    - "The seeded 'Good behavior' tag exists for every existing family"
  artifacts:
    - path: "supabase/migrations/05.9-01-wallet-settings-grade-scale.sql"
      provides: "applied to live DB"
    - path: "supabase/migrations/05.9-03-behavior-tags.sql"
      provides: "applied to live DB"
  key_links:
    - from: "local migration files"
      to: "live prod Postgres"
      via: "pg node one-liner per CLAUDE.md"
      pattern: "SUPABASE_DB_URL"
---

<objective>
[BLOCKING] Apply the three Phase 5.9 migrations to the live prod DB, in order, and verify the schema landed. This gate exists because build/type/unit checks all pass WITHOUT the push (TypeScript types don't come from the live DB) — the integration tests in wave 4 will silently false-positive or hard-fail against an unmigrated DB. Nothing downstream may run until this completes.

Purpose: Make the authored schema real before any integration test touches the live DB.
Output: Three applied migrations + a verification readout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.9-rules-presets/05.9-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply 05.9-01, 05.9-02, 05.9-03 to the live DB in order</name>
  <what-built>Three authored migration files in supabase/migrations/ (from plan 01): grade_scale/grade_coin_map columns, subject_grades.grade TEXT widen, behavior_tags/behavior_marks tables.</what-built>
  <read_first>
    - CLAUDE.md §"Applying DB migrations" (the exact pg node one-liner; SUPABASE_DB_URL must be set in .env.local)
    - supabase/migrations/05.9-01-wallet-settings-grade-scale.sql
    - supabase/migrations/05.9-02-subject-grades-widen.sql
    - supabase/migrations/05.9-03-behavior-tags.sql
  </read_first>
  <how-to-verify>
    1. Ensure `SUPABASE_DB_URL` is set in `.env.local` (Session pooler URI). If unset, this task requires the operator to add it or apply the SQL via the Supabase SQL Editor.
    2. Install the transient pg client: `npm i pg --no-save` (already the project's documented pattern; not added to package.json).
    3. Apply each file IN ORDER using the CLAUDE.md one-liner (once per file): 05.9-01, then 05.9-02, then 05.9-03. Each should print "applied".
    4. Verify columns: query `information_schema.columns` for `wallet_settings` — expect `grade_scale` and `grade_coin_map` present; query `subject_grades` — expect `grade` data_type = `text`.
    5. Verify tables: `SELECT to_regclass('public.behavior_tags'), to_regclass('public.behavior_marks')` — both non-null.
    6. Verify seed: `SELECT count(*) FROM behavior_tags WHERE legacy_key = 'good_behavior'` equals the family count (`SELECT count(*) FROM families`).
    7. Verify the INSERT guard exists: `SELECT count(*) FROM pg_policies WHERE tablename = 'behavior_marks'` returns 4, and the INSERT policy's `with_check` text contains `status = 'pending'`.
  </how-to-verify>
  <action>Operator applies the three migrations in order via the CLAUDE.md pg one-liner (or Supabase SQL Editor), then runs the schema readbacks in how-to-verify. This is a human-gated live-DB apply — Claude prepares the exact commands; the operator executes and confirms.</action>
  <verify>
    <automated>MISSING — this is a live-DB apply gated on operator/DB access; verified via the SQL readbacks in how-to-verify, then confirmed by the wave-4 integration tests running green against the migrated DB.</automated>
  </verify>
  <resume-signal>Type "applied" once all three migrations report success and the schema readbacks (columns, tables, seed count, INSERT policy) match. Report any error output verbatim if a migration fails.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev machine → prod Postgres | DDL applied with the DB owner connection string; operator-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-059-SC | Tampering | `npm i pg --no-save` install | accept | `pg` is the project's already-documented, dev-only transient for migration application (CLAUDE.md); not a new dependency, not persisted to package.json. RESEARCH Package Legitimacy Audit: "not applicable — installs no new external packages." No [ASSUMED]/[SUS] gate required. |
| T-059-07 | Denial of Service | subject_grades TYPE widen | accept | Lossless `ALTER COLUMN ... TYPE text USING grade::text` on a modest table; brief lock acceptable, no rollback needed (widen is non-destructive). |
</threat_model>

<verification>
- All three migrations report "applied".
- Schema readbacks (columns, tables, seed count, INSERT policy with_check) match expectations.
- This plan modifies no repo files — it is a live-DB state change only.
</verification>

<success_criteria>
The live prod DB carries the full Phase 5.9 schema, verified by direct readback, unblocking every wave-3/4 plan.
</success_criteria>

<output>
Create `.planning/phases/05.9-rules-presets/05.9-03-SUMMARY.md` when done. Record the readback results (column presence, table presence, seed count vs family count, INSERT policy with_check text).
</output>
