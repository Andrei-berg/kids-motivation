---
phase: 05.9-rules-presets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/05.9-01-wallet-settings-grade-scale.sql
  - supabase/migrations/05.9-02-subject-grades-widen.sql
  - supabase/migrations/05.9-03-behavior-tags.sql
autonomous: true
requirements: [05.9-SC2, 05.9-SC3]
must_haves:
  truths:
    - "wallet_settings can store grade_scale + grade_coin_map without a new RLS policy"
    - "subject_grades.grade can hold a non-numeric literal like '11' or 'A' after the widen"
    - "behavior_tags/behavior_marks exist mirroring room_tasks/room_checks, with a status column and a child-cannot-self-approve INSERT guard"
  artifacts:
    - path: "supabase/migrations/05.9-01-wallet-settings-grade-scale.sql"
      provides: "grade_scale + grade_coin_map columns (additive, no RLS change)"
      contains: "ADD COLUMN IF NOT EXISTS grade_scale"
    - path: "supabase/migrations/05.9-02-subject-grades-widen.sql"
      provides: "grade TEXT widen + numeric CHECK drop"
      contains: "ALTER COLUMN grade TYPE TEXT"
    - path: "supabase/migrations/05.9-03-behavior-tags.sql"
      provides: "behavior_tags + behavior_marks tables, RLS, seed, legacy-delete guard, status-pending INSERT constraint"
      contains: "CREATE TABLE IF NOT EXISTS public.behavior_marks"
  key_links:
    - from: "behavior_marks INSERT (non-service-role)"
      to: "status='pending'"
      via: "WITH CHECK / trigger constraint"
      pattern: "status = 'pending'"
---

<objective>
Author the three SQL migrations for Phase 5.9 de-hardcoding: grade_scale/grade_coin_map columns on `wallet_settings`, a TEXT widen of `subject_grades.grade` (dropping the numeric CHECK), and the `behavior_tags`/`behavior_marks` table pair (a structural clone of `room_tasks`/`room_checks` with a propose/approve `status` column and RLS that forbids a child from self-approving).

Purpose: Every later plan depends on this schema. This plan AUTHORS the SQL only — plan 03 applies it to the live DB (BLOCKING wave).
Output: Three `supabase/migrations/05.9-0N-*.sql` files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.9-rules-presets/05.9-CONTEXT.md
@.planning/phases/05.9-rules-presets/05.9-PATTERNS.md
@.planning/phases/05.9-rules-presets/05.9-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: grade_scale + grade_coin_map columns (05.9-01) and subject_grades widen (05.9-02)</name>
  <files>supabase/migrations/05.9-01-wallet-settings-grade-scale.sql, supabase/migrations/05.9-02-subject-grades-widen.sql</files>
  <read_first>
    - supabase/migrations/04.4-03-wallet-rls-readonly.sql (confirms wallet_settings is ALREADY RLS SELECT-only — new columns inherit it, NO new policy needed)
    - app/api/wallet/_lib.ts lines 36-76 (SETTINGS_DEFAULTS, loadSettings — the shape these columns must satisfy)
    - .planning/phases/05.9-rules-presets/05.9-PATTERNS.md §"05.9-01 and 05.9-02" (additive-column + widen guidance)
  </read_first>
  <action>
    05.9-01: `ALTER TABLE public.wallet_settings ADD COLUMN IF NOT EXISTS grade_scale TEXT NOT NULL DEFAULT 'five_point' CHECK (grade_scale IN ('five_point','twelve_point','a_f'))` and `ADD COLUMN IF NOT EXISTS grade_coin_map JSONB` (nullable — read-time fallback derives it from coins_per_grade_1..5 in plan 04, so NO backfill UPDATE here per resolved Open Question 4). Do NOT add or alter any RLS policy — wallet_settings already carries the 04.4-03 SELECT-only lock and these columns inherit it. Include a leading comment block naming the phase and the D-05 decision.
    05.9-02: BEFORE writing, resolve the live CHECK constraint name (RESEARCH captured the behavior `CHECK (grade >= 2 AND grade <= 5)` but not the constraint name). Run against the live DB: query `information_schema.check_constraints`/`pg_constraint` for the constraint on `subject_grades.grade` using the documented `pg` node one-liner (see CLAUDE.md "Applying DB migrations" — a read-only SELECT, not a DDL apply). Then author: `ALTER TABLE public.subject_grades ALTER COLUMN grade TYPE TEXT USING grade::text;` followed by `ALTER TABLE public.subject_grades DROP CONSTRAINT IF EXISTS <resolved_name>;`. Use `DROP CONSTRAINT IF EXISTS` with the resolved name; if the name resolves to the conventional `subject_grades_grade_check`, use that. This is a non-money table (family-isolation RLS) — no RLS template needed. The widen is lossless (integers "2".."5" become literal strings).
  </action>
  <verify>
    <automated>test -f supabase/migrations/05.9-01-wallet-settings-grade-scale.sql && test -f supabase/migrations/05.9-02-subject-grades-widen.sql && grep -q "grade_scale IN ('five_point','twelve_point','a_f')" supabase/migrations/05.9-01-wallet-settings-grade-scale.sql && grep -q "ALTER COLUMN grade TYPE TEXT" supabase/migrations/05.9-02-subject-grades-widen.sql</automated>
  </verify>
  <done>Both SQL files exist; 05.9-01 adds both columns with the grade_scale CHECK and no RLS change; 05.9-02 widens grade to TEXT and drops the numeric CHECK by its live-verified name.</done>
  <acceptance_criteria>
    - `grep -c "ADD COLUMN IF NOT EXISTS" supabase/migrations/05.9-01-wallet-settings-grade-scale.sql` returns 2 (grade_scale + grade_coin_map)
    - 05.9-01 contains NO `CREATE POLICY` / `ALTER POLICY` / `DROP POLICY` statement (grep returns nothing)
    - 05.9-02 contains `DROP CONSTRAINT IF EXISTS` with the constraint name that was resolved from the live DB (not a guessed name unless it matched)
    - Neither file contains an UPDATE backfill of grade_coin_map
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: behavior_tags + behavior_marks migration (05.9-03) — Room Tasks clone + status/approval guard</name>
  <files>supabase/migrations/05.9-03-behavior-tags.sql</files>
  <read_first>
    - supabase/migrations/05.2-01-room-tasks.sql (the byte-for-byte structural template: table shape, 4 RLS policies per table, seed function + backfill SELECT over families, legacy-delete guard trigger with pg_trigger_depth() <= 1)
    - .planning/phases/05.9-rules-presets/05.9-PATTERNS.md §"05.9-03-behavior-tags.sql" (exact adaptation notes)
    - .planning/phases/05.9-rules-presets/05.9-RESEARCH.md §"Security Domain" (the child-cannot-self-approve INSERT constraint requirement) and §"Pattern 3" (schema skeleton)
  </read_first>
  <action>
    Clone `05.2-01-room-tasks.sql` and rename room_tasks→behavior_tags, room_checks→behavior_marks, task_id→tag_id. Deltas from the analog:
    (1) behavior_tags gains `price INTEGER NOT NULL` (signed — positive OR negative per D-10). Keep `legacy_key TEXT` with CHECK `legacy_key IS NULL OR legacy_key = 'good_behavior'`, plus family_id FK cascade, name, icon, sort_order, is_active, created_at. Keep the `(family_id, legacy_key) WHERE legacy_key IS NOT NULL` unique index and the `family_id` index.
    (2) behavior_marks replaces `done BOOLEAN` with `status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','approved','rejected'))` and adds `proposed_by TEXT`, `decided_by TEXT`, `decided_at TIMESTAMPTZ`. Keep child_id TEXT FK, family_id UUID FK, date DATE, tag_id UUID FK.
    (3) RESOLVED Open Question 2 (multiplicity): DO NOT add a `UNIQUE(child_id, date, tag_id)` constraint — behavior marks are event-like/repeatable (the same tag may legitimately apply twice in one day, e.g. "Helped a sibling" twice). Each mark is a distinct row → its own id → its own idempotency slot in creditAwards. Add a leading comment stating this decision and that it is a deliberate divergence from room_checks' uniqueness constraint.
    (4) RLS: copy the four family-isolation policies (SELECT/INSERT/UPDATE/DELETE) verbatim for BOTH tables. On behavior_marks, the INSERT policy MUST additionally forbid a non-service-role insert from setting status other than 'pending' (RESOLVED threat T-059-03). Enforce this at the DB boundary, not app-layer: EITHER a `WITH CHECK (... AND status = 'pending')` clause on the authenticated INSERT policy (the service-role client bypasses RLS entirely, so parent-direct/approval writes are unaffected), OR a BEFORE INSERT trigger that forces status='pending' when the row is not inserted by the service role. Prefer the `WITH CHECK (status = 'pending')` clause on the family-isolation INSERT policy — simplest and self-documenting.
    (5) Seed function `seed_default_behavior_tags(p_family_id UUID)` seeding ONE row: name 'Good behavior', icon '😊', price 5, legacy_key 'good_behavior', sort_order 0, is_active true, `ON CONFLICT (family_id, legacy_key) WHERE legacy_key IS NOT NULL DO NOTHING`. GRANT EXECUTE to authenticated. Backfill: `SELECT public.seed_default_behavior_tags(id) FROM public.families;`.
    (6) Legacy-delete guard: `behavior_tags_block_legacy_delete_fn()` raising when `OLD.legacy_key IS NOT NULL AND pg_trigger_depth() <= 1`, wired as a BEFORE DELETE trigger (copy the room_tasks guard verbatim, adapt names). Rationale for `<= 1` (not `= 0`) is in the 05.2-01 analog — keep it.
  </action>
  <verify>
    <automated>test -f supabase/migrations/05.9-03-behavior-tags.sql && grep -q "CREATE TABLE IF NOT EXISTS public.behavior_tags" supabase/migrations/05.9-03-behavior-tags.sql && grep -q "status IN ('pending','approved','rejected')" supabase/migrations/05.9-03-behavior-tags.sql && grep -q "seed_default_behavior_tags" supabase/migrations/05.9-03-behavior-tags.sql && ! grep -q "UNIQUE (child_id, date, tag_id)" supabase/migrations/05.9-03-behavior-tags.sql</automated>
  </verify>
  <done>05.9-03 creates both tables with signed price, status column, no same-tag uniqueness constraint, family-isolation RLS on both, a status='pending' INSERT guard on behavior_marks, the seed function + families backfill, and the legacy-delete guard trigger.</done>
  <acceptance_criteria>
    - behavior_marks INSERT policy contains a `WITH CHECK` that includes `status = 'pending'` (or a trigger forcing it) — verifiable by grep for `status = 'pending'`
    - `grep -c "CREATE POLICY" supabase/migrations/05.9-03-behavior-tags.sql` returns 8 (4 per table)
    - price column declared `INTEGER NOT NULL` (signed, no `CHECK (price >= 0)`)
    - No `UNIQUE (child_id, date, tag_id)` present (resolved OQ2)
    - Legacy-delete guard uses `pg_trigger_depth() <= 1`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child client → behavior_marks INSERT | untrusted: a child proposes a tag; must not be able to grant itself 'approved' status |
| authenticated client → wallet_settings write | already denied by 04.4-03 SELECT-only lock; new columns inherit it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-059-03 | Tampering/EoP | behavior_marks INSERT (child role) | mitigate | RLS INSERT `WITH CHECK (status = 'pending')` on the family-isolation policy — a non-service-role insert can only ever create a pending mark; approval flips status only via the service-role path (plan 07). RLS is the boundary, not app discipline. |
| T-059-04 | Tampering | wallet_settings new columns | accept | grade_scale/grade_coin_map inherit the existing 04.4-03 SELECT-only lock; no client write path exists. Write-time value validation handled in plan 04. |
| T-059-05 | Elevation | behavior_tags CRUD | accept | family-isolation RLS (client-writable config, not money) — same trust level as room_tasks; deletion of the seeded default is blocked by the legacy-delete guard. |
</threat_model>

<verification>
- All three SQL files exist and pass their grep gates.
- 05.9-02's dropped-constraint name was resolved against the live DB before authoring (not guessed).
- No file authored here is applied to the live DB by this plan — application is plan 03.
</verification>

<success_criteria>
Three migration files authored, structurally faithful to the 04.4-03 (RLS lock reference), additive-column, and 05.2-01 (Room Tasks clone) precedents, with the two behavior-specific security deviations (status='pending' INSERT guard) and the resolved multiplicity decision (no same-tag uniqueness) baked in.
</success_criteria>

<output>
Create `.planning/phases/05.9-rules-presets/05.9-01-SUMMARY.md` when done. Record the resolved subject_grades CHECK constraint name for plan 03.
</output>
