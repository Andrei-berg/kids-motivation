---
phase: 05.9-rules-presets
plan: 10
type: execute
wave: 4
depends_on: [05.9-02, 05.9-04]
files_modified:
  - app/onboarding/v2/page.tsx
  - lib/onboarding-api.ts
autonomous: false
requirements: [05.9-SC1]
must_haves:
  truths:
    - "Onboarding Step 3 is a 3-card preset picker (no manual number fields) — tap one, continue"
    - "completeOnboarding writes wallet_settings server-side (via the PATCH route), fixing the pre-existing RLS-denied client write"
    - "A new family finishes onboarding without the wallet_settings write being denied by RLS"
  artifacts:
    - path: "app/onboarding/v2/page.tsx"
      provides: "StepCoinRules replaced by preset picker"
      contains: "PresetId"
    - path: "lib/onboarding-api.ts"
      provides: "completeOnboarding(presetId) routing through the server"
      contains: "getPresetValues"
  key_links:
    - from: "completeOnboarding"
      to: "PATCH /api/wallet/settings"
      via: "server-side write of getPresetValues(presetId)"
      pattern: "wallet/settings"
---

<objective>
Replace onboarding's manual coin-rule number entry (StepCoinRules) with a 3-card preset picker (D-04), and fix the pre-existing production defect where completeOnboarding writes wallet_settings from the browser client (RLS-denied since 04.4-03) by routing that write through the server PATCH. RESOLVED assumption A2: reuse the existing PATCH /api/wallet/settings route (requireParent resolves after createFamily) rather than a new dedicated route.

Purpose: One-tap rule philosophy at signup + closing a real onboarding-blocking bug.
Output: Onboarding page preset step + server-side completeOnboarding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.9-rules-presets/05.9-PATTERNS.md
@.planning/phases/05.9-rules-presets/05.9-UI-SPEC.md
@.planning/phases/05.9-rules-presets/05.9-RESEARCH.md
@.planning/phases/05.9-rules-presets/05.9-02-SUMMARY.md
@.planning/phases/05.9-rules-presets/05.9-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: completeOnboarding — server-side preset write (fix the RLS-denied client upsert)</name>
  <files>lib/onboarding-api.ts</files>
  <read_first>
    - lib/onboarding-api.ts (completeOnboarding ~lines 515-569 — the broken createClient().from('wallet_settings').upsert(...) browser write)
    - app/api/wallet/settings/route.ts (the PATCH handler to route through — requireParent + service-role)
    - lib/presets.ts (getPresetValues, PresetId)
    - .planning/phases/05.9-rules-presets/05.9-RESEARCH.md §"Pitfall 3" (the defect) + §"Assumptions Log" A2 (reuse PATCH route)
  </read_first>
  <action>
    Change completeOnboarding's signature from `coinRules?: { g5, g4, g3, g2, room, sport }` to `presetId: PresetId`. Remove the browser-client `createClient().from('wallet_settings').upsert(...)` entirely (it is RLS-denied since 04.4-03). Instead resolve `patch = getPresetValues(presetId)` and write it server-side by calling `PATCH /api/wallet/settings` (fetch the wrapper that CoinsRulesTab uses, e.g. updateWalletSettingsApi, or a direct authenticated fetch to the route) — this runs requireParent server-side and bypasses RLS via the service-role client. This point in the flow is post-createFamily + authenticated-as-parent, so requireParent resolves. Do NOT reintroduce any client-side wallet_settings write. Surface a clear error if the PATCH fails (keep handleFinish's catch working).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "getPresetValues" lib/onboarding-api.ts && ! grep -q "from('wallet_settings').upsert\|from(\"wallet_settings\").upsert" lib/onboarding-api.ts</automated>
  </verify>
  <done>completeOnboarding takes a PresetId and writes preset values through the server PATCH; the browser wallet_settings upsert is gone; tsc clean.</done>
  <acceptance_criteria>
    - No `from('wallet_settings').upsert` (browser client write) remains in the file
    - completeOnboarding resolves values via getPresetValues(presetId) and writes via the server PATCH path
    - Signature no longer accepts g5/g4/g3/g2/room/sport
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: onboarding/v2 StepCoinRules -> 3-card preset picker</name>
  <files>app/onboarding/v2/page.tsx</files>
  <read_first>
    - app/onboarding/v2/page.tsx (current StepCoinRules ~lines 270-360; CoinRulesState local state ~line 577 useState({ g5:5, g4:3, g3:-3, room:3, sport:10 }); handleFinish ~line 614 completeOnboarding(familyId, customCoinRules))
    - components/parent-center/screens/Settings.tsx ChildrenTab card-select idiom ~lines 327-378 (icon + label + desc + selected-dot — the closest in-codebase card-select precedent; copy the interaction structure, not the ink tokens — onboarding is its own light theme)
    - lib/presets.ts (PRESET_IDS, PresetId)
    - .planning/phases/05.9-rules-presets/05.9-UI-SPEC.md §"Copywriting Contract" (3 preset card labels/subheads; "Continue with this preset" enabled the instant a card is tapped; no numbers shown)
  </read_first>
  <action>
    Replace StepCoinRules' manual number fields with 3 preset cards (Classic / No Penalties / Bonuses Only) using the card-select interaction structure (map over PRESET_IDS, active boolean drives border/background, click sets selectedPresetId), styled in the onboarding light theme (not ink tokens). No number fields shown (D-04). Replace the local `coinRules: CoinRulesState` state with `selectedPresetId: PresetId | null`. The step's primary CTA "Continue with this preset" enables the instant a card is tapped. Update handleFinish to call `completeOnboarding(familyId, selectedPresetId)`. All strings via useT() (onboarding.presetStep.* keys from plan 02).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "PresetId\|selectedPresetId" app/onboarding/v2/page.tsx && ! grep -q "CoinRulesState" app/onboarding/v2/page.tsx</automated>
  </verify>
  <done>Onboarding Step 3 is a 3-card preset picker with no number fields; handleFinish passes the presetId; tsc clean.</done>
  <acceptance_criteria>
    - StepCoinRules renders 3 preset cards, no manual coin number inputs
    - CoinRulesState is removed; selectedPresetId state drives the step
    - "Continue with this preset" enables on first tap
    - handleFinish calls completeOnboarding(familyId, selectedPresetId)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify new-family onboarding completes (RLS fix)</name>
  <what-built>Onboarding preset step + server-side completeOnboarding write.</what-built>
  <how-to-verify>
    1. Walk through /onboarding/v2 as a brand-new family (fresh account).
    2. At Step 3, confirm 3 preset cards appear with no number fields; tap one; "Continue with this preset" is enabled.
    3. Finish onboarding and confirm it SUCCEEDS through to the invite-code screen — previously the wallet_settings write was RLS-denied and blocked completion (RESEARCH Pitfall 3).
    4. In Parent Center -> Settings -> Coin Rules, confirm the chosen preset's values are present.
  </how-to-verify>
  <action>Operator walks a brand-new family through /onboarding/v2 and confirms the preset step plus successful completion (the RLS-defect fix) against a running dev server.</action>
  <verify>
    <human-check>Operator confirms onboarding completes without the wallet_settings RLS error and the preset values landed.</human-check>
  </verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| onboarding client -> wallet_settings write | previously a denied client write; now routed server-side via requireParent PATCH |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-059-16 | Tampering | onboarding wallet_settings write | mitigate | Write routes through PATCH /api/wallet/settings (requireParent + service-role + clampGradeCoinMap); the RLS-denied browser upsert is removed. Preset values come from getPresetValues (D-02-scoped). |
| T-059-17 | Denial of Service | onboarding blocked by RLS-denied write | mitigate | This plan fixes the pre-existing defect that blocked new-family onboarding; human-verify confirms completion. |
</threat_model>

<verification>
- npx tsc --noEmit clean; npm run build green.
- Operator confirms a new family completes onboarding and the preset values persist.
</verification>

<success_criteria>
New families pick a rule philosophy in one tap during onboarding, and the (previously broken) wallet_settings write completes server-side.
</success_criteria>

<output>
Create `.planning/phases/05.9-rules-presets/05.9-10-SUMMARY.md` when done. Flag the RLS-defect fix explicitly (it was a live production bug).
</output>
