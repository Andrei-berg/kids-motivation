---
phase: 5.9
slug: rules-presets
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-21
---

# Phase 5.9 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest ^4.1.2 |
| **Config file** | `vitest.config.ts` (assumed standard project root config — confirm path in Wave 0 if missing) |
| **Quick run command** | `npm test` (`vitest run`) — unit/mocked tests |
| **Full suite command** | `npm run test:integration` (`node --env-file=.env.local ./node_modules/vitest/vitest.mjs run tests/integration`) — hits the live DB |
| **Estimated runtime** | ~30-60s unit, ~2-3 min integration (live DB round trips) |

---

## Sampling Rate

- **After every task commit:** Run `npm test`
- **After every plan wave:** Run `npm run test:integration`
- **Before `/gsd:verify-work`:** Full suite must be green, plus `npx tsc --noEmit` clean (critical — the `grade: number → string` type change touches ~6 files)
- **Max feedback latency:** 180 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | 0 | SC1 (preset apply) | — | Applying a preset writes the correct `Partial<WalletSettings>` patch, touching only D-02's field list | unit | `npx vitest run tests/presets.test.ts` | ❌ W0 |
| TBD | TBD | TBD | SC2a (grade_scale drives award mapping) | — | `/api/wallet/award` looks up `grade_coin_map[grade]` correctly for all 3 scales | integration | `npx vitest run tests/integration/award.test.ts -t grade` | ✅ existing, extend |
| TBD | TBD | TBD | SC2b (`subject_grades.grade` stores literal value) | — | Insert/read round-trips `"11"`/`"B+"` without lossy conversion | integration | `npx vitest run tests/integration/award.test.ts` | ✅ existing, extend |
| TBD | TBD | TBD | SC3a (behavior tag CRUD) | T-1 (self-approval EoP) | Add/rename/toggle/reorder/delete a behavior tag | integration | new `tests/integration/behavior-award.test.ts` | ❌ W0 |
| TBD | TBD | TBD | SC3b (behavior award = sum of approved marks) | T-2 (crafted map tampering) | Multiple approved marks on one day sum correctly; pending marks contribute 0 | integration | new `tests/integration/behavior-award.test.ts` | ❌ W0 |
| TBD | TBD | TBD | SC3c (propose/approve flow) | T-1, T-3 (client-set status='approved') | Child-proposed mark stays `pending` until parent approves; only then credited | integration | new `tests/integration/behavior-approval.test.ts` | ❌ W0 |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky — Task/Plan/Wave IDs to be filled in by the planner once PLAN.md files exist.*

---

## Wave 0 Requirements

- [ ] `tests/presets.test.ts` — unit tests for `getPresetValues()` covering D-01's two-value-set claim (Classic ≠ No-penalties/Bonuses-only; No-penalties === Bonuses-only numerically)
- [ ] `tests/integration/behavior-award.test.ts` — covers SC3a/SC3b, modeled on `tests/integration/room-award.test.ts`'s structure
- [ ] `tests/integration/behavior-approval.test.ts` — covers SC3c (propose/approve/reject), modeled on `tests/integration/purchase.test.ts`'s approve/reject shape
- [ ] Extend existing `tests/integration/award.test.ts` grade cases (currently only exercises `grade5`/`EXPECTED.grade5`) to cover all 3 scales once `grade_coin_map` lands — extend the existing file, do not duplicate
- [ ] Confirm `vitest.config.ts` location/content (not directly verified during research)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Onboarding preset picker (Step 3 replaces StepCoinRules) | SC1 | Visual/interaction flow across onboarding steps; UI-SPEC-driven | Walk through `/onboarding/v2` as a new family, confirm 3 preset cards, tap one, confirm `completeOnboarding` succeeds (also exercises the RLS-lock fix — see RESEARCH.md pitfall on `completeOnboarding` currently writing `wallet_settings` client-side) |
| Settings preset preview diff | D-03 | Requires visually confirming old→new value diff renders correctly before confirm | Apply a preset in Parent Center → Settings → Coin Rules, confirm diff table shows per-field before/after, cancel vs confirm both work |
| Behavior tag parent-review queue | SC3c | UI-SPEC-driven screen not fully pinned by CONTEXT.md (left to discretion) | Propose a behavior mark as child, confirm it appears in parent's pending queue, approve/reject, confirm coin credit only on approve |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 180s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
