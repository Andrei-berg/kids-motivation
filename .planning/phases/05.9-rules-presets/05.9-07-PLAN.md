---
phase: 05.9-rules-presets
plan: 07
type: execute
wave: 5
depends_on: [05.9-06]
files_modified:
  - app/parent/behavior/actions.ts
  - components/parent-center/screens/BehaviorApprovalQueue.tsx
  - components/DailyModal.tsx
  - tests/integration/behavior-approval.test.ts
autonomous: true
requirements: [05.9-SC3]
must_haves:
  truths:
    - "Only a parent can approve/reject a pending behavior mark (child self-approval is refused server-side)"
    - "Approval flips status to 'approved' but moves no coins itself — the next /api/wallet/award POST credits it"
    - "A dedicated parent approval queue lists pending child-proposed marks; approve/reject act on one row each"
    - "A parent applying a tag directly in DailyModal takes effect immediately (not gated by approval)"
  artifacts:
    - path: "app/parent/behavior/actions.ts"
      provides: "approveBehaviorMark + rejectBehaviorMark (requireParent, service-role, status-only)"
      exports: ["approveBehaviorMark", "rejectBehaviorMark"]
    - path: "components/parent-center/screens/BehaviorApprovalQueue.tsx"
      provides: "dedicated pending-mark review surface"
  key_links:
    - from: "approveBehaviorMark"
      to: "behavior_marks.status"
      via: "service-role update pending→approved, no coin mutation"
      pattern: "status.*approved"
---

<objective>
Build the money-adjacent approval layer for behavior tags. RESOLVED Open Question 3 (parent review placement): a DEDICATED approval queue surface (`BehaviorApprovalQueue.tsx`) mirroring the shop purchase-approval mental model — the UI-SPEC's "approval queue" copy contract leans this way over folding into DailyModal. Approval flips status only (service-role, requireParent); crediting happens on the next award POST (plan 06's summation). Parent-direct tag application in DailyModal is immediate and ungated (D-12 last bullet).

Purpose: The propose→approve→credit boundary, guarded so a child cannot self-approve.
Output: Server actions + queue component + DailyModal behavior section + approval integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.9-rules-presets/05.9-PATTERNS.md
@.planning/phases/05.9-rules-presets/05.9-UI-SPEC.md
@.planning/phases/05.9-rules-presets/05.9-05-SUMMARY.md
@.planning/phases/05.9-rules-presets/05.9-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: app/parent/behavior/actions.ts — approveBehaviorMark / rejectBehaviorMark</name>
  <files>app/parent/behavior/actions.ts</files>
  <read_first>
    - app/parent/shop/actions.ts (full — the exact analog: `'use server'`, requireParent(), createAdminClient(), fetch-by-id scoped to member.familyId, status==='pending' guard, status transition, best-effort insertAuditEvent + notifyChild)
    - lib/supabase/admin.ts (requireParent, createAdminClient, Membership, AuthError)
    - .planning/phases/05.9-rules-presets/05.9-PATTERNS.md §"app/parent/behavior/actions.ts" (the deliberate divergence: approval moves NO coins — only flips status; do NOT port adjustCoins/applyWalletDelta)
  </read_first>
  <action>
    Create `'use server'` file. `approveBehaviorMark(markId: string): Promise<BehaviorMark>` — `const member = await requireParent()` (child role throws AuthError 403 — a child CANNOT self-approve, threat T-059-01), `createAdminClient()`, fetch the mark by id scoped to `.eq('family_id', member.familyId)`, throw if not found, throw if `status !== 'pending'`, then UPDATE `status='approved'`, `decided_by = member.userId`, `decided_at = now()`. Do NOT move coins here — deliberately divergent from shop's adjustCoins; the next /api/wallet/award POST credits the mark via plan 06's approved-sum (idempotent per mark id). Best-effort side effects (try/catch, fire-and-forget): `void insertAuditEvent({...action_type appropriate...})` and `notifyChild(...)` announcing the approval.
    `rejectBehaviorMark(markId: string, note?: string): Promise<BehaviorMark>` — same guard/fetch/pending-check, UPDATE `status='rejected'`, decided_by/decided_at. No coins to refund (nothing was credited while pending) — simpler than rejectPurchaseAction. Best-effort notify child.
    Never trust a client-supplied approver/status field — derive everything from requireParent() + the DB row.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "requireParent" app/parent/behavior/actions.ts && grep -q "'approved'" app/parent/behavior/actions.ts && ! grep -q "adjustCoins\|applyWalletDelta\|wallet_apply" app/parent/behavior/actions.ts</automated>
  </verify>
  <done>approve/reject are parent-guarded, service-role, status-only transitions with a pending precondition and best-effort notify/audit; no coin mutation inside; tsc clean.</done>
  <acceptance_criteria>
    - Both actions call requireParent() (child role → AuthError 403)
    - Fetch is scoped to member.familyId and asserts status==='pending' before transition
    - No adjustCoins/applyWalletDelta/wallet_apply anywhere (crediting is the award route's job)
    - decided_by/decided_at set from server-derived values, never client body
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: BehaviorApprovalQueue.tsx (dedicated queue) + DailyModal behavior section</name>
  <files>components/parent-center/screens/BehaviorApprovalQueue.tsx, components/DailyModal.tsx</files>
  <read_first>
    - app/kid/(app)/shop/page.tsx (the pending-status list mental model + StatusChip tone="warning" pattern the UI-SPEC references at line 215)
    - components/DailyModal.tsx (behavior state ~line 296 setGoodBehavior; gradeCoinsFor ~line 672; day-block behavior switch ~line 1157)
    - .planning/phases/05.9-rules-presets/05.9-UI-SPEC.md §"Copywriting Contract" (approval queue empty state + "Approve tag"/"Reject tag" + Btn variants; ink theme) and §"Color" (warning=pending only)
    - .planning/phases/05.9-rules-presets/05.9-05-SUMMARY.md (behavior.repo exports: getBehaviorMarks; and behavior.types)
  </read_first>
  <action>
    BehaviorApprovalQueue.tsx (NEW, ink theme, `'use client'`): list PENDING child-proposed marks across the family (fetch pending marks + join tag name/icon/price; resolve child name). One row per mark showing child name, tag icon+name, and the tag's coin value via `<Amount money={true} theme="ink" size="md"/>`, plus **"Approve tag"** (`Btn variant="success"`) and **"Reject tag"** (`Btn variant="danger"`) calling approveBehaviorMark/rejectBehaviorMark, then refresh the list. Empty state per UI-SPEC copy ("No tags waiting for review"). Row padding 8px 16px. Reject is a single tap (no confirm dialog, mirrors shop reject). All strings via useT().
    DailyModal.tsx behavior section: replace the single `good_behavior` boolean intent UI with a behavior-tags view for that child+date. Load the day's behavior_marks (all statuses, for parent visibility) via behavior.repo getBehaviorMarks. Render: pending child-proposed marks show a StatusChip tone="warning" ("Waiting for parent") — but the primary review action lives in the dedicated queue (link/scroll or inline approve is acceptable, but the queue is canonical). A parent applying a tag DIRECTLY here creates an `approved` mark immediately (ungated per D-12) — this write must go through the service-role path: call a small server action (add `applyTagDirect(childId, date, tagId)` to app/parent/behavior/actions.ts if a direct parent apply is offered here, requireParent + insert status='approved') OR reuse approveBehaviorMark after a pending insert; prefer a dedicated `applyTagDirect` action so parent-direct application never routes through the client 'pending' RLS path. Keep gradeCoinsFor updated to the grade_coin_map lookup (mirror the award route; grade is now string) so the modal's coin preview does not diverge.
    Note: if you add applyTagDirect, update Task 1's file (same file) — keep it in app/parent/behavior/actions.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "Approve tag\|approveBehaviorMark\|behaviorApproval" components/parent-center/screens/BehaviorApprovalQueue.tsx && grep -q "grade_coin_map\|gradeCoinsFor" components/DailyModal.tsx</automated>
  </verify>
  <done>A dedicated ink-theme approval queue lists pending marks with Approve/Reject actions; DailyModal shows behavior marks (pending=warning chip) and supports immediate parent-direct tag apply via a service-role action; gradeCoinsFor uses the map lookup; tsc clean.</done>
  <acceptance_criteria>
    - Queue renders one row per pending mark with child name, tag, Amount-rendered value, Approve/Reject buttons
    - Reject has no separate confirm dialog (single tap, mirrors shop)
    - Parent-direct tag apply in DailyModal writes via a requireParent server action (never a client 'approved' insert)
    - DailyModal gradeCoinsFor no longer uses a numeric 1-5 switch (uses grade_coin_map)
    - Pending status uses StatusChip tone="warning" (no invented pending color)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: tests/integration/behavior-approval.test.ts — propose→approve→credit boundary</name>
  <files>tests/integration/behavior-approval.test.ts</files>
  <read_first>
    - tests/integration/purchase.test.ts (the approve/reject-then-recheck-balance template; mocks requireParent/requireFamilyMember)
    - tests/integration/family-fixture.ts (fixture setup/teardown)
    - app/parent/behavior/actions.ts (the actions under test)
    - .planning/phases/05.9-rules-presets/05.9-VALIDATION.md §SC3c
  </read_first>
  <action>
    Model on purchase.test.ts. Cases: (1) a child-proposed mark is created status='pending' and a subsequent /api/wallet/award POST credits 0 for it; (2) approveBehaviorMark flips it to 'approved' and moves NO coins itself (balance unchanged immediately after approve); (3) the NEXT award POST credits the tag price exactly once; re-POST is idempotent (no double-credit); (4) a child-role membership calling approveBehaviorMark throws (self-approval refused — mock a child membership, expect AuthError/403); (5) rejectBehaviorMark flips to 'rejected' and the mark never credits. Mock requireParent/requireFamilyMember per the purchase.test.ts partial-vi.mock pattern; let createAdminClient run for real against the live DB.
  </action>
  <verify>
    <automated>npm run test:integration -- tests/integration/behavior-approval.test.ts</automated>
  </verify>
  <done>All five approval-boundary cases pass against the live DB, including the child-self-approval refusal.</done>
  <acceptance_criteria>
    - `npm run test:integration -- tests/integration/behavior-approval.test.ts` passes
    - A child-role approve attempt is asserted to throw (T-059-01 covered)
    - Approve moves no coins; credit occurs only on the subsequent award POST; re-POST idempotent
    - Rejected marks never credit
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → approveBehaviorMark/rejectBehaviorMark | untrusted caller; must be a parent of the mark's family |
| parent-direct tag apply → behavior_marks(status='approved') | must go through service-role (RLS forbids a client 'approved' insert) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-059-01 | Elevation of Privilege | behavior-mark approval | mitigate | requireParent() in both actions — a child role throws AuthError 403; approver/decided_by derived server-side, never from the client body. Test case 4 asserts refusal. |
| T-059-03 | Tampering | parent-direct 'approved' insert | mitigate | applyTagDirect runs service-role (requireParent) so it legitimately bypasses the client 'pending'-only RLS WITH CHECK; the client path can never produce an 'approved' mark. |
| T-059-13 | Repudiation | approve/reject not audited | accept | Best-effort insertAuditEvent (fire-and-forget, non-blocking) mirrors shop-approval; audit failure never blocks the parent action. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm run test:integration -- tests/integration/behavior-approval.test.ts` green.
</verification>

<success_criteria>
The propose→approve→credit flow is complete and guarded: children cannot self-approve, approval only flips status, credit is idempotent on the next award, and a dedicated parent queue drives the interaction.
</success_criteria>

<output>
Create `.planning/phases/05.9-rules-presets/05.9-07-SUMMARY.md` when done. Note the BehaviorApprovalQueue export + whether applyTagDirect was added, for plan 09's mount.
</output>
