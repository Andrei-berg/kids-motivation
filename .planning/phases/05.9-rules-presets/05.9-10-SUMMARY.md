---
phase: 05.9-rules-presets
plan: 10
subsystem: ui
tags: [onboarding, presets, rls, nextjs]

requires:
  - phase: 05.9-rules-presets (plan 02)
    provides: lib/presets.ts (getPresetValues, PresetId)
  - phase: 05.9-rules-presets (plan 04)
    provides: PATCH /api/wallet/settings server-side write path
provides:
  - "Onboarding Step 3 3-card preset picker (Classic / No Penalties / Bonuses Only), no manual number fields"
  - "completeOnboarding writes wallet_settings server-side via PATCH /api/wallet/settings — fixes a live production defect (RLS-denied client upsert since migration 04.4-03)"
affects: [wave-6]

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - "lib/onboarding-api.ts"
    - "app/onboarding/v2/page.tsx"

key-decisions:
  - "Task 3 (human-verify checkpoint) deferred by operator: verification will happen manually against the production deployment after this phase's work is pushed and deployed, rather than locally pre-merge. Code-side automated checks (tsc, lint, build) all passed before this decision."

patterns-established: []

requirements-completed: ["05.9-SC1"]

duration: 45min
completed: 2026-07-22
---

# Phase 05.9 Plan 10: Onboarding Preset Picker + RLS-Write Fix Summary

**Replaced onboarding's manual coin-rule entry with a 3-card preset picker and fixed a live production defect: `completeOnboarding` was writing `wallet_settings` from the browser client, RLS-denied since migration 04.4-03, silently blocking new-family onboarding completion.**

## Performance

- **Duration:** ~45 min (Tasks 1-2); Task 3 deferred
- **Tasks:** 2/3 automated tasks complete; Task 3 (human-verify) deferred to post-deploy by operator
- **Files modified:** 2

## Accomplishments
- `completeOnboarding` now takes a `PresetId`, resolves values via `getPresetValues(presetId)`, and writes them server-side through the existing `PATCH /api/wallet/settings` route (`requireParent` + service-role) — the RLS-denied browser-client `wallet_settings` upsert is removed entirely
- Onboarding Step 3 (`StepCoinRules`) replaced with 3 preset cards (Classic / No Penalties / Bonuses Only); no manual number fields; "Continue with this preset" enables on first tap
- `CoinRulesState` removed; `selectedPresetId: PresetId | null` drives the step

## Task Commits

1. **Task 1: completeOnboarding — server-side preset write** - `29dcb46` (feat)
2. **Task 2: onboarding/v2 StepCoinRules -> 3-card preset picker** - `8cb1a9e` (feat)

Task 3 (checkpoint:human-verify) — see Deviations below.

## Files Created/Modified
- `lib/onboarding-api.ts` — `completeOnboarding(familyId, presetId)`; removed the browser-client `wallet_settings` upsert; writes via server PATCH
- `app/onboarding/v2/page.tsx` — 3-card preset picker replaces `StepCoinRules`' manual number fields

## Decisions Made
- Task 3's `checkpoint:human-verify` was reached with both automated tasks complete and all automated verification green (`npx tsc --noEmit`, exact grep checks from PLAN.md, plus `npm run build` and `npm run lint` as broader confirmation).
- **Operator decision:** rather than walking through `/onboarding/v2` on a local dev server before merge, the operator chose to defer the manual walkthrough until after this phase's work is pushed and deployed to production (https://kids-motivation.vercel.app), and will verify there directly.

## Deviations from Plan

### Checkpoint Resolution (not an auto-fix — a scope/timing decision by the operator)

**1. Task 3 (checkpoint:human-verify) deferred to post-deploy verification**
- **Found during:** Task 3, the plan's final `checkpoint:human-verify` gate
- **What the plan specified:** Operator walks through `/onboarding/v2` as a brand-new family on a local dev server, confirms the preset picker and successful onboarding completion, before the plan is marked complete.
- **What happened instead:** The operator instructed that this phase's work be committed, merged, and pushed to `main` (triggering the Vercel production deploy), and that the manual onboarding walkthrough happens against the live deployment afterward rather than blocking the merge locally.
- **Why this is acceptable:** All automatable verification (type-check, lint, production build including `/onboarding/v2`) passed before this decision. The remaining verification is inherently a human-judgment step (visually confirming the picker and the RLS-fix in a real browser flow) — the plan's own `checkpoint:human-verify` contract permits the operator to choose where/when that human check happens, not just whether it happens. This is not a bug fix or auto-correction; it is the operator exercising their prerogative over their own verification gate.
- **Follow-up required:** The operator MUST still walk through `/onboarding/v2` as a brand-new family post-deploy and confirm: (a) the 3 preset cards render with no number fields, (b) tapping one enables "Continue with this preset", (c) onboarding completes successfully through to the invite-code screen (the actual RLS-defect fix under test), and (d) Parent Center → Settings → Coin Rules shows the chosen preset's values. If this surfaces an issue, treat it as a regression against this SUMMARY and open a gap-closure plan.

---

**Total deviations:** 0 auto-fixed; 1 operator-directed checkpoint-resolution decision (deferred verification timing, not scope).
**Impact on plan:** No change to what was built or how it was verified programmatically. Only the *timing and environment* of the final human-judgment check moved from pre-merge/local to post-deploy/production, at the operator's explicit direction.

## Issues Encountered
None beyond the checkpoint timing decision above.

## User Setup Required
None — no external service configuration required. (Deployment itself is the orchestrator's next action, not a manual step for the operator.)

## Next Phase Readiness
Wave 4 code is complete and ready to merge. Wave 6 (`05.9-09`, the parent-facing rules UI assembly) is unblocked once this wave merges. **Outstanding:** the operator's post-deploy manual walkthrough of `/onboarding/v2` described above is still owed — track it until confirmed.

---
*Phase: 05.9-rules-presets*
*Completed: 2026-07-22*
