---
phase: 03.3-photos
plan: 02
subsystem: ui
tags: [chat, photos, supabase-storage, lightbox, react, typescript]

# Dependency graph
requires:
  - phase: 03.3-photos
    provides: compressImage, uploadPhoto, getSignedPhotoUrl in lib/photo-upload.ts
  - phase: 03.2-family-chat
    provides: ChatThread, sendMessage, chat_messages table, ReactionPickerBar
provides:
  - Photo attach (camera + gallery) buttons in ChatThread send area
  - Pre-send photo preview with optional caption and send/cancel
  - Compressed client-side upload to Supabase Storage family-photos bucket
  - Inline photo thumbnail rendering (w-36) in chat thread
  - PhotoLightbox component for full-screen photo viewing
  - photo_url column in chat_messages (via SQL migration)
  - PHOTO-01 requirement fulfilled
affects: [03.3-photos/3.3-03, future-photo-features]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Optimistic photo message with local blob URL replaced by signed URL after upload
    - Hidden file inputs triggered via ref.current.click() for camera vs gallery
    - URL.revokeObjectURL called after successful upload or on error

key-files:
  created:
    - components/chat/PhotoLightbox.tsx
    - supabase-chat-photo-column.sql
  modified:
    - lib/models/chat.types.ts
    - lib/repositories/chat.repo.ts
    - components/chat/ChatThread.tsx

key-decisions:
  - "Optimistic UI shows local blob URL immediately; replaced with signed URL after upload completes"
  - "photo_url persisted as signed URL (1h expiry) in DB — acceptable for MVP; long-lived URLs are a future concern"
  - "Camera and gallery are separate hidden inputs, both triggering the same handlePhotoSelect handler"

patterns-established:
  - "Photo preview: local blob URL in state, revoked after upload or on error"
  - "Lightbox: fixed inset-0 z-50 overlay, click backdrop or X button to close"

requirements-completed:
  - PHOTO-01

# Metrics
duration: 5min
completed: 2026-04-14
---

# Phase 3.3 Plan 02: Photo Messages Summary

**Photo messages in family chat: camera/gallery attach, client-side compression, Supabase Storage upload, inline thumbnail with PhotoLightbox full-screen viewer**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-14T14:30:00Z
- **Completed:** 2026-04-14T14:32:08Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Extended ChatMessage type with photo_url field and MessageType with 'photo'
- Added sendMessage photoUrl param with DB insert support
- Created PhotoLightbox component for full-screen photo viewing (click backdrop or X to close)
- Updated ChatThread with camera (📷) and gallery (🖼️) buttons, photo preview overlay with caption, optimistic send flow, and photo message rendering

## Task Commits

1. **Task 1: Extend chat types, repo, and add photo_url DB column migration** - `e0b132e` (feat)
2. **Task 2: PhotoLightbox component + photo message UI in ChatThread** - `7ae9bdf` (feat)

## Files Created/Modified

- `lib/models/chat.types.ts` - Added 'photo' to MessageType, photo_url to ChatMessage
- `lib/repositories/chat.repo.ts` - Added photoUrl param to sendMessage
- `supabase-chat-photo-column.sql` - ALTER TABLE migration to add photo_url column
- `components/chat/PhotoLightbox.tsx` - Full-screen lightbox overlay component
- `components/chat/ChatThread.tsx` - Camera/gallery buttons, photo preview UI, photo message rendering, lightbox integration

## Decisions Made

- Optimistic UI shows local blob URL immediately; replaced with signed URL after upload completes — avoids any visible delay for sender
- photo_url stored as signed URL (1h expiry) in DB — adequate for MVP; long-lived URL management deferred
- Camera and gallery share one `handlePhotoSelect` handler via two separate hidden file inputs

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

Run `supabase-chat-photo-column.sql` in Supabase SQL Editor to add `photo_url TEXT NULL` column to `chat_messages` table.

## Next Phase Readiness

- PHOTO-01 fulfilled — any family member can attach photos in chat
- Plan 3.3-03 (task photo proof) can proceed — photo infrastructure fully in place
- ReactionPickerBar renders under photo messages automatically (no changes needed)

---
*Phase: 03.3-photos*
*Completed: 2026-04-14*
