---
phase: 03.3-photos
plan: "03"
subsystem: photo-proof
tags: [photos, room-cleaning, proof, camera, lightbox, supabase-storage]
dependency_graph:
  requires: [3.3-01, 3.3-02]
  provides: [PHOTO-02]
  affects: [KidDayFillForm, DailyModal, days-table]
tech_stack:
  added: []
  patterns: [fire-and-try-upload, local-blob-preview, signed-url-storage]
key_files:
  created:
    - supabase-room-proof-column.sql
  modified:
    - lib/models/child.types.ts
    - lib/repositories/children.repo.ts
    - components/kid/KidDayFillForm.tsx
    - components/DailyModal.tsx
decisions:
  - roomProofUrl upload is fire-and-try — upload failure is logged but never blocks form submission
  - DailyModal loads room_proof_url into dedicated roomProofUrl state (not a dayData object) to match existing component pattern
  - PhotoLightbox reused from plan 3.3-02 — no new component needed
  - capture="environment" used on file input for camera-only, no gallery access
metrics:
  duration: "~3 minutes"
  completed_date: "2026-04-14"
  tasks_completed: 3
  files_modified: 4
---

# Phase 3.3 Plan 03: Task Photo Proof Summary

**One-liner:** Optional room-cleaning photo proof via camera capture in KidDayFillForm, with parent thumbnail + PhotoLightbox in DailyModal.

---

## Tasks Completed

| # | Task | Commit | Key Files |
|---|------|--------|-----------|
| 1 | DB migration + type + repo extension for room_proof_url | f8cfdc5 | supabase-room-proof-column.sql, child.types.ts, children.repo.ts |
| 2 | Camera capture UI in KidDayFillForm + proof upload on submit | 4c2fb32 | KidDayFillForm.tsx |
| 3 | Proof thumbnail + lightbox in DailyModal parent view | f5e8f0d | DailyModal.tsx |

---

## What Was Built

**PHOTO-02 fully implemented:**

1. **DB migration** (`supabase-room-proof-column.sql`): `ALTER TABLE days ADD COLUMN IF NOT EXISTS room_proof_url TEXT NULL`

2. **Type extension** (`DayData` in `child.types.ts`): added `room_proof_url: string | null`

3. **Repo extension** (`children.repo.ts saveDay`): added `roomProofUrl?: string` param, persisted as `room_proof_url` in the upsert

4. **Kid camera UI** (`KidDayFillForm.tsx`):
   - Hidden `<input type="file" capture="environment">` for camera-only access
   - `proofFile` + `proofLocalUrl` state for preview
   - `handleProofCapture` / `handleProofRetake` handlers
   - 64px thumbnail + "Переснять" button shown after capture
   - On submit: fire-and-try upload via `compressImage` → `uploadPhoto` → `getSignedPhotoUrl`; failure is `console.warn`, never blocks form
   - `roomProofUrl` passed to `saveDay` when upload succeeds

5. **Parent view** (`DailyModal.tsx`):
   - `roomProofUrl` state loaded from `dayData.room_proof_url` on modal open
   - `lightboxProofUrl` state for lightbox toggle
   - 80px proof thumbnail in Комната section — only rendered when URL present
   - Tap opens `PhotoLightbox` (reused from 3.3-02); absent URL = zero UI change

---

## Deviations from Plan

None — plan executed exactly as written.

---

## Decisions Made

- **roomProofUrl upload is fire-and-try**: upload failure is logged but never blocks form submission — consistent with 3.3-02 pattern
- **DailyModal pattern**: loads proof URL into dedicated `roomProofUrl` state (not a `dayData` object) to match the component's existing individual-state pattern
- **PhotoLightbox reuse**: no new lightbox component — `PhotoLightbox` from plan 3.3-02 accepts any URL

---

## Self-Check: PASSED

All files exist. All 3 task commits verified: f8cfdc5, 4c2fb32, f5e8f0d.
