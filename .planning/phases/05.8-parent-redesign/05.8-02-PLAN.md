---
phase: 05.8-parent-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/settings/CalendarSettingsManager.tsx
  - components/settings/PeriodsManager.tsx
autonomous: true
requirements: [D-07, D-09]

must_haves:
  truths:
    - "CalendarSettingsManager renders with ink-theme T tokens, no hardcoded rgba/hex remnants (D-07, part of the D-09 sweep)"
    - "PeriodsManager renders with ink-theme T tokens; every amber Tailwind class is now indigo (never gold) (D-07)"
    - "No gold hex appears in either file"
  artifacts:
    - path: "components/settings/CalendarSettingsManager.tsx"
      provides: "ink-recolored year-settings form"
      contains: "from '@/components/parent-center/tokens'"
    - path: "components/settings/PeriodsManager.tsx"
      provides: "ink-recolored vacation period editor (amber to indigo)"
      contains: "from '@/components/parent-center/tokens'"
  key_links:
    - from: "components/settings/CalendarSettingsManager.tsx"
      to: "components/parent-center/tokens.ts"
      via: "import { T }"
      pattern: "@/components/parent-center/tokens"
---

<objective>
Recolor the two Year Calendar editors onto the ink theme (D-07 / part of the D-09 sweep). CalendarSettingsManager uses inline rgba/hex; PeriodsManager uses Tailwind stock + amber classes. Both convert to inline `T`-token styles, with amber becoming indigo, never gold.

Purpose: These two files are the recolor prerequisite the Year Calendar grid (Plan 05) sits on top of. PeriodsManager's amber-is-not-money trap is the single most likely near-miss in the whole sweep (RESEARCH flagged it explicitly), so it is isolated here with a hard rule.
Output: Two ink-themed editor components, structure and logic unchanged (D-05's own restructuring happens in Plan 05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05.8-parent-redesign/05.8-PATTERNS.md
@.planning/phases/05.8-parent-redesign/05.8-UI-SPEC.md

<interfaces>
components/parent-center/tokens.ts — export const T with keys: bg1, card, cardHi, cardBorder, cardBorderHi, text, textDim, muted, faint, indigo, indigoHi, indigoSoft, success, successSoft, warning, warningSoft, danger, dangerSoft, r, rM, rL. NO gold/goldText key exists on T.
Import path (files live in components/settings/, not parent-center/): import { T } from '@/components/parent-center/tokens'
Reference for a correctly-recolored screen: components/parent-center/screens/Analytics.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recolor CalendarSettingsManager.tsx (inline rgba/hex to T)</name>
  <files>components/settings/CalendarSettingsManager.tsx</files>
  <read_first>
    - components/settings/CalendarSettingsManager.tsx (current inline rgba/hex palette)
    - .planning/phases/05.8-parent-redesign/05.8-PATTERNS.md CalendarSettingsManager recolor substitution table (line-referenced old-to-new mapping)
    - components/parent-center/screens/Analytics.tsx (reference for token usage)
  </read_first>
  <action>
    Add `import { T } from '@/components/parent-center/tokens'`. Apply the PATTERNS.md CalendarSettingsManager substitution table verbatim: `#fff` title becomes `T.text`; `rgba(238,238,255,0.5)` subtitle/labels become `T.muted`; error banner `rgba(244,63,94,*)`/`#F43F5E` become `T.dangerSoft` bg and `T.danger` text; success banner `rgba(16,185,129,*)`/`#34d399` become `T.successSoft` bg and `T.success` text; loading `rgba(238,238,255,0.4)` becomes `T.muted`; form panel `rgba(99,102,241,0.06/0.2)` become `T.indigoSoft` bg and `T.cardBorderHi` border; term-mode toggle track `rgba(0,0,0,0.2)`/`rgba(255,255,255,0.08)` become `T.bg1`/`T.cardBorder`; active term-mode / weekend-day / save button `rgba(99,102,241,0.8)` becomes `T.indigo`; weekend-day inactive `rgba(255,255,255,0.08/0.03)` become `T.cardBorder`/`T.card`. Do NOT introduce any amber or gold. Structure, form fields, and save flow stay exactly as-is (recolor only). The "Save calendar settings" string value was already updated in Plan 01's i18n task; leave the key reference as-is.
  </action>
  <verify>
    <automated>grep -vE '^\s*//' components/settings/CalendarSettingsManager.tsx | grep -qiE 'rgba\(255,255,255|rgba\(238,238,255|#fff|#F43F5E|#34d399|rgba\(99,102,241|amber|#F0BE6A' && echo FORBIDDEN_REMNANTS || (grep -q "@/components/parent-center/tokens" components/settings/CalendarSettingsManager.tsx && echo OK); npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `OK` (not `FORBIDDEN_REMNANTS`) and `npx tsc --noEmit` exits 0
    - `grep -ci "amber\|#F0BE6A\|goldText" components/settings/CalendarSettingsManager.tsx` returns 0
    - Form fields (year start/end, term mode, weekend-day picker, region preset) and the save handler are structurally unchanged (git diff shows style-only edits)
  </acceptance_criteria>
  <done>CalendarSettingsManager imports T and renders entirely on ink tokens; no rgba/hex/amber/gold remnants; behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Recolor PeriodsManager.tsx (Tailwind + amber to inline T, amber to indigo)</name>
  <files>components/settings/PeriodsManager.tsx</files>
  <read_first>
    - components/settings/PeriodsManager.tsx (uses Tailwind bg-gray-*/bg-amber-*/text-amber-* classes, unlike the other managers)
    - .planning/phases/05.8-parent-redesign/05.8-PATTERNS.md PeriodsManager recolor substitution table (Tailwind-to-inline-T, amber-is-not-money rule)
    - .planning/phases/05.8-parent-redesign/05.8-UI-SPEC.md D-07 (hard rule: every amber becomes T.indigo/T.indigoSoft, never gold)
  </read_first>
  <action>
    Add `import { T } from '@/components/parent-center/tokens'`. Replace Tailwind stock/amber classes with inline `T`-token styles per the PATTERNS.md table: `bg-gray-700/50`,`border-gray-600` become `background: T.card, border: 1px solid T.cardBorder`; `text-white` becomes `color: T.text`; `text-gray-400/500` become `color: T.muted`; preset badge `text-amber-400 bg-amber-500/10 border-amber-500/25` become `T.indigoHi` text / `T.indigoSoft` bg / `T.cardBorderHi` border; input `focus:border-amber-500` becomes `T.indigo`; emoji-picker active `border-amber-500 bg-amber-500/15` become `T.indigo` border / `T.indigoSoft` bg; type-filter active tab `bg-amber-500 text-black` become `T.indigo` bg / `#fff` fg; error banner `bg-red-500/15 border-red-500/30 text-red-400` become `T.dangerSoft` bg / `T.danger` text; Save button `bg-amber-500 hover:bg-amber-400 text-black` becomes `T.indigo` bg (primary-action convention); Cancel `bg-gray-700 text-gray-400` become `T.card` bg / `T.muted` text; apply-status `text-amber-400` becomes `T.indigoHi`; add-period dashed CTA amber hover becomes `T.indigo`. HARD RULE: not a single amber/gold value survives; amber here is a primary action / active state, so it becomes indigo. The child-id resolution query (lines ~71-83) and all data logic stay untouched. The "+ Add period" copy value was set in Plan 01's i18n task.
  </action>
  <verify>
    <automated>grep -vE '^\s*//' components/settings/PeriodsManager.tsx | grep -qiE 'amber|bg-gray-|text-gray-|border-gray-|text-white|#F0BE6A' && echo FORBIDDEN_REMNANTS || (grep -q "@/components/parent-center/tokens" components/settings/PeriodsManager.tsx && echo OK); npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `OK` (not `FORBIDDEN_REMNANTS`) and `npx tsc --noEmit` exits 0
    - `grep -ci "amber" components/settings/PeriodsManager.tsx` returns 0 (no amber survives anywhere)
    - `grep -ci "#F0BE6A\|goldText" components/settings/PeriodsManager.tsx` returns 0 (no gold introduced)
    - The `family_members` child-id resolution query is byte-identical to before (git diff shows only style edits around it)
  </acceptance_criteria>
  <done>PeriodsManager imports T, all Tailwind stock + amber classes replaced by inline ink tokens with amber recolored to indigo; data logic untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (browser client, RLS) | Both editors write family config (family_calendar / vacation_periods) through existing family-isolation RLS; no endpoint or write-path change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-058-03 | Tampering | CalendarSettingsManager / PeriodsManager | accept | Style-only change; no data-layer, no write-path, no validation logic touched. Existing RLS remains the boundary |
| T-058-04 | Elevation of Privilege | config writes from these editors | mitigate | Unchanged: writes go through existing browser client under family RLS scoped to parent/extended roles; no admin client introduced |

Pure recolor plan: no new fields, no new writes, no new auth/session/money surface. XSS/injection surface unchanged (no new free-text inputs).
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- Both verify commands print `OK`
- `npm test` full suite still green (no logic changed)
</verification>

<success_criteria>
- Both editors import T and render entirely on the ink theme
- Zero rgba/hex/Tailwind-stock/amber/gold remnants outside comments
- No structural or data-logic changes (recolor only)
</success_criteria>

<output>
Create `.planning/phases/05.8-parent-redesign/05.8-02-SUMMARY.md` when done
</output>
