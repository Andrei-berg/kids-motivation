---
status: partial
phase: 05.8-parent-redesign
source: [05.8-VERIFICATION.md]
started: 2026-07-21T08:33:07Z
updated: 2026-07-21T09:15:00Z
---

## Current Test

[awaiting human testing — operator's local browser was serving a stale cached bundle
(missing the Calendar/Blocks sub-tabs entirely) during the checkpoint session; a private
window failed to load due to auth/session cookies not being available there. Operator
asked to close the phase now and verify after the next deploy.]

## Tests

### 1. D-02 schedule-link picker (relocated by CR-01 fix)
expected: Open Parent Center → Settings → Schedule → Day Constructor (byChild layout,
default). Select a child's own tab (not "Family default"). On a built-in block row (e.g.
Room), tap the "🔗 Schedule link" trigger, pick a schedule item from the dropdown, and
confirm a green "Custom" tag + synced StatusChip appear on that child's row, and that the
family-default tab / other children are unaffected. Re-opening and picking "None" clears
it back to unlinked (indigo trigger).
result: [pending]

### 2. D-08 Weekly Summary "Tasks done" chip (CR-02 fix)
expected: Open Parent Center → Analytics. The "This week → Tasks done" StatusChip on the
Weekly Summary card renders success (green, ≥80%) or warning (yellow, 50-79%) for a
family with mostly-filled, mostly-good days this week — not permanently red/danger as it
was before the fix.
result: [pending]

## Summary

total: 2
passed: 0
issues: 0
pending: 2
skipped: 0
blocked: 0

## Gaps

None recorded — both items are re-confirmation requests after code-level fixes (CR-01,
CR-02), not known defects. Both were independently traced end-to-end in code and verified
passing by two separate automated passes (gsd-code-reviewer + gsd-verifier); only the
live-browser visual confirmation is outstanding.

## Notes for next attempt

- During the 05.8-09 checkpoint session, the operator's browser (localhost:3000) was
  serving a **stale cached bundle** — the Schedule sub-tab row showed only 5 of 7 tabs
  (Subjects/Sections/Vacations/Activities/Room), missing "Calendar" and "Blocks" entirely,
  even after a fresh `npm run dev` restart and full-page scroll-to-top. This is almost
  certainly a PWA service-worker cache (the app registers one; see `manifest.webmanifest`
  in CLAUDE.md) rather than a real UI/layout bug. A private/incognito window would bypass
  it but failed to load for the operator (no auth session there — expected, PIN/email
  login is required per-session).
- **Before re-testing:** hard-refresh (Cmd+Shift+R / disable cache in DevTools Network
  tab) or unregister the service worker (DevTools → Application → Service Workers →
  Unregister) before judging the UI, especially after a fresh deploy.
- Separately disclosed by the re-verification pass (non-blocking, not part of this UAT):
  `components/parent-center/ui.tsx`'s shared `Btn`/`Pill` `success`/`danger` variants still
  hardcode `rgba(0,230,118,*)`/`rgba(255,107,107,*)` (same decimal-RGB neon-remnant class
  WR-02 just fixed in `ChatPanel.tsx`), but in a file outside Phase 5.8's declared D-09
  sweep scope. Candidate for a future cleanup pass.
