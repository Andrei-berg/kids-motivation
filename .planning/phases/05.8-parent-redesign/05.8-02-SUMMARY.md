---
phase: 05.8-parent-redesign
plan: 02
subsystem: ui
tags: [react, inline-styles, design-tokens, tailwind-migration]

# Dependency graph
requires:
  - phase: 05.8-parent-redesign (Plan 01)
    provides: i18n key updates for calendar/period copy strings (leaves key refs untouched here)
provides:
  - CalendarSettingsManager.tsx fully recolored onto ink-theme T tokens (no inline rgba/hex remnants)
  - PeriodsManager.tsx migrated from Tailwind stock/amber classes to inline T-token styles, amber recolored to indigo throughout
affects: [05.8-parent-redesign Plan 05 (Year Calendar grid, builds D-05 accordion on top of these two editors)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Ink-theme recolor: replace inline rgba()/hex literals or Tailwind stock/amber classes with T-token inline styles imported from '@/components/parent-center/tokens'"
    - "amber-is-not-money: any amber/gold-looking primary-action or active-state color that isn't a currency figure becomes T.indigo/T.indigoSoft/T.indigoHi, never gold"

key-files:
  created: []
  modified:
    - components/settings/CalendarSettingsManager.tsx
    - components/settings/PeriodsManager.tsx

key-decisions:
  - "CalendarSettingsManager: collapsed all indigo alpha variants (0.8/0.5/0.25 for active term-mode, weekend-day border/bg, and save button) onto a single T.indigo per the plan's substitution table, rather than preserving per-alpha shades — matches the locked recolor table's grouping of 'active term-mode / weekend-day / save button' under one T.indigo mapping."
  - "PeriodsManager: full-file rewrite (Write, not incremental Edit) given the volume of Tailwind className-to-inline-style conversions across ~15 elements; child-id resolution query verified byte-identical via git diff (no change in that block)."

patterns-established:
  - "Hover-state color transitions previously done via Tailwind hover: classes are now handled with onMouseEnter/onMouseLeave inline handlers when no CSS class layer is available (edit/delete icon buttons, add-period dashed CTA in PeriodsManager)."

requirements-completed: [D-07, D-09]

# Metrics
duration: 12min
completed: 2026-07-21
---

# Phase 05.8 Plan 02: Year Calendar Editors Recolor Summary

**CalendarSettingsManager and PeriodsManager fully recolored onto ink-theme T tokens; PeriodsManager's amber Tailwind classes (badges, focus rings, active tabs, Save/Apply buttons) converted to indigo per the D-07 amber-is-not-money rule, with zero gold introduced.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-21T05:35:00Z
- **Completed:** 2026-07-21T05:47:00Z
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments
- CalendarSettingsManager.tsx: all inline `rgba(...)`/hex literals (`#fff`, `#F43F5E`, `#34d399`, `rgba(99,102,241,*)`, `rgba(238,238,255,*)`, `rgba(255,255,255,*)`, `rgba(0,0,0,*)`) replaced with `T.text`/`T.muted`/`T.dangerSoft`/`T.danger`/`T.successSoft`/`T.success`/`T.indigoSoft`/`T.cardBorderHi`/`T.bg1`/`T.cardBorder`/`T.indigo`
- PeriodsManager.tsx: every Tailwind stock class (`bg-gray-*`, `text-gray-*`, `border-gray-*`, `text-white`, `rounded-*`, spacing utilities) and every `amber` class replaced with inline `T`-token styles; amber recolored to indigo (preset badge, focus rings, emoji-picker active state, type-filter active tab, Save/Apply buttons, apply-status text, add-period dashed CTA hover)
- Both files verified: `npx tsc --noEmit` exits 0, zero forbidden-remnant grep hits, full `npm test` suite green (71 passed / 26 skipped, no regressions), `npm run lint` shows only pre-existing unrelated warnings

## Task Commits

Each task was committed atomically:

1. **Task 1: Recolor CalendarSettingsManager.tsx (inline rgba/hex to T)** - `75de4c3` (feat)
2. **Task 2: Recolor PeriodsManager.tsx (Tailwind + amber to inline T, amber to indigo)** - `5684bba` (feat)

**Plan metadata:** committed alongside this summary (worktree mode — orchestrator finalizes shared-file commit)

## Files Created/Modified
- `components/settings/CalendarSettingsManager.tsx` - Ink-recolored year-settings form (school year dates, term mode toggle, weekend-day picker, region preset select, save flow) — structure and logic unchanged
- `components/settings/PeriodsManager.tsx` - Ink-recolored vacation period editor (list, add/edit form, emoji picker, child-filter tabs, region-preset apply picker) — Tailwind stock classes and amber accents converted to inline T-token styles; data logic and child-id resolution query untouched

## Decisions Made
- Collapsed CalendarSettingsManager's three distinct indigo alpha values (0.8/0.5/0.25) used for active term-mode, weekend-day active border/bg, and the save button into a single flat `T.indigo`, matching the plan's explicit grouping of those three UI states under one substitution entry.
- Used a full-file `Write` rewrite for PeriodsManager.tsx rather than incremental `Edit` calls, given nearly every line touched a `className` — this kept the substitution table's mappings applied consistently in one pass. Verified via `git diff` that the child-id resolution block (`family_members` query) produced zero diff, confirming the data-logic-untouched constraint held.
- Preserved Tailwind's original pixel-equivalent spacing (e.g. `mb-4`→`marginBottom: 16`, `p-4`→`padding: 16`, `text-sm`→`fontSize: 13`) so the recolor is purely visual token substitution with no layout drift.

## Deviations from Plan

None - plan executed exactly as written. Both tasks matched the PATTERNS.md substitution tables verbatim; no bugs, missing functionality, or blocking issues encountered.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Both Year Calendar editors are now fully on the ink theme with zero amber/gold remnants, satisfying the recolor prerequisite Plan 05's `CalendarGrid` sits on top of.
- `PeriodsManager.tsx:63-83`'s child-id resolution block remains the canonical reuse source for `DayBlocksManager.tsx` (D-02/D-03) and `CalendarGrid` (D-06), unchanged and ready to be copied as documented in `05.8-PATTERNS.md`.
- No blockers for downstream Plan 05 (D-05 restructuring can build on this recolored base).

---
*Phase: 05.8-parent-redesign*
*Completed: 2026-07-21*

## Self-Check: PASSED

- FOUND: components/settings/CalendarSettingsManager.tsx
- FOUND: components/settings/PeriodsManager.tsx
- FOUND: .planning/phases/05.8-parent-redesign/05.8-02-SUMMARY.md
- FOUND commit: 75de4c3
- FOUND commit: 5684bba
- FOUND commit: b2276d8
