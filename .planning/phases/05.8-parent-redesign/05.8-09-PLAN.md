---
phase: 05.8-parent-redesign
plan: 09
type: execute
wave: 4
depends_on: [05.8-02, 05.8-03, 05.8-04, 05.8-05, 05.8-06, 05.8-07, 05.8-08]
files_modified: []
autonomous: false
requirements: [D-03, D-05, D-06, D-08, D-09]

must_haves:
  truths:
    - "A human has visually confirmed the ink-theme recolor sweep (D-09) has no cyan/neon-green/hardcoded-hex remnants and gold appears only on money"
    - "The operator has picked the winning D-03 override layout (a, b, or keep both behind the toggle)"
    - "The Year Calendar grid + sick overlay and the Weekly Summary card are confirmed correct in the browser"
  artifacts: []
  key_links: []
---

<objective>
Final consolidated human-verify checkpoint for Phase 5.8 (matching the 05.3/05.7 precedent). Everything automatable was gated per-task; this checkpoint covers the four manual-only verifications from VALIDATION.md: the D-09 recolor bar (visual/aesthetic), the D-03 candidate decision (user-facing design choice), the D-05/D-06 grid + sick overlay legibility, and the D-08 Weekly Summary card copy/layout/values.

Purpose: The recolor bar and the "build both, decide at verification" override layout are explicitly not automatable — a human must eyeball the running app and make the D-03 call.
Output: Operator sign-off (or a punch-list of fixes) recorded in the summary; the D-03 decision recorded for a possible follow-up simplification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.8-parent-redesign/05.8-VALIDATION.md
@.planning/phases/05.8-parent-redesign/05.8-UI-SPEC.md
@.planning/phases/05.8-parent-redesign/05.8-05-SUMMARY.md
@.planning/phases/05.8-parent-redesign/05.8-06-SUMMARY.md
@.planning/phases/05.8-parent-redesign/05.8-07-SUMMARY.md
@.planning/phases/05.8-parent-redesign/05.8-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint automated gate (fast suite + remnant sweep, then a one-time build gate)</name>
  <files></files>
  <read_first>
    - .planning/phases/05.8-parent-redesign/05.8-UI-SPEC.md D-09 Recolor Scope table (the exact in-scope files)
  </read_first>
  <action>
    Run the automated gate before pausing for the human, in two clearly-separated stages so the fast feedback loop is not conflated with the slow production build:
    1. FAST GATE (primary automated verify, seconds): `npm test` (full vitest suite), `npx tsc --noEmit`, and a remnant sweep grep across the in-scope files for forbidden literals (cyan neon `#00E676`/`#4DDCFF`, amber `#F59E0B`/`bg-amber-`/`text-amber-`, raw `rgba(255,255,255`, hardcoded gold `#F0BE6A` outside the Amount atom).
    2. BUILD GATE (one-time end-of-phase, ~minutes — run ONCE here, not per task): `npm run build` (55/55 pages green expected). This is intentionally isolated from the fast gate because a full production build exceeds the fast-feedback budget; it is the single consolidated build verification for the whole phase.
    Compile any failures from either stage into a punch-list for the operator. Do NOT edit code here — this task only produces the evidence the human reviews.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && grep -rvE '^\s*//' components/settings/DayBlocksManager.tsx components/settings/CalendarSettingsManager.tsx components/settings/PeriodsManager.tsx components/settings/ActivitiesManager.tsx components/settings/SectionsManager.tsx components/settings/SubjectsManager.tsx components/settings/RoomTasksManager.tsx components/settings/CalendarGrid.tsx components/parent-center/screens/ChatPanel.tsx components/parent-center/ui.tsx 2>/dev/null | grep -iE '#00E676|#4DDCFF|#F59E0B|bg-amber-|text-amber-|#F0BE6A' ; echo "remnant-exit:$?"</automated>
    <build-gate>npm run build   # one-time consolidated end-of-phase production build (~minutes, exceeds the 30s fast-feedback budget — run once, expect 55/55 pages green)</build-gate>
  </verify>
  <acceptance_criteria>
    - FAST GATE: `npm test` passes, `npx tsc --noEmit` exits 0, and the remnant sweep prints `remnant-exit:1` (grep found none of the forbidden literals across all in-scope files)
    - BUILD GATE: `npm run build` completes (55/55 pages) — run once as the isolated end-of-phase build check
    - Any failure from either stage is captured as a punch-list line for the operator (not silently ignored)
  </acceptance_criteria>
  <done>Fast gate (tests + tsc + remnant sweep) is clean, the one-time build gate completes, or a punch-list of exact failures is prepared for the human checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Parent Center ink-theme recolor sweep (7 settings managers + ChatPanel activity colors + ui.tsx cyan Btn), the new Day Constructor capabilities (D-02 schedule-link picker + D-03 both per-child override layouts behind a toggle), the Year Calendar visual month grid with per-child sick-day overlay (D-05/D-06), and the Analytics "This week" summary card (D-08).
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open Parent Center (http://localhost:3000/parent-center).
    1. RECOLOR BAR (D-09): open Settings → Schedule and visit each sub-tab (Calendar, Vacations, Room, Blocks, Activities) plus the parent Chat screen. Confirm NO cyan, neon-green, or amber remnants; palette matches the ink theme; gold appears ONLY on money amounts. Confirm the ChildProfile Freeze button hover is indigo (not the old cyan).
    2. DAY CONSTRUCTOR (D-02): in Blocks, on a per-child block, open the "Schedule link" picker, link it to a schedule item, confirm the synced chip shows the item title and the block's days_of_week update; test Re-sync and "Not linked".
    3. D-03 DECISION: toggle "By child" vs "By block". Exercise both against real data — create a per-child price override on a block that is schedule-linked or has a vacation multiplier (confirm the override KEEPS the linked days / vacation pricing and does NOT change other children), then remove it (confirm it reverts to the family default). DECIDE: keep candidate (a), candidate (b), or keep both behind the toggle. State your choice.
    4. YEAR CALENDAR (D-05/D-06): open Calendar. Confirm the month grid color-codes school/weekend/vacation correctly, today has an indigo ring, month nav + Today work, the child filter switches family-wide vs single-child, and sick children show as colored dots with a tap popover in family view. Confirm day-numbers are legible mono digits.
    5. WEEKLY SUMMARY (D-08): open Analytics. Confirm the "This week" card shows coins earned (gold), tasks-done rate (chip), and a streak highlight (or the "no streak yet" fallback), with correct-looking values; confirm the mocked charts below are unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" plus your D-03 choice (a / b / both), or list issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Verification-only plan; no code changes, no data crosses a new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-058-20 | n/a (verification) | phase visual sign-off | accept | No new surface introduced; this plan runs gates and records a human decision only |

This plan makes no code changes and introduces no new auth/session/money/write surface. It confirms the phase's read-only + config-write UI is correct.
</threat_model>

<verification>
- Fast gate (npm test + tsc + remnant sweep) green, then the one-time build gate (npm run build) green, before the checkpoint
- Human sign-off obtained with an explicit D-03 layout decision
</verification>

<success_criteria>
- Recolor sweep visually confirmed clean (no cyan/neon-green/amber/hardcoded-hex; gold only on money)
- D-03 winning layout chosen (a / b / both)
- Year Calendar grid + sick overlay and Weekly Summary card confirmed correct
</success_criteria>

<output>
Create `.planning/phases/05.8-parent-redesign/05.8-09-SUMMARY.md` when done, recording the operator's D-03 decision and any punch-list outcomes
</output>
</content>
</invoke>
