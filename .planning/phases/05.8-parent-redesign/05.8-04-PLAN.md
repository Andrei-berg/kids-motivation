---
phase: 05.8-parent-redesign
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - components/parent-center/screens/ChatPanel.tsx
  - components/parent-center/ui.tsx
autonomous: true
requirements: [D-09]

must_haves:
  truths:
    - "ChatPanel's DARK_ACT activity-color map carries no neon-green/gold-amber hardcoded hex"
    - "The ui.tsx cyan Btn variant hover color is token-driven (T.indigoHi), not #4DDCFF"
    - "Streak/badge activity colors use T.warning (not gold); coin figures use the money atom or T.success"
  artifacts:
    - path: "components/parent-center/screens/ChatPanel.tsx"
      provides: "ink-recolored activity classification colors"
      contains: "T.warning"
    - path: "components/parent-center/ui.tsx"
      provides: "token-driven cyan Btn hover"
      contains: "hBg: T.indigoHi"
  key_links:
    - from: "components/parent-center/screens/ChatPanel.tsx"
      to: "components/parent-center/tokens.ts"
      via: "T token color map"
      pattern: "T\\.(warning|danger|indigoHi|success)"
---

<objective>
Close the two genuine hardcoded-neon remnants the research found on the parent-center side: ChatPanel's `DARK_ACT` activity-color map (`#00E676`/`#FF8A80`/`#FFD93D`/`#FFC107`/`#F59E0B`/`#8B7BF5`) and ui.tsx's `cyan` Btn variant hover literal (`#4DDCFF`). Pure recolor in place — no consolidation onto the shared ChatThread component (out of this phase's "reskin, not rebuild" scope).

Purpose: These are the last hardcoded-hex holdouts on the parent-center screens; the six other CONTEXT-listed screens are already on T (spot-check only).
Output: Ink-token activity colors in ChatPanel, token-driven cyan-variant hover in ui.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05.8-parent-redesign/05.8-PATTERNS.md
@.planning/phases/05.8-parent-redesign/05.8-UI-SPEC.md

<interfaces>
components/parent-center/ui.tsx — files here import tokens as: import { T } from './tokens'  (relative, same dir)
components/parent-center/screens/ChatPanel.tsx — import { T } from '../tokens' ; money atom path: import { Amount, LedgerRow } from '@/components/design/atoms'
T token keys available: success, danger, warning, indigoHi, indigoSoft, text, muted. NO gold key on T (money goes through Amount/LedgerRow theme="ink").
DARK_ACT is Record<ActivityType, { bg; border; text; glow? }> at ChatPanel.tsx ~line 48-53 (grep the full map, including coins_loss, before editing).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recolor ChatPanel DARK_ACT map + inline neon literals</name>
  <files>components/parent-center/screens/ChatPanel.tsx</files>
  <read_first>
    - components/parent-center/screens/ChatPanel.tsx (the full DARK_ACT map ~lines 48-53, the inline tab-array literal ~line 136, the hardcoded badge label color ~line 239)
    - .planning/phases/05.8-parent-redesign/05.8-UI-SPEC.md ChatPanel DARK_ACT recolor table (locked old-to-new mapping)
    - components/design/atoms.tsx Amount/LedgerRow signatures (money atom, theme="ink" gives gold)
  </read_first>
  <action>
    Ensure `import { T } from '../tokens'` is present. Apply the UI-SPEC locked DARK_ACT recolor table verbatim: `coins_gain.text` `#00E676` becomes `T.success` (keep it a plain badge label — do NOT hand-roll green; render actual coin figures through `Amount money={true} theme="ink"` only where a numeric coin value is displayed, gold there); `coins_loss.text` `#FF8A80` becomes `T.danger`; `streak.text` `#FFD93D` becomes `T.warning` (streaks are not money, never gold); `badge.text` `#FFC107` becomes `T.warning`; `generic.text` `#8B7BF5` becomes `T.indigoHi` (already equals this hex, pure substitution); the corresponding `bg`/`border` neon rgba values become the matching `hexAlpha`-style soft token (`T.successSoft`/`T.dangerSoft`/`T.warningSoft`/`T.indigoSoft`). Also recolor the inline `activity` tab-array label `#F59E0B` (~line 136) to `T.indigoHi` (nav label, not money) and the hardcoded `color: '#FFC107'` badge label (~line 239) to `T.warning`. Keep all `glow` box-shadow effects but rebase their rgba on the same token hue. Do NOT consolidate onto ChatThread; recolor in place only.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -viE '^\s*//' components/parent-center/screens/ChatPanel.tsx | grep -iE '#00E676|#FF8A80|#FFD93D|#FFC107|#F59E0B|#8B7BF5|#4DDCFF' ; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - The final grep finds none of the six neon hexes (prints `exit:1`)
    - `grep -c "T.warning" components/parent-center/screens/ChatPanel.tsx` returns at least 2 (streak + badge recolored)
    - `grep -ci "goldText\|#F0BE6A" components/parent-center/screens/ChatPanel.tsx` returns 0 (no hand-rolled gold — gold only via Amount atom if a coin figure is shown)
  </acceptance_criteria>
  <done>DARK_ACT map and the two inline neon literals are all token-driven; streak/badge use T.warning; no gold hand-rolled; ChatThread untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ui.tsx cyan Btn variant hover to T.indigoHi</name>
  <files>components/parent-center/ui.tsx</files>
  <read_first>
    - components/parent-center/ui.tsx (the Btn variant map ~line 97, `cyan: { bg: T.cyan, fg: '#04131A', bd: T.cyan, hBg: '#4DDCFF' }`)
    - .planning/phases/05.8-parent-redesign/05.8-UI-SPEC.md ui.tsx cyan variant note (fold hover into T.indigoHi, keep the variant NAME)
  </read_first>
  <action>
    In the `cyan` Btn variant object, change `hBg: '#4DDCFF'` to `hBg: T.indigoHi`. Keep the variant name `cyan` and the `bg`/`fg`/`bd` values as-is (only consumer is ChildProfile.tsx's Freeze button; renaming ripples unnecessarily). No other change to ui.tsx.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "hBg: T.indigoHi" components/parent-center/ui.tsx && grep -vE '^\s*//' components/parent-center/ui.tsx | grep -qi '#4DDCFF' && echo STILL_HARDCODED || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - The verify prints `OK` (T.indigoHi present, `#4DDCFF` gone outside comments)
    - `grep -c "cyan:" components/parent-center/ui.tsx` still returns 1 (variant name preserved)
  </acceptance_criteria>
  <done>The cyan Btn variant hover is token-driven (T.indigoHi); variant name unchanged; no other edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Presentation-only change; no data crosses a new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-058-07 | Tampering | ChatPanel color map / ui.tsx Btn variant | accept | Style-only constants; no message content, data-fetch, or write path touched |

Pure recolor plan: no new auth/session/money-write surface, no new free-text fields (message rendering logic unchanged; only color constants edited).
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- Both grep gates confirm zero neon/hardcoded-hex remnants
- `npm test` still green
</verification>

<success_criteria>
- ChatPanel activity colors and the ui.tsx cyan variant hover are entirely token-driven
- No neon/gold hardcoded hex remains outside comments
- No structural or logic changes; ChatThread not consolidated
</success_criteria>

<output>
Create `.planning/phases/05.8-parent-redesign/05.8-04-SUMMARY.md` when done
</output>
