---
phase: 05.8-parent-redesign
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/settings/ActivitiesManager.tsx
  - components/settings/SectionsManager.tsx
  - components/settings/SubjectsManager.tsx
  - components/settings/RoomTasksManager.tsx
autonomous: true
requirements: [D-09]

must_haves:
  truths:
    - "All four schedule-tab CRUD managers render entirely on ink T tokens"
    - "No cyan/neon-green/amber/hardcoded-rgba/hex remnants remain in any of the four files"
    - "Gold never appears (no money figures in these managers)"
  artifacts:
    - path: "components/settings/ActivitiesManager.tsx"
      provides: "ink-recolored activities CRUD"
      contains: "from '@/components/parent-center/tokens'"
    - path: "components/settings/RoomTasksManager.tsx"
      provides: "ink-recolored room-tasks CRUD"
      contains: "from '@/components/parent-center/tokens'"
  key_links:
    - from: "components/settings/ActivitiesManager.tsx"
      to: "components/parent-center/tokens.ts"
      via: "import { T }"
      pattern: "@/components/parent-center/tokens"
---

<objective>
Recolor the four remaining mounted Schedule-tab CRUD managers (Activities, Sections, Subjects, RoomTasks) onto the ink theme. All four carry the identical `rgba(255,255,255,*)` / hardcoded-hex literal set (just in different proportions) and share one substitution recipe with the DayBlocksManager recolor.

Purpose: These are pure, mechanical recolors with no capability change (D-02/D-03/D-05/D-06/D-08 do not touch them). Isolating them into one plan keeps the four highest-volume mechanical files off the critical path of the feature work.
Output: Four ink-themed managers, structure and CRUD logic unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05.8-parent-redesign/05.8-PATTERNS.md
@.planning/phases/05.8-parent-redesign/05.8-UI-SPEC.md

<interfaces>
components/parent-center/tokens.ts — export const T with keys: bg1, card, cardHi, cardBorder, cardBorderHi, text, textDim, muted, faint, indigo, indigoHi, indigoSoft, success, successSoft, warning, warningSoft, danger, dangerSoft, r, rM, rL. NO gold/goldText key.
Import path (files in components/settings/): import { T } from '@/components/parent-center/tokens'
Substitution recipe is the shared one in PATTERNS.md DayBlocksManager table + RESEARCH.md "Recolor pattern (D-09) concrete replacement table".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recolor ActivitiesManager.tsx and SectionsManager.tsx</name>
  <files>components/settings/ActivitiesManager.tsx, components/settings/SectionsManager.tsx</files>
  <read_first>
    - components/settings/ActivitiesManager.tsx (highest literal count, ~44 instances)
    - components/settings/SectionsManager.tsx
    - .planning/phases/05.8-parent-redesign/05.8-PATTERNS.md DayBlocksManager recolor substitution table and RESEARCH.md "Recolor pattern (D-09) concrete replacement table"
  </read_first>
  <action>
    Add `import { T } from '@/components/parent-center/tokens'` to each file. Apply the shared substitution recipe: `rgba(255,255,255,0.05/0.03/0.04)` card/row surfaces become `T.card`/`T.cardHi`; `rgba(255,255,255,0.1/0.08)` borders become `T.cardBorder`/`T.cardBorderHi`; `rgba(238,238,255,0.5/0.4/0.35/0.3)` muted text become `T.muted`/`T.textDim`; `#fff` primary text becomes `T.text`; `rgba(99,102,241,0.06/0.2/0.25/0.8)` indigo accents/active become `T.indigoSoft`/`T.cardBorderHi`/`T.indigo`; `rgba(244,63,94,*)`/`#F43F5E`/`#f43f5e` error/delete become `T.dangerSoft`/`T.danger`; `rgba(16,185,129,*)`/`#34d399` success/active-toggle become `T.successSoft`/`T.success`. Any Tailwind stock classes (`bg-gray-*`, `text-gray-*`, `amber-*`, `yellow-*`) become inline `T` styles (e.g. `text-yellow-400` to `T.warning`). No structural or CRUD-logic changes (recolor only). No gold and no amber survives.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -viE '^\s*//' components/settings/ActivitiesManager.tsx components/settings/SectionsManager.tsx | grep -iE 'rgba\(255,255,255|rgba\(238,238,255|#fff|#F43F5E|#34d399|amber|bg-gray-|text-gray-|#F0BE6A' ; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - The final grep finds nothing (prints `exit:1`, meaning zero forbidden literals outside comments across both files)
    - `grep -l "@/components/parent-center/tokens" components/settings/ActivitiesManager.tsx components/settings/SectionsManager.tsx` lists both files
    - CRUD handlers and component structure are unchanged (git diff shows style-only edits)
  </acceptance_criteria>
  <done>Both managers import T and render fully on ink tokens; no forbidden remnants; behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Recolor SubjectsManager.tsx and RoomTasksManager.tsx</name>
  <files>components/settings/SubjectsManager.tsx, components/settings/RoomTasksManager.tsx</files>
  <read_first>
    - components/settings/SubjectsManager.tsx (fewest literals, ~4 instances)
    - components/settings/RoomTasksManager.tsx
    - .planning/phases/05.8-parent-redesign/05.8-PATTERNS.md DayBlocksManager recolor substitution table (same recipe as Task 1)
  </read_first>
  <action>
    Apply the identical substitution recipe from Task 1 to both files: add `import { T } from '@/components/parent-center/tokens'`, replace every `rgba(255,255,255,*)` surface/border, `rgba(238,238,255,*)` muted text, `#fff` primary text, `rgba(99,102,241,*)` indigo accents, `#F43F5E`/`rgba(244,63,94,*)` danger, `#34d399`/`rgba(16,185,129,*)` success, and any Tailwind stock/amber classes with the corresponding `T` token inline styles. RoomTasksManager already resolves familyId via `useAppStore()` (05.2 decision) — do not change that or any CRUD/reorder/delete-guard logic. Recolor only. No gold, no amber.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -viE '^\s*//' components/settings/SubjectsManager.tsx components/settings/RoomTasksManager.tsx | grep -iE 'rgba\(255,255,255|rgba\(238,238,255|#fff|#F43F5E|#34d399|amber|bg-gray-|text-gray-|#F0BE6A' ; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - The final grep finds nothing (prints `exit:1`, zero forbidden literals outside comments across both files)
    - `grep -l "@/components/parent-center/tokens" components/settings/SubjectsManager.tsx components/settings/RoomTasksManager.tsx` lists both files
    - RoomTasksManager's `useAppStore()` familyId resolution and delete-guard logic are unchanged (git diff shows style-only edits)
  </acceptance_criteria>
  <done>Both managers import T, render fully on ink tokens; no forbidden remnants; CRUD logic untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (browser client, RLS) | All four managers write family config via existing family-isolation RLS; no write-path or endpoint change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-058-05 | Tampering | Activities/Sections/Subjects/RoomTasks managers | accept | Style-only change; no data-layer, validation, or write-path touched. RLS remains the boundary |
| T-058-06 | Elevation of Privilege | RoomTasks delete-guard | accept | Untouched by this plan; the legacy-task delete guard (05.2 pg_trigger_depth) stays intact |

Pure recolor plan: no new fields, no new writes, no auth/session/money surface, no new free-text inputs (XSS surface unchanged).
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- Both per-task grep gates find zero forbidden remnants
- `npm test` full suite still green (no logic changed)
</verification>

<success_criteria>
- All four managers import T and render entirely on the ink theme
- Zero rgba/hex/Tailwind-stock/amber/gold remnants outside comments
- No structural or CRUD-logic changes (recolor only)
</success_criteria>

<output>
Create `.planning/phases/05.8-parent-redesign/05.8-03-SUMMARY.md` when done
</output>
