---
phase: 05.8
slug: parent-redesign
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-20
---

# Phase 05.8 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest (`vitest.config.ts`, `environment: 'node'`, `include: ['tests/**/*.test.ts']`) |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `npx vitest run tests/day-blocks.test.ts tests/day-type.test.ts` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run <touched test file>`
- **After every plan wave:** Run `npm test`
- **Before `/gsd:verify-work`:** Full suite must be green; D-09's recolor bar
  ("no cyan/neon-green/hardcoded-hex remnants") is verified manually/visually —
  not automatable. Plan a `checkpoint:human-verify` step for the recolor sweep,
  matching the precedent set by 05.3/05.7's consolidated visual sign-off.
- **Max feedback latency:** ~15 seconds (unit suite)

---

## Per-Task Verification Map

This phase has no mapped REQ-IDs (`phase_req_ids` is null for 5.8). Most of the
phase is presentational/visual (D-04, D-07, D-09 recolor; D-05/D-06 grid layout)
and is verified manually against the "no cyan/neon-green/hardcoded-hex remnants"
bar. Three units of new logic are testable:

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| D-02 dow conversion | TBD | 0 | D-02 | — | N/A | unit | `npx vitest run tests/schedule-link.test.ts` | ❌ W0 | ⬜ pending |
| D-08 weekly coin sum | TBD | 0 | D-08 | — | N/A | unit | `npx vitest run tests/wallet.test.ts` | ❌ W0 | ⬜ pending |
| `getDaysInRange` parity | TBD | 0 | D-05/D-06 | — | N/A | unit/integration | `npx vitest run tests/day-blocks.test.ts` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/schedule-link.test.ts` (or extend `tests/day-blocks.test.ts`) —
      covers the D-02 `scheduleDowToBlockDow` day-of-week conversion helper at
      both boundaries (Mon 1→0, Sun 7→6) and mid-week
- [ ] A test covering the Weekly Summary week-window coin sum (D-08) — no
      existing test isolates `getTransactions`'s `since` param; must assert
      only positive `coins_change` rows are summed
- [ ] `getDaysInRange` does not exist yet — must be written (mirroring
      `getWeekData`'s query shape) before it can be tested

*(All three are small, additive gaps — no test framework/config work needed;
`vitest` + the existing `tests/day-blocks.test.ts` fixture pattern (`makeBlock`
helper) are directly reusable.)*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Ink-theme recolor sweep (no cyan/neon-green/hardcoded-hex remnants) | D-09 | Visual/aesthetic judgment, not assertable in a unit test | Open each in-scope screen in the browser, confirm palette matches `lib/design/tokens.ts` ink theme, confirm gold only appears on money amounts |
| Day Constructor per-child override UI (candidate a vs b) | D-03 | User-facing design decision made at verification per explicit user direction (5.7 D-11 precedent) | Build both candidates, exercise both in the browser against real `getDayBlocks` data, pick winner at verification checkpoint |
| Year Calendar month/year grid + sick-day overlay | D-05/D-06 | Visual grid layout and multi-child overlay legibility | Open Year Calendar screen, confirm day cells color-code correctly by day type and sick-day dots/marks render per child |
| Weekly Summary card copy/layout | D-08 | Presentation/tone judgment (family-bank writing rules) | Open Analytics screen, confirm all three facts appear with correct values and readable copy |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
