---
phase: 02.4-shop-approval
plan: "04"
subsystem: ui
tags: [middleware, next.js, routing, parent-dashboard, preview-mode]

# Dependency graph
requires:
  - phase: 02.4-03
    provides: parent dashboard with pending/approved purchase panels
provides:
  - Middleware preview bypass allowing parents to access /kid/* with ?preview=true
  - ChildCard "Kid View" button navigating to /kid/day?childId={id}&preview=true
affects: [02.5-notifications-anims]

# Tech tracking
tech-stack:
  added: []
  patterns: [preview-mode query param bypass in middleware, onKidView prop pattern]

key-files:
  created: []
  modified:
    - middleware.ts
    - app/parent/dashboard/page.tsx

key-decisions:
  - "Preview bypass uses ?preview=true query param (not session/cookie) — stateless, no server state needed"
  - "isPreviewMode declared inside membership guard block — only evaluated when authenticated user exists"

patterns-established:
  - "Preview param pattern: ?preview=true bypasses role guards without account switch"
  - "ChildCard action prop pattern: onKidView follows same pattern as onFillDay"

requirements-completed: [REQ-PARENT-008]

# Metrics
duration: 1min
completed: 2026-04-09
---

# Phase 02.4 Plan 04: Shop Approval — Kid View Preview Mode Summary

**Middleware ?preview=true bypass + parent dashboard 'Смотреть как ребёнок' button per ChildCard**

## Performance

- **Duration:** ~1 min
- **Started:** 2026-04-09T09:03:29Z
- **Completed:** 2026-04-09T09:04:11Z
- **Tasks:** 1 of 2 (checkpoint:human-verify pending)
- **Files modified:** 2

## Accomplishments
- Middleware guard for /kid/* now checks `?preview=true` before redirecting non-child roles
- ChildCard in parent dashboard gains `onKidView` prop and "Смотреть как ребёнок →" secondary button
- Button navigates to `/kid/day?childId={id}&preview=true`
- Plan 03 additions (pending/approved purchase panels) remain fully intact

## Task Commits

1. **Task 1: Middleware preview bypass + dashboard Kid View button** - `a2c57c6` (feat)

**Plan metadata:** (pending final commit)

## Files Created/Modified
- `middleware.ts` — Added `isPreviewMode` check; /kid guard now has `&& !isPreviewMode`
- `app/parent/dashboard/page.tsx` — ChildCard gains `onKidView` prop + "Смотреть как ребёнок →" button

## Decisions Made
- Preview bypass uses `?preview=true` query param (stateless, no session change needed)
- `isPreviewMode` placed inside `if (membership)` block — only evaluated for authenticated users with known role
- `onKidView` prop follows same pattern as `onFillDay` (Option A from plan spec)

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Full phase 2.4 (shop-approval) ready for human end-to-end verification (see checkpoint below)
- After verification: Phase 2.5 notifications-anims can begin

---
*Phase: 02.4-shop-approval*
*Completed: 2026-04-09*
