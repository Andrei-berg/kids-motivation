---
phase: 02.4-shop-approval
plan: "02"
subsystem: ui
tags: [react, next.js, shop, purchase-request, wallet]

# Dependency graph
requires:
  - phase: 02.4-01
    provides: createPurchaseRequest repo function + RewardPurchase status fields (pending/approved/rejected/delivered)
provides:
  - Kid shop page calls createPurchaseRequest instead of purchaseReward (coins NOT deducted on submit)
  - Status display in My Requests: pending/approved/rejected/delivered with color-coded badges
  - Rejection note shown below rejected status badge
affects:
  - 02.4-03 (parent approval UI reads same RewardPurchase status values)
  - 02.4-04 (delivery flow)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "getPurchaseStatusLabel: local helper mapping p.status (with fulfilled fallback) to text + className"
    - "Status badge with optional rejection note below — text-right div wrapping both"

key-files:
  created: []
  modified:
    - app/kid/shop/page.tsx

key-decisions:
  - "Toast copy updated to 'Ждёт одобрения родителя' to match request-not-purchase framing"
  - "getPurchaseStatusLabel uses p.status ?? (p.fulfilled ? 'delivered' : 'pending') for backward compat with old rows"

patterns-established:
  - "Status badge pattern: { text, className } object from helper, applied inline with dynamic className"

requirements-completed:
  - REQ-SHOP-004
  - REQ-SHOP-007

# Metrics
duration: 15min
completed: 2026-04-09
---

# Phase 02.4 Plan 02: Kid Shop Approval Flow Summary

**Kid shop updated to freeze-then-approve flow: createPurchaseRequest replaces purchaseReward, status badges show pending/approved/rejected/delivered with rejection note support**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-04-09T09:00:00Z
- **Completed:** 2026-04-09T09:15:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- `createPurchaseRequest` now called on purchase submit — coins are NOT deducted until parent approves
- Confirmation dialog copy updated: "Отправить запрос" button, explains coins deducted after parent approval
- My Requests section shows all 4 status states (pending=amber, approved=green, rejected=red, delivered=purple) via `getPurchaseStatusLabel` helper
- Rejection note displayed below rejected status badge
- My Requests limit increased from 5 to 10 items for better history visibility

## Task Commits

1. **Task 1: Update kid shop to use createPurchaseRequest + status display** - `88f5a72` (feat)

## Files Created/Modified
- `app/kid/shop/page.tsx` - Import swapped to createPurchaseRequest; confirmation copy + button label updated; getPurchaseStatusLabel helper added; My Requests rebuilt with 4-state badges + rejection notes + 10-item limit

## Decisions Made
- `getPurchaseStatusLabel` uses `p.status ?? (p.fulfilled ? 'delivered' : 'pending')` to handle old rows that have no status field — preserves backward compatibility
- Toast changed to "Запрос отправлен! Ждёт одобрения родителя." — accurate copy since coins are not deducted immediately

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Kid shop now sends purchase requests and displays status correctly
- Ready for 02.4-03: parent approval UI (list of pending requests, approve/reject/deliver actions)

---
*Phase: 02.4-shop-approval*
*Completed: 2026-04-09*
