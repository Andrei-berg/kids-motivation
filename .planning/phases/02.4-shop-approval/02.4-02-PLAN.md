---
phase: 02.4-shop-approval
plan: "02"
type: execute
wave: 2
depends_on:
  - 02.4-01
files_modified:
  - app/kid/shop/page.tsx
autonomous: true
requirements:
  - REQ-SHOP-004
  - REQ-SHOP-007

must_haves:
  truths:
    - "Child taps 'Хочу это!' and confirms → a purchase request is created (coins NOT deducted)"
    - "Child sees status labels: pending=Ожидает одобрения, approved=Одобрено, rejected=Отклонено, delivered=Выдано"
    - "If the item has auto_approve=true, purchase is approved immediately and coins ARE deducted"
    - "Child's coin balance is NOT reduced after submitting a request (only after parent approves)"
    - "My Requests section shows all statuses with correct icons/colors"
  artifacts:
    - path: "app/kid/shop/page.tsx"
      provides: "Updated kid shop that calls createPurchaseRequest instead of purchaseReward"
      contains: "createPurchaseRequest"
  key_links:
    - from: "app/kid/shop/page.tsx handlePurchase()"
      to: "createPurchaseRequest in wallet.repo"
      via: "import and call"
      pattern: "createPurchaseRequest"
---

<objective>
Update the kid shop page to use the freeze-then-approve flow. The "Хочу это!" button now calls `createPurchaseRequest` instead of `purchaseReward`, coins are NOT immediately deducted, and the status display in "My Requests" shows pending/approved/rejected/delivered states.

Purpose: The child's experience changes — submitting a purchase is now a request, not an instant transaction. The UI must communicate this clearly (different confirmation copy, status labels, no balance drop after request).

Output: Updated `app/kid/shop/page.tsx` with new purchase flow and status display.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02.3-kid-screen/02.3-05-SUMMARY.md
@.planning/phases/02.4-shop-approval/02.4-01-SUMMARY.md

<interfaces>
<!-- New function from Plan 01 that this plan calls -->

From lib/repositories/wallet.repo.ts (added in Plan 01):
```typescript
export async function createPurchaseRequest(
  childId: string,
  rewardId: string
): Promise<RewardPurchase>
// Does NOT deduct coins (unless reward.auto_approve === true).
// Returns RewardPurchase with status='pending' (or 'approved' if auto_approve).
// Throws 'Insufficient coins' if wallet.coins < reward.price_coins.
// Throws 'Reward not found' if rewardId invalid.
```

From lib/models/wallet.types.ts (extended in Plan 01):
```typescript
export interface RewardPurchase {
  // ... all existing fields plus:
  status: 'pending' | 'approved' | 'rejected' | 'delivered'
  frozen_coins: number
  processed_by: string | null
  processed_at: string | null
  rejection_note: string | null
}
```

<!-- Status display mapping to use in UI -->
// pending  → '⏳ Ожидает одобрения'  amber badge
// approved → '✅ Одобрено'            green badge
// rejected → '❌ Отклонено'           red badge
// delivered→ '🎉 Выдано'             purple badge
// fallback  → '⏳ Ожидает'            gray (for old rows with no status field)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update kid shop to use createPurchaseRequest + status display</name>
  <files>app/kid/shop/page.tsx</files>
  <action>
    Read the current `app/kid/shop/page.tsx` fully before editing.

    **Change 1 — import:** Replace `purchaseReward` import with `createPurchaseRequest`:
    ```typescript
    // BEFORE:
    import { getWallet, getRewards, purchaseReward, getPurchases } from '@/lib/repositories/wallet.repo'
    // AFTER:
    import { getWallet, getRewards, createPurchaseRequest, getPurchases } from '@/lib/repositories/wallet.repo'
    ```

    **Change 2 — handlePurchase function:** Replace the call to `purchaseReward(activeMemberId, selectedReward.id)` with `createPurchaseRequest(activeMemberId, selectedReward.id)`. The rest of the try/catch/finally logic stays identical — reload wallet + purchases after the call.

    **Change 3 — confirmation dialog copy:** In the `confirmStep === 'confirm'` branch, change the description text from:
    - "Спишется {price} монет за «{title}»"
    to:
    - "Отправить запрос на «{title}» за {price} монет? Монеты спишутся после одобрения родителем."
    
    Also change the confirm button label from "Подтвердить" to "Отправить запрос".

    **Change 4 — toast on success:** Change the success toast from "Куплено!" to "Запрос отправлен! Ждёт одобрения родителя." (if auto_approve was true it will show approved immediately in the list anyway).

    **Change 5 — status display in "My Requests" section:** Replace the current status badge logic:
    ```typescript
    // OLD: p.fulfilled ? '✅ Выполнено' : '⏳ Ожидает'
    // NEW: helper function
    function getPurchaseStatusLabel(p: RewardPurchase): { text: string; className: string } {
      const s = p.status ?? (p.fulfilled ? 'delivered' : 'pending')
      if (s === 'pending')   return { text: '⏳ Ожидает', className: 'bg-amber-100 text-amber-700' }
      if (s === 'approved')  return { text: '✅ Одобрено', className: 'bg-green-100 text-green-700' }
      if (s === 'rejected')  return { text: '❌ Отклонено', className: 'bg-red-100 text-red-700' }
      if (s === 'delivered') return { text: '🎉 Выдано', className: 'bg-violet-100 text-violet-700' }
      return { text: '⏳ Ожидает', className: 'bg-gray-100 text-gray-500' }
    }
    ```
    Apply this helper in the My Requests list rendering. Show up to 10 purchases (increase from 5 to 10 to show full history).

    **Change 6 — rejection note display:** For rejected purchases, if `p.rejection_note` is set, show it as a small gray line below the status badge: `<p className="text-xs text-gray-400 mt-0.5">{p.rejection_note}</p>`

    Do not change any other part of the file (layout, grid, bottom sheet structure, CSS classes).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `createPurchaseRequest` is imported and called (not `purchaseReward`)
    - Status labels show: Ожидает/Одобрено/Отклонено/Выдано
    - Confirmation copy says "Отправить запрос" not "Подтвердить"
    - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` exits 0.
Visual check: open /kid/shop, tap a reward, confirm screen says "Отправить запрос". After submit, "My Requests" shows "⏳ Ожидает".
</verification>

<success_criteria>
1. `createPurchaseRequest` replaces `purchaseReward` call in handlePurchase
2. Confirmation dialog shows "Отправить запрос" button and explains coins deduct after parent approval
3. My Requests shows all 4 status states with correct colors
4. Rejection note visible under rejected items
5. TypeScript zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02.4-shop-approval/02.4-02-SUMMARY.md`
</output>
