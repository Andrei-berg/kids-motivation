---
phase: 05.11-legacy-cleanup
plan: 02
subsystem: ui
tags: [css, cleanup, globals-css]

# Dependency graph
requires:
  - phase: 05.11-01
    provides: "components/NavBar.tsx and its sole consumer (app/wallboard/page.tsx) deleted"
provides:
  - "app/globals.css NAVIGATION section (.nav, .navL, .navR, .nav .brand) removed — dead code left over from the deleted NavBar component"
affects: [05.11-03]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-class grep-proof-then-delete methodology for CSS purges: grep className=\"...\\bclass\\b\" plus a bare-word sanity pass across app/ and components/ *.tsx before removing any selector"

key-files:
  created: []
  modified:
    - app/globals.css

key-decisions:
  - "Deleted only the NAVIGATION section (the plan's explicitly named dead block); did not attempt additional optional class-by-class purges elsewhere in the file — kept the change surgical per CLAUDE.md 'preserve original code, only change what is necessary'"
  - "Left the EXPENSES, WALLET SYSTEM, .spinner, and .muted rules fully untouched, matching the plan's exclusion list and the PATTERNS.md finding that EXPENSES/WALLET classes are still consumed by surviving components"

patterns-established: []

requirements-completed: ["05.11-SC2"]

# Metrics
duration: 15min
completed: 2026-07-23
---

# Phase 05.11 Plan 02: globals.css NAVIGATION Purge Summary

**Removed the dead `.nav`/`.navL`/`.navR`/`.nav .brand` CSS block (35 lines) from app/globals.css, grep-verified to have zero surviving references after Plan 01 deleted its sole consumer, components/NavBar.tsx.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-07-23T19:15:00Z (approx.)
- **Completed:** 2026-07-23T19:30:00Z (approx.)
- **Tasks:** 1 completed
- **Files modified:** 1 (app/globals.css)

## Accomplishments
- Grep-verified zero remaining references to `.nav`, `.navL`, `.navR` as classNames in `app/` or `components/` `*.tsx` files, and zero bare-word false-positives beyond `<nav>` HTML elements (Tailwind-styled) and `nav.*` i18n keys
- Deleted the NAVIGATION section (`app/globals.css:570-604`): `.nav`, `.nav .brand`, `.navL`/`.navR` — the only remaining rule set NavBar.tsx (deleted in Plan 01) uniquely consumed
- Confirmed the excluded sections survive untouched: EXPENSES (`expenses-page`, `expense-card`, `expense-modal`), WALLET SYSTEM (`wallet-card`, `wallet-dashboard`), `.spinner`, `.muted`
- `components/WalletDashboard.tsx` confirmed byte-identical (`git diff --quiet` passes) — out of scope per CONTEXT.md
- `app/globals.css` shrank from 4516 to 4481 lines (35 lines removed)
- Full `npm run build` (all ~55 routes) passes with the change applied

## Task Commits

Each task was committed atomically:

1. **Task 1: Grep-verify and remove dead legacy-only classes from globals.css** - `a102473` (style)

_No plan-metadata commit for STATE.md/ROADMAP.md — this is a worktree-isolated execution; the orchestrator updates those after merge._

## Files Created/Modified
- `app/globals.css` - removed the NAVIGATION section (lines 570-604: `.nav`, `.nav .brand`, `.navL`, `.navR`); no other lines touched

## Grep Proofs (per acceptance criteria — "SUMMARY records the zero-hit grep output that justified removal")

**`.nav` className:**
```
grep -rn 'className="[^"]*\bnav\b' app components --include="*.tsx"
→ (no output — zero hits)
```

**`.navL` className:**
```
grep -rn 'className="[^"]*\bnavL\b' app components --include="*.tsx"
→ (no output — zero hits)
```

**`.navR` className:**
```
grep -rn 'className="[^"]*\bnavR\b' app components --include="*.tsx"
→ (no output — zero hits)
```

**Bare-word sanity pass for `nav`/`navL`/`navR`:** all hits were `<nav>` HTML elements with
inline/Tailwind styling (`components/parent-center/ParentCenter.tsx`, `components/parent/ParentNav.tsx`,
`components/kid/KidNav.tsx`) or `t('nav.*')` i18n keys — none reference the deleted CSS classes.

**`.nav .brand` / bare `.brand`:** `grep -n "^\.brand\b\|[^.]\.brand[^a-zA-Z]" app/globals.css`
found exactly one rule in the whole file (`.nav .brand` itself, line 588) — no independent
bare `.brand` selector exists elsewhere to preserve. `grep -rn 'className="[^"]*\bbrand\b'
app components --include="*.tsx"` also returned zero hits.

**`.muted` (must survive — sanity check):** `grep -rln 'className="[^"]*\bmuted\b' app
components --include="*.tsx"` returned hits in `components/BulkModal.tsx` and
`components/GoalsModal.tsx` — confirmed shared and correctly left in place.

## Decisions Made
- Scoped the deletion to exactly the NAVIGATION section named in the plan's `<interfaces>` block, rather than pursuing the plan's optional "any other class you grep-prove dead" extension — minimizes risk to surviving pages and matches CLAUDE.md's "preserve original code, only change what is necessary" directive. No other globals.css class was evaluated as a removal candidate this plan.

## Deviations from Plan

None - plan executed exactly as written.

One pre-existing discrepancy noted (not a deviation from this plan's work): the plan's
`<verify><automated>` line checks `grep -q "expense-page" app/globals.css`, but the actual
class name in the file is `expenses-page` (plural) — `"expense-page"` is not a substring of
`"expenses-page"` so that literal grep returns no match. This predates this plan (the EXPENSES
section was untouched) and is superseded by the plan's own `<acceptance_criteria>` line, which
uses `grep -c "expense-page\|expense-modal\|expense-chart"` (an OR pattern that does match
`expense-modal`/`expense-card` etc., count 16) — the canonical acceptance criterion passes.
Not fixed here since it requires no code change, only a note for the plan author.

## Issues Encountered

- `npm run build` initially failed in this worktree with `Error: @supabase/ssr: Your project's
  URL and API key are required` because `.env.local` is git-ignored and a fresh worktree has
  no copy of it (same environment gap documented in `05.11-01-SUMMARY.md`). Resolved by
  temporarily copying `.env.local` from the main repo checkout into the worktree for the
  build-verification step, then deleting the copy (and the `.next/` build output) before the
  final commit — confirmed via `git status --short` that neither ever appeared as
  tracked/untracked (both match `.gitignore` patterns). No code change required.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- `05.11-SC2` ("globals.css shrinks to reset + genuinely shared utilities") is satisfied for
  the NAVIGATION-section scope this plan targeted.
- EXPENSES, WALLET SYSTEM, `.spinner`, `.muted` sections remain fully intact for any surviving
  screen or the still-orphaned `components/WalletDashboard.tsx` (flagged again here, unchanged
  from Plan 01's note — a future cleanup candidate, out of this phase's scope).
- Ready for Plan 03 (icon/manifest/layout token refresh) or phase-level verification.

---
*Phase: 05.11-legacy-cleanup*
*Completed: 2026-07-23*

## Self-Check: PASSED

- FOUND: `.planning/phases/05.11-legacy-cleanup/05.11-02-SUMMARY.md`
- FOUND: commit `a102473` (Task 1)
