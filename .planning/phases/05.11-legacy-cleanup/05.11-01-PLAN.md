---
phase: 05.11-legacy-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.js
  - middleware.ts
  - app/page.tsx
  - app/dashboard/page.tsx
  - app/wallet/page.tsx
  - app/analytics/page.tsx
  - app/wallboard/page.tsx
  - app/expenses/page.tsx
  - app/settings/page.tsx
  - app/streaks/page.tsx
  - app/records/page.tsx
  - app/audit/page.tsx
  - app/coach-rating/page.tsx
  - components/NavBar.tsx
autonomous: true
requirements: ["05.11-SC1"]

must_haves:
  truths:
    - "Requesting any of the 10 legacy routes returns an HTTP redirect to its mapped destination; no legacy page component is compiled or rendered"
    - "The 10 legacy app/*/page.tsx files and components/NavBar.tsx no longer exist in the repository"
    - "Post-login landing and the extended/unknown-role fallback resolve to /parent-center, never to the deleted /dashboard"
  artifacts:
    - path: "next.config.js"
      provides: "async redirects() with 10 server-level legacy-route redirects"
      contains: "async redirects()"
    - path: "middleware.ts"
      provides: "role-based landing whose else branch targets /parent-center"
      contains: "/parent-center"
  key_links:
    - from: "middleware.ts else branch"
      to: "/parent-center"
      via: "url.pathname assignment"
      pattern: "url\\.pathname = '/parent-center'"
    - from: "app/page.tsx postAuthPath + auto-redirect effect"
      to: "/parent-center"
      via: "redirect string literal"
      pattern: "'/parent-center'"
    - from: "next.config.js redirects()"
      to: "/parent-center and /parent/settings"
      via: "static source/destination entries"
      pattern: "destination: '/parent(-center|/settings)'"
---

<objective>
Retire the 10 legacy page routes: replace their in-app redirect-stub page code with
server-level `next.config.js` redirects, delete the page files (and the now-orphaned
`components/NavBar.tsx`), and repoint the two remaining hard references to the deleted
`/dashboard` route (`middleware.ts` fallback landing + `app/page.tsx` post-auth landing)
at `/parent-center`.

Purpose: satisfy ROADMAP 5.11 Success Criterion 1 — "One UI remains." Legacy routes must
redirect (not render their own page code) and their page code must be deleted.
Output: a slimmer route tree with no legacy page components; all legacy paths resolve via
static server-level redirects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.11-legacy-cleanup/05.11-CONTEXT.md
@.planning/phases/05.11-legacy-cleanup/05.11-PATTERNS.md

<interfaces>
<!-- Verified current state (read directly during planning) — executor should trust these
     and confirm, not re-derive from scratch. -->

Legacy route → next.config.js redirect destination (from CONTEXT.md D-04 + PATTERNS.md,
each verified by reading the stub file and its target):

| source        | destination        | permanent | why this destination |
|---------------|--------------------|-----------|----------------------|
| /dashboard    | /parent-center     | true      | stub → /parent/dashboard → /parent-center |
| /wallet       | /parent-center     | true      | stub → /parent/wallets → /parent-center |
| /analytics    | /parent-center     | true      | stub → /parent/analytics → /parent-center |
| /expenses     | /parent-center     | true      | stub → /parent/dashboard → /parent-center |
| /settings     | /parent/settings   | true      | /parent/settings is a REAL page (do NOT point at /parent-center) |
| /streaks      | /parent-center     | true      | stub → /parent/dashboard → /parent-center |
| /records      | /parent-center     | true      | stub → /parent/dashboard → /parent-center |
| /audit        | /parent-center     | true      | stub → /parent/dashboard → /parent-center |
| /coach-rating | /parent-center     | true      | stub → /parent/dashboard → /parent-center |
| /wallboard    | /parent-center     | true      | D-01: cut, no replacement; same fallback |

next.config.js current shape (verified): a single `nextConfig` object exporting
`reactStrictMode`, `experimental.instrumentationHook`, and `async headers()`. The new
`async redirects()` is a sibling function on that same object. The file is wrapped by
`withSentryConfig(nextConfig, {...})` — do not disturb that wrapper.

middleware.ts:72-74 (verified) — the else branch of the `pathname === '/'` role switch
currently sets `url.pathname = '/dashboard'`. Only this one string changes to
`/parent-center`. The `!membership` (→ /onboarding), `parent`, and `child` branches and
the separate `/parent/*` parent-only guard elsewhere in the file are NOT touched.

app/page.tsx (verified) — two references to the deleted route:
  - line 37 (`postAuthPath` helper): `return hasFamily ? '/dashboard' : '/onboarding'`
  - line 44 (auto-redirect effect): `router.replace(joinCode ? \`...\` : '/dashboard')`
  Both `'/dashboard'` literals change to `'/parent-center'`. The `joinCode`/`/onboarding`
  arms are untouched.

components/NavBar.tsx (verified) — sole importer is app/wallboard/page.tsx:4. Once
wallboard is deleted, NavBar has zero importers → delete it (D-02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add next.config.js redirects() and delete the 10 legacy route files + NavBar</name>
  <files>next.config.js, app/dashboard/page.tsx, app/wallet/page.tsx, app/analytics/page.tsx, app/wallboard/page.tsx, app/expenses/page.tsx, app/settings/page.tsx, app/streaks/page.tsx, app/records/page.tsx, app/audit/page.tsx, app/coach-rating/page.tsx, components/NavBar.tsx</files>
  <read_first>
    - next.config.js (see the current `nextConfig` object shape and the existing `async headers()` sibling function to mirror)
    - app/dashboard/page.tsx and app/settings/page.tsx (confirm each is a 2-line redirect stub before deleting; settings' target /parent/settings is a real page)
    - app/wallboard/page.tsx (617-line real page — confirm it imports NavBar and is being cut per D-01, no migration)
    - components/NavBar.tsx (the component being deleted; confirm no other importer)
    - .planning/phases/05.11-legacy-cleanup/05.11-PATTERNS.md (the verified stub→destination table and the "Suggested redirects() block" shape)
  </read_first>
  <action>
    Add a new `async redirects()` method to the same `nextConfig` object in next.config.js,
    as a sibling to the existing `async headers()` (place it directly after headers()). It
    returns an array of 10 objects, each with `source`, `destination`, and `permanent: true`,
    using EXACTLY the source→destination pairs from the interfaces table above: /dashboard,
    /wallet, /analytics, /expenses, /streaks, /records, /audit, /coach-rating, and /wallboard
    all → /parent-center; and /settings → /parent/settings (NOT /parent-center — this is the
    one exception; /parent/settings is a real page). Do not modify `reactStrictMode`,
    `experimental`, `headers()`, or the `withSentryConfig(...)` wrapper.

    Then delete the 10 legacy page files and their now-empty parent directories:
    app/dashboard/, app/wallet/, app/analytics/, app/wallboard/, app/expenses/,
    app/settings/, app/streaks/, app/records/, app/audit/, app/coach-rating/ (use
    `git rm` on each page.tsx, then remove the empty directory). Do NOT delete anything
    under app/parent/ — those stub/real pages stay.

    Finally delete components/NavBar.tsx (git rm) — it is imported only by the now-deleted
    app/wallboard/page.tsx (D-02).
  </action>
  <verify>
    <automated>test ! -e app/dashboard/page.tsx && test ! -e app/wallet/page.tsx && test ! -e app/analytics/page.tsx && test ! -e app/wallboard/page.tsx && test ! -e app/expenses/page.tsx && test ! -e app/settings/page.tsx && test ! -e app/streaks/page.tsx && test ! -e app/records/page.tsx && test ! -e app/audit/page.tsx && test ! -e app/coach-rating/page.tsx && test ! -e components/NavBar.tsx && grep -q "async redirects()" next.config.js && test -e app/parent/settings/page.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "permanent: true" next.config.js` returns 10
    - `grep -q "source: '/settings'" next.config.js` AND the /settings entry's destination is `/parent/settings` (not `/parent-center`)
    - `grep -c "destination: '/parent-center'" next.config.js` returns 9
    - All 10 files listed in `<files>` (the legacy page.tsx set) plus components/NavBar.tsx are absent (`test ! -e` passes for each)
    - app/parent/settings/page.tsx still exists (untouched)
    - `npm run build` exits 0 (no missing-module error from the deleted NavBar import, proving no stray importer remained)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Repoint the two deleted-/dashboard landing references to /parent-center</name>
  <files>middleware.ts, app/page.tsx</files>
  <read_first>
    - middleware.ts (read the `pathname === '/'` role switch at ~lines 63-77; the else branch on line 74 currently sets `/dashboard`)
    - app/page.tsx (read `postAuthPath` at ~lines 35-38 and the auto-redirect effect at ~lines 41-48; both reference `/dashboard`)
    - .planning/phases/05.11-legacy-cleanup/05.11-CONTEXT.md (D-03 — repoint targets are /parent-center, NOT /parent/dashboard)
  </read_first>
  <action>
    In middleware.ts, change ONLY the else branch of the `pathname === '/'` role switch:
    `url.pathname = '/dashboard'` → `url.pathname = '/parent-center'` (D-03). Leave the
    preceding comment ("extended or unknown role — safe fallback") and every other branch
    (`!membership` → /onboarding, `parent`, `child`) exactly as-is. Do not touch the
    `/parent/*` parent-only guard or any other logic in the file.

    In app/page.tsx, change the two `'/dashboard'` string literals to `'/parent-center'`:
    (1) in `postAuthPath` the `hasFamily ? '/dashboard' : '/onboarding'` return, and
    (2) in the auto-redirect `useEffect`, the `router.replace(joinCode ? ... : '/dashboard')`
    fallback arm. Leave the `joinCode` / `/onboarding` arms unchanged.
  </action>
  <verify>
    <automated>! grep -rn "'/dashboard'" middleware.ts app/page.tsx && grep -q "url.pathname = '/parent-center'" middleware.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'/dashboard'" middleware.ts app/page.tsx` returns 0 (no remaining reference to the deleted route in either file)
    - middleware.ts else branch reads `url.pathname = '/parent-center'`
    - app/page.tsx `postAuthPath` returns `/parent-center` for the `hasFamily` case and the auto-redirect effect falls back to `/parent-center`
    - The `!membership → '/onboarding'`, `parent`, and `child` branches in middleware.ts are byte-unchanged except the single else-branch string
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server route layer | HTTP request hits a legacy path; next.config redirects fire before filesystem routing |
| browser → middleware | authenticated request to `/` resolves a role-based landing redirect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.11-01 | Spoofing / EoP (open redirect) | next.config.js `redirects()` entries | accept | All 20 fields (10 `source` + 10 `destination`) are hardcoded static string literals with NO user-controlled input; destinations are all same-origin app paths (`/parent-center`, `/parent/settings`). Cannot be coerced to an external origin — not an open redirect. Verified: every entry is a literal per the interfaces table. |
| T-05.11-02 | Tampering / EoP (weakened access guard) | middleware.ts else branch + app/page.tsx landing | accept | Change is the redirect *target string* only (`/dashboard` → `/parent-center`); the enclosing role checks and the separate `/parent/*` parent-only guard are untouched. Extended/unknown role already resolved transitively to /parent-center (via the old `/dashboard` → `/parent/dashboard` → `/parent-center` chain) before this change, so no route becomes newly reachable. Verified by acceptance criterion that non-else branches are byte-unchanged. |
| T-05.11-03 | Information disclosure | deletion of legacy page files | accept | Deleted files were 2-line redirect stubs (plus wallboard, a client-rendered view that read only through existing RLS-bound Supabase clients). Deletion removes code; it exposes no new data path. No money/auth code in scope. |
| T-05.11-SC | Tampering (supply chain) | npm/pip/cargo installs | mitigate | No package-manager install occurs in this plan (pure config edits + deletions). Not applicable here; the sharp one-off install is gated in Plan 03's threat model. |
</threat_model>

<verification>
- `npm run build` compiles the full route tree with no legacy page files and no NavBar import errors.
- `curl -sI http://localhost:3000/dashboard` (dev server) returns a 3xx redirect to /parent-center; `/settings` redirects to /parent/settings.
- No occurrence of the string `/dashboard` remains as a redirect target in middleware.ts or app/page.tsx.
</verification>

<success_criteria>
- All 10 legacy page files and components/NavBar.tsx are deleted.
- next.config.js `redirects()` returns the 10 correct source→destination pairs (9 → /parent-center, /settings → /parent/settings), all `permanent: true`.
- middleware.ts fallback landing and both app/page.tsx landing references target /parent-center.
- `npm run build` and `npx tsc --noEmit` both exit 0.
</success_criteria>

<output>
Create `.planning/phases/05.11-legacy-cleanup/05.11-01-SUMMARY.md` when done.
</output>
