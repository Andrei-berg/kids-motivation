---
phase: 05.11-legacy-cleanup
plan: 03
subsystem: ui
tags: [pwa, manifest, app-icon, sharp, branding, design-tokens]

# Dependency graph
requires:
  - phase: 05.11-legacy-cleanup (UI-SPEC + PATTERNS plans)
    provides: locked App Icon / Brand Mark visual spec (coin geometry, hex values) and manifest/viewport metadata table
provides:
  - Three real FamilyCoins-branded app-icon PNGs (gold coin + rim + indigo star cutout on indigo square), replacing flat unbranded squares
  - apple-touch-icon.png corrected from wrong 192x192 to spec-correct 180x180
  - app/manifest.ts start_url pointed at a surviving route ('/'), colors aligned to locked tokens
  - app/layout.tsx viewport.themeColor aligned to manifest theme_color
affects: [05.11 phase verification / human visual UAT, future PWA/install-prompt work]

# Tech tracking
tech-stack:
  added: []  # sharp installed one-off via `npm i sharp --no-save`; not persisted to package.json/lockfile
  patterns:
    - "One-off native-binding tool installs use `npm i <pkg> --no-save` + a scratch script deleted immediately after use (mirrors CLAUDE.md's `npm i pg --no-save` migration precedent)"

key-files:
  created: []
  modified:
    - public/icon-512x512.png
    - public/icon-192x192.png
    - public/apple-touch-icon.png
    - app/manifest.ts
    - app/layout.tsx

key-decisions:
  - "Package Legitimacy Gate (T-05.11-SC): sharp (npmjs.com/package/sharp, lovell/sharp, official high-throughput image library) verified and approved by operator before install; installed with --no-save so it never entered package.json/package-lock.json, removed the scratch script immediately after rasterizing"
  - "Star polygon computed programmatically (standard 10-vertex alternating outer/inner-radius formula, first vertex at -90deg) rather than hand-eyeballed coordinates, per plan instruction"

patterns-established:
  - "5-point star / coin-mark SVG generation formula (outer/inner radius alternation at 36deg steps) documented in this plan's Task 2 action block for reuse if the mark needs re-export at other sizes"

requirements-completed: ["05.11-SC3"]

# Metrics
duration: 25min
completed: 2026-07-23
---

# Phase 05.11 Plan 03: FamilyCoins App Icon + Manifest Branding Summary

**Regenerated all three PWA app-icon PNGs from a single SVG master (gold coin + gold-deep rim + indigo star cutout via `sharp`) and fixed the stale manifest `start_url`/colors plus layout viewport color to match the locked design tokens.**

## Performance

- **Duration:** ~25 min (18:44 base checkout to 19:08 final commit)
- **Started:** 2026-07-23T18:44:00+03:00 (base commit `afeffa4`)
- **Completed:** 2026-07-23T19:08:33+03:00
- **Tasks:** 3 (1 checkpoint gate + 2 auto)
- **Files modified:** 5

## Accomplishments
- Package Legitimacy Gate (Task 1) relayed to the human operator; approved before any install occurred
- Three branded, correctly-sized, fully-opaque icon PNGs rasterized from one 512px SVG master: 512x512, 192x192, and apple-touch corrected to 180x180 (was wrongly 192x192)
- `app/manifest.ts`: `start_url` fixed from the deleted `/dashboard` route to `/`; `theme_color`/`background_color` aligned to `base.indigo` (#6C5CE7) / `ink.bg` (#141021)
- `app/layout.tsx`: `viewport.themeColor` aligned to the same `#6C5CE7`, matching manifest
- `npm run build` verified exit 0 with `/manifest.webmanifest` emitted

## Task Commits

1. **Task 1: Verify `sharp` package legitimacy before the one-off install** — checkpoint gate, no file changes, no separate commit (operator approved "approved" via coordinator relay; see Decisions below)
2. **Task 2: Author the coin-mark SVG and rasterize the three icon PNGs** - `b99a803` (feat)
3. **Task 3: Update manifest.ts and layout.tsx to the locked start_url and brand colors** - `abdb43a` (fix)

**Plan metadata:** committed separately by this agent below (SUMMARY.md commit)

_Note: worktree mode — STATE.md/ROADMAP.md are NOT touched by this agent; the orchestrator updates them centrally after merge._

## Files Created/Modified
- `public/icon-512x512.png` - 512x512 master icon: gold coin (r=169, fill #E9A83C) + gold-deep rim (r=161.5, stroke #C6871E, width 15) + indigo 5-point star cutout (outer r=76, inner r=38, first point up) on #6C5CE7 background, flattened opaque RGB
- `public/icon-192x192.png` - Same mark downscaled to 192x192, RGB (opaque)
- `public/apple-touch-icon.png` - Same mark downscaled to 180x180 (dimension corrected from the prior wrong 192x192), RGB (opaque)
- `app/manifest.ts` - `start_url: '/dashboard' -> '/'`, `background_color: '#0f0f23' -> '#141021'`, `theme_color: '#6366f1' -> '#6C5CE7'`; `icons` array and name/short_name/description/display/orientation byte-unchanged
- `app/layout.tsx` - `viewport.themeColor: '#6366f1' -> '#6C5CE7'`; metadata/appleWebApp/font calls untouched

## Decisions Made
- **sharp approved for one-off use** by the human operator via the coordinator relay: verified as the official `lovell/sharp` package on npmjs.com (millions of weekly downloads, actively maintained, not a typosquat); installed with `npm i sharp --no-save` so it never entered `package.json`/`package-lock.json` (confirmed via `grep -c '"sharp"' package.json package-lock.json` → 0 in both); the rasterization script lived only as a temporary uncommitted file (`.scratch-gen-icons.mjs` at the worktree root) and was deleted immediately after the three PNGs were written.
- Star vertex coordinates were computed with the standard alternating outer/inner-radius trigonometric formula (not hand-typed), per the plan's explicit instruction to avoid eyeballed coordinates.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Copied a temporary `.env.local` into the worktree to unblock `npm run build` verification**
- **Found during:** Task 3 (`npm run build` verification step)
- **Issue:** The worktree checkout had no `.env.local`; `npm run build` failed during "Collecting page data" with `@supabase/ssr: Your project's URL and API key are required to create a Supabase client!` for API routes that construct a Supabase client at module scope (e.g. `/api/wallet/award`, `/api/wallet/withdraw/approve`, `/api/cron/daily`). This is a pre-existing worktree-environment gap unrelated to the manifest/layout edits — the same env values already exist in the main repo checkout's gitignored `.env.local`.
- **Fix:** Copied `/Users/andrei/Projects/kids-motivation/.env.local` into the worktree root (`.env.local` is gitignored — `.env*.local` pattern — so it was never staged or committed), reran `npm run build`, confirmed success (all routes compiled, `/manifest.webmanifest` present in output), then deleted the copied file after verification.
- **Files modified:** none (gitignored env file only, not part of the commit)
- **Verification:** `npm run build` completed with route list including `○ /manifest.webmanifest`; no errors
- **Committed in:** N/A — no tracked file changed by this fix

---

**Total deviations:** 1 auto-fixed (1 blocking — missing env var for build verification)
**Impact on plan:** No scope creep; the fix only unblocked local build verification and touched no tracked files.

## Issues Encountered
- Worktree HEAD was found on an unrelated, stale branch history (unrelated commits like "fix(settings): fold vacation periods back into the Calendar tab") instead of the expected base `afeffa4`. Per the mandatory branch-check protocol (branch was correctly in the `worktree-agent-*` namespace, not a protected ref, working tree clean), reset the branch to `afeffa4953e3867daa44555d4aacd920fe848eea` before starting any task work.
- First `npm i sharp --no-save` attempt hit a transient `ECONNRESET` against the npm registry; retried successfully.

## User Setup Required
None - no external service configuration required.

## Known Stubs
None - no stub patterns introduced (icon PNGs are static assets; manifest/layout edits are scalar literal changes only).

## Threat Flags
None - matches the plan's pre-declared threat register (T-05.11-SC mitigated via the operator legitimacy gate + `--no-save`; T-05.11-05/06 accepted as static-config/static-asset changes with no new security surface).

## Next Phase Readiness
- ROADMAP 5.11 Success Criterion 3 (app icon/splash/manifest carry FamilyCoins branding) is code-complete for this plan; final human visual pass (installed PWA/home-screen icon legibility, splash background color) remains part of phase-level verification, not this plan.
- **Out-of-scope cleanup candidate** (as requested by this plan's `<output>` section): `components/WalletDashboard.tsx` appears to be dead code — a repo-wide grep for `WalletDashboard` found no import/usage outside the file itself. Not touched here (outside this plan's file scope); flagging for a future legacy-cleanup plan to confirm and remove if genuinely unused.

---
*Phase: 05.11-legacy-cleanup*
*Completed: 2026-07-23*

## Self-Check: PASSED

All claimed files verified present on disk (`public/icon-512x512.png`, `public/icon-192x192.png`,
`public/apple-touch-icon.png`, `app/manifest.ts`, `app/layout.tsx`, this SUMMARY.md). Both task
commit hashes (`b99a803`, `abdb43a`) verified present in `git log --oneline --all`.
