---
phase: 09.4-dayform-styles
plan: 01
subsystem: ui
tags: [react, kid-ui, day-fill, i18n, css-animation, reduced-motion]

# Dependency graph
requires:
  - phase: 09.3-dayform-default
    provides: StickySummaryBar / QuickRow / InlinePanelRow stateless primitives, coin-flyup anchoring (D-16), reduced-motion double-guard idiom
provides:
  - components/kid/day-fill/BottomSheet.tsx — explicit-close-only scrim + slide-up sheet (D-18)
  - components/kid/day-fill/TileCard.tsx — 2-column grid tile with rest/done/loss states (D-17)
  - components/kid/day-fill/DotTrack.tsx — tappable Instagram-Stories dot progress track (D-12)
  - components/kid/day-fill/StepCoinHeader.tsx — slim sticky coin total for story-stepper (D-09)
  - reduced-motion-guarded .kid-bottom-sheet / .kid-tile / .kid-dot-fill CSS in app/globals.css
  - 17 new locale keys (kidFillForm.* tile-sheet + story-stepper copy, kidProfile.* fill-style picker copy) in ru.json + en.json
affects: [09.4-02-tile-sheet-shell, 09.4-03-story-stepper-shell, 09.4-04-profile-picker]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Stateless presentation primitive: every string arrives as a prop, no i18n hook call inside the component (mirrors StickySummaryBar/QuickRow/InlinePanelRow)"
    - "Fixed-overlay bottom sheet without react-dom portal: position:fixed scrim + stopPropagation inner panel (ProfileSheet idiom)"
    - "Reduced-motion double guard: CSS animation/transition confined to @media (prefers-reduced-motion: no-preference), plus a post-mount JS matchMedia check that withholds the animating className entirely"

key-files:
  created:
    - components/kid/day-fill/BottomSheet.tsx
    - components/kid/day-fill/TileCard.tsx
    - components/kid/day-fill/DotTrack.tsx
    - components/kid/day-fill/StepCoinHeader.tsx
  modified:
    - app/globals.css
    - public/locales/ru.json
    - public/locales/en.json

key-decisions:
  - "kid-bottom-sheet/kid-tile/kid-dot-fill CSS block rewritten as a single @media (prefers-reduced-motion: no-preference) wrapper, mirroring the existing Phase 09.3 coin-flyup-loss/kid-mood-pop double-guard idiom verbatim"
  - "BottomSheet exposes exactly three close call sites (scrim onClick, X button onClick, optional doneLabel button onClick) and nothing inside children can trigger onClose — D-18 hard rule enforced structurally, not just by convention"
  - "TileCard has no disabled prop by design — a locked day still needs the tile tappable so its read-only sheet can open (D-13)"
  - "StepCoinHeader deliberately omits ProgressRing/pct/ringLabel — it is StickySummaryBar minus the ring, not a fork with new visuals"

patterns-established:
  - "Precedence-ordered mutually-exclusive visual states (loss > done > rest) resolved via simple ternary chains reading only from the K token object — no new hex literals introduced"

requirements-completed: [DAYFORM-02, DAYFORM-03]

# Metrics
duration: 35min
completed: 2026-09-19
---

# Phase 09.4 Plan 01: Sheet/Tile/Dot Primitives + i18n Summary

**Four stateless kid-UI primitives (BottomSheet, TileCard, DotTrack, StepCoinHeader) plus their reduced-motion-guarded CSS and 17 new bilingual locale keys, ready for plans 02/03 to assemble into the tile-sheet and story-stepper day-fill shells.**

## Performance

- **Duration:** ~35 min
- **Completed:** 2026-09-19T18:30:38Z
- **Tasks:** 3/3 completed
- **Files modified:** 7 (4 created, 3 modified)

## Accomplishments
- Rewrote the unguarded `.kid-bottom-sheet` slide-up animation into a single `@media (prefers-reduced-motion: no-preference)` block, added `.kid-tile`/`.kid-tile:active` press transition and `.kid-dot-fill` width/background transition under the same guard (D-18/D-12)
- Built `BottomSheet.tsx` with a structurally-enforced explicit-close-only contract (D-18): only the scrim tap, X button, and optional `doneLabel` button call `onClose` — nothing inside `children` can close it
- Built `TileCard.tsx` with three mutually exclusive visual states (rest / done-mint / loss-danger) resolved in strict precedence order, reusing the exact coin-tag trailing idiom from `KidDayFillForm.tsx`'s `trailingFor` (D-17)
- Built `DotTrack.tsx`, a tappable dot-progress track where any dot (forward or backward) jumps directly via `onJump` (D-12) — no fourth "skipped" state
- Built `StepCoinHeader.tsx` as `StickySummaryBar` minus the completion ring, for the story-stepper shell's slim persistent coin total (D-09)
- Added all 17 new locale keys (12 `kidFillForm.*`, 2 `kidProfile.*` flat + 3 nested `kidProfile.fillStyleOption.*`) to both `ru.json` and `en.json` at matching positions, leaving `kidProfile.fillStyleActive` untouched for plan 04

## Task Commits

Each task was committed atomically:

1. **Task 1: Reduced-motion-guarded sheet/tile/dot CSS in app/globals.css** - `d175a8f` (feat)
2. **Task 2: BottomSheet, TileCard, DotTrack, StepCoinHeader components** - `8f80887` (feat)
3. **Task 3: i18n keys for both new fill styles and the ProfileSheet picker** - `87d3852` (docs)

_No plan-metadata commit yet — this is a worktree execution; the orchestrator commits STATE.md/ROADMAP.md centrally after merge. This SUMMARY.md itself is committed separately below per worktree protocol._

## Files Created/Modified
- `components/kid/day-fill/BottomSheet.tsx` - Scrim + slide-up sheet primitive, explicit-close-only (D-18)
- `components/kid/day-fill/TileCard.tsx` - 2-column grid tile, rest/done/loss states (D-17)
- `components/kid/day-fill/DotTrack.tsx` - Tappable dot-progress track (D-12)
- `components/kid/day-fill/StepCoinHeader.tsx` - Slim sticky coin total for story-stepper (D-09)
- `app/globals.css` - `.kid-bottom-sheet`/`.kid-tile`/`.kid-dot-fill` animations moved behind reduced-motion guard
- `public/locales/ru.json` - 17 new keys added to `kidFillForm`/`kidProfile`
- `public/locales/en.json` - Same 17 keys, matching structure/position

## Decisions Made
- Used the sketch's `.28s cubic-bezier(.2,.9,.3,1.1)` easing for `kid-slide-up` (CONTEXT.md's Claude's-Discretion note explicitly allows either the sketch or the original `.3s ease-out` timing)
- Kept two identical-shape comment edits in `globals.css`/`StepCoinHeader.tsx` phrased to avoid re-triggering their own acceptance-criteria greps (e.g. describing `.kid-dot-fill` in prose without repeating the literal class-name string twice) — purely a documentation-wording choice, no behavior change
- Copied `.env.local` from the main repo into this worktree (gitignored, never staged) solely to run `npm run build` locally for verification — the worktree has no env file by default and the build fails on missing Supabase credentials without it

## Deviations from Plan

None - plan executed exactly as written. All three tasks match their `<action>` blocks and every acceptance-criteria grep/build check passed as specified.

## Issues Encountered
- First `npm run build` failed with `@supabase/ssr: Your project's URL and API key are required` because this git worktree has no `.env.local` (correctly gitignored, not copied into worktrees by default). Resolved by copying the main repo's `.env.local` into the worktree for local verification only — never staged or committed, confirmed via `git status --short` showing no such file.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- All four primitives exist with exactly the interfaces this plan's `<interfaces>` block specifies, are stateless, and read colour only from `K` — plans 02 (tile-sheet shell) and 03 (story-stepper shell) can now assemble them without inventing new visuals
- All 17 new locale strings exist in both languages; `kidProfile.fillStyleActive` still renders correctly until plan 04 replaces `ProfileSheet`'s single button with the 3-way picker
- `npx tsc --noEmit`, `npm run lint` (0 new warnings), and `npm run build` are all green
- No blockers for wave 2 (plans 02/03, which depend on this plan per the phase's dependency graph)

---
*Phase: 09.4-dayform-styles*
*Completed: 2026-09-19*
