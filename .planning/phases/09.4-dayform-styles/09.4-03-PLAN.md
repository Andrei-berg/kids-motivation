---
phase: 09.4-dayform-styles
plan: 03
type: execute
wave: 3
depends_on: [09.4-01, 09.4-02]
files_modified:
  - components/kid/day-fill/StoryStepper.tsx
  - components/kid/KidDayFillForm.tsx
autonomous: true
requirements: [DAYFORM-03]

must_haves:
  truths:
    - "With fill_style story-stepper exactly one category fills the screen at a time behind a dot-progress track"
    - "All extra activities share one Activities step and all custom day-blocks share one Custom step"
    - "Completing a single-answer step auto-advances to the next applicable not-yet-done step after a short confirmation beat"
    - "An explicit skip control advances without writing anything and leaves the dot in its not-done state"
    - "Tapping any dot jumps to that step so an answer can be changed"
    - "A slim coin total stays visible with the dot track throughout the flow"
    - "After the last applicable step a summary screen carries the same Save button — nothing auto-submits"
    - "A locked past day renders the dot track with read-only steps and no skip/advance controls"
  artifacts:
    - path: "components/kid/day-fill/StoryStepper.tsx"
      provides: "dot-track stepper, auto-advance, skip/next, summary screen"
      exports: ["default", "FillStep"]
      min_lines: 60
    - path: "components/kid/KidDayFillForm.tsx"
      provides: "story-stepper render branch, step descriptors, extracted save/error footer"
      contains: "story-stepper"
  key_links:
    - from: "components/kid/KidDayFillForm.tsx applicable map"
      to: "StoryStepper steps array"
      via: "only applicable categories become steps (D-10)"
      pattern: "applicable\\["
    - from: "components/kid/day-fill/StoryStepper.tsx"
      to: "components/kid/day-fill/DotTrack.tsx"
      via: "tappable dots jump to a step (D-12)"
      pattern: "DotTrack"
    - from: "components/kid/day-fill/StoryStepper.tsx summary screen"
      to: "KidDayFillForm pinned save block"
      via: "footer prop carrying the unchanged save + error JSX (D-15)"
      pattern: "footer"
---

<objective>
Ship the `story-stepper` fill style: one category per full screen behind a tappable dot-progress
track, auto-advancing on completion, with an explicit skip escape hatch and a final summary screen
that carries the same Save button every other style uses.

Purpose: DAYFORM-03 and CONTEXT decisions D-04, D-05, D-09, D-10, D-11, D-12, D-13, D-15, D-16.
Output: a `StoryStepper` container plus a second render branch in `KidDayFillForm` — again zero
changes to state, save, or award logic (D-13 reuse principle).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.4-dayform-styles/09.4-CONTEXT.md
@.planning/phases/09.4-dayform-styles/09.4-UI-SPEC.md
@.planning/phases/09.4-dayform-styles/09.4-PATTERNS.md
@.planning/phases/09.4-dayform-styles/09.4-01-SUMMARY.md
@.planning/phases/09.4-dayform-styles/09.4-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Built in plan 01 — consume as-is. -->

components/kid/day-fill/DotTrack.tsx:
```typescript
interface DotTrackProps {
  dots: Array<{ id: string; label: string; done: boolean }>
  currentIdx: number
  onJump: (idx: number) => void
  ariaLabelFor: (idx: number, label: string) => string
}
```

components/kid/day-fill/StepCoinHeader.tsx:
```typescript
interface StepCoinHeaderProps { coins: number; caption: string; lockedLabel?: string | null }
```

NEW in this plan — components/kid/day-fill/StoryStepper.tsx:
```typescript
export interface FillStep {
  id: string
  label: string
  icon: string
  status: string        // reused tileDone/tileNotFilled copy, shown as the step-card description
  done: boolean
  autoAdvance: boolean  // true only when the step presents a single answer control
  body: React.ReactNode
}
interface StoryStepperProps {
  steps: FillStep[]
  locked: boolean
  subtitle: string
  labels: {
    skip: string        // t('kidFillForm.stepSkip')
    next: string        // t('kidFillForm.stepNext')
    doneToast: string   // t('kidFillForm.stepDoneToast')
    summaryTitle: string
    summaryBody: string
    dotLabel: (idx: number, label: string) => string
  }
  footer: React.ReactNode   // the unchanged saveError banner + pinned Save block (D-15)
}
export default function StoryStepper(props: StoryStepperProps): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StoryStepper container — dot track, auto-advance, skip, summary screen</name>
  <files>components/kid/day-fill/StoryStepper.tsx</files>
  <read_first>
    - components/kid/day-fill/DotTrack.tsx and components/kid/day-fill/StepCoinHeader.tsx (plan 01)
    - .planning/phases/09.4-dayform-styles/09.4-UI-SPEC.md section "Interaction Contract —
      Story-Stepper style" (layout order, dot states, auto-advance behaviour, summary screen)
    - .planning/phases/09.4-dayform-styles/09.4-PATTERNS.md section on the sketch `#variant-b`
      (`renderStep`/`renderDots`/`stepMove`/`stepAdvanceAuto`, the validated 450ms toast-then-advance
      beat, and the `.step-card` CSS: min-height 340, border-radius 24, centred icon/title/desc)
    - components/kid/KidDayFillForm.tsx lines 158-172 (the `reduced` state idiom) — reuse the same
      matchMedia guard here for anything animated
    - .planning/phases/09.4-dayform-styles/09.4-CONTEXT.md decisions D-10, D-11, D-12, D-15
  </read_first>
  <action>
    Create `components/kid/day-fill/StoryStepper.tsx` as a `'use client'` module-level default export
    with the exact `FillStep` / `StoryStepperProps` shapes in this plan's `<interfaces>` block (export
    `FillStep` — KidDayFillForm imports it as a type).

    State: `const [stepIdx, setStepIdx] = useState(0)` and `const [toast, setToast] = useState(false)`.
    Index `steps.length` is the summary screen, so valid `stepIdx` values run 0..steps.length. Guard
    the empty case: when `steps.length === 0`, render the summary screen only.

    Layout, top to bottom: `<DotTrack dots={steps.map(s => ({ id: s.id, label: s.label, done: s.done }))}
    currentIdx={stepIdx} onJump={setStepIdx} ariaLabelFor={labels.dotLabel} />` (D-12 — tapping any dot
    jumps, both directions); the `subtitle` line rendered only while `stepIdx === 0`; then either the
    step card or the summary screen; then, when not on the summary screen and `locked` is false, the
    nav row.

    Step card: a centred card with `margin: '20px 16px'`, `borderRadius: 24`, `minHeight: 340`,
    `padding: '28px 22px'`, `background: K.card`, `border: 1.5px solid ${K.line}`, flex column,
    `alignItems: 'center'`, `justifyContent: 'center'`, `textAlign: 'center'`. Inside: the step icon at
    fontSize 52; the label in Display type (K.fDisp, 20, 900, K.ink, margin '14px 0 4px'); the
    `status` string in Label type (K.fBody, 13.5→13, 700, K.ink3, margin '0 0 20px') as the step
    description; then `<div data-fill-row style={{ width: '100%', textAlign: 'left' }}>{step.body}</div>`
    — the `data-fill-row` wrapper is D-16's only wiring requirement, letting KidDayFillForm's existing
    `onPointerDownCapture` anchor the coin flyup inside the step card.

    Nav row: one full-width secondary button (minHeight 48, borderRadius 16, border `1.5px solid
    ${K.line}`, background K.card, color K.ink2, K.fDisp 15/800) whose label is
    `steps[stepIdx].done ? labels.next : labels.skip` and whose onClick advances by calling the same
    `advance()` helper. Per D-11 this control writes nothing, marks nothing, and leaves the step's dot
    in its normal state — it only changes `stepIdx`.

    `advance()` sets `stepIdx` to the first index greater than the current one whose `done` is false,
    or to `steps.length` (the summary) when no such index exists. It never skips an applicable step
    that simply has no input yet in a way that hides it — a blank step stays reachable through its dot
    (D-10/D-12).

    Auto-advance (D-10): keep a `useRef<Record<string, boolean>>` of the previous `done` value per step
    id, refreshed on every `steps` change. In a `useEffect` keyed on `steps`, when `locked` is false,
    `stepIdx < steps.length`, the current step's `autoAdvance` is true, its previous recorded `done`
    was false, and its current `done` is true — set `toast` true and start a 450ms `setTimeout` that
    calls `advance()` and clears the toast. Store the timer in a ref and clear it in the effect cleanup
    and on unmount so a fast dot-tap never advances after the child has already navigated away.
    Auto-advance must never fire on mount (the previous-done ref is seeded from the first `steps` value
    it sees, so a step that is already done when the stepper mounts produces no false→true transition).

    Toast: a small centred pill (borderRadius 999, background K.mintSoft, border `1.5px solid ${K.mint}`,
    K.fBody 13/700, color K.mintDeep) showing `labels.doneToast`, rendered above the step card while
    `toast` is true. It is plain conditional content with no CSS animation, so no reduced-motion guard
    is needed; if any animated affordance is added, apply the `reduced` matchMedia idiom from
    KidDayFillForm lines 167-172.

    Summary screen (D-15): heading `labels.summaryTitle` (K.fDisp, 22, 900, K.ink), body
    `labels.summaryBody` (K.fBody, 13, 700, K.ink3), then a compact recap list — one row per step with
    its icon, label, and a ✓ in K.mintDeep when `done` (otherwise a `—` in K.ink3) — each row tappable
    to `setStepIdx(idx)` so the child can go back and change an answer, then `{footer}`. The summary
    screen never submits anything itself: the Save action lives entirely inside the `footer` node the
    caller passes, so no style ever auto-submits.

    This component owns no coin maths, performs no fetch, and imports nothing from `lib/repositories`
    or `lib/supabase`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "supabase\|repositories\|fetch(" components/kid/day-fill/StoryStepper.tsx` returns 0.
    - `grep -c "data-fill-row" components/kid/day-fill/StoryStepper.tsx` returns 1.
    - `grep -c "clearTimeout" components/kid/day-fill/StoryStepper.tsx` is >= 1 (timer cleanup present).
    - `grep -c "450" components/kid/day-fill/StoryStepper.tsx` returns 1 (the validated confirmation beat).
    - `grep -c "labels.next\|labels.skip" components/kid/day-fill/StoryStepper.tsx` shows the nav label
      chosen by `steps[stepIdx].done`, and neither branch calls any prop other than the local advance.
    - `grep -c "locked" components/kid/day-fill/StoryStepper.tsx` is >= 2 (nav row hidden and
      auto-advance disabled when locked).
    - `npx tsc --noEmit` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>A stateless-over-caller-state stepper drives one step at a time with auto-advance, skip, dot jumps, and a summary screen.</done>
</task>

<task type="auto">
  <name>Task 2: KidDayFillForm — extract the save/error footer and build the step descriptors</name>
  <files>components/kid/KidDayFillForm.tsx</files>
  <read_first>
    - components/kid/KidDayFillForm.tsx as it stands after plan 02: the body consts, `moodBody`,
      `buildTiles`, `applicable` (501-520), `done`/`doneAll` (1234-1252), `sectionCoins` (408-470),
      `isLocked`, and the current return (the `saveError` banner and the pinned save block that must
      become reusable nodes)
    - .planning/phases/09.4-dayform-styles/09.4-CONTEXT.md decisions D-04, D-05, D-09, D-10, D-15
    - .planning/phases/09.3-dayform-default/09.3-CONTEXT.md decision D-09 (the applicable-map rule
      this phase reuses unchanged)
  </read_first>
  <action>
    Two additive edits; state, handlers, coin maths, and the award call stay untouched.

    (a) Extract the footer. Convert the existing `saveError && (...)` banner JSX into
    `const saveErrorBlock = saveError ? (...) : null` and the existing pinned save block
    (`<div style={{ position: 'sticky', bottom: 76, zIndex: 20, marginTop: 18, padding: '10px 16px 0' }}>`
    with its `isLocked` branch, the spinner branch, and the `trustNote` line) into
    `const saveBlock = (...)`, both declared just above the return. The JSX must be moved
    byte-for-byte. Render `{saveErrorBlock}{saveBlock}` where they are today for the sticky-summary and
    tile-sheet branches, so those two styles are unchanged; the story-stepper branch will instead hand
    `<>{saveErrorBlock}{saveBlock}</>` to StoryStepper's `footer` prop (D-15/D-14 parity — one Save
    component, three styles).

    (b) Add a plain function `buildSteps(): FillStep[]` at component scope next to `buildTiles` (same
    "plain function, not nested component" rule). Its grain is deliberately COARSER than `buildTiles`
    (D-04), and it emits a step only for an applicable category:

    1. behavior — when `behaviorTags.length > 0`: body `behaviorBody`, done `done.behavior`,
       icon '🌟', autoAdvance false (multi-select).
    2. room — when `applicable.room`: body `roomBody`, done `done.room`, icon from the visible block's
       `icon ?? '🏠'` under flag-on, else '🏠'; autoAdvance `roomTasks.length === 1`.
    3. Activities — ONE grouped step when `applicable.activity` (D-04: a checklist of all activities
       together, not one step per activity): body `extraActivitiesBody`, done `done.activity`,
       icon '⭐', autoAdvance `activities.length === 1`.
    4. grades — when `applicable.grade`: body `gradesBody`, done `done.grade`, icon '📚',
       autoAdvance `subjects.length === 1`.
    5. exercises — when `applicable.exercise`: body `exercisesBody`, done `done.exercise`, icon '🤸',
       autoAdvance `exerciseTypes.length === 1`.
    6. sport — when `applicable.sport`: body `sectionsBody`, done `done.sport`, icon '🏆',
       autoAdvance `sections.length === 1`.
    7. book — when `applicable.book`: body `readingBody`, done `done.book`, icon '📖',
       autoAdvance false (a multi-field form).
    8. Custom — ONE grouped step (D-04) when `dayBlocksEnabled` and at least one visible block has no
       `legacy_key` and its `applicable[custom:${id}]` is true: body is a flex column of
       `renderCustomBlock(block)` for each such block, done is true when every one of them is done,
       icon '⭐', autoAdvance false unless exactly one custom block is in the group.
    9. mood — last, always applicable: body `moodBody`, done `done.mood`, icon '✨', autoAdvance true
       (a single 5-option pick).

    Labels come from the same sources `buildTiles` uses (`block.name` under flag-on, the
    `kidFillForm.*Section` keys under flag-off; the Activities and Custom grouped steps use
    `t('kidFillForm.extraSection')` and, for Custom, the first custom block's name is NOT used — use
    `t('kidFillForm.extraSection')`'s sibling only if one exists, otherwise fall back to the block
    group's generic label already used by sticky-summary). `status` is
    `done ? t('kidFillForm.tileDone') : t('kidFillForm.tileNotFilled')`, the same two keys the tiles
    use — no new per-category prompt copy is introduced this phase.

    The `autoAdvance` rule above is this plan's binding interpretation of D-10/DAYFORM-03, taken under
    UI-SPEC's explicit discretion over the grouped steps' "addressed" definition: a step auto-advances
    only when its control is a single answer, because auto-advancing after the first of five room
    tasks would strand the other four. Every multi-item step advances through the nav control instead,
    which reads "Дальше →" once something is entered and "Пропустить →" while blank. Record this
    interpretation in the plan SUMMARY — the phase-close traceability matrix cites it for DAYFORM-03.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const saveBlock" components/kid/KidDayFillForm.tsx` returns 1 and
      `grep -c "const saveErrorBlock" components/kid/KidDayFillForm.tsx` returns 1.
    - `grep -c "position: 'sticky', bottom: 76" components/kid/KidDayFillForm.tsx` returns 1 (one save
      bar definition shared by all three styles).
    - `grep -c "function buildSteps" components/kid/KidDayFillForm.tsx` returns 1, declared at
      component scope.
    - `grep -c "autoAdvance" components/kid/KidDayFillForm.tsx` is >= 8 (one per emitted step).
    - `git diff components/kid/KidDayFillForm.tsx` shows no change inside `sectionCoins`, `applicable`,
      `handleSubmit`, `creditAwardAndFinish`, or `handleRetryAward`, and
      `grep -c "api/wallet/award" components/kid/KidDayFillForm.tsx` still returns 1.
    - `npx tsc --noEmit`, `npm run lint`, `npm test` all exit 0.
  </acceptance_criteria>
  <done>Step descriptors exist at D-04 grain with an explicit auto-advance rule, and the Save bar is a single reusable node.</done>
</task>

<task type="auto">
  <name>Task 3: KidDayFillForm — story-stepper render branch with the slim coin header</name>
  <files>components/kid/KidDayFillForm.tsx</files>
  <read_first>
    - components/kid/KidDayFillForm.tsx return block as it stands after task 2
    - components/kid/day-fill/StepCoinHeader.tsx and components/kid/day-fill/StoryStepper.tsx
    - .planning/phases/09.4-dayform-styles/09.4-CONTEXT.md decisions D-09, D-13, D-15, D-16
    - .planning/phases/09.4-dayform-styles/09.4-UI-SPEC.md section "Locked day (D-13)" for story-stepper
  </read_first>
  <action>
    Extend the render branch created in plan 02 into a three-way branch, still keeping the
    `data-fill-style` wrapper, its `onPointerDownCapture` handler, and `<CoinFlyup flyups={flyups} />`
    outside it (D-16 requires one shared anchoring mechanism for all three styles).

    Header: render `<StickySummaryBar ... />` only when `style !== 'story-stepper'`, and render
    `<StepCoinHeader coins={coinsPreview} caption={t('kidFillForm.stickyEarnedToday')}
    lockedLabel={isLocked ? t('kidFillForm.locked') : null} />` when `style === 'story-stepper'` (D-09:
    a slim persistent coin total instead of the ring, because the dot track already conveys completion).
    Reuse the existing `kidFillForm.stickyEarnedToday` and `kidFillForm.locked` keys — no new copy.

    Body: when `style === 'story-stepper'` render `<StoryStepper steps={buildSteps()} locked={isLocked}
    subtitle={t('kidFillForm.stepperSub')} labels={{ skip: t('kidFillForm.stepSkip'),
    next: t('kidFillForm.stepNext'), doneToast: t('kidFillForm.stepDoneToast'),
    summaryTitle: t('kidFillForm.stepSummaryTitle'), summaryBody: t('kidFillForm.stepSummaryBody'),
    dotLabel: (n, label) => t('kidFillForm.stepDotLabel', { n: n + 1, label }) }}
    footer={<>{saveErrorBlock}{saveBlock}</>} />` and render NOTHING else below it — the save bar and
    error banner must not also appear outside the stepper in this branch (D-15: the Save tap lives on
    the summary screen). The other two branches keep rendering `{saveErrorBlock}{saveBlock}` inline as
    before.

    When `nothingToday` is true the stepper still receives whatever `buildSteps()` returns (behavior
    and mood remain applicable); render the existing `nothingToday` card above the stepper rather than
    replacing it, matching how sticky-summary keeps the mood group visible in that case.

    D-13 (locked past day): pass `locked={isLocked}` so StoryStepper hides the skip/next control and
    disables auto-advance; the reused bodies already disable their own controls via `isLocked`, and
    the `footer`'s existing `isLocked` branch already renders the "day locked" message instead of the
    Save button — so a locked stepper shows the dot track, read-only steps, and the locked notice, with
    no fallback to sticky-summary.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<StoryStepper" components/kid/KidDayFillForm.tsx` returns 1 and
      `grep -c "<StepCoinHeader" components/kid/KidDayFillForm.tsx` returns 1.
    - `grep -c "style === 'story-stepper'" components/kid/KidDayFillForm.tsx` is >= 2 (header gate and
      body branch).
    - `grep -c "<StickySummaryBar" components/kid/KidDayFillForm.tsx` returns 1, guarded by
      `style !== 'story-stepper'`.
    - `grep -c "footer={" components/kid/KidDayFillForm.tsx` returns 1 and passes both
      `saveErrorBlock` and `saveBlock`.
    - Reading the return block confirms `saveBlock` is NOT additionally rendered outside StoryStepper
      in the story-stepper branch (no duplicate Save button on screen).
    - `grep -c "onPointerDownCapture" components/kid/KidDayFillForm.tsx` returns 1, still on the
      outermost `data-fill-style` div.
    - `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build` all exit 0.
  </acceptance_criteria>
  <done>Selecting story-stepper renders the slim coin header, dot track, one step at a time, and a summary screen carrying the shared Save button.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child browser → KidDayFillForm state | The stepper shell re-renders the same reused bodies; nothing new is written from the client |
| kid client → `/api/wallet/award` | Unchanged crediting boundary; the summary screen's Save is the same `handleSubmit` path |
| locked past day → step controls | `locked` hides advance controls; `isLocked` disables the bodies; the award route remains the backstop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-094-09 | Tampering | story-stepper render branch | mitigate | Render-only: acceptance criteria assert zero diff inside `sectionCoins`/`handleSubmit`/`creditAwardAndFinish` and exactly one `api/wallet/award` call site; `StoryStepper` imports no supabase/repository/fetch (asserted by grep). |
| T-094-10 | Tampering | skip control (D-11) | mitigate | The skip/next control's only effect is `setStepIdx`; it calls no save/propose/toggle handler, so skipping cannot mark a category done or credit coins. Asserted by reading the nav-row onClick in acceptance criteria. |
| T-094-11 | Denial of Service | auto-advance timer | mitigate | The 450ms timer is stored in a ref and cleared on effect re-run and unmount, so rapid dot navigation cannot queue advances or fire after unmount (the channel/timer-leak failure mode already recorded for Supabase Realtime cleanup in STATE.md). |
| T-094-12 | Elevation of Privilege | locked-day stepper | mitigate | `locked` suppresses auto-advance and the nav control while the reused bodies keep their `isLocked` disabling; the award route's date/backfill gating remains authoritative regardless of client rendering. |
| T-094-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
1. `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build` all exit 0.
2. `grep -c "api/wallet/award" components/kid/KidDayFillForm.tsx` returns 1.
3. `grep -rn "supabase\|repositories\|fetch(" components/kid/day-fill/StoryStepper.tsx` returns nothing.
4. `grep -c "position: 'sticky', bottom: 76" components/kid/KidDayFillForm.tsx` returns 1 (single Save
   bar definition shared by all three styles).
</verification>

<success_criteria>
- `fill_style === 'story-stepper'` renders one applicable category per screen behind a tappable dot
  track, with a slim persistent coin total.
- Extra activities collapse into one Activities step and custom blocks into one Custom step (D-04).
- Single-answer steps auto-advance after a ~450ms confirmation beat; multi-item steps advance via the
  nav control; skipping writes nothing and leaves the dot not-done.
- After the last applicable step a summary screen shows a recap and the same Save button — no style
  auto-submits.
- A locked day renders the dot track read-only with no advance controls.
- All four gates green; sticky-summary and tile-sheet unchanged.
</success_criteria>

<output>
Create `.planning/phases/09.4-dayform-styles/09.4-03-SUMMARY.md` when done.
</output>
