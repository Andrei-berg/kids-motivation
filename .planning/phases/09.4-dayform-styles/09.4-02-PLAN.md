---
phase: 09.4-dayform-styles
plan: 02
type: execute
wave: 2
depends_on: [09.4-01]
files_modified:
  - components/kid/day-fill/TileGrid.tsx
  - components/kid/KidDayFillForm.tsx
autonomous: true
requirements: [DAYFORM-02]

must_haves:
  truths:
    - "With fill_style tile-sheet the day renders as a strict 2-column grid of per-category status tiles"
    - "Every extra activity, every custom day-block, and mood each get their own tile"
    - "Tapping a tile opens a bottom sheet containing only that category's existing body JSX"
    - "Closing the sheet updates the originating tile in place with no page reflow and no grid remount"
    - "A category that nets negative coins today renders its tile with the danger tint, never mint"
    - "The sticky ring + coin total and the pinned Save button are the same components sticky-summary uses"
    - "A locked past day still renders the tile grid; its sheets open read-only with no save-and-close button"
    - "A coin flyup anchors inside the open sheet, not at the header"
  artifacts:
    - path: "components/kid/day-fill/TileGrid.tsx"
      provides: "2-column tile grid + single shared bottom sheet"
      exports: ["default", "FillTile"]
      min_lines: 40
    - path: "components/kid/KidDayFillForm.tsx"
      provides: "tile-sheet render branch, moodBody extraction, tile descriptor assembly"
      contains: "tile-sheet"
  key_links:
    - from: "components/kid/KidDayFillForm.tsx"
      to: "components/kid/day-fill/TileGrid.tsx"
      via: "tiles descriptor array built from the existing bodies"
      pattern: "<TileGrid"
    - from: "components/kid/day-fill/TileGrid.tsx"
      to: "components/kid/day-fill/BottomSheet.tsx"
      via: "single sheet whose content is the open tile's body"
      pattern: "BottomSheet"
    - from: "components/kid/KidDayFillForm.tsx sectionCoins"
      to: "TileCard loss/coins props"
      via: "existing per-category coin map, no new coin maths"
      pattern: "sectionCoins\\["
---

<objective>
Ship the `tile-sheet` fill style: a strict 2-column grid of per-category status tiles over the exact
state/validation/save/award logic already in `KidDayFillForm`, with each tile opening a bottom sheet
scoped to that one category's existing body JSX.

Purpose: DAYFORM-02 and CONTEXT decisions D-01, D-02, D-03, D-08, D-13, D-14, D-16, D-17, D-18.
Output: a new `TileGrid` container plus a render branch in `KidDayFillForm` — zero changes to
`sectionCoins`, `handleSubmit`, `creditAwardAndFinish`, or the `/api/wallet/award` call (D-13 reuse
principle carried over from Phase 9.3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.4-dayform-styles/09.4-CONTEXT.md
@.planning/phases/09.4-dayform-styles/09.4-UI-SPEC.md
@.planning/phases/09.4-dayform-styles/09.4-PATTERNS.md
@.planning/phases/09.4-dayform-styles/09.4-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Built in plan 01 — consume as-is, do not re-declare or fork. -->

components/kid/day-fill/TileCard.tsx:
```typescript
interface TileCardProps {
  label: string; icon: string; done: boolean; loss?: boolean
  status: string; coins?: number; onTap: () => void
}
```

components/kid/day-fill/BottomSheet.tsx:
```typescript
interface BottomSheetProps {
  open: boolean; title: string; icon?: string
  onClose: () => void; closeLabel: string; doneLabel?: string | null
  children: React.ReactNode
}
```

components/kid/day-fill/StickySummaryBar.tsx (Phase 9.3, reuse verbatim — D-08):
```typescript
interface StickySummaryBarProps {
  pct: number; coins: number; caption: string; ringLabel: string; lockedLabel?: string | null
}
```

NEW in this plan — components/kid/day-fill/TileGrid.tsx:
```typescript
export interface FillTile {
  id: string
  label: string
  icon: string
  done: boolean
  loss: boolean
  status: string
  coins?: number
  body: React.ReactNode
}
interface TileGridProps {
  tiles: FillTile[]
  openId: string | null
  onOpen: (id: string) => void
  onClose: () => void
  subtitle: string             // t('kidFillForm.tileSheetSub')
  closeLabel: string           // t('kidFillForm.sheetClose')
  doneLabel: string | null     // t('kidFillForm.sheetDone'); null on a locked day (D-13)
  emptyState?: React.ReactNode // the existing nothingToday card, when applicable
}
export default function TileGrid(props: TileGridProps): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TileGrid container component</name>
  <files>components/kid/day-fill/TileGrid.tsx</files>
  <read_first>
    - components/kid/day-fill/TileCard.tsx and components/kid/day-fill/BottomSheet.tsx (built in plan
      01 — the exact props to pass)
    - components/kid/day-fill/InlinePanelRow.tsx (the doc comment's "stable position + stable key so
      controlled inputs never remount" rule that governs how `body` elements must be rendered)
    - .planning/phases/09.4-dayform-styles/09.4-UI-SPEC.md section "Interaction Contract — Tile-Sheet
      style" (layout order, three tile states, sheet behaviour, no-page-reflow requirement)
    - .planning/phases/09.4-dayform-styles/09.4-PATTERNS.md section on the sketch `#variant-a`
      (`tile-grid` + single `#sheet` whose content is swapped by `openSheet(cat)` — one sheet
      instance, not one per category)
  </read_first>
  <action>
    Create `components/kid/day-fill/TileGrid.tsx` as a `'use client'` module-level default export with
    the exact `FillTile` / `TileGridProps` shapes in this plan's `<interfaces>` block (export the
    `FillTile` interface — KidDayFillForm imports it as a type).

    Render, top to bottom: the `subtitle` line (K.fBody, 13, 700, K.ink3, padding '10px 16px 0'); the
    `emptyState` node when provided; then a grid container with `display: 'grid'`,
    `gridTemplateColumns: '1fr 1fr'` (strict two columns at every viewport — DAYFORM-02 wording, no
    responsive reflow), `gap: 8` (sm token), `padding: 16` (md token), mapping `tiles` to
    `<TileCard key={tile.id} ... onTap={() => onOpen(tile.id)} />` passing label/icon/done/loss/status/
    coins straight through.

    Below the grid render exactly ONE `<BottomSheet>` — never one per tile. Resolve
    `const openTile = tiles.find(t => t.id === openId) ?? null` and pass `open={openTile !== null}`,
    `title={openTile?.label ?? ''}`, `icon={openTile?.icon}`, `onClose={onClose}`, `closeLabel`,
    `doneLabel`, and `{openTile?.body}` as children. Wrap the children in a single
    `<div data-fill-row>` so the coin flyup anchors at the sheet body rather than the page header —
    this is D-16's only wiring requirement, since `KidDayFillForm`'s existing
    `onPointerDownCapture` handler walks `closest('[data-fill-row]')` and React propagates events from
    this subtree normally (no portal is involved; plan 01 built BottomSheet as a plain fixed-position
    overlay, matching the ProfileSheet/AvatarPicker idiom).

    Do not introduce any state in this component beyond nothing at all: `openId` is owned by
    `KidDayFillForm` so that reopening a sheet never resets anything and so the grid never remounts.
    Do not sort, filter, or re-order `tiles` — the caller's order is authoritative, and re-sorting on
    done-state would violate UI-SPEC's "the grid never remounts or re-sorts on sheet close".
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useState\|useEffect\|useMemo" components/kid/day-fill/TileGrid.tsx` returns 0.
    - `grep -c "<BottomSheet" components/kid/day-fill/TileGrid.tsx` returns 1.
    - `grep -c "gridTemplateColumns: '1fr 1fr'" components/kid/day-fill/TileGrid.tsx` returns 1.
    - `grep -c "data-fill-row" components/kid/day-fill/TileGrid.tsx` returns 1.
    - `grep -c "\.sort(\|\.filter(" components/kid/day-fill/TileGrid.tsx` returns 0 except for the
      single `tiles.find(...)` lookup (verify by reading, `find` is permitted).
    - `npx tsc --noEmit` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>A stateless TileGrid renders a 2-column tile grid plus one shared bottom sheet.</done>
</task>

<task type="auto">
  <name>Task 2: KidDayFillForm — extract moodBody and assemble the tile descriptors</name>
  <files>components/kid/KidDayFillForm.tsx</files>
  <read_first>
    - components/kid/KidDayFillForm.tsx in full, with particular attention to: the body consts
      (`roomBody` 942-953, `extraActivitiesBody` 955-962, `gradesBody` 964-1050, `exercisesBody`
      1052-1095, `sectionsBody` 1097-1159, `readingBody` 1161-1170, `behaviorBody` 1181-1231); the
      inline mood picker JSX inside `renderGroup('mood', ...)` at 1429-1455; `sectionCoins` 408-470;
      `applicable` 501-520; `done`/`doneAll` 1234-1252; `trailingFor` 1256-1266;
      `renderBuiltinBlock` 1318-1342 and `renderCustomBlock` 1345-1356; `isLocked` 244-247
    - components/kid/day-blocks/ActivitiesBlock.tsx (it already renders one `data-fill-row` QuickRow
      per activity — passing a single-element array is the per-activity tile body, no new body needed)
    - components/kid/day-blocks/CustomBlock.tsx (same, one row per block)
    - .planning/phases/09.4-dayform-styles/09.4-CONTEXT.md decisions D-01, D-02, D-03, D-17
  </read_first>
  <action>
    Two edits, both additive — do not touch state, handlers, `sectionCoins`, `coinsPreview`,
    `applicable`, `handleSubmit`, `creditAwardAndFinish`, or the award fetch.

    (a) Extract the mood picker. Move the JSX currently passed inline as `renderGroup`'s `body`
    argument for the mood group (lines 1429-1455: the flex row mapping `MOOD_OPTIONS` to the 5 emoji
    buttons with the `kid-mood-pop` class and `setMoodPop` handler) into a new `const moodBody = (...)`
    declared next to the other body consts, after `behaviorBody`. Change the sticky-summary render to
    `renderGroup('mood', t('kidFillForm.moodSection'), '✨', done.mood, moodBody)`. The JSX itself
    must be moved byte-for-byte — same keys, same `kid-mood-pop` conditional, same `isLocked` guards —
    so sticky-summary behaviour is unchanged and all three styles share one mood picker (D-02/D-05).

    (b) Add `const [openTileId, setOpenTileId] = useState<string | null>(null)` near `openPanels`
    (line 526), and a plain function `buildTiles(): FillTile[]` defined after `renderCustomBlock`
    (NOT a nested component, NOT a useMemo returning JSX — it closes over the body consts and is
    called once during render, exactly like `renderBuiltinBlock`). It returns descriptors in this
    order:

    1. behavior — only when `behaviorTags.length > 0`. id 'behavior', icon '🌟', label
       `t('kidFillForm.behaviorSection')`, body `behaviorBody`, done `done.behavior`.
    2. When `dayBlocksEnabled` is true: iterate `visibleBlocks` in order (skip everything when
       `nothingToday`). For a block with a `legacy_key`, mirror `renderBuiltinBlock`'s gating exactly:
       'room' → one tile (id 'room', label `block.name`, icon `block.icon ?? '🏠'`, body `roomBody`,
       done `done.room`); 'activity' → one tile PER activity in `activities` (D-01), id
       `activity:${act.id}`, label `act.name`, icon `act.emoji ?? '⭐'`, done
       `checkedActivities.has(act.id)`, body `<ActivitiesBlock activities={[act]} checked={checkedActivities}
       onToggle={toggleActivity} isLocked={isLocked} />`; 'grade' → one tile only when
       `dayType === 'school' && subjects.length > 0`, body `gradesBody`, icon `block.icon ?? '📚'`;
       'exercise' → one tile only when `exerciseTypes.length > 0`, body `exercisesBody`, icon
       `block.icon ?? '🤸'`; 'sport' → one tile, body `sectionsBody`, icon `block.icon ?? '🏆'`;
       'book' → one tile, body `readingBody`, icon `block.icon ?? '📖'`; 'behavior' and the default
       case → nothing (already emitted in step 1). For a block WITHOUT a `legacy_key`, emit one tile
       per custom block (D-01), id `custom:${block.id}`, label `block.name`, icon `block.icon ?? '⭐'`,
       done `!!customBlockDone[block.id]`, body `renderCustomBlock(block)`.
    3. When `dayBlocksEnabled` is false: room (id 'room', label `t('kidFillForm.roomSection')`, icon
       '🏠', body `roomBody`); one tile per activity as in step 2; grades when
       `dayType === 'school' && subjects.length > 0` (label `t('kidFillForm.gradesSection')`, icon
       '📚'); exercises when `exerciseTypes.length > 0` (label `t('kidFillForm.exercisesSection')`,
       icon '🤸'); sport (label `t('kidFillForm.sectionsSection')`, icon '🏆'); reading (label
       `t('kidFillForm.readingSection')`, icon '📖').
    4. mood — always last (D-02): id 'mood', icon '✨', label `t('kidFillForm.moodSection')`, body
       `moodBody`, done `done.mood`.

    Per-tile `coins` and `loss` (D-17) come from the already-computed maps, never from new maths:
    for the aggregate ids ('room', 'grade', 'sport', 'book') use `sectionCoins[id]`; for
    `activity:${act.id}` use `checkedActivities.has(act.id) ? act.coins : undefined`; for
    `custom:${block.id}` use `customBlockDone[block.id] ? block.price : undefined`; 'exercise',
    'behavior' and 'mood' carry no coins. `loss` is `typeof coins === 'number' && coins < 0`. A loss
    tile must not also read as mint-done — plan 01's TileCard already enforces that precedence, so
    just pass both flags honestly.

    `status` is `tileDone ? t('kidFillForm.tileDone') : t('kidFillForm.tileNotFilled')` for every tile
    — no per-category status copy is invented in this phase (UI-SPEC's "2 предмета" example is
    illustrative, and the coin tag already carries the quantitative signal).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const moodBody" components/kid/KidDayFillForm.tsx` returns 1 and
      `grep -c "renderGroup('mood'" components/kid/KidDayFillForm.tsx` returns 1 with `moodBody` as its
      body argument.
    - `git diff components/kid/KidDayFillForm.tsx` shows NO changes inside `sectionCoins`, `applicable`,
      `handleSubmit`, `creditAwardAndFinish`, `handleRetryAward`, or any `use*` state declaration other
      than the single added `openTileId` line.
    - `grep -c "function buildTiles" components/kid/KidDayFillForm.tsx` returns 1, and the function is
      declared at component scope (not inside another function, not wrapped in useMemo/useCallback).
    - `grep -c "activity:\${" components/kid/KidDayFillForm.tsx` and
      `grep -c "custom:\${" components/kid/KidDayFillForm.tsx` each return at least 1 (individual
      tiles per D-01).
    - `grep -c "api/wallet/award" components/kid/KidDayFillForm.tsx` returns exactly 1 (unchanged
      single crediting call site).
    - `npx tsc --noEmit`, `npm run lint`, `npm test` all exit 0.
  </acceptance_criteria>
  <done>Tile descriptors for every applicable category and item exist, sourced entirely from the reused bodies and coin maps.</done>
</task>

<task type="auto">
  <name>Task 3: KidDayFillForm — tile-sheet render branch, locked read-only, shared save bar</name>
  <files>components/kid/KidDayFillForm.tsx</files>
  <read_first>
    - components/kid/KidDayFillForm.tsx lines 1358-1511 (the current return: the `data-fill-style`
      wrapper with `onPointerDownCapture`, `<CoinFlyup>`, `<StickySummaryBar>`, the checklist div, the
      `saveError` banner, and the pinned save block at 1477-1508)
    - components/kid/day-fill/StickySummaryBar.tsx (reused verbatim for D-08 — same props, no fork)
    - .planning/phases/09.4-dayform-styles/09.4-CONTEXT.md decisions D-08, D-13, D-14, D-16, D-18
    - .planning/phases/09.4-dayform-styles/09.4-UI-SPEC.md sections "Locked day (D-13)" and the
      Copywriting Contract row for the primary CTA
  </read_first>
  <action>
    Wire the branch with the smallest possible diff. The wrapper `<div data-fill-style={style}>`, its
    `onPointerDownCapture` handler, `<CoinFlyup flyups={flyups} />`, the `<StickySummaryBar>` mount
    (identical props in both styles — D-08 reuses it as-is), the `saveError` banner, and the pinned
    save block (D-14: same component, same `position: sticky; bottom: 76` placement, same
    partial-save behaviour) all stay OUTSIDE the branch, exactly where they are today. Only the
    checklist `<div style={{ padding: '12px 16px 0', ... }}>` block (lines 1395-1456) becomes
    conditional.

    Replace that block with `style === 'tile-sheet' ? <TileGrid ... /> : <div style={{ padding:
    '12px 16px 0', ... }}>...existing checklist JSX unchanged...</div>`. Pass to TileGrid:
    `tiles={buildTiles()}`, `openId={openTileId}`, `onOpen={setOpenTileId}`,
    `onClose={() => setOpenTileId(null)}`, `subtitle={t('kidFillForm.tileSheetSub')}`,
    `closeLabel={t('kidFillForm.sheetClose')}`,
    `doneLabel={isLocked ? null : t('kidFillForm.sheetDone')}` (D-13: a locked sheet gets the X and
    the scrim only, no save-and-close affordance), and `emptyState` = the existing `nothingToday` card
    JSX (lines 1403-1409) when `nothingToday` is true, otherwise undefined.

    D-13's read-only behaviour needs no further work: every reused body (`RoomBlock`,
    `ActivitiesBlock`, `CustomBlock`, `BookBlock`, the grade buttons, the coach-rating stars, the
    behavior tags, the mood buttons) already takes `isLocked` and disables its own controls, and the
    `lockedLabel` pill already flows through `StickySummaryBar`. Confirm this by reading, and do not
    add a second locking mechanism.

    Because the 'story-stepper' value has no implementation until plan 03, the ternary's else branch
    (sticky-summary JSX) must remain the fall-through for it, and `app/kid/actions/fill-style.ts`
    still rejects that value until plan 04 — so no child can reach an unimplemented style mid-phase.
    Do not remove the `data-fill-style={style}` attribute: the verification plan greps on it.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<TileGrid" components/kid/KidDayFillForm.tsx` returns 1 and
      `grep -c "style === 'tile-sheet'" components/kid/KidDayFillForm.tsx` returns 1.
    - `grep -c "<StickySummaryBar" components/kid/KidDayFillForm.tsx` returns exactly 1 (one shared
      mount, not one per branch).
    - `grep -c "onPointerDownCapture" components/kid/KidDayFillForm.tsx` returns 1 and it is still on
      the outermost `data-fill-style` div (verify by reading).
    - `git diff components/kid/KidDayFillForm.tsx` shows the pinned save block (`position: 'sticky',
      bottom: 76`) and the `saveError` banner with zero changed lines.
    - `grep -c "isLocked ? null : t('kidFillForm.sheetDone')" components/kid/KidDayFillForm.tsx`
      returns 1 (D-13).
    - `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build` all exit 0.
  </acceptance_criteria>
  <done>Selecting tile-sheet renders the grid + sheet; sticky-summary renders byte-identically to before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child browser → KidDayFillForm state | The tile shell re-renders the same reused bodies; every write still leaves the browser through the pre-existing repo functions and `/api/wallet/award` |
| kid client → `/api/wallet/award` | Unchanged server-side crediting boundary; the client sends only `{childId, date}` |
| locked past day → editable controls | `isLocked` gates the reused bodies in the browser; the award route is the authoritative backstop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-094-05 | Tampering | tile-sheet render branch | mitigate | The branch is render-only: acceptance criteria assert `git diff` shows zero changes to `sectionCoins`, `handleSubmit`, `creditAwardAndFinish`, and that `api/wallet/award` still appears exactly once. No coin value is computed or written by `TileGrid`/`TileCard`. |
| T-094-06 | Elevation of Privilege | locked past-day sheet | mitigate | Locked sheets pass `doneLabel={null}` and every reused body keeps its existing `isLocked` disabling; a forged client edit still cannot credit coins because `/api/wallet/award` recomputes server-side from saved rows and the day-lock/backfill gate lives there (Phase 9.3 / backfill_mode work). |
| T-094-07 | Tampering | per-custom-block tile | mitigate | `renderCustomBlock` is reused unchanged, preserving the T-056-12 UI-level guard that `who_fills === 'parent'` blocks are not kid-toggleable; the award route remains the authoritative backstop. |
| T-094-08 | Information Disclosure | tile coin tags | accept | Tiles surface the same client-side `sectionCoins` preview already visible in sticky-summary to the same authenticated child; no cross-child or cross-family data is introduced (tiles are built from state already loaded for this childId). |
| T-094-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
1. `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build` all exit 0.
2. `grep -c "api/wallet/award" components/kid/KidDayFillForm.tsx` returns 1.
3. `git diff components/kid/KidDayFillForm.tsx` contains no modification inside `sectionCoins`,
   `applicable`, `handleSubmit`, `creditAwardAndFinish`, or `handleRetryAward`.
4. `grep -c "data-fill-row" components/kid/day-fill/TileGrid.tsx` returns 1 (D-16 anchoring).
</verification>

<success_criteria>
- `fill_style === 'tile-sheet'` renders a strict 2-column grid with one tile per category, per extra
  activity, per custom day-block, and one for mood.
- Tapping a tile opens a single shared bottom sheet containing only that category's reused body;
  nothing but an explicit close dismisses it.
- Net-negative categories render the danger-tinted tile state and never the mint done state.
- Sticky ring/total and the pinned Save button are shared, unmodified, with sticky-summary.
- A locked day renders the grid with read-only sheets and no save-and-close button.
- All four gates green and the sticky-summary style unchanged.
</success_criteria>

<output>
Create `.planning/phases/09.4-dayform-styles/09.4-02-SUMMARY.md` when done.
</output>
