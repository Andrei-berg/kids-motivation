# Phase 9.4: dayform-styles - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-19
**Phase:** 9.4-dayform-styles
**Areas discussed:** Tile/step granularity, Persistent progress in these styles, Story-stepper navigation & skip, Locked days & final save, Coin feedback & tile visual details (extra area)

---

## Tile/step granularity

| Option | Description | Selected |
|--------|-------------|----------|
| Individual tiles | Each activity/custom-block is its own one-tap unit with its own status (mirrors 9.3's D-05) | ✓ |
| Grouped by category | One "Activities" tile covers all activities; one "Custom" tile covers all custom blocks | |

**User's choice:** Individual tiles.

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, its own tile | Tapping opens a sheet with the 5-emoji picker + "wow" pop animation | ✓ |
| No tile — pinned always-visible strip | Preserves zero-tap-to-see philosophy for mood specifically | |

**User's choice:** Yes, mood gets its own tile.

| Option | Description | Selected |
|--------|-------------|----------|
| One step per category-group | "Activities" is one step listing all activities; "Custom" is one step listing all custom blocks | ✓ |
| One step per individual item | Every single activity/custom-block gets its own full-screen step | |

**User's choice:** One step per category-group.

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, its own step | Wow pop animation plays on that step, then auto-advances like any other step | ✓ |
| No — persistent strip alongside the stepper | Breaks the one-thing-at-a-time framing | |

**User's choice:** Yes, mood gets its own step.

---

## Persistent progress in these styles

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — reuse StickySummaryBar | Same header component sits above the tile grid | ✓ |
| No header — grid alone shows progress | Purest version of variant A | |

**User's choice:** Reuse StickySummaryBar for tile-sheet.

| Option | Description | Selected |
|--------|-------------|----------|
| Small persistent total in the header | A slim bar above the dot-track shows the ticking coin total | ✓ |
| Hidden until the end | Only floating +N per step, total revealed at the end | |

**User's choice:** Small persistent total in story-stepper's header.

---

## Story-stepper navigation & skip

| Option | Description | Selected |
|--------|-------------|----------|
| Visit every applicable step once | Auto-advance always walks the full sequence, never silently skips | ✓ |
| Auto-skip steps with nothing to fill | Implicit timing/heuristic bypass | |

**User's choice:** Visit every applicable step once.

| Option | Description | Selected |
|--------|-------------|----------|
| Skip = advance only, stays revisitable | Nothing saved/marked, dot stays not-done | ✓ |
| Skip = mark as intentionally passed | New tri-state dot concept | |

**User's choice:** Skip = advance only, stays revisitable.

| Option | Description | Selected |
|--------|-------------|----------|
| Tappable dots — jump to any step | Instagram-Stories-style, real re-tappability parity with other styles | ✓ |
| Forward-only | No way back mid-flow | |

**User's choice:** Tappable dots.

---

## Locked days & final save

| Option | Description | Selected |
|--------|-------------|----------|
| Same shell, read-only | Tiles/stepper still render in the chosen style, no toggle controls | ✓ |
| Fall back to sticky-summary's locked view | Kid loses preferred look when reviewing a past day | |

**User's choice:** Same shell, read-only.

| Option | Description | Selected |
|--------|-------------|----------|
| Same pinned bottom save button as sticky-summary | Reuses existing component, no completion gate | ✓ |
| Save only appears once every applicable tile is done | Real behavior change from today | |

**User's choice:** Same pinned bottom save button.

| Option | Description | Selected |
|--------|-------------|----------|
| Short summary screen with an explicit Save button | Recap before save fires, one last edit chance via dots | ✓ |
| Auto-save immediately on completing the last step | Fully game-like, no extra tap | |

**User's choice:** Short summary screen with an explicit Save button.

---

## Coin feedback & tile visual details (extra area)

Raised after the initial 4 areas were covered — user chose to discuss rather than leave to discretion.

| Option | Description | Selected |
|--------|-------------|----------|
| At the tap point inside the sheet/step | Reuses the exact data-fill-row + onPointerDownCapture mechanism from 9.3 | ✓ |
| At a fixed spot near the coin total in the header | Loses the "reward pops where you acted" feel | |

**User's choice:** At the tap point.

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — loss tint on the tile | Mirrors 9.3's D-11 transparency principle | ✓ |
| No — tiles stay binary done/not-done | Simpler but less transparent | |

**User's choice:** Yes, loss tint on the tile.

| Option | Description | Selected |
|--------|-------------|----------|
| Explicit close only | Consistent across every sheet regardless of category type | ✓ |
| Auto-close on single-tap answer | Two different sheet behaviors depending on category shape | |

**User's choice:** Explicit close only.

---

## Claude's Discretion

- Exact bottom-sheet open/close transition timing/easing beyond the sketch's base CSS
- Exact dot-track visual treatment for current/done/not-yet-visited states
- Exact layout relationship between story-stepper's persistent coin total and the dot-track
- Exact wording/visual of the skip control and the end-of-stepper summary screen's recap content
- Confirmed (not really open): tile-sheet's grid is a fixed 2-column layout per DAYFORM-02's literal wording, no responsive reflow expected

## Deferred Ideas

- **Loot-box/crate reward mechanic** — raised mid-discussion by the user (Brawl Stars-style
  crates with variable rarity, given for weekly achievements, containing shop-like bonuses,
  plus a higher-variance "chaos" crate tier). Identified as a new economy capability outside
  Phase 9.4's day-fill-styles boundary. Not placed on the roadmap yet — flagged for a future
  phase/milestone with its own dedicated discussion (schema, drop-rate fairness/transparency
  vs. the app's "honest consequence" principle, COPPA-adjacent randomized-reward considerations).
