---
phase: 09.4-dayform-styles
plan: 05
type: execute
wave: 5
depends_on: [09.4-04]
files_modified:
  - project-docs/data-model.md
  - project-docs/system-architecture.md
autonomous: false
requirements: [DAYFORM-02, DAYFORM-03]

must_haves:
  truths:
    - "Every gate (typecheck, lint, tests, build) is green on the finished phase"
    - "Every phase decision D-01..D-05 and D-08..D-18 has traceable code evidence"
    - "DAYFORM-02 and DAYFORM-03 are evidenced, and DAYFORM-01's all-three-styles wording is now literally satisfied"
    - "project-docs no longer claims tile-sheet/story-stepper are unimplemented"
    - "A human has confirmed both new styles in a real browser, including skip, dot jumps, loss tint, reduced motion and a locked past day"
  artifacts:
    - path: "project-docs/system-architecture.md"
      provides: "all three fill styles and the new day-fill components documented"
      contains: "story-stepper"
    - path: "project-docs/data-model.md"
      provides: "fill_style paragraph corrected — all three values implemented"
      contains: "fill_style"
  key_links:
    - from: ".planning/phases/09.4-dayform-styles/09.4-05-SUMMARY.md"
      to: "code evidence per decision"
      via: "D-01..D-18 traceability matrix"
      pattern: "D-18"
---

<objective>
Close phase 9.4: prove every gate is green, trace every decision and requirement to concrete code,
correct the `project-docs` statements that still say only `sticky-summary` is implemented, and get the
operator's browser sign-off on the two new interaction styles.

Purpose: both success criteria for this phase are interaction-shaped (auto-advance pacing, sheet
close semantics, no-reflow tile updates) — automated gates prove the wiring, a human proves the feel.
This mirrors the phase-close pattern recorded as decision [Phase 09.3-06] in STATE.md, which exists
specifically because a ROADMAP "Complete" marker has previously not matched reality in this project.
Output: green gates, a complete traceability matrix, corrected docs, an operator-approved phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09.4-dayform-styles/09.4-CONTEXT.md
@.planning/phases/09.4-dayform-styles/09.4-UI-SPEC.md
@.planning/phases/09.4-dayform-styles/09.4-01-SUMMARY.md
@.planning/phases/09.4-dayform-styles/09.4-02-SUMMARY.md
@.planning/phases/09.4-dayform-styles/09.4-03-SUMMARY.md
@.planning/phases/09.4-dayform-styles/09.4-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate sweep and the D-01..D-18 / DAYFORM-02-03 traceability matrix</name>
  <files>(no source files — verification only; findings go in the plan SUMMARY)</files>
  <read_first>
    - .planning/phases/09.4-dayform-styles/09.4-CONTEXT.md (the full decision list — note the
      numbering gap: this phase has D-01..D-05 and D-08..D-18; there is no D-06 or D-07, and D-05's
      in-text reference to "D-06/D-07 below" means the auto-advance/skip rules now numbered D-10/D-11)
    - .planning/REQUIREMENTS.md lines 29-45 (DAYFORM-01..06 wording and the traceability table at
      lines 97-102 that still marks DAYFORM-02/03 Pending)
    - .planning/phases/09.4-dayform-styles/09.4-0{1,2,3,4}-SUMMARY.md (what each plan reported)
    - scripts/verify-migrations-applied.mjs (the live-schema audit — no migration ships this phase, but
      `children.fill_style` must still be present at phase close)
  </read_first>
  <action>
    Run every gate and quote the exact output in the SUMMARY: `npx tsc --noEmit`, `npm run lint`,
    `npm test`, `npm run build`, and — because this phase writes a value the database CHECK-constrains
    — `node --env-file=.env.local scripts/verify-migrations-applied.mjs` to re-confirm
    `children.fill_style` and `children_fill_style_check` still exist in prod rather than trusting the
    Phase 9.3 SUMMARY (the "ROADMAP Complete ≠ migration ran" failure mode recorded in STATE.md).

    Then write a traceability matrix into the SUMMARY with one row per decision (D-01, D-02, D-03,
    D-04, D-05, D-08 … D-18) and one row per requirement (DAYFORM-02, DAYFORM-03, plus a note on
    DAYFORM-01 and DAYFORM-05). Each row names the file and symbol that implements it plus the grep or
    command that proves it. Use these evidence anchors:
    D-01 → `buildTiles` in `KidDayFillForm.tsx` emitting `activity:${id}` and `custom:${id}` tiles;
    D-02 → the mood tile emitted last by `buildTiles` plus `const moodBody`;
    D-03 → one tile each for room/grade/exercise/sport/book/behavior in `buildTiles`;
    D-04 → `buildSteps` emitting ONE Activities step and ONE Custom step (grep that neither
    `activity:${` nor `custom:${` appears inside `buildSteps`);
    D-05 → the mood step in `buildSteps` with `autoAdvance` true and `moodBody` reused;
    D-08 → the single `<StickySummaryBar` mount shared by sticky-summary and tile-sheet;
    D-09 → `<StepCoinHeader` in the story-stepper branch and the absence of `ProgressRing` in
    `StepCoinHeader.tsx`;
    D-10 → `buildSteps` gating on the `applicable` map plus StoryStepper's `advance()`/auto-advance
    effect;
    D-11 → StoryStepper's nav-row onClick calling only the local advance (no write/propose/toggle);
    D-12 → `<DotTrack ... onJump={setStepIdx}` and DotTrack's per-dot button;
    D-13 → `doneLabel={isLocked ? null : ...}` for tile-sheet and `locked={isLocked}` for
    story-stepper, plus the reused bodies' existing `isLocked` disabling;
    D-14 → exactly one `position: 'sticky', bottom: 76` save block, shared by all three styles;
    D-15 → StoryStepper's summary screen rendering `{footer}` and no submit of its own;
    D-16 → the single `onPointerDownCapture` on the `data-fill-style` wrapper plus the
    `data-fill-row` wrappers in `TileGrid.tsx` and `StoryStepper.tsx`;
    D-17 → TileCard's `${K.danger}14`/`${K.danger}40` loss branch and the `loss` flag derived from
    `sectionCoins`/`act.coins`/`block.price`;
    D-18 → BottomSheet's three and only three `onClose` call sites and the absence of any close
    triggered from `children`;
    DAYFORM-02 → `gridTemplateColumns: '1fr 1fr'` + the single shared `<BottomSheet>`;
    DAYFORM-03 → the dot track + the auto-advance effect + the skip control, and explicitly record
    plan 03's documented interpretation that auto-advance fires for single-answer steps while
    multi-item steps advance via the nav control (cite plan 03's SUMMARY);
    DAYFORM-01 → now literally satisfied by the 3-way picker (grep `fillStyleOption` in
    `ProfileSheet.tsx`) with the Phase-9.3 scope guard gone;
    DAYFORM-05 → both new styles reuse the same `useCoinAnimation`/`CoinFlyup` mechanism with no
    second animation system.

    Also assert the phase's reuse contract (the D-13 principle carried over from Phase 9.3): quote
    `git diff` evidence that `sectionCoins`, `coinsPreview`, `applicable`, `handleSubmit`,
    `creditAwardAndFinish`, and `handleRetryAward` in `KidDayFillForm.tsx` are unchanged across the
    whole phase, and that `grep -c "api/wallet/award" components/kid/KidDayFillForm.tsx` is 1.

    If any row cannot be evidenced, STOP and report it as a gap rather than writing a green matrix.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <automated>node --env-file=.env.local scripts/verify-migrations-applied.mjs</automated>
  </verify>
  <acceptance_criteria>
    - All five commands above exit 0, with their output quoted in the SUMMARY.
    - The SUMMARY contains a matrix with a row for each of D-01..D-05, D-08..D-18, DAYFORM-02 and
      DAYFORM-03 (plus the DAYFORM-01/05 notes), each naming a file and a proving command; no row
      reads "n/a" or "assumed".
    - The SUMMARY records the CONTEXT.md numbering gap (no D-06/D-07 exist in this phase).
    - `grep -c "api/wallet/award" components/kid/KidDayFillForm.tsx` returns 1.
    - `grep -c "position: 'sticky', bottom: 76" components/kid/KidDayFillForm.tsx` returns 1.
    - `grep -c "Fill style not available yet" app/kid/actions/fill-style.ts` returns 0.
    - `grep -rn "fillStyleActive" --include="*.ts" --include="*.tsx" --include="*.json" . | grep -v .planning`
      returns nothing.
    - `npx vitest run tests/fill-style-validation.test.ts tests/day-fill-progress.test.ts` exits 0.
  </acceptance_criteria>
  <done>All gates green, the live schema re-confirmed, and every decision and requirement traced to code evidence.</done>
</task>

<task type="auto">
  <name>Task 2: project-docs correction</name>
  <files>project-docs/data-model.md, project-docs/system-architecture.md</files>
  <read_first>
    - project-docs/data-model.md lines 224-231 (the `children.fill_style` paragraph, which currently
      says "Only `sticky-summary` is implemented as of Phase 9.3; `tile-sheet`/`story-stepper` arrive
      in Phase 9.4")
    - project-docs/system-architecture.md lines 129-160 ("Kid Day-fill Render Layer", which currently
      says "As of Phase 9.3 `sticky-summary` is the only implemented style")
    - .planning/phases/09.4-dayform-styles/09.4-0{1,2,3,4}-SUMMARY.md (the as-built component list)
  </read_first>
  <action>
    In `project-docs/data-model.md`, edit the `children.fill_style` paragraph so it states that all
    three values are implemented as of Phase 9.4 and that the value is whitelist-validated in
    `app/kid/actions/fill-style.ts` before the service-role write, with
    `children_fill_style_check` as the database-level backstop. Leave the column definition, the CHECK
    constraint name, and the migration filename exactly as written.

    In `project-docs/system-architecture.md`, update the "Kid Day-fill Render Layer" section: replace
    the "As of Phase 9.3 `sticky-summary` is the only implemented style" sentence with a statement
    that all three styles ship as of Phase 9.4, and extend the component list to name the pieces added
    this phase — `TileGrid` + `TileCard` + `BottomSheet` (2-column grid whose tiles open one shared
    bottom sheet scoped to a single category), `StoryStepper` + `DotTrack` + `StepCoinHeader`
    (one category per screen behind a tappable dot track with auto-advance, an explicit skip, and a
    final summary screen that carries the shared Save button). State explicitly that all three styles
    are render-only shells over the same `KidDayFillForm` state/save/award pipeline, that the pinned
    Save button and `/api/wallet/award` call are shared and unchanged, and that the same
    `useCoinAnimation`/`CoinFlyup` mechanism anchors flyups in all three via one
    `onPointerDownCapture` handler and `data-fill-row` markers.

    Keep both edits in the surrounding prose style and scoped to these two sections; do not
    restructure either document.
  </action>
  <verify>
    <automated>grep -c "story-stepper" project-docs/system-architecture.md</automated>
    <automated>grep -c "fill_style" project-docs/data-model.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "only implemented style\|Only \`sticky-summary\` is implemented" project-docs/system-architecture.md project-docs/data-model.md`
      returns 0 for both files.
    - `grep -c "TileGrid\|StoryStepper" project-docs/system-architecture.md` is >= 2.
    - `grep -c "children_fill_style_check" project-docs/data-model.md` returns 1 (constraint name
      preserved).
    - `grep -c "2026-09-19-child-fill-style.sql" project-docs/data-model.md` returns 1 (migration
      reference preserved).
    - `git diff --stat project-docs/` shows only these two files changed.
  </acceptance_criteria>
  <done>project-docs describe all three shipped fill styles and the components behind them.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator browser verification of tile-sheet and story-stepper</name>
  <files>(no files — operator verification of the running app)</files>
  <read_first>
    - .planning/phases/09.4-dayform-styles/09.4-UI-SPEC.md (both "Interaction Contract" sections)
    - .planning/phases/09.4-dayform-styles/09.4-CONTEXT.md (D-10, D-11, D-12, D-13, D-15, D-17, D-18)
  </read_first>
  <action>
    Start the dev server (`npm run dev`), then PAUSE and hand the 16 numbered steps in
    `<how-to-verify>` to the operator verbatim. Do not self-approve, do not simulate the browser, and
    do not mark the phase complete before the resume signal arrives. If the operator reports a
    deviation, record it in the SUMMARY as a gap citing the step number and stop — do not attempt a fix
    inside this checkpoint task.
  </action>
  <what-built>
    Two new day-fill styles over the unchanged KidDayFillForm pipeline: `tile-sheet` (a 2-column grid
    with one tile per category, per extra activity, per custom day-block and one for mood, each opening
    a bottom sheet scoped to that category, with a danger tint on net-negative categories) and
    `story-stepper` (one category per screen behind a tappable dot track, a slim live coin total,
    auto-advance on single-answer steps, an explicit skip, and a summary screen carrying the shared
    Save button). Plus a real 3-way style picker in the kid's ProfileSheet backed by the now
    whitelist-hardened server action. Everything automatable was already asserted in task 1
    (typecheck, lint, tests, build, live schema, decision traceability). What remains needs eyes and a
    finger.
  </what-built>
  <how-to-verify>
    1. `npm run dev`, open http://localhost:3000, sign in as a child (family code + PIN at
       `/kid/login`), tap the header avatar → ProfileSheet.
    2. Picker: under "Стиль заполнения дня" there are THREE options (Сводка / Плитки / Степпер), the
       current one marked with a ✓, each at least a fingertip tall. Pick "Плитки", close the sheet,
       reopen it — "Плитки" is still marked (the choice persisted to the database).
    3. Go to `/kid/day` for TODAY. The day renders as a 2-column grid of tiles, with the same ring +
       coin total bar on top as before. Every configured extra activity and every custom day-block has
       its OWN tile (not one grouped "Доп. занятия" tile), and mood has its own tile.
    4. Tap the room tile: a sheet slides up from the bottom containing only the room checklist. Tick
       enough tasks to cross the room threshold — a green "+N 💰" floats up near the control you
       tapped (inside the sheet, not up at the header) and the total ticks up.
    5. With the sheet still open, tap the mood tile's category equivalent later — first confirm the
       sheet does NOT close by itself after any single answer. Close it with the × (or the "Готово"
       button, or by tapping the dimmed background). The room tile now shows its done state and the
       page behind it has NOT jumped or reflowed — the other tiles are exactly where they were.
    6. Tap the mood tile: the 5-emoji picker appears in its sheet; picking one plays the pop animation
       and the sheet STAYS OPEN until you close it explicitly.
    7. Open the grades tile, pick a grade with a negative coin value in your family's rules: a red
       downward "-N 💰" fires and, after closing the sheet, that tile is tinted red/danger — NOT mint —
       with the negative amount shown.
    8. Save the day with the pinned bottom button (same button as before). The save → award →
       confetti/stamp flow works and the wallet matches what the server credited.
    9. Back in ProfileSheet, switch to "Степпер" and return to `/kid/day`.
    10. Stepper: a slim coin total sits above a row of dots; ONE category fills the screen. There are
        as many dots as applicable categories — all your extra activities share a single "Доп. занятия"
        step and all custom blocks share a single step (coarser than the tiles, deliberately).
    11. Answer a single-answer step (mood is the clearest): a brief "Готово! ✓" appears and the screen
        advances by itself to the next unfinished step, without feeling instantaneous.
    12. On a multi-item step (room, grades), confirm it does NOT jump away after the first item —
        you can finish the whole checklist, and the bottom control reads "Дальше →" once something is
        entered and "Пропустить →" while it is blank.
    13. Tap "Пропустить →" on a blank step: it advances and nothing is marked — that category's dot
        stays empty, and nothing is saved for it.
    14. Tap an EARLIER dot: you jump back to that step and can change the answer; the change is
        reflected in the coin total at the top.
    15. Walk to the end: a summary screen appears with a recap and the same Save button (nothing
        auto-saved before you tapped it). Tapping a recap row jumps back to that step. Save, and check
        the wallet credited correctly.
    16. Edge cases: (a) turn on macOS System Settings → Accessibility → Display → Reduce motion and
        reload — sheets appear instantly with no slide, dots change with no glide, but everything still
        works; (b) open an already-filled PAST day in each style — the tiles/dots still render in your
        chosen style (no fallback to Сводка), the sheets/steps open read-only with no toggles, there is
        no skip control, no coin flyups fire on open, and the "🔒 Закрыт" state shows.
  </how-to-verify>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <human-check>Steps 1-16 above all behave as described.</human-check>
  </verify>
  <acceptance_criteria>
    - The operator confirms each of the 16 steps, or reports specific deviations naming the step number.
    - Any reported deviation is captured in the SUMMARY as a gap with the step number, not silently
      closed.
    - Steps 5 and 6 (explicit-close-only, no reflow — D-18/DAYFORM-02), 7 (loss tint — D-17), 11-13
      (auto-advance pacing, no premature jump, non-writing skip — D-10/D-11), 14 (dot jump — D-12), 15
      (no auto-submit — D-15) and 16b (locked day in the chosen style — D-13) are explicitly confirmed:
      these are the decisions no terminal command can assert.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the issues by step number.</resume-signal>
  <done>Operator has confirmed both new fill styles end to end in a browser.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → prod Postgres | Read-only verification queries via `SUPABASE_DB_URL` |
| operator browser → the running app | Manual exercise of the kid session built in plans 01-04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-094-18 | Repudiation | phase-close verification | mitigate | Gate outputs and the D-01..D-18 traceability matrix are recorded verbatim in the SUMMARY, so a later "was this actually verified?" question has evidence — the STATE.md-recorded failure mode of trusting a "Complete" marker is exactly what task 1's live-schema re-check exists to prevent. |
| T-094-19 | Tampering | verification script against prod | accept | `verify-migrations-applied.mjs` issues read-only catalog queries only; it never writes. |
| T-094-20 | Information Disclosure | project-docs update | mitigate | Documentation records component names, column names and the write path only — no secrets, keys, or connection strings are added to `project-docs/`. |
| T-094-21 | Elevation of Privilege | operator verification of the locked past day | mitigate | Step 16b explicitly exercises the read-only locked-day path in both new styles; any editable control found there is a reportable gap, because client-side locking is defense-in-depth over the award route's authoritative date gating. |
| T-094-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
1. `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build` all exit 0.
2. `node --env-file=.env.local scripts/verify-migrations-applied.mjs` exits 0.
3. The SUMMARY holds a complete D-01..D-05 / D-08..D-18 + DAYFORM-02/03 traceability matrix.
4. `project-docs/data-model.md` and `project-docs/system-architecture.md` no longer claim only
   `sticky-summary` is implemented.
5. Operator sign-off recorded (or gaps listed by step number).
</verification>

<success_criteria>
- Every gate green and the live schema re-confirmed at phase close.
- Every phase decision and both phase requirements traced to concrete code evidence, with the
  DAYFORM-03 auto-advance interpretation recorded explicitly.
- project-docs describe all three shipped styles and the components behind them.
- A human has confirmed the sheet close semantics, no-reflow tile update, loss tint, auto-advance
  pacing, non-writing skip, dot jumps, the no-auto-submit summary screen, reduced motion, and the
  locked past day in a real browser.
</success_criteria>

<output>
Create `.planning/phases/09.4-dayform-styles/09.4-05-SUMMARY.md` when done.
</output>
