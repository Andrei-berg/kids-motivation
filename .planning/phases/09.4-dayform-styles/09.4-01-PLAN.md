---
phase: 09.4-dayform-styles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/kid/day-fill/BottomSheet.tsx
  - components/kid/day-fill/TileCard.tsx
  - components/kid/day-fill/DotTrack.tsx
  - components/kid/day-fill/StepCoinHeader.tsx
  - app/globals.css
  - public/locales/ru.json
  - public/locales/en.json
autonomous: true
requirements: [DAYFORM-02, DAYFORM-03]

must_haves:
  truths:
    - "A bottom sheet can be opened and closed only by an explicit close action — never by answering inside it"
    - "A tile can render three mutually exclusive states: at rest, done (mint), net-negative today (danger tint)"
    - "A dot-progress track renders one tappable dot per step with done / current / not-visited states"
    - "A slim coin header shows the live ticking coin total without a completion ring"
    - "Every new animation is double-guarded for prefers-reduced-motion"
    - "Every new string is available in both ru and en locales"
  artifacts:
    - path: "components/kid/day-fill/BottomSheet.tsx"
      provides: "Scrim + slide-up sheet with explicit-close-only semantics (D-18)"
      exports: ["default"]
    - path: "components/kid/day-fill/TileCard.tsx"
      provides: "2-column grid tile with rest/done/loss states (D-17)"
      exports: ["default"]
    - path: "components/kid/day-fill/DotTrack.tsx"
      provides: "Tappable Instagram-Stories dot track (D-12)"
      exports: ["default"]
    - path: "components/kid/day-fill/StepCoinHeader.tsx"
      provides: "Slim persistent coin total for story-stepper (D-09)"
      exports: ["default"]
    - path: "app/globals.css"
      provides: "reduced-motion-guarded kid-slide-up / kid-tile / kid-dot-fill"
      contains: "kid-dot-fill"
  key_links:
    - from: "components/kid/day-fill/BottomSheet.tsx"
      to: "app/globals.css .kid-bottom-sheet"
      via: "className applied only when motion is not reduced"
      pattern: "kid-bottom-sheet"
    - from: "components/kid/day-fill/TileCard.tsx"
      to: "components/kid/design/kidTheme K"
      via: "K.mint / K.mintSoft / K.danger tokens only, no new hex"
      pattern: "K\\.(mint|danger|line)"
---

<objective>
Build the four stateless presentation primitives that both new fill styles are assembled from —
`BottomSheet`, `TileCard`, `DotTrack`, `StepCoinHeader` — add the reduced-motion-guarded CSS they
need, and land every new i18n key for phases 9.4's two shells and the ProfileSheet picker.

Purpose: plans 02 and 03 wire these into `KidDayFillForm` without inventing visuals mid-flight;
keeping the primitives stateless and copy-agnostic mirrors what Phase 9.3 did with
`StickySummaryBar` / `QuickRow` / `InlinePanelRow` (which is exactly why `StickySummaryBar` is
reusable verbatim this phase).
Output: 4 new components, 1 CSS block rewritten in place, 16 new locale keys × 2 languages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.4-dayform-styles/09.4-CONTEXT.md
@.planning/phases/09.4-dayform-styles/09.4-UI-SPEC.md
@.planning/phases/09.4-dayform-styles/09.4-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- The exact contracts plans 02 and 03 will consume. Build to these signatures. -->

components/kid/day-fill/BottomSheet.tsx:
```typescript
interface BottomSheetProps {
  open: boolean
  title: string
  icon?: string
  onClose: () => void
  closeLabel: string          // aria-label for the X button
  doneLabel?: string | null   // renders the explicit "Готово" close button; null/undefined = omit it
  children: React.ReactNode
}
export default function BottomSheet(props: BottomSheetProps): JSX.Element | null
```

components/kid/day-fill/TileCard.tsx:
```typescript
interface TileCardProps {
  label: string
  icon: string
  done: boolean
  loss?: boolean        // mutually exclusive with the mint done styling
  status: string        // subtext, e.g. "Не заполнено" / "Готово ✓"
  coins?: number        // signed per-category coin contribution; omitted when 0/absent
  onTap: () => void
}
export default function TileCard(props: TileCardProps): JSX.Element
```

components/kid/day-fill/DotTrack.tsx:
```typescript
interface DotTrackProps {
  dots: Array<{ id: string; label: string; done: boolean }>
  currentIdx: number
  onJump: (idx: number) => void
  ariaLabelFor: (idx: number, label: string) => string
}
export default function DotTrack(props: DotTrackProps): JSX.Element
```

components/kid/day-fill/StepCoinHeader.tsx:
```typescript
interface StepCoinHeaderProps {
  coins: number
  caption: string
  lockedLabel?: string | null
}
export default function StepCoinHeader(props: StepCoinHeaderProps): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduced-motion-guarded sheet/tile/dot CSS in app/globals.css</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css lines 4370-4460 (the current unguarded `.kid-bottom-sheet` / `@keyframes
      kid-slide-up` block at 4375-4383, and the established double-guard idiom at 4430-4451 where
      `.coin-flyup-number.loss` / `.kid-mood-pop` keep their keyframes INSIDE
      `@media (prefers-reduced-motion: no-preference)`)
    - components/kid/CoinAnimation.tsx lines 18-24 (the `reducedMotion()` JS half of the same guard)
    - .planning/phases/09.4-dayform-styles/09.4-PATTERNS.md section "app/globals.css"
  </read_first>
  <action>
    Replace the block currently at app/globals.css lines 4375-4383 (comment "Bottom sheet slide-up",
    `@keyframes kid-slide-up`, and the unguarded `.kid-bottom-sheet { animation: ... }`) with a single
    `@media (prefers-reduced-motion: no-preference)` block containing, in this order:
    the `kid-slide-up` keyframes (`from { transform: translateY(100%); }` → `to { transform:
    translateY(0); }`), `.kid-bottom-sheet { animation: kid-slide-up .28s cubic-bezier(.2,.9,.3,1.1)
    forwards; }` (the sketch's easing, per CONTEXT.md's Claude's-Discretion note to use the sketch CSS
    as the base), `.kid-tile { transition: border-color .15s, transform .1s; }`,
    `.kid-tile:active { transform: scale(0.97); }`, and
    `.kid-dot-fill { transition: width .3s, background-color .2s; }`.

    Keep the existing comment style: head the block with a short comment naming Phase 09.4 and D-18/
    D-12, mirroring the "Phase 09.3 / D-08 / D-11" comment at line 4412. Do NOT touch any other rule
    in the file — `.kid-bottom-sheet` has zero other consumers in the repo today (verified: the only
    occurrence outside globals.css is none), so this is a safe in-place replacement, not a rename.
    Put no base (non-animated) properties inside the media query: `.kid-tile` and `.kid-dot-fill`
    carry all of their layout/colour via inline styles from the components, so a reduced-motion user
    gets the identical static appearance with no transition.
  </action>
  <verify>
    <automated>grep -c "kid-dot-fill" app/globals.css</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "kid-slide-up" app/globals.css` shows both occurrences (keyframes + animation property)
      at line numbers greater than the line of the `@media (prefers-reduced-motion: no-preference)`
      opening that introduces them.
    - `grep -v '^\s*/\*' app/globals.css | grep -c "kid-tile:active"` returns 1.
    - `grep -c "kid-dot-fill" app/globals.css` returns 1.
    - No `.kid-bottom-sheet` animation declaration exists outside a `prefers-reduced-motion:
      no-preference` media block (inspect the surrounding 20 lines to confirm).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Sheet slide-up, tile press, and dot fill transitions all live behind the established reduced-motion guard.</done>
</task>

<task type="auto">
  <name>Task 2: BottomSheet, TileCard, DotTrack, StepCoinHeader components</name>
  <files>components/kid/day-fill/BottomSheet.tsx, components/kid/day-fill/TileCard.tsx, components/kid/day-fill/DotTrack.tsx, components/kid/day-fill/StepCoinHeader.tsx</files>
  <read_first>
    - components/kid/day-fill/StickySummaryBar.tsx (the whole 65-line file — StepCoinHeader is this
      file minus the ProgressRing/pct/ringLabel; copy its AnimatedNum + Coin + caption block and its
      lockedLabel pill verbatim)
    - components/kid/day-fill/InlinePanelRow.tsx (the doc comment's "plain renderer, stable key" rule
      and the `open ? K.sky : done ? K.mint : K.line` border idiom at line 31)
    - components/kid/day-fill/QuickRow.tsx (the `done ? K.mint : K.line` + `K.mintSoft` icon-chip
      treatment these tiles must match)
    - components/kid/ProfileSheet.tsx lines 86-106 (the codebase's fixed-overlay bottom-sheet idiom:
      `position: fixed; inset: 0` scrim with onClick-to-close plus a `stopPropagation` inner panel —
      there is NO react-dom portal anywhere in this codebase, do not introduce one)
    - components/kid/design/kidTheme.ts (the complete K token set — no new hex values allowed)
    - components/kid/design/atoms.tsx lines 151-168 (`Coin`) and 339-366 (`AnimatedNum`)
    - .planning/phases/09.4-dayform-styles/09.4-UI-SPEC.md sections "Spacing Scale", "Typography",
      "Color", "Interaction Contract — Tile-Sheet style", "Interaction Contract — Story-Stepper style"
  </read_first>
  <action>
    Create four `'use client'` components under components/kid/day-fill/, each a module-level default
    export with the exact prop interface given in this plan's `<interfaces>` block. All four are
    stateless and copy-agnostic (every string arrives as a prop) — the same contract
    StickySummaryBar/QuickRow/InlinePanelRow follow.

    BottomSheet.tsx — returns null when `open` is false. When open, renders two siblings: a scrim
    (`position: fixed; inset: 0; background: rgba(20,20,30,0.45); zIndex: 230`) whose onClick calls
    `onClose`, and the sheet panel (`position: fixed; left: 0; right: 0; bottom: 0; zIndex: 231;
    background: K.card; borderRadius: '22px 22px 0 0'; padding: '10px 16px calc(22px +
    env(safe-area-inset-bottom, 0px))'; maxHeight: '85vh'; overflowY: 'auto'`) whose onClick calls
    `stopPropagation`. Inside the panel, top to bottom: a 40×4 grip bar (`background: K.line`,
    `borderRadius: 999`, `margin: '0 auto 12px'`); a header row with the `icon` glyph plus `title`
    styled Heading (fontFamily K.fDisp, fontSize 22, fontWeight 900, color K.ink) and, pushed right, a
    square close button (minWidth/minHeight 44, borderRadius 14, border `1.5px solid ${K.line}`,
    background K.card, aria-label `closeLabel`, textual × glyph) calling `onClose`; then `children`;
    then, only when `doneLabel` is a non-empty string, a full-width primary button (minHeight 48,
    borderRadius 16, background K.sky, color '#fff', fontFamily K.fDisp, fontSize 16, fontWeight 800)
    labelled `doneLabel` that calls `onClose`.

    D-18 is a hard rule for this component: BottomSheet must never close itself in response to
    anything inside `children`. The ONLY three close paths are the scrim tap, the X button, and the
    optional doneLabel button. Do not add an onChange/onClick listener on the children wrapper, and do
    not close on Escape-key auto-dismiss logic that could fire from an inner control.

    Apply the reduced-motion double guard exactly as KidDayFillForm lines 167-172 do: a `reduced`
    state initialised in a mount effect from `window.matchMedia('(prefers-reduced-motion: reduce)')
    .matches` with the `typeof window`/`typeof window.matchMedia` guards, and apply
    `className="kid-bottom-sheet"` to the panel only when `!reduced` (when reduced, the panel renders
    already in place with no animation, since the class carries only the animation).

    TileCard.tsx — a `<button type="button" className="kid-tile">` sized to fill its grid cell:
    textAlign left, padding 16 (md token), borderRadius 18, position relative, overflow hidden,
    minHeight 92, cursor pointer, display flex column gap 4. Three mutually exclusive visual states,
    resolved in this precedence order: when `loss` is true use `background: ${K.danger}14` and
    `border: 1.5px solid ${K.danger}40` with the status text in solid K.danger; else when `done` is
    true use `background: K.mintSoft`, `border: 1.5px solid ${K.mint}`, status text K.mintDeep, and a
    22×22 checkmark badge absolutely positioned at top 10 right 10 (borderRadius '50%', background
    K.mint, color '#fff', fontSize 13, centred ✓); else `background: K.card`, `border: 1.5px solid
    ${K.line}`, status text K.ink3. A loss tile never shows the mint badge and never shows mint
    colours. Content: the `icon` glyph at fontSize 24; `label` in Body type (K.fDisp, 16, 700, K.ink);
    `status` in Label type (K.fBody, 13, 700, state colour); and when `coins` is a non-zero number a
    trailing inline-flex row rendering `{coins > 0 ? '+' : ''}{coins}` in K.fNum 13/700 coloured
    `coins < 0 ? K.danger : K.mintDeep` followed by `<Coin size={15} />` — copy this coin-tag idiom
    verbatim from KidDayFillForm's `trailingFor` (lines 1256-1266) so all three fill styles show coin
    contributions identically. TileCard has no `disabled` prop: on a locked day the tile stays
    tappable because D-13 requires a locked tile to still open its (read-only) sheet.

    DotTrack.tsx — a flex row (`gap: 6`, `padding: '14px 16px 4px'`). One `<button type="button">` per
    entry of `dots`: flex 1, height 28 (touch-comfortable hit area around a 5px bar), padding 0,
    border none, background transparent, cursor pointer, aria-label `ariaLabelFor(idx, dot.label)`,
    `aria-current={idx === currentIdx ? 'step' : undefined}`, onClick `() => onJump(idx)` for every
    index (forward and backward — D-12's "tap any earlier dot" is the minimum, allowing any index is a
    superset and costs nothing). Inside each button a 5px-tall track (`width: '100%'`, borderRadius
    999, background K.line, overflow hidden) containing a `<span className="kid-dot-fill">` whose
    `width` is `'100%'` when `dot.done` else `'0%'`, height '100%', display block, and whose
    background is K.sky when `idx === currentIdx` and K.mint otherwise. A current-but-not-done dot
    therefore reads as a sky-tinted empty track, which is exactly the sketch's
    `.dot.current .fill` behaviour; there is no fourth "skipped" state (D-11).

    StepCoinHeader.tsx — structurally StickySummaryBar with the ring removed: same sticky positioning
    (`position: 'sticky', top: 0, zIndex: 15`), same `background: K.cream`, same
    `borderBottom: 1px solid ${K.line}`, same flex/alignItems/gap 12, but `padding: '7px 16px'` giving
    a single-row ~44-48px band (UI-SPEC's budget — the dot-track needs its own band below it). Keep
    the `AnimatedNum value={coins} duration={500}` in K.fDisp 20/900 K.mangoDeep followed by
    `<Coin size={17} />`, the caption in K.fBody 13/700 K.ink3, and the optional `lockedLabel` pill —
    all copied verbatim from StickySummaryBar. Do not import ProgressRing here and do not accept
    `pct`/`ringLabel` props.

    Every colour must come from the `K` object; introduce no new hex literal and no second token
    source (`lib/design/tokens` is parent-owned and off-limits on kid screens).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist under components/kid/day-fill/ and each begins with `'use client'`.
    - `grep -c "createPortal" components/kid/day-fill/*.tsx` returns 0 for every file.
    - `grep -c "#" components/kid/day-fill/TileCard.tsx` counts no 6-digit hex literal (only the
      `${K.danger}14` / `${K.danger}40` alpha-suffix idiom and `'#fff'` for the badge glyph are
      permitted) — verify by reading the file, not by hex count alone.
    - `grep -c "ProgressRing\|pct" components/kid/day-fill/StepCoinHeader.tsx` returns 0.
    - `grep -c "kid-bottom-sheet" components/kid/day-fill/BottomSheet.tsx` returns 1 and it appears in
      a `!reduced` conditional.
    - `grep -c "onClose" components/kid/day-fill/BottomSheet.tsx` shows exactly three call sites
      (scrim onClick, X button onClick, doneLabel button onClick) plus the prop declaration.
    - `grep -c "kid-dot-fill" components/kid/day-fill/DotTrack.tsx` returns 1.
    - `npx tsc --noEmit`, `npm run lint`, and `npm run build` all exit 0.
  </acceptance_criteria>
  <done>Four stateless primitives exist with the exact interfaces plans 02/03 consume, and the build is green.</done>
</task>

<task type="auto">
  <name>Task 3: i18n keys for both new fill styles and the ProfileSheet picker</name>
  <files>public/locales/ru.json, public/locales/en.json</files>
  <read_first>
    - public/locales/ru.json around lines 1222-1260 (the `kidFillForm` object — match its flat key
      style and the position of `stickyEarnedToday` / `ringLabel` / `locked`)
    - public/locales/ru.json and public/locales/en.json around the `kidProfile` object (en.json line
      ~1961) where `fillStyleActive` currently lives
    - lib/i18n.tsx lines 13-40 (`getNestedValue` splits on '.', so an arbitrarily nested object such
      as `kidProfile.fillStyleOption.tileSheet` resolves correctly; `{{var}}` interpolation is a
      plain regex replace)
    - .planning/phases/09.4-dayform-styles/09.4-UI-SPEC.md section "Copywriting Contract"
  </read_first>
  <action>
    Add to the `kidFillForm` object in BOTH locale files (ru value / en value):
    tileSheetSub = "Тапни плитку — заполни за пару секунд" / "Tap a tile — fill it in a few seconds";
    tileNotFilled = "Не заполнено" / "Not filled";
    tileDone = "Готово ✓" / "Done ✓";
    sheetDone = "Готово" / "Done";
    sheetClose = "Закрыть" / "Close";
    stepperSub = "Шаг за шагом — заполнил, и сразу дальше" / "Step by step — fill it, move on";
    stepSkip = "Пропустить →" / "Skip →";
    stepNext = "Дальше →" / "Next →";
    stepDoneToast = "Готово! ✓" / "Done! ✓";
    stepSummaryTitle = "Всё готово!" / "All done!";
    stepSummaryBody = "Проверь и сохрани — можно вернуться к любому шагу" / "Check it over and save — you can revisit any step";
    stepDotLabel = "Шаг {{n}}: {{label}}" / "Step {{n}}: {{label}}".

    Add to the `kidProfile` object in BOTH locale files:
    fillStyleHeading = "Стиль заполнения дня" / "Day fill style";
    fillStyleError = "Не удалось сохранить стиль" / "Could not save the style";
    and a nested `fillStyleOption` object with stickySummary = "Сводка" / "Summary",
    tileSheet = "Плитки" / "Tiles", storyStepper = "Степпер" / "Stepper".

    `kidFillForm.stepNext` is an addition beyond the UI-SPEC copy table, taken under CONTEXT.md's
    explicit discretion over "exact wording/visual of the skip control": a multi-item step whose
    checklist the child has actually filled must not advertise itself as "Пропустить". The nav control
    swaps between stepSkip (nothing entered) and stepNext (something entered) — both behave
    identically and write nothing (D-11).

    Do NOT remove or repoint `kidProfile.fillStyleActive` in this plan — `ProfileSheet.tsx` still
    renders it until plan 04 replaces the button; deleting the key now would render the raw key string
    in the kid's profile for the duration of plans 02-03. Plan 04 retires it.
    Keep both files byte-comparable in structure: every key added to ru.json must be added at the same
    position in en.json.
  </action>
  <verify>
    <automated>node -e "const r=require('./public/locales/ru.json'),e=require('./public/locales/en.json');const k=['tileSheetSub','tileNotFilled','tileDone','sheetDone','sheetClose','stepperSub','stepSkip','stepNext','stepDoneToast','stepSummaryTitle','stepSummaryBody','stepDotLabel'];const miss=[...k.filter(x=>!r.kidFillForm[x]||!e.kidFillForm[x])];const p=['fillStyleHeading','fillStyleError'].filter(x=>!r.kidProfile[x]||!e.kidProfile[x]);const o=['stickySummary','tileSheet','storyStepper'].filter(x=>!r.kidProfile.fillStyleOption?.[x]||!e.kidProfile.fillStyleOption?.[x]);if(miss.length||p.length||o.length){console.error('MISSING',miss,p,o);process.exit(1)}console.log('all 17 keys present in ru+en')"</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The node assertion above prints "all 17 keys present in ru+en" and exits 0.
    - `node -e "JSON.parse(require('fs').readFileSync('public/locales/ru.json','utf8'))"` and the same
      for en.json both exit 0 (valid JSON, no trailing comma).
    - `grep -c "fillStyleActive" public/locales/ru.json public/locales/en.json` still returns 1 for
      each file (the Phase 9.3 key is untouched by this plan).
    - `node -e "const r=require('./public/locales/ru.json');if(r.kidFillForm.stepDotLabel.indexOf('{{n}}')<0||r.kidFillForm.stepDotLabel.indexOf('{{label}}')<0)process.exit(1)"` exits 0.
  </acceptance_criteria>
  <done>All 17 new strings exist in both locales with matching structure and no hardcoded copy left for plans 02-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child browser → kid Day screen DOM | The new sheet/tile/dot primitives render inside the authenticated kid session; they receive already-computed preview values as props |
| locale JSON → rendered UI | Translation strings are interpolated into the DOM via React text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-094-01 | Tampering | BottomSheet / TileCard / DotTrack / StepCoinHeader | mitigate | All four are stateless presentation-only components with no Supabase import, no fetch, and no write callback — they cannot become a client-side money write path. Enforced by acceptance grep: no `supabase`, no `fetch`, no `/api/` string in any of the four files. |
| T-094-02 | Information Disclosure | StepCoinHeader / TileCard coin display | accept | Coin values displayed are the same client-side `sectionCoins`/`coinsPreview` estimate already shown by the shipped sticky-summary style to the same authenticated child; no new data is exposed and `/api/wallet/award` remains the sole crediting authority. |
| T-094-03 | Denial of Service | `.kid-bottom-sheet` animation | mitigate | Animation is confined to a `prefers-reduced-motion: no-preference` media block plus a post-mount JS class gate, so a motion-sensitive user never receives it (the accessibility failure mode the 05.3-02 double-guard idiom exists to prevent). |
| T-094-04 | Tampering | locale JSON edits | mitigate | Keys are added, none removed or repointed; `fillStyleActive` is explicitly preserved so no in-flight screen renders a raw key. JSON validity asserted by a parse check in acceptance criteria. |
| T-094-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — every primitive is built from existing React + `K` tokens. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
1. `npx tsc --noEmit`, `npm run lint`, `npm run build` all exit 0.
2. The 17-key locale assertion node script exits 0.
3. `grep -rn "supabase\|fetch(\|/api/" components/kid/day-fill/BottomSheet.tsx components/kid/day-fill/TileCard.tsx components/kid/day-fill/DotTrack.tsx components/kid/day-fill/StepCoinHeader.tsx` returns nothing.
4. No `.kid-bottom-sheet` animation declaration exists outside a reduced-motion media block.
</verification>

<success_criteria>
- `BottomSheet`, `TileCard`, `DotTrack`, `StepCoinHeader` exist with exactly the interfaces declared
  in this plan's `<interfaces>` block, are stateless, and import colour only from `K`.
- The sheet slide-up, tile press, and dot fill transitions are reduced-motion double-guarded.
- All 17 new strings exist in both `ru.json` and `en.json`; `kidProfile.fillStyleActive` still exists.
- Typecheck, lint, and build are green.
</success_criteria>

<output>
Create `.planning/phases/09.4-dayform-styles/09.4-01-SUMMARY.md` when done.
</output>
