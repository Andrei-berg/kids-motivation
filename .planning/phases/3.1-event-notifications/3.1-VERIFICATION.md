---
phase: 3.1-event-notifications
verified: 2026-04-13T13:00:00Z
status: human_needed
score: 9/9 must-haves verified
re_verification: false
human_verification:
  - test: "Child receives push on purchase approval"
    expected: "After parent approves a shop purchase in /parent/dashboard, the child's browser (if subscribed) receives a push notification with reward title and icon, tapping opens /kid/wallet"
    why_human: "Cannot invoke web push delivery from grep — requires live Supabase session, registered push subscription, and browser notification rendering"
  - test: "Child receives push on badge award"
    expected: "After saving a day that triggers a badge (e.g. first 5-grade), child receives a push with badge icon and title, tapping opens /kid/achievements"
    why_human: "Requires live day-save flow with badge trigger condition; cannot simulate push delivery in CI"
  - test: "Child receives push on coin credit and debit"
    expected: "Any coin operation (grade save, room cleaning, sport, P2P) sends a push with signed delta (e.g. '+5 монет — Пятёрка') and deep-link to /kid/wallet"
    why_human: "End-to-end push delivery requires live subscriptions and running dev server"
  - test: "Medal of the Day end-to-end flow"
    expected: "Parent composes medal (message + coins) in /parent/dashboard, clicks Отправить — success message appears. Child sees medal banner on /kid/day. Child receives push with medal text and coin amount. Second attempt same day shows error 'Медаль уже отправлена сегодня'."
    why_human: "Requires SQL migration (supabase-schema-medal.sql) to be applied in Supabase, then live multi-user test across parent and child sessions"
  - test: "Medal SQL migration applied"
    expected: "supabase-schema-medal.sql is run in Supabase SQL Editor; medals table exists with RLS preventing non-family access"
    why_human: "Cannot verify remote Supabase schema from local codebase — requires manual SQL execution"
---

# Phase 3.1: event-notifications Verification Report

**Phase Goal:** Children and parents receive timely push notifications for every meaningful event — task confirmed, badge earned, wallet credited, Medal of the Day sent
**Verified:** 2026-04-13T13:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | Child receives a push notification when parent confirms a task (shop purchase approval) | ? UNCERTAIN | `approvePurchase` in wallet.repo.ts:556 calls `notifyChild(purchase.child_id, ..., '/kid/wallet')` via dynamic import after status update. Code is substantive and wired. Push delivery to browser cannot be verified without live subscriptions. |
| 2  | The notification body shows the reward title and coin amount | ? UNCERTAIN | wallet.repo.ts:559–561 passes `purchase.reward_title` and reward icon as title/body. Correct per plan spec. Cannot verify rendered push payload without live browser. |
| 3  | Tapping the purchase notification opens /kid/wallet | ? UNCERTAIN | URL `/kid/wallet` hardcoded at wallet.repo.ts:561. Code correct; browser tap behavior needs human verification. |
| 4  | Child receives a push notification when a badge is awarded | ? UNCERTAIN | badges.service.ts:359–365 calls `notifyChild(childId, ..., '/kid/achievements')` after XP update. Code wired and substantive. |
| 5  | Child receives a push notification when coins are credited or deducted | ? UNCERTAIN | wallet.repo.ts:78–87 calls `notifyChild` inside `updateWalletCoins` with signed delta format. Central hook covers all coin operations. |
| 6  | Tapping badge notification opens /kid/achievements | ? UNCERTAIN | URL `/kid/achievements` at badges.service.ts:364. Code correct; browser behavior needs human verification. |
| 7  | Parent can compose a Medal of the Day with message and bonus coins | ✓ VERIFIED | Parent dashboard page.tsx:453–487 renders medal compose form with textarea (maxLength=200), coin input (0-100), and Отправить button per child card. State maps keyed by child.id. |
| 8  | Submitting medal saves to DB, credits coins, and sends push | ✓ VERIFIED | send-medal.ts:20–78: validates inputs, checks one-per-day via maybeSingle(), inserts to medals table, calls updateWalletCoins if coins>0, calls notifyChild with message text and coin amount to '/kid/day'. |
| 9  | Child sees the Medal of the Day on their /kid/day page | ✓ VERIFIED | kid/day/page.tsx:117,176,188–195: `medal` state loaded in parallel via Promise.all, queried from medals table by child_id+date, rendered as yellow banner at page.tsx:393–406 when medal !== null. |

**Score:** 9/9 truths coded (7 pending human verification for push delivery)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/actions/push-notifications.ts` | Shared notifyChild helper | ✓ VERIFIED | 53 lines. Exports `notifyChild(childId, title, body, url)`. Resolves child_id → member_id via family_members, queries push_subscriptions, calls sendPushToSubscription via Promise.allSettled. Error wrapped in try/catch with console.warn. |
| `lib/repositories/wallet.repo.ts` | approvePurchase + updateWalletCoins with push triggers | ✓ VERIFIED | Line 78: notifyChild in updateWalletCoins after createTransaction. Line 556: notifyChild in approvePurchase after status update. Both use dynamic import pattern. |
| `lib/services/badges.service.ts` | awardBadge with push trigger | ✓ VERIFIED | Line 359: notifyChild called after XP update block in awardBadge function. Dynamic import pattern. |
| `supabase-schema-medal.sql` | medals table DDL + RLS | ✓ VERIFIED | 31 lines. CREATE TABLE medals with family_id (UUID), child_id (TEXT), date, message, coins, sent_by. ENABLE ROW LEVEL SECURITY. Two policies: family read, parent-only insert. Requires manual application in Supabase SQL Editor. |
| `app/actions/send-medal.ts` | sendMedal server action | ✓ VERIFIED | 79 lines. Exports sendMedal(params). Validates message (1-200 chars), coins (0-100, integer). One-per-day guard via maybeSingle(). Inserts medal, credits coins, sends push. Returns SendMedalResult. |
| `app/parent/dashboard/page.tsx` | Medal compose form per child | ✓ VERIFIED | Imports sendMedal. Five state maps keyed by child.id (medalMessage, medalCoins, medalSending, medalSent, medalError). familyId fetched from family_members on load. handleSendMedal calls sendMedal and updates state. Medal UI per child card. |
| `app/kid/day/page.tsx` | Medal display on kid day view | ✓ VERIFIED | medal state, parallel fetch from medals table by child_id+date, yellow banner rendered conditionally with message, coins text, and sent_by attribution. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| wallet.repo.ts approvePurchase | push-notifications.ts notifyChild | dynamic import after status update | ✓ WIRED | wallet.repo.ts:554–564: `const { notifyChild } = await import('@/app/actions/push-notifications')` + `await notifyChild(purchase.child_id, ...)` |
| wallet.repo.ts updateWalletCoins | push-notifications.ts notifyChild | dynamic import after createTransaction | ✓ WIRED | wallet.repo.ts:77–88: dynamic import pattern, notifyChild(childId, ...) |
| badges.service.ts awardBadge | push-notifications.ts notifyChild | dynamic import after DB writes | ✓ WIRED | badges.service.ts:358–366: dynamic import pattern, notifyChild(childId, badge.icon + badge.title, badge.description, '/kid/achievements') |
| app/parent/dashboard/page.tsx | app/actions/send-medal.ts sendMedal | form submit handler | ✓ WIRED | page.tsx:19: `import { sendMedal }`. page.tsx:384: `const result = await sendMedal({childId, familyId, message, coins})` in handleSendMedal |
| app/actions/send-medal.ts | wallet.repo.ts updateWalletCoins | import and call after medals insert | ✓ WIRED | send-medal.ts:4: `import { updateWalletCoins }`. send-medal.ts:58: `await updateWalletCoins(childId, coins, ...)` |
| app/actions/send-medal.ts | push-notifications.ts notifyChild | import and call after coin credit | ✓ WIRED | send-medal.ts:5: `import { notifyChild }`. send-medal.ts:68: `await notifyChild(childId, '🏅 Медаль дня!', message+coinText, '/kid/day')` |
| app/kid/day/page.tsx | medals table | Supabase query by child_id + date | ✓ WIRED | page.tsx:188–194: dynamic import of supabase/client, query `.from('medals').select(...).eq('child_id', resolvedId).eq('date', today).maybeSingle()` |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| NOTIF-01 | 3.1-01 | Child receives push when parent confirms a task | ✓ SATISFIED | approvePurchase in wallet.repo.ts:554–564 calls notifyChild after purchase status set to 'approved' |
| NOTIF-02 | 3.1-02 | Child receives push when earning a badge | ✓ SATISFIED | awardBadge in badges.service.ts:358–366 calls notifyChild after XP update with badge icon/title/description |
| NOTIF-03 | 3.1-02 | Child receives push when coins credited or deducted | ✓ SATISFIED | updateWalletCoins in wallet.repo.ts:77–88 calls notifyChild with signed delta — covers all coin operations centrally |
| NOTIF-04 | 3.1-03 | Parent can send Medal of the Day; child receives push | ✓ SATISFIED | send-medal.ts with validation, one-per-day guard, coin credit, push notification. Parent UI in dashboard. Child display on /kid/day |

No orphaned requirements — all four NOTIF-01 through NOTIF-04 are claimed by plans and implemented.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | — | — | — | — |

No TODOs, FIXMEs, placeholder returns, or empty implementations found in the affected files.

### TypeScript Compilation

`npx tsc --noEmit` exits with code 0 — no type errors introduced by this phase.

### Commits Verified

All commits documented in SUMMARYs exist in git log:
- `e121573` feat(3.1-01): create shared notifyChild push notification helper
- `0280119` feat(3.1-01): hook notifyChild into approvePurchase
- `e5991b7` feat(3.1-02): push notification on badge award
- `d77742f` feat(3.1-02): push notification on coin credit/debit
- `7739bda` feat(3.1-03): add medals table migration and sendMedal server action
- `3a3d3b5` feat(3.1-03): add medal compose UI in parent dashboard and medal display in kid day

### Human Verification Required

#### 1. Push notification delivery (NOTIF-01 through NOTIF-04)

**Test:** With a child device subscribed to push notifications, perform each triggering action: approve a shop purchase, save a day that earns a badge, save a day with grades (coin change), send a medal from the parent dashboard.
**Expected:** Each action delivers a push notification to the child's browser with correct title and body text. Tapping each notification navigates to the correct URL (/kid/wallet for purchase/coins, /kid/achievements for badges, /kid/day for medal).
**Why human:** Web Push delivery requires a live Supabase instance with real push_subscriptions rows, VAPID keys, and a browser to receive and render the notification. Cannot be verified by code analysis or TypeScript alone.

#### 2. Medal of the Day SQL migration

**Test:** Open Supabase SQL Editor, run `supabase-schema-medal.sql`. Verify medals table is created with correct columns and RLS policies.
**Expected:** Table created without errors. A child user cannot insert medals (RLS blocks). A parent user can insert. Both can select medals belonging to their family.
**Why human:** Cannot verify that the migration has been applied to the remote Supabase project from local file inspection.

#### 3. Medal one-per-day guard

**Test:** Send a medal to a child. Then attempt to send another medal to the same child on the same day.
**Expected:** First send succeeds ("Медаль отправлена!"). Second send shows error "Медаль уже отправлена сегодня".
**Why human:** Requires live database state with the medals table applied.

### Gaps Summary

No functional gaps in the code. All must-have artifacts exist, are substantive (real logic, not stubs), and are wired via confirmed import+call chains. TypeScript compiles cleanly. REQUIREMENTS.md marks all four NOTIF requirements as complete.

The only open items are human verification of push delivery behavior in a live browser environment and the manual SQL migration step — neither is a code gap. The SUMMARY for Plan 03 notes that Task 3 was a human checkpoint and was approved, but that approval predates this automated verification and cannot be independently confirmed here without a live environment.

---

_Verified: 2026-04-13T13:00:00Z_
_Verifier: Claude (gsd-verifier)_
