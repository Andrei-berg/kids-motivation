---
phase: 02.2-parent-center
plan: "05"
type: execute
wave: 2
depends_on:
  - "02.2-01"
files_modified:
  - app/parent/shop/page.tsx
  - lib/repositories/wallet.repo.ts
autonomous: true
requirements:
  - REQ-PARENT-005
  - REQ-SHOP-001
  - REQ-SHOP-002
  - REQ-SHOP-005
  - REQ-SHOP-006

must_haves:
  truths:
    - "Parent opens /parent/shop and sees a list of all shop items (rewards) for the family"
    - "Parent can create a new shop item with: title, icon, type (virtual/material/experience/money), price in coins, auto-approve toggle"
    - "Parent can edit an existing item (title, icon, price, active status, auto-approve)"
    - "Parent can delete a shop item with a confirmation step"
    - "Parent can load 5 starter templates with one click (e.g. Кино, Игра на телефоне 1 час)"
    - "Auto-approve toggle is visible per item and saveable"
  artifacts:
    - path: "app/parent/shop/page.tsx"
      provides: "Shop catalogue CRUD: list, create, edit, delete, starter templates, auto-approve"
      min_lines: 140
  key_links:
    - from: "app/parent/shop/page.tsx"
      to: "lib/repositories/wallet.repo (getRewards)"
      via: "load all rewards for the family on mount"
    - from: "app/parent/shop/page.tsx"
      to: "lib/repositories/wallet.repo (addReward)"
      via: "create form submit"
    - from: "app/parent/shop/page.tsx"
      to: "lib/repositories/wallet.repo (updateReward)"
      via: "edit form submit + auto-approve toggle"
    - from: "app/parent/shop/page.tsx"
      to: "lib/repositories/wallet.repo (deleteReward)"
      via: "delete button with confirmation"
---

<objective>
Create /parent/shop — the shop catalogue management page. Parents can create, edit, and delete shop items (stored as `rewards` in DB), toggle auto-approval, and load starter templates. All CRUD operations use existing wallet.repo.ts functions — no new Supabase queries needed.

Purpose: Parents must configure the shop before children can browse it (Phase 2.3 depends on this catalogue).
Output: app/parent/shop/page.tsx (fully functional shop CRUD)
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/02.2-parent-center/02.2-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Use these directly. -->

From lib/models/wallet.types.ts:
```typescript
export interface Reward {
  id: string
  title: string
  description: string | null
  icon: string
  reward_type: 'coins' | 'money'
  price_coins: number | null
  price_money: number | null
  is_active: boolean
  for_child: string | null   // null = available to all children
  category: string           // 'virtual' | 'material' | 'experience' | 'money'
  created_by: string
  created_at: string
  purchase_count: number
}
```

From lib/repositories/wallet.repo.ts:
```typescript
export async function getRewards(filters?: {
  childId?: string
  rewardType?: 'coins' | 'money'
  isActive?: boolean
}): Promise<Reward[]>

export async function addReward(reward: Partial<Reward>): Promise<Reward>
// Required fields: title, icon, reward_type ('coins'), price_coins, category
// is_active defaults to true

export async function updateReward(rewardId: string, updates: Partial<Reward>): Promise<Reward>

export async function deleteReward(rewardId: string): Promise<void>
```

NOTE: The `rewards` table does NOT have an `auto_approve` column in the current schema.
To support auto-approve (REQ-SHOP-005), add it via `updateReward` after adding the column.
If the column doesn't exist yet, store auto_approve preference in a local `metadata` JSON field
or skip the DB column and add a migration note in the SUMMARY. The UI toggle should exist
regardless — the executor must check if `auto_approve` column exists in the rewards table
by looking at the Reward type; if absent, add it to the Partial<Reward> call and note in SUMMARY
that a DB migration `ALTER TABLE rewards ADD COLUMN IF NOT EXISTS auto_approve BOOLEAN DEFAULT false;`
is needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /parent/shop — item list + create/edit form</name>
  <files>app/parent/shop/page.tsx</files>
  <action>
Create `app/parent/shop/page.tsx` (mark `'use client'`). No NavBar — layout.tsx provides navigation.

**Page structure:**
- Title: `<h1 className="text-2xl font-bold text-white mb-4">Магазин</h1>`
- "Добавить позицию" button (top right): `bg-indigo-600 text-white rounded-lg px-4 py-2 text-sm`
- "Загрузить шаблоны" button next to it: `bg-gray-700 text-gray-200 rounded-lg px-4 py-2 text-sm`

**Item list:**
- Load with `getRewards()` (no filters — show all family rewards)
- Each item card (`bg-gray-800 rounded-xl p-4 flex items-center justify-between`):
  - Left: icon + title + price badge (`💰 {price_coins} монет`) + category chip
  - Center: `is_active` toggle pill (green/gray) — calls `updateReward(id, { is_active: !item.is_active })`
  - Right: Edit button (pencil icon) + Delete button (trash icon, red)
- Skeleton loading: 3 placeholder cards with `animate-pulse`
- Empty state: `"Магазин пуст. Добавьте первую позицию или загрузите шаблоны."`

**Create/Edit form** (inline panel or slide-in, shown when "Добавить позицию" or Edit clicked):
- Use a `showForm` boolean state + `editingReward: Reward | null` state
- Form fields:
  - Иконка: text input (emoji picker not needed — just type emoji directly), default `🎁`
  - Название: text input, required, max 60 chars
  - Описание: textarea, optional
  - Тип: `<select>` with options: `virtual` → "Виртуальный", `material` → "Материальный", `experience` → "Опыт", `money` → "За деньги"
  - Цена (монеты): number input, min=1
  - Для кого: `<select>` with "Для всех" (null) + each child by name
  - Авто-одобрение: checkbox — `<label>Авто-одобрение (не требует подтверждения)</label>` — passes `auto_approve: true/false` to `addReward` / `updateReward`
  - Активен: checkbox (for edit mode)
- Submit calls `addReward(formData)` for new, `updateReward(id, formData)` for existing
- On success: close form, reload rewards list
- Cancel button closes form without saving
- Form panel style: `bg-gray-900 rounded-xl p-5 border border-gray-700 mt-4`

**Delete flow:**
- Delete button sets `deletingId` state
- Shows inline confirmation: `"Удалить {title}?" [Да] [Отмена]`
- On confirm: calls `deleteReward(id)`, reloads list
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "parent/shop" || echo "no type errors in shop page"</automated>
  </verify>
  <done>
    - Item list renders all family rewards with is_active toggle
    - Create form opens, validates, and submits via addReward
    - Edit form pre-populates and submits via updateReward
    - Delete shows confirmation before calling deleteReward
    - TypeScript clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Starter templates + auto-approve migration note</name>
  <files>app/parent/shop/page.tsx, lib/repositories/wallet.repo.ts</files>
  <action>
**Starter templates** (REQ-SHOP-006):

Add a `loadStarterTemplates()` function inside the page component:

```typescript
const STARTER_TEMPLATES = [
  { title: 'Кино', icon: '🎬', category: 'experience', price_coins: 150, description: 'Поход в кинотеатр' },
  { title: 'Игра на телефоне 1 час', icon: '📱', category: 'virtual', price_coins: 50, description: 'Дополнительный час экранного времени' },
  { title: 'Поздний отбой', icon: '🌙', category: 'virtual', price_coins: 80, description: 'Лечь спать на 1 час позже' },
  { title: 'Пицца', icon: '🍕', category: 'material', price_coins: 120, description: 'Заказать пиццу' },
  { title: 'Поездка в парк', icon: '🎡', category: 'experience', price_coins: 200, description: 'Поездка в парк аттракционов' },
]

async function loadStarterTemplates() {
  setLoadingTemplates(true)
  try {
    await Promise.all(
      STARTER_TEMPLATES.map(t =>
        addReward({
          ...t,
          reward_type: 'coins',
          is_active: true,
          for_child: null,
        })
      )
    )
    await reloadRewards()
    setTemplatesLoaded(true)
  } catch (e) {
    setError('Ошибка загрузки шаблонов')
  } finally {
    setLoadingTemplates(false)
  }
}
```

"Загрузить шаблоны" button:
- Shows loading spinner while `loadingTemplates === true`
- After success, shows `"✓ Шаблоны загружены"` and disables the button (`setTemplatesLoaded(true)`)
- Confirm dialog before loading: `"Загрузить 5 стандартных позиций в магазин?"` (use `window.confirm`)

**Auto-approve DB migration:**
In `lib/repositories/wallet.repo.ts`, find the `addReward` and `updateReward` functions. Add `auto_approve` to the fields passed to Supabase (it will be a no-op if the column doesn't exist yet — Supabase ignores unknown fields in upserts when not selected). Add a comment:

```typescript
// NOTE: requires DB migration:
// ALTER TABLE rewards ADD COLUMN IF NOT EXISTS auto_approve BOOLEAN DEFAULT false;
// Run in Supabase SQL Editor before auto-approve takes effect.
```

The UI toggle already works from Task 1. The column migration is documented here but must be run manually by the developer — add to SUMMARY.md as a required DB step.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "parent/shop|wallet.repo" || echo "no type errors"</automated>
  </verify>
  <done>
    - "Загрузить шаблоны" button creates all 5 templates in one click
    - Button disables after successful load to prevent duplicates
    - auto_approve field passed through addReward/updateReward with migration note
    - TypeScript clean
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors in app/parent/shop/
- `npm run build` succeeds
- /parent/shop route loads and shows existing rewards
- Creating a new reward via the form adds it to the list without page reload
</verification>

<success_criteria>
- Parent can create, edit, and delete shop items (REQ-SHOP-001)
- Item types: virtual, material, experience, money (REQ-SHOP-002)
- Auto-approve toggle exists per item (REQ-SHOP-005)
- 5 starter templates loadable with one click (REQ-SHOP-006)
- is_active toggle works inline without opening the edit form
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-parent-center/02.2-05-SUMMARY.md` following the summary template.
</output>
