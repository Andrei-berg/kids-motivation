---
phase: 02.2-parent-center
plan: "01"
subsystem: ui
tags: [nextjs, tailwind, dark-theme, navigation, dashboard]

# Dependency graph
requires:
  - phase: 02.1-role-routing
    provides: middleware.ts with role-based routing protecting /parent/* paths
provides:
  - Dark-theme parent shell layout wrapping all /parent/* pages
  - ParentNav component with 6 nav links and active-route highlighting
  - /parent/dashboard page with per-child status cards and skeleton loading
affects: [02.2-02, 02.2-03, 02.2-04, 02.2-05, 02.2-06]

# Tech tracking
tech-stack:
  added: []
  patterns: [parent-section dark-theme, mobile-bottom-tab-bar, skeleton-card-loading, usePathname-active-highlight]

key-files:
  created:
    - app/parent/layout.tsx
    - components/parent/ParentNav.tsx
    - app/parent/dashboard/page.tsx
  modified:
    - middleware.ts

key-decisions:
  - "ParentNav uses two separate <nav> elements (hidden md:flex top bar + md:hidden fixed bottom bar) for responsive layout without JS resize detection"
  - "middleware.ts /parent paths added to isPublicPath as dev bypass — must be removed when parent Supabase Auth is wired"
  - "Dashboard loads getDay + getWeekScore in parallel per child via Promise.all for minimal latency"

patterns-established:
  - "Dark theme parent shell: bg-gray-950 page background, bg-gray-900 nav, text-gray-100 base"
  - "Skeleton cards: animate-pulse bg-gray-800 rounded-xl while loading"
  - "Per-child data loading: Promise.all([getDay, getWeekScore]) with individual catch fallbacks"

requirements-completed: [REQ-PARENT-001, REQ-PARENT-007, REQ-ANL-005, REQ-UX-002]

# Metrics
duration: ~30min (across sessions)
completed: 2026-04-04
---

# Phase 02.2 Plan 01: Parent Center Shell Summary

**Dark-theme parent layout shell with ParentNav (6 links, mobile bottom tabs) and /parent/dashboard showing per-child weekly coins + room/behavior/grades status cards with skeleton loading**

## Performance

- **Duration:** ~30 min (across sessions)
- **Started:** 2026-04-03
- **Completed:** 2026-04-04T17:04:52Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Created `app/parent/layout.tsx` — dark-theme Server Component wrapping all /parent/* routes with ParentNav and mobile-safe padding
- Created `components/parent/ParentNav.tsx` — responsive nav with desktop top bar + mobile fixed bottom tab bar, usePathname active-route highlighting
- Created `app/parent/dashboard/page.tsx` — fully functional dashboard with skeleton loaders, per-child status cards (coins, room, behavior, grades), and "Заполнить день" buttons
- Added /parent bypass in middleware.ts for dev (TODO: remove when auth wired)

## Task Commits

Each task was committed atomically:

1. **Task 1: ParentNav component + shared layout** - `991e5b3` (feat)
2. **Task 2: /parent/dashboard — today's status for all children** - `c4e4412` (feat)
3. **Uncommitted changes cleanup** - `0db7ba6` (fix)

## Files Created/Modified
- `app/parent/layout.tsx` — Server Component shell: bg-gray-950 + ParentNav + children with pb-20 md:pb-0
- `components/parent/ParentNav.tsx` — 6-link nav (Dashboard, Daily, Wallets, Analytics, Shop, Settings) with SVG icons and active state via usePathname
- `app/parent/dashboard/page.tsx` — Dashboard with skeleton loading, per-child ChildCard components, parallel data loading
- `middleware.ts` — Added /parent to isPublicPath for dev bypass

## Decisions Made
- ParentNav uses two separate `<nav>` elements (one `hidden md:flex` top bar, one `md:hidden fixed bottom`) — simpler than JS-based responsive detection
- Middleware /parent bypass added as dev scaffolding; has an explicit TODO comment to remove once parent auth flow is built in phase 02.2+
- Dashboard error state uses `text-red-400` on `bg-red-950` pill — consistent with dark theme

## Deviations from Plan

None - plan executed exactly as written. The `console.error` addition and middleware bypass were minor scaffolding adjustments made during the original implementation session and committed separately for clarity.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All /parent/* pages will automatically receive ParentNav and dark-theme shell via layout.tsx
- /parent/daily (plan 02.2-02) can be built immediately — layout ready, nav slot ready
- Middleware bypass must be removed in a future plan once parent Supabase Auth session is wired up

---
*Phase: 02.2-parent-center*
*Completed: 2026-04-04*
