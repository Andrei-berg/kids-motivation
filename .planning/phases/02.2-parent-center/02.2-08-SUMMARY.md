---
phase: 02.2-parent-center
plan: "08"
subsystem: ui
tags: [wallet, settings, coach-rating, audit-log, supabase, typescript]

# Dependency graph
requires:
  - phase: 02.2-parent-center
    provides: wallet_settings table, awardCoinsForSport, /parent/settings UI, wallet_transactions audit trail
provides:
  - coins_per_coach_5/4/3/2/1 columns in wallet_settings (via SQL migration)
  - auto_approve column in rewards table (via SQL migration)
  - WalletSettings interface extended with 5 coach rating fields
  - awardCoinsForSport reads configurable values from wallet_settings
  - logSettingsChange() audit trail helper
  - /parent/settings shows coach rating coin rules (5 editable inputs)
  - handleSave logs settings change events to wallet_transactions
affects: [02.3-kid-screen, 02.4-shop-approval, DailyModal coach rating sections]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Configurable penalty values: stored as positive integers in DB, negated at award time"
    - "Audit trail via wallet_transactions insert with transaction_type='settings_change' and coins_change=0"

key-files:
  created:
    - supabase/migrations/02.2-08-coach-rating-auto-approve.sql
  modified:
    - lib/models/wallet.types.ts
    - lib/repositories/wallet.repo.ts
    - lib/wallet-api.ts
    - app/parent/settings/page.tsx

key-decisions:
  - "Coach penalty values stored as positive integers in DB (coins_per_coach_2=3 means -3 coins) — negated in awardCoinsForSport"
  - "Settings change logged to wallet_transactions per child with coins_change=0 — visible in getAuditLog()"

patterns-established:
  - "Audit trail: insert to wallet_transactions with transaction_type='settings_change', coins_change=0"

requirements-completed: [REQ-COIN-001, REQ-COIN-005, REQ-SHOP-005, REQ-SEC-006]

# Metrics
duration: 15min
completed: 2026-04-05
---

# Phase 02.2 Plan 08: Coach Rating Configurable + Audit Log Summary

**Configurable coach rating coin rules in /parent/settings, audit log on save, and DB migration for missing columns (auto_approve on rewards, coins_per_coach_* on wallet_settings)**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-04-05T08:07:19Z
- **Completed:** 2026-04-05T08:22:00Z
- **Tasks:** 2
- **Files modified:** 4 (+ 1 SQL migration created)

## Accomplishments
- SQL migration file adds 5 coach rating columns to wallet_settings and auto_approve to rewards table with IF NOT EXISTS guards
- awardCoinsForSport no longer uses hardcoded SPORT_REWARDS map — reads coins_per_coach_* from wallet_settings
- /parent/settings now shows "Оценка тренера (секции)" subsection with 5 editable inputs for rating 1–5
- Saving settings logs a wallet_transactions record per child (transaction_type='settings_change') visible in the audit journal

## Task Commits

Each task was committed atomically:

1. **Task 1: DB migration + extend WalletSettings type + fix awardCoinsForSport** - `f290ea3` (feat)
2. **Task 2: Coach rating fields in settings form + audit log on save** - `d81e0e2` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `supabase/migrations/02.2-08-coach-rating-auto-approve.sql` - ALTER TABLE migration for wallet_settings (coach columns) and rewards (auto_approve)
- `lib/models/wallet.types.ts` - WalletSettings extended with coins_per_coach_5/4/3/2/1
- `lib/repositories/wallet.repo.ts` - getWalletSettings defaults + awardCoinsForSport reads settings + logSettingsChange() added
- `lib/wallet-api.ts` - logSettingsChange re-exported
- `app/parent/settings/page.tsx` - SettingsForm type + defaultForm + loadSettings + coach UI section + handleSave audit logging

## Decisions Made
- Coach rating 1 and 2 values are stored as positive integers in wallet_settings (e.g., `coins_per_coach_2 = 3`); awardCoinsForSport negates them to get the penalty amount (-3). Consistent with existing grade 3 handling.
- Settings change logged per-child (not globally) using wallet_transactions — this is what getAuditLog() reads, so the entry appears in the per-child journal.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
**DB migration must be applied manually.** Run the following SQL in Supabase SQL Editor or via MCP:

```
supabase/migrations/02.2-08-coach-rating-auto-approve.sql
```

Until this migration is applied:
- Coach rating fields in settings will save to DB but columns don't exist (upsert will fail silently or error)
- auto_approve toggle in shop persists in UI only, not to DB

## Next Phase Readiness
- All 8 plans in phase 02.2 are complete
- Phase 02.3 (kid-screen) is unblocked
- REQ-COIN-001, REQ-COIN-005, REQ-SHOP-005, REQ-SEC-006 are fully satisfied

---
*Phase: 02.2-parent-center*
*Completed: 2026-04-05*
