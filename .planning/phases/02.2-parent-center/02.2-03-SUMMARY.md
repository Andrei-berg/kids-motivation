---
phase: 02.2-parent-center
plan: "03"
subsystem: ui
tags: [wallet, transactions, p2p, exchange, withdrawal, coins, react]

# Dependency graph
requires:
  - phase: 02.2-01
    provides: ParentNav + dark-theme parent layout (app/parent/layout.tsx)
provides:
  - "/parent/wallets page with multi-child wallet management"
  - "Manual bonus/penalty adjustment with reason (REQ-COIN-009, REQ-COIN-010)"
  - "P2P coin transfer between children (REQ-WAL-004)"
  - "Coin-to-money exchange panel (REQ-WAL-005)"
  - "Withdrawal request form + pending/approved list (REQ-WAL-006)"
affects:
  - 02.2-parent-center
  - 02.4-shop-approval

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Promise.all parallel data loading on child tab switch"
    - "Static imports from wallet.repo for all wallet operations"
    - "2-second auto-dismiss success banner via setTimeout"
    - "Collapsible panel with toggle state (exchange section)"

key-files:
  created:
    - app/parent/wallets/page.tsx
  modified: []

key-decisions:
  - "Used actual createP2PTransfer signature (from_child_id, to_child_id, amount, transfer_type) not the plan's simplified interface"
  - "Exchange panel is collapsible by default to reduce visual noise"
  - "P2P panel hidden when family has only 1 child (children.length >= 2 guard)"

patterns-established:
  - "Tab-switch pattern: reset all form state + reload child data on selectedChildId change"
  - "Async action pattern: setSubmitting(true) → try/catch → refresh data → show success/error"

requirements-completed:
  - REQ-PARENT-003
  - REQ-WAL-001
  - REQ-WAL-002
  - REQ-WAL-003
  - REQ-WAL-004
  - REQ-WAL-005
  - REQ-WAL-006
  - REQ-COIN-009
  - REQ-COIN-010
  - REQ-COIN-011

# Metrics
duration: 25min
completed: 2026-04-04
---

# Phase 02.2 Plan 03: Wallets Page Summary

**Multi-child wallet management page at /parent/wallets with coin balance cards, full transaction history, manual bonus/penalty, P2P transfer, coin exchange, and withdrawal request flow using wallet.repo.ts functions**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-04-04T17:15:00Z
- **Completed:** 2026-04-04T17:40:00Z
- **Tasks:** 3
- **Files modified:** 1 (created)

## Accomplishments

- Created `app/parent/wallets/page.tsx` with child tab bar, balance cards (coins + money + 4 stat boxes), skeleton loader, and transaction history list
- Added three action panels: manual bonus/penalty (REQ-COIN-009/010), P2P transfer between siblings (REQ-WAL-004), and collapsible coin exchange with live money preview (REQ-WAL-005)
- Added withdrawal request form with pending (yellow badge) and approved (green badge, last 5) lists that reload on tab switch (REQ-WAL-006)

## Task Commits

Each task was committed atomically:

1. **Task 1: /parent/wallets — balance cards + transaction history** - `8b0db08` (feat)
2. **Task 2: Manual bonus/penalty + P2P transfer + exchange controls** - `f83f603` (feat)
3. **Task 3: Withdrawal request form + pending withdrawals list** - `2c5eedc` (feat)

## Files Created/Modified

- `app/parent/wallets/page.tsx` - Multi-child wallet management page (516 lines); child tabs, balance summary cards, transaction history, manual adjustment, P2P transfer, exchange, and withdrawal panels

## Decisions Made

- Used actual `createP2PTransfer` signature `{ from_child_id, to_child_id, amount, transfer_type }` instead of the plan's simplified `{ fromChildId, toChildId, coins, description }` — the plan's interface spec was inaccurate; the actual repo function has different param names
- Exchange panel is collapsed by default to keep the page tidy; user expands it when needed
- P2P panel is only rendered when `children.length >= 2`, guarding single-child families
- All wallet functions imported statically at top of file (refactored from dynamic imports used in initial draft)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected createP2PTransfer parameter names**
- **Found during:** Task 2 (P2P transfer panel)
- **Issue:** Plan specified `{ fromChildId, toChildId, coins, description }` but actual `wallet.repo.ts` function uses `{ from_child_id, to_child_id, amount, transfer_type, note }`
- **Fix:** Used the actual function signature from `lib/repositories/wallet.repo.ts`
- **Files modified:** app/parent/wallets/page.tsx
- **Verification:** TypeScript compiler accepted the call with no errors
- **Committed in:** f83f603 (Task 2 commit)

**2. [Rule 1 - Bug] Used actual exchangeCoins return type (CoinExchange)**
- **Found during:** Task 2 (exchange panel)
- **Issue:** Plan stated `exchangeCoins` returns `{ coinsDeducted, moneyAdded }` but actual function returns `CoinExchange` object
- **Fix:** Ignored return value (only the side-effect wallet update is needed); refreshed wallet via `getWallet` call after exchange
- **Files modified:** app/parent/wallets/page.tsx
- **Verification:** TypeScript passes, exchange panel submits correctly
- **Committed in:** f83f603 (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (both Rule 1 — incorrect interface specs in plan vs actual repo)
**Impact on plan:** Both fixes required for correctness. No scope changes.

## Issues Encountered

- `npm run build` fails with `ENOENT pages-manifest.json` — this is a pre-existing infrastructure build artifact issue (build fails in mid-step due to environment), NOT caused by this plan. TypeScript compilation succeeds cleanly (`npx tsc --noEmit` passes).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `/parent/wallets` is ready for parent use: all wallet operations wired to `wallet.repo.ts`
- Next plan (02.2-04 `/parent/analytics`) can proceed independently
- Withdrawal approval flow (setting withdrawal to 'approved') is handled by `approveWithdrawal` in wallet.repo — not wired to a UI in this plan (out of scope for 02.2-03)

---
*Phase: 02.2-parent-center*
*Completed: 2026-04-04*
