# Clean MAX v4 - Kids Motivation System

Система мотивации детей Адама и Алима на Next.js + Supabase + Vercel.

## 🚀 Деплой на Vercel

### 1. Подготовка Supabase

1. **Создать проект на [supabase.com](https://supabase.com)**
   - Зарегистрироваться / войти
   - Нажать "New Project"
   - Выбрать организацию
   - Указать имя проекта (например, `kids-motivation`)
   - Выбрать регион (ближайший к вам)
   - Создать сильный пароль для БД
   - Дождаться создания проекта (~2 минуты)

2. **Создать таблицы**
   - Перейти в SQL Editor (левое меню)
   - Открыть файл `supabase-schema.sql` из проекта
   - Скопировать весь SQL код
   - Вставить в редактор и нажать "Run"
   - Дождаться выполнения всех запросов

3. **Получить API ключи**
   - Settings → API
   - Скопировать `Project URL` (это `NEXT_PUBLIC_SUPABASE_URL`)
   - Скопировать `anon public` ключ (это `NEXT_PUBLIC_SUPABASE_ANON_KEY`)

### 2. Подготовка GitHub

1. **Создать репозиторий на GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/kids-motivation.git
   git push -u origin main
   ```

### 3. Деплой на Vercel

1. **Перейти на [vercel.com](https://vercel.com)**
   - Войти через GitHub
   - Нажать "Add New..." → "Project"
   - Выбрать репозиторий `kids-motivation`

2. **Настроить Environment Variables**
   - В разделе "Environment Variables" добавить:
     ```
     NEXT_PUBLIC_SUPABASE_URL = ваш_supabase_url
     NEXT_PUBLIC_SUPABASE_ANON_KEY = ваш_supabase_anon_key
     PARENT_PIN_HASH = MTIzNA== (это хеш от "1234", можно изменить)
     ```

3. **Deploy**
   - Нажать "Deploy"
   - Дождаться сборки (~2-3 минуты)
   - Получить ссылку на приложение (например, `kids-motivation.vercel.app`)

## 💻 Локальная разработка

### Установка зависимостей

```bash
npm install
```

### Настройка переменных окружения

1. Скопировать `.env.local.example` в `.env.local`:
   ```bash
   cp .env.local.example .env.local
   ```

2. Заполнить переменные в `.env.local`:
   ```env
   NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
   PARENT_PIN_HASH=MTIzNA==
   ```

### Запуск dev сервера

```bash
npm run dev
```

Приложение будет доступно на http://localhost:3000

### Сборка для продакшена

```bash
npm run build
npm start
```

## 📂 Структура проекта

```
kids-motivation/
├── app/                  # Next.js App Router
│   ├── globals.css      # Глобальные стили
│   ├── layout.tsx       # Главный layout
│   ├── page.tsx         # Главная страница (редирект на /kid)
│   ├── kid/            # Kid Screen
│   ├── analytics/      # Analytics Screen
│   ├── weekly/         # Weekly Review
│   ├── wallboard/      # Wallboard
│   ├── streaks/        # Super Streaks
│   └── records/        # Records
├── components/          # React компоненты
│   └── NavBar.tsx      # Навигация
├── lib/                # Библиотеки
│   └── supabase.ts     # Supabase клиент
├── utils/              # Утилиты
│   └── helpers.ts      # Функции для дат и расчетов
├── supabase-schema.sql # SQL схема для Supabase
├── package.json        # Зависимости
├── next.config.js      # Next.js конфиг
├── tsconfig.json       # TypeScript конфиг
└── README.md           # Документация
```

## 🔧 Настройка

### Изменение PIN кода

PIN код по умолчанию: `1234`

Чтобы изменить:
1. Выбрать новый PIN (например, `5678`)
2. Закодировать в base64:
   ```bash
   echo -n "5678" | base64
   # Результат: NTY3OA==
   ```
3. Обновить `PARENT_PIN_HASH` в Vercel Environment Variables

### Добавление новых детей

В Supabase SQL Editor:
```sql
INSERT INTO children (id, name, emoji, age, base_weekly) 
VALUES ('new_kid', 'Имя', '👧', 10, 500);
```

### Изменение настроек

В Supabase SQL Editor:
```sql
UPDATE settings SET value = '600' WHERE key = 'baseWeekly';
```

## 📊 Основные функции

- **Kid Screen** - главный экран для детей
- **Analytics** - аналитика и графики
- **Weekly Review** - недельный отчет
- **Wallboard** - общий экран для всей семьи
- **Super Streaks** - серии достижений
- **Records** - рекорды и бейджи
- **Goals** - система целей и накоплений

## 🔒 Безопасность

- Родительский доступ защищен PIN кодом
- RLS (Row Level Security) в Supabase
- Environment variables для секретных ключей
- HTTPS через Vercel

## 📝 Лицензия

MIT - используйте свободно для своей семьи!

## 🆘 Помощь

Если что-то не работает:
1. Проверить логи в Vercel Dashboard
2. Проверить таблицы в Supabase Table Editor
3. Проверить Network tab в браузере (F12)
4. Проверить консоль браузера на ошибки

## 🎯 TODO (будущие улучшения)

- [ ] Мобильная версия (PWA)
- [ ] Push уведомления
- [ ] Экспорт в PDF
- [ ] Темная тема
- [ ] Мультиязычность
- [ ] Интеграция с календарем
